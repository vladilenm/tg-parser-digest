# Dependencies
node_modules/

# Secrets
.env
.env.local

# Logs
*.log
# (data/run-*.log already covered by `data/*` below — listed here for grep-ability.)

# Runtime data archives (raw + output + hash-cache)
data/*
!data/.gitkeep

# OS
.DS_Store

# Editor
.vscode/
.idea/

# TS build artifacts
dist/
build/
*.tsbuildinfo

# Claude Code agent worktrees (local, ephemeral)
.claude/worktrees/
logs.txt