---
phase: quick-260508-eb5
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/web-scraper.ts
  - websites.json
  - scripts/diag-fetch.mjs
  - scripts/diagnose-web-fetches.mjs
autonomous: true
requirements:
  - QUICK-260508-eb5
must_haves:
  truths:
    - "undici Agent форсит IPv4 — Happy Eyeballs больше не висит на v6 SYN-timeout у российских корп-сайтов"
    - "rupec.ru tickается без TLS altname error (URL без www соответствует cert SAN)"
    - "При фейле fetchSite в логе видна err.cause.code и err.cause.message — а не голое 'fetch failed'"
    - "errors[] в WebRunSummary содержит cause-информацию (для итоговых отчётов)"
    - "Диагностический скрипт scripts/diagnose-web-fetches.mjs закоммичен и воспроизводит те же опции undici что и web-scraper"
    - "npx tsc --noEmit проходит"
  artifacts:
    - path: "src/web-scraper.ts"
      provides: "httpDispatcher с connect.family=4; расширенный лог с err.cause"
      contains: "family: 4"
    - path: "websites.json"
      provides: "rupec.ru без www-префикса"
      contains: "https://rupec.ru/news/"
    - path: "scripts/diagnose-web-fetches.mjs"
      provides: "Репродьюсер fetch-фейлов с дампом err.cause"
  key_links:
    - from: "src/web-scraper.ts httpDispatcher"
      to: "fetch() в fetchSite"
      via: "dispatcher option в RequestInit"
      pattern: "dispatcher: httpDispatcher"
    - from: "Promise.allSettled map-catch (строки ~338-343)"
      to: "log.warn + errors[]"
      via: "formatErr(err) helper"
      pattern: "err\\.cause"
---

<objective>
Quick-fix трёх проблем web-scraper, обнаруженных по логам последнего прогона:

1. **7×UND_ERR_CONNECT_TIMEOUT** на российских корп-сайтах (gazprom-neft.ru, gazpromneft-sm.ru, gazpromneft-oil.ru, g-energy.org, bitum.gazprom-neft.ru, tk418.ru, tatneft.ru) — Node Happy Eyeballs пытается IPv6 первым, эти сайты не отвечают на v6, итог 10s timeout. Лечится одной строкой: `family: 4` в существующем undici Agent.

2. **1×ERR_TLS_CERT_ALTNAME_INVALID** на www.rupec.ru — TLS-сертификат содержит SAN только `rupec.ru` (без www). Лечится правкой URL в websites.json.

3. **Слепой лог** — текущий map-catch в runWebPipeline печатает только `(err as Error).message`, который для undici TypeError равен голому `"fetch failed"`. Реальная причина зашита в `err.cause.code` / `err.cause.message`. Расширяем форматирование.

Бонус: orchestrator создал `scripts/diag-fetch.mjs` для разовой диагностики. Имеет долговременную ценность как репродьюсер undici-фейлов с тем же Agent + headers что и web-scraper. Переименовываем в `scripts/diagnose-web-fetches.mjs` и коммитим.

Purpose: Снизить 11 fetch failures до 2-3 (in-scope: 7+1+0 = 8 устранённых; out-of-scope остаётся neftegaz.ru SSL chain и nangs.org cloudflare-403). Логи становятся диагностируемыми.

Output: Один atomic commit `quick-260508-eb5: ...` с правкой web-scraper.ts, websites.json и переименованным diag-скриптом.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@src/web-scraper.ts
@websites.json
@scripts/diag-fetch.mjs

<interfaces>
<!-- Текущее состояние ключевых блоков в src/web-scraper.ts (после quick-260508-cy1). -->
<!-- Executor правит ровно эти места — без раскопок. -->

src/web-scraper.ts:17 — import:
```ts
import { Agent } from "undici";
```

src/web-scraper.ts:19-26 — текущий dispatcher (после quick-260508-cy1):
```ts
// quick-260508-cy1: undici h1-only dispatcher — обходит HTTP/2 frame-parsing и TLS-fingerprint
// проблемы на сайтах с Tengine (neftegaz.ru), Bitrix-стеком и корпоративных WAF.
// Без него native fetch падает с generic "fetch failed" после успешного TCP/TLS handshake.
// curl --http1.1 с тем же UA проходит на тех же URL — корневая причина именно в h2-pipeline undici.
const httpDispatcher = new Agent({
  allowH2: false,
  connect: { timeout: 10_000 },
});
```

src/web-scraper.ts:317-345 — map-задача в runWebPipeline (catch на ~338-343):
```ts
const results = await Promise.allSettled(
  websites.map(async (site, idx) => {
    const mode = site.rss ? "rss" : "html";
    log.info(`[web-scraper] [${idx + 1}/${total}] start: ${site.url} (${mode})`);
    try {
      // ... fetchSite/fetchRssAsPosts ...
      return sitePosts;
    } catch (err) {
      completed++;
      const msg = (err as Error)?.message ?? String(err);
      log.warn(`[web-scraper] [${completed}/${total}] fail: ${site.url} (${mode}) — ${msg}`);
      throw err;
    }
  })
);
```

src/web-scraper.ts:350-358 — final-aggregation, тоже использует `(r.reason as Error).message`:
```ts
if (r.status === "rejected") {
  const msg = (r.reason as Error)?.message ?? String(r.reason);
  errors.push(`${site.url}: ${msg}`);
  websitesSkipped++;
  continue;
}
```

websites.json:5 — целевой entry:
```json
{ "url": "https://www.rupec.ru/news/" },
```

scripts/diag-fetch.mjs (untracked) — orchestrator создал; подлежит rename в `scripts/diagnose-web-fetches.mjs` без изменения содержимого.
</interfaces>

<discovery_summary>
Из orchestrator-диагностики (НЕ перепроверяй):

| Сайт | err.cause.code | Disposition |
|------|----------------|-------------|
| 7 сайтов (gazprom-neft.ru и т.д.) | UND_ERR_CONNECT_TIMEOUT | FIX: family:4 |
| www.rupec.ru | ERR_TLS_CERT_ALTNAME_INVALID | FIX: URL без www |
| neftegaz.ru | UNABLE_TO_VERIFY_LEAF_SIGNATURE | OUT OF SCOPE |
| nangs.org | HTTP 403 cloudflare | OUT OF SCOPE |
| oilcapital.ru | OK 200 (был интермиттент) | NO-OP |

Документация undici Connect.family: https://undici.nodejs.org/#/docs/api/Connector
- `family: 4` — IPv4 only (это и применяем).
- `family: 0` (default) — Happy Eyeballs (текущее, проблемное поведение).
</discovery_summary>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix httpDispatcher (IPv4-only) + расширить error logging + rupec без www + закоммитить diag-скрипт</name>
  <files>src/web-scraper.ts, websites.json, scripts/diag-fetch.mjs, scripts/diagnose-web-fetches.mjs</files>
  <action>
Ровно четыре изменения, в одной задаче (atomic commit):

**1. src/web-scraper.ts — httpDispatcher (строки ~19-26):**

Дополнить `connect` объект полем `family: 4` и расширить существующий quick-260508-cy1 комментарий новым параграфом-блоком в стиле проекта (отдельный quick-260508-eb5 тег). Финальный вид:

```ts
// quick-260508-cy1: undici h1-only dispatcher — обходит HTTP/2 frame-parsing и TLS-fingerprint
// проблемы на сайтах с Tengine (neftegaz.ru), Bitrix-стеком и корпоративных WAF.
// Без него native fetch падает с generic "fetch failed" после успешного TCP/TLS handshake.
// curl --http1.1 с тем же UA проходит на тех же URL — корневая причина именно в h2-pipeline undici.
//
// quick-260508-eb5: connect.family=4 — IPv4-only, обход Node Happy Eyeballs.
// gazprom-neft.ru / gazpromneft-sm.ru / gazpromneft-oil.ru / g-energy.org /
// bitum.gazprom-neft.ru / tk418.ru / tatneft.ru не отвечают на IPv6 SYN, и v6-попытка
// висит до connect-timeout (UND_ERR_CONNECT_TIMEOUT). family:4 пропускает v6-ветку.
const httpDispatcher = new Agent({
  allowH2: false,
  connect: { timeout: 10_000, family: 4 },
});
```

Никакого нового импорта не требуется — `Agent` уже импортирован, тип `connect` принимает `family` (см. undici Connector docs).

**2. src/web-scraper.ts — formatErrCause helper + использование в обоих местах:**

Сразу после блока httpDispatcher (то есть после строки `});` в районе 26) добавить **новый helper**:

```ts
// quick-260508-eb5: форматирование undici-ошибок с раскруткой err.cause.
// Native fetch заворачивает реальную причину в TypeError("fetch failed").cause.
// Без раскрутки в логах видно только "fetch failed" — причина (CONNECT_TIMEOUT, TLS, DNS) теряется.
function formatErrCause(err: unknown): string {
  const e = err as { message?: string; cause?: { code?: string; message?: string } } | null;
  const msg = e?.message ?? String(err);
  const cause = e?.cause;
  if (cause && (cause.code || cause.message)) {
    const parts = [cause.code, cause.message].filter(Boolean).join(" — ");
    return `${msg} (cause: ${parts})`;
  }
  return msg;
}
```

Затем в map-catch'е (строки ~338-343) **заменить** `const msg = (err as Error)?.message ?? String(err);` на:

```ts
const msg = formatErrCause(err);
```

И в final-aggregation (строки ~353-356) **заменить** `const msg = (r.reason as Error)?.message ?? String(r.reason);` на:

```ts
const msg = formatErrCause(r.reason);
```

Так и лог-строка `[web-scraper] [N/M] fail: ...`, и errors[] в WebRunSummary получают одинаково обогащённую причину. Никаких других мест дублировать не надо — других catch'ей с message-форматированием в файле нет.

**3. websites.json — rupec без www (строка 5):**

```json
{ "url": "https://rupec.ru/news/" },
```

(Просто убрать `www.`. Поле `name` отсутствовало и не появляется — channelUsername соберётся из hostname как `rupec.ru`. Никаких других entries не трогаем.)

**4. scripts/diag-fetch.mjs → scripts/diagnose-web-fetches.mjs (rename + commit):**

- Переименовать файл через `git mv scripts/diag-fetch.mjs scripts/diagnose-web-fetches.mjs`.
- Содержимое НЕ менять (там уже корректный undici Agent + headers, идентичные web-scraper.ts; dumpErr() выводит cause.code/message/errors[]).
- В коммите файл уйдёт как новый (был untracked) — `git mv` для untracked-файла эквивалентен mv + add. Если git ругается, выполнить `mv` вручную и потом `git add scripts/diagnose-web-fetches.mjs`.

**Что НЕ делать (out-of-scope, не отклоняться):**
- НЕ трогать fetchSite внутри (try/finally вокруг fetch уже корректный, throw'ит native error с cause — наш formatErrCause раскрутит его уже на уровне map-catch).
- НЕ менять neftegaz.ru / nangs.org / oilcapital.ru — отдельные тикеты.
- НЕ добавлять retry, NODE_OPTIONS, NODE_TLS_REJECT_UNAUTHORIZED — не лечит и небезопасно.
- НЕ создавать unit-тестов (явно вне scope quick-fix).
- НЕ трогать другие entries в websites.json.

**Verify pre-commit:**
- `npx tsc --noEmit` — должен пройти без ошибок (новое поле `family` в `connect` поддерживается типами undici; helper типизирован через unknown→narrow).
- `git status` — должно показать modified: `src/web-scraper.ts`, `websites.json`; renamed: `scripts/diag-fetch.mjs` → `scripts/diagnose-web-fetches.mjs`.
- (опционально, не блокирует) `node scripts/diagnose-web-fetches.mjs` — должен показать что 7 ранее тайм-аутивших сайтов теперь либо OK, либо имеют другую причину (для регрессии скрипт надо обновить чтобы он тоже использовал family:4 — но это можно НЕ делать, скрипт остаётся репродьюсером старого поведения для сравнения; так логичнее).

**Commit:**
Один atomic commit с сообщением:

```
quick-260508-eb5: IPv4-only undici Agent + raw rupec.ru + log err.cause

- httpDispatcher: connect.family=4 — обход Happy Eyeballs IPv6 SYN-timeout
  на gazprom-neft.ru / gazpromneft-* / g-energy / bitum / tk418 / tatneft
  (UND_ERR_CONNECT_TIMEOUT исчезает).
- websites.json: rupec.ru без www-префикса — TLS SAN не содержит www
  (ERR_TLS_CERT_ALTNAME_INVALID).
- formatErrCause() helper: раскрутка err.cause.code/message в логе
  fetch-фейлов и в WebRunSummary.errors[] — больше не голое "fetch failed".
- scripts/diag-fetch.mjs → scripts/diagnose-web-fetches.mjs:
  переименование untracked-репродьюсера в постоянный диагностический tool.
```

(Co-Authored-By trailer добавлять не нужно — это локальный quick-fix workflow.)
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>
- src/web-scraper.ts:25 содержит `family: 4` внутри connect-объекта.
- src/web-scraper.ts содержит export-less helper `function formatErrCause(err: unknown): string` ниже httpDispatcher.
- map-catch и final-aggregation оба используют `formatErrCause(...)` (никаких прямых `(err as Error)?.message ?? String(err)` для fetch-ошибок не остаётся).
- websites.json:5 содержит `"https://rupec.ru/news/"` (без www).
- scripts/diagnose-web-fetches.mjs существует и содержит тот же Agent + headers + dumpErr что был в diag-fetch.mjs.
- scripts/diag-fetch.mjs не существует.
- `npx tsc --noEmit` exit code 0.
- Один git commit с префиксом `quick-260508-eb5:` содержит все четыре изменения.
- `git status` чистый после коммита (untracked diag-скрипта больше нет — он либо в коммите как renamed, либо как added).
  </done>
</task>

</tasks>

<verification>
1. **Type-check:** `npx tsc --noEmit` — exit 0.
2. **Diff inspection:**
   - `git show HEAD --stat` — четыре файла (web-scraper.ts modify, websites.json modify, diag-fetch.mjs delete, diagnose-web-fetches.mjs add ИЛИ один rename entry).
   - `git show HEAD -- src/web-scraper.ts` — присутствует `family: 4` и `formatErrCause`.
   - `git show HEAD -- websites.json` — diff показывает `-www.rupec.ru` / `+rupec.ru`.
3. **Behavioral (опционально, требует сети):** ручной `npm start` — в логах для rupec.ru/gazprom-neft.ru/tk418.ru/tatneft.ru/g-energy.org/gazpromneft-sm.ru/gazpromneft-oil.ru/bitum.gazprom-neft.ru должно появляться `fetch ok` либо `fail: ... (cause: <code> — <message>)`. Голого `fail: ... — fetch failed` без cause быть не должно.
</verification>

<success_criteria>
- Один atomic commit `quick-260508-eb5: ...` существует на ветке main.
- `npx tsc --noEmit` проходит.
- 8 ранее фейлящихся сайтов (7 connect-timeout + 1 TLS altname) теперь либо успешны, либо фейлятся с другой причиной — но эта причина видна в логе (`cause: <code> — <message>`).
- 2 out-of-scope сайта (neftegaz.ru leaf signature, nangs.org cloudflare 403) остаются как есть, но теперь их причина тоже читаема в логе.
- `scripts/diagnose-web-fetches.mjs` закоммичен и пригоден для будущих regression-проверок.
- Никаких новых runtime-зависимостей; никаких новых тестов; никаких изменений вне scope.
</success_criteria>

<output>
After completion, create `.planning/quick/260508-eb5-ipv4-only-undici-agent-rupec-www-log-err/260508-eb5-SUMMARY.md` per template, фиксируя:
- Конкретные строки file:line после правки (httpDispatcher, formatErrCause, обе замены msg-формирования).
- До/после для одного-двух фейлов из лога (если был выполнен пробный `npm start` или прогон scripts/diagnose-web-fetches.mjs).
- Список всё ещё фейлящихся сайтов с их новой (видимой) причиной — чтобы следующие quick'и (neftegaz SSL chain, nangs cloudflare) могли стартовать сразу с диагнозом.
</output>
