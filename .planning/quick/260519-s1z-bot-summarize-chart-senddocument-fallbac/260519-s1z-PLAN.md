---
phase: quick-260519-s1z
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/bot.ts
  - src/__tests__/bot-summarize.test.ts
autonomous: true
requirements:
  - QUICK-260519-s1z
must_haves:
  truths:
    - "При sendPhoto 400 PHOTO_INVALID_DIMENSIONS handleSummarizeCommand вызывает sendDocument с теми же байтами и caption — в чат прилетает chart.png как документ"
    - "Если sendDocument тоже падает — handler НЕ падает: log.warn и тихий exit (narrative уже доставлен)"
    - "На любую другую sendPhoto-ошибку (не PHOTO_INVALID_DIMENSIONS) fallback НЕ срабатывает — existing catch ловит и log.warn, поведение совместимо с p3g"
    - "sendPhotoMultipart не изменена — её existing тесты (multipart upload, FormData boundary) остаются зелёными"
  artifacts:
    - path: "src/bot.ts"
      provides: "sendDocumentMultipart helper + fallback в handleSummarizeCommand chart block (lines 556-570)"
      contains: "sendDocumentMultipart"
    - path: "src/__tests__/bot-summarize.test.ts"
      provides: "Обновлённый PHOTO_INVALID_DIMENSIONS тест — assert на sendDocument fallback вместо silent swallow"
      contains: "sendDocument"
  key_links:
    - from: "src/bot.ts handleSummarizeCommand chart try-block"
      to: "sendDocumentMultipart"
      via: "catch(err) → if /PHOTO_INVALID_DIMENSIONS/i.test(err.message) → sendDocumentMultipart"
      pattern: "PHOTO_INVALID_DIMENSIONS"
    - from: "sendDocumentMultipart"
      to: "Telegram Bot API /sendDocument"
      via: "fetch POST FormData (document field вместо photo)"
      pattern: "/sendDocument"
---

<objective>
После quick-260519-pwy quickchart рисует error-PNG (с текстом ошибки внутри), но Telegram отвергает sendPhoto с 400 PHOTO_INVALID_DIMENSIONS — узкий text-banner не проходит TG photo-ratio. Делаем fallback на sendDocument: тот же PNG отправится как документ (preview Telegram сгенерирует), без жёстких ограничений на размеры.

Purpose: Юзер всё равно увидит причину 400 (текст на error-PNG), даже если photo-rejection. Chart остаётся bonus-feature — narrative-шаг не блокируется.
Output: src/bot.ts с новой функцией sendDocumentMultipart + try/catch fallback внутри chart-блока handleSummarizeCommand; расширенный bot-summarize.test.ts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@src/bot.ts
@src/__tests__/bot-summarize.test.ts
@src/__tests__/bot-handlers.test.ts

<interfaces>
<!-- Существующая функция (не трогаем — copy-pattern для sendDocumentMultipart) -->
src/bot.ts:274-303:
```ts
async function sendPhotoMultipart(
  token: string,
  chatId: number,
  png: Uint8Array,
  caption: string
): Promise<void> {
  const form = new FormData();
  form.append("chat_id", String(chatId));
  form.append("caption", caption);
  const pngCopy = new Uint8Array(new ArrayBuffer(png.byteLength));
  pngCopy.set(png);
  form.append("photo", new Blob([pngCopy], { type: "image/png" }), "chart.png");
  form.append("reply_markup", JSON.stringify(MAIN_KEYBOARD));
  const res = await fetch(`${TG_API}/bot${token}/sendPhoto`, {
    method: "POST",
    body: form,
  });
  if (!res.ok) {
    const responseBody = await res.text();
    throw new Error(
      `[bot] sendPhoto failed: ${res.status} ${responseBody.slice(0, 300)}`
    );
  }
}
```

<!-- Существующий call-site в handleSummarizeCommand (bot.ts:556-570 — нужно расширить try/catch) -->
```ts
try {
  const chartPng = await generateChartPng(result);
  if (chartPng) {
    await sendPhotoMultipart(
      token,
      chatId,
      chartPng,
      "Δ цены и объёмы по НПЗ (top-10)"
    );
  }
} catch (chartErr) {
  log.warn(
    `[bot] /summarize chart failed (narrative was delivered): ${(chartErr as Error).message}`
  );
}
```

<!-- Existing test (bot-summarize.test.ts:413-454) — currently asserts handler НЕ падает на 400.
     После плана: assert на sendDocument fallback (form.get("document") instanceof Blob). -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Добавить sendDocumentMultipart + fallback в handleSummarizeCommand chart-блоке</name>
  <files>src/bot.ts</files>
  <behavior>
    - sendDocumentMultipart(token, chatId, bytes, filename, caption) шлёт POST на /sendDocument с FormData, поле "document" вместо "photo", filename из параметра (chart.png), caption из параметра, reply_markup = MAIN_KEYBOARD. На !res.ok throw'ает с тем же форматом сообщения что и sendPhotoMultipart: "[bot] sendDocument failed: {status} {body.slice(0,300)}".
    - Внутри handleSummarizeCommand chart-блока (bot.ts:556-570): обернуть sendPhotoMultipart в inner try/catch. Если err.message matches /PHOTO_INVALID_DIMENSIONS/i → log.warn("[bot] sendPhoto rejected with PHOTO_INVALID_DIMENSIONS — falling back to sendDocument") + await sendDocumentMultipart(token, chatId, chartPng, "chart.png", "Δ цены и объёмы по НПЗ (top-10)"). На любую другую sendPhoto-ошибку — re-throw (внешний catch уже логирует).
    - Внешний try/catch (тот что есть на 566-570) теперь ловит:
      (а) re-throw'нутую sendPhoto-ошибку non-PHOTO_INVALID_DIMENSIONS — log.warn как сейчас,
      (б) sendDocument-ошибку из fallback'а — тоже log.warn (handler не падает).
    - sendPhotoMultipart НЕ меняется (тесты multipart upload остаются зелёными).
  </behavior>
  <action>
    1. В src/bot.ts добавить функцию `sendDocumentMultipart` сразу после `sendPhotoMultipart` (после строки 303). Скопировать паттерн sendPhotoMultipart:
       - signature: `async function sendDocumentMultipart(token: string, chatId: number, bytes: Uint8Array, filename: string, caption: string): Promise<void>`
       - body: form.append("chat_id", String(chatId)); form.append("caption", caption); затем bytesCopy через `new Uint8Array(new ArrayBuffer(bytes.byteLength))` + set; form.append("document", new Blob([bytesCopy], { type: "image/png" }), filename); form.append("reply_markup", JSON.stringify(MAIN_KEYBOARD));
       - fetch на `${TG_API}/bot${token}/sendDocument`, method POST, body: form (БЕЗ content-type, FormData сам выставит boundary).
       - if (!res.ok) throw new Error(`[bot] sendDocument failed: ${res.status} ${responseBody.slice(0, 300)}`);
       - JSDoc-комментарий: "Fallback для sendPhotoMultipart когда TG отвергает PNG с PHOTO_INVALID_DIMENSIONS (quick-260519-s1z). Используется ТОЛЬКО из handleSummarizeCommand chart-блока — отправляет тот же PNG как документ (TG сгенерирует preview без жёстких ограничений на ratio/dimensions). На !ok — throw, caller ловит во внешнем try/catch."
    2. В `handleSummarizeCommand` (bot.ts:556-570) переписать chart-блок. Структура:
       ```
       try {
         const chartPng = await generateChartPng(result);
         if (chartPng) {
           try {
             await sendPhotoMultipart(token, chatId, chartPng, "Δ цены и объёмы по НПЗ (top-10)");
           } catch (photoErr) {
             const photoMsg = (photoErr as Error).message ?? String(photoErr);
             if (/PHOTO_INVALID_DIMENSIONS/i.test(photoMsg)) {
               log.warn(`[bot] sendPhoto rejected with PHOTO_INVALID_DIMENSIONS — falling back to sendDocument: ${photoMsg}`);
               await sendDocumentMultipart(token, chatId, chartPng, "chart.png", "Δ цены и объёмы по НПЗ (top-10)");
             } else {
               throw photoErr;
             }
           }
         }
       } catch (chartErr) {
         log.warn(
           `[bot] /summarize chart failed (narrative was delivered): ${(chartErr as Error).message}`
         );
       }
       ```
       Комментарий над блоком — расширить существующий с упоминанием quick-260519-s1z и PHOTO_INVALID_DIMENSIONS fallback на sendDocument (error-PNG идёт как файл, юзер видит причину 400 на превью документа).
    3. Не менять sendPhotoMultipart — она остаётся как есть (back-compat для multipart upload тестов в bot-summarize.test.ts).
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/bot-handlers.test.ts src/__tests__/upload-chart.test.ts</automated>
  </verify>
  <done>
    - src/bot.ts экспортирует/содержит функцию sendDocumentMultipart (grep "sendDocumentMultipart" src/bot.ts даёт ≥2 совпадения — definition + call).
    - handleSummarizeCommand chart-блок содержит inner try/catch с регэкспом /PHOTO_INVALID_DIMENSIONS/i.
    - sendPhotoMultipart body не изменён (git diff src/bot.ts показывает изменения только в новой функции + chart-блоке handleSummarizeCommand).
    - npx vitest run src/__tests__/bot-handlers.test.ts src/__tests__/upload-chart.test.ts — все тесты зелёные (sendPhotoMultipart unchanged → существующие тесты multipart upload зелёные).
    - npx tsc --noEmit чисто.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Обновить bot-summarize.test.ts — assert на sendDocument fallback при PHOTO_INVALID_DIMENSIONS</name>
  <files>src/__tests__/bot-summarize.test.ts</files>
  <behavior>
    - Существующий тест "does NOT crash handler when multipart sendPhoto returns 400" (lines 413-454) переписать:
      (а) для url содержащего "/sendPhoto" возвращать 400 с text() = `'{"ok":false,"error_code":400,"description":"Bad Request: PHOTO_INVALID_DIMENSIONS"}'` (как production-ответ TG);
      (б) для url содержащего "/sendDocument" возвращать 200 OK;
      (в) assert handler.resolves.not.toThrow;
      (г) assert sendPhoto вызван 1 раз (multipartCallsTo("sendPhoto").length === 1);
      (д) NEW: assert sendDocument вызван 1 раз — form.get("document") instanceof Blob с type "image/png", form.get("chat_id") === "555", form.get("caption") matches /Δ цены/, blob.size === pngBytes.length;
      (е) assert narrative всё равно доставлен (sendMessage с "LLM narrative" присутствует).
    - Новый тест: "handler NOT crash when both sendPhoto returns PHOTO_INVALID_DIMENSIONS AND sendDocument also returns 400" — оба endpoint'а возвращают 400, handler.resolves.not.toThrow, оба endpoint'а вызваны ровно по 1 разу, narrative доставлен.
    - Новый тест: "sendPhoto на НЕ-PHOTO_INVALID_DIMENSIONS ошибку (e.g., generic 500) НЕ вызывает sendDocument fallback" — sendPhoto endpoint возвращает 500 с body "internal error", handler.resolves.not.toThrow, sendPhoto вызван 1 раз, sendDocument НЕ вызван (multipartCallsTo("sendDocument").length === 0).
    - Helper multipartCallsTo уже существует (lines 84-89) — используем как есть, передавая "sendDocument" как method.
  </behavior>
  <action>
    1. Открыть src/__tests__/bot-summarize.test.ts. Заменить блок "does NOT crash handler when multipart sendPhoto returns 400" (строки 413-454) на:
       - Тот же setup pngBytes + mockedGenerateChartPng.mockResolvedValueOnce(pngBytes).
       - fetchMock.mockImplementation: для "/sendPhoto" → { ok: false, status: 400, text: async () => '{"ok":false,"error_code":400,"description":"Bad Request: PHOTO_INVALID_DIMENSIONS"}' }; для "/sendDocument" → { ok: true, json: async () => ({ ok: true, result: {} }), text: async () => '' }; для остального — default ok:true.
       - await expect(handleCommand(...)).resolves.not.toThrow().
       - expect(multipartCallsTo("sendPhoto").length).toBe(1).
       - NEW: const docBodies = multipartCallsTo("sendDocument"); expect(docBodies.length).toBe(1); const docForm = docBodies[0]; expect(docForm.get("chat_id")).toBe("555"); expect(String(docForm.get("caption"))).toMatch(/Δ цены/); const docBlob = docForm.get("document"); expect(docBlob).toBeInstanceOf(Blob); expect((docBlob as Blob).type).toBe("image/png"); expect((docBlob as Blob).size).toBe(pngBytes.length);
       - assert narrative: expect(fetchCallsTo("sendMessage").some((c) => String(c.text).includes("LLM narrative"))).toBe(true).
       - Rename тест: "falls back to sendDocument when sendPhoto returns PHOTO_INVALID_DIMENSIONS (quick-260519-s1z)".
    2. Добавить новый it-блок ниже: "handler does NOT crash when both sendPhoto AND fallback sendDocument fail":
       - pngBytes + mockedGenerateChartPng.mockResolvedValueOnce(pngBytes).
       - fetchMock.mockImplementation: "/sendPhoto" → 400 PHOTO_INVALID_DIMENSIONS; "/sendDocument" → 400 "doc rejected"; остальное — ok:true.
       - await expect(handleCommand(...)).resolves.not.toThrow().
       - expect(multipartCallsTo("sendPhoto").length).toBe(1); expect(multipartCallsTo("sendDocument").length).toBe(1).
       - Narrative assert as выше.
    3. Добавить новый it-блок: "does NOT fall back to sendDocument on non-PHOTO_INVALID_DIMENSIONS error":
       - pngBytes + mockedGenerateChartPng.mockResolvedValueOnce(pngBytes).
       - fetchMock.mockImplementation: "/sendPhoto" → { ok: false, status: 500, text: async () => 'internal server error' }; остальное — ok:true.
       - await expect(handleCommand(...)).resolves.not.toThrow().
       - expect(multipartCallsTo("sendPhoto").length).toBe(1).
       - expect(multipartCallsTo("sendDocument").length).toBe(0).
       - Narrative assert.
    4. Оставить два предыдущих теста ("sends PNG chart via multipart sendPhoto after narrative when bytes are non-null", "does NOT call sendPhoto when generateChartPng returns null", "does NOT crash handler when generateChartPng throws unexpectedly") как есть — они тестируют sendPhotoMultipart которая не менялась.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/bot-handlers.test.ts src/__tests__/upload-chart.test.ts src/__tests__/bot-summarize.test.ts</automated>
  </verify>
  <done>
    - Тест "falls back to sendDocument when sendPhoto returns PHOTO_INVALID_DIMENSIONS" проходит и assert'ит sendDocument blob.
    - Два новых теста (both-fail, non-PHOTO_INVALID_DIMENSIONS) проходят.
    - Все остальные тесты bot-summarize.test.ts (8 предыдущих) остаются зелёными.
    - Полный run: npx vitest run src/__tests__/bot-handlers.test.ts src/__tests__/upload-chart.test.ts src/__tests__/bot-summarize.test.ts — все passed.
  </done>
</task>

</tasks>

<verification>
1. npx tsc --noEmit — без ошибок типов.
2. npx vitest run src/__tests__/bot-handlers.test.ts src/__tests__/upload-chart.test.ts src/__tests__/bot-summarize.test.ts — все тесты passed.
3. grep "sendDocumentMultipart" src/bot.ts — ≥2 совпадения (definition + call в handleSummarizeCommand fallback).
4. grep "PHOTO_INVALID_DIMENSIONS" src/bot.ts — 1 совпадение (regex в inner catch).
5. git diff src/bot.ts — sendPhotoMultipart body не тронут (изменения только в новой функции + handleSummarizeCommand chart-блоке).
</verification>

<success_criteria>
- При TG response 400 PHOTO_INVALID_DIMENSIONS на sendPhoto bot не падает, делает fallback на sendDocument, который доставляет тот же PNG как файл (юзер видит превью документа с error-текстом quickchart внутри).
- Если sendDocument тоже падает (теоретически) — log.warn, handler завершается успешно, narrative уже доставлен.
- На ошибки sendPhoto не-PHOTO_INVALID_DIMENSIONS (500, network) fallback НЕ срабатывает — поведение совместимо с quick-260519-p3g (existing outer catch + log.warn).
- sendPhotoMultipart не изменена; backward-compat upload-чарта (happy-path) сохранён.
- Никаких новых runtime-зависимостей (FormData/Blob — стандартные lib.dom/Node 20.6+ globals, как в sendPhotoMultipart).
</success_criteria>

<output>
After completion, create `.planning/quick/260519-s1z-bot-summarize-chart-senddocument-fallbac/260519-s1z-SUMMARY.md` per templates/summary.md
</output>
