---
phase: 260520-i5v
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/dashboard.ts
  - src/run.ts
  - scripts/run-once.ts
  - scripts/run-once-web.ts
autonomous: true
requirements:
  - I5V-01
  - I5V-02
  - I5V-03

must_haves:
  truths:
    - "`src/dashboard.ts` экспортирует `buildAndSendDashboard(runId: string): Promise<void>`, который никогда не throw'ит наружу"
    - "`src/run.ts:tick()` после web-pipeline вызывает `await buildAndSendDashboard(runId)` БЕЗ внешнего try/catch — функция сама гасит ошибки"
    - "`scripts/run-once.ts` после успешного `runPipeline()` вызывает `await buildAndSendDashboard(runId)` ДО `process.exit(0)`"
    - "`scripts/run-once-web.ts` после успешного `runWebPipeline()` вызывает `await buildAndSendDashboard(runId)` ДО `process.exit(0)`"
    - "Ручной `npm run start:once` отправляет HTML-файл `dashboard-DD.MM.YYYY.html` в `TG_CHANNEL_ID` (если переменные заданы)"
    - "Ручной `npm run start:once:web` отправляет тот же файл в `TG_CHANNEL_ID`"
    - "Внутри `buildAndSendDashboard` имя файла формируется через `Intl.DateTimeFormat('ru-RU', { timeZone: 'Europe/Moscow', day: '2-digit', month: '2-digit', year: 'numeric' })` → `dashboard-DD.MM.YYYY.html`"
    - "При любой ошибке внутри (build или send) — `log.error` + `sendAlert({ stage: 'dashboard', message, runId, stack })`; функция возвращает void, исключения наружу не пробрасываются"
    - "`mskDateDmy()` исчезает из `src/run.ts` (либо переезжает в `src/dashboard.ts` как локальный helper, либо инлайнится в `buildAndSendDashboard`) — в `src/run.ts` его больше нет"
    - "Существующие тесты (`npm test`) проходят: 2600+ зелёных"
  artifacts:
    - path: "src/dashboard.ts"
      provides: "Экспорт `buildAndSendDashboard(runId)` (плюс существующий `buildDashboard()`)"
      contains: "export async function buildAndSendDashboard"
    - path: "src/run.ts"
      provides: "tick() с упрощённым dashboard-блоком (один await вместо ~25 строк inline try/catch)"
      contains: "await buildAndSendDashboard(runId)"
    - path: "scripts/run-once.ts"
      provides: "Ручной TG-прогон с автоотправкой дашборда"
      contains: "buildAndSendDashboard"
    - path: "scripts/run-once-web.ts"
      provides: "Ручной web-прогон с автоотправкой дашборда"
      contains: "buildAndSendDashboard"
  key_links:
    - from: "src/run.ts:tick()"
      to: "src/dashboard.ts:buildAndSendDashboard"
      via: "ESM import + await после web-pipeline"
      pattern: "buildAndSendDashboard\\(runId\\)"
    - from: "scripts/run-once.ts"
      to: "src/dashboard.ts:buildAndSendDashboard"
      via: "import from '../src/dashboard.js'"
      pattern: "from \"\\.\\./src/dashboard\\.js\""
    - from: "scripts/run-once-web.ts"
      to: "src/dashboard.ts:buildAndSendDashboard"
      via: "import from '../src/dashboard.js'"
      pattern: "from \"\\.\\./src/dashboard\\.js\""
    - from: "src/dashboard.ts:buildAndSendDashboard"
      to: "src/deliver.ts:sendDashboardDocument"
      via: "прямой вызов внутри try-блока"
      pattern: "sendDashboardDocument\\("
    - from: "src/dashboard.ts:buildAndSendDashboard"
      to: "src/alert.ts:sendAlert"
      via: "catch-блок при любой ошибке"
      pattern: "sendAlert\\(\\s*\\{\\s*stage:\\s*[\"']dashboard[\"']"
---

<objective>
Вынести dashboard-блок (build + send + try/catch + sendAlert) из `tick()` в переиспользуемую функцию `buildAndSendDashboard(runId)` в `src/dashboard.ts`. Затем подключить её к ручным скриптам `scripts/run-once.ts` и `scripts/run-once-web.ts`, чтобы ручной прогон тоже отправлял HTML-файл дашборда в канал дайджеста.

Purpose: оператор хочет дёрнуть `npm run start:once` (или `:web`) для дебага и получить тот же артефакт, что в plановом cron-тике — без копипасты dashboard-логики по трём файлам.

Output: один helper в `src/dashboard.ts`, упрощённый tick() в `src/run.ts`, и две короткие строки в каждом из run-once скриптов.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md
@src/dashboard.ts
@src/run.ts
@src/deliver.ts
@src/alert.ts
@scripts/run-once.ts
@scripts/run-once-web.ts

<interfaces>
<!-- Точные сигнатуры существующих функций, которые helper будет использовать. -->
<!-- Executor должен использовать их 1:1, не лазить по коду. -->

From src/dashboard.ts:
```typescript
export async function buildDashboard(): Promise<{ path: string; bytes: number }>;
// Throws on empty volume — caller must catch.
```

From src/deliver.ts:
```typescript
export async function sendDashboardDocument(
  filePath: string,
  fileName: string
): Promise<void>;
// Soft skip (log.warn + return) если TG_BOT_TOKEN / TG_CHANNEL_ID не заданы.
// Throws с HTTP-статусом и телом при !res.ok.
```

From src/alert.ts:
```typescript
export interface AlertPayload {
  stage: string;
  message: string;
  runId: string;
  stack?: string;
}
export async function sendAlert(payload: AlertPayload): Promise<void>;
// Никогда не throw'ит наружу (D-15).
```

From src/logger.ts (используется во всех файлах):
```typescript
export const log: { info(...); warn(...); error(...); };
```

Текущий inline-блок в src/run.ts:tick() (строки 119-144), который нужно вынести:
```typescript
log.info(`[tick] runId=${runId} ─── Dashboard ───`);
try {
  const { path: dashPath, bytes } = await buildDashboard();
  log.info(
    `[tick] runId=${runId} dashboard built: ${dashPath} (${(bytes / 1024).toFixed(1)} KB)`
  );
  const fileName = `dashboard-${mskDateDmy()}.html`;
  await sendDashboardDocument(dashPath, fileName);
} catch (err) {
  const e = err as Error;
  log.error(`[tick] runId=${runId} dashboard failed`, e);
  try {
    await sendAlert({
      stage: "dashboard",
      message: e?.message ?? String(err),
      runId,
      stack: e?.stack,
    });
  } catch (alertErr) {
    log.error("alert send failed", alertErr);
  }
}
```

Текущий `mskDateDmy()` в src/run.ts (строки 21-31) — единственный consumer = dashboard-блок:
```typescript
function mskDateDmy(): string {
  return new Intl.DateTimeFormat("ru-RU", {
    timeZone: "Europe/Moscow",
    day: "2-digit",
    month: "2-digit",
    year: "numeric",
  })
    .format(new Date())
    .replace(/ /g, "")
    .trim();
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Добавить `buildAndSendDashboard(runId)` в src/dashboard.ts</name>
  <files>src/dashboard.ts</files>
  <action>
В конце `src/dashboard.ts` (после `export async function buildDashboard`) добавить:

1. Локальный helper `mskDateDmy()` (копия из `src/run.ts:21-31`) — формат `Intl.DateTimeFormat("ru-RU", { timeZone: "Europe/Moscow", day: "2-digit", month: "2-digit", year: "numeric" }).format(new Date()).replace(/ /g, "").trim()`. Делать его `function`, не `export` — наружу нужен только `buildAndSendDashboard`.

2. Импорты в шапке файла:
   - `import { log } from "./logger.js";`
   - `import { sendAlert } from "./alert.js";`
   - `import { sendDashboardDocument } from "./deliver.js";`

3. Экспорт:
```typescript
/**
 * I5V-01: build dashboard HTML + send как document attachment в TG_CHANNEL_ID.
 * НИКОГДА не throw'ит — внутренний try/catch ловит ошибки build/send и
 * сообщает оператору через sendAlert(stage="dashboard").
 *
 * Вызывается из:
 *   - src/run.ts:tick() после TG+web pipelines (cron-режим, daemon);
 *   - scripts/run-once.ts (`npm run start:once`);
 *   - scripts/run-once-web.ts (`npm run start:once:web`).
 *
 * runId передаётся caller'ом — попадает в sendAlert payload, чтобы
 * оператор мог сматчить алёрт с runId pipeline'а в логах.
 */
export async function buildAndSendDashboard(runId: string): Promise<void> {
  log.info(`[dashboard] runId=${runId} build + send start`);
  try {
    const { path: dashPath, bytes } = await buildDashboard();
    log.info(
      `[dashboard] runId=${runId} built: ${dashPath} (${(bytes / 1024).toFixed(1)} KB)`
    );
    const fileName = `dashboard-${mskDateDmy()}.html`;
    await sendDashboardDocument(dashPath, fileName);
    log.info(`[dashboard] runId=${runId} sent as ${fileName}`);
  } catch (err) {
    const e = err as Error;
    log.error(`[dashboard] runId=${runId} failed`, e);
    try {
      await sendAlert({
        stage: "dashboard",
        message: e?.message ?? String(err),
        runId,
        stack: e?.stack,
      });
    } catch (alertErr) {
      log.error("[dashboard] alert send failed", alertErr);
    }
  }
}
```

Важно: в `catch` ничего не пробрасывать, функция должна вернуть `void` при любом исходе — это контракт, на который полагаются ВСЕ три caller'а (tick() убирает внешний try/catch, run-once.ts не должен exit'ить с кодом 1 из-за упавшего дашборда).

Не трогать существующий `buildDashboard()` и весь модуль выше.
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -n "export async function buildAndSendDashboard" src/dashboard.ts && grep -n "stage: \"dashboard\"" src/dashboard.ts</automated>
  </verify>
  <done>
    - `npx tsc --noEmit` — clean (без warnings про unused imports).
    - В `src/dashboard.ts` есть `export async function buildAndSendDashboard(runId: string): Promise<void>`.
    - Импорты `log`, `sendAlert`, `sendDashboardDocument` присутствуют в шапке файла.
    - Локальный `mskDateDmy()` объявлен (не экспортирован).
    - `buildDashboard()` НЕ изменён (по-прежнему throw'ит при пустом volume).
  </done>
</task>

<task type="auto">
  <name>Task 2: Подключить helper в run.ts (упростить tick) и в оба run-once скрипта</name>
  <files>src/run.ts, scripts/run-once.ts, scripts/run-once-web.ts</files>
  <action>

**src/run.ts:**

1. Удалить функцию `mskDateDmy()` (строки 21-31) целиком — она больше не нужна (переехала в `src/dashboard.ts` как локальный helper).

2. В импортах:
   - Заменить `import { buildDashboard } from "./dashboard.js";` на `import { buildAndSendDashboard } from "./dashboard.js";`.
   - Удалить `import { sendDashboardDocument } from "./deliver.js";` (больше не используется в этом файле).

3. Заменить весь блок dashboard внутри `tick()` (строки 119-144 в текущем файле, секция `// ---- H5X-03/04: Dashboard build + send (must NOT fail tick) ----`) на:

```typescript
// ---- I5V: Dashboard build + send (never throws, см. src/dashboard.ts) ----
log.info(`[tick] runId=${runId} ─── Dashboard ───`);
await buildAndSendDashboard(runId);
```

Никакого внешнего try/catch — функция сама гасит всё. Внутренний `try/catch` блока tick'а (WR-09 catch-all на строке 145) трогать НЕ нужно: он останется страховкой для остальной логики, а buildAndSendDashboard сам по себе наружу ничего не пробросит.

**scripts/run-once.ts:**

После `logRunSummary(summary);` и ДО `process.exit(0);` вставить:
```typescript
  await buildAndSendDashboard(runId);
```

И добавить импорт в шапке:
```typescript
import { buildAndSendDashboard } from "../src/dashboard.js";
```

Структура итогового файла:
```typescript
import { runPipeline } from "../src/pipeline.js";
import { logRunSummary } from "../src/logger.js";
import { buildAndSendDashboard } from "../src/dashboard.js";

try {
  const runId = crypto.randomUUID().slice(0, 8);
  const summary = await runPipeline(runId);
  logRunSummary(summary);
  await buildAndSendDashboard(runId);
  process.exit(0);
} catch (err) {
  console.error("[run-once] pipeline failed:", err);
  process.exit(1);
}
```

**scripts/run-once-web.ts:**

Аналогично. После `logWebRunSummary(webSummary);` (или как там в файле — `logWebRunSummary(summary)` — смотри `src/logger.ts` и текущий код) и ДО `process.exit(0);` вставить `await buildAndSendDashboard(runId);`. Добавить тот же импорт.

Итоговая структура:
```typescript
import { runWebPipeline } from "../src/web-scraper.js";
import { logWebRunSummary } from "../src/logger.js";
import { buildAndSendDashboard } from "../src/dashboard.js";

try {
  const runId = crypto.randomUUID().slice(0, 8);
  const summary = await runWebPipeline(runId);
  logWebRunSummary(summary);
  await buildAndSendDashboard(runId);
  process.exit(0);
} catch (err) {
  console.error("[run-once-web] web pipeline failed:", err);
  process.exit(1);
}
```

Важно: `buildAndSendDashboard` не throw'ит, поэтому он НЕ попадёт в catch и НЕ заэкзитит с кодом 1 даже если, например, TG_CHANNEL_ID не задан — внутри функция вызовет `sendDashboardDocument`, который сделает log.warn + return. Это правильное поведение для ручного дебага.
  </action>
  <verify>
    <automated>npx tsc --noEmit && ! grep -n "mskDateDmy" src/run.ts && ! grep -n "sendDashboardDocument" src/run.ts && grep -n "buildAndSendDashboard" src/run.ts scripts/run-once.ts scripts/run-once-web.ts</automated>
  </verify>
  <done>
    - `npx tsc --noEmit` — clean.
    - В `src/run.ts` нет `mskDateDmy`, нет `sendDashboardDocument`, есть ровно один `await buildAndSendDashboard(runId)` в tick().
    - WR-09 outer try/catch в tick() остался нетронутым (он по-прежнему ловит ошибки jitter/TG/web блоков).
    - В `scripts/run-once.ts` и `scripts/run-once-web.ts` есть импорт `buildAndSendDashboard` из `../src/dashboard.js` и вызов между log*Summary и process.exit(0).
    - В обоих скриптах вызов helper'а стоит ВНУТРИ существующего `try`-блока, чтобы любая (теоретическая) синхронная ошибка импорта/runtime попала в catch с `process.exit(1)` (хотя helper не throw'ит, инвариант try-блока сохраняется).
  </done>
</task>

<task type="auto">
  <name>Task 3: Прогнать тесты и убедиться что регрессий нет</name>
  <files>(no files modified — test run)</files>
  <action>
Запустить `npm test`. Цель: убедиться, что 2600+ тестов остаются зелёными.

Если какой-то тест ломается из-за переноса `mskDateDmy()` или import-цепочки — поправить ТОЛЬКО сломанный тест (обновить путь импорта или мок), НЕ добавлять новые тесты для `buildAndSendDashboard` (это в out-of-scope для quick — helper тривиальный wrapper из 15 строк, его покрытие — manual smoke через `npm run start:once`).

Дополнительный smoke (опционально, если есть `.env` с реальными TG_BOT_TOKEN/TG_CHANNEL_ID): `npm run start:once` должен после `Run summary` вывести лог `[dashboard] runId=... build + send start` → `[dashboard] runId=... sent as dashboard-DD.MM.YYYY.html`. Если переменные не заданы — `[deliver] sendDashboardDocument: TG_BOT_TOKEN или TG_CHANNEL_ID не задан — dashboard skipped` (это OK, soft skip).
  </action>
  <verify>
    <automated>npm test 2>&1 | tail -20</automated>
  </verify>
  <done>
    - `npm test` — все тесты проходят (≥2600 зелёных, 0 fail).
    - `npx tsc --noEmit` — clean (повторная проверка после всех изменений).
    - При smoke (если возможно): `npm run start:once` отправляет дашборд в канал.
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` — 0 errors.
- `npm test` — ≥2600 зелёных тестов, 0 fail.
- `grep -n "buildAndSendDashboard" src/dashboard.ts src/run.ts scripts/run-once.ts scripts/run-once-web.ts` — ровно 4 матча (один export + три call site'а).
- `grep -n "mskDateDmy" src/run.ts` — пусто (функция переехала в dashboard.ts).
- `grep -n "sendDashboardDocument" src/run.ts` — пусто (caller теперь dashboard.ts, не run.ts).
- В `src/run.ts:tick()` секция dashboard состоит из ровно двух строк: `log.info("─── Dashboard ───")` + `await buildAndSendDashboard(runId)`.
</verification>

<success_criteria>
- `buildAndSendDashboard(runId: string): Promise<void>` существует в `src/dashboard.ts` и никогда не throw'ит наружу.
- `tick()` в `src/run.ts` упрощён до одного await; WR-09 catch-all сохранён.
- `npm run start:once` и `npm run start:once:web` отправляют HTML-файл `dashboard-DD.MM.YYYY.html` в `TG_CHANNEL_ID` (если переменные заданы; иначе soft skip без exit 1).
- 2600+ тестов остаются зелёными.
- Никакого дублирования логики build+send+alert между tick(), run-once.ts, run-once-web.ts — единственная реализация в dashboard.ts.
</success_criteria>

<output>
After completion, create `.planning/quick/260520-i5v-reuse-dashboard-send-from-run-once-scrip/260520-i5v-SUMMARY.md` per the standard summary template.
</output>
