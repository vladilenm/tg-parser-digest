---
phase: quick-260519-k6c
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/web-scraper.ts
  - src/__tests__/web-scraper.test.ts
autonomous: false
requirements:
  - QUICK-260519-K6C
must_haves:
  truths:
    - "Когда хотя бы один сайт упал с rejected (network/timeout/HTTP non-2xx/unsafe-redirect/broken-cert), в конце HTML-дайджеста (и в контентном, и в placeholder-пути) присутствует блок «⚠️ Не удалось распарсить (N)»"
    - "Блок перечисляет URL'ы упавших сайтов и краткую причину (то, что уже копится в errors[])"
    - "Когда rejected-сайтов нет (все ok или skipped по пустому контенту), блок НЕ добавляется — никакого «Не удалось распарсить — нет»"
    - "Сайты, которые fulfilled но вернули 0 постов (HTML <200ch / RSS 0 fresh items), в блок НЕ попадают — паритет различия «лёг» vs «жив, но новостей нет»"
    - "URL'ы и reason экранированы через escapeHtml (HTML-инъекция из error message невозможна)"
    - "vitest unit-тесты фиксируют контракт билдера: non-empty → блок, empty → пустая строка, escape работает"
  artifacts:
    - path: "src/web-scraper.ts"
      provides: "новый exported helper buildFailedSitesBlock(failedSites) + интеграция в runWebPipeline для обоих путей доставки"
      contains: "export function buildFailedSitesBlock"
    - path: "src/__tests__/web-scraper.test.ts"
      provides: "describe-блок для buildFailedSitesBlock с 3 кейсами (non-empty / empty / HTML-escape)"
      contains: "buildFailedSitesBlock"
  key_links:
    - from: "runWebPipeline (контентный путь, ~line 521-523)"
      to: "buildFailedSitesBlock"
      via: "finalHtml + блок → sendToChannel + writeOutputWeb"
      pattern: "composeWebDigest.*buildFailedSitesBlock|finalHtml.*\\+"
    - from: "runWebPipeline (placeholder путь, ~line 472-477)"
      to: "buildFailedSitesBlock"
      via: "placeholder + блок → sendToChannel + writeOutputWeb"
      pattern: "buildPlaceholderHtml.*buildFailedSitesBlock|placeholderWithFailed"
    - from: "Promise.allSettled цикл (line ~416-434)"
      to: "failedSites array"
      via: "при r.status === rejected — push {url, reason} наравне с errors[]"
      pattern: "failedSites\\.push|r\\.status === \"rejected\""
---

<objective>
Добавить в конец web-дайджеста (HTML, отправляемый в закрытый Telegram-канал) отдельный блок «⚠️ Не удалось распарсить (N)» с перечнем URL'ов сайтов, к которым не удалось получить доступ. Это улучшит прозрачность для оператора: видно, какие источники сегодня выпали и что чинить.

Purpose: Сейчас оператор не видит, какие именно сайты упали — только агрегатные счётчики (`succeeded`/`total`) в подзаголовке. Если из 33 сайтов на проде 14 fail'ятся, нужно отдельный список с URL'ами для ручной починки (cert / DNS / селектор).

Output:
- Обновлённый `src/web-scraper.ts` с новым exported helper `buildFailedSitesBlock` и его интеграцией в оба пути доставки (контентный и placeholder)
- Новые vitest unit-тесты в `src/__tests__/web-scraper.test.ts`, фиксирующие контракт билдера
- Без изменений public shape `WebRunSummary`, без новых runtime-зависимостей
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md
@src/web-scraper.ts
@src/types.ts
@src/deliver.ts
@src/summarize.ts
@src/__tests__/web-scraper.test.ts
@websites.json

<interfaces>
<!-- Extracted from codebase. Use these directly — no extra exploration needed. -->

From src/summarize.ts:
```typescript
// Уже экспортированный escapeHtml: экранирует &, <, > (кавычки не трогаем — Telegram HTML их не интерпретирует).
export function escapeHtml(s: string): string;
```

From src/web-scraper.ts (текущее состояние):
```typescript
// Уже есть и переиспользуется: разворачивает err.cause в читаемое "msg (cause: CODE — text)"
function formatErrCause(err: unknown): string;

// Уже exported (тестами зафиксирован контракт split-by `\n\n`) — менять НЕ нужно
export function composeWebDigest(summarizedHtml: string, succeeded: number, total: number): string;

// Private — placeholder для technical-fail. В этом плане его НЕ переписываем, только обёртываем
// результат на стороне runWebPipeline (см. план).
function buildPlaceholderHtml(total: number): string;

// В runWebPipeline уже копится errors: string[] в формате `${site.url}: ${msg}` (msg из formatErrCause).
// Параллельно с ним добавим failedSites: Array<{url: string, reason: string}> в том же for-цикле.
```

From src/deliver.ts:
```typescript
// chunkHtml уже корректно режет по \n\n / \n. Блок «⚠️ Не удалось распарсить» использует
// один \n между bullet'ами и \n\n перед заголовком блока — совместимо.
export function chunkHtml(html: string, max?: number): string[];
export async function sendToChannel(html: string): Promise<void>;
```

From src/types.ts:
```typescript
// WebRunSummary: НЕ меняем public shape. errors[] уже там, его достаточно.
export interface WebRunSummary { /* ... */ errors: string[]; }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement buildFailedSitesBlock + integrate into both delivery paths + add vitest tests</name>
  <files>src/web-scraper.ts, src/__tests__/web-scraper.test.ts</files>
  <behavior>
    Tests to write FIRST (RED), then implementation (GREEN):

    **A. `buildFailedSitesBlock` — happy path (non-empty)**
    - Input: `[{url: "https://a.example/", reason: "HTTP 500"}, {url: "https://b.example/news", reason: "fetch failed (cause: UND_ERR_CONNECT_TIMEOUT — Connect Timeout Error)"}]`
    - Output starts with `\n\n<b>⚠️ Не удалось распарсить (2)</b>\n`
    - Each entry rendered as: `• <code>https://a.example/</code> — HTTP 500` separated by `\n`
    - Counter `(N)` matches `failedSites.length`

    **B. `buildFailedSitesBlock` — empty input**
    - Input: `[]`
    - Output: `""` (empty string — caller просто конкатенирует, ничего не появится)

    **C. `buildFailedSitesBlock` — HTML escape**
    - Input: `[{url: "https://x.example/?a=1&b=2", reason: "<script>alert(1)</script>"}]`
    - Output contains `&amp;` (not `&` in url), `&lt;script&gt;` (not raw `<script>`)
    - `escapeHtml` from `./summarize.js` must be applied to BOTH url и reason

    **D. (опционально, но желательно) reason length cap**
    - Input: reason длиной 500 символов
    - Output: bullet содержит reason обрезанный до ~120 символов с суффиксом `…` (или без — выбрать одну стратегию и зафиксировать тестом)
  </behavior>
  <action>
    **Step 1 — Tests (RED).** В `src/__tests__/web-scraper.test.ts`:
    - Добавь импорт `buildFailedSitesBlock` из `../web-scraper.js` (рядом с уже импортируемыми `composeWebDigest`, `extractText`, `siteToPost`).
    - Добавь новый `describe("buildFailedSitesBlock", () => { ... })` блок в конец файла с 3-4 кейсами (см. `<behavior>` A/B/C/D).
    - Запусти `npm test -- web-scraper` — все новые кейсы должны fail'иться (модуль ещё не экспортирует функцию).

    **Step 2 — Implementation (GREEN).** В `src/web-scraper.ts`:

    2a. Добавь exported helper рядом с `buildWebHeader` (после line ~317, до `buildPlaceholderHtml`):
    ```typescript
    // =============================================================================
    // buildFailedSitesBlock — quick-260519-k6c.
    // Возвращает HTML-блок «⚠️ Не удалось распарсить (N)» для аппенда в конец дайджеста.
    // На вход — failedSites из runWebPipeline (только rejected, НЕ fulfilled-empty).
    // На пустой массив → "" (caller конкатенирует, ничего не появится).
    //
    // EXPORTED — чтобы юнит-тесты зафиксировали контракт (формат блока, escape, empty case).
    // =============================================================================
    const REASON_MAX_CHARS = 120;
    export function buildFailedSitesBlock(
      failedSites: Array<{ url: string; reason: string }>
    ): string {
      if (failedSites.length === 0) return "";
      const lines = failedSites.map((f) => {
        const reason =
          f.reason.length > REASON_MAX_CHARS
            ? f.reason.slice(0, REASON_MAX_CHARS) + "…"
            : f.reason;
        return `• <code>${escapeHtml(f.url)}</code> — ${escapeHtml(reason)}`;
      });
      return `\n\n<b>⚠️ Не удалось распарсить (${failedSites.length})</b>\n` + lines.join("\n");
    }
    ```
    Префикс `\n\n` ВСТРОЕН в блок — caller просто конкатенирует, не нужно добавлять разделитель снаружи. Это упрощает обе точки интеграции.

    2b. В `runWebPipeline`, в for-цикле обхода `results` (line ~416-434), параллельно с `errors.push(...)` добавь сбор `failedSites`:
    ```typescript
    const errors: string[] = [];
    const failedSites: Array<{ url: string; reason: string }> = [];  // ← новое
    // ...
    for (let i = 0; i < results.length; i++) {
      const r = results[i]!;
      const site = websites[i]!;
      if (r.status === "rejected") {
        const msg = formatErrCause(r.reason);
        errors.push(`${site.url}: ${msg}`);
        failedSites.push({ url: site.url, reason: msg });  // ← новое
        websitesSkipped++;
        continue;
      }
      // fulfilled-empty branch ОСТАЁТСЯ без изменений — НЕ пушим в failedSites
      // (паритет различия «сайт лёг» vs «сайт жив, но новостей нет»).
      ...
    }
    ```

    2c. Интегрируй блок в **placeholder-путь** (line ~471-477):
    ```typescript
    if (websitesSucceeded === 0 && websites.length > 0 && mergedPosts.length === 0) {
      const placeholder = buildPlaceholderHtml(websites.length) + buildFailedSitesBlock(failedSites);
      // ... остальное без изменений (sendToChannel/writeOutputWeb/sendAlert на placeholder)
    }
    ```

    2d. Интегрируй блок в **контентный путь** (line ~521-523):
    ```typescript
    } else {
      const finalHtml = composeWebDigest(html, websitesSucceeded, websites.length) + buildFailedSitesBlock(failedSites);
      await sendToChannel(finalHtml);
      writeOutputWeb(finalHtml, runId);
      // ... остальное без изменений (digestDelivered/commitHashCache/лог)
    }
    ```

    Note: D-14 silence-путь (когда `itemsCount === 0`) — НЕ трогаем, там вообще ничего не шлём.

    **Step 3 — Verify (GREEN confirmed).**
    - `npm test -- web-scraper` — все 4 новых кейса pass, существующие composeWebDigest/extractText/fetchSite/siteToPost/loadWebsites тесты ОСТАЮТСЯ pass (regression check).
    - `npx tsc --noEmit` — strict TS чист.

    **Что НЕ делать (anti-scope):**
    - НЕ менять `composeWebDigest` сигнатуру/поведение (контракт зафиксирован существующим тестом).
    - НЕ менять `buildPlaceholderHtml` сигнатуру (private, можно но не нужно — обёртка снаружи проще).
    - НЕ добавлять `failedSites` в `WebRunSummary` (есть `errors`, public shape оставляем).
    - НЕ трогать TG-pipeline (`src/pipeline.ts`) — там «каналы», семантика другая.
    - НЕ добавлять блок в D-14 silence-путь (там ВООБЩЕ ничего не шлём — это by design).
    - НЕ пушить fulfilled-empty сайты в failedSites — оператор не должен путать «лёг» и «жив, но 0 новостей».
  </action>
  <verify>
    <automated>npm test -- web-scraper</automated>
  </verify>
  <done>
    - `npm test -- web-scraper` PASS: 3-4 новых describe-кейса для `buildFailedSitesBlock` зелёные, существующие — без regression.
    - `npx tsc --noEmit` PASS — strict TS чист.
    - `buildFailedSitesBlock` exported из `src/web-scraper.ts`.
    - В `runWebPipeline` собирается `failedSites: Array<{url, reason}>` ТОЛЬКО для `r.status === "rejected"` (НЕ для fulfilled-empty).
    - Оба пути доставки (контентный line ~521 + placeholder line ~471) конкатенируют результат `buildFailedSitesBlock(failedSites)` ПОСЛЕ основного HTML.
    - `WebRunSummary` public shape — без изменений.
    - Новых runtime-зависимостей нет (`package.json` не трогали).
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification — реальный прогон + визуальная проверка блока в канале</name>
  <what-built>
    - Новый exported helper `buildFailedSitesBlock(failedSites)` в `src/web-scraper.ts`
    - Интегрирован в оба пути доставки web-дайджеста (контентный + placeholder)
    - vitest unit-тесты зафиксировали контракт (non-empty / empty / HTML-escape)
  </what-built>
  <how-to-verify>
    1. **Запусти web-pipeline вживую:** `npm run start:once:web`
    2. **Открой закрытый Telegram-канал** (тот, что в `TG_CHANNEL_ID`).
    3. **Найди свежий web-дайджест** (header: «🌐 Веб-источники — {сегодня}»).
    4. **Прокрути в самый конец сообщения.** Ожидаемое поведение:
       - Если в этом прогоне ХОТЯ БЫ один сайт упал с network/HTTP-ошибкой → внизу есть блок:
         ```
         ⚠️ Не удалось распарсить (N)
         • https://... — <причина>
         • ...
         ```
         где N == количество rejected сайтов в логах.
       - Если ВСЕ 33 сайта успешно ответили (`succeeded == total`, ничего не fail'илось) → блока НЕТ совсем (никакого «Не удалось распарсить — нет»).
       - Сайты, которые fulfilled но дали 0 постов (например, RSS 0 fresh items, или HTML <200ch) — в блоке НЕ должны фигурировать.
    5. **Сверь URL'ы в блоке с warn-логами:** `cat data/run-*.log | grep "web-scraper.*fail:"` — каждый «fail» URL должен быть в блоке, и наоборот.
    6. **Если N большое (>20 сайтов)** — убедись, что сообщение либо влезло в один chunk (4000ch), либо корректно разрезано chunkHtml по `\n` между bullet'ами (без рваных HTML-тегов).
    7. **Проверь архив:** `cat data/output/$(date +%Y-%m-%d)-web.md | tail -30` — блок должен присутствовать и в архиве, идентичен тому, что в канале.
  </how-to-verify>
  <resume-signal>Type "approved" если блок отображается корректно, или опиши проблемы (формат, escape, лишние/недостающие сайты, разрыв chunk'а)</resume-signal>
</task>

</tasks>

<verification>
- `npm test` — все тесты PASS (новые + регрессионные)
- `npx tsc --noEmit` — strict TS чист
- Один live-прогон `npm run start:once:web` показывает блок «⚠️ Не удалось распарсить (N)» в конце дайджеста в закрытом Telegram-канале (или его отсутствие, если все сайты ok)
- Содержимое блока совпадает с warn-логами `[web-scraper] ... fail:` (URL и причины)
- Архив `data/output/YYYY-MM-DD-web.md` идентичен отправленному в канал
</verification>

<success_criteria>
- В закрытом Telegram-канале конец web-дайджеста содержит блок недоступных сайтов (когда rejected > 0)
- Блок отсутствует, когда все сайты успешно отвечают (rejected == 0)
- Сайты с fulfilled-empty (RSS 0 items, HTML <200ch) в блок НЕ попадают
- HTML экранирование защищает от инъекций из error messages
- `WebRunSummary` shape не сломан (downstream-консьюмеры, если есть, не задеты)
- TG-pipeline не задет (`src/pipeline.ts` без изменений)
- Новых runtime-зависимостей нет
</success_criteria>

<output>
After completion, create `.planning/quick/260519-k6c-failed-sites-block/260519-k6c-SUMMARY.md`
</output>
