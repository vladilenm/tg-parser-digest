---
quick_id: 260518-fug
description: Добавить ключевое слово «Росавтодор» в категорию bitumen (Pass 1 классификатор)
created: 2026-05-18
mode: quick
---

# Quick Task 260518-fug — План

## Контекст

Заказчик прислал новость о Росавтодоре, выступившем против цементных дорог. Попросил добавить такие новости в категорию `#битум`, например через ключевое слово «Росавтодор» — это федеральный регулятор автодорожной отрасли, любая новость про него ↔ дорожное строительство ↔ битум/асфальт.

## Что меняем

Pass 1 — `CLASSIFY_SYSTEM_PROMPT` в [src/summarize.ts:41](../../../src/summarize.ts#L41).
Категория bitumen перечисляет ключевые слова: `битум, ПБВ, асфальт, битумные вяжущие, bitumen`. Добавляем `Росавтодор` в конец перечисления.

Pass 2 (`buildSummarizeCategoryPrompt`) **не правим** — он не перечисляет per-category ключевые слова, только per-mentions; на этапе Pass 2 пост уже отнесён к категории, и нужен только дословный экстракт.

## Tasks

### Task 1 — Расширить bitumen keywords в Pass 1

- **files**: `src/summarize.ts`
- **action**: В строке 41 (`"  bitumen    — ... bitumen"`) добавить «Росавтодор» в конец перечисления через запятую.
- **verify**: `grep -n "Росавтодор" src/summarize.ts` → попадание в строку bitumen.
- **done**: Поправлена ровно одна строка, никаких изменений сигнатур/тестов; `npm run check` (если есть) или хотя бы tsx parse не падает.

## must_haves

- truths: новость про Росавтодор после правки попадает в категорию bitumen на Pass 1
- artifacts: `src/summarize.ts` строка 41 содержит подстроку «Росавтодор»
- key_links: [src/summarize.ts:41](../../../src/summarize.ts#L41)

## Не делаем

- Не трогаем Pass 2 — там нет per-category keyword-листов.
- Не добавляем тестов на текст промпта — текст промпта не покрыт юнит-тестами проекта (видно по `src/__tests__/summarize.test.ts`).
- Не правим CLAUDE.md / PROJECT.md — это micro-tweak ключевого слова, не архитектурное решение.
