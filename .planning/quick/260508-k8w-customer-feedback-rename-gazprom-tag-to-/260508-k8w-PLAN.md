---
phase: quick-260508-k8w
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - websites.json
  - channels.json
  - src/summarize.ts
  - README.md
autonomous: true
requirements:
  - QUICK-260508-k8w
must_haves:
  truths:
    - "websites.json содержит rolf-oil и lubrigard в массиве websites"
    - "channels.json содержит rolf_oil и lubrigard в массиве channels"
    - "CLASSIFY_SYSTEM_PROMPT содержит правило 3a с ключевыми словами для rosneft (Роснефть, РН-, РНПК, и т.д.)"
    - "CLASSIFY_SYSTEM_PROMPT содержит правило 3b с ключевыми словами для gazpromneft (Газпромнефть, ГПН, Московский НПЗ, NIS, и т.д.)"
    - "CLASSIFY_SYSTEM_PROMPT содержит правило 3c с ключевыми словами для lukoil (ЛУКОЙЛ, ЛЛК, НОРСИ, Litasco, и т.д.)"
    - "Категория oil содержит ключевое слово «антифриз»"
    - "SECTION_HEADERS содержит хэштеги: #Бункер, #Масла, #Керосин, #Нефтехимия, #Битум, #Компании"
    - "README.md:77 ссылается на «#Компании» вместо «Упоминания компаний»"
    - "vitest tests pass — npm test exit 0"
    - "Никакой user-facing label не использует литерал [Газпром] — только [ГПН]"
  artifacts:
    - path: "websites.json"
      provides: "Список web-источников с rolf-oil и lubrigard для категории Масла"
      contains: "rolf-oil"
    - path: "channels.json"
      provides: "Список TG-каналов с rolf_oil и lubrigard"
      contains: "rolf_oil"
    - path: "src/summarize.ts"
      provides: "CLASSIFY_SYSTEM_PROMPT с правилами 3a/3b/3c, oil+антифриз, SECTION_HEADERS с хэштегами"
      contains: "#Компании"
    - path: "README.md"
      provides: "Обновлённое описание секции «#Компании»"
      contains: "#Компании"
  key_links:
    - from: "src/summarize.ts SECTION_HEADERS"
      to: "renderHtml output"
      via: "lines.push(`<b>${sectionHeader}</b>`)"
      pattern: "<b>🏢 #Компании</b>"
    - from: "websites.json"
      to: "src/web-scraper.ts loader"
      via: "WebsitesFileSchema (Zod)"
      pattern: "rolfoil.ru|lubrigard.ru"
    - from: "channels.json"
      to: "src/channels-store.ts loader"
      via: "JSON.parse + array of {username}"
      pattern: "rolf_oil|lubrigard"
---

<objective>
Apply 5 customer feedback items in a single quick task: add new sources for Oils category (Rolf, Lubrigard via website + TG channel), expand company keyword lists in the classify prompt (rosneft / gazpromneft / lukoil — full authoritative list), add «антифриз» to the oil category keyword list, and convert section headers to clickable Telegram hashtags (#Бункер / #Масла / #Керосин / #Нефтехимия / #Битум / #Компании). README.md line 77 aligned with the new header.

Purpose: Customer requested better recall for company-tagged posts (especially Lukoil refineries and Rosneft subsidiaries that previous prompt missed), expanded source coverage in the Oils category, and Telegram-native hashtag navigation in the digest output.

Output: Updated websites.json, channels.json, src/summarize.ts, README.md. Tests still green (npm test).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@src/summarize.ts
@websites.json
@channels.json
@src/__tests__/summarize.test.ts
@README.md

<interfaces>
<!-- Key locations / contracts the executor needs. Embedded so no codebase exploration is required. -->

src/summarize.ts current state (key landmarks):
- Lines 29–56: `CLASSIFY_SYSTEM_PROMPT` — array of strings joined with "\n". Rules 3a/3b currently exist for `gazpromneft` only (lines 47–51). Rules 3a/3b need to be REPLACED with three rules 3a/3b/3c covering rosneft/gazpromneft/lukoil. The split rule «не помечай gazpromneft если только Газпром без нефть» must be PRESERVED — it goes inside new rule 3b as a sub-clause.
- Line 38: oil category description — append «, антифриз».
- Lines 256–263: `SECTION_HEADERS` — convert each header label to include a `#`-prefixed hashtag. The 6th block (mentions) becomes `🏢 #Компании` (single token).
- Line 305: `lines.push(`<b>${sectionHeader}</b>`)` — wraps header in <b>; Telegram parses `#Word` inside `<b>` correctly. No render-side change needed.
- `MENTION_LABEL.gazpromneft = "ГПН"` already in place from quick task 260508-b55.

websites.json structure:
```json
{ "websites": [ { "url": "...", "name": "...", "rss": "..." }, ... ] }
```
The file is read by `src/web-scraper.ts` and validated by `WebsitesFileSchema` (Zod) in `src/schema.ts`. New entries must be valid URLs.

channels.json structure:
```json
{ "channels": [ { "username": "..." }, ... ] }
```
Read by `src/channels-store.ts`.

src/__tests__/summarize.test.ts coverage:
- Tests assert `<b>[РОСНЕФТЬ]</b>`, `3 постов из 2 каналов за 24ч`, `<i>— нет упоминаний за сутки</i>`, deep-link href. NO test asserts the literal SECTION_HEADER strings. Adding `#` to headers will NOT break existing tests.
- If a test starts asserting a header literal, the executor must update the assertion to match the new format (it does NOT today).
</interfaces>

<discretion_captured>
- For the 6th section (mentions / orphan-company posts) the customer's pattern is one hashtag per section. Telegram does not support multi-word hashtags (`#Упоминания компаний` would only tag «Упоминания»). Decision: use `#Компании` — single token, semantically clean, navigation-friendly. The prose «Упоминания компаний» is dropped from the header but stays in the codebase comment at line 248 (descriptive, not user-facing).
</discretion_captured>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Rolf and Lubrigard to source lists</name>
  <files>websites.json, channels.json</files>
  <action>
1) `websites.json`: insert two new entries into the `websites` array. Place them in the «oils-adjacent» cluster near the bottom of the file, immediately after the `kixx` entry (line 45) and before `felix` — this keeps oil-category sources grouped:
   ```json
   { "url": "https://rolfoil.ru/news/", "name": "rolf-oil" },
   { "url": "https://lubrigard.ru/press-center", "name": "lubrigard" },
   ```
   Keep JSON valid: trailing comma rules — every entry except the last in the array must end with `,`. Indentation: 4 spaces (match existing file). Verify the file still parses as JSON (`node -e "JSON.parse(require('fs').readFileSync('websites.json','utf8'))"`).

2) `channels.json`: append two entries to the `channels` array, after the existing last entry `{"username":"oilgasinform"}` (line 151). New entries follow existing 4-space indentation pattern:
   ```json
       {
         "username": "rolf_oil"
       },
       {
         "username": "lubrigard"
       }
   ```
   The previous last entry's closing `}` needs a `,` appended. Final file ends with `]\n}`.

Do NOT touch any other entries. Do NOT reorder. Do NOT change formatting of pre-existing lines.
  </action>
  <verify>
    <automated>node -e "const w=require('./websites.json');const c=require('./channels.json');const wn=w.websites.map(x=>x.name||x.url);const cn=c.channels.map(x=>x.username);if(!wn.includes('rolf-oil')||!wn.includes('lubrigard'))process.exit(1);if(!cn.includes('rolf_oil')||!cn.includes('lubrigard'))process.exit(1);console.log('OK')"</automated>
  </verify>
  <done>
- websites.json contains entries with names `rolf-oil` and `lubrigard`
- channels.json contains usernames `rolf_oil` and `lubrigard`
- Both files parse as valid JSON
- No other entries changed
- One atomic commit: `feat(quick-260508-k8w): add rolf-oil + lubrigard sources to websites.json and channels.json`
  </done>
</task>

<task type="auto">
  <name>Task 2: Expand CLASSIFY_SYSTEM_PROMPT — full keyword lists for rosneft/gazpromneft/lukoil + антифриз in oil category</name>
  <files>src/summarize.ts</files>
  <action>
Edit `src/summarize.ts` `CLASSIFY_SYSTEM_PROMPT` (lines 29–56) in two places:

1) Line 38 — append «, антифриз» to the oil category description. Result:
   ```ts
   "  oil        — смазочные материалы и масла: смазки, lubricants, моторное масло, базовые масла, индустриальные масла, антифриз",
   ```

2) Lines 47–51 — replace the existing 2-rule block (3a/3b only for gazpromneft) with the new 3-rule block (3a/3b/3c) covering all three companies. The customer's authoritative keyword lists must be preserved verbatim (these are matched by the LLM literally — typos break recall). The «Газпром БЕЗ нефть → не помечай» exclusion must be PRESERVED, now nested inside the new rule 3b.

   Replace lines 47–51 with these 27 lines (each entry is one element of the JS string array, joined with "\n"):

   ```ts
   "3a) Литерал 'rosneft' назначай, если в тексте упомянуты любые из форм:",
   "    Роснефть, РН- (с дефисом — префикс дочерних бренд-обозначений),",
   "    Новокуйбышевский НПЗ, Рязанский НПЗ, Рязанская НПК, РНПК,",
   "    Туапсинский НПЗ, Сызранский НПЗ, СвНИИНП, РН-ЦИР,",
   "    Ангарский НПЗ, Ангарская НХК, АНХК, Ачинский НПЗ,",
   "    Куйбышевский НПЗ, Башнефть, Уфанефтехим, Новойл,",
   "    ЯНОС, Ярославский НПЗ, Славнефть, НЗМП.",
   "3b) Литерал 'gazpromneft' назначай, если в тексте упомянуты любые из форм:",
   "    Газпромнефть (слитно), Газпром нефть (через пробел — официальное название),",
   "    Газпром-нефть (через дефис), ГПН (аббревиатура),",
   "    Московский НПЗ, МНПЗ, Омский НПЗ, NIS.",
   "    НЕ назначай 'gazpromneft', если в тексте только 'Газпром' БЕЗ 'нефть' и без",
   "    иных форм выше — это материнская газовая компания, она нам не нужна.",
   "3c) Литерал 'lukoil' назначай, если в тексте упомянуты любые из форм:",
   "    ЛУКОЙЛ, Lukoil, ЛЛК, Нижегородский НПЗ, Нижегороднефтеоргсинтез, НОРСИ,",
   "    Волгоградский НПЗ, Волгограднефтепереработка, Пермский НПЗ,",
   "    Пермнефтеоргсинтез, ПНОС, Ухтинский НПЗ, Бургас, Петротел,",
   "    Литаско, Litasco.",
   ```

   Surrounding lines untouched: line 46 `"3) mentions[] — ..."` stays as the parent rule; line 52 `"4) Каждый пост — ..."` stays as the next rule. Only lines 47–51 are replaced.

Do NOT change any other rule (1, 2, 3, 4) or the JSON example on line 55. Do NOT touch the buildSummarizeCategoryPrompt function. Do NOT touch SECTION_HEADERS in this task — that's Task 3.

Notes for the executor:
- The CLASSIFY_SYSTEM_PROMPT array is joined via `.join("\n")` (line 56), so each string element becomes one line in the prompt. Multi-line entries via continuation strings (e.g., `"    Газпром..."` then `"    Московский НПЗ..."`) are fine — the LLM reads them as continuation of the rule.
- TypeScript strings: keep single-quoted literals like `'gazpromneft'`, `'rosneft'`, `'lukoil'` inside double-quoted JS strings — no escaping needed.
- Cyrillic identifiers (РНПК, ЛЛК, ЯНОС, ПНОС, НОРСИ, etc.) must be Cyrillic, not Latin lookalikes. Use exact characters from the keyword list above.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p . 2>&1 | grep -E "(error|src/summarize)" || true; grep -c "'rosneft'" src/summarize.ts; grep -c "'lukoil'" src/summarize.ts; grep -c "антифриз" src/summarize.ts; grep -c "Башнефть" src/summarize.ts; grep -c "Litasco" src/summarize.ts; grep -c "Московский НПЗ" src/summarize.ts</automated>
  </verify>
  <done>
- TypeScript compilation passes (no errors in src/summarize.ts)
- CLASSIFY_SYSTEM_PROMPT contains rules `3a)`, `3b)`, `3c)` (one occurrence each)
- Rule 3a contains "Башнефть", "РНПК", "Ангарский НПЗ"
- Rule 3b contains "Московский НПЗ", "МНПЗ", "Омский НПЗ", "NIS" and the "НЕ назначай ... материнская газовая компания" exclusion clause
- Rule 3c contains "ЛУКОЙЛ", "ЛЛК", "НОРСИ", "Litasco"
- oil category line contains «антифриз»
- One atomic commit: `feat(quick-260508-k8w): expand classify prompt with full company keyword lists + add антифриз to oil category`
  </done>
</task>

<task type="auto">
  <name>Task 3: Add hashtags to SECTION_HEADERS + align README.md:77</name>
  <files>src/summarize.ts, README.md</files>
  <action>
1) `src/summarize.ts` lines 256–263 — replace the SECTION_HEADERS array. Add `#` prefix to each category name. The 6th entry's header changes from `🏢 Упоминания компаний` to `🏢 #Компании` (single-token hashtag — Telegram does not support multi-word hashtags). The TypeScript type and key values stay unchanged; only the `header` strings change:

   ```ts
   const SECTION_HEADERS: Array<{ key: keyof Pick<DigestJson, "bunker"|"oil"|"kerosene"|"petrochem"|"bitumen"|"mentions">; header: string }> = [
     { key: "bunker",    header: "🚢 #Бункер" },
     { key: "oil",       header: "🛢 #Масла" },
     { key: "kerosene",  header: "✈️ #Керосин" },
     { key: "petrochem", header: "⚗️ #Нефтехимия" },
     { key: "bitumen",   header: "🛣 #Битум" },
     { key: "mentions",  header: "🏢 #Компании" },
   ];
   ```

   The render at line 305 (`lines.push(`<b>${sectionHeader}</b>`)`) is unchanged — Telegram's HTML parser correctly treats `#Word` inside `<b>...</b>` as a clickable hashtag. No additional escaping is needed because `#` is not a special HTML character.

   Do NOT touch the comment at line 248 — it's an internal code comment (`«Упоминания компаний» (orphans)`) that documents the architectural concept, not user-facing output.

2) `README.md` line 77 — change the bullet's bold label to mirror the new header. Keep the prose explanation intact.

   FROM:
   ```
      - 🏢 **Упоминания компаний** — посты без чёткой категории, но с упоминанием Роснефть/Лукойл/Газпромнефть (orphans). Пост в категории с mentions получает inline-маркер `[РОСНЕФТЬ]`/`[ЛУКОЙЛ]`/`[ГПН]` перед summary.
   ```

   TO:
   ```
      - 🏢 **#Компании** — посты без чёткой категории, но с упоминанием Роснефть/Лукойл/Газпромнефть (orphans). Пост в категории с mentions получает inline-маркер `[РОСНЕФТЬ]`/`[ЛУКОЙЛ]`/`[ГПН]` перед summary.
   ```

   Do NOT touch line 3 (descriptive prose, not output literals — per task brief). Do NOT touch line 210 (also descriptive prose about web-digest format — not the user-facing tag).

3) Run `npm test` to confirm vitest still passes. Existing tests do NOT assert any SECTION_HEADERS literal — only `<b>[РОСНЕФТЬ]</b>`, `3 постов из 2 каналов за 24ч`, `<i>— нет упоминаний за сутки</i>`, and deep-link href. Adding `#` should be a no-op for the test suite. If any test fails because it asserts a header literal (it shouldn't today, but if a future test does), update the assertion to match the new format.
  </action>
  <verify>
    <automated>npm test 2>&1 | tail -20; grep -c "#Компании" src/summarize.ts; grep -c "#Бункер" src/summarize.ts; grep -c "#Масла" src/summarize.ts; grep -c "#Керосин" src/summarize.ts; grep -c "#Нефтехимия" src/summarize.ts; grep -c "#Битум" src/summarize.ts; grep -c "🏢 \*\*#Компании\*\*" README.md</automated>
  </verify>
  <done>
- SECTION_HEADERS contains all 6 hashtag-prefixed labels: `🚢 #Бункер`, `🛢 #Масла`, `✈️ #Керосин`, `⚗️ #Нефтехимия`, `🛣 #Битум`, `🏢 #Компании`
- README.md:77 shows `🏢 **#Компании**` (no longer `🏢 **Упоминания компаний**`)
- README.md:3 and README.md:210 unchanged (descriptive prose)
- `npm test` exits 0 (all vitest tests pass)
- One atomic commit: `feat(quick-260508-k8w): add hashtag navigation to digest section headers + align README`
  </done>
</task>

</tasks>

<verification>
After all 3 tasks committed, run:

```bash
npm test                                          # vitest must pass
node -e "JSON.parse(require('fs').readFileSync('websites.json','utf8'))"   # valid JSON
node -e "JSON.parse(require('fs').readFileSync('channels.json','utf8'))"   # valid JSON
npx tsc --noEmit -p .                             # TypeScript still compiles
grep -c "'rosneft'\|'lukoil'\|'gazpromneft'" src/summarize.ts   # all three literals present
grep -c "rolf-oil\|lubrigard" websites.json       # ≥ 2 (both names)
grep -c "rolf_oil\|lubrigard" channels.json       # ≥ 2 (both usernames)
grep -c "#Бункер\|#Масла\|#Керосин\|#Нефтехимия\|#Битум\|#Компании" src/summarize.ts   # all six hashtags
grep -E "Газпром\]" src/ README.md -r            # MUST be empty (no remaining [Газпром] tag)
```
</verification>

<success_criteria>
- [ ] websites.json contains rolf-oil + lubrigard entries (valid JSON)
- [ ] channels.json contains rolf_oil + lubrigard usernames (valid JSON)
- [ ] CLASSIFY_SYSTEM_PROMPT has rule 3a (rosneft full keyword list incl. Башнефть, РНПК, Ангарский НПЗ, ЯНОС, Славнефть, НЗМП)
- [ ] CLASSIFY_SYSTEM_PROMPT has rule 3b (gazpromneft + Московский НПЗ + NIS + «не помечай если только Газпром без нефть» exclusion preserved)
- [ ] CLASSIFY_SYSTEM_PROMPT has rule 3c (lukoil full keyword list incl. ЛЛК, НОРСИ, ПНОС, Litasco)
- [ ] oil category description includes «антифриз»
- [ ] SECTION_HEADERS uses 6 hashtagged labels (#Бункер, #Масла, #Керосин, #Нефтехимия, #Битум, #Компании)
- [ ] README.md:77 reads `🏢 **#Компании**`
- [ ] No remaining user-facing literal `[Газпром]` (audit clean)
- [ ] `npm test` passes (vitest green)
- [ ] `npx tsc --noEmit` passes (no TS errors)
- [ ] Three atomic commits, one per task, on current branch
</success_criteria>

<output>
After completion, create `.planning/quick/260508-k8w-customer-feedback-rename-gazprom-tag-to-/260508-k8w-SUMMARY.md` documenting:
- Files modified (websites.json, channels.json, src/summarize.ts, README.md)
- 3 commit SHAs
- Verification: npm test output (vitest), TypeScript compilation, JSON parse checks
- Customer feedback items addressed (5/5)
- Discretion captured: `#Компании` chosen for 6th block (single-token Telegram hashtag constraint)
</output>
