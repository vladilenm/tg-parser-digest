---
phase: 260508-b55
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types.ts
  - src/schema.ts
  - src/summarize.ts
  - src/__tests__/summarize.test.ts
  - README.md
autonomous: true
requirements:
  - QUICK-260508-b55
must_haves:
  truths:
    - "Категории в CLASSIFY_SYSTEM_PROMPT (src/summarize.ts) расширены синонимами RU+EN+аббревиатурами по 5 темам (bunker, oil, kerosene, petrochem, bitumen) — LLM получает на вход более широкий лексикон."
    - "Отслеживаемая компания «gazprom» полностью заменена на «gazpromneft» во всех слоях: тип Mention, zod-схема MENTIONS, MENTION_LABEL, тексты промптов, тесты и README."
    - "В CLASSIFY_SYSTEM_PROMPT явно указаны варианты написания «Газпромнефть» / «Газпром нефть» / «Газпром-нефть» / «ГПН» И явно сказано НЕ помечать просто «Газпром» (без «нефть»)."
    - "MENTION_LABEL[gazpromneft] === 'ГПН' — inline-маркер в HTML-дайджесте теперь [ГПН] вместо [ГАЗПРОМ]."
    - "npm test проходит без ошибок (тесты обновлены под gazpromneft, TypeScript компилируется без unresolved Mention literal'ов)."
  artifacts:
    - path: "src/types.ts"
      provides: "Mention type literal — 'gazprom' заменён на 'gazpromneft'"
      contains: "gazpromneft"
    - path: "src/schema.ts"
      provides: "MENTIONS const + MentionSchema — 'gazprom' заменён на 'gazpromneft'"
      contains: "gazpromneft"
    - path: "src/summarize.ts"
      provides: "CLASSIFY_SYSTEM_PROMPT с обогащёнными категориями + варианты написания Газпромнефть; MENTION_LABEL.gazpromneft = 'ГПН'"
      contains: "gazpromneft"
    - path: "src/__tests__/summarize.test.ts"
      provides: "Тесты обновлены: ClassificationEntry с mentions:['gazpromneft'] компилируется"
    - path: "README.md"
      provides: "Раздел про маркеры компаний обновлён: Роснефть/Лукойл/Газпромнефть, маркер [ГПН]"
      contains: "ГПН"
  key_links:
    - from: "src/types.ts (Mention)"
      to: "src/schema.ts (MENTIONS const)"
      via: "оба должны содержать одинаковый литерал 'gazpromneft'"
      pattern: "gazpromneft"
    - from: "src/summarize.ts (MENTION_LABEL)"
      to: "src/types.ts (Mention)"
      via: "Record<Mention, string> требует ключ gazpromneft"
      pattern: "gazpromneft:\\s*\"ГПН\""
---

<objective>
Расширить категории-описания в CLASSIFY_SYSTEM_PROMPT синонимами RU+EN+аббревиатурами и
полностью заменить отслеживаемую компанию gazprom (материнская, газовые новости) на
gazpromneft (ГПН — нефтепродукты, наша целевая повестка).

Purpose: повысить полноту classification pass'а (Pass 1) за счёт расширенного лексикона
и переключиться на нужного эмитента — Газпромнефть вместо Газпром.

Output: обновлённый промпт классификатора + полная переименовка идентификатора Mention
по 5 файлам (types, schema, summarize, tests, README) синхронно. Архитектура и
keyword-whitelist pre-filter НЕ вводятся.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@CLAUDE.md
@src/summarize.ts
@src/types.ts
@src/schema.ts
@src/__tests__/summarize.test.ts
@README.md

<interfaces>
<!-- Текущие сигнатуры, которые ломаются при переименовании. Меняем синхронно. -->

src/types.ts:
```typescript
export type Mention = "rosneft" | "lukoil" | "gazprom";  // → "gazpromneft"
```

src/schema.ts:
```typescript
export const MENTIONS = ["rosneft", "lukoil", "gazprom"] as const;  // → "gazpromneft"
export const MentionSchema = z.enum(MENTIONS);
```

src/summarize.ts:
```typescript
const MENTION_LABEL: Record<Mention, string> = {
  rosneft: "РОСНЕФТЬ",
  lukoil: "ЛУКОЙЛ",
  gazprom: "ГАЗПРОМ",   // → gazpromneft: "ГПН"
};
```

Места, где литерал "gazprom" встречается в тексте промптов (без TS-проверки):
- src/summarize.ts:34 — CLASSIFY_SYSTEM_PROMPT, "{rosneft,lukoil,gazprom}"
- src/summarize.ts:46 — CLASSIFY_SYSTEM_PROMPT, "только компании из {rosneft,lukoil,gazprom}"
- src/summarize.ts:126 — buildSummarizeCategoryPrompt, "{rosneft,lukoil,gazprom}"
</interfaces>

<gazpromneft_synonyms_brief>
Все формы, которые LLM должна ловить как "gazpromneft":
- "Газпромнефть" (slitno)
- "Газпром нефть" (через пробел, официальное название)
- "Газпром-нефть" (через дефис)
- "ГПН" (аббревиатура)

НЕ помечать как gazpromneft:
- Просто "Газпром" (без "нефть") — это материнская компания, газовая повестка, нам не нужна.
</gazpromneft_synonyms_brief>

<category_synonyms_brief>
Обогащение описаний категорий в CLASSIFY_SYSTEM_PROMPT (src/summarize.ts:38-42):
- bunker: бункер, мазут, судовое топливо, FO, Fuel oil, бункеровка, морские порты
- oil: смазочные материалы, смазки, lubricants, моторное масло, базовые масла, масла, индустриальные масла
- kerosene: керосин, авиакеросин, авиатопливо, JET, jet fuel, SAF
- petrochem: нефтехимия, полимеры, нефтехимический синтез, этилен, пропилен
- bitumen: битум, ПБВ, асфальт, дорожное строительство, битумные вяжущие, bitumen
</category_synonyms_brief>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Переименовать Mention gazprom → gazpromneft в типах и zod-схеме</name>
  <files>src/types.ts, src/schema.ts</files>
  <action>
Сделать атомарное переименование литерала идентификатора компании. Это первая в цепочке
правка — после неё TypeScript будет ломаться в summarize.ts и тестах, поэтому Task 2
обязан выполниться следом (но коммит общий — см. Task 3).

src/types.ts (строка 15):
- Заменить `export type Mention = "rosneft" | "lukoil" | "gazprom";` на
  `export type Mention = "rosneft" | "lukoil" | "gazpromneft";`
- Строка 23 — обновить комментарий: `подмножество ["rosneft","lukoil","gazprom"]` →
  `подмножество ["rosneft","lukoil","gazpromneft"]`.

src/schema.ts (строка 7):
- Заменить `export const MENTIONS = ["rosneft", "lukoil", "gazprom"] as const;` на
  `export const MENTIONS = ["rosneft", "lukoil", "gazpromneft"] as const;`
- MentionSchema (строка 10) НЕ трогать — он автоматически подхватит новый литерал из const-массива.

Никаких других правок в этих файлах. Структура DigestItem, ClassificationEntrySchema, и т.п.
не меняется.
  </action>
  <verify>
    <automated>grep -n "gazprom" src/types.ts src/schema.ts | grep -v "gazpromneft" || echo "OK: no bare gazprom left"</automated>
  </verify>
  <done>В src/types.ts и src/schema.ts больше нет вхождений "gazprom" без суффикса "neft". TypeScript теперь ожидает gazpromneft везде, где ссылается на тип Mention или константу MENTIONS.</done>
</task>

<task type="auto">
  <name>Task 2: Обогатить CLASSIFY_SYSTEM_PROMPT категориями + добавить варианты написания gazpromneft + перепривязать MENTION_LABEL и mention-литералы в промптах/тестах/README</name>
  <files>src/summarize.ts, src/__tests__/summarize.test.ts, README.md</files>
  <action>
Это «широкая, но плоская» правка: текстовое обогащение промпта + механическая
синхронизация литерала «gazprom» → «gazpromneft» во всех остальных местах. Делать
аккуратно — порядок ниже.

(A) src/summarize.ts — CLASSIFY_SYSTEM_PROMPT (строки 29-51):

Заменить блок «Категории:» (строки 36-42) на расширенные описания. Формат — те же
строки массива, выровнены пробелами как сейчас. Пример итогового вида:
```
"Категории:",
"  bunker     — бункерное/судовое топливо: бункер, мазут, судовое топливо, FO, Fuel oil, бункеровка, морские порты",
"  oil        — смазочные материалы и масла: смазки, lubricants, моторное масло, базовые масла, индустриальные масла",
"  kerosene   — авиатопливо: керосин, авиакеросин, JET, jet fuel, SAF",
"  petrochem  — нефтехимия: полимеры, нефтехимический синтез, этилен, пропилен",
"  bitumen    — битум и дорожно-строительные нефтепродукты: битум, ПБВ, асфальт, битумные вяжущие, bitumen",
```

В строках 34 и 46 заменить `{rosneft,lukoil,gazprom}` → `{rosneft,lukoil,gazpromneft}`.

После строки с правилом (3) (про mentions[]) ДОБАВИТЬ новое правило про варианты
написания Газпромнефти. Вставить ровно перед строкой `"4) Каждый пост — ровно одна
запись в results (url совпадает с входным).",`:
```
"3a) Литерал 'gazpromneft' назначай, если в тексте явно упомянуты любые из форм:",
"    'Газпромнефть' (слитно), 'Газпром нефть' (через пробел — официальное название),",
"    'Газпром-нефть' (через дефис), 'ГПН' (аббревиатура).",
"3b) НЕ помечай 'gazpromneft', если в тексте только 'Газпром' БЕЗ 'нефть' — это",
"    материнская газовая компания, она нам не нужна.",
```

(B) src/summarize.ts — buildSummarizeCategoryPrompt (строка 126):

Заменить `{rosneft,lukoil,gazprom}` → `{rosneft,lukoil,gazpromneft}` в правиле (4).
Никаких других правок в этом промпте — синонимы и поведение Pass 2 НЕ меняем
(категория уже выбрана на Pass 1, а Pass 2 — extractive editor, лексикон ему не нужен).

(C) src/summarize.ts — MENTION_LABEL (строки 260-264):

Заменить:
```typescript
const MENTION_LABEL: Record<Mention, string> = {
  rosneft: "РОСНЕФТЬ",
  lukoil: "ЛУКОЙЛ",
  gazprom: "ГАЗПРОМ",
};
```
на:
```typescript
const MENTION_LABEL: Record<Mention, string> = {
  rosneft: "РОСНЕФТЬ",
  lukoil: "ЛУКОЙЛ",
  gazpromneft: "ГПН",
};
```

Также в комментарии перед блоком (строки 247-248) обновить пример маркеров:
`[РОСНЕФТЬ] / [ЛУКОЙЛ] / [ГАЗПРОМ]` → `[РОСНЕФТЬ] / [ЛУКОЙЛ] / [ГПН]`.

(D) src/__tests__/summarize.test.ts:

В фикстуре describe("groupByBucket") (строка 191):
- `{ url: "https://t.me/ch/3", category: null, mentions: ["lukoil"] },` — оставляем как есть (lukoil).
- Других "gazprom" в тестах нет, но если grep что-то найдёт — заменить literal на "gazpromneft".

После правок убедиться через `grep -n "gazprom" src/__tests__/summarize.test.ts | grep -v gazpromneft`
что чистых "gazprom" не осталось.

(E) README.md:

Строка 3: `Роснефть/Лукойл/Газпром` → `Роснефть/Лукойл/Газпромнефть`.
Строка 77: `Роснефть/Лукойл/Газпром (orphans). Пост в категории с mentions получает inline-маркер `[РОСНЕФТЬ]`/`[ЛУКОЙЛ]`/`[ГАЗПРОМ]`` →
`Роснефть/Лукойл/Газпромнефть (orphans). Пост в категории с mentions получает inline-маркер `[РОСНЕФТЬ]`/`[ЛУКОЙЛ]`/`[ГПН]``.

Никаких других правок в README — структура секций, команды, env-vars не трогаем.

ВАЖНО: НЕ менять архитектуру, НЕ вводить keyword-whitelist pre-filter, НЕ трогать
buildSummarizeCategoryPrompt кроме одной замены литерала в правиле 4. Это правка
промпта + переименование идентификатора, не рефактор.
  </action>
  <verify>
    <automated>grep -rn "gazprom" src README.md | grep -v gazpromneft || echo "OK: only gazpromneft remains"</automated>
  </verify>
  <done>Категории в CLASSIFY_SYSTEM_PROMPT обогащены синонимами; правила 3a/3b добавлены; все mentions-литералы и MENTION_LABEL переключены на gazpromneft / "ГПН"; в README маркер [ГПН] и Газпромнефть; чистых "gazprom" нигде не осталось (grep подтверждает).</done>
</task>

<task type="auto">
  <name>Task 3: Прогнать tsc + npm test и зафиксировать целостность правок</name>
  <files>(никаких; только запуск проверок)</files>
  <action>
Финальная проверка: убедиться, что проект компилируется и тесты зелёные после
синхронной переименовки во всех 5 файлах.

1) Сначала прогнать TypeScript-проверку. В этом проекте есть либо `npm run typecheck`,
   либо просто tsx без bundle — проверить package.json. Если скрипта нет — запустить
   `npx tsc --noEmit` (tsconfig.json уже строгий, см. CLAUDE.md).

2) Затем `npm test` — vitest на src/__tests__/. Тесты используют моки DeepSeek, так что
   изменения промпта на них не влияют, но переименование Mention должно дать корректную
   компиляцию ClassificationEntry с mentions: ["lukoil"] и т.п.

3) Если тест/tsc падает — НЕ изобретать новый функционал. Дочитать ошибку, починить
   точечно (скорее всего пропущенный grep-литерал в одном из файлов) и перезапустить.

4) Опционально (если не ломает поток): запустить grep ещё раз для подтверждения:
   `grep -rn "gazprom" src/ README.md | grep -v gazpromneft` — должен быть пустой
   вывод (или явное "OK").

Регрессий быть не должно: API summarize() не меняется, формат HTML не меняется
(только текст метки [ГПН] вместо [ГАЗПРОМ]).
  </action>
  <verify>
    <automated>npm test 2>&1 | tail -20</automated>
  </verify>
  <done>npm test проходит без ошибок; tsc --noEmit (или эквивалент) без unresolved Mention literal'ов; grep на bare "gazprom" в src/ и README.md возвращает пусто.</done>
</task>

</tasks>

<verification>
1. `grep -rn "gazprom" src/ README.md | grep -v gazpromneft` → пустой вывод (целостность переименования).
2. `npm test` → все vitest-сьюты зелёные.
3. `npx tsc --noEmit` (или `npm run typecheck` если есть) → 0 ошибок.
4. Ручная инспекция CLASSIFY_SYSTEM_PROMPT в src/summarize.ts:29-55:
   - блок «Категории:» содержит синонимы из category_synonyms_brief;
   - присутствуют новые правила 3a и 3b про варианты написания Газпромнефть и явный запрет на одиночный «Газпром»;
   - литерал {rosneft,lukoil,gazpromneft} в правилах (1) и (3).
5. Ручная инспекция MENTION_LABEL в src/summarize.ts: ровно три ключа — rosneft/lukoil/gazpromneft, значение gazpromneft = "ГПН".
</verification>

<success_criteria>
- [ ] src/types.ts: Mention = "rosneft" | "lukoil" | "gazpromneft"
- [ ] src/schema.ts: MENTIONS const содержит "gazpromneft", не "gazprom"
- [ ] src/summarize.ts: CLASSIFY_SYSTEM_PROMPT обогащён синонимами по 5 категориям + новые правила 3a/3b
- [ ] src/summarize.ts: MENTION_LABEL.gazpromneft === "ГПН"
- [ ] src/__tests__/summarize.test.ts: чистых "gazprom" нет
- [ ] README.md: маркер [ГПН] и название Газпромнефть в разделах про маркеры
- [ ] npm test зелёный
- [ ] tsc --noEmit без ошибок
</success_criteria>

<output>
После завершения создать `.planning/quick/260508-b55-classify-system-prompt-gazprom-gazpromne/260508-b55-SUMMARY.md`
с кратким описанием:
- какие файлы изменены и какие именно правки;
- результат `npm test` + tsc;
- подтверждение что архитектура (двухпроходная LLM, bucketing, verifyExtractiveness) НЕ трогалась.
</output>
