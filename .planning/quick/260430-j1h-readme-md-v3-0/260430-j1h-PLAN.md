---
phase: quick
plan: 260430-j1h
type: execute
wave: 1
depends_on: []
files_modified: [README.md]
autonomous: true
requirements: [DOC-05]

must_haves:
  truths:
    - "README описывает v3.0, не v2.0 — нет ни одного 'v2.0' без явного контекста 'было раньше'"
    - "В секции env перечислены все 8 обязательных переменных включая BOT_TOKEN_ALERTS и ALERTS_CHAT_ID"
    - "Счётчик runtime-зависимостей = 5: telegram, openai, yaml, node-cron, zod"
    - "Структура дайджеста описана: 5 категорий (бункер/масла/керосин/нефтехимия/битум) + блок «Упоминания компаний»"
    - "data/ директория описана: data/raw/, data/output/, data/hash-cache.json"
    - "README ссылается на docs/RUNBOOK.md и docs/CHANNELS.md"
    - "Документ без TODO, placeholder-фраз и устаревших ссылок — годен как третий документ acceptance-пакета"
  artifacts:
    - path: "README.md"
      provides: "Актуальная документация v3.0"
  key_links:
    - from: "README.md"
      to: "docs/RUNBOOK.md"
      via: "markdown ссылка"
    - from: "README.md"
      to: "docs/CHANNELS.md"
      via: "markdown ссылка"
---

<objective>
Переписать README.md под v3.0: убрать v2.0-артефакты, добавить описание 5-категорийного дайджеста, BOT_TOKEN_ALERTS/ALERTS_CHAT_ID в секцию env, data/ директорию (raw/output/hash-cache.json), обновить счётчик runtime-зависимостей до 5 (добавить zod), добавить ссылки на docs/RUNBOOK.md и docs/CHANNELS.md.

Purpose: README является третьим документом acceptance-пакета ACCEPT-02 (наряду с RUNBOOK.md и CHANNELS.md) — должен быть чётким, без TODO и полностью отражать текущее состояние проекта.
Output: README.md — единственный изменяемый файл.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-code/01-CONTEXT.md
@README.md
@.env.example
@src/types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Переписать README.md под v3.0</name>
  <files>README.md</files>
  <action>
Полностью переписать README.md, сохраняя русский язык и структуру разделов исходника, но приводя содержимое к v3.0. Технические термины (npm, env-var, paths) оставить на английском.

**Что убрать / заменить:**

1. Строку `v2.0: \`npm start\` — long-running daemon...` в шапке заменить на v3.0-формулировку без версионного лейбла:
   - Кратко: daemon под PM2 (или Docker), ежедневно в 20:15 MSK (с jitter), структурированный дайджест по 5 направлениям нефтегаза/нефтехимии + блок упоминаний Роснефть/Лукойл/Газпром. Без БД, без внешнего крона.

2. В разделе «Подготовка перед первым прогоном → §2. Секреты и конфиг»:
   - Список переменных `.env` должен включать все 8 обязательных: `TG_API_ID`, `TG_API_HASH`, `TG_SESSION`, `TG_BOT_TOKEN`, `TG_CHANNEL_ID`, `DEEPSEEK_API_KEY`, `BOT_TOKEN_ALERTS`, `ALERTS_CHAT_ID`.
   - `BOT_TOKEN_ALERTS` — отдельный бот (BotFather `/newbot`), только для технических алертов в личку владельца (НЕ в канал Заказчика).
   - `ALERTS_CHAT_ID` — numeric chat_id личного чата владельца с alert-ботом (узнать: `/start` alert-бот → переслать ответ в @userinfobot).

3. Счётчик runtime-зависимостей: изменить «четыре» → «пять»; в список добавить `zod` (schema-валидация ответа DeepSeek).

4. В разделе «§5. Первый запуск daemon» → описание tick():
   - Убрать «In-memory дедуплицирует повторы» — деплой теперь различает два уровня dedup.
   - Добавить: «Первая ступень dedup — in-memory по `${username}:${messageId}` в рамках прогона. Вторая ступень — SHA-256 hash-cache (`data/hash-cache.json`, rolling 14 дней) против кросс-прогонных повторов.»
   - Добавить: результат LLM проходит Zod-валидацию `DigestJsonSchema`; при мисматче прогон завершается с ошибкой и алертом в личку владельца.
   - Обновить описание структуры дайджеста: 5 секций (🚢 Бункер, 🛢 Масла, ✈️ Керосин, ⚗️ Нефтехимия, 🛣 Битум) + 🏢 Упоминания компаний (только orphans — посты без категории, но с упоминанием Роснефть/Лукойл/Газпром). Пустая секция явно помечается «— нет упоминаний за сутки», не молчит. Inline-маркер `[РОСНЕФТЬ]` / `[ЛУКОЙЛ]` / `[ГАЗПРОМ]` перед summary для постов в категории с mentions.

5. Добавить раздел **«Архив прогонов (`data/`)»** после «§5. Первый запуск daemon» (перед «Запуск на VPS»):

```
## Архив прогонов (`data/`)

После каждого tick daemon создаёт или обновляет:

- `data/raw/YYYY-MM-DD.json` — сырые посты, собранные за сутки (до dedup и LLM). Дата по MSK. Записывается сразу после fetch, до остальных шагов — даже если LLM или доставка упали, raw-данные сохранены для аудита.
- `data/output/YYYY-MM-DD.md` — HTML-дайджест байт-в-байт идентичный отправленному в канал Заказчика. Записывается только после успешной доставки.
- `data/hash-cache.json` — SHA-256 хэши доставленных постов (rolling window 14 дней) для кросс-прогонной дедупликации. Обновляется только после успешной доставки, чтобы при сбое следующий тик повторил те же посты.

Директория `data/` добавлена в `.gitignore` (не коммитится). На VDS persistence обеспечивается тем, что `data/` находится внутри рабочей директории `/opt/tg-parser-demo/data/`, которая переживает `git pull`. На Timeweb Cloud Apps — volume на `/app/data` (см. раздел Docker).
```

6. Добавить ссылки на оперативную документацию. Добавить новый раздел **«Оперативная документация»** перед «Ежедневный summary-лог»:

```
## Оперативная документация

- [docs/RUNBOOK.md](docs/RUNBOOK.md) — сценарии диагностики и восстановления: что делать при FloodWait, ChannelPrivateError, сбое DeepSeek, сбое alert-бота и т.д.
- [docs/CHANNELS.md](docs/CHANNELS.md) — checklist добавления/замены канала в `channels.yaml`, проверки подписки user-аккаунта и перезапуска daemon.
```

7. Раздел «Структура проекта»: обновить дерево файлов под v3.0:
   - В `src/` добавить: `pipeline.ts`, `schema.ts` (Zod-схема DigestJson), `dedup.ts` (hash-cache CRUD), `archive.ts` (writeRaw, writeOutput), `alert.ts` (sendAlert через Bot API), `logger.ts`, `run.ts` (daemon entrypoint с node-cron).
   - Убрать упоминание `run.ts` как «entrypoint» в старом месте — теперь `run.ts` — daemon с cron.
   - Добавить `data/` в дерево: `data/raw/`, `data/output/`, `data/hash-cache.json` (в .gitignore).
   - В `package.json` — «5 runtime-зависимостей».

8. В «Как проверить, что всё работает»:
   - Критерий 1: «`npm start` на 15 каналах» → «daemon запускается, лог `daemon started, schedule: ...Europe/Moscow`, в 20:15 MSK запускается tick без FloodWaitError».
   - Добавить критерий: «В `data/output/` появляется файл `YYYY-MM-DD.md` с тем же HTML, что пришёл в Telegram-канал».
   - Добавить критерий: «При намеренной ошибке (например, неверный DEEPSEEK_API_KEY) — алерт приходит в личку владельца за ≤60 секунд, в канал Заказчика ничего не уходит».

9. В «Известные ограничения»:
   - Убрать «Нет персистентности — одна и та же новость может попасть...» (hash-cache решил это в v3.0).
   - Оставить остальные ограничения.
   - Добавить: «SHA-256 hash-cache не распознаёт семантически одинаковые, но текстуально разные посты (e.g., репост с небольшой правкой). Semantic dedupe отложен в v4.0+.»

10. Ссылку `spec-app.md §12` в «Известные ограничения» заменить на `docs/RUNBOOK.md` (spec-app.md — внутренний документ, не для acceptance-пакета).

**Что НЕ трогать:**
- Разделы «Запуск в 3 команды», «Запуск на VPS (PM2)», «Деплой через Docker / Timeweb Cloud Apps», «Деплой на Timeweb VDS» — оставить без изменений (они актуальны).
- «Ежедневный summary-лог» — раздел актуален, оставить без изменений.
- «Troubleshooting» — оставить без изменений (ошибки те же).
- «Лицензия» — оставить.
- Все предупреждения про TG_SESSION (безопасность) — оставить.

**Итоговое состояние шапки README** (первые 6 строк):
```
# tg-parser-demo

Ежедневный (20:15 MSK) HTML-дайджест событий российского нефтегаза/нефтехимии за последние 24 часа в закрытом Telegram-канале. Дайджест разбит на 5 тематических секций (Бункер, Масла, Керосин, Нефтехимия, Битум) + блок «Упоминания компаний» (Роснефть/Лукойл/Газпром). Каждая цитата дословно присутствует в исходном посте — без галлюцинаций LLM.

Long-running daemon на Node.js + node-cron; на VPS работает под PM2 или Docker. Без БД, без внешнего крона — всё в одном процессе.
```
  </action>
  <verify>
- Открыть README.md и убедиться:
  - Нет строки начинающейся с `v2.0:` в шапке (в исходнике была вторая строка под заголовком)
  - Присутствует слово `zod` в списке зависимостей
  - Присутствуют `BOT_TOKEN_ALERTS` и `ALERTS_CHAT_ID` в секции env
  - Присутствует раздел «Архив прогонов» с упоминанием `data/raw/`, `data/output/`, `data/hash-cache.json`
  - Присутствуют ссылки на `docs/RUNBOOK.md` и `docs/CHANNELS.md`
  - Присутствует описание 5 категорий (Бункер, Масла, Керосин, Нефтехимия, Битум)
  </verify>
  <done>README.md полностью отражает v3.0: 5 категорий, 5 зависимостей, все env-переменные, data/ директория, ссылки на docs/, нет устаревших v2.0-артефактов в шапке. Годен как третий документ acceptance-пакета.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| README.md → reader | Только документация, нет исполняемого кода |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-j1h-01 | Information Disclosure | README.md | accept | Документ содержит только публичную архитектурную информацию; секреты (.env) не коммитятся — covered by .gitignore |
</threat_model>

<verification>
После завершения Task 1:

```bash
# Убедиться что файл изменён
git diff --stat README.md

# Проверить ключевые маркеры v3.0 в README
grep -n "zod" README.md
grep -n "BOT_TOKEN_ALERTS" README.md
grep -n "ALERTS_CHAT_ID" README.md
grep -n "hash-cache" README.md
grep -n "RUNBOOK" README.md
grep -n "CHANNELS.md" README.md
grep -n "Бункер\|Масла\|Керосин\|Нефтехимия\|Битум" README.md

# Убедиться что v2.0-лейбл в шапке убран
head -5 README.md
```
</verification>

<success_criteria>
1. README.md не содержит `v2.0:` в шапке и не описывает проект как v2.0
2. Счётчик зависимостей = 5, zod упомянут явно
3. Все 8 обязательных env-переменных перечислены включая BOT_TOKEN_ALERTS и ALERTS_CHAT_ID
4. Раздел «Архив прогонов» описывает data/raw/, data/output/, data/hash-cache.json с объяснением логики записи
5. Ссылки на docs/RUNBOOK.md и docs/CHANNELS.md присутствуют в разделе «Оперативная документация»
6. Структура дайджеста описана: 5 секций с emoji-заголовками + блок «Упоминания компаний»
7. README не содержит TODO, placeholder или ссылок на внутренние spec-файлы как источники для читателя
</success_criteria>

<output>
После завершения создать `.planning/quick/260430-j1h-readme-md-v3-0/260430-j1h-SUMMARY.md` по шаблону summary.md.
</output>
