---
phase: 260519-na3
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/bot.ts
  - src/__tests__/bot-handlers.test.ts
autonomous: true
requirements:
  - BOT-UI-01  # ReplyKeyboard 2×2 в каждом ответе бота (D-01, D-05)
  - BOT-UI-02  # setMyCommands при старте (7 команд, D-02, D-06)
  - BOT-UI-03  # /start handler (D-03, D-07)
  - BOT-UI-04  # /help handler (D-04, D-07)
  - BOT-UI-05  # Mapping эмодзи-кнопок → команды (D-01)

must_haves:
  truths:
    - "После любого ответа бота на команду пользователь видит компактную клавиатуру 2×2 с 4 кнопками: '📊 Статус загрузок', '🧠 Сделать сводку', '📋 Каналы новостей', '❓ Помощь'."
    - "При нажатии на кнопку '📊 Статус загрузок' бот реагирует так же, как на /upload_status."
    - "При нажатии на кнопку '🧠 Сделать сводку' бот реагирует так же, как на /summarize."
    - "При нажатии на кнопку '📋 Каналы новостей' бот реагирует так же, как на /channels."
    - "При нажатии на кнопку '❓ Помощь' бот показывает инструкцию (как /help)."
    - "В меню Telegram (иконка слева от поля ввода) у пользователя отображаются 7 команд: /start /help /upload_status /summarize /channels /add_channel /remove_channel."
    - "/start приветствует и показывает клавиатуру; /help печатает короткую инструкцию (как пользоваться xlsx-аплоадом, /summarize, /upload_status, /channels)."
    - "callback-flow /remove_channel (editMessageText / editMessageReplyMarkup) НЕ ломается reply-клавиатурой (inline-кнопки confirm/cancel остаются inline)."
    - "Non-allowlist пользователь, нажавший кнопку или /start — silent ignore (как и для остальных команд)."
    - "Если setMyCommands упал при старте — бот не падает, warn в лог, polling продолжается."
  artifacts:
    - path: "src/bot.ts"
      provides: "MAIN_KEYBOARD константа, расширенный sendReply (+ reply_markup по дефолту), /start handler, /help handler, mapping эмодзи-кнопок→команды в handleCommand, registerBotCommands() helper, вызов в startBot перед pollLoop."
      contains: "MAIN_KEYBOARD"
    - path: "src/__tests__/bot-handlers.test.ts"
      provides: "vitest-кейсы: handleCommand('/start') → sendMessage с приветствием + reply_markup.keyboard; handleCommand('/help') → инструкция + клавиатура; mapping '📊 Статус загрузок' → ветка /upload_status; non-allowlist /start silent ignore; существующие тесты /channels/add/remove проходят (regression — reply_markup присутствует, inline_keyboard в /remove_channel НЕ затронут)."
  key_links:
    - from: "src/bot.ts:sendReply (расширенный)"
      to: "MAIN_KEYBOARD"
      via: "reply_markup = { keyboard: MAIN_KEYBOARD, resize_keyboard: true, is_persistent: true } по дефолту"
      pattern: "reply_markup.*keyboard.*resize_keyboard"
    - from: "src/bot.ts:handleCommand"
      to: "нормализация text → cmd"
      via: "switch на эмодзи-text в начале handler'а перед '.startsWith(/)' проверкой"
      pattern: "📊 Статус загрузок|🧠 Сделать сводку|📋 Каналы новостей|❓ Помощь"
    - from: "src/bot.ts:startBot"
      to: "registerBotCommands(token) → tgFetch('setMyCommands', ...)"
      via: "вызов между deleteWebhook и входом в pollLoop, обёрнут в try/catch с log.warn"
      pattern: "setMyCommands"
---

<objective>
Bot UI affordances — ReplyKeyboard 2×2 при каждом ответе бота + setMyCommands при старте + новые /start и /help handlers + mapping кнопок-эмодзи в существующие команды.

Purpose: Дать оператору видимые элементы управления (нижняя клавиатура + меню команд слева) вместо «слепого» набора команд по памяти. Существующие 5 команд (/channels, /add_channel, /remove_channel, /upload_status, /summarize) и /remove_channel inline-keyboard flow НЕ меняются — только обёртываются + добавляются 2 новых handler'а.

Output: Расширенный src/bot.ts (MAIN_KEYBOARD, registerBotCommands, /start, /help, mapping); расширенный src/__tests__/bot-handlers.test.ts (новые describe-блоки + регрессия reply_markup в существующих); все vitest зелёные.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@.planning/STATE.md

<!-- Existing source — РАСШИРЯЕМ, не переписываем -->
@src/bot.ts
@src/__tests__/bot-handlers.test.ts

<interfaces>
<!-- Контракты, на которые опирается этот план. Извлечено из src/bot.ts. -->
<!-- Executor использует напрямую — никакой codebase exploration не требуется. -->

From src/bot.ts (extending these):
```typescript
// Существующие helpers — НЕ ТРОГАЕМ их сигнатуры:
async function tgFetch<T>(token: string, method: string, body: unknown): Promise<T>;
async function sendReply(token: string, chatId: number, replyToMessageId: number, text: string): Promise<void>;
async function sendReplyWithKeyboard(token: string, chatId: number, replyToMessageId: number, text: string, keyboard: TgInlineKeyboardButton[][]): Promise<void>;
async function sendPlain(token: string, chatId: number, text: string): Promise<void>;
async function sendMarkdown(token: string, chatId: number, text: string): Promise<void>;

// Существующие exports — НЕ ТРОГАЕМ:
export function parseAllowlist(envValue: string | undefined): Set<number>;
export function normalizeUsername(raw: string): string | null;
export async function handleCommand(token: string, msg: TgMessage, allowlist: Set<number>): Promise<void>;
export async function handleCallbackQuery(token: string, cb: TgCallbackQuery, allowlist: Set<number>): Promise<void>;
export async function handleDocument(token: string, msg: TgMessage, allowlist: Set<number>): Promise<void>;
export async function startBot(): Promise<void>;

// Telegram типы — РАСШИРЯЕМ TgReplyMarkup, чтобы поддержать reply-keyboard
// (помимо существующего inline_keyboard):
interface TgKeyboardButton { text: string; }
interface TgReplyKeyboardMarkup {
  keyboard: TgKeyboardButton[][];
  resize_keyboard?: boolean;
  is_persistent?: boolean;
  one_time_keyboard?: boolean;
}
// Существующий TgReplyMarkup для inline-кнопок оставляем как есть.
```

From .planning/STATE.md:
- Phase 2 (Bot Commands) ✅ completed; phase 3 (Web) ✅ completed.
- D-07/D-08: non-allowlist → silent ignore + log.info("[bot] denied: from=N cmd=...").
- D-09: sendReply использует reply_to_message_id, plain text без HTML/Markdown.
- D-11..D-14: inline-keyboard /remove_channel — confirm/cancel callback_data "rm:<username>:<action>".
- Стиль логгирования: log.info / log.warn / log.error (src/logger.ts → console.log с timestamp+префиксом).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: MAIN_KEYBOARD + расширенный sendReply (по умолчанию прикрепляет reply-клавиатуру) + типы TgReplyKeyboardMarkup</name>
  <files>src/bot.ts, src/__tests__/bot-handlers.test.ts</files>
  <behavior>
    Поведенческие изменения (теста-первые):
    - sendReply теперь ДОЛЖЕН по умолчанию прикладывать MAIN_KEYBOARD как reply_markup.
    - В body запроса sendMessage появляется поле `reply_markup` со структурой:
      `{ keyboard: [[{text: "📊 Статус загрузок"}, {text: "🧠 Сделать сводку"}],
                    [{text: "📋 Каналы новостей"}, {text: "❓ Помощь"}]],
         resize_keyboard: true, is_persistent: true }`
    - sendReplyWithKeyboard (для inline /remove_channel) НЕ ТРОГАЕМ — inline_keyboard остаётся inline.
    - sendPlain / sendMarkdown — по дефолту тоже добавляют MAIN_KEYBOARD (на каждом исходящем bot→user сообщении должна быть клавиатура, кроме editMessage*).

    Регрессионные тесты, которые должны падать ДО изменения и проходить ПОСЛЕ:
    - В тесте "/channels (BOT-01) → allowlist-пользователь видит список каналов":
      добавить `expect(sendCalls[0].reply_markup).toBeDefined();`
      `expect((sendCalls[0].reply_markup as any).keyboard).toEqual(<MAIN_KEYBOARD layout>);`
      `expect((sendCalls[0].reply_markup as any).resize_keyboard).toBe(true);`
      `expect((sendCalls[0].reply_markup as any).is_persistent).toBe(true);`
    - В тесте "/remove_channel (BOT-03)" reply_markup ОСТАЁТСЯ inline_keyboard (НЕ перезатёрт reply-keyboard):
      `expect(replyMarkup.inline_keyboard).toBeDefined();`
      `expect((replyMarkup as any).keyboard).toBeUndefined();`
  </behavior>
  <action>
1. **src/bot.ts — добавить тип и константу** (после блока TgReplyMarkup, ~line 75):
   ```typescript
   interface TgKeyboardButton { text: string; }
   interface TgReplyKeyboardMarkup {
     keyboard: TgKeyboardButton[][];
     resize_keyboard?: boolean;
     is_persistent?: boolean;
     one_time_keyboard?: boolean;
   }

   // BOT-UI-01 / D-01 / D-05: 2×2 нижняя клавиатура.
   // Текст кнопки = текст сообщения от юзера → handleCommand сматчит через mapping ниже.
   const MAIN_KEYBOARD: TgReplyKeyboardMarkup = {
     keyboard: [
       [{ text: "📊 Статус загрузок" }, { text: "🧠 Сделать сводку" }],
       [{ text: "📋 Каналы новостей" }, { text: "❓ Помощь" }],
     ],
     resize_keyboard: true,
     is_persistent: true,
   };
   ```

2. **Расширить sendReply** (~line 154) — по дефолту прикладывает MAIN_KEYBOARD:
   ```typescript
   async function sendReply(
     token: string,
     chatId: number,
     replyToMessageId: number,
     text: string
   ): Promise<void> {
     await tgFetch<{ ok: boolean }>(token, "sendMessage", {
       chat_id: chatId,
       reply_to_message_id: replyToMessageId,
       text,
       disable_web_page_preview: true,
       reply_markup: MAIN_KEYBOARD,
     });
   }
   ```

3. **Расширить sendPlain и sendMarkdown** — по дефолту тоже прикладывают MAIN_KEYBOARD (все bot→user исходящие должны иметь клавиатуру согласно D-05). Добавить `reply_markup: MAIN_KEYBOARD` в их tgFetch-вызовы.

4. **sendReplyWithKeyboard НЕ ТРОГАЕМ** — inline-keyboard /remove_channel должен остаться inline (НЕ перезатирать MAIN_KEYBOARD'ом).

5. **src/__tests__/bot-handlers.test.ts — обновить регрессионные ожидания**:
   - В тесте /channels (~line 364) добавить assertions про reply_markup.keyboard / resize_keyboard / is_persistent.
   - В тесте /remove_channel (~line 470) подтвердить что inline_keyboard остался, а keyboard НЕ появился.

6. Запустить vitest — все существующие тесты должны проходить (с новыми assertions).
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/bot-handlers.test.ts</automated>
  </verify>
  <done>
    - MAIN_KEYBOARD объявлен в src/bot.ts с layout 2×2 (4 кнопки в указанном порядке).
    - sendReply / sendPlain / sendMarkdown добавляют `reply_markup: MAIN_KEYBOARD` к каждому sendMessage.
    - sendReplyWithKeyboard НЕ затронут (inline_keyboard сохраняется в callback /remove_channel).
    - Все существующие тесты bot-handlers.test.ts зелёные + новые регрессионные assertions (reply_markup в /channels, отсутствие keyboard в inline-callback ответе /remove_channel) проходят.
    - `npx tsc --noEmit` без ошибок (тип TgReplyKeyboardMarkup корректен).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: /start и /help handlers + mapping эмодзи-кнопок → команды</name>
  <files>src/bot.ts, src/__tests__/bot-handlers.test.ts</files>
  <behavior>
    Поведенческие изменения (теста-первые):
    - `handleCommand` с `text="/start"` от allowlist-пользователя → sendMessage с приветствием
      («Привет! Я бот-помощник по битуму. Доступные действия — на клавиатуре ниже или через меню /»)
      + reply_markup = MAIN_KEYBOARD.
    - `handleCommand` с `text="/help"` от allowlist-пользователя → sendMessage с инструкцией
      (содержит: «xlsx», «биржа», «FCA», «/summarize», «/upload_status», «/channels»).
    - **Mapping эмодзи→команды**: handleCommand нормализует входной text ДО парсинга команды:
      * "📊 Статус загрузок"   → внутренне как "/upload_status"
      * "🧠 Сделать сводку"   → внутренне как "/summarize"
      * "📋 Каналы новостей"  → внутренне как "/channels"
      * "❓ Помощь"            → внутренне как "/help"
    - Non-allowlist /start или нажатие кнопки → silent ignore (без fetch) + log "[bot] denied: from=N cmd=/start".
    - Команды /start и /help ТАКЖЕ должны быть приняты с suffix @botname (как /channels@MyBot — см. существующий код:541).

    Тесты-первые (RED перед GREEN):
    1. `it("/start от allowlist → приветствие + reply_markup с keyboard 2×2", ...)`
    2. `it("/help от allowlist → инструкция содержит ключевые слова xlsx/биржа/FCA/summarize/upload_status/channels", ...)`
    3. `it("кнопка '📊 Статус загрузок' маршрутизируется как /upload_status", ...)` — withCurrentChannels не нужен; ожидаем sendMessage с текстом "Папка ..." (см. listWeek branch).
    4. `it("кнопка '🧠 Сделать сводку' маршрутизируется как /summarize", ...)` — ожидаем sendMessage с одной из веток handleSummarizeCommand (для пустой недели — текст "За эту неделю файлов не загружено...").
    5. `it("кнопка '📋 Каналы новостей' маршрутизируется как /channels", ...)` — ожидаем text содержит "Каналов:" или "Список каналов пуст".
    6. `it("кнопка '❓ Помощь' маршрутизируется как /help", ...)` — ожидаем инструкцию.
    7. `it("/start от non-allowlist — silent ignore + log [bot] denied: cmd=/start", ...)`.
    8. `it("/start@MyBot парсится как /start", ...)`.
  </behavior>
  <action>
1. **src/bot.ts — добавить mapping в начало handleCommand** (~line 530, СРАЗУ после извлечения userId):
   ```typescript
   // BOT-UI-05 / D-01: mapping эмодзи-кнопок → текст команды.
   // Reply-клавиатура шлёт текст кнопки как обычное сообщение от юзера;
   // нормализуем до /command, чтобы дальше handler работал унифицированно.
   const rawText = msg.text?.trim() ?? "";
   const EMOJI_BUTTON_MAP: Record<string, string> = {
     "📊 Статус загрузок": "/upload_status",
     "🧠 Сделать сводку": "/summarize",
     "📋 Каналы новостей": "/channels",
     "❓ Помощь": "/help",
   };
   const text = EMOJI_BUTTON_MAP[rawText] ?? rawText;
   if (!userId || !text.startsWith("/")) return;
   ```
   (Старая строка `const text = msg.text?.trim() ?? "";` заменяется на блок выше.)

2. **Добавить ветки `/start` и `/help` в handleCommand** (перед "Прочие команды игнорируются молча", ~line 641):
   ```typescript
   if (cmd === "/start") {
     await sendReply(
       token,
       msg.chat.id,
       msg.message_id,
       "Привет! Я бот-помощник по битуму. Доступные действия — на клавиатуре ниже или через меню / слева от поля ввода."
     );
     return;
   }
   if (cmd === "/help") {
     const helpText = [
       "Как пользоваться:",
       "",
       "1. Пришлите xlsx-файл (биржа / биржа-объёмы / FCA) — бот распознает тип по A1 и сохранит в data/uploads/YYYY-WW.",
       "2. После загрузки пары (биржа + FCA) — нажмите «🧠 Сделать сводку» (или /summarize) для LLM-обзора.",
       "3. «📊 Статус загрузок» (/upload_status) — что лежит в текущей неделе.",
       "4. «📋 Каналы новостей» (/channels) — управление списком каналов для ежедневного дайджеста (отдельный пайплайн).",
       "",
       "Дополнительно: /add_channel @name, /remove_channel @name.",
     ].join("\n");
     await sendReply(token, msg.chat.id, msg.message_id, helpText);
     return;
   }
   ```

3. **src/__tests__/bot-handlers.test.ts — добавить новые describe-блоки** (после блока "handleCommand /remove_channel"):

   ```typescript
   describe("handleCommand /start (BOT-UI-03)", () => {
     it("/start от allowlist → приветствие + reply_markup с keyboard 2×2", async () => {
       const allowlist = new Set([111]);
       const msg = { message_id: 1, chat: { id: 555 }, from: { id: 111 }, text: "/start" };
       await handleCommand("token", msg, allowlist);
       const calls = fetchCallsTo("sendMessage");
       expect(calls.length).toBe(1);
       expect(calls[0].text).toMatch(/Привет/i);
       const rm = calls[0].reply_markup as { keyboard?: unknown[][] };
       expect(rm.keyboard).toBeDefined();
       expect(rm.keyboard).toHaveLength(2);
       expect((rm.keyboard as Array<Array<{ text: string }>>)[0][0].text).toBe("📊 Статус загрузок");
     });

     it("/start@MyBot парсится как /start", async () => {
       const allowlist = new Set([111]);
       const msg = { message_id: 1, chat: { id: 555 }, from: { id: 111 }, text: "/start@MyBot" };
       await handleCommand("token", msg, allowlist);
       expect(fetchCallsTo("sendMessage").length).toBe(1);
     });

     it("/start от non-allowlist → silent ignore + log [bot] denied: cmd=/start", async () => {
       const allowlist = new Set([111]);
       const msg = { message_id: 1, chat: { id: 555 }, from: { id: 999 }, text: "/start" };
       await handleCommand("token", msg, allowlist);
       expect(globalThis.fetch).not.toHaveBeenCalled();
       expect(consoleLogContains("[bot] denied:")).toBe(true);
     });
   });

   describe("handleCommand /help (BOT-UI-04)", () => {
     it("/help → инструкция содержит ключевые слова (xlsx, биржа, FCA, /summarize, /upload_status, /channels)", async () => {
       const allowlist = new Set([111]);
       const msg = { message_id: 1, chat: { id: 555 }, from: { id: 111 }, text: "/help" };
       await handleCommand("token", msg, allowlist);
       const calls = fetchCallsTo("sendMessage");
       expect(calls.length).toBe(1);
       const t = calls[0].text as string;
       expect(t).toMatch(/xlsx/i);
       expect(t).toMatch(/биржа/i);
       expect(t).toMatch(/FCA/i);
       expect(t).toContain("/summarize");
       expect(t).toContain("/upload_status");
       expect(t).toContain("/channels");
     });
   });

   describe("handleCommand emoji-button mapping (BOT-UI-05)", () => {
     it("'📋 Каналы новостей' маршрутизируется как /channels", async () => {
       mockedLoadChannels.mockReturnValue([{ username: "a" }]);
       const allowlist = new Set([111]);
       const msg = { message_id: 1, chat: { id: 555 }, from: { id: 111 }, text: "📋 Каналы новостей" };
       await handleCommand("token", msg, allowlist);
       const calls = fetchCallsTo("sendMessage");
       expect(calls.length).toBe(1);
       expect(calls[0].text).toContain("Каналов:");
     });

     it("'❓ Помощь' маршрутизируется как /help", async () => {
       const allowlist = new Set([111]);
       const msg = { message_id: 1, chat: { id: 555 }, from: { id: 111 }, text: "❓ Помощь" };
       await handleCommand("token", msg, allowlist);
       const calls = fetchCallsTo("sendMessage");
       expect(calls.length).toBe(1);
       expect(calls[0].text).toMatch(/xlsx/i);
     });

     it("'📊 Статус загрузок' маршрутизируется как /upload_status (текст 'Папка ...')", async () => {
       const allowlist = new Set([111]);
       const msg = { message_id: 1, chat: { id: 555 }, from: { id: 111 }, text: "📊 Статус загрузок" };
       await handleCommand("token", msg, allowlist);
       const calls = fetchCallsTo("sendMessage");
       expect(calls.length).toBe(1);
       expect(calls[0].text).toMatch(/Папка/);
     });

     it("'🧠 Сделать сводку' маршрутизируется как /summarize (для пустой недели — текст 'файлов не загружено')", async () => {
       const allowlist = new Set([111]);
       const msg = { message_id: 1, chat: { id: 555 }, from: { id: 111 }, text: "🧠 Сделать сводку" };
       // Пустая неделя → ветка 1 handleSummarizeCommand.
       await handleCommand("token", msg, allowlist);
       const calls = fetchCallsTo("sendMessage");
       expect(calls.length).toBe(1);
       expect(calls[0].text).toMatch(/файлов не загружено/i);
     });

     it("обычное текстовое сообщение без / и без эмодзи-кнопки → ignored", async () => {
       const allowlist = new Set([111]);
       const msg = { message_id: 1, chat: { id: 555 }, from: { id: 111 }, text: "просто привет" };
       await handleCommand("token", msg, allowlist);
       expect(globalThis.fetch).not.toHaveBeenCalled();
     });
   });
   ```

4. Запустить vitest — все тесты (старые + новые) должны проходить.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/bot-handlers.test.ts</automated>
  </verify>
  <done>
    - В src/bot.ts: EMOJI_BUTTON_MAP применяется в начале handleCommand; ветки `/start` и `/help` добавлены перед "Прочие команды игнорируются".
    - В src/__tests__/bot-handlers.test.ts: три новых describe-блока (handleCommand /start, /help, emoji-button mapping) — все тесты зелёные.
    - Non-allowlist /start даёт silent ignore + log "[bot] denied: cmd=/start" — проверено в тесте.
    - Существующие тесты (parseAllowlist, normalizeUsername, parseRemoveCallbackData, addChannel, removeChannel, listChannels, handleCommand allowlist/channels/add/remove, handleCallbackQuery) продолжают проходить.
    - `npx tsc --noEmit` без ошибок.
  </done>
</task>

<task type="auto">
  <name>Task 3: registerBotCommands (setMyCommands) в startBot между deleteWebhook и pollLoop</name>
  <files>src/bot.ts</files>
  <action>
1. **src/bot.ts — добавить helper registerBotCommands** перед функцией pollLoop (~line 799):
   ```typescript
   /**
    * BOT-UI-02 / D-02 / D-06: при старте регистрируем 7 команд для меню Telegram
    * (иконка слева от поля ввода). Вызывается ОДИН раз при старте бота.
    *
    * На ошибку — warn, не падать (D-06): меню не критично для работы;
    * pollLoop стартует независимо.
    */
   async function registerBotCommands(token: string): Promise<void> {
     const commands = [
       { command: "start", description: "Запустить бота / показать меню" },
       { command: "help", description: "Инструкция" },
       { command: "upload_status", description: "Статус загрузок за текущую неделю" },
       { command: "summarize", description: "LLM-сводка по загруженным xlsx" },
       { command: "channels", description: "Список каналов" },
       { command: "add_channel", description: "Добавить канал" },
       { command: "remove_channel", description: "Удалить канал" },
     ];
     try {
       await tgFetch<{ ok: boolean }>(token, "setMyCommands", { commands });
       log.info(`[bot] setMyCommands ok (${commands.length} commands)`);
     } catch (err) {
       log.warn(`[bot] setMyCommands failed: ${(err as Error).message}`);
     }
   }
   ```

2. **Вызвать registerBotCommands внутри pollLoop** — СРАЗУ после `deleteWebhook ok`, ДО первого `pollOnce`. Найти блок (~line 805–813):
   ```typescript
   try {
     await tgFetch<{ ok: boolean }>(token, "deleteWebhook", { drop_pending_updates: false });
     log.info("[bot] deleteWebhook ok (drop_pending_updates=false)");
   } catch (err) {
     log.error(`[bot] deleteWebhook failed: ${(err as Error).message}`);
   }
   ```
   Добавить СРАЗУ ПОСЛЕ:
   ```typescript
   // BOT-UI-02: регистрируем команды для меню Telegram (без блокировки polling).
   await registerBotCommands(token);
   ```
   (registerBotCommands внутри себя try/catch'ит, поэтому await безопасен.)

3. **Manual smoke** (executor выполняет лично перед коммитом):
   - `npm run build` (или `npx tsc --noEmit`) — без ошибок.
   - `npx vitest run` — все тесты зелёные.
   - Опционально: `npm start` (если TG_BOT_TOKEN задан) → в логе должна появиться строка `[bot] setMyCommands ok (7 commands)` между `deleteWebhook ok` и первым `getUpdates`. Открыть DM с ботом, нажать иконку меню слева от поля ввода — увидеть 7 команд. Нажать /start — увидеть клавиатуру 2×2.

4. **Никаких новых тестов** для этой задачи: setMyCommands — простой fire-and-forget вызов tgFetch, та же сетевая обвязка покрыта существующими тестами через моки fetch. Поведение «warn на ошибку, не падать» — стандартный try/catch паттерн (как answerCallbackQuery), отдельный тест избыточен.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/bot-handlers.test.ts && npx tsc --noEmit</automated>
  </verify>
  <done>
    - В src/bot.ts: функция registerBotCommands определена (7 команд с описаниями), вызывается из pollLoop ОДИН РАЗ между deleteWebhook и первым pollOnce.
    - При успехе — log.info("[bot] setMyCommands ok (7 commands)"); при ошибке — log.warn(...), polling продолжается.
    - `npx tsc --noEmit` без ошибок.
    - `npx vitest run` — все тесты зелёные.
    - Manual smoke (executor): при `npm start` с задеплоенным токеном — в Telegram меню (иконка слева от поля) видны 7 команд; нажатие /start показывает клавиатуру 2×2.
  </done>
</task>

</tasks>

<verification>
- Все три задачи закоммичены отдельными атомарными коммитами (`feat(quick-260519-na3): task N — <название>`).
- Между задачами `npx vitest run src/__tests__/bot-handlers.test.ts` — зелёный.
- После всех задач: `npx tsc --noEmit` без ошибок.
- Manual smoke checklist (выполняется после Task 3 локально / на деплое):
  1. Открыть DM с ботом → нажать иконку меню (слева от поля ввода) → увидеть 7 команд (/start /help /upload_status /summarize /channels /add_channel /remove_channel) с описаниями.
  2. Отправить /start → увидеть приветствие + клавиатуру 2×2 под полем ввода.
  3. Нажать «📊 Статус загрузок» → ответ как от /upload_status.
  4. Нажать «🧠 Сделать сводку» → ответ как от /summarize (для пустой недели — «файлов не загружено…»).
  5. Нажать «📋 Каналы новостей» → список каналов.
  6. Нажать «❓ Помощь» → инструкция.
  7. После каждого ответа клавиатура остаётся (`is_persistent: true`).
  8. /remove_channel @name → inline-кнопки confirm/cancel работают, нижняя клавиатура НЕ затёрта.
  9. От non-allowlist аккаунта /start → бот молчит (silent ignore).
</verification>

<success_criteria>
- 5 truth'ов из must_haves выполнены (4 кнопки маршрутизируют в правильные ветки, /start приветствует и показывает клавиатуру, /help печатает инструкцию, setMyCommands регистрирует 7 команд).
- Inline-callback flow /remove_channel НЕ сломан (confirm/cancel редактируют сообщение, нижняя клавиатура остаётся независимо).
- Non-allowlist пользователи продолжают получать silent ignore для /start и для кнопок (текст-эмодзи).
- На любую ошибку setMyCommands при старте — warn + polling запускается (resilience).
- vitest полностью зелёный, tsc без ошибок.
- Никаких новых runtime-зависимостей в package.json.
</success_criteria>

<output>
After completion, create `.planning/quick/260519-na3-bot-ui-replykeyboard-2x2-setmycommands-s/260519-na3-SUMMARY.md` per the standard summary template.
</output>
