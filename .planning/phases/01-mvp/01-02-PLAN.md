---
phase: 01-mvp
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types.ts
  - src/telegram.ts
  - src/summarize.ts
autonomous: true
requirements:
  - FETCH-01
  - FETCH-02
  - FETCH-03
  - FETCH-04
  - FETCH-05
  - FETCH-06
  - SUM-01
  - SUM-02
  - SUM-03
  - SUM-04

must_haves:
  truths:
    - "Модуль `src/telegram.ts` экспортирует `createClient()`, `fetchLast24h(client, username, opts)` и тип `Post`"
    - "`createClient()` создаёт `TelegramClient` с `StringSession(process.env.TG_SESSION)`, `deviceModel: \"Desktop\"`, `systemVersion: \"Windows 11\"`, `appVersion: \"5.3.0 x64\"`, `langCode: \"ru\"`, `systemLangCode: \"ru\"`"
    - "`fetchLast24h` итерирует `client.iterMessages(username, { limit, offsetDate: 0, reverse: false })`, останавливается при `msg.date < sinceUnix`, возвращает массив `{ channelUsername, messageId, postedAt, text, url }`"
    - "FloodWaitError → sleep(err.seconds*1000+2000) + один retry; второй подряд FloodWait → ошибка пробрасывается наверх"
    - "ChannelPrivateError / UsernameNotOccupiedError / UsernameInvalidError → warn + пустой массив, прогон не падает"
    - "Модуль `src/summarize.ts` экспортирует `summarize(posts): Promise<string>` и вызывает DeepSeek одним батчем через openai SDK с `response_format: { type: \"json_object\" }`"
    - "После JSON.parse каждый `item.keyQuote` валидируется серверной проверкой `sourceText.includes(item.keyQuote.trim())` через `Map<url, Post>`"
    - "Нарушение валидации (нет match / url не найден) → skip + logger.warn в stderr, итоговый HTML собирается из оставшихся записей"
    - "`renderHtml` возвращает HTML-строку с шапкой `<b>Нефтегаз — {date}</b>\\n<i>{N} постов из {K} каналов за 24ч</i>`, секциями `<b>Заголовок</b>` и буллетами по формату D-11"
    - "`escapeHtml` экранирует `<`, `>`, `&` для summary, keyQuote, channel; url валидируется через `new URL()`"
    - "`npx tsc --noEmit` exits 0 после создания всех трёх файлов"
  artifacts:
    - path: "src/types.ts"
      provides: "общие типы Post, DigestJson, DigestItem, DigestSection"
      exports: ["Post", "DigestJson", "DigestItem", "DigestSection"]
    - path: "src/telegram.ts"
      provides: "GramJS client factory + fetchLast24h + anti-ban identity"
      exports: ["createClient", "fetchLast24h", "sleep", "randomInt"]
      contains: "deviceModel"
    - path: "src/summarize.ts"
      provides: "DeepSeek call + keyQuote validation + HTML rendering"
      exports: ["summarize", "renderHtml", "escapeHtml"]
      contains: "includes"
  key_links:
    - from: "src/telegram.ts createClient"
      to: "process.env.TG_SESSION / TG_API_ID / TG_API_HASH"
      via: "StringSession + TelegramClient constructor"
      pattern: "StringSession\\(process\\.env\\.TG_SESSION"
    - from: "src/telegram.ts createClient"
      to: "anti-ban identity"
      via: "хардкод deviceModel/systemVersion/appVersion/langCode"
      pattern: "deviceModel:\\s*\"Desktop\""
    - from: "src/summarize.ts summarize"
      to: "DeepSeek chat.completions"
      via: "OpenAI SDK с baseURL=DEEPSEEK_BASE_URL"
      pattern: "chat\\.completions\\.create"
    - from: "src/summarize.ts validate"
      to: "каждый item.keyQuote ↔ source text"
      via: "Map<url, Post> + sourceText.includes(item.keyQuote.trim())"
      pattern: "\\.includes\\("
    - from: "src/summarize.ts renderHtml"
      to: "HTML output + escape"
      via: "escapeHtml(summary) + escapeHtml(keyQuote) + escapeHtml(channel) + new URL(url)"
      pattern: "escapeHtml"
---

<objective>
Реализовать две серверных функции MVP-пайплайна:

1. **`src/telegram.ts`** — GramJS user-клиент с правдоподобной идентичностью (Desktop/Windows 11, ru) и `fetchLast24h(client, username, opts)`, которая читает последние 24 часа одного канала, уважает `MAX_MESSAGES_PER_CHANNEL`, обрабатывает FloodWaitError и частные ошибки каналов, возвращает массив `Post`.

2. **`src/summarize.ts`** — DeepSeek-вызов одним батчем (`response_format: json_object`), ручная валидация JSON-ответа, СЕРВЕРНАЯ проверка дословности `keyQuote` в исходных постах, рендер HTML с шапкой, секциями и буллетами, экранирование пользовательского текста.

Purpose: защитить Core Value («каждая цитата дословно в исходнике») кодом, а не только промптом (D-01..D-05). Обеспечить anti-ban правдоподобность клиента (D-06..D-08) и строгий HTML-формат дайджеста (D-09..D-13).

Output:
- `src/types.ts` — общие интерфейсы `Post`, `DigestJson`, `DigestItem`, `DigestSection` (чтобы plan 03 мог их переиспользовать).
- `src/telegram.ts` — `createClient()`, `fetchLast24h()`, вспомогательные `sleep()` и `randomInt()`.
- `src/summarize.ts` — `summarize(posts)`, `renderHtml(digest, posts)`, `escapeHtml(s)`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-mvp/01-CONTEXT.md
@spec-app.md
@CLAUDE.md

<interfaces>
Выставляемые контракты (plan 03 импортирует отсюда):

src/types.ts:
    export interface Post {
      channelUsername: string;
      messageId: number;
      postedAt: string;   // ISO 8601
      text: string;
      url: string;        // "https://t.me/<username>/<messageId>"
    }

    export interface DigestItem {
      summary: string;   // до 250 символов
      keyQuote: string;  // дословная подстрока Post.text
      url: string;       // Post.url
      channel: string;   // Post.channelUsername (без @)
    }

    export interface DigestSection {
      title: string;
      items: DigestItem[];
    }

    export interface DigestJson {
      generatedAt: string;
      sections: DigestSection[];
    }

src/telegram.ts:
    export function createClient(): TelegramClient;
    export async function fetchLast24h(
      client: TelegramClient,
      username: string,
      opts: { limit: number; windowHours: number }
    ): Promise<Post[]>;
    export function sleep(ms: number): Promise<void>;
    export function randomInt(min: number, max: number): number;

src/summarize.ts:
    export async function summarize(posts: Post[]): Promise<string /* html */>;
    export function renderHtml(digest: DigestJson, posts: Post[]): string;
    export function escapeHtml(s: string): string;

Зависимости от plan 01 (frontmatter):
- process.env переменные: TG_API_ID, TG_API_HASH, TG_SESSION, DEEPSEEK_API_KEY, DEEPSEEK_MODEL, DEEPSEEK_BASE_URL, LOG_LEVEL
- package.json уже содержит `telegram` и `openai`
- tsconfig.json уже содержит strict + moduleResolution bundler

Locked decisions из 01-CONTEXT.md, применимые в этом плане:

D-01: серверная верификация keyQuote обязательна.
D-02: алгоритм = sourceText.includes(item.keyQuote.trim()) — строго + trim по краям, БЕЗ case-insensitive, БЕЗ whitespace-нормализации внутри.
D-03: item → post маппится по item.url через Map<url, Post>; url не совпал — skip.
D-04: нарушение = skip + logger.warn (channel, messageId, keyQuote, 60-char сниппет text) в stderr.
D-05: только stderr, не писать в файл.
D-06: deviceModel="Desktop", systemVersion="Windows 11", appVersion="5.3.0 x64", langCode="ru", systemLangCode="ru".
D-07: захардкод в src/telegram.ts, не в .env, не в channels.yaml.
D-08: connectionRetries и useWSS — дефолты GramJS (не переопределяем).
D-09: шапка `<b>Нефтегаз — {DD MMM YYYY}</b>\n<i>{N} постов из {K} каналов за 24ч</i>\n\n`. N = общее число собранных постов, K = число каналов с ≥1 постом.
D-10: заголовки тем `<b>Заголовок</b>`, без emoji, без нумерации.
D-11: буллет одной строкой: `• {summary} — <i>«{keyQuote}»</i> — <a href="{url}">@{channel}</a>`.
D-12: разделитель между секциями — `\n\n`.
D-13: экранирование <, >, & для summary/keyQuote/channel; url через new URL(); рендер — конкатенация, без Handlebars.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Создать src/types.ts и src/telegram.ts (GramJS client + fetchLast24h + anti-ban)</name>
  <files>src/types.ts, src/telegram.ts</files>

  <read_first>
    - spec-app.md §7.3 (src/telegram.ts — createClient + fetchLast24h), §9 (anti-ban чек-лист 7 пунктов)
    - .planning/REQUIREMENTS.md (FETCH-01..FETCH-06)
    - .planning/phases/01-mvp/01-CONTEXT.md (§Implementation Decisions — D-06, D-07, D-08 — identity GramJS-клиента)
    - package.json (убедиться что `telegram` в dependencies, `@types/node` в devDependencies)
    - tsconfig.json (strict + moduleResolution bundler)
  </read_first>

  <action>
Создай два файла.

**1. `src/types.ts`** — общие интерфейсы для всего пайплайна:

    // src/types.ts — общие типы для пайплайна tg-parser-demo.

    export interface Post {
      channelUsername: string;  // без "@", как в channels.yaml
      messageId: number;
      postedAt: string;         // ISO 8601, например "2026-04-21T10:15:03.000Z"
      text: string;             // plain text сообщения (без entities)
      url: string;              // "https://t.me/<username>/<messageId>"
    }

    export interface DigestItem {
      summary: string;          // 1-2 предложения, до 250 символов
      keyQuote: string;         // дословная подстрока Post.text
      url: string;              // должен совпасть с Post.url одного из собранных постов
      channel: string;          // Post.channelUsername (без "@")
    }

    export interface DigestSection {
      title: string;            // короткий заголовок темы
      items: DigestItem[];
    }

    export interface DigestJson {
      generatedAt: string;      // ISO 8601, заполняется DeepSeek-ом
      sections: DigestSection[];
    }

**2. `src/telegram.ts`** — GramJS client factory + `fetchLast24h` + утилиты sleep/randomInt + глобальные хардкод-константы identity (D-07: именно в src/telegram.ts, не в .env).

Точная реализация:

    // src/telegram.ts — GramJS user-client с anti-ban identity + fetchLast24h.
    // Идентичность клиента (D-06, D-07): захардкожена здесь, НЕ в .env и НЕ в channels.yaml.

    import { TelegramClient, Api } from "telegram";
    import { StringSession } from "telegram/sessions/index.js";
    import { FloodWaitError } from "telegram/errors/index.js";
    import type { Post } from "./types.js";

    // D-06: правдоподобная идентичность Telegram Desktop на Windows 11, RU.
    const CLIENT_IDENTITY = {
      deviceModel: "Desktop",
      systemVersion: "Windows 11",
      appVersion: "5.3.0 x64",
      langCode: "ru",
      systemLangCode: "ru",
    } as const;

    /** Пауза в миллисекундах. */
    export function sleep(ms: number): Promise<void> {
      return new Promise((resolve) => setTimeout(resolve, ms));
    }

    /** Случайное целое в [min, max] включительно. */
    export function randomInt(min: number, max: number): number {
      return Math.floor(Math.random() * (max - min + 1)) + min;
    }

    /**
     * Создаёт GramJS TelegramClient с persistent StringSession из TG_SESSION.
     * D-07: CLIENT_IDENTITY захардкожена в этом файле.
     * D-08: connectionRetries/useWSS — дефолты GramJS (не переопределяем).
     */
    export function createClient(): TelegramClient {
      const apiId = Number(process.env.TG_API_ID);
      const apiHash = process.env.TG_API_HASH;
      const sessionString = process.env.TG_SESSION;

      if (!Number.isFinite(apiId) || apiId <= 0) {
        throw new Error("TG_API_ID не задан или невалиден (ожидается положительное число).");
      }
      if (!apiHash) {
        throw new Error("TG_API_HASH не задан.");
      }
      if (!sessionString) {
        throw new Error("TG_SESSION не задан. Запусти `npm run login` и скопируй StringSession в .env.");
      }

      const session = new StringSession(sessionString);
      return new TelegramClient(session, apiId, apiHash, {
        ...CLIENT_IDENTITY,
      });
    }

    /**
     * Читает последние windowHours часов канала username.
     * FETCH-02: sinceUnix = now - windowHours*3600; итерация останавливается при msg.date < sinceUnix.
     * FETCH-03: возвращает { channelUsername, messageId, postedAt, text, url }.
     * FETCH-04: FloodWaitError — sleep(err.seconds*1000+2000) + один retry; второй подряд — пробросить.
     * FETCH-05: ChannelPrivateError / UsernameNotOccupiedError / UsernameInvalidError — warn + [].
     */
    export async function fetchLast24h(
      client: TelegramClient,
      username: string,
      opts: { limit: number; windowHours: number }
    ): Promise<Post[]> {
      const { limit, windowHours } = opts;
      const sinceUnix = Math.floor(Date.now() / 1000) - windowHours * 3600;

      const tryFetch = async (): Promise<Post[]> => {
        const results: Post[] = [];
        // iterMessages возвращает новейшие сверху (reverse: false).
        const iter = client.iterMessages(username, {
          limit,
          offsetDate: 0,
          reverse: false,
        });
        for await (const msg of iter) {
          // Тип msg — Api.Message. Поля: id (number), date (unix seconds), message (string).
          const date = typeof (msg as any).date === "number" ? (msg as any).date : 0;
          if (!date) continue;
          if (date < sinceUnix) break; // FETCH-02: остановка за окном

          const text = typeof (msg as any).message === "string" ? (msg as any).message : "";
          if (!text) continue; // Скипаем репост-только-медиа (Claude's Discretion в CONTEXT.md)

          const messageId = typeof (msg as any).id === "number" ? (msg as any).id : Number((msg as any).id);
          const url = `https://t.me/${username}/${messageId}`;
          results.push({
            channelUsername: username,
            messageId,
            postedAt: new Date(date * 1000).toISOString(),
            text,
            url,
          });
        }
        return results;
      };

      let retried = false;
      // Внешний loop — максимум 2 попытки (один retry на FloodWait).
      while (true) {
        try {
          return await tryFetch();
        } catch (err: unknown) {
          const name = (err as { className?: string; constructor?: { name?: string } } | null)?.constructor?.name ?? "";
          const errorMessage = (err as Error)?.message ?? String(err);

          // FETCH-05: частные ошибки канала — warn + пустой массив.
          if (
            name === "ChannelPrivateError" ||
            name === "UsernameNotOccupiedError" ||
            name === "UsernameInvalidError" ||
            errorMessage.includes("CHANNEL_PRIVATE") ||
            errorMessage.includes("USERNAME_NOT_OCCUPIED") ||
            errorMessage.includes("USERNAME_INVALID")
          ) {
            console.warn(`[telegram] channel skipped: ${username} — ${name || errorMessage}`);
            return [];
          }

          // FETCH-04: FloodWait — один retry.
          if (err instanceof FloodWaitError || name === "FloodWaitError") {
            const seconds = (err as unknown as { seconds?: number }).seconds ?? 30;
            if (retried) {
              console.error(`[telegram] second FloodWait on ${username}, aborting (${seconds}s).`);
              throw err;
            }
            console.warn(`[telegram] FloodWait on ${username}: sleeping ${seconds}s + 2s, then retry.`);
            await sleep(seconds * 1000 + 2000);
            retried = true;
            continue;
          }

          // Прочие ошибки — пробрасываем наверх (поймается в src/run.ts).
          throw err;
        }
      }
    }

Важные детали:
- `from "./types.js"` (не `./types`) — `moduleResolution: bundler` + ESM требуют `.js` в импортах TS.
- `from "telegram/sessions/index.js"` / `from "telegram/errors/index.js"` — такие же ESM-safe пути, используемые GramJS.
- `(msg as any)` — GramJS типы `Api.Message` не полностью покрывают поле `message`; каст безопасен (проверяем `typeof` внутри).
- Анти-ban (D-06..D-08): `CLIENT_IDENTITY` — `as const`, спред в constructor, никаких переопределений connectionRetries/useWSS.
- FETCH-06 (jitter между каналами) — реализуется НЕ здесь, а в `src/run.ts` (plan 03), где мы итерируем по списку каналов.
- Логи через `console.warn` / `console.error` (D-05: stderr only); `console.log` НЕ используется.
- Скип постов без текста — Claude's Discretion из CONTEXT.md (репост-только-медиа, text=""/undefined). Фиксируем явно: text пусто → пропускаем до LLM.

После записи проверь `npx tsc --noEmit` — 0 ошибок типов.
  </action>

  <verify>
    <automated>test -f src/types.ts && test -f src/telegram.ts && grep -q "export interface Post" src/types.ts && grep -q "export interface DigestJson" src/types.ts && grep -q 'deviceModel: "Desktop"' src/telegram.ts && grep -q 'systemVersion: "Windows 11"' src/telegram.ts && grep -q 'appVersion: "5.3.0 x64"' src/telegram.ts && grep -q 'langCode: "ru"' src/telegram.ts && grep -q 'systemLangCode: "ru"' src/telegram.ts && grep -q "StringSession(sessionString)" src/telegram.ts && grep -q "iterMessages" src/telegram.ts && grep -q "FloodWait" src/telegram.ts && grep -q "ChannelPrivateError\|CHANNEL_PRIVATE" src/telegram.ts && npx tsc --noEmit</automated>
  </verify>

  <acceptance_criteria>
    - `src/types.ts` экспортирует `Post`, `DigestItem`, `DigestSection`, `DigestJson` (grep -q "export interface <Name>" для каждого)
    - `Post` имеет поля `channelUsername: string`, `messageId: number`, `postedAt: string`, `text: string`, `url: string`
    - `src/telegram.ts` импортирует `TelegramClient` и `StringSession` и `FloodWaitError` из пакета `telegram`
    - `src/telegram.ts` содержит строки (дословно, D-06): `deviceModel: "Desktop"`, `systemVersion: "Windows 11"`, `appVersion: "5.3.0 x64"`, `langCode: "ru"`, `systemLangCode: "ru"` (grep -q для каждой)
    - `src/telegram.ts` НЕ читает identity из process.env (grep -v результат для: `process\.env\.[A-Z_]*DEVICE\|process\.env\.[A-Z_]*SYSTEM\|process\.env\.[A-Z_]*APP_VERSION` — ноль совпадений, D-07)
    - `src/telegram.ts` НЕ устанавливает `connectionRetries: <число>` в объекте опций `TelegramClient` (D-08 — дефолты). Допустимо полное отсутствие поля (`grep -c "connectionRetries" src/telegram.ts` = 0) ИЛИ только в scripts/login.ts (этот файл — не src/telegram.ts). Если есть в src/telegram.ts — ошибка.
    - `src/telegram.ts` экспортирует `createClient`, `fetchLast24h`, `sleep`, `randomInt` (grep -q "export function")
    - `fetchLast24h` содержит вычисление `sinceUnix` как `Math.floor(Date.now() / 1000) - windowHours * 3600` (FETCH-02)
    - `fetchLast24h` содержит проверку `date < sinceUnix` и `break` из цикла (FETCH-02)
    - `fetchLast24h` конструирует url как `https://t.me/${username}/${messageId}` (FETCH-03)
    - `fetchLast24h` содержит обработку FloodWaitError с `sleep(` и `seconds` (FETCH-04)
    - `fetchLast24h` содержит обработку `ChannelPrivateError` / `UsernameNotOccupiedError` / `UsernameInvalidError` или соответствующих строк `CHANNEL_PRIVATE` / `USERNAME_NOT_OCCUPIED` / `USERNAME_INVALID` (FETCH-05)
    - `fetchLast24h` логирует ошибки только через `console.warn` или `console.error` (D-05, grep -v -q "writeFile" src/telegram.ts)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>

  <done>
    `src/types.ts` экспортирует все 4 интерфейса. `src/telegram.ts` создаёт GramJS-клиента с захардкоженной идентичностью (D-06, D-07), использует дефолты connectionRetries/useWSS (D-08), корректно обрабатывает FloodWait (FETCH-04) и частные ошибки каналов (FETCH-05), возвращает массив Post с правильным url (FETCH-03). `npx tsc --noEmit` без ошибок.
  </done>
</task>

<task type="auto">
  <name>Task 2: Создать src/summarize.ts — DeepSeek + валидация + renderHtml</name>
  <files>src/summarize.ts</files>

  <read_first>
    - spec-app.md §7.4 (summarize — DeepSeek + validate + renderHtml), §8 (SYSTEM_PROMPT + JSON-схема ответа)
    - .planning/REQUIREMENTS.md (SUM-01, SUM-02, SUM-03, SUM-04)
    - .planning/phases/01-mvp/01-CONTEXT.md (§Implementation Decisions — D-01, D-02, D-03, D-04, D-05 — верификация keyQuote; D-09, D-10, D-11, D-12, D-13 — HTML-формат)
    - src/types.ts (Post, DigestJson, DigestItem, DigestSection — должны уже существовать после Task 1)
    - package.json (убедиться что `openai` в dependencies)
  </read_first>

  <action>
Создай `src/summarize.ts`. Файл имеет три ответственности:

1. **`summarize(posts)`** — делает batch-запрос к DeepSeek через openai SDK (SUM-01), получает JSON, парсит, валидирует, верифицирует keyQuote дословно (D-01..D-05), зовёт renderHtml.
2. **`renderHtml(digest, posts)`** — строит HTML-строку с шапкой, секциями, буллетами (D-09..D-13), экранирует пользовательский текст.
3. **`escapeHtml(s)`** — экранирует `<`, `>`, `&` (D-13, SUM-04).

Точная реализация (TypeScript, ESM):

    // src/summarize.ts — DeepSeek-суммаризация + серверная проверка дословности keyQuote + HTML-рендер.

    import OpenAI from "openai";
    import type { Post, DigestJson, DigestItem, DigestSection } from "./types.js";

    // ============================================================================
    // SYSTEM_PROMPT — §8 spec-app.md. Экстрактивность обязательна; keyQuote ДОЛЖЕН быть
    // дословной подстрокой text. Серверная проверка (D-01) — финальный барьер, а не этот промпт.
    // ============================================================================
    const SYSTEM_PROMPT = [
      "Ты — экстрактивный редактор русскоязычной ленты. На вход получаешь JSON { posts: [...] },",
      "где posts[i] = { channelUsername, postedAt, text, url }.",
      "",
      "Жёсткие правила:",
      "1) Пиши ТОЛЬКО по фактам из text. Никаких домыслов, чисел или имён, отсутствующих в исходнике.",
      "2) keyQuote каждой записи ДОЛЖЕН быть дословной подстрокой text (для верификации).",
      "3) summary — 1–2 предложения на русском, до 250 символов.",
      "4) Отбирай не более 15 самых содержательных постов в сумме по всем каналам.",
      "5) Группировку по темам придумай сам — 3–6 групп, короткие заголовки.",
      "6) Возвращай строго JSON без markdown и комментариев:",
      "{",
      '  "generatedAt": "ISO8601",',
      '  "sections": [',
      "    {",
      '      "title": "Короткий заголовок темы",',
      '      "items": [',
      '        { "summary": "...", "keyQuote": "...", "url": "https://t.me/...", "channel": "username" }',
      "      ]",
      "    }",
      "  ]",
      "}",
    ].join("\n");

    // ============================================================================
    // Экранирование HTML. D-13 + SUM-04: <, >, & экранируются; кавычки не трогаем
    // (Telegram HTML их не интерпретирует). url обрабатывается отдельно через new URL().
    // ============================================================================
    export function escapeHtml(s: string): string {
      return s.replace(/&/g, "&amp;").replace(/</g, "&lt;").replace(/>/g, "&gt;");
    }

    // ============================================================================
    // Форматирование даты для шапки (D-09). Русская локаль, короткий формат.
    // Пример: "21 апр. 2026 г."
    // ============================================================================
    function formatDateRu(iso: string): string {
      const d = new Date(iso);
      if (isNaN(d.getTime())) return iso;
      return new Intl.DateTimeFormat("ru-RU", {
        day: "numeric",
        month: "short",
        year: "numeric",
      }).format(d);
    }

    // ============================================================================
    // Валидация структуры JSON-ответа (SUM-03): ручной typeof/Array.isArray, без zod.
    // Бросает Error при структурных нарушениях (тогда src/run.ts упадёт с exit 1).
    // ============================================================================
    function validate(x: unknown): asserts x is DigestJson {
      if (typeof x !== "object" || x === null) {
        throw new Error("DeepSeek response: not an object");
      }
      const obj = x as Record<string, unknown>;
      if (typeof obj.generatedAt !== "string") {
        throw new Error("DeepSeek response: generatedAt must be string");
      }
      if (!Array.isArray(obj.sections)) {
        throw new Error("DeepSeek response: sections must be array");
      }
      for (const section of obj.sections) {
        if (typeof section !== "object" || section === null) {
          throw new Error("DeepSeek response: section must be object");
        }
        const s = section as Record<string, unknown>;
        if (typeof s.title !== "string" || !s.title) {
          throw new Error("DeepSeek response: section.title must be non-empty string");
        }
        if (!Array.isArray(s.items)) {
          throw new Error("DeepSeek response: section.items must be array");
        }
        for (const item of s.items) {
          if (typeof item !== "object" || item === null) {
            throw new Error("DeepSeek response: item must be object");
          }
          const it = item as Record<string, unknown>;
          for (const field of ["summary", "keyQuote", "url", "channel"] as const) {
            if (typeof it[field] !== "string" || !it[field]) {
              throw new Error(`DeepSeek response: item.${field} must be non-empty string`);
            }
          }
        }
      }
    }

    // ============================================================================
    // Серверная проверка keyQuote (D-01..D-05) + маппинг item→post по url (D-03).
    // Возвращает новый DigestJson только с валидными items; удаляет пустые секции.
    // Побочный эффект: console.warn на каждое нарушение (D-04, D-05).
    // ============================================================================
    function verifyExtractiveness(digest: DigestJson, posts: Post[]): DigestJson {
      // D-03: Map<url, Post>. Ключ — url поста, полученный в src/telegram.ts.
      const byUrl = new Map<string, Post>();
      for (const p of posts) byUrl.set(p.url, p);

      const cleanedSections: DigestSection[] = [];
      for (const section of digest.sections) {
        const validItems: DigestItem[] = [];
        for (const item of section.items) {
          const post = byUrl.get(item.url);
          if (!post) {
            // D-03: url не совпал ни с одним собранным постом — skip.
            console.warn(
              `[summarize] skip (url not in source): channel=${item.channel} url=${item.url} keyQuote="${item.keyQuote.slice(0, 60)}"`
            );
            continue;
          }
          // D-02: sourceText.includes(keyQuote.trim()) — строго + trim по краям.
          const needle = item.keyQuote.trim();
          if (!post.text.includes(needle)) {
            // D-04: skip + warn с channel, messageId, keyQuote, 60-char сниппет text.
            const snippet = post.text.slice(0, 60).replace(/\n/g, " ");
            console.warn(
              `[summarize] skip (keyQuote not in source): channel=${post.channelUsername} messageId=${post.messageId} keyQuote="${needle}" textSnippet="${snippet}"`
            );
            continue;
          }
          validItems.push({
            summary: item.summary,
            keyQuote: item.keyQuote,
            url: item.url,
            channel: item.channel,
          });
        }
        if (validItems.length > 0) {
          cleanedSections.push({ title: section.title, items: validItems });
        }
      }
      return {
        generatedAt: digest.generatedAt,
        sections: cleanedSections,
      };
    }

    // ============================================================================
    // Рендер HTML (D-09..D-13, SUM-04). Чистая конкатенация строк, без Handlebars.
    // D-09: шапка `<b>Нефтегаз — {date}</b>\n<i>{N} постов из {K} каналов за 24ч</i>\n\n`.
    //   N = общее число собранных постов (posts.length — плана).
    //   K = число каналов с ≥1 собранным постом.
    // D-10: заголовок темы = `<b>{title}</b>` (без emoji, без нумерации).
    // D-11: буллет = `• {summary} — <i>«{keyQuote}»</i> — <a href="{url}">@{channel}</a>`.
    // D-12: разделитель секций — одна пустая строка (\n\n).
    // D-13: escapeHtml для summary/keyQuote/channel; url валидируется через new URL().
    // ============================================================================
    export function renderHtml(digest: DigestJson, posts: Post[]): string {
      const date = formatDateRu(digest.generatedAt);
      const n = posts.length;
      const k = new Set(posts.map((p) => p.channelUsername)).size;

      const header =
        `<b>Нефтегаз — ${escapeHtml(date)}</b>\n` +
        `<i>${n} постов из ${k} каналов за 24ч</i>\n\n`;

      const sectionsHtml: string[] = [];
      for (const section of digest.sections) {
        const lines: string[] = [];
        lines.push(`<b>${escapeHtml(section.title)}</b>`);
        for (const item of section.items) {
          // D-13: валидация url через new URL(). Невалидный — пропускаем.
          let safeUrl: string;
          try {
            safeUrl = new URL(item.url).toString();
          } catch {
            console.warn(`[summarize] skip (bad url): ${item.url}`);
            continue;
          }
          // D-11: точный формат буллета.
          lines.push(
            `• ${escapeHtml(item.summary)} — <i>«${escapeHtml(item.keyQuote)}»</i> — <a href="${safeUrl}">@${escapeHtml(item.channel)}</a>`
          );
        }
        sectionsHtml.push(lines.join("\n"));
      }

      // D-12: между секциями — \n\n (пустая строка).
      return header + sectionsHtml.join("\n\n");
    }

    // ============================================================================
    // summarize(posts) — главная функция модуля (SUM-01, SUM-02, SUM-03, SUM-04).
    // ============================================================================
    export async function summarize(posts: Post[]): Promise<string> {
      const apiKey = process.env.DEEPSEEK_API_KEY;
      if (!apiKey) {
        throw new Error("DEEPSEEK_API_KEY не задан.");
      }
      const baseURL = process.env.DEEPSEEK_BASE_URL ?? "https://api.deepseek.com";
      const model = process.env.DEEPSEEK_MODEL ?? "deepseek-chat";

      const client = new OpenAI({ apiKey, baseURL });

      // SUM-01: один батч-запрос, response_format: json_object.
      const completion = await client.chat.completions.create({
        model,
        response_format: { type: "json_object" },
        messages: [
          { role: "system", content: SYSTEM_PROMPT },
          { role: "user", content: JSON.stringify({ posts }) },
        ],
      });

      const raw = completion.choices[0]?.message?.content ?? "{}";

      // SUM-03: парсинг + ручная валидация; ошибка → пробрасываем наверх (src/run.ts сделает exit 1).
      let parsed: unknown;
      try {
        parsed = JSON.parse(raw);
      } catch (err) {
        console.error("[summarize] DeepSeek вернул невалидный JSON. Raw (first 500 chars):");
        console.error(raw.slice(0, 500));
        throw new Error(`Invalid JSON from DeepSeek: ${(err as Error).message}`);
      }
      validate(parsed);

      // D-01..D-05: серверная верификация дословности keyQuote.
      const verified = verifyExtractiveness(parsed, posts);

      // Если после верификации не осталось ни одной записи — дайджест пустой,
      // но рендерим шапку (оператор увидит "N постов из K каналов за 24ч",
      // но ни одной темы). Это валидный сценарий (LLM всё прогнал, всё отсеялось).
      return renderHtml(verified, posts);
    }

Важные детали:
- Импорт типов — `import type { Post, DigestJson, DigestItem, DigestSection } from "./types.js"`. `type`-only импорт не попадает в runtime (чище).
- `escapeHtml` — ровно три замены `&`, `<`, `>` в этом порядке. D-13 явно говорит: кавычки не трогаем (Telegram HTML не парсит `"` как спец-символ вне атрибутов, а в `<a href="">` мы сами пишем URL из `new URL()`).
- `asserts x is DigestJson` — type predicate; после `validate(parsed)` TypeScript знает что `parsed: DigestJson`.
- `verifyExtractiveness` использует `.trim()` ТОЛЬКО по краям и `.includes()` — никакого `toLowerCase`, `replace(/\s+/g, ' ')` (D-02).
- `console.warn` в stderr (в Node по умолчанию) — соответствует D-05.
- URL-валидация через `new URL(item.url).toString()` — если строка не парсится, катч + skip (это тоже защита, связанная с T-04 SSRF, хотя основной барьер — D-03 маппинг по url).

После записи — `npx tsc --noEmit` должен пройти с 0 ошибок.
  </action>

  <verify>
    <automated>test -f src/summarize.ts && grep -q "response_format: { type: \"json_object\" }" src/summarize.ts && grep -q "DEEPSEEK_API_KEY" src/summarize.ts && grep -q "DEEPSEEK_MODEL" src/summarize.ts && grep -q "DEEPSEEK_BASE_URL" src/summarize.ts && grep -q "export function escapeHtml" src/summarize.ts && grep -q "export function renderHtml" src/summarize.ts && grep -q "export async function summarize" src/summarize.ts && grep -q "\\.includes(needle)" src/summarize.ts && grep -q "\\.keyQuote\\.trim()" src/summarize.ts && grep -q "Map<string, Post>" src/summarize.ts && grep -q "<b>Нефтегаз" src/summarize.ts && grep -q "постов из" src/summarize.ts && grep -q "новый URL\\|new URL" src/summarize.ts && grep -q "console.warn" src/summarize.ts && test $(grep -c "escapeHtml" src/summarize.ts) -ge 5 && npx tsc --noEmit</automated>
  </verify>

  <acceptance_criteria>
    - `src/summarize.ts` импортирует `OpenAI` из `openai` и типы из `./types.js`
    - `src/summarize.ts` содержит SYSTEM_PROMPT с пунктами «дословной подстрокой text», «не более 15», «3–6 групп», «строго JSON без markdown» (grep по ключевым фразам)
    - `src/summarize.ts` вызывает `client.chat.completions.create` с `response_format: { type: "json_object" }` (SUM-01)
    - `src/summarize.ts` читает `DEEPSEEK_API_KEY`, `DEEPSEEK_MODEL`, `DEEPSEEK_BASE_URL` из `process.env`
    - `src/summarize.ts` содержит ручную валидацию через `typeof` и `Array.isArray` (SUM-03) — НЕ импортирует `zod`
    - `src/summarize.ts` содержит ровно реализацию D-02: `post.text.includes(item.keyQuote.trim())` (grep -q "\.includes(needle)" И grep -q "\.trim()")
    - `src/summarize.ts` НЕ содержит `toLowerCase` в логике проверки keyQuote (grep -v -q "keyQuote.*toLowerCase\|toLowerCase.*keyQuote")
    - `src/summarize.ts` НЕ содержит `replace(/\s+/g` применённого к keyQuote или text
    - `src/summarize.ts` содержит `Map<string, Post>` или эквивалент для маппинга по url (D-03)
    - `src/summarize.ts` при нарушении дословности делает `console.warn` с channel, messageId, keyQuote, сниппетом (D-04, grep совпадение с "channel=" И "messageId=" И "keyQuote=" И "textSnippet")
    - `src/summarize.ts` НЕ пишет в файл (grep -v -q "writeFile\|appendFile\|fs\.write" — 0 совпадений в этом модуле, D-05)
    - `src/summarize.ts` содержит `<b>Нефтегаз — ` в renderHtml (D-09)
    - `src/summarize.ts` содержит шаблон `постов из ` и `каналов за 24ч` (D-09)
    - `src/summarize.ts` строит буллет с `• ` и `<i>«` и `»</i>` и `<a href=` и `">@` (D-11)
    - `src/summarize.ts` использует `new URL(` для валидации url (D-13)
    - `src/summarize.ts` вызывает `escapeHtml` минимум 5 раз (summary, keyQuote, channel, date, title) — `grep -c "escapeHtml" src/summarize.ts` ≥ 5
    - `src/summarize.ts` НЕ импортирует `handlebars` или другие шаблонизаторы (D-13)
    - `src/summarize.ts` экспортирует `summarize`, `renderHtml`, `escapeHtml` (grep -q "^export ")
    - `npx tsc --noEmit` exits 0 после создания файла
  </acceptance_criteria>

  <done>
    `src/summarize.ts` содержит три экспортируемых функции. `summarize` делает batch-запрос к DeepSeek (SUM-01), валидирует JSON (SUM-03), прогоняет каждый keyQuote через серверную проверку дословности (D-01..D-05), рендерит HTML по формату D-09..D-13. `escapeHtml` экранирует `<`, `>`, `&`. Никаких внешних зависимостей кроме `openai`. `npx tsc --noEmit` без ошибок.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Telegram MTProto ↔ GramJS | внешний текст постов приходит как `msg.message`, считается untrusted |
| DeepSeek ↔ summarize | JSON-ответ DeepSeek — untrusted (LLM может галлюцинировать или попытаться инъекцию) |
| src/summarize.ts → HTML ↔ Bot API | HTML-строка уходит в `parse_mode: HTML` — любой непроэкранированный `<` разрушит рендер |
| process.env ↔ src/telegram.ts / src/summarize.ts | секреты читаются один раз при старте, не логируются |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02 | Tampering (HTML injection в Telegram) | `src/summarize.ts renderHtml` | mitigate | `escapeHtml(s)` экранирует `&`, `<`, `>` перед вставкой в HTML для `summary`, `keyQuote`, `channel`, `title`, `date` (Task 2, acceptance: `grep -c escapeHtml` ≥ 5). `url` пропускается через `new URL().toString()` и вставляется в `href="..."` (D-13). |
| T-03 | Tampering (prompt injection) | `src/summarize.ts summarize` | mitigate | Серверная проверка `sourceText.includes(item.keyQuote.trim())` в `verifyExtractiveness` (Task 2, D-01..D-04). Даже если LLM подчинится инъекции, дословный match не пройдёт → skip+warn. Нарушения не роняют прогон (D-04). |
| T-04 | Tampering (SSRF через url) | `src/summarize.ts` | mitigate | D-03: `Map<url, Post>` — если LLM придумала url, не матчащийся ни одному собранному посту, запись скипается. Дополнительно `new URL(item.url)` в renderHtml валидирует синтаксис URL (Task 2). |
| T-06 | Denial of Service (FloodWait ban) | `src/telegram.ts fetchLast24h` | mitigate | FloodWaitError → sleep(err.seconds*1000+2000) + один retry (FETCH-04, Task 1). Ограниченное окно 24ч (FETCH-02). Правдоподобная identity Desktop/Win11/ru (D-06, FETCH-01). Второй FloodWait подряд — ошибка наверх (поймается в plan 03). |
| T-05 | Information Disclosure (session hijack) | `src/telegram.ts` | accept | TG_SESSION читается из process.env (запрошено в plan 01: `.env` уже в `.gitignore`). Модуль не логирует содержимое session, не пишет в файл. Дополнительных мер не требуется. |

## Accepted Risks

- **T-05** в этом плане принят: `.gitignore` (plan 01) — единственный механизм защиты `.env`. Полагаемся на дисциплину оператора не публиковать `.env`.
</threat_model>

<verification>
После завершения всех task'ов:

1. **Структура:**

       test -f src/types.ts && test -f src/telegram.ts && test -f src/summarize.ts

2. **TypeScript:**

       npx tsc --noEmit  # exit 0

3. **D-06..D-08 (anti-ban identity) в src/telegram.ts:**

       grep -q 'deviceModel: "Desktop"' src/telegram.ts
       grep -q 'systemVersion: "Windows 11"' src/telegram.ts
       grep -q 'appVersion: "5.3.0 x64"' src/telegram.ts
       grep -q 'langCode: "ru"' src/telegram.ts
       grep -q 'systemLangCode: "ru"' src/telegram.ts
       # D-08: дефолты — connectionRetries не должен быть в src/telegram.ts (только в scripts/login.ts)
       ! grep -q "connectionRetries" src/telegram.ts

4. **D-01..D-05 (серверная проверка keyQuote) в src/summarize.ts:**

       grep -q "Map<string, Post>" src/summarize.ts
       grep -q "\\.keyQuote\\.trim()" src/summarize.ts
       grep -q "\\.includes(needle)" src/summarize.ts
       ! grep -q "keyQuote.*toLowerCase" src/summarize.ts
       ! grep -q "replace(/\\\\s+" src/summarize.ts
       grep -q "console.warn" src/summarize.ts
       ! grep -q "writeFile\\|appendFile" src/summarize.ts

5. **D-09..D-13 (HTML-формат) в src/summarize.ts:**

       grep -q "<b>Нефтегаз — " src/summarize.ts
       grep -q "постов из" src/summarize.ts
       grep -q "каналов за 24ч" src/summarize.ts
       grep -q "<i>«" src/summarize.ts
       grep -q "»</i>" src/summarize.ts
       grep -q '<a href="' src/summarize.ts
       grep -q "new URL(" src/summarize.ts
       test $(grep -c "escapeHtml" src/summarize.ts) -ge 5
       ! grep -q "handlebars\\|Handlebars" src/summarize.ts

6. **SUM-01 (одним батчем + json_object):**

       grep -q "chat.completions.create" src/summarize.ts
       grep -q 'response_format: { type: "json_object" }' src/summarize.ts

7. **FETCH-02 / FETCH-03 / FETCH-04 / FETCH-05 в src/telegram.ts:**

       grep -q "windowHours \\* 3600" src/telegram.ts
       grep -q "https://t.me/" src/telegram.ts
       grep -q "FloodWait" src/telegram.ts
       grep -q "CHANNEL_PRIVATE\\|ChannelPrivateError" src/telegram.ts
</verification>

<success_criteria>
- [ ] `src/types.ts` экспортирует `Post`, `DigestItem`, `DigestSection`, `DigestJson`
- [ ] `src/telegram.ts` содержит `deviceModel="Desktop"` + `systemVersion="Windows 11"` + `appVersion="5.3.0 x64"` + `langCode="ru"` + `systemLangCode="ru"` (D-06, D-07)
- [ ] `src/telegram.ts` НЕ переопределяет `connectionRetries` / `useWSS` (D-08)
- [ ] `fetchLast24h` реализует FETCH-02 (окно), FETCH-03 (url), FETCH-04 (FloodWait + один retry), FETCH-05 (частные ошибки → warn + пустой массив)
- [ ] `src/summarize.ts` делает SUM-01 batch-вызов с `response_format: json_object`
- [ ] `src/summarize.ts` валидирует JSON без zod (SUM-03)
- [ ] `src/summarize.ts` выполняет серверную проверку `post.text.includes(item.keyQuote.trim())` через `Map<url, Post>` (D-01, D-02, D-03)
- [ ] Нарушения keyQuote → skip + console.warn с деталями, дайджест не падает (D-04, D-05)
- [ ] `renderHtml` формирует шапку, секции `<b>Заголовок</b>`, буллеты по формату D-11, разделитель `\n\n` (D-09..D-12)
- [ ] `escapeHtml` используется ≥ 5 раз для всех пользовательских строк (D-13, SUM-04)
- [ ] Никакого `handlebars` не импортируется (D-13)
- [ ] `npx tsc --noEmit` exits 0
</success_criteria>

<output>
После завершения создать `.planning/phases/01-mvp/01-02-SUMMARY.md` со списком:
- созданных файлов (src/types.ts, src/telegram.ts, src/summarize.ts) и их экспортов;
- применённых D-XX решений (D-01 D-02 D-03 D-04 D-05 D-06 D-07 D-08 D-09 D-10 D-11 D-12 D-13);
- покрытых requirements (FETCH-01..06, SUM-01..04);
- интерфейсов для plan 03: `createClient`, `fetchLast24h`, `summarize`, плюс тип `Post`.
</output>
