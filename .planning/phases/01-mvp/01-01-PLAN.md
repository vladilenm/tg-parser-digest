---
phase: 01-mvp
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - .env.example
  - channels.yaml
  - .gitignore
  - scripts/login.ts
autonomous: true
requirements:
  - CFG-01
  - CFG-02
  - CFG-03
  - CFG-04
  - CFG-05
  - AUTH-01
  - AUTH-02
user_setup:
  - service: telegram-api
    why: "Создание `TG_API_ID` / `TG_API_HASH` для GramJS user-session"
    env_vars:
      - name: TG_API_ID
        source: "https://my.telegram.org → API development tools → App api_id"
      - name: TG_API_HASH
        source: "https://my.telegram.org → API development tools → App api_hash"
    dashboard_config:
      - task: "Запустить `npm run login`, ввести телефон/код/2FA, скопировать StringSession в `.env` как `TG_SESSION`"
        location: "локальный терминал"
  - service: telegram-bot
    why: "Доставка дайджеста в приватный канал"
    env_vars:
      - name: TG_BOT_TOKEN
        source: "@BotFather → /newbot → скопировать токен"
      - name: TG_CHANNEL_ID
        source: "@username_to_id_bot (переслать любое сообщение из канала) → формат -100xxxxxxxxxx"
    dashboard_config:
      - task: "Создать приватный Telegram-канал и добавить бота администратором (с правом постить сообщения)"
        location: "Telegram-клиент"
  - service: deepseek
    why: "Суммаризация постов одним батчем через OpenAI-совместимый endpoint"
    env_vars:
      - name: DEEPSEEK_API_KEY
        source: "https://platform.deepseek.com → API keys"

must_haves:
  truths:
    - "Оператор может выполнить `npm install` и получить корректное дерево зависимостей (`telegram`, `openai`, `yaml` в dependencies; `tsx`, `typescript`, `@types/node` в devDependencies)"
    - "`npm run login` запускается и печатает StringSession для ручного копирования в `.env`"
    - "`npx tsc --noEmit` завершается с кодом 0"
    - "`.env` не попадает в git (присутствует в `.gitignore`)"
    - "`channels.yaml` загружается библиотекой `yaml` и содержит 10–15 публичных username-ов"
  artifacts:
    - path: "package.json"
      provides: "манифест с type=module, scripts.login (через --env-file), scripts.start (через --env-file)"
      contains: "\"type\": \"module\""
    - path: "tsconfig.json"
      provides: "strict-конфиг TypeScript без билда"
      contains: "\"moduleResolution\": \"bundler\""
    - path: ".env.example"
      provides: "шаблон всех 12 переменных окружения с комментариями-источниками"
      contains: "TG_API_ID"
    - path: "channels.yaml"
      provides: "список 10–15 публичных каналов российского нефтегаза с полем priority"
      contains: "channels:"
    - path: ".gitignore"
      provides: "защита .env и node_modules от коммита"
      contains: ".env"
    - path: "scripts/login.ts"
      provides: "интерактивная генерация TG_SESSION через GramJS + readline"
      contains: "StringSession"
  key_links:
    - from: "package.json"
      to: "scripts/login.ts"
      via: "scripts.login вызывает tsx scripts/login.ts"
      pattern: "tsx scripts/login.ts"
    - from: "scripts/login.ts"
      to: ".env (ручное копирование)"
      via: "console.log client.session.save()"
      pattern: "session\\.save\\(\\)"
    - from: ".gitignore"
      to: ".env"
      via: "файл исключён из git"
      pattern: "^\\.env$"
---

<objective>
Собрать каркас проекта: `package.json` + `tsconfig.json` + `.env.example` + `channels.yaml` + `.gitignore` + `scripts/login.ts`. После этого `npm install` должен пройти без ошибок, `npx tsc --noEmit` — с exit 0, а `npm run login` — интерактивно сгенерировать StringSession для ручного копирования в `.env`.

Purpose: заложить контракты окружения (ровно 3 runtime-зависимости, Node 20.6+ `--env-file`, TypeScript без build-шага) и единственный способ получить user-session для GramJS. Всё остальное (fetch, summarize, deliver, run) опирается на эти файлы.

Output:
- `package.json` с двумя scripts (`login`, `start`) и тремя runtime-зависимостями.
- `tsconfig.json` strict + `moduleResolution: bundler` + `noEmit: true`.
- `.env.example` со всеми 12 переменными и комментариями-источниками.
- `channels.yaml` с 10–15 публичными username + полем `priority`.
- `.gitignore` блокирующий `.env` и `node_modules/`.
- `scripts/login.ts` — разовый интерактивный логин + `console.log(client.session.save())`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-mvp/01-CONTEXT.md
@spec-app.md
@CLAUDE.md

<interfaces>
Переменные окружения (из .env.example, читаются через process.env в downstream модулях):
- TG_API_ID: string (число, но в process.env — строка)
- TG_API_HASH: string
- TG_SESSION: string (StringSession)
- TG_BOT_TOKEN: string
- TG_CHANNEL_ID: string (формат "-100xxxxxxxxxx")
- DEEPSEEK_API_KEY: string
- DEEPSEEK_MODEL: string (default "deepseek-chat")
- DEEPSEEK_BASE_URL: string (default "https://api.deepseek.com")
- FETCH_WINDOW_HOURS: string (парсится в Number, default 24)
- MAX_MESSAGES_PER_CHANNEL: string (парсится в Number, default 50)
- CHANNEL_DELAY_MS: string (парсится в Number, default 1000)
- LOG_LEVEL: string (default "info")

Структура channels.yaml (читается в src/run.ts в plan 03):
    channels:
      - username: "neftegazru"
        priority: 1
      - username: "oilfornication"
        priority: 2

Поле priority зарезервировано (не используется в MVP).

Команды:
- npm install  — ставит зависимости
- npm run login — запускает scripts/login.ts под tsx с --env-file=.env, печатает StringSession
- npm start — node --env-file=.env --import tsx src/run.ts (main entrypoint будет реализован в plan 03)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Создать package.json, tsconfig.json, .gitignore</name>
  <files>package.json, tsconfig.json, .gitignore</files>

  <read_first>
    - spec-app.md §3 (стек), §4 (структура), §7.1 (package.json + tsconfig.json), §10 (команды запуска)
    - .planning/phases/01-mvp/01-CONTEXT.md (§Canonical References + §Domain)
    - .planning/REQUIREMENTS.md (CFG-01, CFG-02, CFG-05)
    - CLAUDE.md (constraints: ровно 3 runtime-зависимости, Node 20.6+, TypeScript без build-шага)
  </read_first>

  <action>
Создай три файла:

**1. `package.json`** — минимальный, ESM, с единственным набором scripts. Оба script'а используют `--env-file=.env`, чтобы и логин, и основной прогон видели `TG_API_ID`/`TG_API_HASH`/и пр. одинаково (без этого `scripts/login.ts` не получит доступ к переменным):

Содержимое `package.json`:

- `"name": "tg-parser-demo"`
- `"version": "0.1.0"`
- `"private": true`
- `"type": "module"`
- `"engines": { "node": ">=20.6.0" }`
- `"scripts"`:
  - `"login": "node --env-file=.env --import tsx scripts/login.ts"`
  - `"start": "node --env-file=.env --import tsx src/run.ts"`
- `"dependencies"` — РОВНО три ключа:
  - `"openai": "^4.0.0"`
  - `"telegram": "^2.22.0"`
  - `"yaml": "^2.5.0"`
- `"devDependencies"`:
  - `"@types/node": "^20.0.0"`
  - `"tsx": "^4.0.0"`
  - `"typescript": "^5.4.0"`

Никаких `dotenv`, `pino`, `winston`, `zod`, `handlebars` — CLAUDE.md и spec-app.md §3 фиксируют ровно три runtime-зависимости.

Про `login` script: REQUIREMENTS.md CFG-01 упоминает `tsx scripts/login.ts`, но без `--env-file=.env` скрипт не увидит `TG_API_ID`/`TG_API_HASH`. Префикс `node --env-file=.env --import tsx` — симметричен с `start` и решает эту проблему. Это не отход от требования, а его корректная реализация.

**2. `tsconfig.json`** — strict, без эмиссии. Поля `compilerOptions`:

- `"target": "ES2022"`
- `"module": "ESNext"`
- `"moduleResolution": "bundler"`
- `"strict": true`
- `"esModuleInterop": true`
- `"skipLibCheck": true`
- `"noEmit": true`
- `"resolveJsonModule": true`
- `"allowSyntheticDefaultImports": true`

Плюс `"include": ["src/**/*.ts", "scripts/**/*.ts"]`.

**3. `.gitignore`** — защита секретов и артефактов (T-01 secrets leak, T-05 session hijack):

    # Dependencies
    node_modules/

    # Secrets
    .env
    .env.local

    # Logs
    *.log

    # OS
    .DS_Store

    # Editor
    .vscode/
    .idea/

    # TS build artifacts
    dist/
    build/
    *.tsbuildinfo

Важно: `.env.example` НЕ должен быть в `.gitignore` (его коммитим), только `.env`.

После записи — запусти `npm install` (оператор подтвердит команду); затем проверь что `npx tsc --noEmit` завершается с exit 0 (пустой проект — tsc должен завершиться без ошибок).
  </action>

  <verify>
    <automated>test -f package.json && test -f tsconfig.json && test -f .gitignore && grep -q '"type": "module"' package.json && grep -q '"moduleResolution": "bundler"' tsconfig.json && grep -Fx '.env' .gitignore && grep -Fx 'node_modules/' .gitignore</automated>
  </verify>

  <acceptance_criteria>
    - `package.json` contains `"type": "module"`
    - `package.json` contains `"login": "node --env-file=.env --import tsx scripts/login.ts"`
    - `package.json` contains `"start": "node --env-file=.env --import tsx src/run.ts"`
    - `package.json` dependencies содержит РОВНО три ключа: `openai`, `telegram`, `yaml` (проверить: `node -e "console.log(Object.keys(require('./package.json').dependencies).sort().join(','))"` → `openai,telegram,yaml`)
    - `package.json` devDependencies содержит `tsx`, `typescript`, `@types/node`
    - `package.json` содержит `"engines"` с `"node": ">=20.6.0"`
    - `tsconfig.json` содержит `"strict": true`
    - `tsconfig.json` содержит `"moduleResolution": "bundler"`
    - `tsconfig.json` содержит `"noEmit": true`
    - `tsconfig.json` содержит `"target": "ES2022"`
    - `.gitignore` содержит строку `.env` (на отдельной строке, `grep -Fx ".env" .gitignore` exit 0)
    - `.gitignore` содержит строку `node_modules/` (`grep -Fx "node_modules/" .gitignore` exit 0)
    - После `npm install` существует каталог `node_modules/` (создаётся оператором; в acceptance — наличие каталога)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>

  <done>
    Три файла созданы и валидны. `npm install` ставит зависимости без ошибок. `npx tsc --noEmit` exits 0. `.gitignore` блокирует `.env` и `node_modules/`.
  </done>
</task>

<task type="auto">
  <name>Task 2: Создать .env.example и channels.yaml</name>
  <files>.env.example, channels.yaml</files>

  <read_first>
    - spec-app.md §5 (.env.example), §6 (channels.yaml формат)
    - .planning/REQUIREMENTS.md (CFG-03, CFG-04)
    - .planning/phases/01-mvp/01-CONTEXT.md (§Deferred: финальный список каналов готовит оператор)
    - CLAUDE.md (Core Value: российский нефтегаз/нефтехимия)
  </read_first>

  <action>
**1. `.env.example`** — дословно все 12 переменных из spec-app.md §5 с комментариями-источниками. Никаких реальных значений (T-01 secrets leak).

Структура файла (bash-style комментарии `#`, блоки с визуальными разделителями):

Блок 1 — **Telegram user-session**:
- `TG_API_ID=`  (коммент сверху: `# https://my.telegram.org → API development tools → App api_id`)
- `TG_API_HASH=`  (коммент: `# https://my.telegram.org → API development tools → App api_hash`)
- `TG_SESSION=`  (коммент: `# StringSession: генерируется через npm run login, скопировать сюда`)

Блок 2 — **Telegram bot**:
- `TG_BOT_TOKEN=`  (коммент: `# @BotFather → /newbot → скопировать токен`)
- `TG_CHANNEL_ID=`  (коммент: `# -100xxxxxxxxxx (приватный канал, бот — admin). Получить через @username_to_id_bot`)

Блок 3 — **DeepSeek**:
- `DEEPSEEK_API_KEY=`  (коммент: `# https://platform.deepseek.com → API keys`)
- `DEEPSEEK_MODEL=deepseek-chat`
- `DEEPSEEK_BASE_URL=https://api.deepseek.com`

Блок 4 — **Параметры прогона** (значения по умолчанию ЗАДАНЫ):
- `FETCH_WINDOW_HOURS=24`
- `MAX_MESSAGES_PER_CHANNEL=50`
- `CHANNEL_DELAY_MS=1000`  (коммент: `# базовая задержка между каналами; к ней добавляется jitter 0–500мс`)
- `LOG_LEVEL=info`  (коммент: `# debug | info | warn | error`)

Итого ровно 12 переменных. Реальных секретов НЕ вписывай (T-01).

**2. `channels.yaml`** — 10–15 публичных каналов российского нефтегаза/нефтехимии. Обязательно включить два якорных: `neftegazru` и `oilfornication` (упомянуты в ROADMAP.md и REQUIREMENTS.md CFG-04). Остальные — отраслевые TG-каналы; оператор подредактирует руками перед первым прогоном (см. §Deferred в CONTEXT.md).

Структура (ровно такая — правила YAML требуют корректного отступа):

    # Список публичных Telegram-каналов российского нефтегаза/нефтехимии.
    # User-аккаунт из TG_SESSION ОБЯЗАН быть подписан на каждый из них —
    # иначе GramJS бросит ChannelPrivateError (этот канал будет пропущен).
    # Поле priority зарезервировано (не используется в MVP).

    channels:
      - username: "neftegazru"
        priority: 1
      - username: "oilfornication"
        priority: 2
      - username: "oil_gas_forum"
        priority: 3
      - username: "neftianka"
        priority: 3
      - username: "energytodaygroup"
        priority: 3
      - username: "oilcapital"
        priority: 3
      - username: "interfaxonline"
        priority: 4
      - username: "tass_agency"
        priority: 4
      - username: "rbc_news"
        priority: 4
      - username: "kommersant"
        priority: 4
      - username: "vedomosti"
        priority: 4
      - username: "prime1"
        priority: 4

12 каналов — в границе 10–15. Username'ы — seed-список; README в plan 03 предупредит оператора, что часть каналов можно заменить на более профильные.
  </action>

  <verify>
    <automated>test -f .env.example && test -f channels.yaml && grep -q "^TG_API_ID=" .env.example && grep -q "^DEEPSEEK_API_KEY=" .env.example && grep -q "^CHANNEL_DELAY_MS=1000" .env.example && node -e "import('yaml').then(({default: yaml}) => { const fs = require('fs'); const data = yaml.parse(fs.readFileSync('channels.yaml', 'utf8')); if (!Array.isArray(data.channels) || data.channels.length < 10 || data.channels.length > 15) { console.error('channels must be 10-15, got', data.channels && data.channels.length); process.exit(1); } for (const c of data.channels) { if (typeof c.username !== 'string' || !c.username) { console.error('invalid channel:', c); process.exit(1); } } console.log('channels.yaml valid:', data.channels.length); })"</automated>
  </verify>

  <acceptance_criteria>
    - `.env.example` содержит все 12 имён переменных: `TG_API_ID`, `TG_API_HASH`, `TG_SESSION`, `TG_BOT_TOKEN`, `TG_CHANNEL_ID`, `DEEPSEEK_API_KEY`, `DEEPSEEK_MODEL`, `DEEPSEEK_BASE_URL`, `FETCH_WINDOW_HOURS`, `MAX_MESSAGES_PER_CHANNEL`, `CHANNEL_DELAY_MS`, `LOG_LEVEL` (каждое проверяется `grep -q "^<NAME>=" .env.example`)
    - `.env.example` содержит дефолты: `DEEPSEEK_MODEL=deepseek-chat`, `DEEPSEEK_BASE_URL=https://api.deepseek.com`, `FETCH_WINDOW_HOURS=24`, `MAX_MESSAGES_PER_CHANNEL=50`, `CHANNEL_DELAY_MS=1000`, `LOG_LEVEL=info`
    - `.env.example` содержит комментарий «my.telegram.org» около `TG_API_ID`/`TG_API_HASH`
    - `.env.example` содержит комментарий «BotFather» около `TG_BOT_TOKEN`
    - `.env.example` не содержит реальных секретов: строки `^TG_API_HASH=.+` / `^DEEPSEEK_API_KEY=.+` / `^TG_BOT_TOKEN=.+` / `^TG_SESSION=.+` — отсутствуют (значения после `=` пустые)
    - `channels.yaml` парсится библиотекой `yaml` (автомат-verify выше) и содержит массив `channels` из 10–15 элементов
    - Каждый элемент имеет строковое непустое `username` и число `priority`
    - `channels.yaml` содержит `username: "neftegazru"` и `username: "oilfornication"`
    - `channels.yaml` содержит заголовочный комментарий про «user-аккаунт должен быть подписан»
  </acceptance_criteria>

  <done>
    `.env.example` содержит все 12 переменных с пустыми значениями (кроме дефолтов) и комментариями-источниками. `channels.yaml` парсится и содержит 10–15 каналов с username+priority. Ни один файл не содержит реальных секретов.
  </done>
</task>

<task type="auto">
  <name>Task 3: Создать scripts/login.ts — разовая генерация TG_SESSION</name>
  <files>scripts/login.ts</files>

  <read_first>
    - spec-app.md §7.2 (scripts/login.ts — StringSession + client.start + readline)
    - .planning/REQUIREMENTS.md (AUTH-01, AUTH-02)
    - package.json (scripts.login — убедиться что путь совпадает с `scripts/login.ts`)
    - CLAUDE.md (Node.js 20.6+, ESM, без билда)
  </read_first>

  <action>
Создай `scripts/login.ts` — интерактивный скрипт для разовой генерации StringSession. Запуск: `npm run login`. Использует только `telegram` (GramJS) и node built-in `readline`.

Точная реализация (TypeScript, ESM):

    // scripts/login.ts — разовая генерация TG_SESSION через GramJS StringSession.
    // Запуск: `npm run login` (подставляет --env-file=.env, так что TG_API_ID/TG_API_HASH видны через process.env)
    // После успеха скрипт печатает строку StringSession — скопируй её в .env как TG_SESSION.

    import { TelegramClient } from "telegram";
    import { StringSession } from "telegram/sessions/index.js";
    import readline from "node:readline/promises";
    import { stdin as input, stdout as output } from "node:process";

    const apiId = Number(process.env.TG_API_ID);
    const apiHash = process.env.TG_API_HASH;

    if (!Number.isFinite(apiId) || apiId <= 0) {
      console.error("TG_API_ID не задан или невалиден. Возьми его на https://my.telegram.org → API development tools.");
      process.exit(1);
    }
    if (!apiHash || apiHash.length < 8) {
      console.error("TG_API_HASH не задан. Возьми его на https://my.telegram.org → API development tools.");
      process.exit(1);
    }

    const rl = readline.createInterface({ input, output });
    const ask = (q: string) => rl.question(q);

    async function main(): Promise<void> {
      const session = new StringSession("");
      const client = new TelegramClient(session, apiId, apiHash, {
        connectionRetries: 5,
      });

      await client.start({
        phoneNumber: async () => (await ask("Телефон (в международном формате, +7...): ")).trim(),
        phoneCode: async () => (await ask("Код из Telegram: ")).trim(),
        password: async () => (await ask("2FA-пароль (если включён, иначе Enter): ")).trim(),
        onError: (err) => {
          console.error("Ошибка логина:", err);
        },
      });

      const saved = client.session.save() as unknown as string;
      console.log("\n============================================");
      console.log("TG_SESSION получен. Скопируй строку ниже в .env как TG_SESSION:");
      console.log("============================================\n");
      console.log(saved);
      console.log("\n============================================");
      console.log("Готово. Не публикуй TG_SESSION — это полный доступ к твоему user-аккаунту.");
      console.log("============================================");

      rl.close();
      await client.disconnect();
      process.exit(0);
    }

    main().catch((err) => {
      console.error("Фатальная ошибка:", err);
      process.exit(1);
    });

Важные детали:
- `TelegramClient` читает `apiId` как **число**, поэтому явно приводим `Number(process.env.TG_API_ID)` и валидируем (`Number.isFinite`).
- `client.start({...})` (AUTH-01): GramJS сам вызовет callback'и для телефона/кода/пароля. `readline/promises` даёт async-friendly API.
- `client.session.save()` (AUTH-02): в StringSession это синхронный метод, возвращает строку. Печатаем в stdout, НЕ сохраняем в файл (T-05: защита от случайного коммита; принцип «без персистентности»).
- Предупреждение о секретности `TG_SESSION` в выводе — явная защита от утечки (T-05).
- Финальный `process.exit(0)` обязателен — GramJS может оставить сокет открытым, без exit процесс зависнет.
- Тип `readline.createInterface({ input, output })` — используем версию `node:readline/promises`, чтобы `rl.question` возвращала `Promise<string>`.
- После создания файла — убедись что `npx tsc --noEmit` проходит без ошибок.
  </action>

  <verify>
    <automated>test -f scripts/login.ts && grep -q "StringSession" scripts/login.ts && grep -q "client.start" scripts/login.ts && grep -q "session.save" scripts/login.ts && grep -q "readline" scripts/login.ts && npx tsc --noEmit</automated>
  </verify>

  <acceptance_criteria>
    - Файл `scripts/login.ts` существует
    - Содержит `import { TelegramClient } from "telegram"`
    - Содержит `import { StringSession } from "telegram/sessions/index.js"` (или эквивалент, но должен импортироваться именно `StringSession`)
    - Содержит `import readline from "node:readline/promises"` (или `from "readline/promises"`)
    - Содержит `new StringSession("")` — пустая строка-инициализатор (AUTH-01)
    - Содержит вызов `client.start(` с колбэками `phoneNumber`, `phoneCode`, `password` (AUTH-01)
    - Содержит `client.session.save()` с печатью результата через `console.log` (AUTH-02)
    - Содержит валидацию `Number.isFinite(apiId)` / обработку невалидного `TG_API_ID`
    - Содержит `process.exit(0)` в успешной ветке и `process.exit(1)` в catch
    - Содержит предупреждение о секретности `TG_SESSION` в stdout-выводе (grep: слово «секрет» или «не публикуй» или «user-аккаунт»)
    - `npx tsc --noEmit` exits 0 (файл компилируется без ошибок типов)
    - Файл НЕ пишет `TG_SESSION` в файл / не создаёт никаких `*.jsonl` / `logs/` (проверка: grep -v -q "writeFileSync\|writeFile\|appendFileSync" scripts/login.ts или аналог)
  </acceptance_criteria>

  <done>
    `scripts/login.ts` компилируется без ошибок, интерактивно принимает телефон/код/2FA через readline и печатает StringSession в stdout с предупреждением о секретности. Никаких файлов сессии не создаётся.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local fs ↔ git | `.env` содержит TG_API_HASH, TG_SESSION, TG_BOT_TOKEN, DEEPSEEK_API_KEY — никогда не должен попасть в коммит |
| user input ↔ TelegramClient | ручной ввод телефона/кода/2FA в scripts/login.ts идёт напрямую в GramJS, без шага санитизации (это ок — GramJS сам обрабатывает) |
| scripts/login.ts stdout ↔ оператор | StringSession печатается в терминал, оператор копирует вручную |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01 | Information Disclosure | `.env` | mitigate | `.gitignore` содержит строку `.env` (Task 1, grep -Fx ".env" .gitignore). `.env.example` содержит только шаблоны без реальных значений (Task 2, acceptance: значения после `=` пусты для секретных полей). |
| T-05 | Information Disclosure (session hijack) | `scripts/login.ts` | mitigate | StringSession печатается только в stdout, НЕ записывается в файл (Task 3, acceptance: нет вызовов writeFile/appendFile). Вывод содержит предупреждение «не публикуй TG_SESSION». `.env` в `.gitignore` (Task 1). |
| T-06 | Denial of Service (FloodWait ban) | `scripts/login.ts` | accept | `login.ts` вызывается разово, не делает fetch-запросов к каналам. FloodWait не применим на этом этапе. Риск перенесён в plan 02 (anti-ban для `src/telegram.ts`). |

## Accepted Risks

- **T-06 (FloodWait)** на этом плане принимается, так как `scripts/login.ts` не читает сообщения, а только логинит user-аккаунт (разовая операция). Основные меры против FloodWait — в plan 02 (`src/telegram.ts`).
</threat_model>

<verification>
После завершения всех task'ов:

1. **Структура файлов:**

       test -f package.json && test -f tsconfig.json && test -f .env.example && test -f channels.yaml && test -f .gitignore && test -f scripts/login.ts

2. **Зависимости:**

       npm install  # exit 0
       node -e "const d = require('./package.json').dependencies; const keys = Object.keys(d).sort(); if (keys.join(',') !== 'openai,telegram,yaml') { console.error('wrong deps:', keys); process.exit(1); }"

3. **TypeScript:**

       npx tsc --noEmit  # exit 0

4. **Git-игнор секретов:**

       grep -Fx '.env' .gitignore  # exit 0
       grep -Fx 'node_modules/' .gitignore  # exit 0

5. **channels.yaml парсится:**

       node -e "import('yaml').then(({default: yaml}) => { const fs = require('fs'); const data = yaml.parse(fs.readFileSync('channels.yaml', 'utf8')); console.log('channels:', data.channels.length); })"

6. **`.env.example` не содержит секретов** (все секретные поля пусты после `=`):

       ! grep -E '^(TG_API_HASH|TG_SESSION|TG_BOT_TOKEN|DEEPSEEK_API_KEY)=.+' .env.example
</verification>

<success_criteria>
- [ ] `npm install` завершается без ошибок, `node_modules/` создан
- [ ] `npx tsc --noEmit` exits 0
- [ ] `.env` в `.gitignore`, `node_modules/` в `.gitignore`
- [ ] `channels.yaml` парсится, содержит 10–15 публичных каналов с полями `username` и `priority`
- [ ] `.env.example` содержит все 12 переменных с комментариями-источниками, без реальных секретов
- [ ] `scripts/login.ts` компилируется и содержит корректную цепочку `StringSession("")` → `client.start(callbacks)` → `console.log(client.session.save())`
- [ ] package.json содержит РОВНО три runtime-зависимости: `openai`, `telegram`, `yaml`
</success_criteria>

<output>
После завершения создать `.planning/phases/01-mvp/01-01-SUMMARY.md` со списком созданных файлов, примененных D-XX решений (нет — этот plan не затрагивает D-XX), покрытых requirements (CFG-01..05, AUTH-01, AUTH-02) и готовых интерфейсов для plan 02 и plan 03 (переменные окружения, формат `channels.yaml`, команды `npm run login` / `npm start`).
</output>
