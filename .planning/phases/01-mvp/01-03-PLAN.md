---
phase: 01-mvp
plan: 03
type: execute
wave: 2
depends_on:
  - 01
  - 02
files_modified:
  - src/deliver.ts
  - src/run.ts
  - README.md
autonomous: false
requirements:
  - DELIVER-01
  - DELIVER-02
  - DELIVER-03
  - DELIVER-04
  - RUN-01
  - RUN-02
  - RUN-03
  - OPS-01
  - OPS-02

must_haves:
  truths:
    - "Модуль `src/deliver.ts` экспортирует `sendToChannel(html)` и `chunkHtml(html, max)`; отправка идёт через встроенный `fetch` к `https://api.telegram.org/bot<TOKEN>/sendMessage` с `parse_mode: \"HTML\"` и `disable_web_page_preview: true` (DELIVER-01)"
    - "`chunkHtml` режет длинный дайджест на части ≤4000 символов по закрывающим тегам/переносам строк, не рвёт HTML посередине (DELIVER-02)"
    - "Если частей > 1, каждая префиксуется `(i/N)\\n` в начале (DELIVER-03)"
    - "При `res.ok === false` в `sendToChannel` бросается Error с HTTP-статусом и телом ответа (DELIVER-04)"
    - "`src/run.ts` экспортирует `main()`, читает `channels.yaml`, создаёт клиент, обходит каналы последовательно с `sleep(CHANNEL_DELAY_MS + randomInt(0, 500))` (FETCH-06 прокинут в run), вызывает `client.connect()` и `client.disconnect()` (RUN-01)"
    - "Если `posts.length === 0` — `console.log(\"No posts in window — skipping digest\")` + `process.exit(0)` БЕЗ вызова `summarize` и `sendToChannel` (RUN-02)"
    - "Иначе: `summarize(posts)` → `sendToChannel(html)` → `process.exit(0)`; глобальный `catch` → `console.error` + `process.exit(1)` (RUN-03)"
    - "`README.md` документирует 3 команды (`npm install` → `npm run login` → `npm start`) и фиксирует дисциплину «не чаще одного прогона в 10–15 минут» (OPS-01)"
    - "Ручная приёмка по 5 критериям §11 spec-app.md пройдена оператором (OPS-02)"
  artifacts:
    - path: "src/deliver.ts"
      provides: "sendToChannel + chunkHtml"
      exports: ["sendToChannel", "chunkHtml"]
      contains: "api.telegram.org"
    - path: "src/run.ts"
      provides: "main() — entrypoint, склейка всех шагов + пустой день + глобальный catch"
      exports: ["main"]
      contains: "loadChannelsYaml"
    - path: "README.md"
      provides: "инструкция запуска в 3 команды + ссылки-источники + дисциплина 10–15 мин"
      contains: "npm run login"
  key_links:
    - from: "src/run.ts main"
      to: "channels.yaml"
      via: "yaml.parse(readFileSync('./channels.yaml', 'utf8'))"
      pattern: "channels\\.yaml"
    - from: "src/run.ts main"
      to: "createClient / fetchLast24h (plan 02)"
      via: "import from ./telegram.js"
      pattern: "from \"\\./telegram"
    - from: "src/run.ts main"
      to: "summarize (plan 02)"
      via: "import from ./summarize.js"
      pattern: "from \"\\./summarize"
    - from: "src/run.ts main"
      to: "sendToChannel (этот plan)"
      via: "import from ./deliver.js"
      pattern: "from \"\\./deliver"
    - from: "src/deliver.ts sendToChannel"
      to: "Telegram Bot API"
      via: "fetch POST /bot<TOKEN>/sendMessage + parse_mode HTML"
      pattern: "api\\.telegram\\.org/bot"
---

<objective>
Замкнуть пайплайн MVP: доставить HTML-дайджест в приватный Telegram-канал, написать склейку-entrypoint и README для оператора, затем пройти ручную приёмку по 5 критериям §11.

1. **`src/deliver.ts`** — `sendToChannel(html)` отправляет HTML в приватный канал через Telegram Bot API (встроенный `fetch`, без SDK). `chunkHtml(html, 4000)` режет длинный дайджест по закрывающим тегам/переносам строк без разрыва HTML, нумерует части `(i/N)`.

2. **`src/run.ts`** — `main()`: читает `channels.yaml`, создаёт GramJS клиента, последовательно обходит каналы с jitter, обрабатывает пустой день, вызывает summarize+deliver, корректно завершает процесс.

3. **`README.md`** — 3 команды запуска, ссылки где получить секреты, дисциплина частоты запусков.

4. **Финальный checkpoint** — ручная приёмка по 5 критериям §11 spec-app.md. Гейт: оператор подтверждает «approved» или фиксирует дефекты.

Output:
- Рабочий `npm start` end-to-end (собрать → LLM → отправить).
- Воспроизводимый «пустой день» сценарий (exit 0, без LLM и Telegram).
- README, по которому новый оператор запустит проект в 3 команды.
- Подтверждённая приёмка.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-mvp/01-CONTEXT.md
@.planning/phases/01-mvp/01-01-PLAN.md
@.planning/phases/01-mvp/01-02-PLAN.md
@spec-app.md
@CLAUDE.md

<interfaces>
Импорты из предыдущих планов:

From plan 01 (package.json + .env.example + channels.yaml):
- process.env.TG_BOT_TOKEN: string
- process.env.TG_CHANNEL_ID: string ("-100xxxxxxxxxx")
- process.env.CHANNEL_DELAY_MS: string (default "1000")
- process.env.MAX_MESSAGES_PER_CHANNEL: string (default "50")
- process.env.FETCH_WINDOW_HOURS: string (default "24")
- channels.yaml со структурой { channels: [{ username, priority }] }

From plan 02 (src/types.ts + src/telegram.ts + src/summarize.ts):
- import type { Post } from "./types.js"
- import { createClient, fetchLast24h, sleep, randomInt } from "./telegram.js"
- import { summarize } from "./summarize.js"

Новые экспорты этого плана:
    // src/deliver.ts
    export function chunkHtml(html: string, max: number): string[];
    export async function sendToChannel(html: string): Promise<void>;

    // src/run.ts
    export async function main(): Promise<void>;  // вызывается в конце файла с глобальным catch
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Создать src/deliver.ts — chunkHtml + sendToChannel через Bot API fetch</name>
  <files>src/deliver.ts</files>

  <read_first>
    - spec-app.md §7.5 (src/deliver.ts — fetch + parse_mode HTML + chunkHtml)
    - .planning/REQUIREMENTS.md (DELIVER-01, DELIVER-02, DELIVER-03, DELIVER-04)
    - .planning/phases/01-mvp/01-CONTEXT.md (§Claude's Discretion — chunkHtml на усмотрение, не рвать HTML посередине)
    - CLAUDE.md (Telegram Bot API лимит 4096 → режем с запасом ~4000)
  </read_first>

  <action>
Создай `src/deliver.ts`. Файл не имеет runtime-зависимостей — только встроенный `fetch` (Node 20.6+) и `process.env`.

Точная реализация:

    // src/deliver.ts — доставка HTML-дайджеста в приватный Telegram-канал через Bot API.
    // Использует встроенный fetch (Node 20.6+), без каких-либо SDK.

    const TELEGRAM_LIMIT = 4096;
    const CHUNK_SAFE_LIMIT = 4000;  // запас ~96 символов на префикс "(i/N)\n" + заголовки тегов

    /**
     * Режет HTML-строку на части, каждая ≤ max символов.
     * DELIVER-02: не рвём HTML посередине тега. Приоритеты разрыва:
     *   1. Двойной перенос (\n\n) — граница секций, идеальный разрыв.
     *   2. Одиночный перенос (\n) — граница строки/буллета.
     *   3. Пробел — как последний fallback, но только ВНЕ открытого тега.
     *
     * Если строка ≤ max — возвращаем [html].
     */
    export function chunkHtml(html: string, max: number = CHUNK_SAFE_LIMIT): string[] {
      if (html.length <= max) return [html];

      const parts: string[] = [];
      let remaining = html;

      while (remaining.length > max) {
        // Ищем лучший разрыв в пределах [0, max].
        const window = remaining.slice(0, max);

        // Приоритет 1: последний \n\n в окне.
        let cutAt = window.lastIndexOf("\n\n");
        if (cutAt < Math.floor(max * 0.5)) {
          // Приоритет 2: последний \n.
          const singleLf = window.lastIndexOf("\n");
          if (singleLf > cutAt) cutAt = singleLf;
        }
        if (cutAt < Math.floor(max * 0.5)) {
          // Приоритет 3: последний пробел, но ДО первого незакрытого "<".
          // Простая эвристика — ищем последний пробел, чтобы не разрезать "<a href".
          const lastSpace = window.lastIndexOf(" ");
          if (lastSpace > cutAt) cutAt = lastSpace;
        }
        if (cutAt <= 0) {
          // Не нашли хорошего разрыва — режем по max (крайний случай, не должен возникать
          // на нашей HTML-структуре, где всё разделено \n\n между секциями).
          cutAt = max;
        }

        parts.push(remaining.slice(0, cutAt).trimEnd());
        remaining = remaining.slice(cutAt).trimStart();
      }
      if (remaining.length > 0) parts.push(remaining);
      return parts;
    }

    /**
     * Отправляет html в приватный канал через Bot API sendMessage.
     * DELIVER-01: parse_mode HTML, disable_web_page_preview true.
     * DELIVER-03: если частей > 1, каждая префиксуется "(i/N)\n".
     * DELIVER-04: res.ok === false → throw Error с HTTP-статусом и телом.
     */
    export async function sendToChannel(html: string): Promise<void> {
      const token = process.env.TG_BOT_TOKEN;
      const chatId = process.env.TG_CHANNEL_ID;
      if (!token) throw new Error("TG_BOT_TOKEN не задан.");
      if (!chatId) throw new Error("TG_CHANNEL_ID не задан.");

      const parts = chunkHtml(html, CHUNK_SAFE_LIMIT);
      for (let i = 0; i < parts.length; i++) {
        const text = parts.length > 1 ? `(${i + 1}/${parts.length})\n${parts[i]}` : parts[i];

        // Лимит Telegram — 4096; с запасом 4000 + префикс 10 символов нам хватит.
        if (text.length > TELEGRAM_LIMIT) {
          throw new Error(`Telegram message part ${i + 1} exceeds ${TELEGRAM_LIMIT} chars: ${text.length}`);
        }

        const body = {
          chat_id: chatId,
          text,
          parse_mode: "HTML",
          disable_web_page_preview: true,
        };
        const res = await fetch(`https://api.telegram.org/bot${token}/sendMessage`, {
          method: "POST",
          headers: { "content-type": "application/json" },
          body: JSON.stringify(body),
        });
        if (!res.ok) {
          // DELIVER-04: HTTP-статус + тело ответа.
          const responseBody = await res.text();
          throw new Error(`Telegram sendMessage failed: ${res.status} ${responseBody}`);
        }
      }
    }

Важные детали:
- `const CHUNK_SAFE_LIMIT = 4000` — с запасом от лимита 4096 (оставляет ~96 символов на префикс `(i/N)\n` и непопадание в край при split).
- `chunkHtml` не парсит HTML — только эвристика по `\n\n` / `\n` / пробелу. Это ок: в D-12 мы выбрали `\n\n` между секциями, так что обычный дайджест распределится по секционным границам.
- `text.length > TELEGRAM_LIMIT` — дополнительная страховка: если чанк с префиксом всё-таки превысит 4096, лучше упасть с понятной ошибкой, чем получить 400 от Bot API.
- `fetch` — Node 20 built-in, без `import`.
- Никаких `console.log` в этом модуле: логгирование — в `src/run.ts`.

После записи — `npx tsc --noEmit` должен пройти.
  </action>

  <verify>
    <automated>test -f src/deliver.ts && grep -q "export function chunkHtml" src/deliver.ts && grep -q "export async function sendToChannel" src/deliver.ts && grep -q "api.telegram.org/bot" src/deliver.ts && grep -q 'parse_mode: "HTML"' src/deliver.ts && grep -q "disable_web_page_preview: true" src/deliver.ts && grep -q "!res.ok" src/deliver.ts && grep -q "TG_BOT_TOKEN" src/deliver.ts && grep -q "TG_CHANNEL_ID" src/deliver.ts && grep -q "(i/N)\\|i + 1\\|/\\${parts.length}" src/deliver.ts && npx tsc --noEmit</automated>
  </verify>

  <acceptance_criteria>
    - `src/deliver.ts` экспортирует `chunkHtml(html: string, max?: number): string[]` и `sendToChannel(html: string): Promise<void>`
    - `sendToChannel` читает `process.env.TG_BOT_TOKEN` и `process.env.TG_CHANNEL_ID` и бросает Error если не заданы
    - `sendToChannel` вызывает `fetch` по URL, содержащему `api.telegram.org/bot` + token + `/sendMessage`
    - Тело запроса содержит `chat_id`, `text`, `parse_mode: "HTML"`, `disable_web_page_preview: true` (DELIVER-01)
    - При `!res.ok` бросается Error с HTTP-статусом (`res.status`) и телом ответа (`await res.text()`) (DELIVER-04)
    - Если частей больше одной, каждый `text` префиксуется `(${i+1}/${parts.length})\n` (DELIVER-03, grep паттерн `\`\\(\\$\\{i \\+ 1\\}/\\$\\{parts\\.length\\}\\)\``)
    - `chunkHtml` принимает максимальный размер (default ≤ 4000, с запасом от лимита 4096) и возвращает массив строк, каждая ≤ max символов
    - `chunkHtml` предпочитает разрыв по `\n\n`, затем `\n`, затем пробелу (DELIVER-02, grep -q "\\\\n\\\\n" и "lastIndexOf" в src/deliver.ts)
    - `src/deliver.ts` НЕ импортирует никаких сторонних пакетов (grep -v "^import.*from \"openai\\|telegram\\|yaml")
    - `src/deliver.ts` использует встроенный `fetch` (НЕ `import fetch from ...`)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>

  <done>
    `src/deliver.ts` реализует DELIVER-01..04. `sendToChannel` отправляет HTML через Bot API; `chunkHtml` режет длинный HTML по `\n\n`/`\n`/пробелу. При ошибке — Error с HTTP-статусом и телом. `npx tsc --noEmit` без ошибок.
  </done>
</task>

<task type="auto">
  <name>Task 2: Создать src/run.ts — main() entrypoint + пустой день + глобальный catch</name>
  <files>src/run.ts</files>

  <read_first>
    - spec-app.md §7.6 (src/run.ts — main() + пустой день + catch)
    - .planning/REQUIREMENTS.md (RUN-01, RUN-02, RUN-03, FETCH-06)
    - .planning/phases/01-mvp/01-02-PLAN.md (interfaces: createClient, fetchLast24h, summarize, sleep, randomInt, Post)
    - src/telegram.ts и src/summarize.ts (должны уже существовать после plan 02 Task 1/2)
    - src/deliver.ts (должен уже существовать после Task 1 этого плана)
    - channels.yaml (формат { channels: [{ username, priority }] })
    - CLAUDE.md (Node 20.6+, ESM, без билда)
  </read_first>

  <action>
Создай `src/run.ts` — единственный entrypoint, который запускается через `npm start`. Файл склеивает всё: config → GramJS → посты → LLM → доставка.

Точная реализация:

    // src/run.ts — entrypoint MVP. Запуск: `npm start`.
    // Склейка: channels.yaml → GramJS → Post[] → DeepSeek → HTML → Bot API.

    import { readFileSync } from "node:fs";
    import yaml from "yaml";
    import type { Post } from "./types.js";
    import { createClient, fetchLast24h, sleep, randomInt } from "./telegram.js";
    import { summarize } from "./summarize.js";
    import { sendToChannel } from "./deliver.js";

    interface ChannelEntry {
      username: string;
      priority?: number;
    }

    interface ChannelsFile {
      channels: ChannelEntry[];
    }

    function loadChannelsYaml(path: string): ChannelEntry[] {
      const raw = readFileSync(path, "utf8");
      const parsed = yaml.parse(raw) as ChannelsFile | null;
      if (!parsed || !Array.isArray(parsed.channels)) {
        throw new Error(`${path}: корневой ключ "channels" отсутствует или не массив`);
      }
      const channels: ChannelEntry[] = [];
      for (const c of parsed.channels) {
        if (!c || typeof c.username !== "string" || !c.username) {
          throw new Error(`${path}: запись без строкового поля username: ${JSON.stringify(c)}`);
        }
        channels.push({ username: c.username, priority: c.priority });
      }
      if (channels.length < 1) {
        throw new Error(`${path}: список channels пуст`);
      }
      return channels;
    }

    export async function main(): Promise<void> {
      const channels = loadChannelsYaml("./channels.yaml");
      console.log(`[run] каналов в списке: ${channels.length}`);

      const limit = Number(process.env.MAX_MESSAGES_PER_CHANNEL ?? 50);
      const windowHours = Number(process.env.FETCH_WINDOW_HOURS ?? 24);
      const channelDelayMs = Number(process.env.CHANNEL_DELAY_MS ?? 1000);

      // RUN-01: создаём клиент, подключаемся
      const client = createClient();
      await client.connect();

      const posts: Post[] = [];
      try {
        for (let i = 0; i < channels.length; i++) {
          const { username } = channels[i];
          try {
            const fetched = await fetchLast24h(client, username, { limit, windowHours });
            posts.push(...fetched);
            console.log(`[run] ${username}: ${fetched.length} постов`);
          } catch (err) {
            // FETCH-04 второй подряд FloodWait проброшен наверх — разрываем весь прогон.
            throw err;
          }
          // FETCH-06: jitter между каналами (кроме после последнего).
          if (i < channels.length - 1) {
            await sleep(channelDelayMs + randomInt(0, 500));
          }
        }
      } finally {
        await client.disconnect();
      }

      console.log(`[run] всего собрано: ${posts.length} постов из ${new Set(posts.map((p) => p.channelUsername)).size} каналов`);

      // RUN-02: пустой день.
      if (posts.length === 0) {
        console.log("No posts in window — skipping digest");
        process.exit(0);
      }

      // RUN-03: summarize → deliver → exit 0.
      const html = await summarize(posts);
      await sendToChannel(html);
      console.log("[run] дайджест отправлен.");
      process.exit(0);
    }

    // Entrypoint: запускаем main() с глобальным catch.
    main().catch((err) => {
      // RUN-03: любая ошибка → stderr + exit 1.
      console.error("[run] Фатальная ошибка:", err);
      process.exit(1);
    });

Важные детали:
- `import yaml from "yaml"` — дефолт-экспорт библиотеки `yaml` v2. Если TS ругается на default export — проверь `tsconfig.json` (у нас `esModuleInterop: true` и `allowSyntheticDefaultImports: true`, должно работать).
- `channels[i].username` — деструктуризация: `const { username } = channels[i]`. Если в TS strict жалуется на индекс — можно явно: `channels[i]!.username`, но в массиве с проверкой `i < channels.length` это безопасно.
- FETCH-06 (jitter) — ЗДЕСЬ (не в src/telegram.ts), как оговорено в plan 02. Между каналами, не после последнего.
- RUN-02: `posts.length === 0` → `console.log("No posts in window — skipping digest")` + `process.exit(0)`. НЕ вызываем `summarize` и `sendToChannel`.
- RUN-03: глобальный `.catch(err => { console.error(err); process.exit(1) })` на `main()`.
- `client.disconnect()` — в `finally`, чтобы не держать MTProto-сокет открытым при ошибке.
- Все логи — `console.log` / `console.error`. По принципу stderr-only (D-05) — `warn` шёл в src/summarize.ts и src/telegram.ts, здесь оператор видит прогресс и ошибки в обычных потоках.

После записи — `npx tsc --noEmit` должен пройти.
  </action>

  <verify>
    <automated>test -f src/run.ts && grep -q "loadChannelsYaml" src/run.ts && grep -q 'channels.yaml' src/run.ts && grep -q "createClient" src/run.ts && grep -q "fetchLast24h" src/run.ts && grep -q "summarize" src/run.ts && grep -q "sendToChannel" src/run.ts && grep -q "No posts in window" src/run.ts && grep -q "process.exit(0)" src/run.ts && grep -q "process.exit(1)" src/run.ts && grep -q "CHANNEL_DELAY_MS" src/run.ts && grep -q "randomInt" src/run.ts && grep -q "disconnect" src/run.ts && grep -q "main().catch" src/run.ts && npx tsc --noEmit</automated>
  </verify>

  <acceptance_criteria>
    - `src/run.ts` импортирует `readFileSync` из `node:fs` и `yaml` из `yaml`
    - `src/run.ts` импортирует `createClient`, `fetchLast24h`, `sleep`, `randomInt` из `./telegram.js`
    - `src/run.ts` импортирует `summarize` из `./summarize.js`
    - `src/run.ts` импортирует `sendToChannel` из `./deliver.js`
    - `src/run.ts` содержит функцию `loadChannelsYaml` которая парсит YAML и валидирует формат `{ channels: [{ username }] }`
    - `src/run.ts` экспортирует `async function main(): Promise<void>` (RUN-01)
    - `main()` вызывает `createClient()` и `client.connect()` (RUN-01)
    - `main()` итерирует по каналам и между ними делает `await sleep(CHANNEL_DELAY_MS + randomInt(0, 500))` (FETCH-06)
    - `main()` вызывает `client.disconnect()` в `finally` (RUN-01)
    - `main()` содержит проверку `posts.length === 0`, логирует `"No posts in window — skipping digest"`, делает `process.exit(0)` БЕЗ вызова `summarize` или `sendToChannel` (RUN-02; проверить что блок `if (posts.length === 0)` идёт ПЕРЕД вызовами `summarize(posts)` и `sendToChannel(html)`)
    - `main()` в happy path вызывает `summarize(posts)` → `sendToChannel(html)` → `process.exit(0)` (RUN-03)
    - Файл завершается `main().catch((err) => { console.error(...); process.exit(1); })` (RUN-03)
    - `src/run.ts` читает `MAX_MESSAGES_PER_CHANNEL`, `FETCH_WINDOW_HOURS`, `CHANNEL_DELAY_MS` из `process.env` с дефолтами `50` / `24` / `1000`
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>

  <done>
    `src/run.ts` — рабочий entrypoint. `main()` собирает каналы → посты → (при пустом дне выходит 0, иначе) → LLM → доставка. Глобальный catch ловит любые ошибки и завершает с exit 1. `client.disconnect()` вызывается в finally. `npx tsc --noEmit` без ошибок.
  </done>
</task>

<task type="auto">
  <name>Task 3: Создать README.md — 3 команды запуска + подписка на каналы + бот-админ + дисциплина 10–15 мин</name>
  <files>README.md</files>

  <read_first>
    - spec-app.md §10 (запуск локально — 6 шагов), §11 (5 критериев приёмки), §9 (чек-лист anti-ban, пункт про частоту запусков)
    - .planning/REQUIREMENTS.md (OPS-01, OPS-02)
    - .planning/PROJECT.md (Core Value + What This Is)
    - package.json (scripts.login, scripts.start — имена должны совпадать с тем, что в README)
    - .env.example (список переменных — должен быть референс в README)
    - channels.yaml (README должен говорить, что оператор подписан на все username оттуда)
  </read_first>

  <action>
Создай `README.md` — первое, что читает новый оператор. Структура:

1. **Название + Core Value** (1 абзац).
2. **Требования** — Node 20.6+, macOS/Linux, Telegram-аккаунт для user-session.
3. **Запуск в 3 команды** — `npm install` → `npm run login` → `npm start`. С разъяснением каждой.
4. **Подготовка перед первым прогоном** — получить TG_API_ID/HASH, токен бота, ID канала, подписаться на каналы, добавить бота админом.
5. **Дисциплина оператора** — не чаще одного прогона в 10–15 минут (OPS-01 явно требует, §9 чек-лист anti-ban пункт 7).
6. **Как проверить, что всё работает** — 5 критериев §11 spec-app.md в явном виде.
7. **Известные ограничения** — ссылка на §12 spec-app.md (нет персистентности, нет дедупа, только публичные каналы).
8. **Troubleshooting** — основные ошибки и что делать.

Содержимое (markdown):

    # tg-parser-demo

    За один `npm start` получить в закрытом Telegram-канале HTML-дайджест событий российского нефтегаза/нефтехимии за последние 24 часа, где каждая цитата дословно присутствует в исходном посте — без галлюцинаций LLM.

    Один Node.js-скрипт, запускается руками. Без БД, без Docker, без крона.

    ## Требования

    - Node.js **20.6+** (нужен флаг `--env-file` без `dotenv`).
    - Telegram-аккаунт, подписанный на все каналы из `channels.yaml`.
    - DeepSeek API-ключ (https://platform.deepseek.com).
    - Личный приватный Telegram-канал, в который бот будет постить дайджест.

    ## Запуск в 3 команды

        npm install
        npm run login     # разовая генерация TG_SESSION
        npm start         # собрать за 24ч → DeepSeek → отправить в канал

    ## Подготовка перед первым прогоном

    ### 1. Установка зависимостей

        npm install

    Будет установлено ровно три runtime-зависимости: `telegram` (GramJS), `openai` (DeepSeek через OpenAI-совместимый SDK), `yaml`.

    ### 2. Секреты и конфиг

        cp .env.example .env

    Заполни в `.env`:

    - `TG_API_ID`, `TG_API_HASH` — возьми на https://my.telegram.org → API development tools → Create application.
    - `TG_BOT_TOKEN` — создай бота через [@BotFather](https://t.me/BotFather) (`/newbot`).
    - `TG_CHANNEL_ID` — создай приватный Telegram-канал, добавь бота админом (право Post Messages), пересылкой любого сообщения из канала на [@username_to_id_bot](https://t.me/username_to_id_bot) получи ID вида `-100xxxxxxxxxx`.
    - `DEEPSEEK_API_KEY` — https://platform.deepseek.com → API keys.

    `TG_SESSION` оставь пустым — его сгенерирует следующий шаг.

    ### 3. Разовая генерация TG_SESSION

        npm run login

    Скрипт спросит телефон (в международном формате), код из Telegram и (если включена) 2FA-пароль. После успеха он напечатает строку StringSession — **скопируй её в `.env` как `TG_SESSION`**.

    **Не публикуй `TG_SESSION`** — это полный доступ к твоему user-аккаунту. Файл `.env` уже в `.gitignore`.

    ### 4. Подписка user-аккаунта на каналы

    User-аккаунт, чей `TG_SESSION` лежит в `.env`, **должен быть подписан** на каждый канал из `channels.yaml` — иначе GramJS выбросит `ChannelPrivateError` и канал будет пропущен.

    Список по умолчанию в `channels.yaml` — отраслевые каналы российского нефтегаза/нефтехимии. Можно заменить на свой список (10–15 публичных каналов по `username`).

    ### 5. Первый прогон

        npm start

    Что произойдёт:

    1. Скрипт обойдёт каналы по одному (с задержкой `CHANNEL_DELAY_MS + 0–500ms jitter`).
    2. Соберёт посты за последние `FETCH_WINDOW_HOURS=24` часа, до `MAX_MESSAGES_PER_CHANNEL=50` на канал.
    3. Если постов 0 — напишет `No posts in window — skipping digest` и выйдет с кодом 0.
    4. Иначе — отправит батч в DeepSeek, получит JSON с 3–6 секциями, проверит дословность каждой `keyQuote` в исходных постах, отрендерит HTML.
    5. Отправит HTML в приватный канал через Bot API (`parse_mode: HTML`). Если больше 4000 символов — нарежет на части `(i/N)`.

    ## Дисциплина запусков

    **Не чаще одного прогона в 10–15 минут.** В коде защиты нет — это обязанность оператора.

    Почему: Telegram MTProto может отдать `FloodWaitError` на подозрительно частых запросах, и user-аккаунт временно заблокируется. Одного прогона достаточно, чтобы покрыть 24-часовое окно, — повторы дают ту же новость и грузят API впустую.

    Если после `FloodWaitError` скрипт сделал retry и снова получил FloodWait — прогон прервётся с exit 1. Подожди 30–60 минут и попробуй снова.

    ## Как проверить, что всё работает (5 критериев приёмки)

    1. **Сбор быстрый.** `npm start` на 15 каналах завершается за < 60 секунд и не выбрасывает `FloodWaitError`.
    2. **Дословность цитат.** Для выборки из 20 постов каждый `keyQuote` в пришедшем HTML-дайджесте дословно найден в исходном тексте поста (проверяется вручную — открываешь ссылку `@channel/messageId` и ищешь цитату).
    3. **HTML рендерится.** В приватный канал приходит одно сообщение или корректно пронумерованные части `(1/N)`, `parse_mode: HTML` рендерится без ошибок, теги `<b>` / `<i>` / `<a>` видны как форматирование.
    4. **Идемпотентность.** Повторный запуск через 15+ минут не триггерит FloodWait и не крашится (может отправить тот же дайджест повторно — это допустимо в MVP).
    5. **Пустой день.** Если за 24 часа ни одного поста нет (выходные, технические выходные каналов) — скрипт логирует `No posts in window — skipping digest`, выходит с кодом 0, DeepSeek и Telegram не дёргаются.

    ## Известные ограничения

    См. `spec-app.md` §12:

    - Нет персистентности — одна и та же новость может попасть в дайджест несколько дней подряд, если канал её продолжает репостить.
    - Нет дедупликации между каналами — один инфоповод из трёх каналов займёт три строки в дайджесте (LLM частично гасит, беря только 15 «самых содержательных»).
    - Классификация тем — LLM генерирует на каждом прогоне, темы могут плавать между запусками.
    - Приватные каналы по `invite hash` не поддерживаются (только публичные по `username`).
    - Ретраев на уровне всего прогона нет — если DeepSeek или Telegram упали, скрипт выходит с кодом 1, оператор перезапускает руками.

    ## Troubleshooting

    ### `TG_SESSION не задан`

    Ты пропустил шаг 3. Запусти `npm run login` и скопируй StringSession в `.env`.

    ### `ChannelPrivateError` / канал пропускается

    User-аккаунт не подписан на этот канал. Открой Telegram-клиент под тем же номером, что использовался в `npm run login`, и подпишись на канал.

    ### Второй `FloodWaitError` подряд → exit 1

    Ты запускаешь слишком часто или прогон попал в квоту. Подожди 30–60 минут и попробуй снова. Соблюдай дисциплину «не чаще одного прогона в 10–15 минут».

    ### `Telegram sendMessage failed: 400`

    Скорее всего HTML-теги разрушились при разрезе. Проверь, что `src/summarize.ts` не был изменён и экранирует `<`, `>`, `&` в пользовательском тексте.

    ### DeepSeek вернул невалидный JSON

    Скрипт выходит с `exit 1` и печатает первые 500 символов raw-ответа. Проверь, что `DEEPSEEK_MODEL=deepseek-chat` и `response_format: json_object` корректно переданы. При повторе проблемы — открыть issue.

    ## Структура проекта

        ./
        ├── package.json              # scripts: login, start + 3 runtime-зависимости
        ├── tsconfig.json             # strict, ESNext, moduleResolution: bundler
        ├── .env.example              # шаблон секретов и параметров
        ├── channels.yaml             # список публичных каналов
        ├── README.md                 # этот файл
        ├── scripts/
        │   └── login.ts              # разовая генерация TG_SESSION
        └── src/
            ├── types.ts              # Post, DigestJson, DigestItem, DigestSection
            ├── telegram.ts           # createClient + fetchLast24h (GramJS)
            ├── summarize.ts          # DeepSeek + валидация keyQuote + renderHtml
            ├── deliver.ts            # sendToChannel + chunkHtml (Bot API)
            └── run.ts                # entrypoint: fetch → summarize → deliver

    ## Лицензия

    Private / personal use.

Важно:
- Никаких автотестов не упоминается — их нет по принципу MVP (`spec-app.md` §Out of scope).
- `npm start` — дословное имя script'а (совпадает с `package.json`).
- Дисциплина «10–15 минут» упомянута в отдельном разделе — OPS-01 требует прямой фиксации.
- Ссылки на ресурсы — кликабельные markdown-ссылки.
  </action>

  <verify>
    <automated>test -f README.md && grep -q "npm install" README.md && grep -q "npm run login" README.md && grep -q "npm start" README.md && grep -q "10.15" README.md && grep -q "дисциплин" README.md && grep -q "BotFather" README.md && grep -q "my.telegram.org" README.md && grep -q "DeepSeek" README.md && grep -q "username_to_id_bot\|TG_CHANNEL_ID" README.md && grep -q "No posts in window" README.md && grep -q "подписан" README.md && grep -q "FloodWait" README.md</automated>
  </verify>

  <acceptance_criteria>
    - `README.md` существует в корне проекта
    - Содержит Core Value / описание проекта (1 абзац с упоминанием «дайджест» / «дословн»)
    - Содержит блок с тремя командами в порядке: `npm install` → `npm run login` → `npm start`
    - Упоминает `my.telegram.org` (где брать TG_API_ID/HASH)
    - Упоминает `@BotFather` или `BotFather` (где брать TG_BOT_TOKEN)
    - Упоминает `@username_to_id_bot` или эквивалентный способ получить TG_CHANNEL_ID
    - Упоминает, что user-аккаунт должен быть **подписан** на каналы из `channels.yaml` (grep -q "подписан" README.md)
    - Содержит явную фиксацию дисциплины «не чаще одного прогона в **10–15** минут» (OPS-01; grep -q "10.15" README.md где `.` — любой разделитель)
    - Упоминает 5 критериев приёмки из §11 (сбор <60с, дословность цитат, HTML-рендер, идемпотентность, пустой день) — хотя бы по одному ключевому слову на критерий
    - Упоминает `No posts in window` как ожидаемое поведение при пустом дне
    - Содержит секцию Troubleshooting с упоминанием `FloodWait`, `ChannelPrivateError`, `TG_SESSION`
    - Упоминает, что `.env` в `.gitignore` / не публиковать `TG_SESSION` (T-05 защита через документацию)
    - НЕ упоминает автотесты / jest / vitest (они out of scope в MVP)
  </acceptance_criteria>

  <done>
    README.md описывает запуск в 3 команды, подготовку (TG_API_ID, токен бота, ID канала, подписка, бот-админ), дисциплину «10–15 минут», 5 критериев приёмки, известные ограничения и troubleshooting. Новый оператор может запустить проект по README без дополнительных вопросов.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Ручная приёмка MVP по 5 критериям §11 spec-app.md</name>
  <files>— (checkpoint; код не модифицируется)</files>
  <action>
    Перед checkpoint убедись, что все предыдущие 3 задачи плана выполнены: src/deliver.ts, src/run.ts, README.md созданы, `npx tsc --noEmit` exits 0, `npm install` успешен. После этого выполни пошаговую верификацию из секции `<how-to-verify>` и получи подтверждение оператора. Это blocking-чекпойнт — plan не считается завершённым до ответа оператора.
  </action>
  <verify>
    <automated>test -f src/deliver.ts && test -f src/run.ts && test -f README.md && npx tsc --noEmit</automated>
  </verify>
  <done>
    Оператор ответил `approved` по всем 5 критериям §11 spec-app.md, либо список дефектов зафиксирован в SUMMARY для последующего `/gsd-plan-phase 1 --gaps`.
  </done>

  <what-built>
    Полный MVP-пайплайн tg-parser-demo:
    - `package.json`, `tsconfig.json`, `.env.example`, `channels.yaml`, `.gitignore` (plan 01)
    - `scripts/login.ts` — разовая генерация TG_SESSION (plan 01)
    - `src/types.ts` — общие типы (plan 02)
    - `src/telegram.ts` — GramJS client с anti-ban identity + fetchLast24h (plan 02)
    - `src/summarize.ts` — DeepSeek + серверная проверка keyQuote + renderHtml (plan 02)
    - `src/deliver.ts` — sendToChannel + chunkHtml через Bot API (plan 03)
    - `src/run.ts` — main() entrypoint с пустым днём и глобальным catch (plan 03)
    - `README.md` — инструкция запуска в 3 команды + дисциплина (plan 03)
  </what-built>

  <how-to-verify>
    Выполни ручную приёмку по 5 критериям §11 spec-app.md. Перед проверкой:

    ## Подготовка (если ещё не сделано)

    1. `cp .env.example .env` и заполни TG_API_ID, TG_API_HASH, TG_BOT_TOKEN, TG_CHANNEL_ID, DEEPSEEK_API_KEY.
    2. `npm install` — должен пройти без ошибок (только `openai`, `telegram`, `yaml` в dependencies).
    3. `npm run login` — пройди интерактивный логин, скопируй StringSession в `.env` как `TG_SESSION`.
    4. Подпишись user-аккаунтом на все каналы из `channels.yaml` в Telegram-клиенте.
    5. Создай приватный канал, добавь бота (по TG_BOT_TOKEN) администратором с правом Post Messages, получи TG_CHANNEL_ID (`-100xxxxxxxxxx`) и впиши в `.env`.

    ## Критерий 1: Сбор быстрый и без FloodWait

       time npm start

    Ожидаемо:
    - Команда завершается за **< 60 секунд** на 15 каналах.
    - В stderr/stdout НЕТ строк `FloodWaitError` или `Second FloodWait`.
    - В логах видно `[run] <channel>: <N> постов` для каждого канала (FETCH-05: недоступные каналы логируются как warn и пропускаются — это ок).

    ## Критерий 2: Дословность цитат

    Открой приватный канал с пришедшим дайджестом. Выбери **20 случайных буллетов** из любых секций. Для каждого:

    1. Извлеки `keyQuote` (текст между `«` и `»`).
    2. Кликни на ссылку `@channel` — она ведёт на `https://t.me/<channel>/<messageId>`.
    3. Открой пост, скопируй его текст, проверь через Ctrl+F / Cmd+F что цитата **дословно** присутствует.

    Ожидаемо: **20/20** совпадений. Любое несовпадение — дефект (в коде верификация `post.text.includes(keyQuote.trim())` должна была пресечь такой item).

    ## Критерий 3: Корректная HTML-доставка

    В приватном канале:

    - Если дайджест ≤ 4000 символов — одно сообщение.
    - Если больше — **корректно пронумерованные части** `(1/N)`, `(2/N)`, ..., `(N/N)`.
    - Теги `<b>Заголовок</b>` отображаются как **жирный текст**.
    - Теги `<i>«цитата»</i>` отображаются как *курсив*.
    - `<a href="...">@channel</a>` — кликабельная ссылка, ведёт на конкретный пост в Telegram.
    - НЕТ видимых сырых тегов (`<b>`, `&amp;`, `&lt;`), НЕТ ошибки `Bad Request: can't parse entities`.

    ## Критерий 4: Пустой день

    Временно (не коммитя) подмени `.env`:

       FETCH_WINDOW_HOURS=0.01

    Запусти `npm start`. Ожидаемо:
    - В stdout появляется строка `No posts in window — skipping digest`.
    - Скрипт завершается с `exit 0` (в bash: `echo $?` = `0`).
    - В приватный канал **ничего не приходит**.
    - В логах НЕТ обращений к DeepSeek и Telegram Bot API.

    Верни `FETCH_WINDOW_HOURS=24` в `.env`.

    ## Критерий 5: Идемпотентность

    Подожди **15 минут** с момента первого прогона. Запусти `npm start` ещё раз.

    Ожидаемо:
    - Скрипт завершается успешно (exit 0).
    - НЕТ `FloodWaitError`.
    - В канал приходит второй дайджест (возможно, с пересекающимися новостями — это ок в MVP).

    ## Итоговое подтверждение

    Ответь одним из вариантов:

    - `approved` — все 5 критериев пройдены.
    - `defect: <описание>` — один или несколько критериев не пройдены; опиши что именно.
  </how-to-verify>

  <resume-signal>Ответь `approved` или `defect: <описание>`</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| src/run.ts ↔ channels.yaml | YAML с диска, парсится `yaml.parse`, валидируется loadChannelsYaml |
| src/deliver.ts ↔ Telegram Bot API | HTTPS к api.telegram.org; ответ — JSON, проверяется `res.ok` |
| process.env ↔ src/deliver.ts | TG_BOT_TOKEN читается один раз, не логируется |
| README.md ↔ оператор | документация — источник дисциплины; код защиты от частых запусков НЕТ |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01 | Information Disclosure (secrets leak) | `.env` / README | mitigate | README явно предупреждает «Не публикуй TG_SESSION» и «`.env` в `.gitignore`» (Task 3, acceptance: grep "секрет\|не публикуй\|.gitignore"). `.gitignore` с `.env` создан в plan 01. |
| T-02 | Tampering (HTML injection в Telegram) | `src/deliver.ts sendToChannel` | mitigate | `sendToChannel` передаёт `parse_mode: "HTML"` и `disable_web_page_preview: true` (Task 1, DELIVER-01). Сам HTML уже проэкранирован в plan 02 (summarize.ts renderHtml + escapeHtml). При ошибке parse_mode Bot API вернёт 400 → DELIVER-04 бросит Error → src/run.ts поймает → exit 1. |
| T-06 | Denial of Service (FloodWait ban) | `src/run.ts` / README | mitigate | FETCH-06 (jitter sleep между каналами) реализован в src/run.ts (Task 2). README фиксирует дисциплину «не чаще одного прогона в 10–15 минут» (Task 3, OPS-01). FloodWait обрабатывается в src/telegram.ts (plan 02); второй подряд — прерывает прогон с exit 1 (FETCH-04). |

## Accepted Risks

- **T-01 (secrets leak)**: `.env` защищён только `.gitignore` и дисциплиной README. Оператор может случайно закоммитить модифицированный `.env` с `git add -f`. Дополнительных защит (pre-commit hook) в MVP нет — это дисциплина, не код.
- **T-06 частичный**: «не чаще одного прогона в 10–15 минут» — документация, в коде защиты НЕТ. Оператор может проигнорировать и спровоцировать FloodWait. Второй FloodWait подряд прервёт прогон с exit 1 (FETCH-04), но аккаунт может получить временный бан от Telegram.
</threat_model>

<verification>
После завершения всех task'ов:

1. **Файлы существуют:**

       test -f src/deliver.ts && test -f src/run.ts && test -f README.md

2. **TypeScript компилируется:**

       npx tsc --noEmit

3. **Зависимости src/run.ts:**

       grep -q 'from "./telegram' src/run.ts
       grep -q 'from "./summarize' src/run.ts
       grep -q 'from "./deliver' src/run.ts
       grep -q 'from "yaml"' src/run.ts

4. **DELIVER-01..04:**

       grep -q 'api.telegram.org/bot' src/deliver.ts
       grep -q 'parse_mode: "HTML"' src/deliver.ts
       grep -q 'disable_web_page_preview: true' src/deliver.ts
       grep -q '!res.ok' src/deliver.ts

5. **RUN-01..03:**

       grep -q 'loadChannelsYaml' src/run.ts
       grep -q 'No posts in window' src/run.ts
       grep -q 'process.exit(0)' src/run.ts
       grep -q 'process.exit(1)' src/run.ts

6. **OPS-01 в README:**

       grep -q "npm install" README.md
       grep -q "npm run login" README.md
       grep -q "npm start" README.md
       grep -q "10.15" README.md    # «10–15» с любым en-dash или -
       grep -q "дисциплин" README.md

7. **Checkpoint (OPS-02):** ручная приёмка по 5 критериям пройдена оператором (`approved`).
</verification>

<success_criteria>
- [ ] `src/deliver.ts` реализует `sendToChannel` (DELIVER-01, DELIVER-04) и `chunkHtml` (DELIVER-02, DELIVER-03)
- [ ] `src/run.ts` реализует `main()` с загрузкой channels.yaml, обходом с jitter (FETCH-06), пустым днём (RUN-02), happy path (RUN-03), глобальным catch
- [ ] `README.md` документирует 3 команды, подготовку, дисциплину «10–15 минут», 5 критериев приёмки, troubleshooting
- [ ] `npx tsc --noEmit` exits 0 после всех 3 non-checkpoint tasks
- [ ] Ручная приёмка по 5 критериям §11 пройдена (`approved`)
- [ ] Второй FloodWait подряд действительно завершает прогон с exit 1 (проверяется по коду: src/telegram.ts бросает, src/run.ts ловит в catch)
- [ ] Пустой день НЕ вызывает summarize и sendToChannel (проверяется по коду: RUN-02 блок выше чем вызовы)
</success_criteria>

<output>
После завершения создать `.planning/phases/01-mvp/01-03-SUMMARY.md` со списком:
- созданных файлов (src/deliver.ts, src/run.ts, README.md);
- покрытых requirements (DELIVER-01..04, RUN-01..03, OPS-01, OPS-02);
- итогов ручной приёмки (5/5 критериев или список дефектов);
- статуса Phase 1 (Done / Needs follow-up).

Если приёмка провалена (дефекты) — зафиксировать дефекты в SUMMARY и в STATE.md как todos для закрытия через `/gsd-plan-phase 1 --gaps`.
</output>
