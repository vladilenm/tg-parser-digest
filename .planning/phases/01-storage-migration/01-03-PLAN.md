---
phase: 01-storage-migration
plan: 03
type: execute
wave: 3
depends_on: ["01-01", "01-02"]
files_modified:
  - src/pipeline.ts
autonomous: true
requirements:
  - STORE-01
must_haves:
  truths:
    - "src/pipeline.ts больше не содержит функции loadChannelsYaml и не импортирует yaml-пакет напрямую"
    - "src/pipeline.ts больше не содержит локального interface ChannelEntry/ChannelsFile — ChannelEntry импортируется из channels-store"
    - "runPipeline() читает каналы через loadChannels() из channels-store.ts; при отсутствии channels.json daemon автоматически мигрирует YAML и продолжает работу без падения"
    - "Все 6 счётчиков RunSummary (channelsTotal/Succeeded/Skipped, postsCollected/Deduped/Dropped, digestDelivered) рассчитываются по тем же формулам, что и до миграции — никакая v3.0 функциональность не сломана"
  artifacts:
    - path: "src/pipeline.ts"
      provides: "runPipeline через store-API; удалены loadChannelsYaml + yaml import + ChannelEntry/ChannelsFile interfaces"
      contains: "loadChannels"
  key_links:
    - from: "src/pipeline.ts:runPipeline"
      to: "src/channels-store.ts:loadChannels"
      via: "import + call"
      pattern: "loadChannels\\("
    - from: "src/pipeline.ts (top of file)"
      to: "ChannelEntry from channels-store"
      via: "import type { ChannelEntry }"
      pattern: "import.*ChannelEntry.*channels-store"
---

<objective>
Перевести `src/pipeline.ts` со старого `loadChannelsYaml('./channels.yaml')` на новый `loadChannels()` из `channels-store.ts`. Удалить дублирующиеся определения `ChannelEntry`/`ChannelsFile` и прямой импорт `yaml`. Pipeline продолжает работать без потери функциональности (Fisher-Yates shuffle, per-channel try/catch, dedup, archive — всё нетронуто).

Purpose: Финальная wiring-задача Phase 1 — без неё store-API существует, но pipeline всё ещё читает YAML напрямую (success criterion #1 ROADMAP не выполнен).
Output: Очищенный `src/pipeline.ts`, размер уменьшается на ~25 строк (loadChannelsYaml + ChannelsFile interface + yaml import удалены).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-storage-migration/01-CONTEXT.md
@.planning/phases/01-storage-migration/01-01-SUMMARY.md
@.planning/phases/01-storage-migration/01-02-SUMMARY.md
@src/pipeline.ts
@src/channels-store.ts
@src/types.ts

<interfaces>
<!-- API, на которое мы переходим (создано в Plan 01-01 + 01-02). -->

From src/channels-store.ts:
```typescript
export const CHANNELS_PATH: "./channels.json";
export type ChannelEntry = { username: string; priority?: number };
export function loadChannels(): ChannelEntry[];
export function saveChannels(channels: ChannelEntry[]): Promise<void>;
export function mutate(fn: (current: ChannelEntry[]) => Promise<ChannelEntry[]> | ChannelEntry[]): Promise<void>;
```

Текущий контракт runPipeline в src/pipeline.ts (НЕ меняется):
```typescript
export async function runPipeline(): Promise<RunSummary>;
```

Текущий вызов в src/run.ts:24 (НЕ меняется):
```typescript
const summary = await runPipeline();
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Заменить loadChannelsYaml на loadChannels из channels-store; удалить устаревший код</name>

  <read_first>
    - src/pipeline.ts (полностью — 162 строки)
    - src/channels-store.ts (созданный в Plan 01-01 + 01-02)
  </read_first>

  <files>src/pipeline.ts</files>

  <behavior>
    - import { readFileSync } из node:fs удаляется (он используется ТОЛЬКО в loadChannelsYaml).
    - import yaml from "yaml" удаляется (yaml-пакет всё ещё в package.json для channels-store, но pipeline.ts больше не зависит от него напрямую).
    - Локальные interface ChannelEntry и interface ChannelsFile удаляются.
    - Функция loadChannelsYaml удаляется целиком (lines 23-40).
    - В шапке pipeline.ts добавляется import для ChannelEntry и loadChannels из channels-store.
    - На месте `loadChannelsYaml('./channels.yaml')` (line 52) используется `loadChannels()`.
    - Тип переменной channels остаётся `ChannelEntry[]` (через type-only import).
    - Все остальные части runPipeline (Fisher-Yates shuffle, цикл по каналам, dedup, archive, summarize, deliver) — не трогать ни одного символа.
    - При отсутствии channels.json — pipeline стартует, channels-store сам мигрирует YAML, runPipeline получает массив и продолжает работу (поведение проверяется автоматически через тесты в Plan 01-04).
  </behavior>

  <action>
Открыть `src/pipeline.ts` и применить следующие точечные правки:

**Правка 1: imports (строки 1-12)**

ДО:
```typescript
// src/pipeline.ts — логика одного прогона tg-parser-demo.
// Вызывается из daemon-tick в src/run.ts; сама не знает про node-cron/PM2.

import { readFileSync } from "node:fs";
import yaml from "yaml";
import type { Post, RunSummary } from "./types.js";
import { createClient, fetchLast24h, sleep, randomInt } from "./telegram.js";
import { summarize } from "./summarize.js";
import { sendToChannel } from "./deliver.js";
import { log } from "./logger.js";
import { writeRaw, writeOutput } from "./archive.js";
import { dedupAgainstCache, commitHashCache } from "./dedup.js";
```

ПОСЛЕ:
```typescript
// src/pipeline.ts — логика одного прогона tg-parser-demo.
// Вызывается из daemon-tick в src/run.ts; сама не знает про node-cron/PM2.
// Список каналов читается через src/channels-store.ts (channels.json + auto-migration).

import type { Post, RunSummary } from "./types.js";
import { loadChannels, type ChannelEntry } from "./channels-store.js";
import { createClient, fetchLast24h, sleep, randomInt } from "./telegram.js";
import { summarize } from "./summarize.js";
import { sendToChannel } from "./deliver.js";
import { log } from "./logger.js";
import { writeRaw, writeOutput } from "./archive.js";
import { dedupAgainstCache, commitHashCache } from "./dedup.js";
```

(`readFileSync` и `import yaml` удалены; добавлен `loadChannels` + type-only `ChannelEntry`.)

**Правка 2: удалить interfaces + loadChannelsYaml (строки 14-40)**

Удалить полностью следующие блоки:

```typescript
interface ChannelEntry {
  username: string;
  priority?: number;
}

interface ChannelsFile {
  channels: ChannelEntry[];
}

function loadChannelsYaml(path: string): ChannelEntry[] {
  const raw = readFileSync(path, "utf8");
  const parsed = yaml.parse(raw) as ChannelsFile | null;
  if (!parsed || !Array.isArray(parsed.channels)) {
    throw new Error(`${path}: корневой ключ "channels" отсутствует или не массив`);
  }
  const channels: ChannelEntry[] = [];
  for (const c of parsed.channels) {
    if (!c || typeof c.username !== "string" || !c.username) {
      throw new Error(`${path}: запись без строкового поля username: ${JSON.stringify(c)}`);
    }
    channels.push({ username: c.username, priority: c.priority });
  }
  if (channels.length < 1) {
    throw new Error(`${path}: список channels пуст`);
  }
  return channels;
}
```

После удаления — между блоком импортов и JSDoc-комментарием `/**\n * Один прогон пайплайна:` остаётся одна пустая строка.

**Правка 3: вызов в runPipeline (текущая строка 52)**

ДО:
```typescript
  const channels = loadChannelsYaml("./channels.yaml");
```

ПОСЛЕ:
```typescript
  const channels: ChannelEntry[] = loadChannels();
```

(Аргумент пути убираем — он захардкожен в `CHANNELS_PATH` константе. Тип переменной указан явно для читаемости.)

**Правка 4: обновить JSDoc на функции runPipeline**

В JSDoc-комментарии перед `export async function runPipeline()` есть строка «читает channels.yaml». Заменить на «читает channels.json через src/channels-store.ts».

ДО:
```typescript
/**
 * Один прогон пайплайна: читает channels.yaml, обходит все каналы (per-channel try/catch),
 * дедуплицирует посты в рамках прогона, отдаёт батч DeepSeek'у, шлёт HTML в канал, возвращает RunSummary.
 * Не завершает процесс — ошибки пробрасывает вызывающему (daemon в src/run.ts).
 */
```

ПОСЛЕ:
```typescript
/**
 * Один прогон пайплайна: читает channels.json через channels-store (auto-migration с YAML на первом запуске),
 * обходит все каналы (per-channel try/catch), дедуплицирует посты в рамках прогона,
 * отдаёт батч DeepSeek'у, шлёт HTML в канал, возвращает RunSummary.
 * Не завершает процесс — ошибки пробрасывает вызывающему (daemon в src/run.ts).
 */
```

**Никаких других правок в pipeline.ts.** Fisher-Yates shuffle, цикл по каналам, try/catch на канал, dedup, writeRaw/writeOutput, summarize, sendToChannel, commitHashCache — всё остаётся побайтно.

После правок:
```bash
npx tsc --noEmit
```
Должен пройти зелёным. Если есть ошибки про неиспользованный yaml/readFileSync — значит правка 1 неполная.
  </action>

  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "loadChannelsYaml" src/pipeline.ts` returns 0.
    - `grep -c "import yaml" src/pipeline.ts` returns 0.
    - `grep -c "readFileSync" src/pipeline.ts` returns 0.
    - `grep -c "interface ChannelsFile" src/pipeline.ts` returns 0.
    - `grep -c "interface ChannelEntry" src/pipeline.ts` returns 0.
    - `grep -c "from \"./channels-store.js\"" src/pipeline.ts` returns 1.
    - `grep -c "loadChannels()" src/pipeline.ts` returns 1.
    - `grep -c "channels.yaml" src/pipeline.ts` returns 0.
    - `grep -c "channels.json" src/pipeline.ts` returns at least 1 (в комментарии шапки или JSDoc).
    - `npx tsc --noEmit` exits with code 0.
    - `wc -l src/pipeline.ts` shows fewer lines than 162 (loadChannelsYaml + interfaces удалены, ~25 строк меньше).
  </acceptance_criteria>

  <done>pipeline.ts больше не зависит от yaml-пакета напрямую и не содержит loadChannelsYaml/ChannelsFile/ChannelEntry interfaces; читает каналы через loadChannels() из channels-store; tsc зелёный; вся остальная логика runPipeline нетронута.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pipeline.ts → channels-store.ts | Wiring-граница: pipeline теряет прямой доступ к YAML, доверяет валидации channels-store |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-12 | Tampering | Регрессия v3.0 функциональности (Fisher-Yates / dedup / archive ломаются при правке pipeline.ts) | mitigate | Точечные правки только в imports + одной строке-вызове + JSDoc; vitest-тесты в Plan 01-04 (если они есть для pipeline) или ручной smoke `npm start:once` после Phase 1. Acceptance criteria: `wc -l` подтверждает только сокращение (~25 строк), а не другие изменения. |
| T-01-13 | Denial | channels-store.ts throw'ит на битом JSON → runPipeline падает | mitigate | Уже митигировано в Plan 01-01 (T-01-03): `tick()` в src/run.ts:26 ловит, шлёт alert через sendAlert. Pipeline не должен глотать эту ошибку — это by design (D-03). |
| T-01-14 | Information Disclosure | JSDoc упоминает auto-migration | accept | Внутренний комментарий, в логи/наружу не попадает. |

ASVS L1.
</threat_model>

<verification>
- `npx tsc --noEmit` — 0 ошибок.
- `git diff src/pipeline.ts` показывает только: удаление import yaml/readFileSync, удаление двух interface'ов, удаление loadChannelsYaml, добавление import из channels-store, замена одной строки вызова, обновление JSDoc.
- `git diff package.json` — пусто (yaml-пакет всё ещё нужен channels-store, но import оттуда был и в Plan 01-02).
- Ручной smoke (опционально, после Plan 01-04): `node --env-file=.env --import tsx scripts/run-once.ts` — завершается успешно с тем же RunSummary форматом, что до миграции.
</verification>

<success_criteria>
1. pipeline.ts больше не зависит от `yaml` и `readFileSync` напрямую.
2. pipeline.ts читает каналы через `loadChannels()` из `channels-store.ts`.
3. Никаких изменений в логике runPipeline (shuffle, try/catch, dedup, archive, summarize, deliver).
4. `npx tsc --noEmit` зелёный.
5. Никаких новых deps в package.json.
</success_criteria>

<output>
Создать `.planning/phases/01-storage-migration/01-03-SUMMARY.md`:
- Что сделано (4 точечные правки в pipeline.ts)
- Что осталось нетронутым (Fisher-Yates, dedup, archive, summarize, deliver — побайтно)
- Files modified (src/pipeline.ts)
- Регрессионные риски (smoke npm start:once рекомендован после Plan 01-04)
</output>
