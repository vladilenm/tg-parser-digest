---
phase: 01-storage-migration
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/channels-store.ts
autonomous: true
requirements:
  - STORE-03
must_haves:
  truths:
    - "При отсутствии channels.json и наличии channels.yaml — loadChannels() автоматически читает YAML, пишет JSON через mutex+atomic, возвращает массив каналов"
    - "Лог `[channels-store] migrated channels.yaml → channels.json (N каналов)` появляется ровно один раз при первой миграции"
    - "При повторном вызове loadChannels() — JSON уже на диске, миграция НЕ повторяется (идемпотентность)"
    - "channels.yaml остаётся на диске нетронутым после миграции (D-12 — backup)"
    - "При отсутствии и channels.json, и channels.yaml — loadChannels() throw'ит (D-13: нечего мигрировать; tick() ловит и шлёт alert)"
  artifacts:
    - path: "src/channels-store.ts"
      provides: "loadChannels() с lazy auto-migration; helper readYamlChannels"
      contains: "migrated channels.yaml"
  key_links:
    - from: "loadChannels (channels.json missing)"
      to: "yaml.parse(channels.yaml)"
      via: "lazy внутри loadChannels — fallback ветка"
      pattern: "yaml\\.parse"
    - from: "loadChannels (after yaml read)"
      to: "saveChannels (write JSON)"
      via: "первый запуск пишет JSON через тот же mutex+atomic путь"
      pattern: "saveChannels|atomicWriteJson"
---

<objective>
Добавить lazy auto-migration в `loadChannels()` из Plan 01-01: при отсутствии `channels.json` — прочитать `channels.yaml`, валидировать через Zod, записать JSON через тот же атомарный путь, вернуть массив. Идемпотентность гарантирована тем, что после первого запуска JSON уже на диске и второй вызов идёт по основной ветке.

Purpose: Закрыть STORE-03 — оператор делает `npm start` после деплоя v4.0, daemon сам конвертирует YAML→JSON без ручных шагов. Pipeline продолжает работать без вмешательства (success criterion #2 в ROADMAP).
Output: Обновлённый `src/channels-store.ts` с веткой миграции внутри `loadChannels()`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-storage-migration/01-CONTEXT.md
@.planning/phases/01-storage-migration/01-01-SUMMARY.md
@src/channels-store.ts
@src/pipeline.ts
@src/dedup.ts

<interfaces>
<!-- Контракты, созданные в Plan 01-01 (читаем из src/channels-store.ts). -->

From src/channels-store.ts (создано в Plan 01-01):
```typescript
export const CHANNELS_PATH = "./channels.json";
export type ChannelEntry = { username: string; priority?: number };

const ChannelsFileSchema = z.object({
  channels: z.array(z.object({
    username: z.string().min(1),
    priority: z.number().int().optional(),
  })).min(1),
});

export function loadChannels(): ChannelEntry[];
export function saveChannels(channels: ChannelEntry[]): Promise<void>;
export function mutate(fn): Promise<void>;
```

From src/pipeline.ts:23-40 (текущий loadChannelsYaml — эталон ручной валидации YAML; стиль адаптируем под Zod):
```typescript
function loadChannelsYaml(path: string): ChannelEntry[] {
  const raw = readFileSync(path, "utf8");
  const parsed = yaml.parse(raw) as ChannelsFile | null;
  if (!parsed || !Array.isArray(parsed.channels)) {
    throw new Error(`${path}: корневой ключ "channels" отсутствует или не массив`);
  }
  // ...
}
```

From package.json (yaml уже runtime-dep):
```json
"yaml": "^2.5.0"
```
</interfaces>

<locked_decisions>
- D-11: Триггер — lazy внутри loadChannels(). Если channels.json отсутствует — читаем channels.yaml, пишем JSON, возвращаем массив. Идемпотентно.
- D-12: channels.yaml остаётся на диске как backup. НИКАКИХ rename в .bak. НИКАКОГО удаления.
- D-13: Лог `[channels-store] migrated channels.yaml → channels.json (N каналов)` ровно один раз (при первом срабатывании). Если YAML тоже отсутствует — throw.
</locked_decisions>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Добавить lazy auto-migration в loadChannels (channels.yaml → channels.json)</name>

  <read_first>
    - src/channels-store.ts (текущий loadChannels из Plan 01-01)
    - .planning/phases/01-storage-migration/01-CONTEXT.md (D-11, D-12, D-13)
    - src/pipeline.ts:23-40 (текущий loadChannelsYaml как референс структуры YAML)
    - channels.yaml (формат файла-источника)
  </read_first>

  <files>src/channels-store.ts</files>

  <behavior>
    - Сценарий A (channels.json существует): loadChannels() работает как раньше — читает JSON, валидирует Zod, возвращает.
    - Сценарий B (channels.json отсутствует, channels.yaml существует): loadChannels() читает YAML, парсит yaml.parse, валидирует через ту же ChannelsFileSchema, синхронно пишет JSON через atomicWriteJson, логирует ровно один раз `[channels-store] migrated channels.yaml → channels.json (${N} каналов)`, возвращает массив.
    - Сценарий C (оба файла отсутствуют): loadChannels() throw'ит Error с сообщением `[channels-store] no source file: neither ./channels.json nor ./channels.yaml exists`.
    - Сценарий D (повторный вызов после B): loadChannels() видит channels.json, идёт по сценарию A, ничего НЕ логирует про миграцию (идемпотентность).
    - Миграция выполняется СИНХРОННО (не через `mutate()` async API): loadChannels по контракту sync, и миграция должна вернуть результат сразу. Атомарность гарантируется тем же `.tmp + rename` (renameSync — sync вызов).
    - При CRASH между записью .tmp и rename — основной channels.json отсутствует, при следующем запуске миграция повторится с тем же YAML (идемпотентность D-11 в краш-сценарии).
  </behavior>

  <action>
В `src/channels-store.ts` модифицировать функцию `loadChannels` (которая была создана в Plan 01-01) так, чтобы она поддерживала auto-migration. Вот итоговая структура (вы добавляете import yaml, helper, и расширяете loadChannels):

1. **Добавить import** в начало файла (после существующих imports):
```typescript
import yaml from "yaml";
```

2. **Добавить путь YAML как module-level константу** (рядом с CHANNELS_PATH):
```typescript
const YAML_FALLBACK_PATH = "./channels.yaml";
```
(Это internal const, НЕ экспортируется.)

3. **Заменить текущую функцию loadChannels** на следующую реализацию:

```typescript
/**
 * Прочитать channels.json и вернуть массив каналов.
 * D-06: без mutex — POSIX rename(2) гарантирует, что reader увидит либо старый, либо новый файл целиком.
 * D-03: на битом JSON или провале Zod — throw наверх.
 *
 * STORE-03 / D-11: lazy auto-migration. Если channels.json отсутствует — читаем channels.yaml,
 * пишем JSON через .tmp + rename, возвращаем массив. Идемпотентно: при следующем вызове JSON уже есть.
 * D-12: channels.yaml остаётся на диске как backup (никакого .bak rename, никакого удаления).
 * D-13: если оба файла отсутствуют — throw.
 */
export function loadChannels(): ChannelEntry[] {
  if (!existsSync(CHANNELS_PATH)) {
    // D-13: ни JSON, ни YAML — нечего мигрировать.
    if (!existsSync(YAML_FALLBACK_PATH)) {
      throw new Error(
        `[channels-store] no source file: neither ${CHANNELS_PATH} nor ${YAML_FALLBACK_PATH} exists`
      );
    }
    // D-11: миграция YAML → JSON.
    const yamlRaw = readFileSync(YAML_FALLBACK_PATH, "utf8");
    let yamlParsed: unknown;
    try {
      yamlParsed = yaml.parse(yamlRaw);
    } catch (err) {
      throw new Error(
        `[channels-store] failed to parse ${YAML_FALLBACK_PATH}: ${(err as Error).message}`
      );
    }
    const validated: ChannelsFile = ChannelsFileSchema.parse(yamlParsed);
    // Синхронная атомарная запись — миграция должна завершиться до return массива.
    // Mutex в этой ветке не нужен: первый запуск, конкурентов на запись нет.
    atomicWriteJson(CHANNELS_PATH, validated);
    log.info(
      `[channels-store] migrated ${YAML_FALLBACK_PATH} → ${CHANNELS_PATH} (${validated.channels.length} каналов)`
    );
    return validated.channels;
  }

  // Основной путь: channels.json существует.
  const raw = readFileSync(CHANNELS_PATH, "utf8");
  let parsed: unknown;
  try {
    parsed = JSON.parse(raw);
  } catch (err) {
    throw new Error(
      `[channels-store] failed to parse ${CHANNELS_PATH}: ${(err as Error).message}`
    );
  }
  const validated: ChannelsFile = ChannelsFileSchema.parse(parsed);
  return validated.channels;
}
```

ВАЖНО:
- В ветке миграции мьютекс НЕ используется (это первый запуск, конкурентных писателей нет; сам `atomicWriteJson` синхронный и атомарный).
- Лог `[channels-store] migrated ...` пишется в той же ветке — так что он печатается ровно один раз (при следующем вызове ветка не выполнится, потому что existsSync(CHANNELS_PATH) === true).
- channels.yaml после миграции НЕ удаляется и НЕ переименовывается (D-12).
- Сообщение в логе содержит `migrated channels.yaml → channels.json` (подстрока для grep-проверки).

После изменений:
```bash
npx tsc --noEmit
```
Должен пройти зелёным.
  </action>

  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "import yaml from \"yaml\"" src/channels-store.ts` returns 1.
    - `grep -c "YAML_FALLBACK_PATH" src/channels-store.ts` returns at least 2 (declaration + usage).
    - `grep -c "existsSync(CHANNELS_PATH)" src/channels-store.ts` returns at least 1.
    - `grep -c "existsSync(YAML_FALLBACK_PATH)" src/channels-store.ts` returns at least 1.
    - `grep -c "no source file" src/channels-store.ts` returns 1 (D-13 error message).
    - `grep -c "migrated.*channels.yaml.*channels.json" src/channels-store.ts` returns at least 1 (D-13 log message).
    - `grep -c "yaml.parse" src/channels-store.ts` returns at least 1.
    - `grep -c "rmSync\\|unlinkSync" src/channels-store.ts` returns 0 (D-12: YAML НЕ удаляется).
    - `grep -E "renameSync.*\\.bak|rename.*bak" src/channels-store.ts` returns nothing (D-12: никаких .bak).
    - `npx tsc --noEmit` exits with code 0.
    - `git diff package.json` shows no changes (yaml уже был, никаких новых deps).
  </acceptance_criteria>

  <done>loadChannels поддерживает три сценария (JSON exists / YAML fallback / both missing), миграция логируется один раз, channels.yaml остаётся на диске; tsc зелёный; deps не менялись.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem (channels.yaml) → process | Существующий YAML — оператор писал его руками; формат может расходиться с ожидаемой схемой |
| migration crash window | Между read(yaml) и rename(.tmp → channels.json) — узкое окно потенциального несоответствия |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-07 | Tampering / Integrity | YAML с невалидной схемой роняет миграцию | mitigate | `ChannelsFileSchema.parse(yamlParsed)` — Zod валидирует ту же схему, что и для JSON; throw → tick() в run.ts ловит → alert (D-03 идиома). |
| T-01-08 | Tampering | Crash mid-migration оставляет YAML+JSON несинхронными | mitigate | Идемпотентность D-11: crash до renameSync — JSON отсутствует, при следующем `npm start` миграция повторится с того же YAML (он не удаляется по D-12). Crash после rename — JSON валиден, повторная миграция не запускается (existsSync проверка). |
| T-01-09 | Denial | Параллельные первые запуски → две конкурентные миграции | accept | PM2 forks=1, daemon single-process; параллельных запусков нет. Если оператор вручную запустит npm start второй раз поверх первого — `existsSync` после rename блокирует повторную миграцию; до rename — последний writeFileSync(.tmp) выигрывает, обе записи валидны (одинаковый YAML-источник). |
| T-01-10 | Denial | Битый channels.yaml → daemon молча зависает | mitigate | yaml.parse error → catch → throw с явным сообщением `[channels-store] failed to parse ./channels.yaml: ...` → tick() ловит → alert. Daemon не молчит. |
| T-01-11 | Information Disclosure | Лог содержит количество каналов (50) — низкая ценность | accept | Информация безвредная, помогает оператору verify. |

ASVS L1.
</threat_model>

<verification>
- `npx tsc --noEmit` — 0 ошибок.
- Ручной smoke (опционально): удалить `channels.json` (если есть), запустить `npm run start:once` (если scripts/run-once.ts существует) или просто `node --env-file=.env --import tsx -e 'import("./src/channels-store.ts").then(m => console.log(m.loadChannels().length))'` — увидеть лог `migrated channels.yaml → channels.json (50 каналов)` и число 50 в stdout. После — `ls channels.json` показывает файл с двухпробельным отступом. Повторный запуск НЕ логирует миграцию.
- `git diff package.json` — пусто.
- `wc -l channels.yaml` — файл всё ещё на диске после миграции (если smoke сделан).
</verification>

<success_criteria>
1. Удаление `channels.json` + запуск `loadChannels()` → файл воссоздаётся из YAML, лог появляется.
2. Повторный вызов `loadChannels()` после п.1 — НЕ логирует миграцию (идемпотентность).
3. Удаление обоих файлов + запуск → throw с явным сообщением (поймает tick() в run.ts → alert).
4. `channels.yaml` остаётся на диске после миграции.
5. `npx tsc --noEmit` зелёный, deps не менялись.
</success_criteria>

<output>
Создать `.planning/phases/01-storage-migration/01-02-SUMMARY.md`:
- Что сделано (lazy migration ветка добавлена в loadChannels)
- Locked decisions implemented (D-11, D-12, D-13)
- Edge cases handled (crash mid-migration, both files missing, idempotency)
- Files modified (src/channels-store.ts)
- Что НЕ сделано (consumer wiring в pipeline.ts — Plan 01-03; tests — Plan 01-04)
</output>
