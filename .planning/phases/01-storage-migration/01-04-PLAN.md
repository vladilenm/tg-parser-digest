---
phase: 01-storage-migration
plan: 04
type: tdd
wave: 3
depends_on: ["01-01", "01-02"]
files_modified:
  - src/__tests__/channels-store.test.ts
autonomous: true
requirements:
  - STORE-01
  - STORE-02
  - STORE-03
must_haves:
  truths:
    - "Vitest-тест mutex-concurrency: Promise.all из двух mutate-вызовов даёт финальный файл с обоими каналами и валидной JSON-структурой (success criterion #3 ROADMAP)"
    - "Vitest-тест happy-path loadChannels: валидный JSON парсится, возвращает массив правильной длины"
    - "Vitest-тест Zod failure: битый JSON или нарушенная схема приводят к throw"
    - "Vitest-тест auto-migration: при отсутствии channels.json и наличии channels.yaml — loadChannels() конвертирует, лог появляется, JSON создан"
    - "Vitest-тест idempotency миграции: второй вызов loadChannels() после миграции не повторяет миграцию (нет повторного лога)"
    - "Vitest-тест both-missing: при отсутствии обоих файлов loadChannels() throw'ит"
    - "Vitest-тест атомарной записи: после saveChannels файл валиден; при ошибке внутри fn в mutate() основной файл нетронут"
    - "npx vitest run src/__tests__/channels-store.test.ts exits 0 со всеми тестами зелёными"
  artifacts:
    - path: "src/__tests__/channels-store.test.ts"
      provides: "Vitest-тесты для channels-store.ts: 7+ тестов, покрытие STORE-01/02/03"
      min_lines: 200
  key_links:
    - from: "test"
      to: "tmpdir + chdir"
      via: "process.chdir в beforeEach — channels-store читает ./channels.json относительно cwd"
      pattern: "process\\.chdir|os\\.tmpdir"
    - from: "concurrency test"
      to: "store.mutate"
      via: "Promise.all([mutate(addOne), mutate(addTwo)])"
      pattern: "Promise\\.all.*mutate"
---

<objective>
Создать `src/__tests__/channels-store.test.ts` с Vitest-тестами для модуля `channels-store.ts`, покрывающими все три STORE-* requirements:
- STORE-01: loadChannels возвращает каналы из JSON.
- STORE-02: атомарная запись + mutex (центральный тест — `Promise.all` концурентных mutate).
- STORE-03: auto-migration YAML→JSON, идемпотентность, throw при both-missing.

Purpose: Mutex-concurrency-тест — must-have для Phase 1 (success criterion #3 ROADMAP). Без vitest-симуляции concurrent mutate невозможно доказать, что race condition между ботом и cron-tick'ом митигирована.
Output: Один файл `src/__tests__/channels-store.test.ts` с 7+ test-cases. `npx vitest run` зелёный.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-storage-migration/01-CONTEXT.md
@.planning/phases/01-storage-migration/01-01-SUMMARY.md
@src/channels-store.ts
@src/__tests__/summarize.test.ts
@vitest.config.ts

<interfaces>
<!-- Public API channels-store, против которого пишутся тесты. -->

From src/channels-store.ts:
```typescript
export const CHANNELS_PATH: "./channels.json";
export type ChannelEntry = { username: string; priority?: number };

export function loadChannels(): ChannelEntry[];          // sync, без mutex
export function saveChannels(channels: ChannelEntry[]): Promise<void>;  // через mutex + atomic
export function mutate(
  fn: (current: ChannelEntry[]) => Promise<ChannelEntry[]> | ChannelEntry[]
): Promise<void>;                                         // read-modify-write через mutex
```

Существующий стиль тестов (src/__tests__/summarize.test.ts):
```typescript
import { describe, it, expect } from "vitest";
import { fn } from "../module.js";
// helpers + describe blocks
```

Vitest config (vitest.config.ts):
```typescript
{ test: { environment: "node" } }
```
</interfaces>

<test_strategy>
**Изоляция между тестами:**
- channels-store читает/пишет `./channels.json` относительно cwd. Чтобы не загрязнять реальный repo:
  1. В `beforeEach` создаём временную директорию через `os.tmpdir() + crypto.randomUUID()`.
  2. `process.chdir(tmpDir)` — теперь `./channels.json` живёт в tmp.
  3. В `afterEach` — `process.chdir(originalCwd)` + `fs.rmSync(tmpDir, { recursive: true, force: true })`.

**Mutex state:**
- `lockChain` — module-level let, переживает между тестами (это by design для daemon, но в тестах мы хотим чистый старт).
- Решение: `lockChain` остаётся, но beforeEach дожидается его (`await new Promise(r => setImmediate(r))` после возможных предыдущих mutate'ов) ИЛИ просто завершаем все promises в afterEach. Поскольку каждый тест создаёт свой tmpdir, и если тесты await'ят Promise.all — состояние chain'а в норме.

**Cache mocks:**
- Для теста "log миграции один раз" — следить за `console.log` через `vi.spyOn(console, 'log').mockImplementation(() => {})` и считать вызовы с подстрокой `migrated`. log.info использует console.log внутри (см. src/logger.ts).
</test_strategy>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Написать channels-store.test.ts с 7+ тестами (happy-path, mutex, migration)</name>

  <read_first>
    - src/channels-store.ts (поле для тестирования)
    - src/__tests__/summarize.test.ts (стиль existing vitest-тестов в проекте)
    - src/logger.ts (log.info использует console.log — для spy в migration-тесте)
    - vitest.config.ts (environment: "node")
  </read_first>

  <files>src/__tests__/channels-store.test.ts</files>

  <action>
Создать файл `src/__tests__/channels-store.test.ts` со следующим содержимым (полная реализация — executor копирует целиком):

```typescript
// src/__tests__/channels-store.test.ts — Vitest для channels-store (Phase 1 STORE-01..03).
// Покрывает: happy-path load, atomic write, mutex concurrency (Promise.all),
// auto-migration YAML→JSON, идемпотентность миграции, both-files-missing throw.

import { describe, it, expect, beforeEach, afterEach, vi } from "vitest";
import { mkdtempSync, rmSync, writeFileSync, existsSync, readFileSync } from "node:fs";
import { tmpdir } from "node:os";
import { join } from "node:path";

// ВАЖНО: каждый тест создаёт изолированный tmpdir и делает process.chdir.
// channels-store читает "./channels.json" относительно cwd.
// Импортируем модуль ОДИН раз (module-level lockChain переживает между тестами,
// но это OK: каждый тест await'ит свои операции).
import {
  loadChannels,
  saveChannels,
  mutate,
  CHANNELS_PATH,
  type ChannelEntry,
} from "../channels-store.js";

const ORIGINAL_CWD = process.cwd();
let workdir: string;

beforeEach(() => {
  workdir = mkdtempSync(join(tmpdir(), "channels-store-test-"));
  process.chdir(workdir);
});

afterEach(() => {
  process.chdir(ORIGINAL_CWD);
  rmSync(workdir, { recursive: true, force: true });
  vi.restoreAllMocks();
});

// ---------------------------------------------------------------------------
// Helpers
// ---------------------------------------------------------------------------

function writeJson(channels: ChannelEntry[]): void {
  writeFileSync("./channels.json", JSON.stringify({ channels }, null, 2), "utf8");
}

function writeYaml(channels: ChannelEntry[]): void {
  // Минимальный валидный YAML — простой случай хватает.
  const lines = ["channels:"];
  for (const c of channels) {
    lines.push(`  - username: "${c.username}"`);
    if (c.priority !== undefined) {
      lines.push(`    priority: ${c.priority}`);
    }
  }
  writeFileSync("./channels.yaml", lines.join("\n") + "\n", "utf8");
}

function readJsonFromDisk(): { channels: ChannelEntry[] } {
  return JSON.parse(readFileSync("./channels.json", "utf8"));
}

// ---------------------------------------------------------------------------
// STORE-01: loadChannels happy-path + Zod failure
// ---------------------------------------------------------------------------

describe("loadChannels (STORE-01)", () => {
  it("читает массив каналов из валидного channels.json", () => {
    writeJson([
      { username: "ch1", priority: 1 },
      { username: "ch2" },
    ]);
    const result = loadChannels();
    expect(result).toHaveLength(2);
    expect(result[0]).toEqual({ username: "ch1", priority: 1 });
    expect(result[1]).toEqual({ username: "ch2" });
  });

  it("CHANNELS_PATH экспортируется как './channels.json'", () => {
    expect(CHANNELS_PATH).toBe("./channels.json");
  });

  it("throws при битом JSON-синтаксисе (D-03)", () => {
    writeFileSync("./channels.json", "{not valid json", "utf8");
    expect(() => loadChannels()).toThrow(/failed to parse/);
  });

  it("throws при пустом массиве каналов (Zod .min(1))", () => {
    writeFileSync("./channels.json", JSON.stringify({ channels: [] }), "utf8");
    expect(() => loadChannels()).toThrow();
  });

  it("throws при отсутствии username", () => {
    writeFileSync(
      "./channels.json",
      JSON.stringify({ channels: [{ priority: 1 }] }),
      "utf8"
    );
    expect(() => loadChannels()).toThrow();
  });
});

// ---------------------------------------------------------------------------
// STORE-02: saveChannels (atomic write) + mutex concurrency
// ---------------------------------------------------------------------------

describe("saveChannels + mutex (STORE-02)", () => {
  it("saveChannels пишет валидный JSON с двухпробельным отступом (D-04)", async () => {
    await saveChannels([{ username: "ch1", priority: 1 }]);
    const raw = readFileSync("./channels.json", "utf8");
    // D-04: двухпробельный отступ.
    expect(raw).toContain('  "channels"');
    expect(raw).toContain('    "username"');
    // Round-trip через loadChannels.
    expect(loadChannels()).toEqual([{ username: "ch1", priority: 1 }]);
  });

  it("после saveChannels старый .tmp не остаётся на диске (rename, не copy)", async () => {
    await saveChannels([{ username: "ch1" }]);
    expect(existsSync("./channels.json")).toBe(true);
    expect(existsSync("./channels.json.tmp")).toBe(false);
  });

  it("CRITICAL: Promise.all из двух concurrent mutate сохраняет ОБА канала (success criterion #3)", async () => {
    // Предусловие: стартовый файл с одним каналом.
    writeJson([{ username: "base", priority: 0 }]);

    // Две конкурентные mutate-операции — имитируют race "бот добавляет / cron читает".
    await Promise.all([
      mutate((current) => [...current, { username: "added-by-bot", priority: 99 }]),
      mutate((current) => [...current, { username: "added-by-cron", priority: 100 }]),
    ]);

    // ОБА канала должны оказаться в финальном файле, ни один не потерян.
    const final = loadChannels();
    const usernames = final.map((c) => c.username);
    expect(usernames).toContain("base");
    expect(usernames).toContain("added-by-bot");
    expect(usernames).toContain("added-by-cron");
    expect(final).toHaveLength(3);

    // Файл валидный JSON, не повреждён.
    const onDisk = readJsonFromDisk();
    expect(onDisk.channels).toHaveLength(3);
  });

  it("стресс-тест: 10 concurrent mutate сохраняют все 10 добавлений", async () => {
    writeJson([{ username: "base" }]);

    const ops = Array.from({ length: 10 }, (_, i) =>
      mutate((current) => [...current, { username: `concurrent-${i}` }])
    );
    await Promise.all(ops);

    const final = loadChannels();
    expect(final).toHaveLength(11); // base + 10 concurrent
    for (let i = 0; i < 10; i++) {
      expect(final.map((c) => c.username)).toContain(`concurrent-${i}`);
    }
  });

  it("при throw из fn внутри mutate — основной channels.json НЕ модифицируется", async () => {
    writeJson([{ username: "ch1" }]);
    await expect(
      mutate(() => {
        throw new Error("simulated failure inside fn");
      })
    ).rejects.toThrow(/simulated failure/);

    // Файл остался прежним.
    const final = loadChannels();
    expect(final).toEqual([{ username: "ch1" }]);
  });

  it("после rejected mutate — следующая mutate-операция работает корректно (mutex не залип)", async () => {
    writeJson([{ username: "ch1" }]);

    await expect(
      mutate(() => {
        throw new Error("first op fails");
      })
    ).rejects.toThrow();

    // Следующая операция должна нормально пройти.
    await mutate((current) => [...current, { username: "ch2" }]);
    expect(loadChannels()).toHaveLength(2);
  });
});

// ---------------------------------------------------------------------------
// STORE-03: auto-migration YAML→JSON
// ---------------------------------------------------------------------------

describe("auto-migration YAML→JSON (STORE-03)", () => {
  it("при отсутствии channels.json и наличии channels.yaml — мигрирует и логирует", () => {
    writeYaml([
      { username: "ch1", priority: 1 },
      { username: "ch2", priority: 2 },
    ]);

    // Spy на console.log (log.info → console.log в src/logger.ts).
    const logSpy = vi.spyOn(console, "log").mockImplementation(() => {});

    const result = loadChannels();

    expect(result).toHaveLength(2);
    expect(result[0]).toEqual({ username: "ch1", priority: 1 });
    expect(existsSync("./channels.json")).toBe(true);

    // D-13: лог содержит подстроку "migrated channels.yaml".
    const logCalls = logSpy.mock.calls.map((c) => c.join(" "));
    const migrationLog = logCalls.find((line) => line.includes("migrated"));
    expect(migrationLog).toBeDefined();
    expect(migrationLog).toContain("channels.yaml");
    expect(migrationLog).toContain("channels.json");
    expect(migrationLog).toContain("(2 каналов)");
  });

  it("после миграции channels.yaml ОСТАЁТСЯ на диске (D-12)", () => {
    writeYaml([{ username: "ch1" }]);
    loadChannels();
    expect(existsSync("./channels.yaml")).toBe(true); // не удалён
    expect(existsSync("./channels.json")).toBe(true); // создан
  });

  it("идемпотентность: второй loadChannels() НЕ повторяет миграцию", () => {
    writeYaml([{ username: "ch1" }]);
    loadChannels(); // первый — мигрирует

    const logSpy = vi.spyOn(console, "log").mockImplementation(() => {});
    const result = loadChannels(); // второй — НЕ должен мигрировать
    expect(result).toEqual([{ username: "ch1" }]);

    const logCalls = logSpy.mock.calls.map((c) => c.join(" "));
    const migrationLog = logCalls.find((line) => line.includes("migrated"));
    expect(migrationLog).toBeUndefined();
  });

  it("throws когда оба файла отсутствуют (D-13)", () => {
    // ни channels.json, ни channels.yaml не созданы — tmpdir пустой.
    expect(() => loadChannels()).toThrow(/no source file/);
  });
});
```

После создания запустить:
```bash
npx vitest run src/__tests__/channels-store.test.ts
```
Ожидание: все тесты зелёные (12+ тестов в 3 describe-блоках).

ВАЖНО:
- **НЕ** добавлять новые dev-deps. `vitest` уже в devDeps (`^4.1.5`), `node:fs` / `node:os` / `node:path` — встроенные.
- Если тест на лог-миграцию падает из-за разных форматов timestamp в logger — проверять только подстроку `migrated` (как в коде выше), а не точное соответствие. Spy на `console.log` ловит весь `[ISO] [info] [channels-store] migrated ...` целиком.
- `vi.spyOn(console, "log")` важно делать ПОСЛЕ первого loadChannels (чтобы не перехватить лишние вызовы инициализации).
  </action>

  <verify>
    <automated>npx vitest run src/__tests__/channels-store.test.ts</automated>
  </verify>

  <acceptance_criteria>
    - File `src/__tests__/channels-store.test.ts` exists.
    - `grep -c "describe(" src/__tests__/channels-store.test.ts` returns 3 (STORE-01, STORE-02, STORE-03 группы).
    - `grep -c "Promise.all" src/__tests__/channels-store.test.ts` returns at least 2 (must-have concurrency tests).
    - `grep -c "process.chdir" src/__tests__/channels-store.test.ts` returns at least 2 (beforeEach + afterEach).
    - `grep -c "mkdtempSync" src/__tests__/channels-store.test.ts` returns 1.
    - `grep -c "no source file" src/__tests__/channels-store.test.ts` returns 1 (D-13 throw тест).
    - `grep -c "migrated" src/__tests__/channels-store.test.ts` returns at least 2 (migration log + idempotency tests).
    - `grep -c "added-by-bot.*added-by-cron\\|added-by-cron.*added-by-bot" src/__tests__/channels-store.test.ts` returns at least 0 (concurrency assertion present somewhere — softer check via `toContain.*added-by-bot`).
    - `npx vitest run src/__tests__/channels-store.test.ts` exits 0.
    - `npx vitest run src/__tests__/channels-store.test.ts 2>&1 | grep -E "Tests +[0-9]+ passed"` shows at least 12 passed.
    - `git diff package.json` shows no changes (vitest уже в devDeps).
  </acceptance_criteria>

  <done>Тестовый файл создан, все describe-блоки покрывают STORE-01/02/03, mutex-concurrency-тест с Promise.all зелёный, идемпотентность миграции зелёная, throw на both-missing зелёный.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test cwd → real repo | Тесты пишут files в tmpdir, НЕ в корень репо (защита через mkdtempSync + chdir) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-15 | Tampering | Тест перезаписывает реальный channels.json в корне репо | mitigate | beforeEach: `mkdtempSync(tmpdir())` + `process.chdir(workdir)`. afterEach: chdir обратно + rmSync. CHANNELS_PATH относительный (`./channels.json`), так что chdir изолирует тесты. |
| T-01-16 | Denial | Module-level lockChain переживает между тестами и блокирует следующие mutate | mitigate | Каждый тест сам await'ит Promise.all своих mutate-операций — chain «пустеет» к концу теста. Если flaky — добавить `await new Promise(r => setImmediate(r))` в afterEach. |
| T-01-17 | Information Disclosure | Тестовые tmpdir не очищаются | mitigate | afterEach: `rmSync(workdir, { recursive: true, force: true })`. |
| T-01-18 | False positive | Mutex-тест зелёный из-за случайного порядка, а не реальной сериализации | mitigate | Стресс-тест с 10 concurrent mutate (более жёсткий, чем 2) — если mutex сломан, потеря хотя бы одного из 10 произойдёт почти наверняка. Дополнительно — assert на длину массива (точно 11), а не «contains». |

ASVS L1.
</threat_model>

<verification>
- `npx vitest run src/__tests__/channels-store.test.ts` — все тесты зелёные.
- `npx vitest run` (полный suite) — также зелёный (никаких регрессий в summarize.test.ts).
- `git diff package.json` — пусто.
- В CI/локально: тесты не оставляют файлов в корне репо (`ls channels.json channels.json.tmp` после прогона — отсутствуют, кроме настоящего channels.json/yaml, которые были до тестов).
</verification>

<success_criteria>
1. 12+ тестов зелёные, разбиты на 3 describe-блока (STORE-01, STORE-02, STORE-03).
2. CRITICAL test (success criterion #3 ROADMAP) — Promise.all из двух concurrent mutate сохраняет оба канала — присутствует и проходит.
3. Стресс-тест (10 concurrent) — присутствует и проходит.
4. Тесты идемпотентности миграции и both-missing throw — присутствуют и проходят.
5. Никаких изменений в package.json (vitest уже dev-dep).
6. Тесты НЕ загрязняют корень репо (изоляция через mkdtempSync+chdir).
</success_criteria>

<output>
Создать `.planning/phases/01-storage-migration/01-04-SUMMARY.md`:
- Что сделано (12+ vitest-тестов в 3 группах)
- Тестовое покрытие (mapping STORE-01/02/03 → describe-блоки)
- Изоляция (mkdtempSync + chdir + rmSync)
- Подтверждение success criterion #3 ROADMAP — concurrency-тест зелёный
- Files modified (src/__tests__/channels-store.test.ts created)
</output>
