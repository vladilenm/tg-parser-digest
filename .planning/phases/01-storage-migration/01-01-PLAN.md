---
phase: 01-storage-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/channels-store.ts
  - src/types.ts
autonomous: true
requirements:
  - STORE-01
  - STORE-02
must_haves:
  truths:
    - "Модуль src/channels-store.ts существует и экспортирует ровно три public-функции: loadChannels, saveChannels, mutate"
    - "saveChannels пишет ./channels.json атомарно через .tmp + rename — конкурентный читатель никогда не увидит частично записанный файл"
    - "mutate(fn) сериализует одновременные записи через in-process promise-chain mutex без новых runtime-зависимостей"
    - "Битый JSON или провал Zod-валидации в loadChannels приводят к throw наверх (нет fallback на YAML, нет молчаливого восстановления)"
    - "Type ChannelEntry экспортируется из channels-store.ts (а не из pipeline.ts)"
  artifacts:
    - path: "src/channels-store.ts"
      provides: "store API: loadChannels(), saveChannels(), mutate(), const CHANNELS_PATH, exported type ChannelEntry"
      exports: ["loadChannels", "saveChannels", "mutate", "CHANNELS_PATH", "ChannelEntry"]
      min_lines: 80
    - path: "src/types.ts"
      provides: "обновлённый комментарий в Post.channelUsername (ссылка на channels.json)"
      contains: "channels.json"
  key_links:
    - from: "src/channels-store.ts"
      to: "node:fs writeFileSync + renameSync"
      via: "atomicWriteJson helper (паттерн из src/archive.ts:34-39)"
      pattern: "renameSync\\("
    - from: "src/channels-store.ts"
      to: "zod"
      via: "import { z } from \"zod\" + ChannelsFileSchema.parse"
      pattern: "ChannelsFileSchema\\.parse"
    - from: "src/channels-store.ts"
      to: "in-process mutex"
      via: "module-level let lockChain: Promise<void>"
      pattern: "lockChain"
---

<objective>
Создать фундаментный модуль `src/channels-store.ts` с тремя public-функциями (`loadChannels`, `saveChannels`, `mutate`), Zod-валидацией, атомарной записью через `.tmp + rename` и самописным in-process mutex'ом — БЕЗ auto-migration (она в Plan 01-02). Перенести интерфейс `ChannelEntry` из `pipeline.ts` в `channels-store.ts` как exported type. Обновить комментарий в `src/types.ts:4`.

Purpose: Заложить контракт store-API (D-08, D-10), который используют (а) Plan 01-02 для миграции, (б) Plan 01-03 для интеграции в pipeline, (в) Phase 2 — для бот-команд. Сигнатура `mutate(fn)` спроектирована под realistic Phase 2 use-case (бот делает `mutate(channels => [...channels, { username, priority: nextPriority }])`).
Output: `src/channels-store.ts` с тремя экспортируемыми функциями, типом `ChannelEntry`, константой `CHANNELS_PATH`. Обновлённый `src/types.ts:4`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-storage-migration/01-CONTEXT.md
@src/archive.ts
@src/dedup.ts
@src/schema.ts
@src/types.ts
@src/pipeline.ts
@src/logger.ts

<interfaces>
<!-- Контракты для executor'а. Извлечены из codebase. -->
<!-- Executor НЕ должен исследовать кодовую базу — все нужные типы здесь. -->

From src/types.ts (existing, импортируется как есть):
```typescript
export interface Post {
  channelUsername: string; // комментарий обновляется в Task 3 на "ссылка на channels.json"
  messageId: number;
  postedAt: string;
  text: string;
  url: string;
}
```

From src/pipeline.ts:14-17 (existing — переезжает в channels-store.ts в Task 1):
```typescript
interface ChannelEntry {
  username: string;
  priority?: number;
}
```

From src/archive.ts:34-39 (эталон atomic write — копируем паттерн):
```typescript
function atomicWriteText(path: string, content: string): void {
  ensureDir(dirname(path));
  const tmp = path + ".tmp";
  writeFileSync(tmp, content, "utf8");
  renameSync(tmp, path);
}
```

From src/logger.ts (существующий API):
```typescript
export const log: {
  info(msg: string, ...ctx: unknown[]): void;
  warn(msg: string, ...ctx: unknown[]): void;
  error(msg: string, ...ctx: unknown[]): void;
};
```

From src/schema.ts (стиль zod-схем в проекте — применяем аналогично):
```typescript
import { z } from "zod";
export const Schema = z.object({...});
```
</interfaces>

<locked_decisions>
<!-- Из .planning/phases/01-storage-migration/01-CONTEXT.md — ВЕРБАТИМ. -->
- D-01: schema 1:1 как YAML — `{ channels: [{ username: string, priority?: number }] }`. Никаких version-обёрток.
- D-02: Zod-схема: `z.object({ channels: z.array(z.object({ username: z.string().min(1), priority: z.number().int().optional() })).min(1) })`.
- D-03: При битом JSON или провале Zod — `throw` наверх. Никакого fallback на YAML.
- D-04: `JSON.stringify(value, null, 2)` (двухпробельный отступ).
- D-05: Mutex — самописный promise-chain, НЕ async-mutex. Никаких новых deps.
- D-06: Mutex сериализует ТОЛЬКО записи. `loadChannels()` читает напрямую без блокировки. POSIX `rename(2)` гарантирует атомарность для reader.
- D-07: `mutate<T>(fn: (current: ChannelEntry[]) => Promise<ChannelEntry[]> | ChannelEntry[]): Promise<void>`.
- D-08: Ровно три public-функции: `loadChannels()`, `saveChannels()`, `mutate()`. Никакого `addChannel`/`removeChannel` (D-09 — это Phase 2).
- D-10: `const CHANNELS_PATH = "./channels.json"`.
</locked_decisions>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Создать скелет channels-store.ts с типами, Zod-схемой, loadChannels (без миграции)</name>

  <read_first>
    - .planning/phases/01-storage-migration/01-CONTEXT.md (locked decisions D-01, D-02, D-03, D-04, D-08, D-10)
    - src/schema.ts (стиль Zod)
    - src/dedup.ts (стиль модуля + readFileSync + JSON.parse + try/catch error message формат)
    - src/pipeline.ts:14-17 (текущий interface ChannelEntry — переносится сюда)
    - src/logger.ts (API log.info/warn/error и stage-prefix `[channels-store]`)
  </read_first>

  <files>src/channels-store.ts</files>

  <behavior>
    - loadChannels() читает ./channels.json через readFileSync(CHANNELS_PATH, "utf8") и возвращает ChannelEntry[].
    - При битом JSON — throw Error с сообщением вида `[channels-store] failed to parse ${CHANNELS_PATH}: <parse-error-message>`.
    - При невалидной структуре (Zod) — throw ZodError (или rethrow с context'ом, см. action).
    - Возвращаемый массив всегда non-empty (Zod `.min(1)`).
    - Файл channels.json отсутствует: НЕ обрабатывается в этой задаче (миграция — Plan 01-02). Сейчас при отсутствии — просто пробрасывается ENOENT-ошибка от readFileSync.
    - ChannelEntry экспортируется как public type.
    - CHANNELS_PATH экспортируется как `export const CHANNELS_PATH = "./channels.json"`.
  </behavior>

  <action>
Создать новый файл `src/channels-store.ts` со следующим содержимым (конкретные значения, не описания):

```typescript
// src/channels-store.ts — хранилище списка Telegram-каналов в channels.json.
// Phase 1 STORE-01/STORE-02: атомарная запись + in-process mutex.
// API: loadChannels (read, no mutex), saveChannels (atomic + mutex), mutate (read-modify-write + mutex).
// Auto-migration из channels.yaml — добавляется в Plan 01-02 (lazy внутри loadChannels).
// CRUD-обёртки (addChannel/removeChannel) — Phase 2 (D-09, рядом с бот-handler'ами).

import { readFileSync, writeFileSync, renameSync, existsSync } from "node:fs";
import { z } from "zod";
import { log } from "./logger.js";

// D-10: единственная константа пути; не из env, не из аргумента.
export const CHANNELS_PATH = "./channels.json";

// D-01: схема 1:1 как в channels.yaml — никаких version-обёрток или audit-полей.
const ChannelEntrySchema = z.object({
  username: z.string().min(1),
  priority: z.number().int().optional(),
});

const ChannelsFileSchema = z.object({
  channels: z.array(ChannelEntrySchema).min(1),
});

export type ChannelEntry = z.infer<typeof ChannelEntrySchema>;
type ChannelsFile = z.infer<typeof ChannelsFileSchema>;

/**
 * Прочитать channels.json и вернуть массив каналов.
 * D-06: без mutex — POSIX rename(2) гарантирует, что reader увидит либо старый, либо новый файл целиком.
 * D-03: на битом JSON или провале Zod — throw наверх. Никакого fallback на YAML.
 * Auto-migration (если channels.json отсутствует) добавляется в Plan 01-02.
 */
export function loadChannels(): ChannelEntry[] {
  const raw = readFileSync(CHANNELS_PATH, "utf8");
  let parsed: unknown;
  try {
    parsed = JSON.parse(raw);
  } catch (err) {
    throw new Error(
      `[channels-store] failed to parse ${CHANNELS_PATH}: ${(err as Error).message}`
    );
  }
  const validated: ChannelsFile = ChannelsFileSchema.parse(parsed);
  return validated.channels;
}
```

ВАЖНО:
- Использовать `import { ... } from "./logger.js"` — суффикс `.js` обязателен (ESM + `moduleResolution: bundler`, см. CLAUDE.md и существующий стиль в `src/dedup.ts`).
- `saveChannels` и `mutate` появятся в Task 2 этого же плана; пока экспортируем только `loadChannels`, `CHANNELS_PATH`, `ChannelEntry`.
- `existsSync` уже импортирован — используется в Task 2.
- НЕ удалять `interface ChannelEntry` из `src/pipeline.ts` в этом таске — это сделает Plan 01-03 (consumer wiring).

После создания файла запустить:
```bash
npx tsc --noEmit
```
Должен пройти без ошибок (если `src/pipeline.ts` ругается — проверить, что `ChannelEntry` всё ещё локально определён в `pipeline.ts`; не трогать его).
  </behavior>

  <action>
Шаги:
1. Создать `src/channels-store.ts` с содержимым выше (используя Write).
2. Запустить `npx tsc --noEmit` — убедиться что 0 ошибок.
3. (Опционально, если есть тестовый channels.json — пропустить, тестирование выносится в Plan 01-04.)

Никаких новых deps в `package.json` — `zod` и `node:fs` уже есть.
  </action>

  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>

  <acceptance_criteria>
    - File `src/channels-store.ts` exists.
    - `grep -c "export const CHANNELS_PATH" src/channels-store.ts` returns 1.
    - `grep -c "export function loadChannels" src/channels-store.ts` returns 1.
    - `grep -c "export type ChannelEntry" src/channels-store.ts` returns 1.
    - `grep -c "ChannelsFileSchema.parse" src/channels-store.ts` returns at least 1.
    - `grep -c "from \"./logger.js\"" src/channels-store.ts` returns 1 (ESM `.js` suffix).
    - `grep -E "channels: z\\.array.*\\.min\\(1\\)" src/channels-store.ts` matches at least 1 line.
    - `npx tsc --noEmit` exits with code 0.
    - `grep -c "addChannel\\|removeChannel" src/channels-store.ts` returns 0 (D-09 — Phase 2).
  </acceptance_criteria>

  <done>Файл создан, типизирован, экспортирует loadChannels/CHANNELS_PATH/ChannelEntry; tsc --noEmit зелёный; никаких новых deps.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Добавить saveChannels + mutate с in-process mutex и атомарной записью</name>

  <read_first>
    - .planning/phases/01-storage-migration/01-CONTEXT.md (D-04, D-05, D-06, D-07, D-08)
    - src/archive.ts:34-39 (эталон atomicWriteText — копируем паттерн)
    - src/dedup.ts (atomicWriteJson + saveHashCache как пример атомарной записи JSON)
    - src/run.ts:11-18 (стиль module-level state — `let isRunning = false`)
    - Файл src/channels-store.ts из Task 1 (тот же файл, который дописываем)
  </read_first>

  <files>src/channels-store.ts</files>

  <behavior>
    - saveChannels(channels: ChannelEntry[]): Promise<void> — записывает массив в channels.json через mutex и атомарный `.tmp + rename`.
    - mutate(fn): Promise<void> — внутри mutex'а: читает текущий список, передаёт в fn (sync или async), записывает результат атомарно.
    - При параллельном вызове двух mutate-операций (Promise.all) — оба завершаются успешно, итоговый файл содержит результат обеих операций (не теряется ни одна), файл всегда валидный JSON.
    - При throw из fn — mutex освобождается, исключение пробрасывается наверх, файл НЕ модифицируется.
    - При падении внутри атомарной записи (ENOSPC, EACCES) — mutex освобождается, исключение пробрасывается, оригинальный channels.json остаётся нетронутым (благодаря .tmp + rename — частичный файл лежит в .tmp, не в основном пути).
  </behavior>

  <action>
Дописать в `src/channels-store.ts` (после loadChannels, перед EOF) следующие функции и helper'ы:

```typescript
// =============================================================================
// Atomic write helper — паттерн из src/archive.ts:34-39, адаптированный под JSON.
// =============================================================================

/**
 * Атомарная запись JSON через .tmp + rename. POSIX rename(2) — атомарная операция,
 * reader никогда не увидит частично записанный файл.
 * D-04: JSON.stringify(value, null, 2) — двухпробельный отступ как в data/raw/*.json.
 */
function atomicWriteJson(path: string, value: unknown): void {
  const tmp = path + ".tmp";
  writeFileSync(tmp, JSON.stringify(value, null, 2), "utf8");
  renameSync(tmp, path);
}

// =============================================================================
// In-process mutex — самописный promise-chain, ~10 LOC.
// D-05: НИКАКОЙ новой зависимости (async-mutex отклонён ради 5-deps cap).
// D-06: сериализует ТОЛЬКО записи. loadChannels() читает напрямую без блокировки.
// =============================================================================

let lockChain: Promise<void> = Promise.resolve();

/**
 * Поставить операцию в очередь mutex'а. Возвращает Promise, который резолвится,
 * когда op завершилась (успешно или с ошибкой). Mutex освобождается в любом случае.
 */
function withLock<T>(op: () => Promise<T>): Promise<T> {
  const result = lockChain.then(op, op);
  // Хвост цепочки не должен реджектить следующего ожидающего — глотаем ошибку
  // в продолжении, но возвращаем оригинальный result наверх.
  lockChain = result.then(
    () => undefined,
    () => undefined
  );
  return result;
}

// =============================================================================
// Public write API.
// =============================================================================

/**
 * Записать массив каналов в channels.json. Атомарно через .tmp + rename, под mutex'ом.
 * D-07/D-08: один из трёх public методов store-API.
 */
export function saveChannels(channels: ChannelEntry[]): Promise<void> {
  return withLock(async () => {
    // Валидируем перед записью — гарантия, что на диск не уйдёт битая структура.
    const payload: ChannelsFile = ChannelsFileSchema.parse({ channels });
    atomicWriteJson(CHANNELS_PATH, payload);
  });
}

/**
 * Read-modify-write критическая секция под mutex'ом. Точка входа для бот-команд в Phase 2.
 * D-07: сигнатура спроектирована под use-case `mutate(channels => [...channels, { username, priority }])`.
 *
 * ВАЖНО: внутри fn НЕЛЬЗЯ вызывать mutate/saveChannels снова — это deadlock.
 * Можно вызвать loadChannels (он читает без mutex'а), но обычно этого не нужно — fn получает current на вход.
 */
export function mutate(
  fn: (current: ChannelEntry[]) => Promise<ChannelEntry[]> | ChannelEntry[]
): Promise<void> {
  return withLock(async () => {
    // Внутри mutex'а читаем актуальный snapshot (а не используем переданный извне) —
    // гарантия, что concurrent mutate видит результат предыдущего.
    const current = loadChannels();
    const next = await fn(current);
    const payload: ChannelsFile = ChannelsFileSchema.parse({ channels: next });
    atomicWriteJson(CHANNELS_PATH, payload);
  });
}
```

ВАЖНО:
- `withLock` использует `lockChain.then(op, op)` — handler в обоих slots одинаковый, чтобы и при success, и при reject предыдущей операции запуск op случался ровно один раз. Это ключевая идиома самописного promise-chain mutex'а.
- `lockChain = result.then(() => undefined, () => undefined)` — хвост чейна всегда резолвится, иначе следующий withLock унаследует rejection и его op никогда не запустится.
- `loadChannels()` внутри `mutate()` вызывается БЕЗ mutex'а — это by design (D-06), и это корректно, потому что мы уже внутри withLock и атомарный rename(2) гарантирует консистентность.

После добавления:
```bash
npx tsc --noEmit
```
Должен пройти зелёным.
  </action>

  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "export function saveChannels" src/channels-store.ts` returns 1.
    - `grep -c "export function mutate" src/channels-store.ts` returns 1.
    - `grep -c "let lockChain" src/channels-store.ts` returns 1.
    - `grep -c "function atomicWriteJson" src/channels-store.ts` returns 1.
    - `grep -c "renameSync" src/channels-store.ts` returns at least 1.
    - `grep -c "JSON.stringify(value, null, 2)" src/channels-store.ts` returns 1 (D-04 вербатим).
    - `grep -c "ChannelsFileSchema.parse" src/channels-store.ts` returns at least 3 (load + saveChannels + mutate).
    - `grep -c "async-mutex\\|p-queue\\|@isaacs/ttlcache" src/channels-store.ts` returns 0 (D-05).
    - `grep -c "async-mutex\\|p-queue" package.json` returns 0 (no new deps).
    - `npx tsc --noEmit` exits with code 0.
    - `wc -l src/channels-store.ts` shows at least 80 lines.
  </acceptance_criteria>

  <done>saveChannels + mutate реализованы, mutex работает через promise-chain, атомарная запись через .tmp+rename, никаких новых runtime/dev deps в package.json, tsc зелёный.</done>
</task>

<task type="auto">
  <name>Task 3: Обновить комментарий в src/types.ts (косметика, не блокирует pipeline)</name>

  <read_first>
    - src/types.ts (строка 4 — комментарий «без "@", как в channels.yaml»)
  </read_first>

  <files>src/types.ts</files>

  <action>
В `src/types.ts` на строке 4 заменить комментарий:

ДО:
```typescript
  channelUsername: string; // без "@", как в channels.yaml
```

ПОСЛЕ:
```typescript
  channelUsername: string; // без "@", как в channels.json
```

Никаких других правок в этом файле.

Проверка:
```bash
grep "channels.json" src/types.ts
grep "channels.yaml" src/types.ts
```
Первая команда должна найти строку, вторая — ничего НЕ найти в src/types.ts (но в pipeline.ts ещё может остаться до Plan 01-03).
  </action>

  <verify>
    <automated>grep -q "channels.json" src/types.ts && ! grep -q "channels.yaml" src/types.ts</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "channels.json" src/types.ts` returns 1.
    - `grep -c "channels.yaml" src/types.ts` returns 0.
    - `npx tsc --noEmit` exits with code 0.
  </acceptance_criteria>

  <done>Комментарий обновлён, ссылается на channels.json; tsc зелёный.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem → process | Чтение `channels.json` — внешний для процесса источник (оператор/будущий бот может записать туда что угодно) |
| concurrent mutate calls → channels.json | Phase 2 будет вызывать mutate из бот-handler'а параллельно с pipeline-tick'ом |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | `channels.json` (concurrent write) | mitigate | In-process promise-chain mutex (`withLock` в `channels-store.ts`) сериализует все записи; reader использует POSIX `rename(2)` — атомарно (D-05, D-06). |
| T-01-02 | Integrity | `channels.json` (partial write при crash) | mitigate | `atomicWriteJson`: сначала `writeFileSync(tmp)`, потом `renameSync(tmp, path)`. Crash до `rename` оставляет старый файл нетронутым; `.tmp` зомби — допустимо, перезатрётся следующим writeом (паттерн из `src/archive.ts:34-39`). |
| T-01-03 | Tampering / Denial | Битый/злонамеренный `channels.json` крашит daemon молча | mitigate | Zod-валидация при `loadChannels()` → `throw` → `tick()` в `src/run.ts:26` ловит → `sendAlert()` уведомляет владельца через `BOT_TOKEN_ALERTS` (D-03). Daemon не молчит. |
| T-01-04 | Information Disclosure | `channels.json` содержит публичные TG-username — низкая ценность | accept | Все каналы публичные (`username`, не приватные invite-hash). Файл коммитится в репозиторий (как `channels.yaml` сейчас). Утечка нерелевантна. |
| T-01-05 | Elevation of Privilege | Path traversal через `CHANNELS_PATH` | accept | Путь захардкожен константой `CHANNELS_PATH = "./channels.json"` (D-10) — не приходит из env, аргумента CLI или сети. Никакого пользовательского ввода в path. |
| T-01-06 | Repudiation | Запись без аудита «кто менял» | accept | Один оператор + один Заказчик (см. PROJECT.md §Constraints — multi-tenancy out of scope). Audit-поля отклонены в CONTEXT.md Deferred. |

ASVS L1.
</threat_model>

<verification>
- `npx tsc --noEmit` — 0 ошибок.
- `grep -c "import.*channels-store" src/` (recursive) — 0 (consumer wiring в Plan 01-03; этот плагин self-contained).
- `git diff --stat package.json` — пусто (никаких новых deps).
- Ручная проверка: `cat src/channels-store.ts | grep -E "^(export|function|const|let|type|interface)"` показывает чистый список public API: CHANNELS_PATH, ChannelEntry, loadChannels, saveChannels, mutate.
</verification>

<success_criteria>
1. `src/channels-store.ts` существует и экспортирует ровно три public-функции (loadChannels, saveChannels, mutate) + CHANNELS_PATH + type ChannelEntry.
2. saveChannels/mutate сериализованы через in-process mutex без новых runtime-deps; запись атомарна (`.tmp + rename`).
3. loadChannels валидирует через Zod (D-02 схема вербатим) и throw'ит на битом JSON (D-03).
4. `src/types.ts:4` ссылается на `channels.json`, не на `channels.yaml`.
5. `npx tsc --noEmit` проходит зелёным.
6. `package.json` не изменился (5 runtime-deps + 5 dev-deps как было).
</success_criteria>

<output>
После завершения создать `.planning/phases/01-storage-migration/01-01-SUMMARY.md` с разделами:
- Что сделано (created files, exported API)
- Locked decisions implemented (D-01..D-08, D-10 — explicit list)
- Что НЕ сделано (миграция в Plan 01-02; интеграция в pipeline.ts в Plan 01-03; CRUD-обёртки в Phase 2 per D-09)
- Patterns established (atomicWriteJson copy-paste from archive.ts; promise-chain mutex idiom)
- Files modified (src/channels-store.ts created, src/types.ts:4 comment updated)
</output>
