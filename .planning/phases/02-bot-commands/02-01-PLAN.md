---
phase: 02-bot-commands
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/bot.ts
  - .env.example
autonomous: true
requirements: [BOT-01, BOT-02, BOT-04, BOT-05]
must_haves:
  truths:
    - "src/bot.ts существует и экспортирует startBot(), stopBot()"
    - "При отсутствии TG_BOT_TOKEN или BOT_ALLOWED_USER_IDS startBot() логирует warn '[bot] ...polling выключен' и возвращается без запуска loop'а"
    - "На boot polling-loop'а вызывается deleteWebhook с drop_pending_updates=false ДО первого getUpdates"
    - "Не-allowlist пользователь получает silent ignore (нет sendMessage / answerCallbackQuery) и логируется через log.info '[bot] denied: from=%d cmd=%s'"
    - "/channels возвращает текущий список из channels.json (loadChannels())"
    - "/add_channel @valid_username проходит regex-валидацию и записывает канал через mutate(); /add_channel с уже существующим каналом возвращает 'уже в списке' без записи"
  artifacts:
    - path: "src/bot.ts"
      provides: "polling loop + auth + /channels + /add_channel + sendMessage helper + CRUD wrappers"
      exports: ["startBot", "stopBot"]
      min_lines: 150
    - path: ".env.example"
      provides: "BOT_ALLOWED_USER_IDS секция с комментарием про @userinfobot"
      contains: "BOT_ALLOWED_USER_IDS"
  key_links:
    - from: "src/bot.ts"
      to: "src/channels-store.ts"
      via: "import { mutate, loadChannels, type ChannelEntry } from './channels-store.js'"
      pattern: "from ['\"]\\./channels-store\\.js['\"]"
    - from: "src/bot.ts"
      to: "https://api.telegram.org/bot${token}"
      via: "raw fetch к getUpdates / sendMessage / deleteWebhook"
      pattern: "api\\.telegram\\.org/bot"
    - from: "src/bot.ts"
      to: "src/logger.ts"
      via: "import { log } from './logger.js'"
      pattern: "from ['\"]\\./logger\\.js['\"]"
---

<objective>
Создать `src/bot.ts` с polling-loop'ом, allowlist-авторизацией, helper'ами и handler'ами для `/channels` и `/add_channel`. CRUD-обёртки над `mutate()` живут в этом же файле (D-16). Регистрация в `src/run.ts` — Plan 3.

Purpose: Закрыть BOT-01 (просмотр), BOT-02 (добавление), BOT-04 (allowlist), BOT-05 (polling без 409). Команда `/remove_channel` (BOT-03) — Plan 2.
Output: Один файл `src/bot.ts` (~180-250 LOC), обновлённый `.env.example` с `BOT_ALLOWED_USER_IDS`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-bot-commands/02-CONTEXT.md
@src/channels-store.ts
@src/alert.ts
@src/deliver.ts
@src/logger.ts
@.env.example

<interfaces>
<!-- Контракты, которые executor использует напрямую — никакого исследования кода. -->

From src/channels-store.ts:
```typescript
export const CHANNELS_PATH: string;
export type ChannelEntry = { username: string };
export function loadChannels(): ChannelEntry[];                  // throws если файла нет
export function saveChannels(channels: ChannelEntry[]): Promise<void>;
export function mutate(
  fn: (current: ChannelEntry[]) => Promise<ChannelEntry[]> | ChannelEntry[]
): Promise<void>;
```

From src/logger.ts:
```typescript
export const log: {
  info(msg: string, ...ctx: unknown[]): void;
  warn(msg: string, ...ctx: unknown[]): void;
  error(msg: string, ...ctx: unknown[]): void;
};
```

Telegram Bot API endpoints (raw fetch, как в src/alert.ts:51):
- POST `https://api.telegram.org/bot${token}/deleteWebhook` — body `{ drop_pending_updates: false }`
- POST `https://api.telegram.org/bot${token}/getUpdates` — body `{ offset, timeout: 30, allowed_updates: ["message", "callback_query"] }`
- POST `https://api.telegram.org/bot${token}/sendMessage` — body `{ chat_id, text, reply_to_message_id? }`

Telegram Update объект (минимальные поля, что нужны):
```typescript
interface TgUser { id: number; }
interface TgChat { id: number; }
interface TgMessage { message_id: number; from?: TgUser; chat: TgChat; text?: string; }
interface TgUpdate { update_id: number; message?: TgMessage; callback_query?: unknown; }
interface TgGetUpdatesResponse { ok: boolean; result: TgUpdate[]; description?: string; }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Создать src/bot.ts со скелетом — env-парсинг, типы, sendMessage helper, deleteWebhook, polling-loop</name>
  <files>src/bot.ts</files>
  <read_first>
    - src/alert.ts (целиком — паттерн raw fetch + behaviour при отсутствии env)
    - src/deliver.ts (целиком — паттерн sendMessage error handling: res.ok → throw)
    - src/channels-store.ts (целиком — для импорта типов и функций)
    - src/logger.ts (целиком — log.info/warn/error со стейдж-префиксом)
    - src/run.ts (целиком — паттерн in-memory state, graceful shutdown через флаг)
    - .env.example (для понимания формата секций)
  </read_first>
  <action>
    Создать новый файл `src/bot.ts` со следующим содержимым:

    1. **Header-комментарий**: `// src/bot.ts — Telegram bot polling + commands handler. Использует тот же TG_BOT_TOKEN, что и delivery (D-01). CRUD-обёртки рядом с handler'ами (D-16).`

    2. **Импорты (ESM с .js суффиксом)**:
       ```typescript
       import { mutate, loadChannels, type ChannelEntry } from "./channels-store.js";
       import { log } from "./logger.js";
       ```

    3. **Типы Telegram API** (минимальные, чтобы избежать `any`):
       ```typescript
       interface TgUser { id: number; }
       interface TgChat { id: number; }
       interface TgMessage { message_id: number; from?: TgUser; chat: TgChat; text?: string; }
       interface TgCallbackQuery { id: string; from: TgUser; message?: TgMessage; data?: string; }
       interface TgUpdate { update_id: number; message?: TgMessage; callback_query?: TgCallbackQuery; }
       interface TgGetUpdatesResponse { ok: boolean; result: TgUpdate[]; description?: string; }
       interface TgInlineKeyboardButton { text: string; callback_data: string; }
       interface TgReplyMarkup { inline_keyboard: TgInlineKeyboardButton[][]; }
       ```

    4. **Module-level state**:
       ```typescript
       const TG_API = "https://api.telegram.org";
       const POLL_TIMEOUT_SEC = 30;
       const USERNAME_REGEX = /^[a-zA-Z][a-zA-Z0-9_]{4,31}$/;
       let lastOffset = 0;
       let stopRequested = false;
       let pollingActive = false;
       ```

    5. **Helper `parseAllowlist(envValue: string | undefined): Set<number>`** — comma-separated numeric → Set<number>; пустые/нечисловые токены пропускает; экспорт для тестов.
       ```typescript
       export function parseAllowlist(envValue: string | undefined): Set<number> {
         if (!envValue) return new Set();
         return new Set(
           envValue
             .split(",")
             .map((s) => s.trim())
             .filter((s) => s.length > 0)
             .map((s) => Number.parseInt(s, 10))
             .filter((n) => Number.isInteger(n) && n > 0)
         );
       }
       ```

    6. **Helper `normalizeUsername(raw: string): string | null`** — strip leading `@`, lowercase НЕ делаем (Telegram username case-insensitive, но храним как ввели), валидация regex.
       ```typescript
       export function normalizeUsername(raw: string): string | null {
         const trimmed = raw.trim();
         const stripped = trimmed.startsWith("@") ? trimmed.slice(1) : trimmed;
         if (!USERNAME_REGEX.test(stripped)) return null;
         return stripped;
       }
       ```

    7. **Helper `tgFetch<T>(token: string, method: string, body: unknown): Promise<T>`** — обёртка над `fetch` к Bot API. На `!res.ok` — throw c HTTP-кодом и телом (паттерн из src/deliver.ts:78-80).
       ```typescript
       async function tgFetch<T>(token: string, method: string, body: unknown): Promise<T> {
         const res = await fetch(`${TG_API}/bot${token}/${method}`, {
           method: "POST",
           headers: { "content-type": "application/json" },
           body: JSON.stringify(body),
         });
         if (!res.ok) {
           const responseBody = await res.text();
           throw new Error(`[bot] ${method} failed: ${res.status} ${responseBody.slice(0, 300)}`);
         }
         return (await res.json()) as T;
       }
       ```

    8. **Helper `sendReply(token: string, chatId: number, replyToMessageId: number, text: string): Promise<void>`** — D-09, D-10: plain-text, reply_to_message_id, БЕЗ parse_mode.
       ```typescript
       async function sendReply(token: string, chatId: number, replyToMessageId: number, text: string): Promise<void> {
         await tgFetch<{ ok: boolean }>(token, "sendMessage", {
           chat_id: chatId,
           reply_to_message_id: replyToMessageId,
           text,
           disable_web_page_preview: true,
         });
       }
       ```

    9. **CRUD-обёртки (D-16)** — exported для тестов:
       ```typescript
       export function listChannels(): string {
         const channels = loadChannels();
         if (channels.length === 0) return "Список каналов пуст";
         const lines = channels.map((c, i) => `${i + 1}. @${c.username}`);
         return `Каналов: ${channels.length}\n` + lines.join("\n");
       }

       export async function addChannel(username: string): Promise<"added" | "exists"> {
         let result: "added" | "exists" = "added";
         await mutate((channels) => {
           if (channels.some((c) => c.username === username)) {
             result = "exists";
             return channels;
           }
           return [...channels, { username }];
         });
         return result;
       }

       export async function removeChannel(username: string): Promise<"removed" | "missing"> {
         let result: "removed" | "missing" = "removed";
         await mutate((channels) => {
           const next = channels.filter((c) => c.username !== username);
           if (next.length === channels.length) result = "missing";
           return next;
         });
         return result;
       }
       ```

    10. **Command router** — exported для тестов:
        ```typescript
        export async function handleCommand(
          token: string,
          msg: TgMessage,
          allowlist: Set<number>
        ): Promise<void> {
          const userId = msg.from?.id;
          const text = msg.text?.trim() ?? "";
          if (!userId || !text.startsWith("/")) return;

          // Извлекаем команду (до пробела или конца) с учётом suffix'а @botname.
          const firstWord = text.split(/\s+/)[0];
          const cmd = firstWord.split("@")[0]; // "/channels@MyBot" → "/channels"

          if (!allowlist.has(userId)) {
            log.info(`[bot] denied: from=${userId} cmd=${cmd}`);
            return;
          }

          if (cmd === "/channels") {
            const reply = listChannels();
            await sendReply(token, msg.chat.id, msg.message_id, reply);
            return;
          }
          if (cmd === "/add_channel") {
            const arg = text.slice(firstWord.length).trim();
            if (!arg) {
              await sendReply(token, msg.chat.id, msg.message_id,
                "Использование: /add_channel <username>\nНапример: /add_channel @durov");
              return;
            }
            const u = normalizeUsername(arg);
            if (!u) {
              await sendReply(token, msg.chat.id, msg.message_id,
                `Невалидный username: ${arg}\nДопустимы: 5-32 символа, начинается с буквы, далее буквы/цифры/_`);
              return;
            }
            const status = await addChannel(u);
            const reply = status === "added"
              ? `Добавлен @${u}. Будет использован в следующем прогоне в 20:15 MSK.`
              : `@${u} уже в списке.`;
            await sendReply(token, msg.chat.id, msg.message_id, reply);
            return;
          }
          // /remove_channel — добавляется в Plan 2.
          // Прочие команды игнорируются молча.
        }
        ```

    11. **Polling-loop**:
        ```typescript
        async function pollOnce(token: string, allowlist: Set<number>): Promise<void> {
          const resp = await tgFetch<TgGetUpdatesResponse>(token, "getUpdates", {
            offset: lastOffset,
            timeout: POLL_TIMEOUT_SEC,
            allowed_updates: ["message", "callback_query"],
          });
          if (!resp.ok) {
            log.warn(`[bot] getUpdates returned ok=false: ${resp.description ?? "(no description)"}`);
            return;
          }
          for (const upd of resp.result) {
            // Сдвигаем offset на update_id + 1, чтобы тот же update не пришёл повторно.
            lastOffset = Math.max(lastOffset, upd.update_id + 1);
            try {
              if (upd.message) {
                await handleCommand(token, upd.message, allowlist);
              }
              // callback_query — Plan 2.
            } catch (err) {
              log.error(`[bot] handler error: ${(err as Error).message}`);
            }
          }
        }

        async function pollLoop(token: string, allowlist: Set<number>): Promise<void> {
          // D-03: deleteWebhook ОБЯЗАТЕЛЕН до первого getUpdates.
          // drop_pending_updates: false — сохраняем очередь команд за время рестарта.
          try {
            await tgFetch<{ ok: boolean }>(token, "deleteWebhook", { drop_pending_updates: false });
            log.info("[bot] deleteWebhook ok (drop_pending_updates=false)");
          } catch (err) {
            log.error(`[bot] deleteWebhook failed: ${(err as Error).message}`);
            // Продолжаем — getUpdates даст 409 если webhook остался, поймаем в catch ниже.
          }

          const backoffMs = [1000, 2000, 4000];
          let backoffIdx = 0;

          while (!stopRequested) {
            try {
              await pollOnce(token, allowlist);
              backoffIdx = 0; // успех — сброс backoff'а
            } catch (err) {
              const delay = backoffMs[Math.min(backoffIdx, backoffMs.length - 1)];
              log.error(`[bot] poll error: ${(err as Error).message}; retry in ${delay}ms`);
              backoffIdx++;
              await new Promise((r) => setTimeout(r, delay));
            }
          }
        }
        ```

    12. **Public lifecycle**:
        ```typescript
        export async function startBot(): Promise<void> {
          const token = process.env.TG_BOT_TOKEN;
          const allowlistRaw = process.env.BOT_ALLOWED_USER_IDS;
          // D-04: симметрично src/alert.ts:23-32 — нет env → warn + return, daemon живёт.
          if (!token || !allowlistRaw) {
            log.warn("[bot] TG_BOT_TOKEN или BOT_ALLOWED_USER_IDS не задан — bot polling выключен");
            return;
          }
          const allowlist = parseAllowlist(allowlistRaw);
          if (allowlist.size === 0) {
            log.warn("[bot] BOT_ALLOWED_USER_IDS пуст после парсинга — bot polling выключен");
            return;
          }
          stopRequested = false;
          pollingActive = true;
          log.info(`[bot] polling started (allowlist size=${allowlist.size})`);
          // Внешний try/catch — финальная страховка: pollLoop сам делает exp.backoff
          // на сетевых ошибках, но если что-то проскочит мимо — логируем + flag вниз.
          // Run.ts (Plan 3) НЕ делает supervisor-loop поверх этого — single fire-and-forget,
          // outer catch там для финального alert. Контракт: startBot НЕ throw'ает наверх.
          try {
            await pollLoop(token, allowlist);
          } catch (err) {
            log.error(`[bot] poll loop crashed: ${(err as Error).message}`);
          } finally {
            pollingActive = false;
            log.info("[bot] polling stopped");
          }
        }

        export function stopBot(): void {
          stopRequested = true;
        }

        export function isBotPolling(): boolean {
          return pollingActive;
        }
        ```

    Структура файла: импорты → константы → типы → helpers (parseAllowlist, normalizeUsername, tgFetch, sendReply) → CRUD (listChannels, addChannel, removeChannel) → handleCommand → pollOnce → pollLoop → startBot/stopBot/isBotPolling.

    Все экспорты, помеченные `export`, должны быть видны для unit-тестов из Plan 4.

    **Контракт со стороны run.ts (Plan 3):** startBot() НЕ throw'ает наверх — внутренний try/catch
    логирует финальный crash. Resilience обеспечивается exp.backoff внутри pollLoop. Run.ts вызывает
    startBot() как fire-and-forget с outer try/catch только для финального alert (без restart-loop'а).
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - File `src/bot.ts` exists.
    - `grep -c "^export " src/bot.ts` returns at least 7 (parseAllowlist, normalizeUsername, listChannels, addChannel, removeChannel, handleCommand, startBot, stopBot, isBotPolling).
    - `grep "export async function startBot" src/bot.ts` returns at least one match.
    - `grep "export function stopBot" src/bot.ts` returns at least one match.
    - `grep "deleteWebhook" src/bot.ts` returns at least one match AND that line/area contains `drop_pending_updates: false`.
    - `grep "BOT_ALLOWED_USER_IDS" src/bot.ts` returns at least one match (env read in startBot).
    - `grep "TG_BOT_TOKEN" src/bot.ts` returns at least one match.
    - `grep "from ['\"]\\./channels-store\\.js['\"]" src/bot.ts` returns at least one match.
    - `grep "from ['\"]\\./logger\\.js['\"]" src/bot.ts` returns at least one match.
    - `grep "\\[bot\\] denied:" src/bot.ts` returns at least one match (D-08 log format).
    - `grep "reply_to_message_id" src/bot.ts` returns at least one match (D-09).
    - `grep "parse_mode" src/bot.ts` returns NO matches (D-10: plain-text).
    - `grep "allowed_updates" src/bot.ts` returns at least one match including `"message"` AND `"callback_query"`.
    - `grep "update_id + 1" src/bot.ts` returns at least one match (W-1: гарантия что offset сдвигается на следующий update).
    - `npx tsc --noEmit` exits 0.
    - File length ≥ 150 lines: `wc -l src/bot.ts` ≥ 150.
  </acceptance_criteria>
  <done>
    `src/bot.ts` создан, `npx tsc --noEmit` проходит, все экспорты на месте, форматы логов и API-вызовов соответствуют D-03/D-04/D-08/D-09/D-10. Polling запускается из startBot, останавливается через stopBot.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Добавить BOT_ALLOWED_USER_IDS в .env.example с инструкцией про @userinfobot</name>
  <files>.env.example</files>
  <read_first>
    - .env.example (целиком — текущая структура секций)
    - .planning/phases/02-bot-commands/02-CONTEXT.md (D-07 формат: comma-separated numeric user.id)
  </read_first>
  <action>
    Открыть `.env.example` и добавить новую секцию ПОСЛЕ блока `Telegram bot (доставка дайджеста...)` (после строки `TG_CHANNEL_ID=`), перед блоком DeepSeek. Точное содержимое нового блока:

    ```
    # -----------------------------------------------------------------------------
    # Telegram bot commands (управление каналами через бота, BOT-04)
    # -----------------------------------------------------------------------------
    # Comma-separated numeric Telegram user.id, имеющих доступ к командам бота.
    # Узнать свой numeric id: написать @userinfobot, скопировать "Id".
    # Пример: BOT_ALLOWED_USER_IDS=12345678,87654321
    # Если переменная пуста или не задана — bot polling не запускается, daemon стартует только с cron.
    BOT_ALLOWED_USER_IDS=
    ```

    Важно: блок добавляется ОТДЕЛЬНОЙ секцией, а не вписывается в существующий блок Bot — visually отделяет «delivery» (TG_BOT_TOKEN+TG_CHANNEL_ID, тот же бот) от «commands» (allowlist, тот же бот через тот же токен). D-01: TG_BOT_TOKEN остаётся ОДИН.
  </action>
  <verify>
    <automated>grep -c "BOT_ALLOWED_USER_IDS" .env.example</automated>
  </verify>
  <acceptance_criteria>
    - `grep "BOT_ALLOWED_USER_IDS" .env.example` returns at least one match.
    - `grep "@userinfobot" .env.example` returns at least one match (инструкция как узнать id).
    - `grep "BOT_ALLOWED_USER_IDS=12345678,87654321" .env.example` returns at least one match (формат-пример).
    - `grep -c "TG_BOT_TOKEN" .env.example` returns exactly 1 (НЕ дублируется новый токен — D-01).
    - Секция `BOT_ALLOWED_USER_IDS` идёт после `TG_CHANNEL_ID` и до `DEEPSEEK_API_KEY` (проверить порядком grep -n).
  </acceptance_criteria>
  <done>
    `.env.example` содержит секцию с `BOT_ALLOWED_USER_IDS`, comment-инструкцией про @userinfobot, примером формата. Никаких новых TOKEN-переменных не добавлено.
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` exits 0 (типы валидны).
- `wc -l src/bot.ts` ≥ 150 (полноценный файл, не заглушка).
- `grep "deleteWebhook" src/bot.ts` найден И в контексте есть `drop_pending_updates: false` (D-03).
- `grep "parse_mode" src/bot.ts` пусто (D-10 plain-text).
- `grep "update_id + 1" src/bot.ts` найден (W-1).
- `grep "BOT_ALLOWED_USER_IDS" .env.example` найден.
- Никаких новых runtime-зависимостей в `package.json` (cap=4: telegram, openai, node-cron, zod).
</verification>

<success_criteria>
1. `src/bot.ts` экспортирует `startBot`, `stopBot`, `isBotPolling`, `parseAllowlist`, `normalizeUsername`, `listChannels`, `addChannel`, `removeChannel`, `handleCommand`.
2. При отсутствии env startBot логирует warn и возвращается (D-04).
3. На boot polling вызывается deleteWebhook с drop_pending_updates=false (D-03).
4. `/channels` от allowlist-пользователя возвращает список (BOT-01).
5. `/add_channel @valid` валидирует username (regex `^[a-zA-Z][a-zA-Z0-9_]{4,31}$`), нормализует (strip `@`), записывает через mutate(); идемпотентно (BOT-02).
6. Не-allowlist пользователь — silent ignore + log.info `[bot] denied: from=%d cmd=%s` (BOT-04, D-07/D-08).
7. `.env.example` содержит `BOT_ALLOWED_USER_IDS=` с комментарием про @userinfobot.
</success_criteria>

<output>
After completion, create `.planning/phases/02-bot-commands/02-01-SUMMARY.md`
</output>
