---
phase: 02-bot-commands
plan: 04
type: execute
wave: 2
depends_on: [02-01, 02-02]
files_modified:
  - src/__tests__/bot-handlers.test.ts
  - README.md
autonomous: true
requirements: [BOT-01, BOT-02, BOT-03, BOT-04]
must_haves:
  truths:
    - "Vitest-тесты покрывают: parseAllowlist (валидные/невалидные ID, пустой, большие positive numeric id), normalizeUsername (regex pass/fail, strip @), parseRemoveCallbackData (валидный confirm/cancel, невалидный)"
    - "Vitest-тесты покрывают CRUD-обёртки (addChannel idempotent, removeChannel idempotent) с mock'ом mutate из channels-store"
    - "Vitest-тесты покрывают handleCommand: allowlist-фильтр (denied silent + log.info), /channels возвращает список, /add_channel валидирует и добавляет, /remove_channel показывает inline-keyboard"
    - "Vitest-тесты покрывают handleCallbackQuery: allowlist-фильтр, парсинг callback_data, idempotent confirm, cancel не меняет файл"
    - "npm test exits 0"
    - "README.md содержит секцию 'Команды бота' с описанием /channels, /add_channel, /remove_channel и инструкцией про BOT_ALLOWED_USER_IDS"
  artifacts:
    - path: "src/__tests__/bot-handlers.test.ts"
      provides: "Vitest test suite для bot-handlers (parseAllowlist, normalizeUsername, parseRemoveCallbackData, handleCommand, handleCallbackQuery, addChannel, removeChannel)"
      contains: ["describe", "vi.mock", "expect"]
      min_lines: 200
    - path: "README.md"
      provides: "секция 'Команды бота' с описанием 3 команд и BOT_ALLOWED_USER_IDS"
      contains_after_edit: ["BOT_ALLOWED_USER_IDS", "/channels", "/add_channel", "/remove_channel"]
  key_links:
    - from: "src/__tests__/bot-handlers.test.ts"
      to: "src/bot.ts"
      via: "import (parseAllowlist, normalizeUsername, parseRemoveCallbackData, handleCommand, handleCallbackQuery, addChannel, removeChannel)"
      pattern: "from ['\"]\\.\\./bot\\.js['\"]"
    - from: "src/__tests__/bot-handlers.test.ts"
      to: "src/channels-store.ts"
      via: "vi.mock('../channels-store.js')"
      pattern: "vi\\.mock\\(['\"]\\.\\./channels-store"
---

<objective>
Покрыть bot-handlers Vitest-тестами и обновить README. Polling-loop не тестируется (требует реальный bot token); covered ручным smoke-cheklist в HUMAN-UAT отдельной фазой. BOT-05 (run.ts integration) НЕ покрывается этим планом — это работа Plan 3, доказывается typecheck'ом и smoke'ом.

Purpose: Доказать BOT-01..04 через автотесты, дать оператору документацию.
Output: `src/__tests__/bot-handlers.test.ts` + расширенный README.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-bot-commands/02-CONTEXT.md
@.planning/phases/02-bot-commands/02-01-PLAN.md
@.planning/phases/02-bot-commands/02-02-PLAN.md
@src/bot.ts
@src/channels-store.ts
@src/__tests__/channels-store.test.ts
@README.md

<interfaces>
<!-- Контракты после Plans 1-2 завершены. Plan 3 (run.ts) НЕ нужен для этого test-плана. -->

From src/bot.ts (всё, что exported для тестов):
```typescript
export function parseAllowlist(envValue: string | undefined): Set<number>;
export function normalizeUsername(raw: string): string | null;
export function parseRemoveCallbackData(data: string): { username: string; action: "confirm" | "cancel" } | null;
export function listChannels(): string;
export function addChannel(username: string): Promise<"added" | "exists">;
export function removeChannel(username: string): Promise<"removed" | "missing">;
export async function handleCommand(token: string, msg: TgMessage, allowlist: Set<number>): Promise<void>;
export async function handleCallbackQuery(token: string, cb: TgCallbackQuery, allowlist: Set<number>): Promise<void>;
```

Vitest mocking pattern (стандартный):
```typescript
import { vi, describe, it, expect, beforeEach } from "vitest";
vi.mock("../channels-store.js", () => ({
  loadChannels: vi.fn(),
  mutate: vi.fn(),
  saveChannels: vi.fn(),
  CHANNELS_PATH: "./channels.json",
}));
```

global.fetch перехватывается через `vi.stubGlobal('fetch', vi.fn().mockResolvedValue({ ok: true, json: async () => ({ ok: true }), text: async () => "" }))`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Создать src/__tests__/bot-handlers.test.ts с Vitest-тестами для всех exported helpers и handlers</name>
  <files>src/__tests__/bot-handlers.test.ts</files>
  <read_first>
    - src/bot.ts (целиком — какие функции exported)
    - src/__tests__/channels-store.test.ts (пример vitest-стиля, mock-паттерны)
    - src/channels-store.ts (mutate signature для mock'а)
    - vitest.config.ts (конфиг)
  </read_first>
  <action>
    Создать новый файл `src/__tests__/bot-handlers.test.ts`.

    Структура (precise content guide):

    1. **Imports + mocks** в начале:
       ```typescript
       import { describe, it, expect, beforeEach, vi } from "vitest";
       import {
         parseAllowlist,
         normalizeUsername,
         parseRemoveCallbackData,
         addChannel,
         removeChannel,
         listChannels,
         handleCommand,
         handleCallbackQuery,
       } from "../bot.js";
       import { loadChannels, mutate, type ChannelEntry } from "../channels-store.js";

       vi.mock("../channels-store.js", () => ({
         CHANNELS_PATH: "./channels.json",
         loadChannels: vi.fn(),
         mutate: vi.fn(),
         saveChannels: vi.fn(),
       }));

       const mockedLoadChannels = vi.mocked(loadChannels);
       const mockedMutate = vi.mocked(mutate);
       ```

    2. **Setup global.fetch перед каждым тестом** (для handleCommand/handleCallbackQuery, которые делают fetch).
       W-6: ОДИН явный helper `withCurrentChannels` — никаких альтернативных подходов.
       I-2: spy на console.log для проверки D-08 формата лога `[bot] denied:`.
       ```typescript
       // W-6: единый helper для подмены текущего состояния channels.json внутри mutate(fn).
       function withCurrentChannels(channels: ChannelEntry[]): void {
         mockedMutate.mockImplementation(async (fn) => {
           await fn(channels);
         });
       }

       // I-2: spy на console.log для проверки `[bot] denied:` (logger пишет в console).
       let consoleLogSpy: ReturnType<typeof vi.spyOn>;

       beforeEach(() => {
         vi.clearAllMocks();
         vi.stubGlobal("fetch", vi.fn().mockResolvedValue({
           ok: true,
           json: async () => ({ ok: true, result: [] }),
           text: async () => "",
         }));
         consoleLogSpy = vi.spyOn(console, "log").mockImplementation(() => {});
         // Default: пустой channels list — переопределяется в каждом тесте через withCurrentChannels.
         withCurrentChannels([]);
         mockedLoadChannels.mockReturnValue([]);
       });
       ```

    3. **describe('parseAllowlist')** — кейсы:
       - Пустая строка / undefined → пустой Set
       - "12345,67890" → Set с двумя числами
       - "  12345 , 67890  " → trim работает
       - "abc,12345" → нечисловое отсеяно, остаётся {12345}
       - "0,-1,12345" → 0 и отрицательные отсеяны (n > 0)
       - "12345,12345" → Set дедупит → size 1
       - **W-2: большие positive numeric user.id**:
         ```typescript
         it("принимает большие positive numeric user.id (Telegram использует 9-10 цифр)", () => {
           expect(parseAllowlist("7382916482")).toEqual(new Set([7382916482]));
         });
         ```

    4. **describe('normalizeUsername')** — кейсы:
       - "@durov" → "durov"
       - "durov" → "durov"
       - "  @ch_name123  " → "ch_name123"
       - "abc" → null (4 символов мало, нужно 5+)
       - "1abcde" → null (начинается с цифры)
       - "abcde" → "abcde" (5 символов — минимум)
       - 32-символьный валидный → возвращён
       - 33-символьный → null
       - "abc-def" → null (дефис не разрешён)

    5. **describe('parseRemoveCallbackData')** — кейсы:
       - "rm:durov:confirm" → { username: "durov", action: "confirm" }
       - "rm:durov:cancel" → { username: "durov", action: "cancel" }
       - "rm:durov:other" → null
       - "other:durov:confirm" → null
       - "rm:durov" → null (неполный формат)
       - "rm::confirm" → null (пустой username)
       - "" → null

    6. **describe('addChannel')** — кейсы (с mock'нутым mutate):
       - addChannel("newch") когда current=[] → mutate вызван 1 раз, возвращает "added".
         I-3: `expect(mockedMutate).toHaveBeenCalledTimes(1)` + проверка что переданная fn — это
         `expect(typeof mockedMutate.mock.calls[0][0]).toBe("function")`.
       - addChannel("existing") когда current=[{username:"existing"}] → mutate вызван, fn вернул unchanged, возвращает "exists":
         ```typescript
         it("возвращает 'exists' если канал уже в списке (idempotent)", async () => {
           withCurrentChannels([{ username: "existing" }]);
           const result = await addChannel("existing");
           expect(result).toBe("exists");
           expect(mockedMutate).toHaveBeenCalledTimes(1);
         });
         ```

    7. **describe('removeChannel')** — кейсы:
       - removeChannel("present") когда current=[{username:"present"}, {username:"other"}]:
         ```typescript
         it("возвращает 'removed' если канал есть", async () => {
           withCurrentChannels([{ username: "present" }, { username: "other" }]);
           const result = await removeChannel("present");
           expect(result).toBe("removed");
         });
         ```
       - removeChannel("absent") когда current=[{username:"other"}] → "missing" (idempotent, D-14):
         ```typescript
         it("возвращает 'missing' если канал отсутствует (D-14, idempotent)", async () => {
           withCurrentChannels([{ username: "other" }]);
           const result = await removeChannel("absent");
           expect(result).toBe("missing");
         });
         ```

    8. **describe('listChannels')** — кейсы (с mock'нутым loadChannels):
       - loadChannels вернул [] → строка "Список каналов пуст"
       - loadChannels вернул [{username:"a"}, {username:"b"}] → строка содержит "Каналов: 2", "1. @a", "2. @b"

    9. **B-5 fix: ОТДЕЛЬНЫЙ describe для handleCommand allowlist** (имя обязательно содержит «handleCommand allowlist» для grep'а acceptance):
       ```typescript
       describe("handleCommand allowlist", () => {
         it("отказывает не-allowlist пользователю молча (silent ignore + log [bot] denied:)", async () => {
           const allowlist = new Set([111]);
           const msg = {
             message_id: 1,
             chat: { id: 555 },
             from: { id: 999 }, // не в allowlist
             text: "/channels",
           };
           await handleCommand("token", msg, allowlist);
           // Silent ignore: fetch НЕ вызван.
           expect(globalThis.fetch).not.toHaveBeenCalled();
           // I-2 / D-08: проверяем формат лога (logger.info → console.log с "[bot] denied:" префиксом).
           const denyLogged = consoleLogSpy.mock.calls.some((call) =>
             call.some((arg) => typeof arg === "string" && arg.includes("[bot] denied:"))
           );
           expect(denyLogged).toBe(true);
         });
       });
       ```

    10. **describe('handleCommand /channels')** (отдельно, имя содержит «handleCommand» И «/channels»):
        ```typescript
        describe("handleCommand /channels", () => {
          it("allowlist-пользователь видит список каналов", async () => {
            mockedLoadChannels.mockReturnValue([{ username: "a" }, { username: "b" }]);
            const allowlist = new Set([111]);
            const msg = {
              message_id: 1,
              chat: { id: 555 },
              from: { id: 111 },
              text: "/channels",
            };
            await handleCommand("token", msg, allowlist);
            // sendMessage вызван с текстом списка.
            const calls = (globalThis.fetch as any).mock.calls;
            expect(calls.length).toBeGreaterThanOrEqual(1);
            const url0 = calls[0][0] as string;
            expect(url0).toContain("/sendMessage");
            const body0 = JSON.parse((calls[0][1].body as string));
            expect(body0.text).toContain("Каналов:");
          });
        });
        ```

    11. **describe('handleCommand /add_channel')** (имя обязательно содержит «handleCommand» И «add_channel»):
        - "/add_channel @validuser" с пустым current → mutate вызван 1 раз, fetch (sendMessage) с текстом "Добавлен @validuser"
        - "/add_channel @existinguser" когда уже есть → mutate вызван, ответ "уже в списке"
        - "/add_channel" без аргумента → fetch с "Использование: /add_channel"
        - "/add_channel @bad" (3 символа) → ответ "Невалидный username"

    12. **describe('handleCommand /remove_channel')** (имя обязательно содержит «handleCommand» И «remove_channel»):
        - "/remove_channel @durov" → fetch вызван с body содержащим `inline_keyboard` и `rm:durov:confirm` И `rm:durov:cancel`

    13. **B-5 fix: ОТДЕЛЬНЫЙ describe для handleCallbackQuery allowlist** (имя обязательно содержит «handleCallbackQuery allowlist»):
        ```typescript
        describe("handleCallbackQuery allowlist", () => {
          it("отказывает не-allowlist пользователю silent (без answerCallbackQuery)", async () => {
            const allowlist = new Set([111]);
            const cb = {
              id: "cb1",
              from: { id: 999 },  // не в allowlist
              message: { message_id: 1, chat: { id: 555 } },
              data: "rm:durov:confirm",
            };
            await handleCallbackQuery("token", cb, allowlist);
            // D-07: НИКАКОГО fetch (даже answerCallbackQuery).
            expect(globalThis.fetch).not.toHaveBeenCalled();
            const denyLogged = consoleLogSpy.mock.calls.some((call) =>
              call.some((arg) => typeof arg === "string" && arg.includes("[bot] denied:"))
            );
            expect(denyLogged).toBe(true);
          });
        });
        ```

    14. **describe('handleCallbackQuery confirm')** (имя обязательно содержит «handleCallbackQuery» И «confirm»):
        - data="rm:absent:confirm", current=[{username: "other"}] → mutate вызван, editMessageText содержит "не найден в списке (возможно, уже удалён)"
        - data="rm:durov:confirm", current=[{username: "durov"}] → mutate вызван, editMessageText содержит "Удалён @durov"

    15. **describe('handleCallbackQuery cancel')** (имя обязательно содержит «handleCallbackQuery» И «cancel»):
        - data="rm:durov:cancel" → mutate НЕ вызван, editMessageText содержит "Отмена удаления @durov"

    Файл должен иметь ≥ 200 строк (полное покрытие).

    Проверка вызовов fetch (хелпер):
    ```typescript
    function fetchCallsTo(method: string): unknown[] {
      const fetchMock = vi.mocked(globalThis.fetch);
      return fetchMock.mock.calls
        .filter(([url]) => typeof url === "string" && url.includes(`/${method}`))
        .map(([, init]) => JSON.parse(((init as RequestInit | undefined)?.body as string) ?? "{}"));
    }
    ```
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <acceptance_criteria>
    - File `src/__tests__/bot-handlers.test.ts` exists.
    - `wc -l src/__tests__/bot-handlers.test.ts` ≥ 200.
    - **B-4 fix: ≥ 12 describe-блоков в сумме**: `grep -c "describe(" src/__tests__/bot-handlers.test.ts` ≥ 12.
    - **B-4 / B-5 fix: per-name acceptance** (12 раздельных grep'ов; каждый ≥1):
      - `grep "describe.*parseAllowlist" src/__tests__/bot-handlers.test.ts` ≥ 1
      - `grep "describe.*normalizeUsername" src/__tests__/bot-handlers.test.ts` ≥ 1
      - `grep "describe.*parseRemoveCallbackData" src/__tests__/bot-handlers.test.ts` ≥ 1
      - `grep "describe.*addChannel" src/__tests__/bot-handlers.test.ts` ≥ 1
      - `grep "describe.*removeChannel" src/__tests__/bot-handlers.test.ts` ≥ 1
      - `grep "describe.*listChannels" src/__tests__/bot-handlers.test.ts` ≥ 1
      - `grep "describe.*handleCommand.*allowlist\\|allowlist.*handleCommand" src/__tests__/bot-handlers.test.ts` ≥ 1
      - `grep "describe.*handleCommand.*add_channel" src/__tests__/bot-handlers.test.ts` ≥ 1
      - `grep "describe.*handleCommand.*remove_channel" src/__tests__/bot-handlers.test.ts` ≥ 1
      - `grep "describe.*handleCallbackQuery.*allowlist\\|allowlist.*handleCallbackQuery" src/__tests__/bot-handlers.test.ts` ≥ 1
      - `grep "describe.*handleCallbackQuery.*confirm" src/__tests__/bot-handlers.test.ts` ≥ 1
      - `grep "describe.*handleCallbackQuery.*cancel" src/__tests__/bot-handlers.test.ts` ≥ 1
    - **B-5 fix: handleCommand /channels должен быть отдельным describe** (не схлопнут с allowlist):
      - `grep "describe.*handleCommand.*\\/channels\\|describe.*\\/channels.*handleCommand" src/__tests__/bot-handlers.test.ts` ≥ 1 (обязателен для покрытия BOT-01).
    - `grep "vi.mock.*channels-store" src/__tests__/bot-handlers.test.ts` ≥ 1.
    - `grep "vi.stubGlobal.*fetch" src/__tests__/bot-handlers.test.ts` ≥ 1.
    - **W-6 fix: единый helper withCurrentChannels** — `grep -c "function withCurrentChannels" src/__tests__/bot-handlers.test.ts` ≥ 1; используется минимум 3 раза: `grep -c "withCurrentChannels(" src/__tests__/bot-handlers.test.ts` ≥ 3.
    - **W-2 fix: большой positive numeric id** — `grep "7382916482" src/__tests__/bot-handlers.test.ts` ≥ 1.
    - **I-2: spy на console.log** — `grep "spyOn(console" src/__tests__/bot-handlers.test.ts` ≥ 1; `grep "\\[bot\\] denied:" src/__tests__/bot-handlers.test.ts` ≥ 1.
    - **I-3: assertions на mutate.toHaveBeenCalled** — `grep "mockedMutate.*toHaveBeenCalled\\|mutate.*toHaveBeenCalled" src/__tests__/bot-handlers.test.ts` ≥ 2.
    - `grep "rm:" src/__tests__/bot-handlers.test.ts` ≥ 4 matches (тестирование callback_data).
    - `grep "уже удалён\\|уже в списке\\|не найден" src/__tests__/bot-handlers.test.ts` ≥ 1 (idempotent BOT-02/BOT-03).
    - `npm test` exits 0.
    - `npm test` output contains text "bot-handlers" (test file picked up by vitest).
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>
    `src/__tests__/bot-handlers.test.ts` создан с описанной структурой; все тесты проходят (`npm test` exits 0); типы валидны.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Добавить секцию «Команды бота» в README.md с инструкцией про BOT_ALLOWED_USER_IDS</name>
  <files>README.md</files>
  <read_first>
    - README.md (целиком — текущая структура, тон, формат секций)
    - .env.example (новая секция BOT_ALLOWED_USER_IDS — ссылаться на ту же инструкцию через @userinfobot)
    - .planning/phases/02-bot-commands/02-CONTEXT.md (полный список 3 команд + UX)
  </read_first>
  <action>
    Открыть `README.md`. Найти подходящее место для новой секции «Команды бота» — желательно после секции запуска под PM2 / запуска daemon, перед секциями про логи или дебаг. Если естественного места нет — добавить новой секцией под `## Команды бота` ближе к концу, до секций tech-debt/архитектуры.

    Содержимое секции (точный текст):

    ```markdown
    ## Команды бота

    Бот использует тот же `TG_BOT_TOKEN`, что и доставка дайджеста. Дополнительно требуется
    `BOT_ALLOWED_USER_IDS` — список Telegram numeric `user.id`, которым разрешено пользоваться командами.

    ### Настройка allowlist

    1. Узнайте свой numeric user_id: напишите [@userinfobot](https://t.me/userinfobot) и скопируйте поле `Id`.
    2. Добавьте `BOT_ALLOWED_USER_IDS=12345678,87654321` в `.env` (через запятую, без пробелов).
    3. Если переменная пустая или не задана — bot polling не запускается, daemon стартует только с cron.

    ### Список команд

    Все команды отправляются в личку боту от пользователя из allowlist. Не-allowlist пользователи
    игнорируются молча (без ответа).

    | Команда | Назначение |
    |---------|------------|
    | `/channels` | Показать текущий список каналов из `channels.json` |
    | `/add_channel <username>` | Добавить публичный канал (валидируется regex, идемпотентно) |
    | `/remove_channel <username>` | Удалить канал с подтверждением через inline-кнопку |

    Примеры:
    - `/add_channel @durov`
    - `/add_channel durov` (с `@` или без)
    - `/remove_channel @some_channel` → бот покажет inline-кнопки «Удалить» / «Отмена»

    ### Поведение при перезапуске

    `pm2 restart` штатно завершает polling-loop (graceful, ≤35s). На boot polling вызывается
    `deleteWebhook(drop_pending_updates: false)` — это сохраняет очередь команд, накопившуюся
    за время перезапуска.

    ### Что не делают команды (Out of Scope)

    - Не резолвят канал через Telegram — оператор сам ставит подписку на user-аккаунт. Несуществующие
      каналы будут пропущены в pipeline (стандартный обработчик `UsernameNotOccupiedError`).
    - Не управляют расписанием прогона / не показывают статус — для этого `pm2 logs`.
    - Не работают через webhook — только long-polling.
    ```

    Если в README уже есть секция про PM2 — обновить её, добавив строку:
    «Перед запуском задайте `BOT_ALLOWED_USER_IDS` в `.env`, иначе bot polling будет выключен (daemon стартует только с cron).»
  </action>
  <verify>
    <automated>grep -c "BOT_ALLOWED_USER_IDS" README.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep "## Команды бота" README.md` returns at least one match.
    - `grep "BOT_ALLOWED_USER_IDS" README.md` returns at least one match.
    - `grep "@userinfobot" README.md` returns at least one match.
    - `grep "/channels" README.md` returns at least one match.
    - `grep "/add_channel" README.md` returns at least one match.
    - `grep "/remove_channel" README.md` returns at least one match.
    - `grep "deleteWebhook" README.md` returns at least one match (D-03 documented).
    - `grep "drop_pending_updates" README.md` returns at least one match.
  </acceptance_criteria>
  <done>
    README.md содержит секцию «Команды бота» с инструкцией про BOT_ALLOWED_USER_IDS, описанием 3 команд, примерами, поведением при перезапуске.
  </done>
</task>

</tasks>

<verification>
- `npm test` exits 0.
- `npx tsc --noEmit` exits 0.
- Все 4 BOT-XX requirements покрыты тестами:
  - BOT-01: describe для listChannels + handleCommand /channels (отдельный describe).
  - BOT-02: describe для addChannel + handleCommand /add_channel (включая невалидные).
  - BOT-03: describe для handleCallbackQuery (confirm/cancel + idempotent + parseRemoveCallbackData) + describe для handleCommand /remove_channel.
  - BOT-04: describe для parseAllowlist + handleCommand allowlist + handleCallbackQuery allowlist (раздельные describe — B-5).
- BOT-05 НЕ требуется здесь — covered Plan 3 (run.ts integration), доказывается typecheck'ом и smoke'ом.
- README обновлён (3 команды + BOT_ALLOWED_USER_IDS + @userinfobot инструкция).
</verification>

<success_criteria>
1. `npm test` показывает "bot-handlers" в выводе и проходит без падений.
2. Покрытие: parseAllowlist (включая большие positive id, W-2), normalizeUsername, parseRemoveCallbackData, addChannel, removeChannel, listChannels, handleCommand (allowlist + 3 команды раздельно), handleCallbackQuery (allowlist + confirm + cancel + idempotent).
3. README документирует команды бота, env-переменную allowlist, инструкцию про @userinfobot.
4. Никаких новых runtime/dev-зависимостей (vitest уже в devDependencies).
</success_criteria>

<output>
After completion, create `.planning/phases/02-bot-commands/02-04-SUMMARY.md`
</output>
