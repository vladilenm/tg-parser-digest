---
phase: 02-bot-commands
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - src/bot.ts
autonomous: true
requirements: [BOT-03]
must_haves:
  truths:
    - "/remove_channel @ch1 от allowlist-пользователя показывает inline-keyboard с двумя кнопками 'Удалить' и 'Отмена'"
    - "callback_data кнопок имеет формат 'rm:<username>:confirm' и 'rm:<username>:cancel' (username self-contained, переживает restart)"
    - "Любой allowlist-пользователь (не только инициатор) может нажать кнопку (D-12)"
    - "Не-allowlist пользователь, нажавший кнопку — silent ignore + log.info '[bot] denied: from=%d cmd=...' (D-07/D-08)"
    - "После confirm: канал удалён из channels.json через mutate(filter), inline-кнопки убраны (editMessageReplyMarkup пустая клавиатура), текст сообщения обновлён через editMessageText (D-13)"
    - "После cancel: channels.json не меняется, inline-кнопки убраны, текст сообщения обновлён (D-13)"
    - "Idempotent: confirm на уже удалённый канал → mutate (filter) пропускает, ответ 'не найден в списке (возможно, уже удалён)' (D-14)"
  artifacts:
    - path: "src/bot.ts"
      provides: "/remove_channel handler + callback_query handler + answerCallbackQuery + editMessage helpers"
      contains_after_edit: ["/remove_channel", "callback_query", "editMessageReplyMarkup", "editMessageText", "rm:"]
  key_links:
    - from: "src/bot.ts (/remove_channel handler)"
      to: "src/bot.ts (sendMessage with reply_markup)"
      via: "inline_keyboard с двумя кнопками"
      pattern: "inline_keyboard"
    - from: "src/bot.ts (callback_query handler)"
      to: "src/bot.ts (removeChannel)"
      via: "вызов removeChannel(username) из обработчика confirm"
      pattern: "removeChannel\\("
    - from: "src/bot.ts (callback_query handler)"
      to: "Telegram editMessageReplyMarkup + editMessageText"
      via: "tgFetch для уборки кнопок и обновления текста"
      pattern: "editMessage(ReplyMarkup|Text)"
---

<objective>
Расширить `src/bot.ts`: добавить handler для `/remove_channel <username>` (показ inline-keyboard) и обработку callback_query для confirm/cancel. Idempotent removeChannel (уже есть из Plan 1) обрабатывает duplicate confirms через mutex (Phase 1).

Purpose: Закрыть BOT-03 — удаление канала через inline-подтверждение.
Output: Расширенный `src/bot.ts` (+~80-100 LOC).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-bot-commands/02-CONTEXT.md
@.planning/phases/02-bot-commands/02-01-PLAN.md
@src/bot.ts
@src/channels-store.ts
@src/logger.ts

<interfaces>
<!-- Контракты после Plan 1 завершён. -->

From src/bot.ts (Plan 1):
```typescript
export function parseAllowlist(envValue: string | undefined): Set<number>;
export function normalizeUsername(raw: string): string | null;
export function listChannels(): string;
export function addChannel(username: string): Promise<"added" | "exists">;
export function removeChannel(username: string): Promise<"removed" | "missing">;
export async function handleCommand(token: string, msg: TgMessage, allowlist: Set<number>): Promise<void>;
```

Telegram Bot API (для callback handling):
- `answerCallbackQuery` — body `{ callback_query_id }` (опционально `text`); ОБЯЗАТЕЛЕН в течение 15 мин (https://core.telegram.org/bots/api#answercallbackquery).
- `editMessageReplyMarkup` — body `{ chat_id, message_id, reply_markup: { inline_keyboard: [] } }`.
- `editMessageText` — body `{ chat_id, message_id, text }`.
- `sendMessage` с `reply_markup` — body `{ chat_id, text, reply_to_message_id, reply_markup: { inline_keyboard: [[{text, callback_data}, ...]] } }`.

callback_data format (D-11):
- `rm:<username>:confirm`
- `rm:<username>:cancel`
- Лимит 64 байта; username ≤32 символов → влезает.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Добавить /remove_channel handler с inline-keyboard в src/bot.ts</name>
  <files>src/bot.ts</files>
  <read_first>
    - src/bot.ts (целиком — существующий код Plan 1, особенно handleCommand и tgFetch)
    - .planning/phases/02-bot-commands/02-CONTEXT.md (D-11, D-13)
  </read_first>
  <action>
    Внести следующие правки в `src/bot.ts`:

    1. **Добавить helper для отправки inline-keyboard** (рядом с `sendReply`):
       ```typescript
       async function sendReplyWithKeyboard(
         token: string,
         chatId: number,
         replyToMessageId: number,
         text: string,
         keyboard: TgInlineKeyboardButton[][]
       ): Promise<void> {
         await tgFetch<{ ok: boolean }>(token, "sendMessage", {
           chat_id: chatId,
           reply_to_message_id: replyToMessageId,
           text,
           disable_web_page_preview: true,
           reply_markup: { inline_keyboard: keyboard },
         });
       }
       ```

    2. **Добавить ветку `/remove_channel` в `handleCommand`** (после ветки `/add_channel`):
       ```typescript
       if (cmd === "/remove_channel") {
         const arg = text.slice(firstWord.length).trim();
         if (!arg) {
           await sendReply(token, msg.chat.id, msg.message_id,
             "Использование: /remove_channel <username>\nНапример: /remove_channel @durov");
           return;
         }
         const u = normalizeUsername(arg);
         if (!u) {
           await sendReply(token, msg.chat.id, msg.message_id,
             `Невалидный username: ${arg}\nДопустимы: 5-32 символа, начинается с буквы, далее буквы/цифры/_`);
           return;
         }
         // D-11: callback_data самодостаточный — username внутри.
         const keyboard: TgInlineKeyboardButton[][] = [[
           { text: "Удалить", callback_data: `rm:${u}:confirm` },
           { text: "Отмена",   callback_data: `rm:${u}:cancel`  },
         ]];
         await sendReplyWithKeyboard(
           token,
           msg.chat.id,
           msg.message_id,
           `Удалить @${u} из списка каналов?`,
           keyboard,
         );
         return;
       }
       ```

    3. **Добавить exported helper для парсинга callback_data** (для тестов):
       ```typescript
       export function parseRemoveCallbackData(data: string): { username: string; action: "confirm" | "cancel" } | null {
         // D-11: формат `rm:<username>:confirm` / `rm:<username>:cancel`.
         const parts = data.split(":");
         if (parts.length !== 3 || parts[0] !== "rm") return null;
         const username = parts[1];
         const action = parts[2];
         if (!username || (action !== "confirm" && action !== "cancel")) return null;
         return { username, action };
       }
       ```

    4. **Добавить `handleCallbackQuery`** (exported для тестов, рядом с handleCommand):
       ```typescript
       export async function handleCallbackQuery(
         token: string,
         cb: TgCallbackQuery,
         allowlist: Set<number>
       ): Promise<void> {
         const userId = cb.from.id;
         const data = cb.data ?? "";

         // D-12: любой allowlist-юзер может нажать; не-allowlist — silent (D-07).
         if (!allowlist.has(userId)) {
           log.info(`[bot] denied: from=${userId} cmd=callback:${data}`);
           // ВАЖНО: НЕ вызываем answerCallbackQuery — silent ignore (D-07).
           return;
         }

         const parsed = parseRemoveCallbackData(data);
         if (!parsed || !cb.message) {
           // Неизвестный формат или отсутствует message (например, inline-mode) — отвечаем нейтрально.
           await tgFetch<{ ok: boolean }>(token, "answerCallbackQuery", {
             callback_query_id: cb.id,
           });
           return;
         }

         const { username, action } = parsed;
         const chatId = cb.message.chat.id;
         const messageId = cb.message.message_id;

         // ack callback (обязательно в течение 15 мин — иначе клиент покажет loading)
         await tgFetch<{ ok: boolean }>(token, "answerCallbackQuery", {
           callback_query_id: cb.id,
         });

         let newText: string;
         if (action === "cancel") {
           newText = `Отмена удаления @${username}.`;
         } else {
           // confirm: D-14 — idempotent.
           const status = await removeChannel(username);
           newText = status === "removed"
             ? `Удалён @${username}.`
             : `@${username} не найден в списке (возможно, уже удалён).`;
         }

         // D-13: сначала убираем кнопки, потом обновляем текст. Если editMessageText упадёт —
         // кнопки всё равно убраны и пользователь не нажмёт повторно.
         // ОБЯЗАТЕЛЬНО: каждый editMessage* в свой try/catch (W-3) — Telegram возвращает
         // 400 если "message is not modified" / message удалён юзером, не должно валить handler.
         try {
           await tgFetch<{ ok: boolean }>(token, "editMessageReplyMarkup", {
             chat_id: chatId,
             message_id: messageId,
             reply_markup: { inline_keyboard: [] },
           });
         } catch (err) {
           log.warn(`[bot] editMessageReplyMarkup failed: ${(err as Error).message}`);
         }
         try {
           await tgFetch<{ ok: boolean }>(token, "editMessageText", {
             chat_id: chatId,
             message_id: messageId,
             text: newText,
           });
         } catch (err) {
           log.warn(`[bot] editMessageText failed: ${(err as Error).message}`);
         }
       }
       ```

    5. **Подключить callback_query в pollOnce** — изменить существующий цикл `for (const upd of resp.result)`:
       ```typescript
       for (const upd of resp.result) {
         lastOffset = Math.max(lastOffset, upd.update_id + 1);
         try {
           if (upd.message) {
             await handleCommand(token, upd.message, allowlist);
           } else if (upd.callback_query) {
             await handleCallbackQuery(token, upd.callback_query, allowlist);
           }
         } catch (err) {
           log.error(`[bot] handler error: ${(err as Error).message}`);
         }
       }
       ```

    Все изменения — в одном файле `src/bot.ts`. Никаких новых файлов, никаких новых импортов. Использовать существующий `tgFetch`, `removeChannel`, `normalizeUsername`, `log`.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep "/remove_channel" src/bot.ts` returns at least one match.
    - `grep "parseRemoveCallbackData" src/bot.ts` returns at least 2 matches (definition + usage).
    - `grep "handleCallbackQuery" src/bot.ts` returns at least 2 matches (definition + usage in pollOnce).
    - `grep "rm:" src/bot.ts` returns at least 2 matches (confirm + cancel callback_data).
    - `grep "answerCallbackQuery" src/bot.ts` returns at least one match.
    - `grep "editMessageReplyMarkup" src/bot.ts` returns at least one match.
    - `grep "editMessageText" src/bot.ts` returns at least one match.
    - `grep "inline_keyboard: \\[\\]" src/bot.ts` returns at least one match (D-13: пустая клавиатура для уборки).
    - `grep "callback:" src/bot.ts` returns at least one match (D-08 log format для callback denied).
    - `grep "removeChannel(" src/bot.ts` returns at least 2 matches (definition + call from handleCallbackQuery).
    - `grep "callback_data" src/bot.ts` returns at least 2 matches (две кнопки confirm/cancel).
    - W-3: оба editMessage* должны быть обёрнуты в try/catch:
      - `grep -B3 "editMessageReplyMarkup" src/bot.ts | grep -c "try {" → ≥1`
      - `grep -B3 "editMessageText" src/bot.ts | grep -c "try {" → ≥1`
      - `grep "editMessageReplyMarkup failed" src/bot.ts → ≥1` (warn-сообщение из catch).
      - `grep "editMessageText failed" src/bot.ts → ≥1` (warn-сообщение из catch).
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>
    `src/bot.ts` содержит ветку `/remove_channel` (показ inline-keyboard) и `handleCallbackQuery` (allowlist-фильтр + парсинг + idempotent remove + editMessage). Подключение callback_query в pollOnce работает. Типы валидны.
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` exits 0.
- `grep "/remove_channel\\|handleCallbackQuery\\|parseRemoveCallbackData" src/bot.ts` суммарно ≥ 5 совпадений.
- `grep "rm:" src/bot.ts` ≥ 2 совпадений.
- callback_data format `rm:<username>:confirm` / `rm:<username>:cancel` — D-11.
- Allowlist-фильтр в handleCallbackQuery идентичен handleCommand (D-07/D-12).
- editMessageReplyMarkup идёт ДО editMessageText (D-13: убрать кнопки, потом текст).
- Каждый editMessage* в своём try/catch с warn-логом (W-3).
- removeChannel вызывается через mutate() (D-14, idempotent).
</verification>

<success_criteria>
1. `/remove_channel @ch1` показывает inline-keyboard с кнопками «Удалить»/«Отмена».
2. callback_data строго `rm:<username>:confirm` / `rm:<username>:cancel`.
3. Confirm: removeChannel вызван (idempotent), сообщение обновлено, кнопки убраны.
4. Cancel: channels.json не изменён, сообщение обновлено, кнопки убраны.
5. Не-allowlist callback — silent ignore (без answerCallbackQuery) + log.info.
6. Невалидная или повреждённая callback_data → answerCallbackQuery без действий (graceful).
7. editMessage* failure не валит handler — warn в лог, продолжение работы (W-3).
</success_criteria>

<output>
After completion, create `.planning/phases/02-bot-commands/02-02-SUMMARY.md`
</output>
