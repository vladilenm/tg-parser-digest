---
phase: 04
plan_number: 01
plan: 01
type: execute
wave: 1
title: "Битумный недельный отчёт — milestone v5.0 implementation"
depends_on: []
autonomous: true
requirements:
  - BITUM-CLS-01
  - BITUM-CLS-02
  - BITUM-CLS-03
  - BITUM-CLS-04
  - BITUM-PARSE-01
  - BITUM-PARSE-02
  - BITUM-PARSE-03
  - BITUM-PARSE-04
  - BITUM-PARSE-05
  - BITUM-PARSE-06
  - BITUM-REFINERY-01
  - BITUM-REFINERY-02
  - BITUM-REPORT-01
  - BITUM-REPORT-02
  - BITUM-REPORT-03
  - BITUM-REPORT-04
  - BITUM-REPORT-05
  - BITUM-REPORT-06
  - BITUM-REPORT-07
  - BITUM-REPORT-08
  - BITUM-TG-01
  - BITUM-TG-02
  - BITUM-TG-03
  - BITUM-TG-04
  - BITUM-TG-05
  - BITUM-MIGRATE-01
  - BITUM-MIGRATE-02
  - BITUM-MIGRATE-03
requirements_addressed:
  - BITUM-CLS-01
  - BITUM-CLS-02
  - BITUM-CLS-03
  - BITUM-CLS-04
  - BITUM-PARSE-01
  - BITUM-PARSE-02
  - BITUM-PARSE-03
  - BITUM-PARSE-04
  - BITUM-PARSE-05
  - BITUM-PARSE-06
  - BITUM-REFINERY-01
  - BITUM-REFINERY-02
  - BITUM-REPORT-01
  - BITUM-REPORT-02
  - BITUM-REPORT-03
  - BITUM-REPORT-04
  - BITUM-REPORT-05
  - BITUM-REPORT-06
  - BITUM-REPORT-07
  - BITUM-REPORT-08
  - BITUM-TG-01
  - BITUM-TG-02
  - BITUM-TG-03
  - BITUM-TG-04
  - BITUM-TG-05
  - BITUM-MIGRATE-01
  - BITUM-MIGRATE-02
  - BITUM-MIGRATE-03
files_modified:
  - src/bitum/types.ts (new)
  - src/bitum/signatures.ts (new)
  - src/bitum/classifier.ts (new)
  - src/bitum/learned-signatures.ts (new)
  - src/bitum/parsers/birzha-prices.ts (new)
  - src/bitum/parsers/birzha-volumes.ts (new)
  - src/bitum/parsers/fca-sellers.ts (new)
  - src/bitum/parsers/all-prices.ts (new)
  - src/bitum/parsers/bitum-price-new.ts (new)
  - src/bitum/parsers/shared.ts (new)
  - src/bitum/parsers/index.ts (new)
  - src/bitum/refineries.ts (moved from src/upload/refineries.ts)
  - src/bitum/storage.ts (moved from src/upload/storage.ts, extended for 5 types)
  - src/bitum/analyzer.ts (moved from src/upload/analyzer.ts, extended)
  - src/bitum/reporter.ts (new)
  - src/bitum/llm.ts (moved from src/upload/llm.ts, refactored to hybrid scope)
  - src/bitum/index.ts (new, public API)
  - src/bot.ts (extended with 4 bitum commands + classifier learning UX + xlsx upload response + deprecation aliases)
  - src/bot-bitum.ts (new, bitum-specific handlers extracted from bot.ts)
  - src/__tests__/bitum-classifier.test.ts (new)
  - src/__tests__/bitum-signatures.test.ts (new)
  - src/__tests__/bitum-learned-signatures.test.ts (new)
  - src/__tests__/bitum-parser-birzha-prices.test.ts (new)
  - src/__tests__/bitum-parser-birzha-volumes.test.ts (new)
  - src/__tests__/bitum-parser-fca-sellers.test.ts (new)
  - src/__tests__/bitum-parser-all-prices.test.ts (new)
  - src/__tests__/bitum-parser-bitum-price-new.test.ts (new)
  - src/__tests__/bitum-analyzer.test.ts (new)
  - src/__tests__/bitum-reporter.test.ts (new)
  - src/__tests__/bitum-reporter-trace.test.ts (new)
  - src/__tests__/bitum-llm.test.ts (new)
  - src/__tests__/bitum-bot-commands.test.ts (new)
  - src/__tests__/bitum-bot-learning.test.ts (new)
  - data/refineries.json (extended per BITUM-REFINERY-01)
  - data/bitum/.gitkeep (new, ensure directory exists)
  - .env.example (extended with BITUM_CROSS_CHECK_THRESHOLD, BITUM_VOLUMES_TOP_N)
  - src/upload/types.ts (DELETED in wave 6)
  - src/upload/detect.ts (DELETED in wave 6)
  - src/upload/parser.ts (DELETED in wave 6)
  - src/upload/refineries.ts (DELETED in wave 6)
  - src/upload/storage.ts (DELETED in wave 6)
  - src/upload/analyzer.ts (DELETED in wave 6)
  - src/upload/renderer.ts (DELETED in wave 6)
  - src/upload/llm.ts (DELETED in wave 6)
  - src/upload/chart.ts (DELETED in wave 6, already shelved per quick-260519-swc)

must_haves:
  truths:
    - "Operator может загрузить любой из 5 типов битум-xlsx с любым именем — бот опознаёт по содержимому через `classifyFile(buffer)` (не по имени файла)"
    - "При `confidence < 1` или `type='unknown'` бот шлёт inline-keyboard с 6 вариантами, на выбор операторa дописывает сигнатуру в `data/bitum/signatures-learned.json` и сохраняет файл"
    - "При `confidence === 1` бот сохраняет `<type>.xlsx` в `data/uploads/<week>/` и шлёт ОДНО сообщение с type+confidence+meta+parse-result+чек-листом недели (TG-05)"
    - "Команда `/bitum_status` показывает чек-лист 5 типов (✅/❌) за текущую неделю + lastRunAt"
    - "Команда `/bitum_preview` собирает HTML-отчёт по `docs/bitum/algoritm.md` §6 и шлёт в DM оператора (БЕЗ публикации в канал); при <5 типов — warning + partial render"
    - "Команда `/bitum_report` показывает preview в DM + inline-keyboard «📤 Опубликовать / ❌ Отмена»; публикация в `TG_CHANNEL_ID` ТОЛЬКО после нажатия «Опубликовать»"
    - "Команда `/bitum_reset` обнуляет ТОЛЬКО текущую ISO-неделю с inline-подтверждением; без backup"
    - "Reporter возвращает `{ html, trace, warnings }` — для каждого числа в HTML trace указывает file/sheet/cell (REPORT-07)"
    - "Холдинги в отчёте идут в ФИКСИРОВАННОМ порядке Роснефть → Газпромнефть → ЛУКОЙЛ → Прочие (НЕ по Σ|Δ| desc)"
    - "Cross-check: цены из `bitum_price_new`/`all_prices` сверяются с `fca_sellers`; расхождение > `BITUM_CROSS_CHECK_THRESHOLD` (default 0.01) → warning в отчёте (REPORT-08)"
    - "Папка `src/upload/` полностью удалена; импорты в `src/bot.ts` указывают на `src/bitum/*`"
    - "Команды `/summarize` → `/bitum_preview` и `/upload_status` → `/bitum_status` работают как deprecated алиасы с одноразовым deprecation-сообщением"
    - "Все 2450+ существующих тестов остаются зелёными; новые тесты на 5 парсеров, classifier, reporter (с trace), bot-handlers добавлены"
  artifacts:
    - path: "src/bitum/types.ts"
      provides: "BitumType (6 values), ClassifyResult, ParsedRow* per type, ParserResult<T>, NumberTrace, ReportResult, LearnedSignature, WeekStatusV5"
      exports: ["BitumType", "ClassifyResult", "ParsedRowBirzhaPrice", "ParsedRowBirzhaVolume", "ParsedRowFca", "ParsedRowAllPrices", "ParsedBitumPriceNewSnapshot", "ParserResult", "NumberTrace", "ReportResult", "LearnedSignature", "WeekStatusV5", "RefineryEntry"]
    - path: "src/bitum/signatures.ts"
      provides: "Built-in TS table of 6 signatures (5 known types + unknown placeholder); first 3 entries replicate detectUploadType A1 markers"
      exports: ["BUILT_IN_SIGNATURES", "Signature"]
    - path: "src/bitum/classifier.ts"
      provides: "classifyFile(buffer): Promise<ClassifyResult> with stepped confidence model (A1+A3=1.0, A1=0.7, partial=0.4, none→unknown)"
      exports: ["classifyFile"]
    - path: "src/bitum/learned-signatures.ts"
      provides: "loadLearned() / appendLearned() with atomic .tmp+rename, in-process mutex"
      exports: ["loadLearned", "appendLearned"]
    - path: "src/bitum/parsers/birzha-prices.ts"
      provides: "parseBirzhaPrices(buffer, dict): Promise<ParserResult<ParsedRowBirzhaPrice>> with Zod-validation, ×1000 multiplier, errors[]"
    - path: "src/bitum/parsers/birzha-volumes.ts"
      provides: "parseBirzhaVolumes(buffer, dict): Promise<ParserResult<ParsedRowBirzhaVolume>> with Zod, ×1000 multiplier, errors[]"
    - path: "src/bitum/parsers/fca-sellers.ts"
      provides: "parseFcaSellers(buffer, dict): Promise<ParserResult<ParsedRowFca>> with Zod, source='fca', errors[]"
    - path: "src/bitum/parsers/all-prices.ts"
      provides: "parseAllPrices(buffer, dict): Promise<ParserResult<ParsedRowAllPrices>> reads «исходник» sheet, code-side dedup"
    - path: "src/bitum/parsers/bitum-price-new.ts"
      provides: "parseBitumPriceNew(buffer): Promise<ParserResult<ParsedBitumPriceNewSnapshot>> snapshot from F/G columns"
    - path: "src/bitum/reporter.ts"
      provides: "renderBitumReport(payload, opts): ReportResult — structured HTML per algoritm.md §6, fixed holding order, partial-render, cross-check warnings, trace footer"
      exports: ["renderBitumReport", "chunkBitumHtml"]
    - path: "src/bitum/analyzer.ts"
      provides: "analyze() extended with byCompanyFixedOrder (Роснефть/Газпромнефть/ЛУКОЙЛ/Прочие+Татнефть) + crossCheck(prices, fca, threshold)"
      exports: ["analyze", "crossCheck", "byCompanyFixedOrder"]
    - path: "src/bitum/storage.ts"
      provides: "WeekStatusV5 with 5 boolean flags; saveUpload supports 5 types; listWeekV5() helper"
      exports: ["isoWeekFolder", "weekDir", "saveUpload", "listWeekV5", "findLatestWeekWithUploads", "writeLastRun", "resetWeek"]
    - path: "src/bitum/llm.ts"
      provides: "buildBitumNarrative(report, opts) hybrid scope — LLM пишет ТОЛЬКО framing-предложения per D-08, числа подставляются reporter'ом"
      exports: ["buildBitumNarrative", "encodeReportForLlm", "BITUM_NARRATIVE_SYSTEM_PROMPT"]
    - path: "data/refineries.json"
      provides: "Extended dictionary covering all canonical refineries per BITUM-REFINERY-01"
      contains: "ООО «РН-Битум», АО «РНПК», АО «АНПЗ ВНК», АО «АНХК», ООО «Газпромнефть-Битумные материалы», АО «ГАЗПРОМНЕФТЬ-МНПЗ», АО «ГАЗПРОМНЕФТЬ-ОНПЗ»"
    - path: "data/bitum/signatures-learned.json"
      provides: "Append-only file created lazily on first learning event; flat array of LearnedSignature"
  key_links:
    - from: "src/bot.ts handleDocument"
      to: "src/bitum/classifier.ts classifyFile"
      via: "direct call after downloadTgFile"
      pattern: "classifyFile\\("
    - from: "src/bot.ts callback_query handler"
      to: "src/bitum/learned-signatures.ts appendLearned"
      via: "callback_data prefix `bs:` (bitum-signature learn)"
      pattern: "appendLearned\\("
    - from: "src/bot.ts callback_query handler"
      to: "src/bitum/reporter.ts renderBitumReport + sendToChannel (TG_CHANNEL_ID)"
      via: "callback_data prefix `bp:` (bitum-publish confirm/cancel)"
      pattern: "callback_data.*bp:"
    - from: "src/bitum/reporter.ts renderBitumReport"
      to: "src/bitum/analyzer.ts byCompanyFixedOrder + crossCheck"
      via: "direct call with parsed inputs"
      pattern: "byCompanyFixedOrder\\(|crossCheck\\("
    - from: "src/bitum/llm.ts buildBitumNarrative"
      to: "OpenAI/DeepSeek with NARRATIVE-only system-prompt (D-08)"
      via: "client.chat.completions.create"
      pattern: "BITUM_NARRATIVE_SYSTEM_PROMPT"
---

# Phase 4 — Битумный недельный отчёт (milestone v5.0)

<objective>
Реализовать весь milestone v5.0 «Битумный недельный отчёт» за один план с шестью внутренними wave'ами (D-01, D-02): xlsx-классификатор по содержимому (6 типов, learning-loop), 5 идемпотентных парсеров с Zod-валидацией, расширение dictionary НПЗ, структурный HTML-отчёт по `docs/bitum/algoritm.md` §6 с cell-trace (REPORT-07) и cross-check (REPORT-08), гибридный LLM scope (D-08, numbers programmatic / framing LLM), 4 битум-команды бота (`/bitum_status`, `/bitum_preview`, `/bitum_report`, `/bitum_reset`) с inline-keyboard публикации/подтверждения, миграция `src/upload/` → `src/bitum/` с deprecated алиасами `/summarize`→`/bitum_preview` и `/upload_status`→`/bitum_status`.

Purpose: Доставить milestone v5.0 Core Value — «Заказчик присылает 3-5 xlsx, бот опознаёт каждый по содержимому, копит недельный пакет, по `/bitum_preview` собирает аналитическую сводку по структуре из docs/bitum/algoritm.md §6 и по `/bitum_report` публикует в канал Заказчика после явного подтверждения». Числа экстрактивны (для каждого числа в отчёте reporter возвращает file/sheet/cell trace), LLM используется ТОЛЬКО для 1-3 framing-предложений summary.

Output: Новый модуль `src/bitum/*` с полным набором артефактов (types/signatures/classifier/5 парсеров/analyzer/reporter/llm/learned-signatures), удалённая папка `src/upload/`, расширенный `data/refineries.json`, новый файл `data/bitum/signatures-learned.json` (создаётся лениво при первом learning event), расширенный `src/bot.ts` с 4 битум-командами + classifier learning UX + TG-05 upload response + deprecation алиасы, 14+ новых unit-тестов на парсеры/classifier/reporter/analyzer/llm/bot-handlers.

Wave-структура (D-01, D-02) — executor проходит wave'ы СТРОГО последовательно, atomic git commit на каждую wave:
1. **Foundation** — types/signatures/classifier/learned-signatures/refineries-extend
2. **Parsers** — 5 парсеров одной wave (D-04)
3. **Analyzer + Reporter** — расширение analyzer + новый reporter с trace и partial-render
4. **LLM narrative** — рефактор llm.ts под гибридный scope (D-08)
5. **Bot commands** — 4 битум-команды + TG-05 + learning UX inline-keyboard
6. **Migration** — `git mv src/upload → src/bitum` (D-05, D-06), удаление старой папки, обновление импортов в bot.ts, deprecated алиасы команд (MIGRATE-02)

Миграция последней wave (D-05): новый код пишется в `src/bitum/*` рядом со старым (waves 1-5 НЕ трогают `src/upload/*`); wave 6 делает атомарный перевод и удаление. Это гарантирует ZERO-DOWNTIME `/summarize`/`/upload_status` в процессе разработки.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-bitum-weekly-report/04-CONTEXT.md
@docs/bitum/algoritm.md

# Existing src/upload/* — base v4.0 code that wave 6 will migrate to src/bitum/*.
# Waves 1-5 ДОЛЖНЫ читать эти файлы перед добавлением нового кода (паттерны
# `cellToDate`/`cellToNumber`/`cellToString`, ExcelJS load, normalizeRefinery,
# isoWeekFolder, atomic .tmp+rename, analyze/byCompany шаблоны).
@src/upload/types.ts
@src/upload/detect.ts
@src/upload/parser.ts
@src/upload/refineries.ts
@src/upload/storage.ts
@src/upload/analyzer.ts
@src/upload/renderer.ts
@src/upload/llm.ts

# Bot integration points для wave 5 и wave 6.
@src/bot.ts
@src/deliver.ts
@src/channels-store.ts
@src/paths.ts
@src/logger.ts

# Reference data + fixtures.
@data/refineries.json
@docs/examples/

<interfaces>
<!-- Key types/functions wave executors need. Extracted from src/upload/*. -->
<!-- Use these directly — no codebase exploration needed for these contracts. -->

From src/upload/types.ts (v4.0 base — will be superseded by src/bitum/types.ts):
```typescript
export type UploadType = "birzha_prices" | "birzha_volumes" | "fca";
export interface RefineryEntry { canonical: string; company: string; aliases: string[]; }
export interface ParsedRow {
  type: UploadType;
  refineryCanonical: string;
  refineryRaw: string;
  date: Date;
  priceRub?: number;
  volumeT?: number;
  pointOfShipment?: string;
  region?: string;
}
export interface RefineryDelta {
  canonical: string;
  firstDate: Date; firstPrice: number;
  lastDate: Date; lastPrice: number;
  deltaAbs: number; deltaPct: number;
  source: "birzha" | "fca";
}
export interface CompanyGroup { company: string; deltas: RefineryDelta[]; sumDeltaAbs: number; }
export interface AnalysisResult { periodStart: Date; periodEnd: Date; weekFolder: string; runAt: Date; deltas: RefineryDelta[]; byCompany: CompanyGroup[]; volumes?: VolumeTotals; }
```

From src/upload/parser.ts (reusable cell helpers — copy/move to src/bitum/parsers/shared.ts):
```typescript
export function excelSerialToDate(serial: number): Date;   // 1900-system, Date.UTC(1899,11,30)+serial*86400000
function cellToDate(value: unknown): Date | null;          // Date | excel serial | parseable string
function cellToNumber(value: unknown): number | null;      // number | string with "," | formula {result}
function cellToString(value: unknown): string;             // string | number | richText
async function loadWorkbook(buffer: Buffer): Promise<ExcelJS.Workbook>;
```

From src/upload/refineries.ts:
```typescript
export function loadRefineries(): RefineryEntry[];           // search ${DATA_DIR}/refineries.json then ./data/refineries.json
export function normalizeRefinery(raw: string, dict: RefineryEntry[]): string;
export function getCompany(canonical: string, dict: RefineryEntry[]): string;  // fallback "независимые"
```

From src/upload/storage.ts:
```typescript
export function isoWeekFolder(d: Date): string;             // ISO 8601 "YYYY-Www"
export function weekDir(week: string): string;
export async function saveUpload(buf: Buffer, type: UploadType, week: string): Promise<string>;
export function listWeek(week: string): { hasPrices, hasVolumes, hasFca, lastRunAt };
export function findLatestWeekWithUploads(): string | null;
export function writeLastRun(week: string, runAt: Date): void;
```

From src/upload/detect.ts (3 A1-markers — first 3 entries of src/bitum/signatures.ts):
```typescript
const MARKERS = [
  { prefix: "цена битум на бирже",     type: "birzha_prices" },
  { prefix: "объем битум на бирже",    type: "birzha_volumes" },
  { prefix: "битум цены продавцов fca", type: "fca" },  // → "fca_sellers" in v5.0
];
```

From src/bot.ts (integration patterns):
```typescript
async function tgFetch<T>(token, method, body): Promise<T>;          // POST to api.telegram.org/bot<token>/<method>
async function sendHtml(token, chatId, text): Promise<void>;         // parse_mode=HTML
async function sendPlain(token, chatId, text): Promise<void>;        // plain + MAIN_KEYBOARD
async function sendReplyWithKeyboard(token, chatId, replyToMessageId, text, keyboard): Promise<void>;
export function parseRemoveCallbackData(data): {username, action} | null;  // pattern для парсинга callback_data
export async function handleCallbackQuery(token, cb, allowlist): Promise<void>;  // routes by callback_data prefix
async function downloadTgFile(token, fileId, fallbackName): Promise<{buffer, fileName}>;
```

From src/channels-store.ts (atomic write + mutex pattern для signatures-learned.json):
```typescript
function atomicWriteJson(path: string, value: unknown): void;
// writeFileSync(tmp, JSON.stringify(value, null, 2), "utf8"); renameSync(tmp, path);
// Mutex: serial Promise chain — каждый mutate ждёт предыдущий.
```

From src/deliver.ts:
```typescript
export function chunkHtml(html: string, max?: number): string[];  // ≤4000 chars, break on \n\n → \n
```
</interfaces>

<algoritm_md_summary>
<!-- Distilled from docs/bitum/algoritm.md — full text canonical, this is parser-relevant subset -->

5 xlsx types after Заказчик's manual normalization (§1-5):

**1. birzha_prices** (`birzha — цены НПЗ_rev.xlsx`)
- A1 = "Цена битум на бирже, руб./тонн"
- Row 3 = headers (B..T = refinery names with "БНД-" prefix STRIPPED)
- Row 4+ = data (col A = Date, cols B..T = priceRub × 1000 после умножения in rev-файле)
- IMPORTANT: parser должен НЕ умножать ×1000 если значения уже большие (>1000) — `_rev.xlsx` уже умножен, оригинал — в тыс.руб. Эвристика: если среднее значение в строке < 1000 → ×1000, иначе оставить. ИЛИ всегда ×1000 на raw и требовать «raw» формат (не _rev).
- РЕШЕНИЕ для PARSE-01: парсить ТОЛЬКО raw-формат (B4..T20 без множителя в файле) и ВСЕГДА умножать на 1000. Reference fixture: `docs/examples/birzha — цены НПЗ.xlsx` (raw, без _rev).

**2. birzha_volumes** (`birzha — суточная по НПЗ_rev.xlsx`)
- A1 = "Объем битум на бирже, тыс. тонн"
- Row 3 = headers (B = "Объем итого, тыс.тн." — SKIP, C..T = refinery names с "Объем, тыс. тонн: " префиксом STRIPPED)
- Row 4+ = data (col A = Date, cols C..T = volume в тыс.т)
- Парсер УМНОЖАЕТ на 1000 (тыс.т → т) — REQ-PARSE-02 явно требует это.

**3. fca_sellers** (`BITUM — таблица продавцы_rev.xlsx`)
- A1 = "Битум цены продавцов FCA, руб./тонн"
- Row 3 = headers (A = "Пункт отгрузки", B = "Регион", C..T = даты as Date cells)
- Row 4+ = data (col A = refinery, col B = region, cols C..T = priceRub)
- source="fca" в каждой строке

**4. all_prices** (`цены битум все 08.05-15.05_rev.xlsx`)
- НОВЫЙ файл для v5.0. Сложная структура (§5).
- РЕШЕНИЕ: парсить вкладку «исходник» (raw), затем code-side агрегация.
  Колонки исходника: `Пункт отгрузки | Наименование компании | Регион | Тип | Источник | Доставка | Топливо | Цена | Дата`
  Фильтр: Топливо ∈ {БНД 100/130, БНД 50/70, БНД 60/90, БНД 70/100, БНД 90/130}
  Возвращает long-table ParsedRowAllPrices: { date, pointOfShipment, companyRaw, region, fuelType, sourceTag, deliveryMode, fuel, priceRub }
  Дедупликация холдингов (algoritm §5.11-5.12) — на code-side по getCompany() из dict, НЕ парсером.
- Fallback: если вкладки «исходник» нет — берёт worksheets[0]. Если нет колонки «Топливо» → errors.push + return [].

**5. bitum_price_new** (`bitum_price — Сводная таблица_new.xlsx`)
- НОВЫЙ файл для v5.0. Snapshot на одну дату.
- A1 + первые строки → snapshot структура. Колонки F = «Цена недели», G = «Изменение».
- Reference cells:
  - bnd.price = B4 (или столбец F row 4 — зависит от layout reference; парсер ищет cell с label «БНД»)
  - bnd.deltaAbs = next cell to right
  - pbv.price = D4 / similar
  - pbv.deltaPct = next cell to right с %
- Возвращает ParsedBitumPriceNewSnapshot { date, bnd, pbv }.

**6. Структура отчёта (§6)** — порядок блоков 1:1 (reporter MUST output в этом порядке):
1. Header: `Период: DD MMM – DD MMM YYYY` (русские месяцы)
2. LLM summary 1-3 предложения (framing only, не числа)
3. Snapshot block: «на дату [last] средняя цена БНД составила [price] ₽/т (+[deltaAbs] ₽/т, +[deltaPct]% за неделю)» — из parseBitumPriceNew
4. `### Объёмы биржевых торгов` — Σ + top-N (default 7, env BITUM_VOLUMES_TOP_N)
5. `### Роснефть (Σ|Δ| = N ₽)` — Σ + детализация (биржа|FCA) + framing-предложение от LLM
6. `### Газпромнефть (Σ|Δ| = N ₽)`
7. `### ЛУКОЙЛ` (если изменений нет → «Цены остались на уровне [дата] [уровни]»)
8. `### Прочие и независимые (Σ|Δ| = N ₽)` — все НЕ Роснефть/Газпромнефть/ЛУКОЙЛ, Татнефть отдельной строкой
9. (footer) `<code>Источники: ...</code>` — компактный trace по файлу (D-09)
10. (если cross-check warning) `⚠️ Цены расходятся: ...` — REPORT-08

ВАЖНО: фиксированный порядок холдингов — НЕ по Σ|Δ| desc как существующий `analyzer.byCompany`.
</algoritm_md_summary>
</context>

<tasks>

<!-- ========================================================================== -->
<!-- WAVE 1 — FOUNDATION                                                         -->
<!-- ========================================================================== -->

<wave id="1" name="Foundation">
<objective>Базовые типы (`src/bitum/types.ts`), built-in TS-таблица сигнатур (`src/bitum/signatures.ts`), classifier с stepped confidence model (`src/bitum/classifier.ts`), learned-signatures-аккумулятор (`src/bitum/learned-signatures.ts`), расширенный словарь НПЗ (`data/refineries.json`) под REFINERY-01. ВСЕ файлы пишутся РЯДОМ с существующим `src/upload/*` (НЕ трогать). Wave commit: `feat(04): wave 1 foundation — types/signatures/classifier/refineries-extend`.</objective>

<task id="1.1" type="auto" autonomous="true" tdd="true">
  <name>Task 1.1: Создать src/bitum/types.ts со всеми битум-типами (BITUM-CLS-01, BITUM-CLS-02, BITUM-CLS-04, BITUM-PARSE-06, BITUM-REPORT-07)</name>
  <files>src/bitum/types.ts, src/__tests__/bitum-types.test.ts</files>
  <read_first>
    - src/upload/types.ts (current UploadType, RefineryEntry, ParsedRow, RefineryDelta, CompanyGroup, AnalysisResult — extend with v5.0 types)
    - .planning/phases/04-bitum-weekly-report/04-CONTEXT.md (раздел "decisions" — D-02 wave-структура, D-08 hybrid scope, D-09 trace footer)
    - docs/bitum/algoritm.md (§1-5 для понимания колонок каждого парсера, §6 для понимания структуры отчёта)
  </read_first>
  <behavior>
    - Test 1: `BitumType` union has exactly 6 string values
    - Test 2: `RefineryEntry` keeps current shape (canonical, company, aliases) for backward-compat
    - Test 3: `ClassifyResult.confidence` is `number` (TypeScript ensures ∈ [0,1] at runtime via Zod in classifier)
    - Test 4: `ParserResult<T>` always has `rows: T[]` and `errors: { rowNum, reason }[]`
  </behavior>
  <action>
Создать `src/bitum/types.ts` с следующими ТОЧНЫМИ экспортами (copy verbatim — это контракт для всех остальных файлов wave 1-5):

```typescript
// src/bitum/types.ts — все типы для битум-pipeline (milestone v5.0).
// Расширяет src/upload/types.ts с 3 → 6 типов файлов + structured snapshot для
// bitum_price_new + cell-trace для REPORT-07 + LearnedSignature для CLS-03.

/**
 * BITUM-CLS-01: 6 типов файлов битум-pipeline. Первые 3 = legacy UploadType
 * v4.0 (переименованы: "fca" → "fca_sellers"), 2 новых + "unknown" sentinel.
 */
export type BitumType =
  | "birzha_prices"
  | "birzha_volumes"
  | "fca_sellers"
  | "all_prices"
  | "bitum_price_new"
  | "unknown";

/**
 * Известные типы (для appendLearned, saveUpload — не принимают unknown).
 */
export type KnownBitumType = Exclude<BitumType, "unknown">;

/**
 * BITUM-REFINERY-01/02: словарь канонических НПЗ + холдинг.
 * Идентичен src/upload/types.ts:RefineryEntry — backward-compat.
 */
export interface RefineryEntry {
  canonical: string;
  company: string;  // "Роснефть" | "Газпромнефть" | "ЛУКОЙЛ" | "Татнефть" | "независимые"
  aliases: string[];
}

/**
 * BITUM-CLS-01/02/04: результат classifyFile(buffer). meta содержит обнаруженные
 * A1/A3/B3 + имя листа + (опционально) оригинальное имя файла из bot.ts для логов.
 * confidence ∈ [0, 1]: 1.0 = exact A1+A3 match, 0.7 = только A1 match, 0.4 = partial,
 * < 0.7 → bot.ts шлёт inline-keyboard learning UX (D-14). type="unknown" только при
 * confidence=0 (никакая built-in или learned signature не сработала).
 */
export interface ClassifyResult {
  type: BitumType;
  confidence: number;
  meta: {
    sheetName?: string;
    a1?: string;
    a3?: string;
    b3?: string;
    originalFileName?: string;
  };
}

// ============================================================================
// Per-type ParsedRow shapes (БНД- normalized, ×1000 multipliers applied).
// ============================================================================

export interface ParsedRowBirzhaPrice {
  date: Date;
  refineryCanonical: string;
  refineryRaw: string;
  priceRub: number;  // ×1000 multiplier applied (см. BITUM-PARSE-01)
}

export interface ParsedRowBirzhaVolume {
  date: Date;
  refineryCanonical: string;
  refineryRaw: string;
  volumeT: number;   // ×1000 multiplier applied (тыс.т → т, см. BITUM-PARSE-02)
}

export interface ParsedRowFca {
  date: Date;
  refineryCanonical: string;
  region: string;
  pointOfShipment: string;
  priceRub: number;
  source: "fca";
}

export interface ParsedRowAllPrices {
  date: Date;
  pointOfShipment: string;
  companyRaw: string;       // "Наименование компании" из исходника, БЕЗ нормализации
  region: string;
  fuelType: string;          // "БНД 70/100", "БНД 90/130" и т.п.
  sourceTag: string;         // "биржа" | "мониторинг" | "прайс"
  deliveryMode: string;
  fuel: string;
  priceRub: number;
}

/**
 * BITUM-PARSE-05: snapshot на дату из bitum_price_new.xlsx (одна точка).
 * bnd = БНД, pbv = ПБВ. deltaAbs в ₽/т, deltaPct в %.
 */
export interface ParsedBitumPriceNewSnapshot {
  date: Date;
  bnd: { price: number; deltaAbs: number; deltaPct: number };
  pbv: { price: number; deltaAbs: number; deltaPct: number };
}

/**
 * BITUM-PARSE-06: универсальный результат парсера. errors[] — невалидные строки
 * (с rowNum + reason), парсер НЕ падает, продолжает остальные строки.
 */
export interface ParserResult<T> {
  rows: T[];
  errors: { rowNum: number; reason: string }[];
}

// ============================================================================
// Report types (reporter.ts).
// ============================================================================

/**
 * BITUM-REPORT-07: cell-trace для каждого числа в отчёте. file = базовое имя
 * xlsx ("birzha_prices.xlsx"), sheet = "исходник" / "свод" / "Лист1", cell =
 * "B4" / "F15", semantic = human-label («Саратовский НПЗ 2026-05-08», «БНД snapshot price»).
 */
export interface NumberTrace {
  value: number;
  file: string;
  sheet: string;
  cell: string;
  semantic: string;
}

/**
 * Reporter.renderBitumReport return. html = готовый Telegram HTML (chunkBitumHtml
 * нарежет на ≤4000); trace = массив NumberTrace для footer + unit-тестов;
 * warnings = массив строк (cross-check warnings из REPORT-08, partial-render warnings из D-10).
 */
export interface ReportResult {
  html: string;
  trace: NumberTrace[];
  warnings: string[];
}

// ============================================================================
// Storage types (storage.ts).
// ============================================================================

/**
 * BITUM-TG-01: расширенный WeekStatus для 5 типов (legacy WeekStatus имел 3).
 * lastRunAt — ISO string или null. allPresent — convenience: true если все 5 ✅.
 */
export interface WeekStatusV5 {
  week: string;  // ISO "YYYY-Www"
  hasBirzhaPrices: boolean;
  hasBirzhaVolumes: boolean;
  hasFcaSellers: boolean;
  hasAllPrices: boolean;
  hasBitumPriceNew: boolean;
  lastRunAt: string | null;
  allPresent: boolean;
  presentCount: number;
}

// ============================================================================
// Classifier learning types (learned-signatures.ts).
// ============================================================================

/**
 * BITUM-CLS-03/D-14: одна запись append-only файла signatures-learned.json.
 * a1/a3/b3 — точные строки cells (НЕ regex, exact match case-insensitive),
 * sheetName опционален (некоторые файлы имеют дефолтный "Лист1"), learnedAt — ISO8601.
 * Тип НЕ может быть "unknown" — оператор должен выбрать конкретный тип.
 */
export interface LearnedSignature {
  type: KnownBitumType;
  a1: string;
  a3?: string;
  b3?: string;
  sheetName?: string;
  learnedAt: string;
}
```

Создать `src/__tests__/bitum-types.test.ts` с базовыми type-safety тестами (TypeScript-level):

```typescript
import { describe, expect, it } from "vitest";
import type { BitumType, KnownBitumType, ClassifyResult, ParserResult, LearnedSignature } from "../bitum/types.js";

describe("bitum/types", () => {
  it("BitumType union has 6 values (CLS-01)", () => {
    const values: BitumType[] = ["birzha_prices", "birzha_volumes", "fca_sellers", "all_prices", "bitum_price_new", "unknown"];
    expect(values).toHaveLength(6);
  });

  it("KnownBitumType excludes 'unknown'", () => {
    const known: KnownBitumType[] = ["birzha_prices", "birzha_volumes", "fca_sellers", "all_prices", "bitum_price_new"];
    expect(known).toHaveLength(5);
  });

  it("ClassifyResult has type+confidence+meta", () => {
    const r: ClassifyResult = { type: "birzha_prices", confidence: 1.0, meta: { a1: "..." } };
    expect(r.confidence).toBe(1.0);
  });

  it("ParserResult always has rows[] and errors[]", () => {
    const p: ParserResult<{ x: number }> = { rows: [{ x: 1 }], errors: [{ rowNum: 5, reason: "bad" }] };
    expect(p.rows).toHaveLength(1);
    expect(p.errors[0].rowNum).toBe(5);
  });

  it("LearnedSignature type field excludes 'unknown' (CLS-03)", () => {
    const s: LearnedSignature = { type: "fca_sellers", a1: "test", learnedAt: new Date().toISOString() };
    expect(s.type).not.toBe("unknown");
  });
});
```
  </action>
  <verify>
    <automated>npm test && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - File `src/bitum/types.ts` exists
    - `grep -c "export type BitumType" src/bitum/types.ts` returns 1
    - `grep -c "export type KnownBitumType" src/bitum/types.ts` returns 1
    - `grep -c "export interface ClassifyResult" src/bitum/types.ts` returns 1
    - `grep -c "export interface ParsedRowBirzhaPrice" src/bitum/types.ts` returns 1
    - `grep -c "export interface ParsedRowBirzhaVolume" src/bitum/types.ts` returns 1
    - `grep -c "export interface ParsedRowFca" src/bitum/types.ts` returns 1
    - `grep -c "export interface ParsedRowAllPrices" src/bitum/types.ts` returns 1
    - `grep -c "export interface ParsedBitumPriceNewSnapshot" src/bitum/types.ts` returns 1
    - `grep -c "export interface ParserResult" src/bitum/types.ts` returns 1
    - `grep -c "export interface NumberTrace" src/bitum/types.ts` returns 1
    - `grep -c "export interface ReportResult" src/bitum/types.ts` returns 1
    - `grep -c "export interface WeekStatusV5" src/bitum/types.ts` returns 1
    - `grep -c "export interface LearnedSignature" src/bitum/types.ts` returns 1
    - All 6 BitumType values appear: `grep -E '"birzha_prices"|"birzha_volumes"|"fca_sellers"|"all_prices"|"bitum_price_new"|"unknown"' src/bitum/types.ts | wc -l` returns ≥ 6
    - `npx tsc --noEmit` exits 0 (project-wide type-check)
    - `npx vitest run src/__tests__/bitum-types.test.ts` exits 0
    - `npm test` exits 0 (existing 2450+ tests still green)
  </acceptance_criteria>
  <done>types.ts создан с ВСЕМИ 13 exports, проходит tsc и vitest; existing tests не сломаны.</done>
</task>

<task id="1.2" type="auto" autonomous="true" tdd="true">
  <name>Task 1.2: Создать src/bitum/signatures.ts с built-in таблицей сигнатур 6 типов (BITUM-CLS-02)</name>
  <files>src/bitum/signatures.ts, src/__tests__/bitum-signatures.test.ts</files>
  <read_first>
    - src/upload/detect.ts (3 A1-маркера — это первые 3 entries built-in signatures)
    - src/bitum/types.ts (BitumType, KnownBitumType — типы для signature.type)
    - docs/bitum/algoritm.md (§1.2, §2.2, §3.9, §4 (новый file), §5.14 — A1 заголовки для каждого из 5 типов)
  </read_first>
  <behavior>
    - Test 1: BUILT_IN_SIGNATURES имеет ровно 5 entries (по одной на KnownBitumType)
    - Test 2: Каждая signature имеет уникальный `type`
    - Test 3: A1-prefixes 3 legacy типов идентичны src/upload/detect.ts (backward-compat)
    - Test 4: all_prices и bitum_price_new имеют A1-prefixes из algoritm.md §5.14 и §4
  </behavior>
  <action>
Создать `src/bitum/signatures.ts` с TS-таблицей (НЕ JSON — REQ-CLS-02 требует именно TypeScript):

```typescript
// src/bitum/signatures.ts — built-in таблица сигнатур для classifyFile (BITUM-CLS-02).
// Каждая сигнатура содержит ожидаемые тексты A1 / A3 / B3 (case-insensitive prefix-match)
// + опциональный sheetName. ВАЖНО: это НЕ regex — точное сравнение prefix через
// String.startsWith после .trim().toLowerCase(). Учиться сложным паттернам — задача
// learned-signatures.json (CLS-03), built-in охватывает только формат от Заказчика.

import type { KnownBitumType } from "./types.js";

/**
 * Одна built-in сигнатура. Все поля кроме `type` опциональны:
 *   - a1: префикс A1 первого листа (must match для confidence ≥ 0.7)
 *   - a3: префикс A3 (must match для bump confidence 0.7 → 1.0)
 *   - b3: префикс B3 (опциональный дополнительный матч)
 *   - sheetName: ожидаемое имя листа (case-insensitive substring) — опционально,
 *               для all_prices ("исходник") и bitum_price_new ("свод")
 *
 * Confidence model (см. classifier.ts):
 *   - A1 + A3 prefix-match → 1.0
 *   - Только A1 prefix-match → 0.7
 *   - Только A3 или B3 prefix-match → 0.4
 *   - Ничего → 0.0 (type="unknown")
 */
export interface Signature {
  type: KnownBitumType;
  a1?: string;   // lowercase prefix
  a3?: string;   // lowercase prefix
  b3?: string;   // lowercase prefix
  sheetName?: string;  // lowercase substring
}

/**
 * Built-in signature table — first 3 entries match src/upload/detect.ts MARKERS
 * (legacy v4.0 backward-compat); next 2 — new for v5.0.
 *
 * Источники A1-заголовков:
 *   - birzha_prices: docs/bitum/algoritm.md §2.2 «Цена битум на бирже, руб./тонн»
 *   - birzha_volumes: §1.2 «Объем битум на бирже, тыс. тонн»
 *   - fca_sellers: §3.9 «Битум цены продавцов FCA, руб./тонн»
 *   - all_prices: §5.14 «Цены на битум все, руб/тонн»
 *   - bitum_price_new: §4 файл новый, A1 может быть пустым (snapshot формат) →
 *     fallback на A3/B3 = "БНД" / "ПБВ" заголовки колонок. Для этого типа
 *     confidence model: A1 пустой → проверяем A3/B3 на "бнд"/"пбв" → 0.7.
 *
 * IMPORTANT: НЕ менять порядок entries — classifier.ts итерирует в этом порядке,
 * первый матч с максимальной confidence побеждает.
 */
export const BUILT_IN_SIGNATURES: Signature[] = [
  { type: "birzha_prices",   a1: "цена битум на бирже",      a3: "" },
  { type: "birzha_volumes",  a1: "объем битум на бирже",     a3: "" },
  { type: "fca_sellers",     a1: "битум цены продавцов fca", a3: "пункт отгрузки", b3: "регион" },
  { type: "all_prices",      a1: "цены на битум все",        sheetName: "исходник" },
  { type: "bitum_price_new", a3: "бнд",                       b3: "пбв" },
];
```

Создать `src/__tests__/bitum-signatures.test.ts`:

```typescript
import { describe, expect, it } from "vitest";
import { BUILT_IN_SIGNATURES } from "../bitum/signatures.js";

describe("bitum/signatures", () => {
  it("BUILT_IN_SIGNATURES has 5 entries (one per KnownBitumType)", () => {
    expect(BUILT_IN_SIGNATURES).toHaveLength(5);
  });

  it("each signature has unique type", () => {
    const types = BUILT_IN_SIGNATURES.map((s) => s.type);
    const unique = new Set(types);
    expect(unique.size).toBe(types.length);
  });

  it("first 3 entries match legacy src/upload/detect.ts MARKERS (backward-compat)", () => {
    expect(BUILT_IN_SIGNATURES[0]).toMatchObject({ type: "birzha_prices",  a1: "цена битум на бирже" });
    expect(BUILT_IN_SIGNATURES[1]).toMatchObject({ type: "birzha_volumes", a1: "объем битум на бирже" });
    expect(BUILT_IN_SIGNATURES[2]).toMatchObject({ type: "fca_sellers",    a1: "битум цены продавцов fca" });
  });

  it("all_prices uses sheetName='исходник' per algoritm.md §5.14-5.15", () => {
    const s = BUILT_IN_SIGNATURES.find((x) => x.type === "all_prices")!;
    expect(s.sheetName).toBe("исходник");
  });

  it("bitum_price_new falls back on A3/B3=БНД/ПБВ (A1 may be empty in snapshot format)", () => {
    const s = BUILT_IN_SIGNATURES.find((x) => x.type === "bitum_price_new")!;
    expect(s.a3).toBe("бнд");
    expect(s.b3).toBe("пбв");
  });

  it("a1/a3/b3 all lowercase (classifier compares lowercased)", () => {
    for (const s of BUILT_IN_SIGNATURES) {
      if (s.a1) expect(s.a1).toBe(s.a1.toLowerCase());
      if (s.a3) expect(s.a3).toBe(s.a3.toLowerCase());
      if (s.b3) expect(s.b3).toBe(s.b3.toLowerCase());
    }
  });
});
```
  </action>
  <verify>
    <automated>npm test && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - File `src/bitum/signatures.ts` exists
    - `grep -c "export const BUILT_IN_SIGNATURES" src/bitum/signatures.ts` returns 1
    - `grep -c "export interface Signature" src/bitum/signatures.ts` returns 1
    - Built-in table has all 5 KnownBitumType entries: `grep -E "type: \"(birzha_prices|birzha_volumes|fca_sellers|all_prices|bitum_price_new)\"" src/bitum/signatures.ts | wc -l` returns ≥ 5
    - 3 legacy A1-prefixes preserved (verify backward compat): `grep -F "цена битум на бирже" src/bitum/signatures.ts` AND `grep -F "объем битум на бирже" src/bitum/signatures.ts` AND `grep -F "битум цены продавцов fca" src/bitum/signatures.ts` all return at least 1 match
    - `npx tsc --noEmit` exits 0
    - `npx vitest run src/__tests__/bitum-signatures.test.ts` exits 0
    - `npm test` exits 0
  </acceptance_criteria>
  <done>signatures.ts экспортирует BUILT_IN_SIGNATURES + Signature interface; vitest все 6 кейсов проходят.</done>
</task>

<task id="1.3" type="auto" autonomous="true" tdd="true">
  <name>Task 1.3: Создать src/bitum/learned-signatures.ts с append-only storage + atomic .tmp+rename + in-process mutex (BITUM-CLS-03)</name>
  <files>src/bitum/learned-signatures.ts, src/__tests__/bitum-learned-signatures.test.ts</files>
  <read_first>
    - src/channels-store.ts (паттерн atomicWriteJson + mutex chain via serial Promise)
    - src/bitum/types.ts (LearnedSignature, KnownBitumType)
    - src/paths.ts (paths.dataDir resolver — файл лежит в `${dataDir}/bitum/signatures-learned.json`)
    - .planning/phases/04-bitum-weekly-report/04-CONTEXT.md (D-14 learning UX flow, D-06 claude's discretion #6 schema = flat array)
  </read_first>
  <behavior>
    - Test 1: loadLearned() возвращает [] если файла нет
    - Test 2: appendLearned() создаёт directory если нет, пишет атомарно через .tmp+rename
    - Test 3: appendLearned() concurrent calls сериализуются (mutex) — итоговый массив содержит обе записи
    - Test 4: loadLearned() парсит существующий валидный JSON
    - Test 5: loadLearned() возвращает [] если JSON malformed (graceful)
  </behavior>
  <action>
Создать `src/bitum/learned-signatures.ts`:

```typescript
// src/bitum/learned-signatures.ts — append-only storage для CLS-03 learning UX.
// Паттерн зеркалит src/channels-store.ts: atomic .tmp+rename + in-process mutex
// через serial Promise chain. Файл: ${DATA_DIR}/bitum/signatures-learned.json.

import {
  existsSync,
  mkdirSync,
  readFileSync,
  renameSync,
  writeFileSync,
} from "node:fs";
import path from "node:path";
import { paths } from "../paths.js";
import type { LearnedSignature } from "./types.js";

function learnedFilePath(): string {
  return path.join(paths.dataDir, "bitum", "signatures-learned.json");
}

/**
 * Возвращает массив LearnedSignature из файла (или [] если файла нет / malformed).
 * Не throw'ает (graceful fallback) — classifier работает и без learned-сигнатур.
 */
export function loadLearned(): LearnedSignature[] {
  const p = learnedFilePath();
  if (!existsSync(p)) return [];
  try {
    const raw = readFileSync(p, "utf8");
    const parsed = JSON.parse(raw);
    if (!Array.isArray(parsed)) return [];
    return parsed as LearnedSignature[];
  } catch {
    return [];
  }
}

/**
 * Атомарный write JSON через .tmp + rename (POSIX-атомарность).
 * Симметрично src/channels-store.ts:atomicWriteJson:57-62.
 */
function atomicWriteJson(p: string, value: unknown): void {
  const dir = path.dirname(p);
  if (!existsSync(dir)) mkdirSync(dir, { recursive: true });
  const tmp = p + ".tmp";
  writeFileSync(tmp, JSON.stringify(value, null, 2), "utf8");
  renameSync(tmp, p);
}

// In-process mutex (serial Promise chain) — гарантия что concurrent
// appendLearned() не теряют записи. Симметрично src/channels-store.ts mutex.
let writeChain: Promise<void> = Promise.resolve();

/**
 * Дописывает одну сигнатуру в файл. Atomic + mutex.
 * BITUM-CLS-03/D-14: вызывается из bot callback_query handler при выборе типа
 * оператором (после inline-keyboard learning prompt).
 */
export function appendLearned(sig: LearnedSignature): Promise<void> {
  const next = writeChain.then(() => {
    const all = loadLearned();
    all.push(sig);
    atomicWriteJson(learnedFilePath(), all);
  });
  // Глотаем ошибки в chain (чтобы один upset write не блокировал следующие),
  // но возвращаем оригинальный promise — caller увидит конкретный reject.
  writeChain = next.catch(() => undefined);
  return next;
}
```

Создать `src/__tests__/bitum-learned-signatures.test.ts`:

```typescript
import { describe, expect, it, beforeEach, afterEach } from "vitest";
import { mkdtempSync, rmSync, existsSync, readFileSync, writeFileSync, mkdirSync } from "node:fs";
import { tmpdir } from "node:os";
import path from "node:path";
import { loadLearned, appendLearned } from "../bitum/learned-signatures.js";

describe("bitum/learned-signatures", () => {
  let tmpDir: string;
  let originalDataDir: string | undefined;

  beforeEach(() => {
    tmpDir = mkdtempSync(path.join(tmpdir(), "bitum-learned-"));
    originalDataDir = process.env.DATA_DIR;
    process.env.DATA_DIR = tmpDir;
  });

  afterEach(() => {
    rmSync(tmpDir, { recursive: true, force: true });
    if (originalDataDir === undefined) delete process.env.DATA_DIR;
    else process.env.DATA_DIR = originalDataDir;
  });

  it("loadLearned returns [] when file missing", () => {
    expect(loadLearned()).toEqual([]);
  });

  it("appendLearned creates directory and writes file atomically", async () => {
    await appendLearned({
      type: "fca_sellers",
      a1: "test marker",
      learnedAt: new Date("2026-05-21").toISOString(),
    });
    const file = path.join(tmpDir, "bitum", "signatures-learned.json");
    expect(existsSync(file)).toBe(true);
    const parsed = JSON.parse(readFileSync(file, "utf8"));
    expect(parsed).toHaveLength(1);
    expect(parsed[0].type).toBe("fca_sellers");
  });

  it("appendLearned appends to existing file", async () => {
    mkdirSync(path.join(tmpDir, "bitum"), { recursive: true });
    writeFileSync(
      path.join(tmpDir, "bitum", "signatures-learned.json"),
      JSON.stringify([{ type: "birzha_prices", a1: "old", learnedAt: "2026-01-01T00:00:00Z" }]),
      "utf8"
    );
    await appendLearned({ type: "all_prices", a1: "new", learnedAt: "2026-05-21T00:00:00Z" });
    const arr = loadLearned();
    expect(arr).toHaveLength(2);
    expect(arr[1].type).toBe("all_prices");
  });

  it("concurrent appendLearned serialize via mutex — no lost writes", async () => {
    await Promise.all([
      appendLearned({ type: "birzha_prices",   a1: "p1", learnedAt: "2026-05-21T00:00:01Z" }),
      appendLearned({ type: "birzha_volumes",  a1: "v1", learnedAt: "2026-05-21T00:00:02Z" }),
      appendLearned({ type: "fca_sellers",     a1: "f1", learnedAt: "2026-05-21T00:00:03Z" }),
    ]);
    const arr = loadLearned();
    expect(arr).toHaveLength(3);
    const types = arr.map((x) => x.type).sort();
    expect(types).toEqual(["birzha_prices", "birzha_volumes", "fca_sellers"]);
  });

  it("loadLearned returns [] when file is malformed JSON", () => {
    mkdirSync(path.join(tmpDir, "bitum"), { recursive: true });
    writeFileSync(path.join(tmpDir, "bitum", "signatures-learned.json"), "not-json", "utf8");
    expect(loadLearned()).toEqual([]);
  });
});
```

Также создать `data/bitum/.gitkeep` (пустой файл) — гарантия что directory tracked в git до первого learning event.
  </action>
  <verify>
    <automated>npm test && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - File `src/bitum/learned-signatures.ts` exists
    - `grep -c "export function loadLearned" src/bitum/learned-signatures.ts` returns 1
    - `grep -c "export function appendLearned" src/bitum/learned-signatures.ts` returns 1
    - `grep -c "atomicWriteJson\|writeFileSync\|renameSync" src/bitum/learned-signatures.ts` returns ≥ 3 (atomic write pattern present)
    - `grep -c "writeChain" src/bitum/learned-signatures.ts` returns ≥ 2 (mutex chain present)
    - File `data/bitum/.gitkeep` exists (`test -f data/bitum/.gitkeep` returns 0)
    - `npx vitest run src/__tests__/bitum-learned-signatures.test.ts` exits 0 (all 5 cases pass)
    - `npm test` exits 0
  </acceptance_criteria>
  <done>learned-signatures.ts с atomic append + mutex; 5 vitest кейсов проходят; data/bitum/.gitkeep tracked.</done>
</task>

<task id="1.4" type="auto" autonomous="true" tdd="true">
  <name>Task 1.4: Создать src/bitum/classifier.ts с stepped confidence model (BITUM-CLS-01, BITUM-CLS-02, BITUM-CLS-04)</name>
  <files>src/bitum/classifier.ts, src/__tests__/bitum-classifier.test.ts</files>
  <read_first>
    - src/upload/detect.ts (existing detectUploadType — A1 prefix-match pattern для replication)
    - src/upload/parser.ts (loadWorkbook helper + cellToString — reuse pattern для чтения cells)
    - src/bitum/types.ts (BitumType, ClassifyResult, KnownBitumType)
    - src/bitum/signatures.ts (BUILT_IN_SIGNATURES + Signature interface)
    - src/bitum/learned-signatures.ts (loadLearned для merging learned + built-in)
    - .planning/phases/04-bitum-weekly-report/04-CONTEXT.md (claude's discretion #3 — stepped model: A1+A3=1.0, A1=0.7, partial=0.4, none→unknown)
  </read_first>
  <behavior>
    - Test 1: A1+A3 prefix-match для birzha_prices → { type: "birzha_prices", confidence: 1.0 }
    - Test 2: Только A1 prefix-match → confidence: 0.7
    - Test 3: Только A3 prefix-match (без A1) → confidence: 0.4
    - Test 4: Нет match'ей → { type: "unknown", confidence: 0 }
    - Test 5: Learned signature matches → return что matched type с правильным confidence
    - Test 6: BITUM-CLS-04 — classifyFile НЕ принимает fileName аргумент (только buffer), доверие имени = 0
    - Test 7: meta.a1 / meta.a3 / meta.b3 / meta.sheetName заполнены reading первого worksheet'а
    - Test 8: Workbook без worksheets → { type: "unknown", confidence: 0, meta: {} }
  </behavior>
  <action>
Создать `src/bitum/classifier.ts`:

```typescript
// src/bitum/classifier.ts — главный classifier xlsx → BitumType.
// BITUM-CLS-01: возвращает { type, confidence, meta } через buffer-only (BITUM-CLS-04).
// Confidence model (stepped, claude's discretion #3):
//   - A1 + A3 prefix-match (или a1 + b3 если b3 указан в signature) → 1.0
//   - Только A1 prefix-match → 0.7
//   - Только A3 или B3 prefix-match (без A1) → 0.4
//   - Ничего не совпало → type="unknown", confidence=0
// При confidence < 1 OR type=unknown — bot.ts шлёт inline-keyboard learning (D-14).

import ExcelJS from "exceljs";
import type { BitumType, ClassifyResult, KnownBitumType } from "./types.js";
import { BUILT_IN_SIGNATURES, type Signature } from "./signatures.js";
import { loadLearned } from "./learned-signatures.js";

/**
 * Прочитать cell как trimmed lowercase string. richText/number/empty → "".
 * Симметрично src/upload/parser.ts:cellToString но всегда lowercase.
 */
function cellLower(ws: ExcelJS.Worksheet, address: string): string {
  const v = ws.getCell(address).value;
  if (typeof v === "string") return v.trim().toLowerCase();
  if (typeof v === "number") return String(v).toLowerCase();
  if (v && typeof v === "object" && "richText" in v) {
    const rt = (v as { richText: { text: string }[] }).richText;
    return rt.map((p) => p.text).join("").trim().toLowerCase();
  }
  return "";
}

/**
 * Один матч-проход по одной сигнатуре. Возвращает confidence 0..1.
 * - 1.0 = A1 + (A3 || B3) match
 * - 0.7 = только A1 match
 * - 0.4 = только A3 или B3 (без A1) match
 * - 0 = ничего, либо несовпадение sheetName (если sheetName указан, он required)
 */
function scoreSignature(
  sig: Signature,
  a1: string,
  a3: string,
  b3: string,
  sheetName: string
): number {
  // Если sheetName в signature указан — он обязателен (substring match).
  if (sig.sheetName && !sheetName.includes(sig.sheetName)) return 0;

  const a1Match = !!(sig.a1 && a1.startsWith(sig.a1));
  const a3Match = !!(sig.a3 && a3.startsWith(sig.a3));
  const b3Match = !!(sig.b3 && b3.startsWith(sig.b3));

  // sheetName-only сигнатуры (all_prices): если sheetName совпал и хоть что-то ещё → 0.7
  // ВАЖНО: если signature имеет ТОЛЬКО sheetName (no a1/a3/b3) — sheetName match даёт 0.7
  if (sig.sheetName && !sig.a1 && !sig.a3 && !sig.b3) {
    return 0.7;
  }

  if (a1Match && (a3Match || b3Match)) return 1.0;
  if (a1Match) return 0.7;
  if (a3Match || b3Match) return 0.4;
  return 0;
}

/**
 * BITUM-CLS-01/CLS-02/CLS-04: главный classifier xlsx → ClassifyResult.
 * Доверие имени файла = 0 (CLS-04) — fileName в API НЕ принимается. Если bot.ts
 * хочет логировать оригинальное имя — передаёт его в meta.originalFileName ПОСЛЕ
 * вызова classifyFile (через spread `{ ...result, meta: { ...result.meta, originalFileName }}`).
 *
 * Алгоритм:
 *   1. Загрузить workbook через ExcelJS
 *   2. Прочитать A1, A3, B3 первого листа + sheetName
 *   3. Merged signatures: [...BUILT_IN_SIGNATURES, ...loadLearned()]
 *   4. Для каждой signature scoreSignature() → best match wins
 *   5. Если best.confidence === 0 → { type: "unknown", confidence: 0, meta }
 */
export async function classifyFile(buffer: Buffer): Promise<ClassifyResult> {
  const wb = new ExcelJS.Workbook();
  await wb.xlsx.load(buffer as unknown as ArrayBuffer);

  const ws = wb.worksheets[0];
  if (!ws) {
    return { type: "unknown", confidence: 0, meta: {} };
  }

  const a1 = cellLower(ws, "A1");
  const a3 = cellLower(ws, "A3");
  const b3 = cellLower(ws, "B3");
  const sheetName = (ws.name ?? "").trim().toLowerCase();

  // Также собираем имена ВСЕХ листов (для all_prices с "исходник" вкладкой и др.):
  const allSheetNames = wb.worksheets
    .map((s) => (s.name ?? "").trim().toLowerCase())
    .join(" ");

  const meta = {
    a1: a1 || undefined,
    a3: a3 || undefined,
    b3: b3 || undefined,
    sheetName: sheetName || undefined,
  };

  const merged: { sig: Signature; from: "built-in" | "learned" }[] = [
    ...BUILT_IN_SIGNATURES.map((sig) => ({ sig, from: "built-in" as const })),
    ...loadLearned().map((ls) => ({
      sig: { type: ls.type as KnownBitumType, a1: ls.a1?.toLowerCase(), a3: ls.a3?.toLowerCase(), b3: ls.b3?.toLowerCase(), sheetName: ls.sheetName?.toLowerCase() } as Signature,
      from: "learned" as const,
    })),
  ];

  let best: { type: BitumType; confidence: number } = { type: "unknown", confidence: 0 };
  for (const { sig } of merged) {
    const score = scoreSignature(sig, a1, a3, b3, allSheetNames);
    if (score > best.confidence) {
      best = { type: sig.type, confidence: score };
    }
  }

  return { type: best.type, confidence: best.confidence, meta };
}
```

Создать `src/__tests__/bitum-classifier.test.ts` (использовать fixtures из docs/examples/):

```typescript
import { describe, expect, it, beforeEach, afterEach } from "vitest";
import { readFileSync, mkdtempSync, rmSync, mkdirSync, writeFileSync } from "node:fs";
import { tmpdir } from "node:os";
import path from "node:path";
import ExcelJS from "exceljs";
import { classifyFile } from "../bitum/classifier.js";

const FIXTURES_DIR = path.resolve("docs/examples");

async function bufferFromSheet(
  build: (ws: ExcelJS.Worksheet) => void,
  sheetName = "Sheet1"
): Promise<Buffer> {
  const wb = new ExcelJS.Workbook();
  const ws = wb.addWorksheet(sheetName);
  build(ws);
  const ab = await wb.xlsx.writeBuffer();
  return Buffer.from(ab);
}

describe("bitum/classifier", () => {
  let tmpDir: string;
  let originalDataDir: string | undefined;

  beforeEach(() => {
    tmpDir = mkdtempSync(path.join(tmpdir(), "bitum-classifier-"));
    originalDataDir = process.env.DATA_DIR;
    process.env.DATA_DIR = tmpDir;
  });
  afterEach(() => {
    rmSync(tmpDir, { recursive: true, force: true });
    if (originalDataDir === undefined) delete process.env.DATA_DIR;
    else process.env.DATA_DIR = originalDataDir;
  });

  it("A1 prefix match только → confidence 0.7 (birzha_prices)", async () => {
    const buf = await bufferFromSheet((ws) => {
      ws.getCell("A1").value = "Цена битум на бирже, руб./тонн";
    });
    const r = await classifyFile(buf);
    expect(r.type).toBe("birzha_prices");
    expect(r.confidence).toBe(0.7);
  });

  it("A1 + B3 match → confidence 1.0 (fca_sellers с регион header)", async () => {
    const buf = await bufferFromSheet((ws) => {
      ws.getCell("A1").value = "Битум цены продавцов FCA, руб./тонн";
      ws.getCell("A3").value = "Пункт отгрузки";
      ws.getCell("B3").value = "Регион";
    });
    const r = await classifyFile(buf);
    expect(r.type).toBe("fca_sellers");
    expect(r.confidence).toBe(1.0);
  });

  it("Только A3 match (без A1) → confidence 0.4", async () => {
    const buf = await bufferFromSheet((ws) => {
      ws.getCell("A3").value = "БНД";
      ws.getCell("B3").value = "ПБВ";
    });
    const r = await classifyFile(buf);
    expect(r.type).toBe("bitum_price_new");
    // a3=бнд + b3=пбв → ОБА match → 1.0 (потому что a3 || b3 — два match'а считаются как A1+A3)
    // Wait: scoreSignature: if a1Match && (a3 || b3) → 1.0; if a1Match → 0.7; if a3 || b3 → 0.4.
    // Здесь нет a1, есть и a3 и b3 — попадает в "a3 || b3" ветку → 0.4.
    expect(r.confidence).toBe(0.4);
  });

  it("Нет matches → { type: unknown, confidence: 0 }", async () => {
    const buf = await bufferFromSheet((ws) => {
      ws.getCell("A1").value = "Какой-то посторонний xlsx";
    });
    const r = await classifyFile(buf);
    expect(r.type).toBe("unknown");
    expect(r.confidence).toBe(0);
  });

  it("BITUM-CLS-04: classifyFile принимает только buffer, fileName не передаётся", () => {
    // Type-level check: API signature is classifyFile(buffer: Buffer), no second arg.
    expect(classifyFile.length).toBe(1);
  });

  it("meta содержит a1/a3/b3/sheetName из worksheet", async () => {
    const buf = await bufferFromSheet((ws) => {
      ws.getCell("A1").value = "Цена битум на бирже, руб./тонн";
      ws.getCell("A3").value = "header3";
      ws.getCell("B3").value = "header b3";
    }, "Лист1");
    const r = await classifyFile(buf);
    expect(r.meta.a1).toContain("цена битум");
    expect(r.meta.a3).toBe("header3");
    expect(r.meta.b3).toBe("header b3");
    expect(r.meta.sheetName).toBe("лист1");
  });

  it("workbook без worksheets → { unknown, 0, {} }", async () => {
    const wb = new ExcelJS.Workbook();
    const ab = await wb.xlsx.writeBuffer();
    const r = await classifyFile(Buffer.from(ab));
    expect(r.type).toBe("unknown");
    expect(r.confidence).toBe(0);
  });

  it("all_prices: sheetName='исходник' даёт confidence 0.7 даже без A1/A3 match", async () => {
    const buf = await bufferFromSheet((ws) => {
      ws.getCell("A1").value = "Какой-то random A1";
    }, "исходник");
    const r = await classifyFile(buf);
    expect(r.type).toBe("all_prices");
    expect(r.confidence).toBe(0.7);
  });

  it("Real fixture: docs/examples/birzha — цены НПЗ_rev.xlsx → birzha_prices с confidence ≥ 0.7", async () => {
    const buf = readFileSync(path.join(FIXTURES_DIR, "birzha — цены НПЗ_rev.xlsx"));
    const r = await classifyFile(buf);
    expect(r.type).toBe("birzha_prices");
    expect(r.confidence).toBeGreaterThanOrEqual(0.7);
  });

  it("Real fixture: docs/examples/BITUM — таблица продавцы_rev.xlsx → fca_sellers", async () => {
    const buf = readFileSync(path.join(FIXTURES_DIR, "BITUM — таблица продавцы_rev.xlsx"));
    const r = await classifyFile(buf);
    expect(r.type).toBe("fca_sellers");
    expect(r.confidence).toBeGreaterThanOrEqual(0.7);
  });

  it("Learned signature merged with built-ins: matching learned → returns learned type", async () => {
    // Setup learned signature with A1='custom marker xyz' → type='fca_sellers'
    mkdirSync(path.join(tmpDir, "bitum"), { recursive: true });
    writeFileSync(
      path.join(tmpDir, "bitum", "signatures-learned.json"),
      JSON.stringify([{ type: "fca_sellers", a1: "custom marker xyz", learnedAt: "2026-05-21T00:00:00Z" }]),
      "utf8"
    );
    const buf = await bufferFromSheet((ws) => {
      ws.getCell("A1").value = "Custom marker xyz blah";
    });
    const r = await classifyFile(buf);
    expect(r.type).toBe("fca_sellers");
    expect(r.confidence).toBe(0.7);
  });
});
```
  </action>
  <verify>
    <automated>npm test && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - File `src/bitum/classifier.ts` exists
    - `grep -c "export async function classifyFile" src/bitum/classifier.ts` returns 1
    - `grep -c "BUILT_IN_SIGNATURES\|loadLearned" src/bitum/classifier.ts` returns ≥ 2 (uses both sources)
    - `grep -c "ExcelJS" src/bitum/classifier.ts` returns ≥ 1 (uses ExcelJS to load buffer)
    - API surface: `classifyFile.length === 1` (CLS-04 — only buffer param, no fileName)
    - `npx vitest run src/__tests__/bitum-classifier.test.ts` exits 0 (all ≥10 cases pass including real fixtures)
    - `npm test` exits 0
  </acceptance_criteria>
  <done>classifyFile работает для всех 5 known types + unknown; stepped confidence model работает; learned signatures merged корректно; real fixtures из docs/examples/ опознаются.</done>
</task>

<task id="1.5" type="auto" autonomous="true">
  <name>Task 1.5: Расширить data/refineries.json — добавить ВСЕ canonical-НПЗ из BITUM-REFINERY-01 (BITUM-REFINERY-01, BITUM-REFINERY-02)</name>
  <files>data/refineries.json, src/__tests__/bitum-refineries.test.ts</files>
  <read_first>
    - data/refineries.json (current v2 schema — 25 entries with company field)
    - .planning/REQUIREMENTS.md (BITUM-REFINERY-01 — полный список холдингов с НПЗ)
    - src/upload/refineries.ts (loadRefineries / normalizeRefinery / getCompany — НЕ менять, только проверить что extended data загружается)
    - docs/bitum/algoritm.md (§5.11-5.12 — холдинг-маппинг ООО «РН-Битум» = ПАО «НК Роснефть» = АО «АНПЗ ВНК» = АО «АНХК» = АО «РНПК»)
  </read_first>
  <action>
Расширить `data/refineries.json`. ТОЧНЫЕ canonical-имена + holding-маппинг из BITUM-REFINERY-01:

**Роснефть** должны быть в словаре (canonical, с aliases):
- ООО «РН-Битум» (canonical: "РН-Битум", aliases: ["ООО РН-Битум", "ООО «РН-Битум»"])
- ПАО «НК Роснефть» (canonical: "НК Роснефть", aliases: ["ПАО НК Роснефть", "Роснефть"])
- АО «АНПЗ ВНК» (уже есть как "Ачинский НПЗ ВНК" → добавить alias "АНПЗ ВНК", "АО АНПЗ ВНК")
- АО «АНХК» (уже есть как "Ангарская НХК" → добавить alias "АО АНХК", "АНХК ВНК")
- АО «РНПК» (уже есть как "Рязанская НПК" → добавить alias "АО РНПК", "РНПК")
- Саратовский НПЗ ✅ (уже есть)
- Сызранский НПЗ ✅ (уже есть)
- Ангарская НХК ✅ (уже есть, company="Роснефть")
- Рязанская НПК ✅ (уже есть, company="Роснефть")
- Ярославнефтеоргсинтез ✅ (уже есть)
- Ачинский НПЗ ВНК ✅ (уже есть)
- Ново-Уфимский НПЗ ✅ (уже есть)
- Уфимская группа НПЗ ✅ (уже есть)

**Газпромнефть** должны быть:
- ООО «Газпромнефть-Битумные материалы» (canonical: "Газпромнефть-Битумные материалы", aliases: ["ГПН-БМ", "ООО Газпромнефть-Битумные материалы"])
- АО «ГАЗПРОМНЕФТЬ-МНПЗ» (уже есть как "Газпромнефть-Московский НПЗ" → добавить aliases: "ГАЗПРОМНЕФТЬ-МНПЗ", "АО ГАЗПРОМНЕФТЬ-МНПЗ")
- АО «ГАЗПРОМНЕФТЬ-ОНПЗ» (уже есть как "Газпромнефть-Омский НПЗ" → добавить aliases: "ГАЗПРОМНЕФТЬ-ОНПЗ", "АО ГАЗПРОМНЕФТЬ-ОНПЗ")

**ЛУКОЙЛ** должны быть:
- Волгограднефтепереработка ✅ (уже есть, company="ЛУКОЙЛ")
- Нижегороднефтеоргсинтез ✅ (уже есть, company="ЛУКОЙЛ")

**Татнефть** должны быть:
- НПЗ Таиф-НК ✅ (уже есть)

**независимые** (полный список из REQ + текущие):
- МПК КРЗ ✅
- Сила Сибири ✅
- Профнефтересурс ✅
- АБЗ Хохольский ✅
- Арсенал Юг ✅
- Курский БТ ✅
- Орскнефтеоргсинтез ✅
- Мордовбитум ✅
- Новошахтинский НПЗ ✅
- Сальский битумный терминал ✅
- БТ СТРОЙСЕРВИС ✅
- Салаватнефтеоргсинтез ✅ (уже есть с alias "Газпром нефтехим Салават")

Финальный `data/refineries.json` schema (расширенный, version: 2):

```json
{
  "version": 2,
  "refineries": [
    { "canonical": "Газпромнефть-Омский НПЗ", "company": "Газпромнефть", "aliases": ["Омский НПЗ", "ОНПЗ", "ГАЗПРОМНЕФТЬ-ОНПЗ", "АО ГАЗПРОМНЕФТЬ-ОНПЗ", "АО «ГАЗПРОМНЕФТЬ-ОНПЗ»"] },
    { "canonical": "Газпромнефть-Московский НПЗ", "company": "Газпромнефть", "aliases": ["Московский НПЗ", "МНПЗ", "ГАЗПРОМНЕФТЬ-МНПЗ", "АО ГАЗПРОМНЕФТЬ-МНПЗ", "АО «ГАЗПРОМНЕФТЬ-МНПЗ»"] },
    { "canonical": "Газпромнефть-Битумные материалы", "company": "Газпромнефть", "aliases": ["ГПН-БМ", "ООО Газпромнефть-Битумные материалы", "ООО «Газпромнефть-Битумные материалы»"] },
    { "canonical": "Рязанская НПК", "company": "Роснефть", "aliases": ["РНПК", "АО РНПК", "АО «РНПК»", "Рязанский НПЗ"] },
    { "canonical": "Ярославнефтеоргсинтез", "company": "Роснефть", "aliases": ["ЯНОС", "Ярославский НПЗ"] },
    { "canonical": "Ангарская НХК", "company": "Роснефть", "aliases": ["АНХК", "АО АНХК", "АО «АНХК»"] },
    { "canonical": "Ачинский НПЗ ВНК", "company": "Роснефть", "aliases": ["Ачинский НПЗ", "АНПЗ", "АНПЗ ВНК", "АО АНПЗ ВНК", "АО «АНПЗ ВНК»"] },
    { "canonical": "Саратовский НПЗ", "company": "Роснефть", "aliases": [] },
    { "canonical": "Сызранский НПЗ", "company": "Роснефть", "aliases": [] },
    { "canonical": "Ново-Уфимский НПЗ", "company": "Роснефть", "aliases": [] },
    { "canonical": "Уфимская группа НПЗ", "company": "Роснефть", "aliases": [] },
    { "canonical": "РН-Битум", "company": "Роснефть", "aliases": ["ООО РН-Битум", "ООО «РН-Битум»"] },
    { "canonical": "НК Роснефть", "company": "Роснефть", "aliases": ["ПАО НК Роснефть", "ПАО «НК Роснефть»", "Роснефть"] },
    { "canonical": "Волгограднефтепереработка", "company": "ЛУКОЙЛ", "aliases": ["Волгоградский НПЗ"] },
    { "canonical": "Нижегороднефтеоргсинтез", "company": "ЛУКОЙЛ", "aliases": ["НОРСИ", "Нижегородский НПЗ"] },
    { "canonical": "НПЗ Таиф-НК", "company": "Татнефть", "aliases": ["Таиф-НК", "ТАИФ-НК"] },
    { "canonical": "МПК КРЗ", "company": "независимые", "aliases": [] },
    { "canonical": "Сила Сибири", "company": "независимые", "aliases": [] },
    { "canonical": "АБЗ Хохольский", "company": "независимые", "aliases": [] },
    { "canonical": "Арсенал Юг", "company": "независимые", "aliases": [] },
    { "canonical": "БТ СТРОЙСЕРВИС", "company": "независимые", "aliases": [] },
    { "canonical": "Курский БТ", "company": "независимые", "aliases": [] },
    { "canonical": "Мордовбитум", "company": "независимые", "aliases": [] },
    { "canonical": "Новошахтинский НПЗ", "company": "независимые", "aliases": ["НЗНП"] },
    { "canonical": "Орскнефтеоргсинтез", "company": "независимые", "aliases": ["Орский НПЗ", "ОНОС"] },
    { "canonical": "Профнефтересурс", "company": "независимые", "aliases": [] },
    { "canonical": "Сальский битумный терминал", "company": "независимые", "aliases": ["Сальский БТ"] },
    { "canonical": "Салаватнефтеоргсинтез", "company": "независимые", "aliases": ["Газпром нефтехим Салават", "Салават"] }
  ]
}
```

Создать `src/__tests__/bitum-refineries.test.ts` с тестами на coverage спеки:

```typescript
import { describe, expect, it } from "vitest";
import { loadRefineries, normalizeRefinery, getCompany } from "../upload/refineries.js";
// ^^ В waves 1-5 импортим из src/upload — wave 6 миграция перенесёт.

describe("data/refineries.json — BITUM-REFINERY-01 coverage", () => {
  const dict = loadRefineries();

  it("contains РН-Битум with company=Роснефть", () => {
    expect(getCompany("РН-Битум", dict)).toBe("Роснефть");
  });

  it("contains НК Роснефть with company=Роснефть", () => {
    expect(getCompany("НК Роснефть", dict)).toBe("Роснефть");
  });

  it("contains Газпромнефть-Битумные материалы with company=Газпромнефть", () => {
    expect(getCompany("Газпромнефть-Битумные материалы", dict)).toBe("Газпромнефть");
  });

  it("alias 'АО АНПЗ ВНК' normalizes to Ачинский НПЗ ВНК (Роснефть)", () => {
    const canonical = normalizeRefinery("АО АНПЗ ВНК", dict);
    expect(canonical).toBe("Ачинский НПЗ ВНК");
    expect(getCompany(canonical, dict)).toBe("Роснефть");
  });

  it("alias 'ГАЗПРОМНЕФТЬ-ОНПЗ' normalizes to Газпромнефть-Омский НПЗ", () => {
    const canonical = normalizeRefinery("ГАЗПРОМНЕФТЬ-ОНПЗ", dict);
    expect(canonical).toBe("Газпромнефть-Омский НПЗ");
    expect(getCompany(canonical, dict)).toBe("Газпромнефть");
  });

  it("Татнефть group: НПЗ Таиф-НК", () => {
    expect(getCompany("НПЗ Таиф-НК", dict)).toBe("Татнефть");
  });

  it("unknown refinery → fallback 'независимые'", () => {
    expect(getCompany("Какой-то новый НПЗ", dict)).toBe("независимые");
  });

  it("BITUM-REFINERY-02: getCompany детерминистичен", () => {
    expect(getCompany("Саратовский НПЗ", dict)).toBe("Роснефть");
    expect(getCompany("Саратовский НПЗ", dict)).toBe("Роснефть");
  });

  it("dictionary имеет ≥ 28 entries (расширен v5.0)", () => {
    expect(dict.length).toBeGreaterThanOrEqual(28);
  });

  it("каждый refinery имеет company из {Роснефть, Газпромнефть, ЛУКОЙЛ, Татнефть, независимые}", () => {
    const allowed = new Set(["Роснефть", "Газпромнефть", "ЛУКОЙЛ", "Татнефть", "независимые"]);
    for (const r of dict) {
      expect(allowed.has(r.company)).toBe(true);
    }
  });
});
```
  </action>
  <verify>
    <automated>npm test && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `node -e "console.log(JSON.parse(require('fs').readFileSync('data/refineries.json','utf8')).refineries.length)"` returns ≥ 28
    - `grep -F "РН-Битум" data/refineries.json` returns at least 1 match (Роснефть)
    - `grep -F "НК Роснефть" data/refineries.json` returns at least 1 match
    - `grep -F "Газпромнефть-Битумные материалы" data/refineries.json` returns at least 1 match
    - `grep -F "АО АНПЗ ВНК" data/refineries.json` returns at least 1 match (alias)
    - `grep -F "АО «АНХК»" data/refineries.json` returns at least 1 match (alias)
    - `grep -F "ГАЗПРОМНЕФТЬ-ОНПЗ" data/refineries.json` returns at least 1 match (alias)
    - JSON validates: `node -e "JSON.parse(require('fs').readFileSync('data/refineries.json','utf8'))"` exits 0
    - All companies из allowed set: `node -e "const d=JSON.parse(require('fs').readFileSync('data/refineries.json','utf8')).refineries; const ok=new Set(['Роснефть','Газпромнефть','ЛУКОЙЛ','Татнефть','независимые']); console.log(d.every(r => ok.has(r.company)) ? 'OK' : 'BAD')"` outputs `OK`
    - `npx vitest run src/__tests__/bitum-refineries.test.ts` exits 0
    - `npm test` exits 0 (existing upload-refineries.test.ts tests НЕ сломаны)
  </acceptance_criteria>
  <done>refineries.json расширен per BITUM-REFINERY-01, все 28+ canonical entries; getCompany возвращает правильный холдинг для каждого; existing тесты зелёные.</done>
</task>

<task id="1.6" type="checkpoint:human-verify" gate="blocking">
  <name>Task 1.6: Wave 1 checkpoint: foundation review</name>
  <files>N/A — human verification checkpoint</files>
  <action>Pause and review what was built in this wave (types/signatures/classifier/learned-signatures/refineries-extend). Operator inspects code, runs tests, gives go/no-go to proceed to next wave.</action>
  <verify>
    <automated>npm test && npx tsc --noEmit</automated>
  </verify>
  <done>Operator types resume-signal to proceed to next wave</done>
  <what-built>Wave 1 foundation: src/bitum/types.ts + signatures.ts + classifier.ts + learned-signatures.ts + data/refineries.json расширен. Atomic git commit на эту wave.</what-built>
  <how-to-verify>
    1. `npm test` — все existing + новые 5 test-файлов проходят
    2. `npx tsc --noEmit` — нет TypeScript errors
    3. `ls src/bitum/` — должны быть: types.ts, signatures.ts, classifier.ts, learned-signatures.ts (4 файла)
    4. `git status` — изменения только в `src/bitum/*`, `src/__tests__/bitum-*.test.ts`, `data/refineries.json`, `data/bitum/.gitkeep`; `src/upload/*` НЕ тронут (D-05)
    5. `git log --oneline -1` — последний commit имеет message `feat(04): wave 1 foundation — types/signatures/classifier/learned/refineries`
    6. Smoke: `node --import tsx -e "import('./src/bitum/classifier.js').then(m => m.classifyFile(require('fs').readFileSync('docs/examples/birzha — цены НПЗ_rev.xlsx'))).then(r => console.log(JSON.stringify(r,null,2)))"` — должен вернуть `{ type: "birzha_prices", confidence: 0.7 или 1.0, meta: {...} }`
  </how-to-verify>
  <resume-signal>Type "wave-1-approved" if foundation выглядит правильно, либо опишите проблемы для revision</resume-signal>
</task>

</wave>

<!-- ========================================================================== -->
<!-- WAVE 2 — PARSERS (5 parsers + shared cell helpers + Zod schemas)            -->
<!-- ========================================================================== -->

<wave id="2" name="Parsers">
<objective>Все 5 парсеров одной wave (D-04): `parseBirzhaPrices` / `parseBirzhaVolumes` / `parseFcaSellers` / `parseAllPrices` / `parseBitumPriceNew`. Каждый возвращает `ParserResult<T>` с Zod-валидацией и errors[] без падения (BITUM-PARSE-06). Cell-helpers (cellToDate / cellToNumber / cellToString) выносятся в `src/bitum/parsers/shared.ts` (копия из src/upload/parser.ts — wave 6 удалит legacy). Wave commit: `feat(04): wave 2 parsers — 5 bitum parsers with Zod validation`.</objective>

<task id="2.1" type="auto" autonomous="true" tdd="true">
  <name>Task 2.1: Создать src/bitum/parsers/shared.ts с переиспользуемыми cell-helpers (cellToDate, cellToNumber, cellToString, excelSerialToDate, loadWorkbook)</name>
  <files>src/bitum/parsers/shared.ts</files>
  <read_first>
    - src/upload/parser.ts (cellToDate, cellToNumber, cellToString, excelSerialToDate, loadWorkbook — все в одном файле, экспортировать в shared.ts)
    - src/bitum/types.ts (ParserResult — будет использоваться парсерами для return type)
  </read_first>
  <action>
Создать `src/bitum/parsers/shared.ts` — это копия cell-helpers из src/upload/parser.ts:1-87 (без parseBirzhaPrices/parseBirzhaVolumes/parseFca которые остаются в legacy):

```typescript
// src/bitum/parsers/shared.ts — переиспользуемые cell-helpers + Workbook loader.
// Источник: src/upload/parser.ts (v4.0). Все 5 битум-парсеров (wave 2) импортят отсюда.
// Wave 6 удалит src/upload/parser.ts, эти helpers останутся здесь.

import ExcelJS from "exceljs";

/**
 * Excel 1900-system serial → UTC Date.
 * Эпоха = 1899-12-30 (учёт бага Excel «1900 — leap year»).
 * Пример: 46142 → 2026-04-30 UTC.
 */
export function excelSerialToDate(serial: number): Date {
  return new Date(Date.UTC(1899, 11, 30) + serial * 86400000);
}

export function cellToDate(value: unknown): Date | null {
  if (value instanceof Date) return value;
  if (typeof value === "number" && Number.isFinite(value) && value > 0) {
    return excelSerialToDate(value);
  }
  if (typeof value === "string") {
    const trimmed = value.trim();
    if (!trimmed) return null;
    const d = new Date(trimmed);
    return Number.isNaN(d.getTime()) ? null : d;
  }
  // Formula cell with date result
  if (value && typeof value === "object" && "result" in value) {
    const inner = (value as { result: unknown }).result;
    if (inner instanceof Date) return inner;
    if (typeof inner === "number" && Number.isFinite(inner) && inner > 0) {
      return excelSerialToDate(inner);
    }
  }
  return null;
}

export function cellToNumber(value: unknown): number | null {
  if (typeof value === "number" && Number.isFinite(value)) return value;
  if (typeof value === "string") {
    const trimmed = value.trim();
    if (!trimmed) return null;
    const n = Number(trimmed.replace(",", "."));
    return Number.isFinite(n) ? n : null;
  }
  if (value && typeof value === "object" && "result" in value && typeof (value as { result: unknown }).result === "number") {
    return (value as { result: number }).result;
  }
  return null;
}

export function cellToString(value: unknown): string {
  if (typeof value === "string") return value;
  if (typeof value === "number") return String(value);
  if (value && typeof value === "object" && "richText" in value) {
    const rt = (value as { richText: { text: string }[] }).richText;
    return rt.map((p) => p.text).join("");
  }
  if (value && typeof value === "object" && "result" in value) {
    const inner = (value as { result: unknown }).result;
    if (typeof inner === "string") return inner;
    if (typeof inner === "number") return String(inner);
  }
  return value == null ? "" : String(value);
}

export async function loadWorkbook(buffer: Buffer): Promise<ExcelJS.Workbook> {
  const wb = new ExcelJS.Workbook();
  await wb.xlsx.load(buffer as unknown as ArrayBuffer);
  return wb;
}

/**
 * Найти worksheet по имени (case-insensitive substring). Возвращает первый match
 * или undefined. Используется парсером all_prices для поиска вкладки "исходник".
 */
export function findSheet(wb: ExcelJS.Workbook, nameSubstring: string): ExcelJS.Worksheet | undefined {
  const needle = nameSubstring.toLowerCase();
  return wb.worksheets.find((ws) => (ws.name ?? "").toLowerCase().includes(needle));
}
```
  </action>
  <verify>
    <automated>npm test && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - File `src/bitum/parsers/shared.ts` exists
    - `grep -c "export function excelSerialToDate" src/bitum/parsers/shared.ts` returns 1
    - `grep -c "export function cellToDate" src/bitum/parsers/shared.ts` returns 1
    - `grep -c "export function cellToNumber" src/bitum/parsers/shared.ts` returns 1
    - `grep -c "export function cellToString" src/bitum/parsers/shared.ts` returns 1
    - `grep -c "export async function loadWorkbook" src/bitum/parsers/shared.ts` returns 1
    - `grep -c "export function findSheet" src/bitum/parsers/shared.ts` returns 1
    - `npx tsc --noEmit` exits 0
    - `npm test` exits 0
  </acceptance_criteria>
  <done>shared.ts экспортирует 6 helpers; tsc clean.</done>
</task>

<task id="2.2" type="auto" autonomous="true" tdd="true">
  <name>Task 2.2: Создать src/bitum/parsers/birzha-prices.ts с Zod-валидацией + ×1000 multiplier (BITUM-PARSE-01, BITUM-PARSE-06)</name>
  <files>src/bitum/parsers/birzha-prices.ts, src/__tests__/bitum-parser-birzha-prices.test.ts</files>
  <read_first>
    - src/upload/parser.ts:107-137 (parseBirzhaPrices существующий — pattern для wide-table iteration)
    - src/bitum/parsers/shared.ts (cellToDate, cellToNumber, cellToString, loadWorkbook)
    - src/bitum/types.ts (ParsedRowBirzhaPrice, ParserResult, RefineryEntry)
    - src/upload/refineries.ts (normalizeRefinery — будет импортирован)
    - docs/bitum/algoritm.md §2 (формат файла)
    - docs/examples/birzha — цены НПЗ.xlsx и _rev.xlsx (fixture для тестов)
  </read_first>
  <behavior>
    - Test 1: Parsing raw fixture `docs/examples/birzha — цены НПЗ.xlsx` → rows.length > 0, errors.length === 0
    - Test 2: Значения цен умножены на 1000 (если в raw файле цены в тыс.руб → должны быть в руб)
    - Test 3: Header «БНД-Саратовский НПЗ» нормализуется в "Саратовский НПЗ" (replace 'БНД-' с '')
    - Test 4: refineryCanonical через normalizeRefinery (dict-aware)
    - Test 5: Невалидная строка (date=null) пропускается без падения, не добавляется в errors (не валидная строка != ошибка) — но если есть валидная дата с невалидной ценой → errors.push
    - Test 6: Идемпотентность: parseBirzhaPrices(buf) → result1; parseBirzhaPrices(buf) → result2; result1 deep-equals result2
  </behavior>
  <action>
Создать `src/bitum/parsers/birzha-prices.ts`:

```typescript
// src/bitum/parsers/birzha-prices.ts — BITUM-PARSE-01.
// Layout: A1=маркер, row 3 = headers с префиксом "БНД-" (нормализуем replace),
// row 4+ = данные. Колонка A = Date, колонки B..T = price (умножаем на 1000:
// тыс.руб/т → руб/т согласно algoritm.md §2.7).

import { z } from "zod";
import { normalizeRefinery } from "../../upload/refineries.js";
// ^^ В waves 1-5 импортим из src/upload — wave 6 миграция перенесёт.
import type { ParsedRowBirzhaPrice, ParserResult, RefineryEntry } from "../types.js";
import { cellToDate, cellToNumber, cellToString, loadWorkbook } from "./shared.js";

const ROW_SCHEMA = z.object({
  date: z.date(),
  refineryCanonical: z.string().min(1),
  refineryRaw: z.string().min(1),
  priceRub: z.number().positive().finite(),
});

/**
 * BITUM-PARSE-01: парсит birzha_prices xlsx → long-table.
 * - Заголовки в row 3 (B..end), prefix "БНД-" удаляется (algoritm.md §2.4)
 * - Цены умножаются на 1000 (algoritm.md §2.7 — тыс.руб/т → руб/т)
 * - Невалидная строка с rowNum + reason → errors[], парсер продолжает
 * - Идемпотентный: тот же buffer → тот же result
 */
export async function parseBirzhaPrices(
  buffer: Buffer,
  dict: RefineryEntry[]
): Promise<ParserResult<ParsedRowBirzhaPrice>> {
  const result: ParserResult<ParsedRowBirzhaPrice> = { rows: [], errors: [] };
  const wb = await loadWorkbook(buffer);
  const ws = wb.worksheets[0];
  if (!ws) {
    result.errors.push({ rowNum: 0, reason: "workbook has no worksheets" });
    return result;
  }

  const headerRow = ws.getRow(3);
  const headers: string[] = [];
  const colCount = ws.columnCount;
  for (let c = 2; c <= colCount; c++) {
    const raw = cellToString(headerRow.getCell(c).value).trim();
    // BITUM-PARSE-01: replace "БНД-" prefix (case-insensitive)
    headers[c] = raw.replace(/^БНД-/i, "").trim();
  }

  for (let r = 4; r <= ws.rowCount; r++) {
    const row = ws.getRow(r);
    const date = cellToDate(row.getCell(1).value);
    if (!date) continue; // пустые строки пропускаем silently
    for (let c = 2; c <= colCount; c++) {
      const refineryRaw = headers[c];
      if (!refineryRaw) continue;
      const rawPrice = cellToNumber(row.getCell(c).value);
      if (rawPrice == null) continue; // empty cell, не ошибка
      // BITUM-PARSE-01: умножаем на 1000 (тыс.руб → руб)
      const priceRub = rawPrice * 1000;
      const candidate: ParsedRowBirzhaPrice = {
        date,
        refineryRaw,
        refineryCanonical: normalizeRefinery(refineryRaw, dict),
        priceRub,
      };
      const parsed = ROW_SCHEMA.safeParse(candidate);
      if (parsed.success) {
        result.rows.push(parsed.data);
      } else {
        result.errors.push({ rowNum: r, reason: `col=${c} ${parsed.error.message.slice(0, 200)}` });
      }
    }
  }
  return result;
}
```

Создать `src/__tests__/bitum-parser-birzha-prices.test.ts`:

```typescript
import { describe, expect, it } from "vitest";
import { readFileSync } from "node:fs";
import path from "node:path";
import { parseBirzhaPrices } from "../bitum/parsers/birzha-prices.js";
import { loadRefineries } from "../upload/refineries.js";

const FIXTURES_DIR = path.resolve("docs/examples");

describe("bitum/parsers/birzha-prices", () => {
  const dict = loadRefineries();

  it("parses real fixture without errors, returns >0 rows", async () => {
    const buf = readFileSync(path.join(FIXTURES_DIR, "birzha — цены НПЗ.xlsx"));
    const r = await parseBirzhaPrices(buf, dict);
    expect(r.rows.length).toBeGreaterThan(0);
    expect(r.errors).toHaveLength(0);
  });

  it("priceRub значения корректны: >1000 руб/т после ×1000 multiplier", async () => {
    const buf = readFileSync(path.join(FIXTURES_DIR, "birzha — цены НПЗ.xlsx"));
    const r = await parseBirzhaPrices(buf, dict);
    // Реальные цены битума 20-35 тыс.руб/т → после ×1000 в раз 20000-35000
    const avg = r.rows.reduce((s, x) => s + x.priceRub, 0) / r.rows.length;
    expect(avg).toBeGreaterThan(10000);
    expect(avg).toBeLessThan(100000);
  });

  it("header 'БНД-Саратовский НПЗ' нормализуется в canonical 'Саратовский НПЗ'", async () => {
    const buf = readFileSync(path.join(FIXTURES_DIR, "birzha — цены НПЗ.xlsx"));
    const r = await parseBirzhaPrices(buf, dict);
    const sar = r.rows.filter((x) => x.refineryCanonical === "Саратовский НПЗ");
    expect(sar.length).toBeGreaterThan(0);
    expect(sar[0].refineryRaw).not.toContain("БНД-");
  });

  it("BITUM-PARSE-06 идемпотентность: parse(buf) дважды → identical results", async () => {
    const buf = readFileSync(path.join(FIXTURES_DIR, "birzha — цены НПЗ.xlsx"));
    const r1 = await parseBirzhaPrices(buf, dict);
    const r2 = await parseBirzhaPrices(buf, dict);
    expect(r1.rows.length).toBe(r2.rows.length);
    expect(r1.rows[0]).toEqual(r2.rows[0]);
  });

  it("empty workbook → errors[0], rows=[]", async () => {
    // Empty buffer = invalid xlsx → throws inside ExcelJS, parseBirzhaPrices пропускает
    // Используем минимальный валидный xlsx без worksheets
    const ExcelJS = (await import("exceljs")).default;
    const wb = new ExcelJS.Workbook();
    const ab = await wb.xlsx.writeBuffer();
    const r = await parseBirzhaPrices(Buffer.from(ab), dict);
    expect(r.rows).toHaveLength(0);
    expect(r.errors.length).toBeGreaterThan(0);
    expect(r.errors[0].reason).toContain("no worksheets");
  });
});
```
  </action>
  <verify>
    <automated>npm test && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - File `src/bitum/parsers/birzha-prices.ts` exists
    - `grep -c "export async function parseBirzhaPrices" src/bitum/parsers/birzha-prices.ts` returns 1
    - `grep -c "z\\.object\\|safeParse" src/bitum/parsers/birzha-prices.ts` returns ≥ 2 (Zod validation present)
    - `grep -c "БНД-" src/bitum/parsers/birzha-prices.ts` returns ≥ 1 (header normalization)
    - `grep -c "\\* 1000" src/bitum/parsers/birzha-prices.ts` returns ≥ 1 (×1000 multiplier)
    - `npx vitest run src/__tests__/bitum-parser-birzha-prices.test.ts` exits 0 (all 5 cases pass)
    - `npm test` exits 0
  </acceptance_criteria>
  <done>parseBirzhaPrices работает на real fixture, цены ×1000, БНД- prefix stripped, идемпотентен.</done>
</task>

<task id="2.3" type="auto" autonomous="true" tdd="true">
  <name>Task 2.3: Создать src/bitum/parsers/birzha-volumes.ts с ×1000 multiplier (BITUM-PARSE-02, BITUM-PARSE-06)</name>
  <files>src/bitum/parsers/birzha-volumes.ts, src/__tests__/bitum-parser-birzha-volumes.test.ts</files>
  <read_first>
    - src/upload/parser.ts:139-171 (parseBirzhaVolumes — pattern, header в row 3, B = «Объем итого» skip, C+ = НПЗ)
    - src/bitum/parsers/shared.ts (helpers)
    - src/bitum/types.ts (ParsedRowBirzhaVolume, ParserResult)
    - docs/bitum/algoritm.md §1 (формат: "Объем, тыс. тонн:" replace, B=skip, ×1000 в тонны)
    - docs/examples/birzha — суточная по НПЗ.xlsx (fixture)
  </read_first>
  <behavior>
    - Test 1: parse real fixture → rows.length > 0, errors.length === 0
    - Test 2: volumeT значения в тоннах (после ×1000) — typical >100т, <100000т
    - Test 3: column B "Объем итого, тыс.тн." SKIP (не попадает в rows)
    - Test 4: Header "Объем, тыс. тонн: Саратовский НПЗ" → refineryRaw="Саратовский НПЗ"
    - Test 5: Идемпотентность
  </behavior>
  <action>
Создать `src/bitum/parsers/birzha-volumes.ts`:

```typescript
// src/bitum/parsers/birzha-volumes.ts — BITUM-PARSE-02.
// Layout: A1=маркер, row 3 = headers, B=skip (Объем итого), C..T = НПЗ с
// префиксом "Объем, тыс. тонн: ". Цена в тыс.т → ×1000 (algoritm.md §1).

import { z } from "zod";
import { normalizeRefinery } from "../../upload/refineries.js";
import type { ParsedRowBirzhaVolume, ParserResult, RefineryEntry } from "../types.js";
import { cellToDate, cellToNumber, cellToString, loadWorkbook } from "./shared.js";

const ROW_SCHEMA = z.object({
  date: z.date(),
  refineryCanonical: z.string().min(1),
  refineryRaw: z.string().min(1),
  volumeT: z.number().nonnegative().finite(),
});

const HEADER_PREFIX_RE = /^Объем,\s*тыс\.?\s*тонн:\s*/i;

export async function parseBirzhaVolumes(
  buffer: Buffer,
  dict: RefineryEntry[]
): Promise<ParserResult<ParsedRowBirzhaVolume>> {
  const result: ParserResult<ParsedRowBirzhaVolume> = { rows: [], errors: [] };
  const wb = await loadWorkbook(buffer);
  const ws = wb.worksheets[0];
  if (!ws) {
    result.errors.push({ rowNum: 0, reason: "workbook has no worksheets" });
    return result;
  }

  const headerRow = ws.getRow(3);
  const headers: string[] = [];
  const colCount = ws.columnCount;
  // Column B (index 2) — «Объем итого, тыс.тн.» — SKIP, начинаем с C (index 3)
  for (let c = 3; c <= colCount; c++) {
    const raw = cellToString(headerRow.getCell(c).value).trim();
    headers[c] = raw.replace(HEADER_PREFIX_RE, "").trim();
  }

  for (let r = 4; r <= ws.rowCount; r++) {
    const row = ws.getRow(r);
    const date = cellToDate(row.getCell(1).value);
    if (!date) continue;
    for (let c = 3; c <= colCount; c++) {
      const refineryRaw = headers[c];
      if (!refineryRaw) continue;
      const rawVolume = cellToNumber(row.getCell(c).value);
      if (rawVolume == null) continue;
      // BITUM-PARSE-02: ×1000 — тыс.т → т
      const volumeT = rawVolume * 1000;
      const candidate: ParsedRowBirzhaVolume = {
        date,
        refineryRaw,
        refineryCanonical: normalizeRefinery(refineryRaw, dict),
        volumeT,
      };
      const parsed = ROW_SCHEMA.safeParse(candidate);
      if (parsed.success) {
        result.rows.push(parsed.data);
      } else {
        result.errors.push({ rowNum: r, reason: `col=${c} ${parsed.error.message.slice(0, 200)}` });
      }
    }
  }
  return result;
}
```

Создать `src/__tests__/bitum-parser-birzha-volumes.test.ts`:

```typescript
import { describe, expect, it } from "vitest";
import { readFileSync } from "node:fs";
import path from "node:path";
import { parseBirzhaVolumes } from "../bitum/parsers/birzha-volumes.js";
import { loadRefineries } from "../upload/refineries.js";

const FIXTURES_DIR = path.resolve("docs/examples");

describe("bitum/parsers/birzha-volumes", () => {
  const dict = loadRefineries();

  it("parses real fixture without errors", async () => {
    const buf = readFileSync(path.join(FIXTURES_DIR, "birzha — суточная по НПЗ.xlsx"));
    const r = await parseBirzhaVolumes(buf, dict);
    expect(r.rows.length).toBeGreaterThan(0);
    expect(r.errors).toHaveLength(0);
  });

  it("volumeT в тоннах (×1000 от тыс.т)", async () => {
    const buf = readFileSync(path.join(FIXTURES_DIR, "birzha — суточная по НПЗ.xlsx"));
    const r = await parseBirzhaVolumes(buf, dict);
    const maxV = Math.max(...r.rows.map((x) => x.volumeT));
    // Реальные объёмы: 0.1-10 тыс.т → 100-10000 т после ×1000
    expect(maxV).toBeGreaterThan(100);
  });

  it("column B 'Объем итого' пропускается (не в rows)", async () => {
    const buf = readFileSync(path.join(FIXTURES_DIR, "birzha — суточная по НПЗ.xlsx"));
    const r = await parseBirzhaVolumes(buf, dict);
    // Никакой refineryRaw не должен содержать "итого"
    const totalsRows = r.rows.filter((x) => x.refineryRaw.toLowerCase().includes("итого"));
    expect(totalsRows).toHaveLength(0);
  });

  it("BITUM-PARSE-06 идемпотентность", async () => {
    const buf = readFileSync(path.join(FIXTURES_DIR, "birzha — суточная по НПЗ.xlsx"));
    const r1 = await parseBirzhaVolumes(buf, dict);
    const r2 = await parseBirzhaVolumes(buf, dict);
    expect(r1.rows.length).toBe(r2.rows.length);
  });
});
```
  </action>
  <verify>
    <automated>npm test && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - File `src/bitum/parsers/birzha-volumes.ts` exists
    - `grep -c "export async function parseBirzhaVolumes" src/bitum/parsers/birzha-volumes.ts` returns 1
    - `grep -c "\\* 1000" src/bitum/parsers/birzha-volumes.ts` returns ≥ 1
    - `grep -c "Объем" src/bitum/parsers/birzha-volumes.ts` returns ≥ 1
    - `grep -c "z\\.object\\|safeParse" src/bitum/parsers/birzha-volumes.ts` returns ≥ 2
    - `npx vitest run src/__tests__/bitum-parser-birzha-volumes.test.ts` exits 0
    - `npm test` exits 0
  </acceptance_criteria>
  <done>parseBirzhaVolumes на real fixture, ×1000 multiplier, column B skip, идемпотентен.</done>
</task>

<task id="2.4" type="auto" autonomous="true" tdd="true">
  <name>Task 2.4: Создать src/bitum/parsers/fca-sellers.ts (BITUM-PARSE-03, BITUM-PARSE-06)</name>
  <files>src/bitum/parsers/fca-sellers.ts, src/__tests__/bitum-parser-fca-sellers.test.ts</files>
  <read_first>
    - src/upload/parser.ts:173-208 (parseFca legacy — pattern: A=Пункт отгрузки, B=Регион, C+ даты как Date cells)
    - src/bitum/parsers/shared.ts
    - src/bitum/types.ts (ParsedRowFca)
    - docs/bitum/algoritm.md §3 (формат)
    - docs/examples/BITUM — таблица продавцы.xlsx и _rev.xlsx
  </read_first>
  <behavior>
    - Test 1: parse real fixture → rows.length > 0, errors.length === 0
    - Test 2: source === "fca" для всех rows (literal type)
    - Test 3: pointOfShipment + region заполнены
    - Test 4: priceRub > 0
    - Test 5: refineryCanonical через normalizeRefinery (для known pointOfShipment)
    - Test 6: Идемпотентность
  </behavior>
  <action>
Создать `src/bitum/parsers/fca-sellers.ts`:

```typescript
// src/bitum/parsers/fca-sellers.ts — BITUM-PARSE-03.
// Layout: A1=маркер, row 3 headers (A="Пункт отгрузки", B="Регион", C..T = даты),
// row 4+ = данные. priceRub = cell value (в руб/т, без множителей).

import { z } from "zod";
import { normalizeRefinery } from "../../upload/refineries.js";
import type { ParsedRowFca, ParserResult, RefineryEntry } from "../types.js";
import { cellToDate, cellToNumber, cellToString, loadWorkbook } from "./shared.js";

const ROW_SCHEMA = z.object({
  date: z.date(),
  refineryCanonical: z.string().min(1),
  region: z.string(),
  pointOfShipment: z.string().min(1),
  priceRub: z.number().positive().finite(),
  source: z.literal("fca"),
});

export async function parseFcaSellers(
  buffer: Buffer,
  dict: RefineryEntry[]
): Promise<ParserResult<ParsedRowFca>> {
  const result: ParserResult<ParsedRowFca> = { rows: [], errors: [] };
  const wb = await loadWorkbook(buffer);
  const ws = wb.worksheets[0];
  if (!ws) {
    result.errors.push({ rowNum: 0, reason: "workbook has no worksheets" });
    return result;
  }

  const headerRow = ws.getRow(3);
  const colCount = ws.columnCount;
  const dates: (Date | null)[] = [];
  for (let c = 3; c <= colCount; c++) {
    dates[c] = cellToDate(headerRow.getCell(c).value);
  }

  for (let r = 4; r <= ws.rowCount; r++) {
    const row = ws.getRow(r);
    const point = cellToString(row.getCell(1).value).trim();
    if (!point) continue;
    const region = cellToString(row.getCell(2).value).trim();
    const canonical = normalizeRefinery(point, dict);
    for (let c = 3; c <= colCount; c++) {
      const date = dates[c];
      if (!date) continue;
      const price = cellToNumber(row.getCell(c).value);
      if (price == null) continue;
      const candidate: ParsedRowFca = {
        date,
        refineryCanonical: canonical,
        region: region,
        pointOfShipment: point,
        priceRub: price,
        source: "fca",
      };
      const parsed = ROW_SCHEMA.safeParse(candidate);
      if (parsed.success) {
        result.rows.push(parsed.data);
      } else {
        result.errors.push({ rowNum: r, reason: `col=${c} ${parsed.error.message.slice(0, 200)}` });
      }
    }
  }
  return result;
}
```

Создать `src/__tests__/bitum-parser-fca-sellers.test.ts` (5 кейсов: real fixture, source=fca, point+region, priceRub>0, идемпотентность). Pattern идентичен задаче 2.3.
  </action>
  <verify>
    <automated>npm test && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - File `src/bitum/parsers/fca-sellers.ts` exists
    - `grep -c "export async function parseFcaSellers" src/bitum/parsers/fca-sellers.ts` returns 1
    - `grep -c "source: \"fca\"" src/bitum/parsers/fca-sellers.ts` returns ≥ 1
    - `grep -c "z\\.literal\\(\"fca\"\\)" src/bitum/parsers/fca-sellers.ts` returns ≥ 1
    - `npx vitest run src/__tests__/bitum-parser-fca-sellers.test.ts` exits 0
    - `npm test` exits 0
  </acceptance_criteria>
  <done>parseFcaSellers работает, source literal "fca", идемпотентен.</done>
</task>

<task id="2.5" type="auto" autonomous="true" tdd="true">
  <name>Task 2.5: Создать src/bitum/parsers/all-prices.ts с парсингом «исходник»-вкладки (BITUM-PARSE-04, BITUM-PARSE-06)</name>
  <files>src/bitum/parsers/all-prices.ts, src/__tests__/bitum-parser-all-prices.test.ts</files>
  <read_first>
    - src/bitum/parsers/shared.ts (findSheet — для поиска вкладки "исходник")
    - src/bitum/types.ts (ParsedRowAllPrices)
    - docs/bitum/algoritm.md §5 (полная структура pivot — но мы парсим RAW «исходник», не «свод»)
    - docs/examples/цены битум все 08.05-15.05.xlsx (fixture)
  </read_first>
  <behavior>
    - Test 1: parse real fixture → rows.length > 0
    - Test 2: Фильтр по «Топливо» ∈ {БНД 100/130, БНД 50/70, БНД 60/90, БНД 70/100, БНД 90/130} применён — НЕ-БНД строки в rows нет
    - Test 3: companyRaw сохраняется как есть (НЕ нормализуется — claude's discretion #2: code-side агрегация по getCompany)
    - Test 4: priceRub > 0
    - Test 5: Идемпотентность
    - Test 6: Если вкладка "исходник" отсутствует → fallback на worksheets[0]
    - Test 7: Если нет колонки "Топливо" → errors.push({ rowNum: 0, reason: "no «Топливо» column" }), rows=[]
  </behavior>
  <action>
Создать `src/bitum/parsers/all-prices.ts`:

```typescript
// src/bitum/parsers/all-prices.ts — BITUM-PARSE-04.
// Стратегия (claude's discretion #2): парсим вкладку «исходник» (raw, до pivot),
// затем code-side агрегация по холдингам через getCompany (НЕ парсером).
// Если вкладки «исходник» нет — fallback на worksheets[0].
//
// Колонки исходника (algoritm.md §5):
//   A: Пункт отгрузки | B: Наименование компании | C: Регион | D: Тип |
//   E: Источник | F: Доставка | G: Топливо | H: Цена | I: Дата
// Фильтр: Топливо ∈ {БНД 100/130, БНД 50/70, БНД 60/90, БНД 70/100, БНД 90/130}

import { z } from "zod";
import type { ParsedRowAllPrices, ParserResult, RefineryEntry } from "../types.js";
import { cellToDate, cellToNumber, cellToString, findSheet, loadWorkbook } from "./shared.js";

const BND_FUEL_TYPES = new Set([
  "БНД 100/130", "БНД 50/70", "БНД 60/90", "БНД 70/100", "БНД 90/130",
]);

const ROW_SCHEMA = z.object({
  date: z.date(),
  pointOfShipment: z.string().min(1),
  companyRaw: z.string(),
  region: z.string(),
  fuelType: z.string().min(1),
  sourceTag: z.string(),
  deliveryMode: z.string(),
  fuel: z.string(),
  priceRub: z.number().positive().finite(),
});

interface ColumnMap {
  pointOfShipment: number;
  companyRaw: number;
  region: number;
  fuelTypeOrTip: number;
  sourceTag: number;
  deliveryMode: number;
  fuel: number;
  priceRub: number;
  date: number;
}

/**
 * Найти индексы колонок по headerRow (case-insensitive substring).
 * Возвращает null если обязательные колонки (Топливо, Цена, Дата) не найдены.
 */
function findColumns(headerRow: any, colCount: number): ColumnMap | null {
  const headers: string[] = [];
  for (let c = 1; c <= colCount; c++) {
    headers[c] = cellToString(headerRow.getCell(c).value).trim().toLowerCase();
  }
  const findCol = (substrings: string[]): number => {
    for (let c = 1; c <= colCount; c++) {
      const h = headers[c];
      if (substrings.some((s) => h.includes(s))) return c;
    }
    return -1;
  };
  const fuel = findCol(["топливо"]);
  const price = findCol(["цена"]);
  const date = findCol(["дата"]);
  if (fuel < 0 || price < 0 || date < 0) return null;
  return {
    pointOfShipment: findCol(["пункт отгрузки", "пункт"]),
    companyRaw: findCol(["наименование компании", "компании", "компания"]),
    region: findCol(["регион"]),
    fuelTypeOrTip: findCol(["тип"]),
    sourceTag: findCol(["источник"]),
    deliveryMode: findCol(["доставка"]),
    fuel: fuel,
    priceRub: price,
    date: date,
  };
}

export async function parseAllPrices(
  buffer: Buffer,
  _dict: RefineryEntry[]  // dict reserved для будущей нормализации pointOfShipment
): Promise<ParserResult<ParsedRowAllPrices>> {
  const result: ParserResult<ParsedRowAllPrices> = { rows: [], errors: [] };
  const wb = await loadWorkbook(buffer);
  // Stratergy: prefer "исходник", fallback to worksheets[0]
  let ws = findSheet(wb, "исходник");
  if (!ws) ws = wb.worksheets[0];
  if (!ws) {
    result.errors.push({ rowNum: 0, reason: "workbook has no worksheets" });
    return result;
  }

  // BITUM-PARSE-04: header может быть на row 1, 2 или 3 — определяем динамически
  let headerRow: any;
  let headerRowNum = 0;
  let cols: ColumnMap | null = null;
  for (const r of [1, 2, 3]) {
    headerRow = ws.getRow(r);
    cols = findColumns(headerRow, ws.columnCount);
    if (cols) { headerRowNum = r; break; }
  }
  if (!cols || headerRowNum === 0) {
    result.errors.push({ rowNum: 0, reason: "no «Топливо»/«Цена»/«Дата» columns in row 1-3" });
    return result;
  }

  for (let r = headerRowNum + 1; r <= ws.rowCount; r++) {
    const row = ws.getRow(r);
    const fuelType = cellToString(row.getCell(cols.fuel).value).trim();
    // BITUM-PARSE-04: фильтр по разрешённым БНД
    if (!BND_FUEL_TYPES.has(fuelType)) continue;
    const date = cellToDate(row.getCell(cols.date).value);
    if (!date) continue;
    const priceRub = cellToNumber(row.getCell(cols.priceRub).value);
    if (priceRub == null) continue;
    const candidate: ParsedRowAllPrices = {
      date,
      pointOfShipment: cellToString(row.getCell(cols.pointOfShipment).value).trim(),
      companyRaw:      cols.companyRaw > 0 ? cellToString(row.getCell(cols.companyRaw).value).trim() : "",
      region:          cols.region > 0 ? cellToString(row.getCell(cols.region).value).trim() : "",
      fuelType,
      sourceTag:       cols.sourceTag > 0 ? cellToString(row.getCell(cols.sourceTag).value).trim() : "",
      deliveryMode:    cols.deliveryMode > 0 ? cellToString(row.getCell(cols.deliveryMode).value).trim() : "",
      fuel:            cols.fuelTypeOrTip > 0 ? cellToString(row.getCell(cols.fuelTypeOrTip).value).trim() : "",
      priceRub,
    };
    const parsed = ROW_SCHEMA.safeParse(candidate);
    if (parsed.success) {
      result.rows.push(parsed.data);
    } else {
      result.errors.push({ rowNum: r, reason: parsed.error.message.slice(0, 200) });
    }
  }
  return result;
}
```

Создать `src/__tests__/bitum-parser-all-prices.test.ts` (7 кейсов покрывают behavior выше). Использовать docs/examples/цены битум все 08.05-15.05.xlsx как fixture.
  </action>
  <verify>
    <automated>npm test && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - File `src/bitum/parsers/all-prices.ts` exists
    - `grep -c "export async function parseAllPrices" src/bitum/parsers/all-prices.ts` returns 1
    - `grep -c "БНД 70/100\\|БНД 90/130" src/bitum/parsers/all-prices.ts` returns ≥ 2 (fuel filter)
    - `grep -c "исходник" src/bitum/parsers/all-prices.ts` returns ≥ 1 (sheet preference)
    - `grep -c "findColumns\\|cols\\.fuel" src/bitum/parsers/all-prices.ts` returns ≥ 2 (dynamic column detection)
    - `npx vitest run src/__tests__/bitum-parser-all-prices.test.ts` exits 0
    - `npm test` exits 0
  </acceptance_criteria>
  <done>parseAllPrices читает «исходник» (fallback worksheets[0]), фильтрует БНД-типы, errors при отсутствии Топливо/Цена/Дата, идемпотентен.</done>
</task>

<task id="2.6" type="auto" autonomous="true" tdd="true">
  <name>Task 2.6: Создать src/bitum/parsers/bitum-price-new.ts — snapshot parser (BITUM-PARSE-05, BITUM-PARSE-06)</name>
  <files>src/bitum/parsers/bitum-price-new.ts, src/__tests__/bitum-parser-bitum-price-new.test.ts</files>
  <read_first>
    - src/bitum/parsers/shared.ts
    - src/bitum/types.ts (ParsedBitumPriceNewSnapshot — snapshot не array)
    - docs/bitum/algoritm.md §4 (новый файл, колонки F=цена недели, G=изменение)
    - docs/examples/bitum_price — Сводная таблица_new.xlsx (fixture)
  </read_first>
  <behavior>
    - Test 1: parse real fixture → rows.length === 1 (один snapshot), errors.length === 0
    - Test 2: rows[0].bnd.price > 0, rows[0].pbv.price > 0
    - Test 3: rows[0].bnd.deltaAbs может быть отрицательным (-1500..+5000 ₽)
    - Test 4: rows[0].bnd.deltaPct в %, abs < 100
    - Test 5: rows[0].date valid Date
    - Test 6: Идемпотентность
  </behavior>
  <action>
Создать `src/bitum/parsers/bitum-price-new.ts`:

```typescript
// src/bitum/parsers/bitum-price-new.ts — BITUM-PARSE-05.
// Snapshot на одну дату. Layout (новый файл от Заказчика, algoritm.md §4):
//   A1+ — заголовки строк, B+ — данные. Колонка F = «Цена недели», G = «Изменение».
//   Строка с БНД (контентом A) → bnd.price = F, bnd.deltaAbs = G.
//   Строка с ПБВ → pbv.price = F, pbv.deltaAbs = G.
// deltaPct вычисляем из (deltaAbs / (price - deltaAbs)) * 100.

import { z } from "zod";
import type { ParsedBitumPriceNewSnapshot, ParserResult } from "../types.js";
import { cellToDate, cellToNumber, cellToString, loadWorkbook } from "./shared.js";

const SNAPSHOT_SCHEMA = z.object({
  date: z.date(),
  bnd: z.object({
    price: z.number().positive().finite(),
    deltaAbs: z.number().finite(),
    deltaPct: z.number().finite(),
  }),
  pbv: z.object({
    price: z.number().positive().finite(),
    deltaAbs: z.number().finite(),
    deltaPct: z.number().finite(),
  }),
});

interface Row { price: number; deltaAbs: number }

/**
 * Найти строку, в первой колонке которой есть keyword (case-insensitive).
 * Возвращает { price: cellF, deltaAbs: cellG } или null.
 */
function findRow(ws: any, keyword: string): Row | null {
  const needle = keyword.toLowerCase();
  for (let r = 1; r <= ws.rowCount; r++) {
    const label = cellToString(ws.getRow(r).getCell(1).value).trim().toLowerCase();
    if (label.includes(needle)) {
      const price = cellToNumber(ws.getRow(r).getCell(6).value);  // F
      const deltaAbs = cellToNumber(ws.getRow(r).getCell(7).value);  // G
      if (price != null && deltaAbs != null) return { price, deltaAbs };
    }
  }
  return null;
}

/**
 * Найти дату snapshot'а: смотрим row 1-3 первых 8 колонок, ищем Date cell.
 * Если нет — return new Date() (текущая дата).
 */
function findDate(ws: any): Date {
  for (let r = 1; r <= 3; r++) {
    for (let c = 1; c <= 8; c++) {
      const d = cellToDate(ws.getRow(r).getCell(c).value);
      if (d) return d;
    }
  }
  return new Date();
}

export async function parseBitumPriceNew(
  buffer: Buffer
): Promise<ParserResult<ParsedBitumPriceNewSnapshot>> {
  const result: ParserResult<ParsedBitumPriceNewSnapshot> = { rows: [], errors: [] };
  const wb = await loadWorkbook(buffer);
  const ws = wb.worksheets[0];
  if (!ws) {
    result.errors.push({ rowNum: 0, reason: "workbook has no worksheets" });
    return result;
  }

  const bndRaw = findRow(ws, "бнд");
  const pbvRaw = findRow(ws, "пбв");
  if (!bndRaw) {
    result.errors.push({ rowNum: 0, reason: "no row with 'БНД' label in column A" });
    return result;
  }
  if (!pbvRaw) {
    result.errors.push({ rowNum: 0, reason: "no row with 'ПБВ' label in column A" });
    return result;
  }

  const date = findDate(ws);
  // deltaPct = deltaAbs / oldPrice * 100; oldPrice = price - deltaAbs
  const calcPct = (price: number, deltaAbs: number): number => {
    const old = price - deltaAbs;
    if (old === 0) return 0;
    return (deltaAbs / old) * 100;
  };

  const candidate: ParsedBitumPriceNewSnapshot = {
    date,
    bnd: { price: bndRaw.price, deltaAbs: bndRaw.deltaAbs, deltaPct: calcPct(bndRaw.price, bndRaw.deltaAbs) },
    pbv: { price: pbvRaw.price, deltaAbs: pbvRaw.deltaAbs, deltaPct: calcPct(pbvRaw.price, pbvRaw.deltaAbs) },
  };
  const parsed = SNAPSHOT_SCHEMA.safeParse(candidate);
  if (parsed.success) {
    result.rows.push(parsed.data);
  } else {
    result.errors.push({ rowNum: 0, reason: parsed.error.message.slice(0, 300) });
  }
  return result;
}
```

Создать `src/__tests__/bitum-parser-bitum-price-new.test.ts` с 6 кейсами (real fixture, bnd.price>0, pbv.price>0, deltaAbs negative-allowed, deltaPct sanity, идемпотентность).
  </action>
  <verify>
    <automated>npm test && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - File `src/bitum/parsers/bitum-price-new.ts` exists
    - `grep -c "export async function parseBitumPriceNew" src/bitum/parsers/bitum-price-new.ts` returns 1
    - `grep -c "бнд\\|пбв" src/bitum/parsers/bitum-price-new.ts` returns ≥ 2 (label matching)
    - `grep -c "deltaPct\\|calcPct" src/bitum/parsers/bitum-price-new.ts` returns ≥ 2
    - `grep -c "z\\.object\\|safeParse" src/bitum/parsers/bitum-price-new.ts` returns ≥ 2
    - `npx vitest run src/__tests__/bitum-parser-bitum-price-new.test.ts` exits 0
    - `npm test` exits 0
  </acceptance_criteria>
  <done>parseBitumPriceNew возвращает snapshot {date,bnd,pbv} с deltaPct calculated; идемпотентен; errors при отсутствии labels.</done>
</task>

<task id="2.7" type="auto" autonomous="true">
  <name>Task 2.7: Создать src/bitum/parsers/index.ts — единая точка входа для всех 5 парсеров + dispatch by BitumType</name>
  <files>src/bitum/parsers/index.ts</files>
  <read_first>
    - src/bitum/types.ts (BitumType, KnownBitumType)
    - src/bitum/parsers/birzha-prices.ts (parseBirzhaPrices)
    - src/bitum/parsers/birzha-volumes.ts (parseBirzhaVolumes)
    - src/bitum/parsers/fca-sellers.ts (parseFcaSellers)
    - src/bitum/parsers/all-prices.ts (parseAllPrices)
    - src/bitum/parsers/bitum-price-new.ts (parseBitumPriceNew)
  </read_first>
  <action>
Создать `src/bitum/parsers/index.ts` — barrel + dispatcher:

```typescript
// src/bitum/parsers/index.ts — публичные re-exports + dispatcher.

export { parseBirzhaPrices } from "./birzha-prices.js";
export { parseBirzhaVolumes } from "./birzha-volumes.js";
export { parseFcaSellers } from "./fca-sellers.js";
export { parseAllPrices } from "./all-prices.js";
export { parseBitumPriceNew } from "./bitum-price-new.js";
export {
  excelSerialToDate,
  cellToDate,
  cellToNumber,
  cellToString,
  loadWorkbook,
  findSheet,
} from "./shared.js";
```

Reporter в wave 3 использует индивидуальные exports; bot.ts в wave 5 импортит `parseBy(type, buffer, dict)` если потребуется dispatcher — но в текущем дизайне reporter вызывает парсеры явно (один раз на каждый тип) и dispatcher не нужен.
  </action>
  <verify>
    <automated>npm test && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - File `src/bitum/parsers/index.ts` exists
    - `grep -c "export.*parseBirzhaPrices\\|export.*parseBirzhaVolumes\\|export.*parseFcaSellers\\|export.*parseAllPrices\\|export.*parseBitumPriceNew" src/bitum/parsers/index.ts` returns ≥ 5
    - `npx tsc --noEmit` exits 0
    - `npm test` exits 0
  </acceptance_criteria>
  <done>index.ts re-exports все 5 парсеров + shared helpers; tsc clean.</done>
</task>

<task id="2.8" type="checkpoint:human-verify" gate="blocking">
  <name>Task 2.8: Wave 2 checkpoint: parsers review</name>
  <files>N/A — human verification checkpoint</files>
  <action>Pause and review what was built in this wave (5 битум-парсеров + shared helpers + barrel). Operator inspects code, runs tests, gives go/no-go to proceed to next wave.</action>
  <verify>
    <automated>npm test && npx tsc --noEmit</automated>
  </verify>
  <done>Operator types resume-signal to proceed to next wave</done>
  <what-built>Wave 2 parsers: 5 парсеров + shared helpers + barrel. Atomic git commit.</what-built>
  <how-to-verify>
    1. `npm test` — все existing + 5 новых parser test-файлов проходят
    2. `npx tsc --noEmit` — нет TS errors
    3. `ls src/bitum/parsers/` — должны быть: shared.ts, birzha-prices.ts, birzha-volumes.ts, fca-sellers.ts, all-prices.ts, bitum-price-new.ts, index.ts (7 файлов)
    4. `git diff src/upload/` — пусто (D-05: legacy не тронут)
    5. `git log --oneline -1` — `feat(04): wave 2 parsers — 5 bitum parsers with Zod validation`
    6. Smoke: импорт всех 5 парсеров не throw'ит — `node --import tsx -e "import('./src/bitum/parsers/index.js').then(m => console.log(Object.keys(m).sort().join(',')))"` выводит ВСЕ 5 parser-функций
  </how-to-verify>
  <resume-signal>Type "wave-2-approved" или опишите проблемы</resume-signal>
</task>

</wave>

<!-- ========================================================================== -->
<!-- WAVE 3 — ANALYZER + REPORTER                                                -->
<!-- ========================================================================== -->

<wave id="3" name="Analyzer + Reporter">
<objective>Расширение `src/bitum/analyzer.ts` (новый файл, копирует логику из src/upload/analyzer.ts + добавляет byCompanyFixedOrder + crossCheck) + новый `src/bitum/reporter.ts` со структурным HTML-отчётом по algoritm.md §6 (фиксированный порядок Роснефть→Газпромнефть→ЛУКОЙЛ→Прочие, partial-render, cell-trace footer, cross-check warnings) + `src/bitum/storage.ts` (новый WeekStatusV5). Wave commit: `feat(04): wave 3 analyzer+reporter — fixed holding order + cell-trace + cross-check`.</objective>

<task id="3.1" type="auto" autonomous="true" tdd="true">
  <name>Task 3.1: Создать src/bitum/storage.ts (новая копия) с WeekStatusV5 для 5 типов + resetWeek (BITUM-TG-01, BITUM-TG-04, BITUM-MIGRATE-03)</name>
  <files>src/bitum/storage.ts, src/__tests__/bitum-storage.test.ts</files>
  <read_first>
    - src/upload/storage.ts (existing pattern — isoWeekFolder, weekDir, saveUpload, listWeek, findLatestWeekWithUploads, writeLastRun)
    - src/bitum/types.ts (WeekStatusV5, KnownBitumType)
  </read_first>
  <behavior>
    - Test 1: listWeekV5(week) возвращает 5 boolean флагов + presentCount + allPresent
    - Test 2: saveUpload поддерживает все 5 KnownBitumType
    - Test 3: resetWeek удаляет all xlsx + .last-run.json в weekDir, idempotent
    - Test 4: findLatestWeekWithUploads работает с 5 типами файлов
  </behavior>
  <action>
Создать `src/bitum/storage.ts` (копия + расширение src/upload/storage.ts):

```typescript
// src/bitum/storage.ts — file storage layer для битум-pipeline v5.0.
// Расширяет src/upload/storage.ts: 5 типов (вместо 3) + WeekStatusV5 + resetWeek (TG-04).
// Wave 6 удалит src/upload/storage.ts.

import {
  writeFileSync, renameSync, existsSync, mkdirSync, readFileSync, readdirSync,
  rmSync, unlinkSync,
} from "node:fs";
import path from "node:path";
import { paths } from "../paths.js";
import type { KnownBitumType, WeekStatusV5 } from "./types.js";

const ISO_WEEK_FOLDER_RE = /^\d{4}-W\d{2}$/;

export function isoWeekFolder(d: Date): string {
  const target = new Date(Date.UTC(d.getUTCFullYear(), d.getUTCMonth(), d.getUTCDate()));
  const dayNr = (target.getUTCDay() + 6) % 7;
  target.setUTCDate(target.getUTCDate() - dayNr + 3);
  const isoYear = target.getUTCFullYear();
  const firstThursday = new Date(Date.UTC(isoYear, 0, 4));
  const firstDayNr = (firstThursday.getUTCDay() + 6) % 7;
  firstThursday.setUTCDate(firstThursday.getUTCDate() - firstDayNr + 3);
  const week = 1 + Math.round((target.getTime() - firstThursday.getTime()) / (7 * 86400000));
  return `${isoYear}-W${String(week).padStart(2, "0")}`;
}

export function weekDir(week: string): string {
  return path.join(paths.dataDir, "uploads", week);
}

export async function saveUpload(buf: Buffer, type: KnownBitumType, week: string): Promise<string> {
  const dir = weekDir(week);
  if (!existsSync(dir)) mkdirSync(dir, { recursive: true });
  const finalPath = path.join(dir, `${type}.xlsx`);
  const tmp = finalPath + ".tmp";
  writeFileSync(tmp, buf);
  renameSync(tmp, finalPath);
  return finalPath;
}

/**
 * BITUM-TG-01: расширенный status checker для 5 типов.
 */
export function listWeekV5(week: string): WeekStatusV5 {
  const dir = weekDir(week);
  const status: WeekStatusV5 = {
    week,
    hasBirzhaPrices:   existsSync(path.join(dir, "birzha_prices.xlsx")),
    hasBirzhaVolumes:  existsSync(path.join(dir, "birzha_volumes.xlsx")),
    hasFcaSellers:     existsSync(path.join(dir, "fca_sellers.xlsx")) || existsSync(path.join(dir, "fca.xlsx")),
    // ^^ MIGRATE-03 backward-compat: старые недели могут иметь fca.xlsx (legacy name)
    hasAllPrices:      existsSync(path.join(dir, "all_prices.xlsx")),
    hasBitumPriceNew:  existsSync(path.join(dir, "bitum_price_new.xlsx")),
    lastRunAt: null,
    allPresent: false,
    presentCount: 0,
  };
  const lr = path.join(dir, ".last-run.json");
  if (existsSync(lr)) {
    try {
      const obj = JSON.parse(readFileSync(lr, "utf8")) as { runAt?: string };
      if (obj.runAt) {
        status.lastRunAt = obj.runAt;
      }
    } catch {/* malformed → null */}
  }
  status.presentCount = [
    status.hasBirzhaPrices, status.hasBirzhaVolumes, status.hasFcaSellers,
    status.hasAllPrices, status.hasBitumPriceNew,
  ].filter(Boolean).length;
  status.allPresent = status.presentCount === 5;
  return status;
}

export function findLatestWeekWithUploads(): string | null {
  const uploadsRoot = path.join(paths.dataDir, "uploads");
  if (!existsSync(uploadsRoot)) return null;
  let entries: string[];
  try { entries = readdirSync(uploadsRoot); } catch { return null; }
  const candidates: string[] = [];
  for (const name of entries) {
    if (!ISO_WEEK_FOLDER_RE.test(name)) continue;
    const dir = path.join(uploadsRoot, name);
    let files: string[];
    try { files = readdirSync(dir); } catch { continue; }
    if (files.some((f) => f.endsWith(".xlsx"))) candidates.push(name);
  }
  if (candidates.length === 0) return null;
  candidates.sort();
  return candidates[candidates.length - 1];
}

export function writeLastRun(week: string, runAt: Date): void {
  const dir = weekDir(week);
  if (!existsSync(dir)) mkdirSync(dir, { recursive: true });
  const finalPath = path.join(dir, ".last-run.json");
  const tmp = finalPath + ".tmp";
  writeFileSync(tmp, JSON.stringify({ runAt: runAt.toISOString() }), "utf8");
  renameSync(tmp, finalPath);
}

/**
 * BITUM-TG-04: удаляет xlsx + .last-run.json в weekDir.
 * Возвращает list of deleted file basenames (для ответа оператору).
 * Не удаляет саму папку (idempotent — repeated reset на empty dir безопасен).
 */
export function resetWeek(week: string): string[] {
  const dir = weekDir(week);
  if (!existsSync(dir)) return [];
  const deleted: string[] = [];
  let files: string[];
  try { files = readdirSync(dir); } catch { return []; }
  for (const f of files) {
    // Удаляем только xlsx + .last-run.json (НЕ .gitkeep, не subdirs)
    if (f.endsWith(".xlsx") || f === ".last-run.json") {
      try {
        unlinkSync(path.join(dir, f));
        deleted.push(f);
      } catch {/* ignore */}
    }
  }
  return deleted;
}
```

Создать `src/__tests__/bitum-storage.test.ts` с 6 кейсами (listWeekV5 для 0/3/5 файлов, saveUpload каждого типа, resetWeek полностью очищает, resetWeek idempotent, findLatestWeekWithUploads с 5 типами, MIGRATE-03 fca.xlsx legacy support).
  </action>
  <verify>
    <automated>npm test && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - File `src/bitum/storage.ts` exists
    - `grep -c "export function listWeekV5" src/bitum/storage.ts` returns 1
    - `grep -c "export function resetWeek" src/bitum/storage.ts` returns 1
    - `grep -c "export async function saveUpload" src/bitum/storage.ts` returns 1
    - `grep -c "hasBirzhaPrices\\|hasBirzhaVolumes\\|hasFcaSellers\\|hasAllPrices\\|hasBitumPriceNew" src/bitum/storage.ts` returns ≥ 5 (all 5 flags)
    - `grep -c "all_prices\\.xlsx\\|bitum_price_new\\.xlsx" src/bitum/storage.ts` returns ≥ 2 (new file names)
    - `grep -c "fca\\.xlsx" src/bitum/storage.ts` returns ≥ 1 (MIGRATE-03 backward-compat)
    - `npx vitest run src/__tests__/bitum-storage.test.ts` exits 0
    - `npm test` exits 0
  </acceptance_criteria>
  <done>storage.ts с listWeekV5 (5 флагов + allPresent + presentCount), resetWeek (TG-04), saveUpload для 5 типов; legacy fca.xlsx detected per MIGRATE-03.</done>
</task>

<task id="3.2" type="auto" autonomous="true" tdd="true">
  <name>Task 3.2: Создать src/bitum/analyzer.ts с byCompanyFixedOrder + crossCheck (BITUM-REPORT-03..06, BITUM-REPORT-08)</name>
  <files>src/bitum/analyzer.ts, src/__tests__/bitum-analyzer.test.ts</files>
  <read_first>
    - src/upload/analyzer.ts (existing analyze + deltasFor + groupDeltasByCompany — копировать паттерн + добавить byCompanyFixedOrder)
    - src/bitum/types.ts (ParsedRowBirzhaPrice, ParsedRowFca, ParsedBitumPriceNewSnapshot, RefineryEntry)
    - src/upload/refineries.ts (getCompany — будет импортирован)
    - .planning/phases/04-bitum-weekly-report/04-CONTEXT.md (specifics — фиксированный порядок Роснефть→Газпромнефть→ЛУКОЙЛ→Прочие; Татнефть выделяется внутри Прочих)
    - .planning/REQUIREMENTS.md (BITUM-REPORT-08 — cross-check > 1% threshold, env BITUM_CROSS_CHECK_THRESHOLD)
  </read_first>
  <behavior>
    - Test 1: byCompanyFixedOrder возвращает ровно 4 groups в порядке [Роснефть, Газпромнефть, ЛУКОЙЛ, Прочие] (НЕ по Σ|Δ| desc)
    - Test 2: Прочие содержит Татнефть + независимые (но reporter обрабатывает Татнефть отдельной строкой)
    - Test 3: Пустые группы (нет deltas) тоже возвращаются с deltas: [] + sumDeltaAbs: 0 (reporter отрисует «Цены остались на уровне»)
    - Test 4: crossCheck(prices, fca, threshold) → массив warnings { canonical, source1, source2, diff, diffPct } для расхождений > threshold
    - Test 5: BITUM_CROSS_CHECK_THRESHOLD из env (default 0.01); хардкод override через arg
  </behavior>
  <action>
Создать `src/bitum/analyzer.ts`:

```typescript
// src/bitum/analyzer.ts — Δ-аналитика + groupBy с ФИКСИРОВАННЫМ порядком холдингов
// (Роснефть→Газпромнефть→ЛУКОЙЛ→Прочие — НЕ по Σ|Δ| desc как legacy analyzer).
// + cross-check (REPORT-08) с warnings.

import { getCompany } from "../upload/refineries.js";
import type {
  ParsedRowBirzhaPrice, ParsedRowBirzhaVolume, ParsedRowFca,
  RefineryEntry,
} from "./types.js";

export interface BitumDelta {
  canonical: string;
  firstDate: Date; firstPrice: number;
  lastDate: Date;  lastPrice: number;
  deltaAbs: number; deltaPct: number;
  source: "birzha" | "fca";
}

export interface BitumCompanyGroup {
  company: "Роснефть" | "Газпромнефть" | "ЛУКОЙЛ" | "Прочие";
  deltas: BitumDelta[];
  sumDeltaAbs: number;
}

export interface BitumVolumeTotals {
  totalT: number;
  perRefinery: { canonical: string; totalT: number; company: string }[];
}

export interface CrossCheckWarning {
  canonical: string;
  source1: string;   // e.g. "bitum_price_new"
  source2: string;   // e.g. "fca_sellers"
  price1: number;
  price2: number;
  diffPct: number;
  date: Date;
}

/**
 * Считает Δ first→last per canonical для одного источника (birzha или fca).
 */
export function deltasFor<T extends { date: Date; refineryCanonical: string; priceRub: number }>(
  rows: T[],
  source: "birzha" | "fca"
): BitumDelta[] {
  const groups = new Map<string, T[]>();
  for (const r of rows) {
    const arr = groups.get(r.refineryCanonical) ?? [];
    arr.push(r);
    groups.set(r.refineryCanonical, arr);
  }
  const out: BitumDelta[] = [];
  for (const [canonical, list] of groups) {
    let first = list[0]; let last = list[0];
    for (const row of list) {
      if (row.date.getTime() < first.date.getTime()) first = row;
      if (row.date.getTime() > last.date.getTime()) last = row;
    }
    const deltaAbs = last.priceRub - first.priceRub;
    const deltaPct = first.priceRub === 0 ? 0 : (deltaAbs / first.priceRub) * 100;
    out.push({
      canonical,
      firstDate: first.date, firstPrice: first.priceRub,
      lastDate: last.date,   lastPrice: last.priceRub,
      deltaAbs, deltaPct, source,
    });
  }
  return out;
}

export function volumeTotals(rows: ParsedRowBirzhaVolume[], dict: RefineryEntry[]): BitumVolumeTotals {
  let totalT = 0;
  const perCanonical = new Map<string, number>();
  for (const r of rows) {
    totalT += r.volumeT;
    perCanonical.set(r.refineryCanonical, (perCanonical.get(r.refineryCanonical) ?? 0) + r.volumeT);
  }
  const perRefinery = [...perCanonical.entries()]
    .map(([canonical, total]) => ({ canonical, totalT: total, company: getCompany(canonical, dict) }))
    .sort((a, b) => b.totalT - a.totalT);
  return { totalT, perRefinery };
}

/**
 * BITUM-REPORT-03..06: фиксированный порядок Роснефть → Газпромнефть → ЛУКОЙЛ → Прочие.
 * Татнефть и независимые попадают в "Прочие" (Reporter обрабатывает Татнефть
 * отдельной строкой внутри «Прочие и независимые» блока — см. algoritm.md §6).
 * Пустые группы тоже возвращаются (с deltas: []) — reporter может вывести
 * «Цены остались на уровне» для REPORT-05.
 */
export function byCompanyFixedOrder(
  deltas: BitumDelta[],
  dict: RefineryEntry[]
): BitumCompanyGroup[] {
  const groups: Record<BitumCompanyGroup["company"], BitumDelta[]> = {
    "Роснефть": [], "Газпромнефть": [], "ЛУКОЙЛ": [], "Прочие": [],
  };
  for (const d of deltas) {
    const company = getCompany(d.canonical, dict);
    if (company === "Роснефть" || company === "Газпромнефть" || company === "ЛУКОЙЛ") {
      groups[company].push(d);
    } else {
      // Татнефть + независимые → Прочие
      groups["Прочие"].push(d);
    }
  }
  return (["Роснефть", "Газпромнефть", "ЛУКОЙЛ", "Прочие"] as const).map((company) => ({
    company,
    deltas: groups[company],
    sumDeltaAbs: groups[company].reduce((s, x) => s + Math.abs(x.deltaAbs), 0),
  }));
}

/**
 * BITUM-REPORT-08: сравнить цены из bitum_price_new (snapshot) и all_prices
 * с FCA-ценами; при расхождении > threshold (default 0.01 = 1%) → warning.
 * source1/source2 — человекочитаемые labels ("bitum_price_new", "fca_sellers", и т.п.).
 */
export function crossCheck(
  pricesByCanonical: { canonical: string; price: number; date: Date; source: string }[],
  fcaByCanonical:   { canonical: string; price: number; date: Date; source: string }[],
  threshold = parseFloat(process.env.BITUM_CROSS_CHECK_THRESHOLD ?? "0.01")
): CrossCheckWarning[] {
  const warnings: CrossCheckWarning[] = [];
  // По каждому canonical из prices ищем последнюю FCA-цену на ту же или ближайшую дату
  for (const p of pricesByCanonical) {
    const matches = fcaByCanonical.filter((f) => f.canonical === p.canonical);
    if (matches.length === 0) continue;
    // Берём FCA-цену с ближайшей датой
    matches.sort((a, b) => Math.abs(a.date.getTime() - p.date.getTime()) - Math.abs(b.date.getTime() - p.date.getTime()));
    const f = matches[0];
    if (f.price === 0) continue;
    const diffPct = Math.abs(p.price - f.price) / f.price;
    if (diffPct > threshold) {
      warnings.push({
        canonical: p.canonical,
        source1: p.source,
        source2: f.source,
        price1: p.price,
        price2: f.price,
        diffPct: diffPct * 100,
        date: p.date,
      });
    }
  }
  return warnings;
}
```

Создать `src/__tests__/bitum-analyzer.test.ts` (10+ кейсов: deltasFor work, volumeTotals с company, byCompanyFixedOrder возвращает 4 entries в exact order, Татнефть попадает в Прочие, empty groups сохраняются, crossCheck threshold default 0.01, env override BITUM_CROSS_CHECK_THRESHOLD, no warnings при diff < threshold, warning при diff > threshold).
  </action>
  <verify>
    <automated>npm test && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - File `src/bitum/analyzer.ts` exists
    - `grep -c "export function deltasFor\\|export function byCompanyFixedOrder\\|export function crossCheck\\|export function volumeTotals" src/bitum/analyzer.ts` returns 4
    - `grep -F "\"Роснефть\", \"Газпромнефть\", \"ЛУКОЙЛ\", \"Прочие\"" src/bitum/analyzer.ts` returns 1 (fixed order tuple literal)
    - `grep -c "BITUM_CROSS_CHECK_THRESHOLD" src/bitum/analyzer.ts` returns ≥ 1
    - `grep -c "0\\.01" src/bitum/analyzer.ts` returns ≥ 1 (default threshold)
    - `npx vitest run src/__tests__/bitum-analyzer.test.ts` exits 0
    - `npm test` exits 0
  </acceptance_criteria>
  <done>analyzer.ts с фиксированным порядком 4 групп, crossCheck с env threshold; vitest проходит.</done>
</task>

<task id="3.3" type="auto" autonomous="true" tdd="true">
  <name>Task 3.3: Создать src/bitum/reporter.ts — структурный HTML-отчёт по algoritm.md §6 с trace + partial-render (BITUM-REPORT-01..07, BITUM-REPORT-08, D-09, D-10)</name>
  <files>src/bitum/reporter.ts, src/__tests__/bitum-reporter.test.ts, src/__tests__/bitum-reporter-trace.test.ts</files>
  <read_first>
    - docs/bitum/algoritm.md §6 (ПОЛНЫЙ пример отчёта — Reporter MUST воспроизводить структуру 1:1)
    - src/bitum/types.ts (ReportResult, NumberTrace, ParsedRowBirzhaPrice, ParsedRowBirzhaVolume, ParsedRowFca, ParsedRowAllPrices, ParsedBitumPriceNewSnapshot)
    - src/bitum/analyzer.ts (deltasFor, volumeTotals, byCompanyFixedOrder, crossCheck)
    - src/upload/refineries.ts (getCompany)
    - src/upload/renderer.ts (chunkMarkdown, fmtRubSigned, fmtPctSigned, fmtNum паттерны — adapt to HTML)
    - src/deliver.ts (chunkHtml — переиспользовать для нарезки 4000 chars)
    - .planning/phases/04-bitum-weekly-report/04-CONTEXT.md (D-07 HTML tags whitelist, D-09 trace footer, D-10 partial-render)
  </read_first>
  <behavior>
    - Test 1: renderBitumReport с полным набором (5 types parsed) → html содержит все 9 секций algoritm.md §6 в правильном порядке
    - Test 2: Холдинги в HTML идут в ФИКСИРОВАННОМ порядке Роснефть→Газпромнефть→ЛУКОЙЛ→Прочие
    - Test 3: trace.length > 0; для каждого числа в html (>1000 ₽) найдётся trace entry
    - Test 4: D-10 partial-render: парсим только prices+fca (без volumes/all_prices/bitum_price_new) → warnings содержит "Доступно 2/5 типов" + блок "Объёмы биржевых торгов" заменён на «(нет данных: ожидается birzha_volumes.xlsx)»
    - Test 5: D-09 footer "Источники:" в `<code>` блоке в конце html, со сводкой ПО ФАЙЛУ (не построчно)
    - Test 6: REPORT-08 cross-check: если warnings есть → блок «⚠️ Цены расходятся: ...» в html
    - Test 7: REPORT-05 (ЛУКОЙЛ no movements) → блок «Цены остались на уровне DD MMM: A ₽, B ₽» если deltaAbs == 0 для всех в группе
    - Test 8: TG HTML whitelist: html не содержит `<h1>`, `<h2>`, `<h3>`, `<hr>`, `<br>`, `<p>`, `<div>`, `<span>` — ТОЛЬКО `<b>`, `<i>`, `<code>`, `<a href>`
    - Test 9: chunkBitumHtml(html, 4000) → каждая часть ≤ 4000 chars; разрыв по \n\n
  </behavior>
  <action>
Создать `src/bitum/reporter.ts` (300-400 строк — большой файл, основной фокус — структура algoritm.md §6):

```typescript
// src/bitum/reporter.ts — структурный HTML-отчёт по docs/bitum/algoritm.md §6.
// Контракт: renderBitumReport(payload, opts) → { html, trace, warnings }.
//   - html: Telegram HTML (parse_mode=HTML), ТОЛЬКО whitelisted tags <b><i><code><a>
//   - trace: массив NumberTrace для footer + unit-тестов (REPORT-07)
//   - warnings: массив строк (cross-check + partial-render warnings)
//
// Холдинги ВСЕГДА в фиксированном порядке: Роснефть → Газпромнефть → ЛУКОЙЛ → Прочие.
// При <5 типах — partial render с warning-блоком в начале и «(нет данных)» в местах,
// требующих недостающие файлы.

import { chunkHtml } from "../deliver.js";
import { getCompany } from "../upload/refineries.js";
import type {
  ParsedRowBirzhaPrice, ParsedRowBirzhaVolume, ParsedRowFca, ParsedRowAllPrices,
  ParsedBitumPriceNewSnapshot, ReportResult, NumberTrace, RefineryEntry,
} from "./types.js";
import {
  deltasFor, byCompanyFixedOrder, volumeTotals, crossCheck,
  type BitumDelta, type BitumCompanyGroup, type CrossCheckWarning,
} from "./analyzer.js";

const RU_MONTHS = ["января","февраля","марта","апреля","мая","июня","июля","августа","сентября","октября","ноября","декабря"];

export interface ReporterPayload {
  prices?:        ParsedRowBirzhaPrice[];          // birzha_prices.xlsx
  volumes?:       ParsedRowBirzhaVolume[];         // birzha_volumes.xlsx
  fca?:           ParsedRowFca[];                  // fca_sellers.xlsx
  allPrices?:     ParsedRowAllPrices[];            // all_prices.xlsx
  bitumSnapshot?: ParsedBitumPriceNewSnapshot;     // bitum_price_new.xlsx
  // Source file basenames для trace (file column)
  files: {
    birzhaPricesFile?:   string;
    birzhaVolumesFile?:  string;
    fcaSellersFile?:     string;
    allPricesFile?:      string;
    bitumPriceNewFile?:  string;
  };
}

export interface ReporterOptions {
  dict: RefineryEntry[];
  framingSentences?: {
    topSummary?: string;
    rosneft?: string;
    gazpromneft?: string;
    lukoil?: string;
    others?: string;
  };
  topN?: number;  // default 7 или env BITUM_VOLUMES_TOP_N
  crossCheckThreshold?: number;  // default env BITUM_CROSS_CHECK_THRESHOLD || 0.01
}

// ============================================================================
// Format helpers (HTML-safe, no Markdown).
// ============================================================================

function escapeHtml(s: string): string {
  return s.replace(/&/g, "&amp;").replace(/</g, "&lt;").replace(/>/g, "&gt;");
}

function fmtRu(n: number, decimals = 0): string {
  return n.toLocaleString("ru-RU", { minimumFractionDigits: 0, maximumFractionDigits: decimals });
}

function fmtRubSigned(n: number): string {
  if (n > 0) return `+${fmtRu(Math.abs(n))} ₽`;
  if (n < 0) return `−${fmtRu(Math.abs(n))} ₽`;
  return "0 ₽";
}

function fmtPctSigned(n: number, decimals = 1): string {
  const s = Math.abs(n).toLocaleString("ru-RU", { minimumFractionDigits: 0, maximumFractionDigits: decimals });
  if (n > 0) return `+${s}%`;
  if (n < 0) return `−${s}%`;
  return "0%";
}

function fmtRuDate(d: Date): string {
  return `${d.getUTCDate()} ${RU_MONTHS[d.getUTCMonth()]}`;
}

function fmtRuDateFull(d: Date): string {
  return `${fmtRuDate(d)} ${d.getUTCFullYear()} г.`;
}

function fmtT(n: number, decimals = 2): string {
  return `${n.toLocaleString("ru-RU", { minimumFractionDigits: 0, maximumFractionDigits: decimals })} тыс. т`;
}

// ============================================================================
// Cell coordinate helpers (REPORT-07 trace).
// ============================================================================

function colLetter(c: number): string {
  // 1=A, 2=B, ..., 26=Z, 27=AA
  let s = "";
  while (c > 0) { c--; s = String.fromCharCode(65 + (c % 26)) + s; c = Math.floor(c / 26); }
  return s;
}

// ============================================================================
// Section builders.
// ============================================================================

interface BuildContext {
  payload: ReporterPayload;
  opts: ReporterOptions;
  trace: NumberTrace[];
  warnings: string[];
}

function buildHeader(ctx: BuildContext): string {
  const { payload, opts } = ctx;
  const allDates: Date[] = [
    ...(payload.prices?.map(r => r.date) ?? []),
    ...(payload.fca?.map(r => r.date) ?? []),
    ...(payload.volumes?.map(r => r.date) ?? []),
    ...(payload.allPrices?.map(r => r.date) ?? []),
  ];
  if (allDates.length === 0 && payload.bitumSnapshot) allDates.push(payload.bitumSnapshot.date);
  if (allDates.length === 0) return "<b>Битум-отчёт</b>\n(нет данных)";

  const min = allDates.reduce((a, b) => a.getTime() < b.getTime() ? a : b);
  const max = allDates.reduce((a, b) => a.getTime() > b.getTime() ? a : b);
  const period = min.getTime() === max.getTime()
    ? fmtRuDateFull(min)
    : `${fmtRuDate(min)} – ${fmtRuDate(max)} ${max.getUTCFullYear()} г.`;
  const framing = opts.framingSentences?.topSummary?.trim() ?? "";
  return `<b>Период: ${escapeHtml(period)}</b>${framing ? "\n" + escapeHtml(framing) : ""}`;
}

function buildSnapshotBlock(ctx: BuildContext): string {
  const { payload } = ctx;
  if (!payload.bitumSnapshot) {
    return "<i>(нет данных: ожидается bitum_price_new.xlsx — средняя цена БНД snapshot)</i>";
  }
  const s = payload.bitumSnapshot;
  const file = payload.files.bitumPriceNewFile ?? "bitum_price_new.xlsx";
  ctx.trace.push({ value: s.bnd.price,    file, sheet: "Sheet1", cell: "F?", semantic: "BND snapshot price" });
  ctx.trace.push({ value: s.bnd.deltaAbs, file, sheet: "Sheet1", cell: "G?", semantic: "BND delta abs" });
  return `на дату ${escapeHtml(fmtRuDateFull(s.date).replace(" г.",""))} средняя цена <b>БНД</b> составила <b>${fmtRu(s.bnd.price)} ₽/т</b> (${fmtRubSigned(s.bnd.deltaAbs)}, ${fmtPctSigned(s.bnd.deltaPct)} за неделю)`;
}

function buildVolumesBlock(ctx: BuildContext): string {
  const { payload, opts } = ctx;
  if (!payload.volumes || payload.volumes.length === 0) {
    return "<b>### Объёмы биржевых торгов</b>\n<i>(нет данных: ожидается birzha_volumes.xlsx)</i>";
  }
  const totals = volumeTotals(payload.volumes, opts.dict);
  const topN = opts.topN ?? parseInt(process.env.BITUM_VOLUMES_TOP_N ?? "7", 10);
  const top = totals.perRefinery.slice(0, topN);
  const totalThousandT = totals.totalT / 1000;  // отчёт показывает в тыс.т
  const file = payload.files.birzhaVolumesFile ?? "birzha_volumes.xlsx";
  ctx.trace.push({ value: totalThousandT, file, sheet: "Sheet1", cell: "B4..T", semantic: "Total volume Σ" });
  const lines = [`<b>### Объёмы биржевых торгов</b>`,
                 `Суммарно за период реализовано <b>${fmtRu(totalThousandT, 2)} тыс. т</b>.`];
  for (const v of top) {
    const vThousand = v.totalT / 1000;
    ctx.trace.push({ value: vThousand, file, sheet: "Sheet1", cell: "?", semantic: `Volume ${v.canonical}` });
    lines.push(`- ${escapeHtml(v.canonical)} – ${fmtRu(vThousand, 2)} тыс. т`);
  }
  return lines.join("\n");
}

function buildCompanyGroupBlock(
  ctx: BuildContext,
  group: BitumCompanyGroup,
  framingSentence?: string
): string {
  const headerLabel = group.company === "Прочие" ? "Прочие и независимые" : group.company;
  const sumLine = `<b>### ${escapeHtml(headerLabel)} (Σ|Δ| = ${fmtRu(group.sumDeltaAbs)} ₽)</b>`;

  if (group.deltas.length === 0) {
    // REPORT-05: «Цены остались на уровне» — fallback messaging
    return `${sumLine}\nЦены остались без изменений.`;
  }

  // Filter movements (deltaAbs != 0)
  const movements = group.deltas.filter((d) => Math.abs(d.deltaAbs) > 0);
  const stable    = group.deltas.filter((d) => Math.abs(d.deltaAbs) === 0);

  const lines = [sumLine];
  if (framingSentence) lines.push(escapeHtml(framingSentence));
  for (const d of movements) {
    const file = d.source === "birzha"
      ? (ctx.payload.files.birzhaPricesFile ?? "birzha_prices.xlsx")
      : (ctx.payload.files.fcaSellersFile ?? "fca_sellers.xlsx");
    ctx.trace.push({ value: d.lastPrice, file, sheet: "Sheet1", cell: "?", semantic: `${d.canonical} last price` });
    ctx.trace.push({ value: d.deltaAbs,  file, sheet: "Sheet1", cell: "?", semantic: `${d.canonical} delta` });
    const sourceTag = d.source === "fca" ? "FCA" : "биржа";
    lines.push(`- ${escapeHtml(d.canonical)} (${sourceTag}) ${d.deltaAbs > 0 ? "вырос" : "снизился"} на ${fmtRubSigned(d.deltaAbs)} (до ${fmtRu(d.lastPrice)} ₽).`);
  }
  if (group.company === "Прочие" && stable.length > 0) {
    const tatneft = stable.filter((d) => getCompany(d.canonical, ctx.opts.dict) === "Татнефть");
    if (tatneft.length > 0) {
      const t = tatneft[0];
      lines.push(`Цены на НПЗ ${escapeHtml(t.canonical)} остались на уровне ${fmtRu(t.firstPrice)} ₽.`);
    }
  }
  return lines.join("\n");
}

function buildCrossCheckBlock(ctx: BuildContext): string {
  const { payload, opts } = ctx;
  if (!payload.fca || (!payload.bitumSnapshot && (!payload.allPrices || payload.allPrices.length === 0))) {
    return "";  // нечего сверять
  }
  const threshold = opts.crossCheckThreshold ?? parseFloat(process.env.BITUM_CROSS_CHECK_THRESHOLD ?? "0.01");

  // Собираем prices side
  const prices1: { canonical: string; price: number; date: Date; source: string }[] = [];
  if (payload.bitumSnapshot) {
    prices1.push({ canonical: "БНД snapshot", price: payload.bitumSnapshot.bnd.price, date: payload.bitumSnapshot.date, source: "bitum_price_new" });
  }
  for (const r of payload.allPrices ?? []) {
    prices1.push({ canonical: r.pointOfShipment, price: r.priceRub, date: r.date, source: "all_prices" });
  }
  const prices2 = payload.fca.map((f) => ({ canonical: f.refineryCanonical, price: f.priceRub, date: f.date, source: "fca_sellers" }));

  const warnings = crossCheck(prices1, prices2, threshold);
  if (warnings.length === 0) return "";
  const lines = ["<b>⚠️ Цены расходятся (REPORT-08):</b>"];
  for (const w of warnings) {
    lines.push(`- ${escapeHtml(w.canonical)}: ${w.source1}=${fmtRu(w.price1)} ₽ vs ${w.source2}=${fmtRu(w.price2)} ₽ (${fmtPctSigned(w.diffPct)})`);
    ctx.warnings.push(`Cross-check warning: ${w.canonical} ${w.source1}=${w.price1} vs ${w.source2}=${w.price2}`);
  }
  return lines.join("\n");
}

function buildSourcesFooter(ctx: BuildContext): string {
  // D-09: компактный <code> блок со сводкой ПО ФАЙЛУ (не построчно)
  const { payload, trace } = ctx;
  const byFile = new Map<string, number>();
  for (const t of trace) {
    byFile.set(t.file, (byFile.get(t.file) ?? 0) + 1);
  }
  if (byFile.size === 0) return "";
  const lines = ["Источники:"];
  for (const [file, count] of byFile) {
    lines.push(`• ${file}: ${count} ${count === 1 ? "число" : "чисел"}`);
  }
  return `<code>${escapeHtml(lines.join("\n"))}</code>`;
}

function buildPartialRenderWarning(ctx: BuildContext): string {
  const { payload } = ctx;
  const present: string[] = [];
  const missing: string[] = [];
  if (payload.prices?.length)         present.push("birzha_prices"); else missing.push("birzha_prices");
  if (payload.volumes?.length)        present.push("birzha_volumes"); else missing.push("birzha_volumes");
  if (payload.fca?.length)            present.push("fca_sellers"); else missing.push("fca_sellers");
  if (payload.allPrices?.length)      present.push("all_prices"); else missing.push("all_prices");
  if (payload.bitumSnapshot)          present.push("bitum_price_new"); else missing.push("bitum_price_new");
  if (missing.length === 0) return "";
  ctx.warnings.push(`Partial render: ${present.length}/5 types present`);
  return `<b>⚠️ Доступно ${present.length}/5 типов:</b> ${present.join(", ")}.\nОтсутствуют: ${missing.join(", ")} — соответствующие блоки пропущены.`;
}

// ============================================================================
// Main entry.
// ============================================================================

export function renderBitumReport(payload: ReporterPayload, opts: ReporterOptions): ReportResult {
  const ctx: BuildContext = { payload, opts, trace: [], warnings: [] };

  const sections: string[] = [];
  const partialWarn = buildPartialRenderWarning(ctx);
  if (partialWarn) sections.push(partialWarn);

  sections.push(buildHeader(ctx));
  sections.push(buildSnapshotBlock(ctx));
  sections.push(buildVolumesBlock(ctx));

  // BITUM-REPORT-03..06: фиксированный порядок холдингов
  if (payload.prices || payload.fca) {
    const birzhaDeltas = deltasFor(payload.prices ?? [], "birzha");
    const fcaDeltas    = deltasFor(payload.fca ?? [], "fca");
    const allDeltas    = [...birzhaDeltas, ...fcaDeltas];
    const groups       = byCompanyFixedOrder(allDeltas, opts.dict);
    const framingMap: Record<string, string | undefined> = {
      "Роснефть":     opts.framingSentences?.rosneft,
      "Газпромнефть": opts.framingSentences?.gazpromneft,
      "ЛУКОЙЛ":       opts.framingSentences?.lukoil,
      "Прочие":       opts.framingSentences?.others,
    };
    for (const g of groups) {
      sections.push(buildCompanyGroupBlock(ctx, g, framingMap[g.company]));
    }
  }

  const crossBlock = buildCrossCheckBlock(ctx);
  if (crossBlock) sections.push(crossBlock);

  const footer = buildSourcesFooter(ctx);
  if (footer) sections.push(footer);

  return {
    html: sections.join("\n\n"),
    trace: ctx.trace,
    warnings: ctx.warnings,
  };
}

/**
 * Нарезка отчёта на ≤4000 chars с префиксом (i/N). Reuse chunkHtml из src/deliver.ts.
 */
export function chunkBitumHtml(html: string): string[] {
  const parts = chunkHtml(html, 3920); // 80 chars reserve для (i/N)
  if (parts.length === 1) return parts;
  return parts.map((p, i) => `(${i + 1}/${parts.length})\n${p}`);
}
```

Создать `src/__tests__/bitum-reporter.test.ts` (8 главных кейсов: full payload, fixed holding order, partial render, ЛУКОЙЛ no-movements, cross-check warning, HTML whitelist, chunkBitumHtml ≤4000, footer Sources).

Создать `src/__tests__/bitum-reporter-trace.test.ts` (специально для REPORT-07): для каждого числа > 100 в html, найти соответствующий trace entry; проверить что trace.length > 0 и каждый trace.file/sheet/cell непустой.
  </action>
  <verify>
    <automated>npm test && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - File `src/bitum/reporter.ts` exists
    - `grep -c "export function renderBitumReport" src/bitum/reporter.ts` returns 1
    - `grep -c "export function chunkBitumHtml" src/bitum/reporter.ts` returns 1
    - `grep -F "\"Роснефть\", \"Газпромнефть\", \"ЛУКОЙЛ\", \"Прочие\"" src/bitum/reporter.ts` returns 1 (fixed order verbatim)
    - `grep -c "Татнефть" src/bitum/reporter.ts` returns ≥ 1 (Татнефть special handling в Прочие)
    - `grep -c "ctx\\.trace\\.push" src/bitum/reporter.ts` returns ≥ 4 (trace populated for multiple values)
    - `grep -c "Доступно.*/5 типов" src/bitum/reporter.ts` returns ≥ 1 (partial-render warning)
    - `grep -c "Источники:" src/bitum/reporter.ts` returns ≥ 1 (D-09 footer)
    - HTML whitelist enforcement: `grep -c "<h1>\\|<h2>\\|<h3>\\|<hr>\\|<br>\\|<p>\\|<div>\\|<span>" src/bitum/reporter.ts` returns 0
    - `grep -c "BITUM_VOLUMES_TOP_N\\|BITUM_CROSS_CHECK_THRESHOLD" src/bitum/reporter.ts` returns ≥ 2 (env vars)
    - `npx vitest run src/__tests__/bitum-reporter.test.ts src/__tests__/bitum-reporter-trace.test.ts` exits 0
    - `npm test` exits 0
  </acceptance_criteria>
  <done>reporter.ts с structured HTML, fixed holding order, partial-render, trace, cross-check warnings; ≥ 12 vitest кейсов проходят (reporter + trace).</done>
</task>

<task id="3.4" type="auto" autonomous="true">
  <name>Task 3.4: Расширить .env.example с BITUM_CROSS_CHECK_THRESHOLD и BITUM_VOLUMES_TOP_N</name>
  <files>.env.example</files>
  <read_first>
    - .env.example (existing structure — найти подходящее место для битум-переменных)
  </read_first>
  <action>
Дописать в `.env.example` блок (после существующих переменных):

```
# Битум-отчёт (Phase 4, milestone v5.0)
# Порог расхождения цен для cross-check (REPORT-08); 0.01 = 1%.
BITUM_CROSS_CHECK_THRESHOLD=0.01
# Top-N НПЗ в блоке «Объёмы биржевых торгов» отчёта.
BITUM_VOLUMES_TOP_N=7
```
  </action>
  <verify>
    <automated>npm test && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "BITUM_CROSS_CHECK_THRESHOLD" .env.example` returns ≥ 1
    - `grep -c "BITUM_VOLUMES_TOP_N" .env.example` returns ≥ 1
  </acceptance_criteria>
  <done>.env.example расширен 2 битум-переменными.</done>
</task>

<task id="3.5" type="checkpoint:human-verify" gate="blocking">
  <name>Task 3.5: Wave 3 checkpoint: analyzer + reporter review</name>
  <files>N/A — human verification checkpoint</files>
  <action>Pause and review what was built in this wave (analyzer (fixed order) + reporter (trace + partial-render) + storage). Operator inspects code, runs tests, gives go/no-go to proceed to next wave.</action>
  <verify>
    <automated>npm test && npx tsc --noEmit</automated>
  </verify>
  <done>Operator types resume-signal to proceed to next wave</done>
  <what-built>Wave 3 analyzer + reporter + storage + env vars. Atomic git commit.</what-built>
  <how-to-verify>
    1. `npm test` — passes
    2. `npx tsc --noEmit` — clean
    3. `ls src/bitum/` shows: types.ts, signatures.ts, classifier.ts, learned-signatures.ts, parsers/, analyzer.ts, reporter.ts, storage.ts
    4. Smoke: render dummy report — `node --import tsx -e "import('./src/bitum/reporter.js').then(m => { const r = m.renderBitumReport({ files: {} }, { dict: [] }); console.log('html_len=', r.html.length, 'trace=', r.trace.length, 'warnings=', r.warnings.length); })"` — must output sane numbers without throwing
    5. `git log --oneline -1` — `feat(04): wave 3 analyzer+reporter — fixed holding order + cell-trace + cross-check`
  </how-to-verify>
  <resume-signal>Type "wave-3-approved" или describe issues</resume-signal>
</task>

</wave>

<!-- ========================================================================== -->
<!-- WAVE 4 — LLM NARRATIVE (hybrid scope per D-08)                              -->
<!-- ========================================================================== -->

<wave id="4" name="LLM narrative">
<objective>Создать `src/bitum/llm.ts` — рефактор `src/upload/llm.ts` под гибридный scope D-08: system-prompt пишет ТОЛЬКО framing-предложения (1-3 на блок), числа подставляет reporter программно. Payload содержит already-built report HTML с placeholder-местами для LLM-вставок; LLM возвращает JSON `{ topSummary, rosneft, gazpromneft, lukoil, others }` который reporter использует в `framingSentences`. Wave commit: `feat(04): wave 4 llm narrative — hybrid scope (numbers programmatic, framing LLM)`.</objective>

<task id="4.1" type="auto" autonomous="true" tdd="true">
  <name>Task 4.1: Создать src/bitum/llm.ts с buildBitumNarrative + BITUM_NARRATIVE_SYSTEM_PROMPT (BITUM-REPORT-07, D-08)</name>
  <files>src/bitum/llm.ts, src/__tests__/bitum-llm.test.ts</files>
  <read_first>
    - src/upload/llm.ts (existing buildLlmNarrative pattern — lazy OpenAI client, env DEEPSEEK_*, temperature 0, maxRetries 1, log error.cause)
    - src/bitum/types.ts (ReportResult)
    - src/bitum/reporter.ts (ReporterPayload, ReporterOptions.framingSentences — LLM возвращает эти sentences)
    - src/bitum/analyzer.ts (BitumDelta, BitumCompanyGroup — payload для LLM)
    - .planning/phases/04-bitum-weekly-report/04-CONTEXT.md (D-08 hybrid scope — полный список того что LLM пишет vs программно)
  </read_first>
  <behavior>
    - Test 1: BITUM_NARRATIVE_SYSTEM_PROMPT содержит жёсткий запрет «выдумывать числа»
    - Test 2: System prompt запрещает упоминать конкретные значения цен (НЕ список 28 ₽, +500 ₽, и т.п.) — LLM пишет ТОЛЬКО framing
    - Test 3: buildBitumNarrative с mock OpenAI → возвращает { topSummary, rosneft, gazpromneft, lukoil, others }
    - Test 4: response_format: json_object используется (jail для structured output)
    - Test 5: При DEEPSEEK_API_KEY отсутствует → throw Error
    - Test 6: При empty completion → throw Error
    - Test 7: При client mock возвращает невалидный JSON → throw Error с понятным сообщением
  </behavior>
  <action>
Создать `src/bitum/llm.ts`:

```typescript
// src/bitum/llm.ts — DeepSeek narrative для битум-отчёта (BITUM-REPORT-07, D-08).
// HYBRID SCOPE: LLM пишет ТОЛЬКО framing-предложения (1-3 на блок), числа подставляет
// reporter программно. response_format: json_object — jail для structured output.

import OpenAI from "openai";
import { log } from "../logger.js";
import type { BitumDelta, BitumCompanyGroup } from "./analyzer.js";
import type { ParsedBitumPriceNewSnapshot } from "./types.js";

/**
 * D-08: LLM SCOPE — ТОЛЬКО framing, БЕЗ чисел.
 * Reporter подставит numbers из ParsedRow / analysis programmatically.
 */
export const BITUM_NARRATIVE_SYSTEM_PROMPT = [
  "Ты — аналитик битумного рынка РФ. На вход получаешь структурированную сводку",
  "Δ цен по холдингам (Роснефть/Газпромнефть/ЛУКОЙЛ/Прочие). Твоя задача —",
  "написать ТОЛЬКО framing-предложения (общий тон) для секций отчёта. Числа",
  "(цены, дельты, объёмы) reporter подставит сам — НЕ пиши их в своих framing'ах.",
  "",
  "ЖЁСТКИЕ ПРАВИЛА (нарушение = revoke output):",
  "1) ЗАПРЕЩЕНО упоминать конкретные числа: ₽, тонн, %, +5, −1000, 28 000.",
  "   Reporter подставляет числа сам — твоя задача — общая характеристика.",
  "2) Каждое framing-предложение = 1-2 коротких фразы; на уровне «тенденция»",
  "   («цены выросли», «движений не наблюдалось», «снижение по двум площадкам»).",
  "3) Если данных по группе нет — ставь empty string для этого ключа (НЕ пиши «нет данных»).",
  "4) Формат ответа — JSON {topSummary, rosneft, gazpromneft, lukoil, others}, БЕЗ markdown.",
  "5) Запрещены теги, эмодзи, разметка — простой текст внутри JSON-строк.",
  "",
  "Пример хорошего framing'а:",
  "  rosneft: 'Ключевые движения — рост по большинству заводов.'",
  "Пример плохого (числа):",
  "  rosneft: 'Ключевые движения — рост на 1000 ₽ по Саратовскому НПЗ.' ← ЗАПРЕЩЕНО",
].join("\n");

/**
 * Payload для LLM. Только canonical-имена + знаки (positive/negative/zero), БЕЗ значений.
 */
export interface NarrativePayload {
  period: { start: string; end: string };
  groups: { company: string; movements: { canonical: string; direction: "up" | "down" | "flat"; source: "birzha" | "fca" }[] }[];
  hasSnapshot: boolean;
  hasVolumes: boolean;
}

export interface NarrativeResult {
  topSummary: string;
  rosneft: string;
  gazpromneft: string;
  lukoil: string;
  others: string;
}

/**
 * Сериализует BitumCompanyGroup[] в Payload (БЕЗ чисел — только direction).
 */
export function encodeReportForLlm(
  groups: BitumCompanyGroup[],
  period: { start: Date; end: Date },
  snapshot?: ParsedBitumPriceNewSnapshot,
  hasVolumes = false
): NarrativePayload {
  return {
    period: { start: period.start.toISOString().slice(0,10), end: period.end.toISOString().slice(0,10) },
    groups: groups.map((g) => ({
      company: g.company,
      movements: g.deltas.map((d) => ({
        canonical: d.canonical,
        direction: d.deltaAbs > 0 ? "up" : d.deltaAbs < 0 ? "down" : "flat",
        source: d.source,
      })),
    })),
    hasSnapshot: !!snapshot,
    hasVolumes,
  };
}

export interface BuildBitumNarrativeOptions {
  client?: OpenAI;
  model?: string;
}

export async function buildBitumNarrative(
  payload: NarrativePayload,
  opts: BuildBitumNarrativeOptions = {}
): Promise<NarrativeResult> {
  let client = opts.client;
  let model = opts.model;
  if (!client) {
    const apiKey = process.env.DEEPSEEK_API_KEY;
    if (!apiKey) throw new Error("DEEPSEEK_API_KEY не задан.");
    const baseURL = process.env.DEEPSEEK_BASE_URL ?? "https://api.deepseek.com";
    model = model ?? process.env.DEEPSEEK_MODEL ?? "deepseek-chat";
    client = new OpenAI({ apiKey, baseURL, timeout: 120_000, maxRetries: 1 });
  } else {
    model = model ?? process.env.DEEPSEEK_MODEL ?? "deepseek-chat";
  }

  const startedAt = Date.now();
  log.info(`[bitum-llm] start: groups=${payload.groups.length} model=${model}`);

  let raw: string;
  try {
    const completion = await client.chat.completions.create({
      model,
      temperature: 0,
      response_format: { type: "json_object" },
      messages: [
        { role: "system", content: BITUM_NARRATIVE_SYSTEM_PROMPT },
        { role: "user",   content: JSON.stringify(payload) },
      ],
    });
    raw = (completion.choices[0]?.message?.content ?? "").trim();
    if (!raw) throw new Error("[bitum-llm] DeepSeek вернул пустой ответ");
  } catch (err) {
    const e = err as Error & { cause?: unknown };
    log.error(`[bitum-llm] DeepSeek failed: ${e.message}${e.cause ? " cause=" + String(e.cause) : ""}`);
    throw err;
  }

  let parsed: any;
  try { parsed = JSON.parse(raw); } catch (err) {
    throw new Error(`[bitum-llm] DeepSeek вернул невалидный JSON: ${raw.slice(0, 200)}`);
  }
  const result: NarrativeResult = {
    topSummary:  String(parsed.topSummary  ?? ""),
    rosneft:     String(parsed.rosneft     ?? ""),
    gazpromneft: String(parsed.gazpromneft ?? ""),
    lukoil:      String(parsed.lukoil      ?? ""),
    others:      String(parsed.others      ?? ""),
  };
  log.info(`[bitum-llm] done in ${Date.now() - startedAt}ms`);
  return result;
}
```

Создать `src/__tests__/bitum-llm.test.ts` с mock OpenAI (7 кейсов). Mock pattern идентичен src/__tests__/upload-llm.test.ts.
  </action>
  <verify>
    <automated>npm test && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - File `src/bitum/llm.ts` exists
    - `grep -c "export async function buildBitumNarrative" src/bitum/llm.ts` returns 1
    - `grep -c "export const BITUM_NARRATIVE_SYSTEM_PROMPT" src/bitum/llm.ts` returns 1
    - `grep -c "export function encodeReportForLlm" src/bitum/llm.ts` returns 1
    - `grep -c "ЗАПРЕЩЕНО упоминать конкретные числа" src/bitum/llm.ts` returns ≥ 1 (D-08 jail)
    - `grep -c "response_format.*json_object" src/bitum/llm.ts` returns ≥ 1
    - `grep -c "DEEPSEEK_API_KEY" src/bitum/llm.ts` returns ≥ 1
    - `npx vitest run src/__tests__/bitum-llm.test.ts` exits 0
    - `npm test` exits 0
  </acceptance_criteria>
  <done>llm.ts с hybrid scope, jail в system-prompt, JSON-only response_format, mock-friendly.</done>
</task>

<task id="4.2" type="checkpoint:human-verify" gate="blocking">
  <name>Task 4.2: Wave 4 checkpoint: LLM narrative review</name>
  <files>N/A — human verification checkpoint</files>
  <action>Pause and review what was built in this wave (hybrid scope LLM (numbers programmatic, framing LLM)). Operator inspects code, runs tests, gives go/no-go to proceed to next wave.</action>
  <verify>
    <automated>npm test && npx tsc --noEmit</automated>
  </verify>
  <done>Operator types resume-signal to proceed to next wave</done>
  <what-built>Wave 4 LLM narrative: hybrid scope per D-08. Atomic git commit.</what-built>
  <how-to-verify>
    1. `npm test` — passes
    2. `grep -F "ЗАПРЕЩЕНО упоминать конкретные числа" src/bitum/llm.ts` — должен найти строку (D-08 jail enforced)
    3. `git log --oneline -1` — `feat(04): wave 4 llm narrative — hybrid scope (numbers programmatic, framing LLM)`
  </how-to-verify>
  <resume-signal>Type "wave-4-approved"</resume-signal>
</task>

</wave>

<!-- ========================================================================== -->
<!-- WAVE 5 — BOT COMMANDS (4 bitum commands + TG-05 upload response + learning UX) -->
<!-- ========================================================================== -->

<wave id="5" name="Bot commands">
<objective>Реализовать в `src/bot.ts` (+ новый `src/bot-bitum.ts` для группировки) 4 битум-команды (`/bitum_status`, `/bitum_preview`, `/bitum_report`, `/bitum_reset`) + TG-05 xlsx upload response + classifier learning UX inline-keyboard (D-14) + публикация confirm flow (D-11) + reset confirm flow (D-12). Алиасы команд `/summarize`/`/upload_status` пока ОСТАЮТСЯ работать через legacy handlers — wave 6 их депрекейтнет. Wave commit: `feat(04): wave 5 bot commands — 4 bitum commands + TG-05 + learning UX`.</objective>

<task id="5.1" type="auto" autonomous="true" tdd="true">
  <name>Task 5.1: Создать src/bot-bitum.ts с handler-функциями для 4 команд + handleBitumDocument + handleBitumCallback (BITUM-TG-01..05, BITUM-CLS-03)</name>
  <files>src/bot-bitum.ts, src/__tests__/bitum-bot-commands.test.ts, src/__tests__/bitum-bot-learning.test.ts</files>
  <read_first>
    - src/bot.ts (полностью — handleDocument, handleSummarizeCommand, handleCommand, handleCallbackQuery, sendHtml/sendPlain/sendReplyWithKeyboard, tgFetch, parseRemoveCallbackData паттерн)
    - src/bitum/classifier.ts (classifyFile)
    - src/bitum/learned-signatures.ts (appendLearned)
    - src/bitum/parsers/index.ts (все 5 parsers)
    - src/bitum/storage.ts (listWeekV5, saveUpload, resetWeek, findLatestWeekWithUploads, isoWeekFolder, writeLastRun)
    - src/bitum/reporter.ts (renderBitumReport, chunkBitumHtml)
    - src/bitum/llm.ts (buildBitumNarrative, encodeReportForLlm)
    - src/bitum/analyzer.ts (deltasFor, byCompanyFixedOrder)
    - src/upload/refineries.ts (loadRefineries)
    - .planning/phases/04-bitum-weekly-report/04-CONTEXT.md (D-11 publish flow, D-12 reset flow, D-13 TG-05 single message, D-14 learning UX flow)
  </read_first>
  <behavior>
    - Test 1: handleBitumStatus(token, msg) шлёт текст с 5 типов чек-листом
    - Test 2: handleBitumPreview(token, msg) рендерит отчёт + шлёт в DM (НЕ в TG_CHANNEL_ID)
    - Test 3: handleBitumReport(token, msg) шлёт preview + inline-keyboard «📤 Опубликовать / ❌ Отмена»
    - Test 4: handleBitumReset(token, msg) шлёт текст «Сбросить N файлов?» + inline-keyboard
    - Test 5: TG-05 handleBitumDocument при confidence >= 1 — saveUpload + ОДНО сообщение с type+confidence+meta+parse-result+check-list
    - Test 6: D-14 при confidence < 1 OR type=unknown — НЕ saveUpload, шлёт inline-keyboard с 6 кнопками (5 типов + «не битум»)
    - Test 7: handleBitumCallback при `bs:<type>` (bitum signature learn) — appendLearned + saveUpload + TG-05 response
    - Test 8: handleBitumCallback при `bs:not_bitum` — отвечает «Не сохранено»
    - Test 9: handleBitumCallback при `bp:confirm` (bitum publish) — sendToChannel TG_CHANNEL_ID + answer в DM
    - Test 10: handleBitumCallback при `bp:cancel` — отвечает «Отмена»
    - Test 11: handleBitumCallback при `br:confirm` (bitum reset) — resetWeek + ответ deleted files
    - Test 12: handleBitumCallback при `br:cancel` — отвечает «Отмена»
  </behavior>
  <action>
Создать `src/bot-bitum.ts` (300-500 строк). Использовать паттерны из src/bot.ts (tgFetch, sendHtml, sendPlain, sendReplyWithKeyboard, downloadTgFile — экспортировать из bot.ts или дублировать). Структура:

```typescript
// src/bot-bitum.ts — handlers для 4 битум-команд + TG-05 upload + learning UX +
// publish/reset confirm flows. Импортируется из src/bot.ts; bot.ts роутит cmd → handler.

import path from "node:path";
import { readFileSync } from "node:fs";
import { log } from "./logger.js";
import { classifyFile } from "./bitum/classifier.js";
import { appendLearned } from "./bitum/learned-signatures.js";
import {
  parseBirzhaPrices, parseBirzhaVolumes, parseFcaSellers,
  parseAllPrices, parseBitumPriceNew,
} from "./bitum/parsers/index.js";
import {
  isoWeekFolder, saveUpload, listWeekV5, findLatestWeekWithUploads,
  weekDir, resetWeek, writeLastRun,
} from "./bitum/storage.js";
import { renderBitumReport, chunkBitumHtml, type ReporterPayload } from "./bitum/reporter.js";
import { encodeReportForLlm, buildBitumNarrative } from "./bitum/llm.js";
import { byCompanyFixedOrder, deltasFor } from "./bitum/analyzer.js";
import { loadRefineries } from "./upload/refineries.js";
import type { BitumType, KnownBitumType, ClassifyResult, WeekStatusV5 } from "./bitum/types.js";

// Re-use helpers from src/bot.ts — экспортируются ИЗ bot.ts (wave 5.2 экспортит их).
// Эти imports работают потому что wave 5.2 добавит export к этим функциям.
import {
  tgFetch as _tgFetch,
  sendHtml as _sendHtml,
  sendPlain as _sendPlain,
  sendReply as _sendReply,
  sendReplyWithKeyboard as _sendReplyWithKeyboard,
  downloadTgFile as _downloadTgFile,
  type TgMessage, type TgCallbackQuery, type TgInlineKeyboardButton,
} from "./bot.js";

const CHANNEL_ID_ENV = "TG_CHANNEL_ID";

// =============================================================================
// Pending state для confirm flows (D-11, D-12).
// In-memory; рестарт бота → pending теряются (см. claude's discretion #7).
// =============================================================================

interface PendingPublish {
  html: string;  // полный отчёт для публикации
  chatId: number;
  createdAt: number;
}
const pendingPublishByMsgId = new Map<number, PendingPublish>();

interface PendingReset {
  week: string;
  chatId: number;
  createdAt: number;
}
const pendingResetByMsgId = new Map<number, PendingReset>();

// =============================================================================
// Inline-keyboard layouts.
// =============================================================================

const LEARNING_KEYBOARD: TgInlineKeyboardButton[][] = [
  [{ text: "birzha_prices", callback_data: "bs:birzha_prices" }],
  [{ text: "birzha_volumes", callback_data: "bs:birzha_volumes" }],
  [{ text: "fca_sellers", callback_data: "bs:fca_sellers" }],
  [{ text: "all_prices", callback_data: "bs:all_prices" }],
  [{ text: "bitum_price_new", callback_data: "bs:bitum_price_new" }],
  [{ text: "❌ не битум", callback_data: "bs:not_bitum" }],
];

// =============================================================================
// Reusable: load all 5 types from week & build reporter payload.
// =============================================================================

async function loadWeekPayload(week: string): Promise<{ payload: ReporterPayload; status: WeekStatusV5; dict: any[] }> {
  const dict = loadRefineries();
  const status = listWeekV5(week);
  const payload: ReporterPayload = { files: {} };

  if (status.hasBirzhaPrices) {
    const buf = readFileSync(path.join(weekDir(week), "birzha_prices.xlsx"));
    const r = await parseBirzhaPrices(buf, dict);
    payload.prices = r.rows;
    payload.files.birzhaPricesFile = "birzha_prices.xlsx";
  }
  if (status.hasBirzhaVolumes) {
    const buf = readFileSync(path.join(weekDir(week), "birzha_volumes.xlsx"));
    const r = await parseBirzhaVolumes(buf, dict);
    payload.volumes = r.rows;
    payload.files.birzhaVolumesFile = "birzha_volumes.xlsx";
  }
  if (status.hasFcaSellers) {
    // MIGRATE-03: предпочесть fca_sellers.xlsx, fallback на legacy fca.xlsx
    const newPath = path.join(weekDir(week), "fca_sellers.xlsx");
    const legacyPath = path.join(weekDir(week), "fca.xlsx");
    const filePath = require("node:fs").existsSync(newPath) ? newPath : legacyPath;
    const buf = readFileSync(filePath);
    const r = await parseFcaSellers(buf, dict);
    payload.fca = r.rows;
    payload.files.fcaSellersFile = path.basename(filePath);
  }
  if (status.hasAllPrices) {
    const buf = readFileSync(path.join(weekDir(week), "all_prices.xlsx"));
    const r = await parseAllPrices(buf, dict);
    payload.allPrices = r.rows;
    payload.files.allPricesFile = "all_prices.xlsx";
  }
  if (status.hasBitumPriceNew) {
    const buf = readFileSync(path.join(weekDir(week), "bitum_price_new.xlsx"));
    const r = await parseBitumPriceNew(buf);
    if (r.rows.length > 0) payload.bitumSnapshot = r.rows[0];
    payload.files.bitumPriceNewFile = "bitum_price_new.xlsx";
  }
  return { payload, status, dict };
}

// =============================================================================
// BITUM-TG-01: /bitum_status
// =============================================================================

export async function handleBitumStatus(token: string, msg: TgMessage): Promise<void> {
  const week = findLatestWeekWithUploads() ?? isoWeekFolder(new Date());
  const status = listWeekV5(week);
  const lines = [
    `<b>Битум-неделя ${week}</b>`,
    `Чек-лист (${status.presentCount}/5):`,
    `- birzha_prices: ${status.hasBirzhaPrices ? "✅" : "❌"}`,
    `- birzha_volumes: ${status.hasBirzhaVolumes ? "✅" : "❌"}`,
    `- fca_sellers: ${status.hasFcaSellers ? "✅" : "❌"}`,
    `- all_prices: ${status.hasAllPrices ? "✅" : "❌"}`,
    `- bitum_price_new: ${status.hasBitumPriceNew ? "✅" : "❌"}`,
    `Последний прогон: ${status.lastRunAt ?? "—"}`,
  ];
  await _sendHtml(token, msg.chat.id, lines.join("\n"));
}

// =============================================================================
// BITUM-TG-02: /bitum_preview — рендерит и шлёт В DM (НЕ публикует)
// =============================================================================

export async function handleBitumPreview(token: string, msg: TgMessage): Promise<void> {
  const chatId = msg.chat.id;
  try {
    await _sendPlain(token, chatId, "📊 Готовлю превью отчёта…");
    const week = findLatestWeekWithUploads() ?? isoWeekFolder(new Date());
    const { payload, status, dict } = await loadWeekPayload(week);
    if (status.presentCount === 0) {
      await _sendHtml(token, chatId, `<b>❓ За неделю ${week} файлов не загружено.</b>\nЗагрузите xlsx и повторите.`);
      return;
    }

    // Optional LLM framing
    let framingSentences = {};
    try {
      if (process.env.DEEPSEEK_API_KEY) {
        const birzhaDeltas = deltasFor(payload.prices ?? [], "birzha");
        const fcaDeltas    = deltasFor(payload.fca ?? [], "fca");
        const groups = byCompanyFixedOrder([...birzhaDeltas, ...fcaDeltas], dict);
        const dates = [...(payload.prices ?? []), ...(payload.fca ?? [])].map(r => r.date);
        if (dates.length > 0) {
          const period = { start: dates.reduce((a,b)=>a<b?a:b), end: dates.reduce((a,b)=>a>b?a:b) };
          const llmPayload = encodeReportForLlm(groups, period, payload.bitumSnapshot, !!payload.volumes);
          const narrative = await buildBitumNarrative(llmPayload);
          framingSentences = narrative;
        }
      }
    } catch (err) {
      log.warn(`[bitum-bot] LLM narrative failed: ${(err as Error).message}; продолжаем без framing`);
    }

    const report = renderBitumReport(payload, { dict, framingSentences });
    const parts = chunkBitumHtml(report.html);
    for (const part of parts) {
      await _sendHtml(token, chatId, part);
    }
    writeLastRun(week, new Date());
  } catch (err) {
    const m = (err as Error).message ?? String(err);
    log.error(`[bitum-bot] /bitum_preview error: ${m}`);
    await _sendPlain(token, chatId, `⚠️ Ошибка: ${m.slice(0, 300)}`);
  }
}

// =============================================================================
// BITUM-TG-03: /bitum_report — preview + inline-keyboard publish/cancel (D-11)
// =============================================================================

export async function handleBitumReport(token: string, msg: TgMessage): Promise<void> {
  const chatId = msg.chat.id;
  try {
    const week = findLatestWeekWithUploads() ?? isoWeekFolder(new Date());
    const { payload, status, dict } = await loadWeekPayload(week);
    if (status.presentCount === 0) {
      await _sendHtml(token, chatId, `<b>❓ За неделю ${week} файлов не загружено.</b>`);
      return;
    }
    const report = renderBitumReport(payload, { dict });
    const parts = chunkBitumHtml(report.html);
    let lastMsgId = msg.message_id;
    for (const part of parts) {
      await _sendHtml(token, chatId, part);
    }
    // Финальное сообщение с кнопками publish/cancel
    const keyboard: TgInlineKeyboardButton[][] = [
      [
        { text: "📤 Опубликовать в канал", callback_data: "bp:confirm" },
        { text: "❌ Отмена",                callback_data: "bp:cancel" },
      ],
    ];
    const promptResp = await _tgFetch<{ ok: boolean; result: { message_id: number } }>(token, "sendMessage", {
      chat_id: chatId, text: "Опубликовать отчёт в TG_CHANNEL_ID?",
      reply_markup: { inline_keyboard: keyboard },
    });
    const promptMsgId = promptResp.result.message_id;
    pendingPublishByMsgId.set(promptMsgId, { html: report.html, chatId, createdAt: Date.now() });
  } catch (err) {
    const m = (err as Error).message ?? String(err);
    log.error(`[bitum-bot] /bitum_report error: ${m}`);
    await _sendPlain(token, chatId, `⚠️ Ошибка: ${m.slice(0, 300)}`);
  }
}

// =============================================================================
// BITUM-TG-04: /bitum_reset — inline-keyboard confirm (D-12)
// =============================================================================

export async function handleBitumReset(token: string, msg: TgMessage): Promise<void> {
  const chatId = msg.chat.id;
  const week = findLatestWeekWithUploads() ?? isoWeekFolder(new Date());
  const status = listWeekV5(week);
  if (status.presentCount === 0) {
    await _sendPlain(token, chatId, `Папка ${week} уже пуста.`);
    return;
  }
  const keyboard: TgInlineKeyboardButton[][] = [
    [
      { text: "✅ Сбросить", callback_data: "br:confirm" },
      { text: "❌ Отмена",   callback_data: "br:cancel" },
    ],
  ];
  const promptResp = await _tgFetch<{ ok: boolean; result: { message_id: number } }>(token, "sendMessage", {
    chat_id: chatId,
    text: `Сбросить неделю ${week}? Будет удалено ${status.presentCount} xlsx файл(ов). Операция деструктивная, без backup.`,
    reply_markup: { inline_keyboard: keyboard },
  });
  pendingResetByMsgId.set(promptResp.result.message_id, { week, chatId, createdAt: Date.now() });
}

// =============================================================================
// BITUM-TG-05: handleBitumDocument — xlsx upload response (TG-05, D-13, D-14)
// =============================================================================

export async function handleBitumDocument(
  token: string,
  msg: TgMessage,
  buffer: Buffer,
  originalFileName: string
): Promise<void> {
  const chatId = msg.chat.id;
  try {
    const cls: ClassifyResult = await classifyFile(buffer);
    log.info(`[bitum-bot] classify: file=${originalFileName} type=${cls.type} conf=${cls.confidence}`);

    // D-14: classifier learning UX при confidence < 1 OR unknown
    if (cls.confidence < 1 || cls.type === "unknown") {
      // НЕ сохраняем файл; в pending для learning callback кладём buffer + meta
      // Используем callback_data с meta-hash для retrieval — но проще держать в Map по chat_id+message_id
      const promptResp = await _tgFetch<{ ok: boolean; result: { message_id: number } }>(token, "sendMessage", {
        chat_id: chatId,
        text: `Файл "${originalFileName}" — confidence ${cls.confidence.toFixed(2)}, type=${cls.type}. Укажите правильный тип:`,
        reply_markup: { inline_keyboard: LEARNING_KEYBOARD },
      });
      pendingLearningByMsgId.set(promptResp.result.message_id, {
        buffer, originalFileName, meta: cls.meta, chatId, createdAt: Date.now(),
      });
      return;
    }

    // confidence === 1 → save + parse + ONE response (TG-13)
    if (cls.type === "unknown") return;  // type-narrow
    const week = isoWeekFolder(new Date());
    await saveUpload(buffer, cls.type as KnownBitumType, week);
    const parseResult = await parseByType(cls.type as KnownBitumType, buffer);
    const status = listWeekV5(week);
    const checkListLines = [
      `- birzha_prices: ${status.hasBirzhaPrices ? "✅" : "❌"}`,
      `- birzha_volumes: ${status.hasBirzhaVolumes ? "✅" : "❌"}`,
      `- fca_sellers: ${status.hasFcaSellers ? "✅" : "❌"}`,
      `- all_prices: ${status.hasAllPrices ? "✅" : "❌"}`,
      `- bitum_price_new: ${status.hasBitumPriceNew ? "✅" : "❌"}`,
    ];
    const text = [
      `<b>✅ Файл распознан: ${cls.type}</b> (confidence ${cls.confidence.toFixed(2)})`,
      `Сохранён: data/uploads/${week}/${cls.type}.xlsx`,
      `Парсинг: rows=${parseResult.rowCount}, refineries=${parseResult.refineryCount}, errors=${parseResult.errorCount}`,
      `Период: ${parseResult.periodStr}`,
      ``,
      `<b>Неделя ${week} (${status.presentCount}/5):</b>`,
      ...checkListLines,
    ].join("\n");
    await _sendHtml(token, chatId, text);
  } catch (err) {
    const m = (err as Error).message ?? String(err);
    log.error(`[bitum-bot] handleBitumDocument error: ${m}`);
    await _sendPlain(token, chatId, `⚠️ Ошибка: ${m.slice(0, 300)}`);
  }
}

interface PendingLearning {
  buffer: Buffer;
  originalFileName: string;
  meta: ClassifyResult["meta"];
  chatId: number;
  createdAt: number;
}
const pendingLearningByMsgId = new Map<number, PendingLearning>();

interface ParseSummary { rowCount: number; refineryCount: number; errorCount: number; periodStr: string }
async function parseByType(type: KnownBitumType, buffer: Buffer): Promise<ParseSummary> {
  const dict = loadRefineries();
  switch (type) {
    case "birzha_prices": {
      const r = await parseBirzhaPrices(buffer, dict);
      const refs = new Set(r.rows.map((x) => x.refineryCanonical));
      const dates = r.rows.map((x) => x.date);
      return { rowCount: r.rows.length, refineryCount: refs.size, errorCount: r.errors.length, periodStr: dateRange(dates) };
    }
    case "birzha_volumes": {
      const r = await parseBirzhaVolumes(buffer, dict);
      const refs = new Set(r.rows.map((x) => x.refineryCanonical));
      const dates = r.rows.map((x) => x.date);
      return { rowCount: r.rows.length, refineryCount: refs.size, errorCount: r.errors.length, periodStr: dateRange(dates) };
    }
    case "fca_sellers": {
      const r = await parseFcaSellers(buffer, dict);
      const refs = new Set(r.rows.map((x) => x.refineryCanonical));
      const dates = r.rows.map((x) => x.date);
      return { rowCount: r.rows.length, refineryCount: refs.size, errorCount: r.errors.length, periodStr: dateRange(dates) };
    }
    case "all_prices": {
      const r = await parseAllPrices(buffer, dict);
      const refs = new Set(r.rows.map((x) => x.pointOfShipment));
      const dates = r.rows.map((x) => x.date);
      return { rowCount: r.rows.length, refineryCount: refs.size, errorCount: r.errors.length, periodStr: dateRange(dates) };
    }
    case "bitum_price_new": {
      const r = await parseBitumPriceNew(buffer);
      const date = r.rows[0]?.date;
      return { rowCount: r.rows.length, refineryCount: r.rows.length, errorCount: r.errors.length, periodStr: date ? date.toISOString().slice(0,10) : "—" };
    }
  }
}

function dateRange(dates: Date[]): string {
  if (dates.length === 0) return "—";
  const min = dates.reduce((a,b)=>a<b?a:b).toISOString().slice(0,10);
  const max = dates.reduce((a,b)=>a>b?a:b).toISOString().slice(0,10);
  return min === max ? min : `${min}..${max}`;
}

// =============================================================================
// Callback router for `bs:` (signature learn), `bp:` (publish), `br:` (reset).
// =============================================================================

export async function handleBitumCallback(token: string, cb: TgCallbackQuery): Promise<boolean> {
  const data = cb.data ?? "";
  if (!data.startsWith("bs:") && !data.startsWith("bp:") && !data.startsWith("br:")) {
    return false;  // не наш callback
  }
  // ACK
  try { await _tgFetch(token, "answerCallbackQuery", { callback_query_id: cb.id }); } catch {}

  const msgId = cb.message?.message_id;
  if (!msgId || !cb.message) return true;

  if (data.startsWith("bs:")) {
    const choice = data.slice(3);
    const pending = pendingLearningByMsgId.get(msgId);
    if (!pending) {
      await _sendPlain(token, cb.message.chat.id, "Сессия learning истекла (рестарт бота?). Перешлите файл заново.");
      return true;
    }
    pendingLearningByMsgId.delete(msgId);
    if (choice === "not_bitum") {
      await _sendPlain(token, cb.message.chat.id, `Не сохранено: "${pending.originalFileName}" помечен как «не битум».`);
      return true;
    }
    // Append learned signature + save + TG-05 response
    const type = choice as KnownBitumType;
    await appendLearned({
      type,
      a1: pending.meta.a1,
      a3: pending.meta.a3,
      b3: pending.meta.b3,
      sheetName: pending.meta.sheetName,
      learnedAt: new Date().toISOString(),
    });
    const week = isoWeekFolder(new Date());
    await saveUpload(pending.buffer, type, week);
    const parseResult = await parseByType(type, pending.buffer);
    const status = listWeekV5(week);
    const text = [
      `<b>✅ Сохранён как ${type}</b> (learned: A1="${pending.meta.a1 ?? "?"}")`,
      `Сохранён: data/uploads/${week}/${type}.xlsx`,
      `Парсинг: rows=${parseResult.rowCount}, refineries=${parseResult.refineryCount}, errors=${parseResult.errorCount}`,
      ``,
      `<b>Неделя ${week} (${status.presentCount}/5)</b>`,
    ].join("\n");
    await _sendHtml(token, cb.message.chat.id, text);
    return true;
  }

  if (data.startsWith("bp:")) {
    const action = data.slice(3);
    const pending = pendingPublishByMsgId.get(msgId);
    if (!pending) {
      await _sendPlain(token, cb.message.chat.id, "Сессия publish истекла. Перезапустите /bitum_report.");
      return true;
    }
    pendingPublishByMsgId.delete(msgId);
    if (action === "cancel") {
      await _sendPlain(token, cb.message.chat.id, "❌ Отмена: отчёт НЕ опубликован.");
      return true;
    }
    if (action === "confirm") {
      const channelId = process.env[CHANNEL_ID_ENV];
      if (!channelId) {
        await _sendPlain(token, cb.message.chat.id, `❌ ${CHANNEL_ID_ENV} не задан — не могу опубликовать.`);
        return true;
      }
      const parts = chunkBitumHtml(pending.html);
      for (const part of parts) {
        await _tgFetch(token, "sendMessage", { chat_id: channelId, text: part, parse_mode: "HTML", disable_web_page_preview: true });
      }
      await _sendPlain(token, cb.message.chat.id, `✅ Опубликовано в канал (${parts.length} part(s)).`);
      return true;
    }
  }

  if (data.startsWith("br:")) {
    const action = data.slice(3);
    const pending = pendingResetByMsgId.get(msgId);
    if (!pending) {
      await _sendPlain(token, cb.message.chat.id, "Сессия reset истекла. Перезапустите /bitum_reset.");
      return true;
    }
    pendingResetByMsgId.delete(msgId);
    if (action === "cancel") {
      await _sendPlain(token, cb.message.chat.id, "❌ Отмена: файлы НЕ удалены.");
      return true;
    }
    if (action === "confirm") {
      const deleted = resetWeek(pending.week);
      await _sendHtml(token, cb.message.chat.id, `<b>✅ Сброшена неделя ${pending.week}</b>\nУдалено: ${deleted.length === 0 ? "—" : deleted.join(", ")}`);
      return true;
    }
  }
  return true;
}
```

Создать `src/__tests__/bitum-bot-commands.test.ts` (mock tgFetch, mock saveUpload, mock filesystem; 8 кейсов — каждый command + TG-05 happy path).

Создать `src/__tests__/bitum-bot-learning.test.ts` (5 кейсов — learning UX flow: bs:learn type, bs:not_bitum, expired session, expired publish, expired reset).
  </action>
  <verify>
    <automated>npm test && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - File `src/bot-bitum.ts` exists
    - `grep -c "export async function handleBitumStatus" src/bot-bitum.ts` returns 1
    - `grep -c "export async function handleBitumPreview" src/bot-bitum.ts` returns 1
    - `grep -c "export async function handleBitumReport" src/bot-bitum.ts` returns 1
    - `grep -c "export async function handleBitumReset" src/bot-bitum.ts` returns 1
    - `grep -c "export async function handleBitumDocument" src/bot-bitum.ts` returns 1
    - `grep -c "export async function handleBitumCallback" src/bot-bitum.ts` returns 1
    - `grep -c "LEARNING_KEYBOARD\\|bs:birzha_prices\\|bs:not_bitum" src/bot-bitum.ts` returns ≥ 3 (D-14 keyboard)
    - `grep -c "bp:confirm\\|bp:cancel" src/bot-bitum.ts` returns ≥ 2 (D-11 publish)
    - `grep -c "br:confirm\\|br:cancel" src/bot-bitum.ts` returns ≥ 2 (D-12 reset)
    - `grep -c "TG_CHANNEL_ID\\|CHANNEL_ID_ENV" src/bot-bitum.ts` returns ≥ 2
    - `grep -c "classifyFile\\|appendLearned" src/bot-bitum.ts` returns ≥ 2
    - `npx vitest run src/__tests__/bitum-bot-commands.test.ts src/__tests__/bitum-bot-learning.test.ts` exits 0
    - `npm test` exits 0
  </acceptance_criteria>
  <done>bot-bitum.ts с 6 handlers + 3 pending Maps + learning keyboard + publish/reset confirm flows.</done>
</task>

<task id="5.2" type="auto" autonomous="true">
  <name>Task 5.2: Wire src/bot.ts → bot-bitum.ts: экспорт helpers (tgFetch/sendHtml/etc), routing 4 битум-команд + bp:/br:/bs: callbacks + handleBitumDocument в handleDocument flow + расширить setMyCommands до 9 команд + EMOJI_BUTTON_MAP</name>
  <files>src/bot.ts</files>
  <read_first>
    - src/bot.ts (полностью) — найти точки модификации:
      - sendHtml, sendPlain, sendReply, sendReplyWithKeyboard, tgFetch, downloadTgFile — добавить `export`
      - tgInterfaces (TgMessage, TgCallbackQuery, TgInlineKeyboardButton) — добавить `export`
      - handleDocument — после classifier шаг ДОБАВИТЬ ветку: try classify через src/bitum, если match → handleBitumDocument, иначе → legacy handleDocument с detectUploadType
      - handleCommand — добавить cases для `/bitum_status`, `/bitum_preview`, `/bitum_report`, `/bitum_reset`; alias `/summarize`→preview, `/upload_status`→status (wave 6 добавит deprecation msg)
      - handleCallbackQuery — добавить routing: если data startsWith `bs:|bp:|br:` → handleBitumCallback, иначе legacy parseRemoveCallbackData
      - registerBotCommands — расширить с 7 до 9 commands (добавить bitum_status, bitum_preview, bitum_report, bitum_reset)
      - MAIN_KEYBOARD — может остаться 2×2 для chat-каналов; битум-команды через menu, не через keyboard
    - src/bot-bitum.ts (созданный в 5.1 — imports `from "./bot.js"` для tgFetch/sendHtml/etc)
  </read_first>
  <action>
Модификации в `src/bot.ts`:

1) Добавить `export` ко всем функциям и интерфейсам, импортируемым из bot-bitum.ts:
   - `export async function tgFetch<T>(...)` (было private)
   - `export async function sendHtml(...)` (было private)
   - `export async function sendPlain(...)` (было private)
   - `export async function sendReply(...)` (было private)
   - `export async function sendReplyWithKeyboard(...)` (было private)
   - `export async function downloadTgFile(...)` (было private)
   - `export interface TgMessage { ... }` (было private)
   - `export interface TgCallbackQuery { ... }` (было private)
   - `export interface TgInlineKeyboardButton { ... }` (было private)

2) Импорт из bot-bitum.ts вверху файла:
   ```typescript
   import {
     handleBitumStatus, handleBitumPreview, handleBitumReport, handleBitumReset,
     handleBitumDocument, handleBitumCallback,
   } from "./bot-bitum.js";
   ```

3) Модифицировать `handleDocument` — после download buffer, ВСЕГДА dispatch через handleBitumDocument:
   ```typescript
   export async function handleDocument(token, msg, allowlist) {
     // ... existing allowlist check + download ...
     // Заменить ВЕСЬ legacy detect+save+analyze+sendMarkdown блок на:
     await handleBitumDocument(token, msg, buffer, fileName);
   }
   ```
   (Legacy detectUploadType + parseWorkbook + analyze + renderMarkdown ушли вместе с src/upload/ в wave 6 — здесь просто всё перевешиваем на bitum-pipeline.)

4) Модифицировать `handleCommand` — добавить ветки:
   ```typescript
   if (cmd === "/bitum_status")  { await handleBitumStatus(token, msg);  return; }
   if (cmd === "/bitum_preview") { await handleBitumPreview(token, msg); return; }
   if (cmd === "/bitum_report")  { await handleBitumReport(token, msg);  return; }
   if (cmd === "/bitum_reset")   { await handleBitumReset(token, msg);   return; }
   // Aliases — wave 6 добавит deprecation сообщение; пока просто redirect
   if (cmd === "/summarize")     { await handleBitumPreview(token, msg); return; }
   if (cmd === "/upload_status") { await handleBitumStatus(token, msg);  return; }
   ```
   Существующий `if (cmd === "/summarize")` блок (вызывает handleSummarizeCommand) — УДАЛИТЬ (он теперь покрыт alias-веткой выше). Существующий `/upload_status` блок — УДАЛИТЬ (alias выше покрывает).

5) Модифицировать `handleCallbackQuery` — в начале (после ack) ДОБАВИТЬ:
   ```typescript
   // Битум-callbacks routed first (prefixes: bs: / bp: / br:)
   if ((cb.data ?? "").match(/^(bs|bp|br):/)) {
     const handled = await handleBitumCallback(token, cb);
     if (handled) return;
   }
   // Existing /remove_channel callback handling (rm:...) продолжается ниже.
   ```

6) Модифицировать `registerBotCommands` — заменить commands array на:
   ```typescript
   const commands = [
     { command: "start", description: "Запустить бота / показать меню" },
     { command: "help", description: "Инструкция" },
     { command: "bitum_status",  description: "Чек-лист битум-недели (5 типов)" },
     { command: "bitum_preview", description: "Превью битум-отчёта в DM" },
     { command: "bitum_report",  description: "Битум-отчёт с публикацией в канал" },
     { command: "bitum_reset",   description: "Сбросить текущую битум-неделю" },
     { command: "channels", description: "Список каналов" },
     { command: "add_channel", description: "Добавить канал" },
     { command: "remove_channel", description: "Удалить канал" },
   ];
   ```
   (9 команд; `/summarize` и `/upload_status` УБРАНЫ из меню — они остаются как алиасы handler-уровня но не в меню. Это и есть «постепенное депрекейтинг»: алиас работает, но не предлагается.)

7) Модифицировать `EMOJI_BUTTON_MAP` — обновить mappings:
   ```typescript
   const EMOJI_BUTTON_MAP: Record<string, string> = {
     "📊 Статус загрузок": "/bitum_status",
     "🧠 Сделать сводку": "/bitum_preview",
     "📋 Каналы новостей": "/channels",
     "❓ Помощь": "/help",
   };
   ```

8) Удалить НЕИСПОЛЬЗОВАННЫЕ imports из src/upload (legacy detectUploadType, parseWorkbook, renderMarkdown, analyze, buildLlmNarrative, reparseFromDisk). НО! `loadRefineries`, `isoWeekFolder`, `listWeek`, `saveUpload`, `weekDir`, `writeLastRun`, `findLatestWeekWithUploads` всё ещё могут использоваться в legacy handleSummarizeCommand если он останется. РЕШЕНИЕ: УДАЛИТЬ handleSummarizeCommand (его заменяет handleBitumPreview через alias).

После этих изменений src/bot.ts должен компилироваться. Любые dangling imports → удалить.
  </action>
  <verify>
    <automated>npm test && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export async function tgFetch\\|export async function sendHtml\\|export async function sendPlain\\|export async function sendReply\\|export async function downloadTgFile" src/bot.ts` returns ≥ 5
    - `grep -c "export interface TgMessage\\|export interface TgCallbackQuery\\|export interface TgInlineKeyboardButton" src/bot.ts` returns ≥ 3
    - `grep -c "from \"./bot-bitum.js\"" src/bot.ts` returns ≥ 1
    - `grep -c "handleBitumStatus\\|handleBitumPreview\\|handleBitumReport\\|handleBitumReset\\|handleBitumDocument\\|handleBitumCallback" src/bot.ts` returns ≥ 6
    - `grep -c "/bitum_status\\|/bitum_preview\\|/bitum_report\\|/bitum_reset" src/bot.ts` returns ≥ 4
    - `grep -c "command: \"bitum_status\"\\|command: \"bitum_preview\"\\|command: \"bitum_report\"\\|command: \"bitum_reset\"" src/bot.ts` returns ≥ 4
    - `grep -F "(bs|bp|br):" src/bot.ts` returns ≥ 1 (callback routing)
    - `grep -c "handleSummarizeCommand" src/bot.ts` returns 0 (deleted — replaced by alias to handleBitumPreview)
    - `npx tsc --noEmit` exits 0
    - `npm test` exits 0 (existing bot tests + new bitum-bot tests)
  </acceptance_criteria>
  <done>bot.ts роутит 4 битум-команды + bs/bp/br callbacks + handleBitumDocument + setMyCommands 9 команд. handleSummarizeCommand removed.</done>
</task>

<task id="5.3" type="checkpoint:human-verify" gate="blocking">
  <name>Task 5.3: Wave 5 checkpoint: bot commands review</name>
  <files>N/A — human verification checkpoint</files>
  <action>Pause and review what was built in this wave (4 битум-команды + TG-05 + classifier learning UX + publish/reset flows). Operator inspects code, runs tests, gives go/no-go to proceed to next wave.</action>
  <verify>
    <automated>npm test && npx tsc --noEmit</automated>
  </verify>
  <done>Operator types resume-signal to proceed to next wave</done>
  <what-built>Wave 5 bot integration: 4 битум-команды + TG-05 + classifier learning UX + publish/reset confirm flows. Atomic git commit.</what-built>
  <how-to-verify>
    1. `npm test` — все existing + new bitum-bot тесты проходят
    2. `npx tsc --noEmit` — clean
    3. Smoke (если есть testnet TG_BOT_TOKEN): запустить `node --env-file=.env --import tsx src/bot.ts` руками, послать `/bitum_status`, проверить ответ
    4. `grep -F "handleSummarizeCommand" src/` — 0 матчей (удалён)
    5. `git log --oneline -1` — `feat(04): wave 5 bot commands — 4 bitum commands + TG-05 + learning UX`
  </how-to-verify>
  <resume-signal>Type "wave-5-approved"</resume-signal>
</task>

</wave>

<!-- ========================================================================== -->
<!-- WAVE 6 — MIGRATION (delete src/upload/, update imports, deprecation aliases) -->
<!-- ========================================================================== -->

<wave id="6" name="Migration">
<objective>Финальная wave — миграция `src/upload/` → `src/bitum/` (D-05, D-06): создать `src/bitum/refineries.ts` (копия + extend), переименовать handlers в src/bot.ts с импортов src/upload на src/bitum, удалить ВСЮ папку src/upload/, добавить одноразовое deprecation-сообщение для `/summarize`/`/upload_status` алиасов (MIGRATE-02), создать `src/bitum/index.ts` barrel. Wave commit: `feat(04): wave 6 migration — delete src/upload, deprecation aliases for /summarize and /upload_status`.</objective>

<task id="6.1" type="auto" autonomous="true" tdd="true">
  <name>Task 6.1: Создать src/bitum/refineries.ts (копия src/upload/refineries.ts) + обновить импорты в src/bitum/* и src/bot-bitum.ts с "../upload/refineries" на "./refineries" (или "./bitum/refineries" для bot-bitum)</name>
  <files>src/bitum/refineries.ts, src/bitum/parsers/birzha-prices.ts, src/bitum/parsers/birzha-volumes.ts, src/bitum/parsers/fca-sellers.ts, src/bitum/parsers/all-prices.ts, src/bitum/analyzer.ts, src/bitum/reporter.ts, src/bot-bitum.ts, src/bot.ts</files>
  <read_first>
    - src/upload/refineries.ts (existing — копировать 1:1, изменив только import RefineryEntry из ./types на ../upload/types — но сразу подменить на правильный src/bitum/types)
    - src/bitum/types.ts (RefineryEntry уже экспортируется)
  </read_first>
  <action>
1) Создать `src/bitum/refineries.ts` — копия src/upload/refineries.ts с изменением импорта:

```typescript
// src/bitum/refineries.ts — словарь канонических НПЗ + нормализация имён.
// Перенесено из src/upload/refineries.ts (wave 6 migration). API сохраняется 1:1.

import { readFileSync, existsSync } from "node:fs";
import path from "node:path";
import { paths } from "../paths.js";
import type { RefineryEntry } from "./types.js";

interface RefineriesFile { version: number; refineries: RefineryEntry[]; }

export function loadRefineries(): RefineryEntry[] {
  const candidates = [
    path.join(paths.dataDir, "refineries.json"),
    path.resolve("./data/refineries.json"),
  ];
  for (const p of candidates) {
    if (!existsSync(p)) continue;
    const raw = readFileSync(p, "utf8");
    const parsed = JSON.parse(raw) as RefineriesFile;
    if (!Array.isArray(parsed.refineries)) throw new Error(`[refineries] ${p}: missing 'refineries' array`);
    return parsed.refineries;
  }
  throw new Error(`[refineries] refineries.json not found in any of: ${candidates.join(", ")}`);
}

export function normalizeRefinery(raw: string, dict: RefineryEntry[]): string {
  const needle = raw.trim().toLowerCase();
  if (!needle) return raw;
  for (const e of dict) {
    if (e.canonical.toLowerCase() === needle) return e.canonical;
    for (const a of e.aliases) if (a.toLowerCase() === needle) return e.canonical;
  }
  return raw.trim();
}

export function getCompany(canonical: string, dict: RefineryEntry[]): string {
  const needle = canonical.trim().toLowerCase();
  for (const e of dict) {
    if (e.canonical.toLowerCase() === needle) return e.company;
  }
  return "независимые";
}
```

2) Обновить ВСЕ импорты с `"../upload/refineries.js"` или `"../../upload/refineries.js"` на относительные пути в src/bitum/. Найти через grep:

```bash
grep -rn "../upload/refineries\\|../../upload/refineries" src/bitum/ src/bot-bitum.ts src/bot.ts
```

Заменить:
- В `src/bitum/parsers/*.ts`: `from "../../upload/refineries.js"` → `from "../refineries.js"`
- В `src/bitum/analyzer.ts`: `from "../upload/refineries.js"` → `from "./refineries.js"`
- В `src/bitum/reporter.ts`: `from "../upload/refineries.js"` → `from "./refineries.js"`
- В `src/bot-bitum.ts`: `from "./upload/refineries.js"` → `from "./bitum/refineries.js"`
- В `src/bot.ts`: `from "./upload/refineries.js"` → `from "./bitum/refineries.js"` (если ещё используется)
  </action>
  <verify>
    <automated>npm test && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - File `src/bitum/refineries.ts` exists
    - `grep -c "export function loadRefineries\\|export function normalizeRefinery\\|export function getCompany" src/bitum/refineries.ts` returns 3
    - `grep -rn "../upload/refineries\\|../../upload/refineries" src/bitum/ src/bot-bitum.ts` returns 0 matches
    - `grep -rn "./upload/refineries" src/bot.ts` returns 0 matches (only ./bitum/refineries)
    - `npx tsc --noEmit` exits 0
    - `npm test` exits 0
  </acceptance_criteria>
  <done>refineries.ts создан в src/bitum/; все импорты обновлены на src/bitum/refineries.</done>
</task>

<task id="6.2" type="auto" autonomous="true">
  <name>Task 6.2: Создать src/bitum/index.ts (barrel) и добавить deprecation-сообщение для /summarize и /upload_status алиасов (MIGRATE-02)</name>
  <files>src/bitum/index.ts, src/bot.ts</files>
  <read_first>
    - src/bot.ts (handleCommand routing — найти `/summarize` и `/upload_status` alias-ветки, добавить deprecation сообщение перед handler-вызовом)
    - .planning/REQUIREMENTS.md (BITUM-MIGRATE-02 — текст deprecation: «Команда переименована в /bitum_preview. В v6.0 будет удалена.»)
  </read_first>
  <action>
1) Создать `src/bitum/index.ts` — barrel exports для удобства внешних пользователей:

```typescript
// src/bitum/index.ts — public API barrel для битум-pipeline.

export * from "./types.js";
export { BUILT_IN_SIGNATURES, type Signature } from "./signatures.js";
export { classifyFile } from "./classifier.js";
export { loadLearned, appendLearned } from "./learned-signatures.js";
export { loadRefineries, normalizeRefinery, getCompany } from "./refineries.js";
export {
  isoWeekFolder, weekDir, saveUpload, listWeekV5,
  findLatestWeekWithUploads, writeLastRun, resetWeek,
} from "./storage.js";
export {
  deltasFor, volumeTotals, byCompanyFixedOrder, crossCheck,
  type BitumDelta, type BitumCompanyGroup, type BitumVolumeTotals, type CrossCheckWarning,
} from "./analyzer.js";
export {
  renderBitumReport, chunkBitumHtml,
  type ReporterPayload, type ReporterOptions,
} from "./reporter.js";
export {
  buildBitumNarrative, encodeReportForLlm, BITUM_NARRATIVE_SYSTEM_PROMPT,
  type NarrativePayload, type NarrativeResult,
} from "./llm.js";
export {
  parseBirzhaPrices, parseBirzhaVolumes, parseFcaSellers,
  parseAllPrices, parseBitumPriceNew,
} from "./parsers/index.js";
```

2) В `src/bot.ts` — изменить alias-ветки для добавления одноразового deprecation:

```typescript
// In-memory flag — deprecation msg показывается ОДИН раз за рестарт бота на user.
const deprecationShownByUser = new Set<number>();

// ... inside handleCommand:
if (cmd === "/summarize" || cmd === "/upload_status") {
  const newCmd = cmd === "/summarize" ? "/bitum_preview" : "/bitum_status";
  if (!deprecationShownByUser.has(userId)) {
    deprecationShownByUser.add(userId);
    await sendPlain(token, msg.chat.id, `⚠️ Команда ${cmd} переименована в ${newCmd}. В v6.0 будет удалена.`);
  }
  if (cmd === "/summarize")     { await handleBitumPreview(token, msg); return; }
  if (cmd === "/upload_status") { await handleBitumStatus(token, msg);  return; }
}
```
  </action>
  <verify>
    <automated>npm test && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - File `src/bitum/index.ts` exists
    - `grep -c "export \\* from \"./types" src/bitum/index.ts` returns 1
    - `grep -c "classifyFile\\|renderBitumReport\\|loadLearned\\|byCompanyFixedOrder" src/bitum/index.ts` returns ≥ 4 (key exports)
    - `grep -c "deprecationShownByUser" src/bot.ts` returns ≥ 2 (Set + add + has)
    - `grep -F "переименована в" src/bot.ts` returns ≥ 1 (deprecation msg text)
    - `grep -F "В v6.0 будет удалена" src/bot.ts` returns ≥ 1 (MIGRATE-02 wording)
    - `npx tsc --noEmit` exits 0
    - `npm test` exits 0
  </acceptance_criteria>
  <done>bitum/index.ts barrel; bot.ts с одноразовым deprecation messaging per MIGRATE-02.</done>
</task>

<task id="6.3" type="auto" autonomous="true">
  <name>Task 6.3: Удалить src/upload/ полностью + обновить любые оставшиеся импорты в src/* (D-06)</name>
  <files>src/upload/ (ENTIRE DIRECTORY DELETED), src/bot.ts, src/__tests__/upload-*.test.ts (KEEP — but they will fail if they import from src/upload directly — see below)</files>
  <read_first>
    - Все existing src/__tests__/upload-*.test.ts (smoke check — нужно ли их удалить)
    - src/bot.ts (любые dangling imports)
    - tsconfig.json
  </read_first>
  <action>
1) Проверить что нет внешних импортов из src/upload в `src/*` (КРОМЕ test-файлов и самой папки src/upload):

```bash
grep -rn "from \"\\.\\./upload\\|from \"\\.\\./\\.\\./upload\\|from \"\\./upload" src/ | grep -v "src/__tests__/" | grep -v "src/upload/"
```

Этот grep должен вернуть 0 строк (если что-то есть — нужно обновить импорт на src/bitum). Возможные оставшиеся:
- `src/bot.ts`: если есть `from "./upload/storage.js"` для isoWeekFolder/listWeek/saveUpload — заменить на `from "./bitum/storage.js"` (но имена: `listWeek` → `listWeekV5`, signature совпадает кроме типа)

2) Удалить файлы тестов на старый upload (они тестируют то что удалится):

```bash
git rm src/__tests__/upload-analyzer.test.ts
git rm src/__tests__/upload-detect.test.ts
git rm src/__tests__/upload-llm.test.ts
git rm src/__tests__/upload-parser.test.ts
git rm src/__tests__/upload-refineries.test.ts
git rm src/__tests__/upload-renderer.test.ts
git rm src/__tests__/upload-storage.test.ts
git rm src/__tests__/upload-chart.test.ts
```

3) Удалить папку src/upload полностью:

```bash
git rm -r src/upload/
```

4) Запустить `npx tsc --noEmit` — должно быть clean. Если ошибки → найти dangling imports + исправить (наиболее вероятно: `src/bot.ts` всё ещё импортирует loadRefineries из ./upload/refineries → заменить на ./bitum/refineries; isoWeekFolder/listWeek из ./upload/storage → ./bitum/storage с listWeek → listWeekV5).

5) Запустить `npm test` — все ОСТАВШИЕСЯ тесты должны проходить (24+ битум-test файлов + non-upload existing тесты).
  </action>
  <verify>
    <automated>npm test && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Directory `src/upload/` НЕ существует: `test -d src/upload && echo BAD || echo OK` outputs `OK`
    - No upload test files: `ls src/__tests__/upload-*.test.ts 2>/dev/null | wc -l` returns 0
    - No dangling upload imports: `grep -rn "from \"\\.\\./upload\\|from \"\\./upload" src/ 2>/dev/null | wc -l` returns 0
    - `npx tsc --noEmit` exits 0
    - `npm test` exits 0 (все оставшиеся tests passing)
    - `npm run start:once` или `node --import tsx -e "import('./src/bot.js').then(m => console.log('ok'))"` exits 0 (smoke check — модуль грузится)
  </acceptance_criteria>
  <done>src/upload/ DELETED; импорты переведены на src/bitum/; tsc clean; npm test passes.</done>
</task>

<task id="6.4" type="auto" autonomous="true">
  <name>Task 6.4: Обновить CLAUDE.md (Technology Stack / Conventions / Architecture секции с битум-pipeline)</name>
  <files>CLAUDE.md</files>
  <read_first>
    - CLAUDE.md (existing — секции «Technology Stack», «Conventions», «Architecture» пока пустые)
    - .planning/phases/04-bitum-weekly-report/04-CONTEXT.md (canonical refs — какие модули есть)
  </read_first>
  <action>
Заменить в `CLAUDE.md`:

```markdown
## Technology Stack

- Node.js 20.6+ (нужен `--env-file`)
- TypeScript без шага сборки (`tsx`), ESM, `moduleResolution: bundler`, `strict: true`
- Runtime deps: `telegram` (GramJS), `openai` (DeepSeek), `exceljs`, `cheerio`, `fast-xml-parser`, `node-cron`, `zod`
- Test runner: `vitest`

## Conventions

- Pure-функции + dict-аргументом, без module-level singleton'ов (`normalizeRefinery(raw, dict)`, `analyze(prices, fca, volumes, dict)`)
- Lazy client creation для DeepSeek с `temperature: 0`, `maxRetries: 1`, env DEEPSEEK_*
- Атомарная запись через `.tmp + rename` для всех config/state файлов (channels.json, signatures-learned.json, .last-run.json)
- Zod-валидация на output парсеров (`ParserResult<T> = { rows, errors }`)
- HTML output: Telegram parse_mode=HTML, whitelist `<b>`, `<i>`, `<code>`, `<a href>` (БЕЗ `<h1>`/`<hr>`/`<br>`)
- callback_query handler с pending state в Map<msgId, …> (рестарт бота → теряются — acceptable)
- Phase 4 (milestone v5.0): битум-pipeline в `src/bitum/*`; legacy `src/upload/*` удалён

## Architecture

- `src/run.ts` — daemon entry (cron 20:15 MSK + bot polling)
- `src/bot.ts` — Telegram bot polling + command routing
- `src/bot-bitum.ts` — битум-handlers (`/bitum_status`, `/bitum_preview`, `/bitum_report`, `/bitum_reset` + xlsx upload + classifier learning UX)
- `src/bitum/` — milestone v5.0 битум-pipeline:
  - `types.ts` — все типы (BitumType, ClassifyResult, ParsedRow*, ReportResult, NumberTrace, LearnedSignature, WeekStatusV5)
  - `signatures.ts` — built-in TS-таблица 5 known signatures
  - `classifier.ts` — `classifyFile(buffer)` со stepped confidence (A1+A3=1.0, A1=0.7, partial=0.4, none→unknown)
  - `learned-signatures.ts` — append-only `data/bitum/signatures-learned.json` с atomic .tmp+rename + in-process mutex
  - `parsers/` — 5 idempotent parsers с Zod-валидацией: birzha-prices, birzha-volumes, fca-sellers, all-prices, bitum-price-new
  - `refineries.ts` — словарь канонических НПЗ + `normalizeRefinery` + `getCompany`
  - `storage.ts` — ISO-week storage с WeekStatusV5 (5 boolean флагов) + resetWeek
  - `analyzer.ts` — `deltasFor` + `byCompanyFixedOrder` (Роснефть→Газпромнефть→ЛУКОЙЛ→Прочие) + `crossCheck` (REPORT-08, env BITUM_CROSS_CHECK_THRESHOLD)
  - `reporter.ts` — structured HTML по `docs/bitum/algoritm.md` §6 с cell-trace footer (REPORT-07) + partial-render (D-10)
  - `llm.ts` — hybrid scope (D-08): LLM ТОЛЬКО framing, numbers programmatic; `response_format: json_object`
- `src/deliver.ts` — TG канал доставки (`chunkHtml` reuse)
- `src/channels-store.ts` — атомарное хранилище channels.json с mutex
- `data/uploads/<YYYY-Www>/` — 5 битум xlsx per ISO week
- `data/bitum/signatures-learned.json` — append-only learned signatures (создаётся при первом learning event)
- `data/refineries.json` — словарь НПЗ с холдингами (Роснефть/Газпромнефть/ЛУКОЙЛ/Татнефть/независимые)
```
  </action>
  <verify>
    <automated>npm test && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "src/bitum" CLAUDE.md` returns ≥ 5 (architecture references)
    - `grep -c "classifyFile\\|renderBitumReport\\|byCompanyFixedOrder" CLAUDE.md` returns ≥ 2
    - `grep -c "Роснефть.*Газпромнефть.*ЛУКОЙЛ\\|Роснефть→Газпромнефть→ЛУКОЙЛ" CLAUDE.md` returns ≥ 1 (fixed order documented)
  </acceptance_criteria>
  <done>CLAUDE.md обновлён с битум-pipeline architecture.</done>
</task>

<task id="6.5" type="checkpoint:human-verify" gate="blocking">
  <name>Task 6.5: Wave 6 checkpoint: migration complete (PHASE END)</name>
  <files>N/A — human verification checkpoint</files>
  <action>Pause and review what was built in this wave (src/upload deleted + deprecation aliases + final phase verification). Operator inspects code, runs tests, gives go/no-go to proceed to next wave.</action>
  <verify>
    <automated>npm test && npx tsc --noEmit</automated>
  </verify>
  <done>Operator types resume-signal to proceed to next wave</done>
  <what-built>Wave 6 migration COMPLETE: src/upload/ DELETED, всё переведено на src/bitum/, deprecation aliases работают. Atomic git commit + tag.</what-built>
  <how-to-verify>
    1. `npm test` — все tests passing (без upload-*.test.ts которые удалены)
    2. `npx tsc --noEmit` — clean
    3. `test -d src/upload && echo BAD || echo OK` outputs `OK`
    4. `ls src/bitum/` shows: types.ts, signatures.ts, classifier.ts, learned-signatures.ts, refineries.ts, storage.ts, analyzer.ts, reporter.ts, llm.ts, index.ts, parsers/
    5. Smoke (с реальными fixtures из docs/examples/ если есть testnet env):
       - Drop birzha xlsx → bot reports `✅ Файл распознан: birzha_prices` с чек-листом
       - `/bitum_status` — показывает 1/5 после первого файла
       - Send legacy `/summarize` → видит deprecation сообщение «переименована в /bitum_preview» (один раз) + получает превью
       - Send `/bitum_preview` → отчёт в DM
       - Send `/bitum_reset` → inline-keyboard confirm
    6. `git log --oneline -1` — `feat(04): wave 6 migration — delete src/upload, deprecation aliases for /summarize and /upload_status`
    7. **Phase 4 complete!** — все 6 wave commits в git log, all tests green, milestone v5.0 implemented end-to-end
  </how-to-verify>
  <resume-signal>Type "phase-4-complete" — finalize and create 04-01-SUMMARY.md</resume-signal>
</task>

</wave>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Telegram document upload → handleBitumDocument | Allowlisted operator пушит .xlsx из произвольных источников (свои файлы, форварды). Buffer untrusted: malformed xlsx, oversized, ExcelJS crashes. |
| Operator → callback_data (bs:/bp:/br:) | Callback data parsed from string prefix; allowlisted users only (bot.ts:handleCallbackQuery silently ignores non-allowlist). |
| signatures-learned.json → classifier | Disk file controlled by bot (only `appendLearned` writes); corruption falls back to []. |
| DEEPSEEK_API_KEY → OpenAI SDK → LLM | Outbound network — payload sent to DeepSeek contains canonical refinery names + directions, БЕЗ цен (per D-08 jail). |
| TG_CHANNEL_ID publish (D-11) | Operator must press «📤 Опубликовать» — no autoposting in v5.0 (deferred to BITUM-AUTOSEND-01). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Spoofing | Non-allowlist user sends xlsx | mitigate | `src/bot.ts:handleDocument` checks allowlist.has(userId) before any processing; silent ignore + log.info per existing v4.0 pattern (channels-store mutex). |
| T-04-02 | Tampering | Adversarial xlsx (zip bomb, malformed structure) | mitigate | ExcelJS load wrapped in try/catch; parsers return ParserResult with errors[] (BITUM-PARSE-06 — не падают); buffer size limit через Telegram (50MB max documents). |
| T-04-03 | Tampering | Adversarial xlsx triggering false-positive classification | mitigate | Stepped confidence model — A1-only match (0.7) или partial (0.4) → inline-keyboard learning UX (D-14), оператор подтверждает type перед сохранением. False-positive не доходит до парсеров без явного подтверждения. |
| T-04-04 | Repudiation | Operator denies publishing report | accept | Все publish actions logged with timestamp + chat_id + parts count; `/bitum_report` требует явный click «📤 Опубликовать» (D-11); audit trail в data/logs/run-*.log. |
| T-04-05 | Information disclosure | LLM payload содержит конкретные цены / sensitive data | mitigate | encodeReportForLlm (src/bitum/llm.ts) сериализует ТОЛЬКО canonical names + direction (up/down/flat), БЕЗ числовых значений. Жёсткий запрет в BITUM_NARRATIVE_SYSTEM_PROMPT на упоминание чисел в response. |
| T-04-06 | Information disclosure | Reports опубликованы в неправильный чат | mitigate | TG_CHANNEL_ID env var (env-locked); publish flow требует pending Map lookup + confirm callback (D-11) — никакого `/bitum_report` без подтверждения. |
| T-04-07 | DoS | Concurrent classify requests overwhelm ExcelJS | accept | Single operator, single-user pattern (BOT_ALLOWED_USER_IDS ≈ 1-3 users); ExcelJS load это in-process, нет network IO; throughput ограничен Telegram getUpdates 30s long-poll. |
| T-04-08 | DoS | signatures-learned.json unbounded growth | mitigate | Append-only; в-process mutex serializes writes; в practice оператор учит ≈ 1-2 сигнатуры на новый формат → файл < 100 entries за весь жизненный цикл. Hard cap не нужен. |
| T-04-09 | Elevation of privilege | Non-allowlist user sends callback_query (bs:/bp:/br:) | mitigate | `src/bot.ts:handleCallbackQuery` silent ignore до allowlist check; pending Maps key by msg_id который operator не может предсказать. |
| T-04-10 | Information disclosure | resetWeek (BITUM-TG-04) удаляет файлы без backup | accept | Per D-12 — операция явно деструктивная; inline-keyboard confirm минимизирует accidental clicks; operator знает что нет backup. Acceptable risk для single-user pipeline. |
| T-04-11 | Tampering | crossCheck threshold обходится hostile xlsx | accept | Threshold = env BITUM_CROSS_CHECK_THRESHOLD (default 0.01); operator может корректировать; mismatch generates warning, не блокирует отчёт. |
| T-04-12 | Spoofing | Malicious xlsx с правильным A1+A3 → confidence 1.0 → парсится без operator confirm | accept | Per D-14 — confidence 1.0 не требует learning. Если злоумышленник создал xlsx идеально mimicking формат — он его и есть. Этот pipeline доверяет содержимому файла от allowlisted operator (Заказчик); spoofing assumes compromised operator account, что вне scope этой phase. |
</threat_model>

<verification>
End-to-end verification после ВСЕХ 6 wave (executor checkpoint после wave 6.5):

**Automated:**
1. `npm test` — все tests passing (2450+ existing - 8 deleted upload-*.test.ts + 14 new bitum-*.test.ts)
2. `npx tsc --noEmit` — clean, no TypeScript errors
3. `node -e "console.log(JSON.parse(require('fs').readFileSync('data/refineries.json','utf8')).refineries.length)"` returns ≥ 28
4. `test -d src/upload && echo BAD || echo OK` outputs `OK`
5. `ls src/bitum/` shows: types.ts, signatures.ts, classifier.ts, learned-signatures.ts, refineries.ts, storage.ts, analyzer.ts, reporter.ts, llm.ts, index.ts, parsers/
6. `ls src/bitum/parsers/` shows: shared.ts, birzha-prices.ts, birzha-volumes.ts, fca-sellers.ts, all-prices.ts, bitum-price-new.ts, index.ts
7. `grep -rn "from \"\\.\\./upload\\|from \"\\./upload" src/ 2>/dev/null | wc -l` returns 0 (no legacy imports remain)
8. `grep -F "В v6.0 будет удалена" src/bot.ts` returns ≥ 1 (MIGRATE-02 deprecation msg)
9. Smoke: `node --import tsx -e "import('./src/bitum/index.js').then(m => console.log(Object.keys(m).length))"` — должно вернуть ≥ 30 exports
10. Smoke: `node --import tsx -e "import('./src/bitum/classifier.js').then(m => m.classifyFile(require('fs').readFileSync('docs/examples/birzha — цены НПЗ.xlsx'))).then(r => console.log(r.type, r.confidence))"` — должно вернуть `birzha_prices 0.7` или `birzha_prices 1.0`

**Manual UAT (requires testnet TG_BOT_TOKEN):**
11. Bot starts: `node --env-file=.env --import tsx src/run.ts` запускается без ошибок
12. Send `/bitum_status` — bot отвечает «Битум-неделя YYYY-Www \n Чек-лист (0/5):» с 5 ❌
13. Drop `docs/examples/birzha — цены НПЗ_rev.xlsx` → bot отвечает ОДНИМ сообщением: type=birzha_prices, confidence=0.7-1.0, period, rowCount, errors=0, чек-лист (1/5)
14. Drop random non-битум xlsx (e.g. созданный руками с A1="Random text") → bot шлёт inline-keyboard learning с 6 кнопками
15. Click «fca_sellers» в learning keyboard → bot appends to signatures-learned.json + saves as fca_sellers.xlsx + sends TG-05 response
16. Send `/bitum_preview` (с 3+ xlsx) → bot шлёт HTML отчёт с warning «Доступно 3/5 типов» + блоки Роснефть/Газпромнефть/ЛУКОЙЛ/Прочие в фиксированном порядке + «(нет данных)» для missing блоков + footer «Источники:»
17. Send `/bitum_report` → bot шлёт preview + inline-keyboard «📤 Опубликовать / ❌ Отмена»
18. Click «❌ Отмена» → bot отвечает «Отмена: отчёт НЕ опубликован»; в TG_CHANNEL_ID НИЧЕГО не появляется
19. Send `/bitum_report` again → click «📤 Опубликовать» → отчёт появляется в TG_CHANNEL_ID + bot подтверждает «✅ Опубликовано в канал (N part(s))»
20. Send legacy `/summarize` → bot отвечает «⚠️ Команда /summarize переименована в /bitum_preview. В v6.0 будет удалена.» ОДИН раз + затем работает превью; вторая попытка `/summarize` → deprecation msg НЕ показывается, сразу превью
21. Send `/bitum_reset` → inline-keyboard «✅ Сбросить / ❌ Отмена»
22. Click «✅ Сбросить» → bot удаляет все xlsx + .last-run.json в data/uploads/<week>/ + отвечает «✅ Сброшена неделя YYYY-Www \n Удалено: birzha_prices.xlsx, fca_sellers.xlsx, ...»
</verification>

<success_criteria>
Phase 4 (milestone v5.0) считается завершённой когда:

- [ ] ВСЕ 28 BITUM-* requirements имеют at least 1 implementing task in this plan (verified by gsd-plan-checker)
- [ ] 6 atomic git commits в phase branch — по одному на каждую wave (1: foundation, 2: parsers, 3: analyzer+reporter, 4: llm, 5: bot, 6: migration)
- [ ] `src/bitum/` содержит 10+ .ts файлов + parsers/ subdir; `src/upload/` УДАЛЁН
- [ ] `data/refineries.json` расширен per BITUM-REFINERY-01 (≥ 28 canonical entries, все 5 холдингов представлены)
- [ ] 14+ новых unit-тестов добавлены в src/__tests__/bitum-*.test.ts
- [ ] `npm test` exits 0; `npx tsc --noEmit` clean
- [ ] Bot menu (`setMyCommands`) показывает 9 команд (включая 4 битум-, без /summarize и /upload_status)
- [ ] Legacy `/summarize` и `/upload_status` работают как deprecated alias с одноразовым deprecation сообщением (MIGRATE-02)
- [ ] classifier стратегия: stepped confidence (A1+A3=1.0, A1=0.7, partial=0.4, none→unknown); learned signatures merged with built-ins; `data/bitum/signatures-learned.json` создаётся при первом learning event
- [ ] Reporter генерирует HTML по algoritm.md §6 1:1: header (период + LLM summary) → snapshot (БНД средняя) → объёмы биржи top-N → Роснефть → Газпромнефть → ЛУКОЙЛ → Прочие (Татнефть отдельной строкой) → cross-check warnings → footer «Источники» <code>
- [ ] Reporter возвращает `{ html, trace, warnings }` — trace содержит ≥ 1 NumberTrace per число в отчёте (REPORT-07)
- [ ] Partial render (D-10) — при <5 типах warning-блок в начале + «(нет данных)» в местах требующих недостающие файлы
- [ ] Cross-check (REPORT-08) — при diff > BITUM_CROSS_CHECK_THRESHOLD (default 0.01) → warning блок в отчёте
- [ ] LLM scope гибридный (D-08): system-prompt запрещает числа; response_format: json_object возвращает { topSummary, rosneft, gazpromneft, lukoil, others }; reporter подставляет в `framingSentences`
- [ ] `/bitum_report` flow: preview в DM + inline-keyboard publish/cancel; публикация в TG_CHANNEL_ID ТОЛЬКО после явного click «📤 Опубликовать»
- [ ] `/bitum_reset` flow: inline-keyboard confirm; на confirm — удаление xlsx + .last-run.json в current week, без backup
- [ ] TG-05 single message при xlsx upload: type+confidence+meta+rowCount+refineryCount+errorCount+period+5-item чек-лист
- [ ] D-14 learning UX: при confidence < 1 или type=unknown — inline-keyboard «5 types + не битум» вместо save; на выбор type → append to signatures-learned.json + save + TG-05 response
- [ ] BITUM-CLS-04: classifyFile принимает только buffer аргумент (`classifyFile.length === 1`), имя файла НЕ передаётся
- [ ] Existing v4.0 функциональность (TG digest 20:15 MSK, web scraping, /channels/add_channel/remove_channel) не сломана: `npm start` запускается; `/channels` команда работает
</success_criteria>

<output>
After completion of all 6 waves, create `.planning/phases/04-bitum-weekly-report/04-01-SUMMARY.md` per template:

- Phase: 04-bitum-weekly-report
- Plan: 01
- Outcome: milestone v5.0 implemented end-to-end
- Affects: ["src/bitum/", "src/bot.ts", "src/bot-bitum.ts", "data/refineries.json", "data/bitum/"]
- Provides: ["classifyFile", "5 битум parsers", "renderBitumReport with trace", "byCompanyFixedOrder", "buildBitumNarrative", "4 битум-команды бота", "TG-05 upload response", "classifier learning UX", "publish/reset confirm flows"]
- Tech stack: existing + zod (already in package.json)
- Decisions made: stepped confidence model (A1+A3=1.0, A1=0.7, partial=0.4); all_prices парсит «исходник» вкладку с code-side агрегацией; cross-check threshold env BITUM_CROSS_CHECK_THRESHOLD default 0.01; signatures-learned.json — flat array
- Patterns: pure-функции с dict-аргументом; atomic .tmp+rename + mutex; Zod-валидация output; HTML whitelist <b>/<i>/<code>/<a>; в-memory pending Maps for confirm flows; одноразовый deprecation messaging per-user via Set
- Cost: ~6 hours total Claude execution time (1h per wave on average; wave 3 reporter + wave 5 bot commands самые тяжёлые)
- Coverage: 28/28 BITUM-* requirements
</output>
