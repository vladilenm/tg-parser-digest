---
phase: 4
plan: 1
title: Битумный недельный отчёт — упрощённый pipeline (full rewrite)
type: execute
wave: 1
depends_on: []
autonomous: true
files_modified:
  - src/bitum/types.ts
  - src/bitum/storage.ts
  - src/bitum/refineries.ts
  - src/bitum/parsers/birzha-volumes.ts
  - src/bitum/parsers/birzha-prices.ts
  - src/bitum/parsers/fca-sellers.ts
  - src/bitum/parsers/bitum-price-new.ts
  - src/bitum/parsers/shared.ts
  - src/bitum/parsers/index.ts
  - src/bitum/analyzer.ts
  - src/bitum/reporter.ts
  - src/bitum/manual-numbers.ts
  - src/bitum/index.ts
  - src/bot-bitum.ts
  - src/bot.ts
  - src/__tests__/bitum-storage.test.ts
  - src/__tests__/bitum-refineries.test.ts
  - src/__tests__/bitum-manual-numbers.test.ts
  - src/__tests__/bitum-parser-birzha-volumes.test.ts
  - src/__tests__/bitum-parser-birzha-prices.test.ts
  - src/__tests__/bitum-parser-fca-sellers.test.ts
  - src/__tests__/bitum-parser-bitum-price-new.test.ts
  - src/__tests__/bitum-analyzer.test.ts
  - src/__tests__/bitum-reporter.test.ts
  - src/__tests__/bitum-bot.test.ts
  - data/refineries.json
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
requirements:
  - BITUM-PARSE-01
  - BITUM-PARSE-02
  - BITUM-PARSE-03
  - BITUM-PARSE-05
  - BITUM-PARSE-06
  - BITUM-REFINERY-01
  - BITUM-REFINERY-02
  - BITUM-REPORT-01
  - BITUM-REPORT-06
  - BITUM-REPORT-07
  - BITUM-REPORT-08
  - BITUM-TG-01
  - BITUM-TG-03
  - BITUM-TG-04
  - BITUM-TG-05
  - BITUM-TG-08
  - BITUM-MIGRATE-01
  - BITUM-MIGRATE-02
  - BITUM-MIGRATE-03
must_haves:
  truths:
    - "Каждая загрузка xlsx в DM вызывает inline-keyboard с 4 кнопками типов + кнопкой «❌ Отмена/Не битум»; никакой автоклассификации нет"
    - "После тапа кнопки файл сохраняется как data/uploads/<ISO-week>/<type>.xlsx и парсится соответствующим парсером; ответ оператору содержит чек-лист 4 типов"
    - "/bitum_status показывает чек-лист текущей ISO-недели (4 boolean + список ручных чисел)"
    - "/bitum_report строит programmatic-дайджест и шлёт превью в DM с кнопками «📤 Опубликовать / ❌ Отмена»; публикация в TG_CHANNEL_ID только после Опубликовать"
    - "/bitum_reset обнуляет текущую ISO-неделю (xlsx + manual-numbers.json) только после подтверждения"
    - "/bitum_add label=value добавляет запись в manual-numbers.json текущей недели; ручные числа появляются в дайджесте отдельным блоком после period header"
    - "Дайджест не использует DeepSeek; все строки — programmatic шаблоны; src/bitum/llm.ts отсутствует"
    - "Все парсеры идемпотентны, возвращают ParserResult<T> = { rows, errors }, не падают на битых строках"
    - "Cell-trace footer присутствует в каждом дайджесте; partial-render warning появляется когда загружены не все 4 типа"
    - "Cross-check между bitum_price_new и birzha_prices/fca_sellers выдаёт warning при расхождении > BITUM_CROSS_CHECK_THRESHOLD"
    - "Если оператор не нажал «📤 Опубликовать» / «❌ Отмена» на превью /bitum_report в течение 15 минут, preview-сессия истекает; для публикации требуется повторный /bitum_report"
  artifacts:
    - path: "src/bitum/types.ts"
      provides: "BitumType union (4 типа), ParsedRow*, ParserResult<T>, WeekStatus, AnalysisResult, ReportResult, NumberTrace, ManualNumber"
      contains: "export type BitumType"
    - path: "src/bitum/storage.ts"
      provides: "isoWeekFolder, weekDir, savedTypes, getWeekStatus, saveXlsx, resetWeek, findLatestWeekWithUploads"
      exports: ["isoWeekFolder", "saveXlsx", "resetWeek", "getWeekStatus"]
    - path: "src/bitum/manual-numbers.ts"
      provides: "addManualNumber, listManualNumbers, clearManualNumbers (atomic .tmp+rename + in-process mutex)"
      exports: ["addManualNumber", "listManualNumbers", "clearManualNumbers"]
    - path: "src/bitum/refineries.ts"
      provides: "loadRefineriesDict, normalizeRefinery (dict-аргументом, pure)"
      exports: ["loadRefineriesDict", "normalizeRefinery"]
    - path: "src/bitum/parsers/birzha-volumes.ts"
      provides: "parseBirzhaVolumes(buffer, dict) => ParserResult<ParsedVolumeRow>"
      exports: ["parseBirzhaVolumes"]
    - path: "src/bitum/parsers/birzha-prices.ts"
      provides: "parseBirzhaPrices(buffer, dict) => ParserResult<ParsedPriceRow>"
      exports: ["parseBirzhaPrices"]
    - path: "src/bitum/parsers/fca-sellers.ts"
      provides: "parseFcaSellers(buffer, dict) => ParserResult<ParsedFcaRow>"
      exports: ["parseFcaSellers"]
    - path: "src/bitum/parsers/bitum-price-new.ts"
      provides: "parseBitumPriceNew(buffer, dict) => ParserResult<ParsedBitumPriceNewRow>"
      exports: ["parseBitumPriceNew"]
    - path: "src/bitum/analyzer.ts"
      provides: "analyzeBitum(parsed, dict) => AnalysisResult с плоским списком movements + cross-check warnings"
      exports: ["analyzeBitum"]
    - path: "src/bitum/reporter.ts"
      provides: "buildReport(analysis, manualNumbers, weekStatus) => ReportResult { html, trace }"
      exports: ["buildReport"]
    - path: "src/bot-bitum.ts"
      provides: "handleBitumDocument, handleBitumStatus, handleBitumReport, handleBitumReset, handleBitumAdd, handleBitumCallback"
      exports: ["handleBitumDocument", "handleBitumStatus", "handleBitumReport", "handleBitumReset", "handleBitumAdd", "handleBitumCallback"]
  key_links:
    - from: "src/bot.ts:handleDocument"
      to: "src/bot-bitum.ts:handleBitumDocument"
      via: "сразу после downloadTgFile"
      pattern: "handleBitumDocument\\("
    - from: "src/bot.ts:handleCommand"
      to: "src/bot-bitum.ts:{handleBitumStatus,handleBitumReport,handleBitumReset,handleBitumAdd}"
      via: "switch по cmd для 4 битум-команд"
      pattern: "/bitum_(status|report|reset|add)"
    - from: "src/bot.ts:handleCallbackQuery"
      to: "src/bot-bitum.ts:handleBitumCallback"
      via: "prefix-routing bu:/br:/brs:"
      pattern: "handleBitumCallback\\("
    - from: "src/bot-bitum.ts:handleBitumReport"
      to: "src/bitum/reporter.ts:buildReport"
      via: "сборка previewHtml перед sendHtml в DM"
      pattern: "buildReport\\("
---

<objective>
Полный rewrite битум-pipeline (milestone v5.1 / "simplified bitum"). Удаляем целиком старую реализацию (`src/bitum/*`, `src/bot-bitum.ts`, 18 vitest-файлов битум-тестов, прошлые ISO-недели в `data/uploads/`, `data/bitum/`), пишем заново под упрощённую модель:
- 4 фиксированных типа файлов вместо 5+unknown
- always-ask UX (5-кнопочный inline-keyboard на каждую загрузку) вместо автоклассификации с learning-loop
- 4 команды (`/bitum_status`, `/bitum_report`, `/bitum_reset`, `/bitum_add`) без `/bitum_preview`
- programmatic-дайджест без LLM, плоский список движений (нет группировки по холдингам)
- ручные числа из `/bitum_add` сохраняются в `manual-numbers.json` и выводятся отдельным блоком после period header
- cell-trace footer и partial-render warning остаются (D-12 §7, D-13)
- cross-check «Битум прайс» vs три других файла (`BITUM_CROSS_CHECK_THRESHOLD`, default 1%)

Purpose: операция Заказчика — присылать 4 xlsx в произвольном порядке в DM боту, тапать кнопку «что это», в конце недели жмёт `/bitum_report`, получает превью в DM, подтверждает кнопкой — публикация в `TG_CHANNEL_ID`. Никакого LLM, никаких группировок по холдингам, никакого signatures-каталога.

Output:
- `src/bitum/` — 8 новых файлов (types, storage, manual-numbers, refineries, 4 парсера, analyzer, reporter, index)
- `src/bot-bitum.ts` — переписан с нуля под always-ask UX
- `src/bot.ts` — bitum-related ветки переписаны (handleDocument, handleCommand, handleCallbackQuery, setMyCommands, /start, /help)
- 10 новых vitest-файлов (по одному на парсер + storage + refineries + manual-numbers + analyzer + reporter + bot)
- `data/refineries.json` — расширен под покрытие 4 эталонных файлов
- `.planning/REQUIREMENTS.md` / `.planning/ROADMAP.md` — синхронизированы со scope (CLAUDE.md sync deferred to separate `/gsd-quick` после execute-phase)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-bitum-weekly-report/04-CONTEXT.md
@.planning/phases/04-bitum-weekly-report/04-DISCUSSION-LOG.md
@docs/bitum/algoritm.md
@CLAUDE.md
@src/deliver.ts
@src/channels-store.ts
@src/paths.ts
@src/logger.ts
@src/bot.ts
@data/refineries.json
@docs/examples/birzha — суточная по НПЗ.xlsx
@docs/examples/birzha — цены НПЗ.xlsx
@docs/examples/BITUM — таблица продавцы.xlsx
@docs/examples/bitum_price — Сводная таблица_new.xlsx

<interfaces>
<!-- Контракты, которые executor должен реализовать. Эти типы фиксируются в Task 2; -->
<!-- последующие задачи импортируют из 'src/bitum/types.js'. -->

```typescript
// src/bitum/types.ts — FIXED contracts, не менять без апдейта плана.

export type BitumType =
  | "birzha_volumes"     // «Биржа суточная по NPZ»
  | "birzha_prices"      // «Биржа цены NPZ»
  | "fca_sellers"        // «Битум таблица продавцы»
  | "bitum_price_new";   // «Битум прайс»

export const BITUM_TYPES: readonly BitumType[] = [
  "birzha_volumes",
  "birzha_prices",
  "fca_sellers",
  "bitum_price_new",
] as const;

// Литералы для inline-keyboard (видны Заказчику) — D-05.
export const BITUM_BUTTON_LABELS: Record<BitumType, string> = {
  birzha_volumes:  "Биржа суточная по NPZ",
  birzha_prices:   "Биржа цены NPZ",
  fca_sellers:     "Битум таблица продавцы",
  bitum_price_new: "Битум прайс",
};

export interface ParserError {
  rowNum: number;       // 1-based; 0 = header / file-level error
  reason: string;
}

export interface ParserResult<T> {
  rows: T[];
  errors: ParserError[];
  // Метаданные для cell-trace footer (D-12 §7).
  meta: {
    fileType: BitumType;
    sheetName: string;
    cellRange: string;   // e.g. "B4:T18"
    rowsCount: number;
  };
}

export interface ParsedVolumeRow {
  date: string;          // ISO YYYY-MM-DD
  refineryCanonical: string;
  refineryRaw: string;
  volumeT: number;       // тонны (исходное * 1000)
}

export interface ParsedPriceRow {
  date: string;
  refineryCanonical: string;
  refineryRaw: string;
  priceRub: number;      // ₽/тонн (исходное * 1000)
}

export interface ParsedFcaRow {
  date: string;          // последняя дата периода (D1 из rev-формата)
  refineryCanonical: string;
  refineryRaw: string;
  region: string;
  pointOfShipment: string;
  priceRub: number;
  deltaWeek: number;     // E-D (формула из rev), может быть 0
}

export interface ParsedBitumPriceNewRow {
  date: string;
  refineryCanonical: string;
  refineryRaw: string;
  company: string;       // прямо из xlsx-колонки, не lookup
  priceRub: number;      // F-колонка
  deltaWeek: number;     // G-колонка
}

export type ParsedByType = {
  birzha_volumes:  ParsedVolumeRow[] | null;
  birzha_prices:   ParsedPriceRow[] | null;
  fca_sellers:     ParsedFcaRow[] | null;
  bitum_price_new: ParsedBitumPriceNewRow[] | null;
};

export interface WeekStatus {
  week: string;          // "2026-W19"
  present: Record<BitumType, boolean>;
  manualNumbersCount: number;
  lastUpdatedAt: string | null;  // ISO; null если папка пуста
}

export interface ManualNumber {
  label: string;
  value: string;
  addedAt: string;       // ISO
}

export interface PriceMovement {
  refineryCanonical: string;
  refineryRaw: string;
  priceFrom: number | null;
  priceTo: number;
  deltaAbs: number;
  deltaPct: number | null;
  source: "birzha" | "fca" | "bitum_price_new";
  // Cell-trace для каждого числа (D-12 §7).
  trace: { fileType: BitumType; sheet: string; cell: string }[];
}

export interface CrossCheckIssue {
  refineryCanonical: string;
  bitumPriceValue: number;
  otherSource: "birzha" | "fca";
  otherValue: number;
  deltaPct: number;          // абсолютный % расхождения
}

export interface VolumeAggregate {
  refineryCanonical: string;
  refineryRaw: string;
  sumT: number;
}

export interface AnalysisResult {
  period: { from: string; to: string };  // ISO даты
  volumes: { totalT: number; byRefinery: VolumeAggregate[] };
  movements: PriceMovement[];            // плоский список, sorted by |deltaAbs| desc (D-11 default)
  crossCheck: CrossCheckIssue[];
  available: Record<BitumType, boolean>;
  thresholdPct: number;                  // из ENV BITUM_CROSS_CHECK_THRESHOLD
}

export interface ReportTrace {
  // Один элемент per file — компактная версия D-12 §7 default.
  fileType: BitumType;
  sheet: string;
  cellRange: string;
  numbersCount: number;
}

export interface ReportResult {
  html: string;          // готов к sendHtml + chunkHtml
  trace: ReportTrace[];  // дублируется в footer html, но и вне html для тестов
}
```

</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Telegram DM → бот (`handleDocument`) | Untrusted xlsx-файл от allowlist-юзера: zip-bomb, malformed sheets, гигантский размер |
| Telegram DM → бот (`handleBitumAdd`) | Свободный текст label=value: HTML-инъекция в дайджест, control-chars, гигантская длина |
| `callback_query` от inline-keyboard | Может быть подделан не-allowlist юзером через известный msg_id; уже фильтруется в `handleCallbackQuery` allowlist-check (см. `src/bot.ts:637-640`) — переиспользуем |
| Filesystem write (`data/uploads/<week>/`) | path traversal через type-имя, race condition между concurrent uploads |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01 | Tampering | `handleBitumDocument` xlsx upload | high | mitigate | Task 9 (`handleBitumDocument`): жёсткий лимит размера BITUM_MAX_XLSX_BYTES=10MB (env, default 10*1024*1024); проверка перед `downloadTgFile`. Превышение → reply «Файл слишком большой (>10 МБ), отклонено» + log.warn без сохранения. |
| T-04-02 | DoS | xlsx с миллионами строк / zip-bomb | high | mitigate | Tasks 6a-6d (парсеры) + `src/bitum/parsers/shared.ts` (общий cap): после `ExcelJS.Workbook.xlsx.load(buffer)` проверять `worksheet.rowCount <= BITUM_MAX_ROWS` (env, default 2000); превышение → ParserError {rowNum:0, reason:"too many rows"} + парсер early-return. Task 9 (`handleBitumDocument`) уже выполнил byte-size pre-check (T-04-01) до load. exceljs сам стримит — zip-bomb до load не пройдёт rowCount-чек. |
| T-04-03 | Tampering | path traversal через type-имя в saveXlsx | medium | mitigate | Task 3 (`saveXlsx`): type приходит ТОЛЬКО из BITUM_TYPES literal union; перед `path.join` ассерт `if (!BITUM_TYPES.includes(type)) throw`; имя файла фикс `${type}.xlsx`, никакого пользовательского ввода в filename. |
| T-04-04 | Tampering / DoS | race между concurrent /bitum_add | medium | mitigate | Task 4 (`manual-numbers.ts`): in-process Promise-chain mutex (паттерн `src/channels-store.ts:71-86`) сериализует все write-операции. Atomic `.tmp + rename`. |
| T-04-05 | Tampering | race между concurrent xlsx upload + /bitum_reset | medium | mitigate | Task 3 (`storage.ts`): один in-process mutex per week ключуется по weekFolder; resetWeek + saveXlsx сериализуются через тот же `withLock`. |
| T-04-06 | Information disclosure | XSS / HTML-инъекция через /bitum_add label=value в дайджест канала | high | mitigate | Task 4 (`manual-numbers.ts:addManualNumber` sanitize): на write — strip control chars (`\x00-\x1F\x7F`), trim, max 200 chars каждое поле. Task 8 (`reporter.ts:escapeHtml`): рендер `<b>${escapeHtml(label)}:</b> ${escapeHtml(value)}` обязательно через util `escapeHtml(s) => s.replace(/&/g,"&amp;").replace(/</g,"&lt;").replace(/>/g,"&gt;")`. |
| T-04-07 | Information disclosure | TG_BOT_TOKEN / TG_CHANNEL_ID попадает в HTML дайджеста или log при ошибке | medium | mitigate | Task 8 (`reporter.ts`): cell-trace footer содержит только relative file paths (нет cwd, нет токенов). Task 9 (`bot-bitum.ts` handlers): при отлове ошибки `sendPlain(... "Ошибка: " + err.message.slice(0,300))` — НЕ включаем token. `tgFetch` уже не логирует body request'а (см. `src/bot.ts:151-168`). |
| T-04-08 | Spoofing | callback_query от не-allowlist user (подделка msg_id) | low | accept | Уже митигировано `handleCallbackQuery` allowlist-check в `src/bot.ts:637`. Никаких изменений в этой проверке. |
| T-04-09 | DoS | flood `/bitum_report` / `/bitum_add` от allowlist-юзера | low | accept | Allowlist малый (1–2 человека), trust-model = bot-allowlist trusted. Rate-limit out of scope (как и в Phase 2 для /add_channel). |
| T-04-10 | Repudiation | оператор отрицает что нажал /bitum_publish | low | accept | Все publish-события логируются `log.info` с userId + msgId + previewHash; persistent-log в `data/logs/run-*.log` (см. `src/logger.ts:25-33`). Audit-trail на уровне log файла. |
</threat_model>

<tasks>

<!-- ============================================================================ -->
<!-- WAVE 1 — CLEANUP (sequential, ровно одна задача, должна закончиться до Wave 2) -->
<!-- ============================================================================ -->

<task type="auto" wave="1">
  <name>Task 1: Полное удаление старой битум-реализации (D-03)</name>
  <files>
    src/bitum/ (rm -rf),
    src/bot-bitum.ts (rm),
    src/__tests__/bitum-*.test.ts (18 files rm),
    data/uploads/2026-W19/ (rm -rf),
    data/uploads/2026-W21/ (rm -rf),
    data/bitum/ (rm -rf если есть),
    docs/examples/цены битум все 08.05-15.05.xlsx (rm),
    docs/examples/цены битум все 08.05-15.05_rev.xlsx (rm),
    src/bot.ts (вычистить только bitum-related блоки — см. action)
  </files>
  <read_first>
    - `src/bot.ts` (read full — нужно увидеть все bitum-ветки перед удалением)
    - `src/bot-bitum.ts` (read первые ~100 строк чтобы убедиться что это именно тот файл который удаляем)
    - `.planning/phases/04-bitum-weekly-report/04-CONTEXT.md` (D-03 — список того что удаляется)
    - `CLAUDE.md` (узнать что в architecture-блоке упоминается старая структура)
  </read_first>
  <action>
    Цель: после Task 1 в репо нет старого битум-кода и старых битум-тестов. Новые файлы создаются с нуля в Wave 2+.

    Конкретно:
    1. `rm -rf src/bitum` (весь каталог)
    2. `rm src/bot-bitum.ts`
    3. `rm src/__tests__/bitum-*.test.ts` — все 18 файлов одной командой через `rm src/__tests__/bitum-*.test.ts`
    4. `rm -rf data/uploads/2026-W19 data/uploads/2026-W21`
    5. `rm -rf data/bitum` (если существует)
    6. `rm "docs/examples/цены битум все 08.05-15.05.xlsx" "docs/examples/цены битум все 08.05-15.05_rev.xlsx"` (D-02: тип all_prices целиком удаляется вместе с эталонными файлами)
    7. **`src/bot.ts` — вычистить bitum-related ТОЛЬКО следующие куски (не трогать /channels, /add_channel, /remove_channel, callback_query для `rm:` префикса, polling-loop, parseAllowlist, normalizeUsername, deliver-helpers):**
       - Удалить импорты строк 7-15: `import { handleBitum* } from "./bot-bitum.js"` и `import { isoWeekFolder } from "./bitum/storage.js"`
       - Удалить `handleDocument` функцию полностью (строки ~289-360 — определить точные границы при чтении файла) — она будет переписана в Task 11
       - Удалить `currentMskWeek()` helper (строки ~279-283) — будет заменён в Task 11 импортом из `src/bitum/storage.js`
       - Удалить функцию `sendMarkdown()` (строки ~232-247) — старый upload-pipeline помечен deprecated, не используем
       - В `handleCommand` удалить ветки: `/bitum_status`, `/bitum_preview`, `/bitum_report`, `/bitum_reset`, `/upload_status`, `/summarize` (с deprecation-сообщением) — оставить только /channels/add/remove/start/help. Также убрать строки `const deprecationShownByUser = new Set<number>()` и `EMOJI_BUTTON_MAP` записи `📊 Статус загрузок` / `🧠 Сделать сводку` (заменим в Task 11)
       - В `handleCallbackQuery` удалить блок `if (/^(bs|bp|br):/.test(data)) { ... handleBitumCallback ... }` (строки ~643-646) — будет заменён в Task 11
       - В `registerBotCommands` удалить все 4 bitum-команды из массива `commands` (строки ~768-774) — будут заменены в Task 11
       - В `handleHelp` (внутри `/help` ветки строки ~577-588) — оставить только non-bitum пункты, временно сократить helpText до строк про /channels/add/remove (полный help переписывается в Task 11)
       - **НЕ удалять** `MAIN_KEYBOARD`, `sendReply`, `sendReplyWithKeyboard`, `sendPlain`, `sendHtml`, `tgFetch`, `downloadTgFile`, `pollLoop`, `startBot`, `parseAllowlist`, `normalizeUsername`, CRUD-обёртки `listChannels`/`addChannel`/`removeChannel` — это инфраструктура для каналов и базовых команд.
    8. **Удалить `src/__tests__/bitum-bot-commands.test.ts`** уже включено в шаг 3, но если зелёный тест `bot-handlers.test.ts` ссылается на удалённые экспорты — поправить тест минимально (вырезать только bitum-related кейсы).

    После всех `rm` запустить `npx tsc --noEmit` — если компиляция не проходит из-за residual references в `src/bot.ts` или других файлах — починить (typically `src/run.ts` тоже может импортировать bitum — проверить и удалить если есть).

    После Task 1 репо должен:
    - Компилироваться: `npx tsc --noEmit` exit 0
    - Не иметь битум-тестов: `ls src/__tests__/bitum-*.test.ts` returns "No such file"
    - Иметь зелёные не-битум тесты: `npx vitest run --reporter=basic` exit 0
  </action>
  <verify>
    <automated>
      test ! -d src/bitum &&
      test ! -f src/bot-bitum.ts &&
      test $(ls src/__tests__/bitum-*.test.ts 2>/dev/null | wc -l) -eq 0 &&
      test ! -d data/uploads/2026-W19 &&
      test ! -d data/uploads/2026-W21 &&
      test ! -d data/bitum &&
      test ! -f "docs/examples/цены битум все 08.05-15.05.xlsx" &&
      grep -L "handleBitum\|src/bitum/" src/bot.ts &&
      npx tsc --noEmit &&
      npx vitest run --reporter=basic
    </automated>
  </verify>
  <acceptance_criteria>
    - `ls src/bitum` returns "No such file or directory"
    - `ls src/bot-bitum.ts` returns "No such file"
    - `ls src/__tests__/bitum-*.test.ts 2>/dev/null | wc -l` returns 0
    - `ls data/uploads/2026-W19 data/uploads/2026-W21 data/bitum 2>/dev/null | wc -l` returns 0
    - `grep -c "handleBitum" src/bot.ts` returns 0
    - `grep -c "src/bitum/" src/bot.ts` returns 0
    - `grep -c "/bitum_" src/bot.ts` returns 0 (старых веток нет — новые добавятся в Task 11)
    - `grep -c "/summarize\|/upload_status" src/bot.ts` returns 0 (D-08 / migration: алиасы deprecated убираем сразу — заказчик хочет clean start)
    - `npx tsc --noEmit` exit code 0
    - `npx vitest run --reporter=basic` exit code 0
  </acceptance_criteria>
  <done>Старый битум-код удалён, репо компилируется, не-битум vitest зелёные. Готова чистая база для Wave 2.</done>
</task>

<!-- ============================================================================ -->
<!-- WAVE 2 — FOUNDATION (types, storage, refineries, manual-numbers) -->
<!-- Все 4 задачи depends_on Task 1; внутри Wave 2 — sequential, потому что Task 3-5 -->
<!-- импортируют типы из Task 2. -->
<!-- ============================================================================ -->

<task type="auto" wave="2" tdd="true">
  <name>Task 2: src/bitum/types.ts — все контракты</name>
  <files>src/bitum/types.ts</files>
  <read_first>
    - `.planning/phases/04-bitum-weekly-report/04-01-PLAN.md` (этот файл, секция `<interfaces>`)
    - `.planning/phases/04-bitum-weekly-report/04-CONTEXT.md` (D-01, D-19)
    - `src/types.ts` (project conventions — узнать что используется ESM .js extension в импортах)
  </read_first>
  <behavior>
    - Файл экспортирует ровно те 14 типов и 2 const, которые перечислены в `<interfaces>` блоке плана
    - `BITUM_TYPES` — readonly tuple длины 4 в фиксированном порядке: birzha_volumes, birzha_prices, fca_sellers, bitum_price_new
    - `BITUM_BUTTON_LABELS` — Record<BitumType, string> с точными UI-литералами из D-05
    - `BitumType` union содержит ровно 4 строковых литерала (никакого `all_prices`, `unknown`)
    - tsc на этот файл compile без warnings
  </behavior>
  <action>
    Создать `src/bitum/types.ts` точно по спецификации из секции `<interfaces>` плана. Скопировать буквально все exported types/const/interfaces из блока interfaces в файл. Финальная проверка:

    - `BITUM_TYPES` использует `as const` для type-narrowing
    - Все интерфейсы экспортируются (export interface / export type)
    - В импортах внутри файла нет внешних зависимостей (только TS-структура)
    - В конце файла НЕТ default export, только named.

    После создания: `npx tsc --noEmit` exit 0.
  </action>
  <verify>
    <automated>
      npx tsc --noEmit &&
      grep -c "export type BitumType" src/bitum/types.ts &&
      grep -c "export const BITUM_TYPES" src/bitum/types.ts &&
      grep -c "export const BITUM_BUTTON_LABELS" src/bitum/types.ts &&
      grep -c "export interface ParserResult" src/bitum/types.ts &&
      grep -c "export interface PriceMovement" src/bitum/types.ts &&
      ! grep -q "all_prices\|unknown" src/bitum/types.ts
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "BitumType" src/bitum/types.ts` >= 5 (тип используется во многих интерфейсах)
    - `grep -c '"birzha_volumes"' src/bitum/types.ts` >= 2 (в union + в BITUM_TYPES + в BITUM_BUTTON_LABELS)
    - `! grep -q "all_prices" src/bitum/types.ts` — нет упоминания удалённого типа
    - `! grep -q "WeekStatusV5\|LearnedSignature\|ClassifyResult" src/bitum/types.ts` — нет упоминания удалённых типов из старой реализации
    - `npx tsc --noEmit` exit 0
  </acceptance_criteria>
  <done>types.ts создан, компилируется, содержит ровно те типы из interfaces-блока плана.</done>
</task>

<task type="auto" wave="2" tdd="true">
  <name>Task 3: src/bitum/storage.ts — ISO-week storage + per-week mutex</name>
  <files>src/bitum/storage.ts, src/__tests__/bitum-storage.test.ts</files>
  <read_first>
    - `src/bitum/types.ts` (только что создан Task 2 — WeekStatus, BitumType, BITUM_TYPES)
    - `src/channels-store.ts` (паттерн in-process mutex + atomic .tmp + rename)
    - `src/paths.ts` (paths.dataDir resolver)
    - `.planning/phases/04-bitum-weekly-report/04-CONTEXT.md` (D-19, D-20, D-07)
  </read_first>
  <behavior>
    - `isoWeekFolder(date: Date): string` — возвращает `YYYY-Www` по ISO 8601 Thursday-rule. Для `new Date("2026-05-15T12:00:00Z")` → `"2026-W20"` (или `2026-W19` — точное значение задаётся тестом по календарю; реализовать ISO 8601 Thursday алгоритм).
    - `weekDir(week: string): string` — возвращает `path.join(paths.dataDir, "uploads", week)`.
    - `saveXlsx(week: string, type: BitumType, buffer: Buffer): Promise<void>` — atomic `.tmp + rename`, файл `${type}.xlsx`. ASSERT `BITUM_TYPES.includes(type)` (T-04-03). Создаёт директорию через mkdirSync recursive если нет. Под per-week mutex (T-04-05). На overwrite — silent overwrite + `log.info("[bitum] overwrite: week=X type=Y prevSize=N newSize=M")` (D-07). **# Default — нужно подтверждение оператора на execute-phase** (silent vs require confirmation).
    - `getWeekStatus(week: string): Promise<WeekStatus>` — читает `${weekDir}/{birzha_volumes,birzha_prices,fca_sellers,bitum_price_new}.xlsx` (4 файла), формирует Record<BitumType, boolean> + считает manual-numbers.json элементы (через readFile + JSON.parse, defensive try/catch — пустой/невалидный → 0).
    - `resetWeek(week: string): Promise<{ deletedFiles: string[] }>` — удаляет директорию недели целиком, возвращает список удалённых basename'ов. Под per-week mutex (T-04-05).
    - `findLatestWeekWithUploads(): Promise<string | null>` — сканирует `paths.dataDir/uploads/`, фильтрует имена по regex `^\d{4}-W\d{2}$`, возвращает максимум lexically (это и есть chronologically для ISO-week формата) у которого хотя бы один из 4 битум-файлов существует. Если нет — `null`.
    - Per-week mutex: `Map<string, Promise<unknown>>` ключ = week. `withWeekLock(week, fn)` — паттерн как `withLock` в `channels-store.ts:77-86`, но per-key.

    Tests (`bitum-storage.test.ts`):
    1. `isoWeekFolder` для известных дат: 2026-01-04 (вс) → 2026-W01, 2026-01-05 (пн) → 2026-W02 (Thursday-rule), 2026-12-31 → 2027-W01 (если четверг попадает в следующий год)
    2. `saveXlsx` создаёт файл по пути и его можно прочитать обратно (buffer equality)
    3. `saveXlsx` ASSERT на невалидный type — throws с сообщением containing "invalid type"
    4. `saveXlsx` для одного и того же type дважды — второй вызов перезаписывает; первый размер ≠ второй размер; log.info вызван с overwrite-сообщением (мокать log через `vi.spyOn`)
    5. `getWeekStatus` для пустой недели — `present` всё false, `manualNumbersCount: 0`, `lastUpdatedAt: null`
    6. `getWeekStatus` после `saveXlsx(week, "birzha_prices", buf)` — `present.birzha_prices: true`, остальные false
    7. `resetWeek` после `saveXlsx` — папка удалена, повторный `getWeekStatus` показывает пусто
    8. `findLatestWeekWithUploads` — кладём файл в `2026-W10` и `2026-W15`, возвращает `"2026-W15"`
    9. Concurrent `saveXlsx` x10 одного типа — mutex обеспечивает что финальный файл не corrupted (один из buffers целиком). Test:  `Promise.all([saveXlsx(w,t,b1), saveXlsx(w,t,b2), ...])` → итоговый файл байт-в-байт равен одному из bN.

    Tests setup: vitest beforeEach создаёт tmpdir через `process.chdir(tmpdir)`, paths.dataDir автоматически указывает туда (см. `paths.ts:1-19`). afterEach — `rm -rf` tmpdir.
  </behavior>
  <action>
    Реализовать `src/bitum/storage.ts` по behavior. ISO-week formula:
    ```
    const d = new Date(Date.UTC(date.getUTCFullYear(), date.getUTCMonth(), date.getUTCDate()));
    d.setUTCDate(d.getUTCDate() + 4 - (d.getUTCDay() || 7));
    const yearStart = new Date(Date.UTC(d.getUTCFullYear(), 0, 1));
    const weekNum = Math.ceil((((d.getTime() - yearStart.getTime()) / 86400000) + 1) / 7);
    return `${d.getUTCFullYear()}-W${String(weekNum).padStart(2, "0")}`;
    ```
    Per-week mutex реализовать через `Map<string, Promise<void>>`; при чтении `lockChain = locks.get(week) ?? Promise.resolve()`, `next = lockChain.then(op, op)`, `locks.set(week, next.then(()=>undefined, ()=>undefined))`. Cleanup — после операции `if (locks.get(week) === entry) locks.delete(week)` (опционально, не критично).

    Atomic write: `writeFileSync(tmp, buf); renameSync(tmp, final)`. Имена: `${type}.xlsx`.

    Тесты через vitest `describe`/`it`. Использовать `vi.useFakeTimers()` НЕ нужно — даты в `isoWeekFolder` тестируем явными `new Date("...")`. Path-helpers: `process.chdir(tmpdir)` в beforeEach.
  </action>
  <verify>
    <automated>
      npx vitest run src/__tests__/bitum-storage.test.ts --reporter=basic
    </automated>
  </verify>
  <acceptance_criteria>
    - `src/bitum/storage.ts` экспортирует: `isoWeekFolder`, `weekDir`, `saveXlsx`, `getWeekStatus`, `resetWeek`, `findLatestWeekWithUploads`
    - `grep -c "withWeekLock\|locks.get\|Map<string" src/bitum/storage.ts` >= 1 (per-week mutex есть)
    - `grep -c "BITUM_TYPES.includes\|throw.*invalid type" src/bitum/storage.ts` >= 1 (path-traversal mitigation T-04-03)
    - `grep -c "renameSync" src/bitum/storage.ts` >= 1 (atomic write)
    - `npx vitest run src/__tests__/bitum-storage.test.ts` exit 0
    - test count в файле >= 9 (по поведениям из behavior)
  </acceptance_criteria>
  <done>storage.ts реализован, 9+ vitest-кейсов зелёные, mutex + atomic write + T-04-03/05 митигированы.</done>
</task>

<task type="auto" wave="2" tdd="true">
  <name>Task 4: src/bitum/manual-numbers.ts — ручные числа /bitum_add</name>
  <files>src/bitum/manual-numbers.ts, src/__tests__/bitum-manual-numbers.test.ts</files>
  <read_first>
    - `src/bitum/types.ts` (ManualNumber тип)
    - `src/channels-store.ts` (mutex + atomic write паттерн)
    - `src/bitum/storage.ts` (weekDir helper — переиспользуем)
    - `.planning/phases/04-bitum-weekly-report/04-CONTEXT.md` (D-14, D-15, D-16, D-17)
  </read_first>
  <behavior>
    - File path: `${weekDir(week)}/manual-numbers.json`, формат `ManualNumber[]` (плоский массив, D-15). **# Default — нужно подтверждение оператора на execute-phase** (плоский массив vs группировка для будущего /bitum_remove).
    - `addManualNumber(week: string, label: string, value: string): Promise<ManualNumber>` — sanitize (strip control chars `[\x00-\x1F\x7F]+` → "", trim, limit к 200 char), под mutex append к существующему массиву, atomic .tmp+rename. Если папка недели не существует — создать через mkdirSync recursive. addedAt = `new Date().toISOString()`. Дубли (тот же label+value) — разрешены, не дедуплицируем (D-14 pure pass-through).
    - `listManualNumbers(week: string): Promise<ManualNumber[]>` — читает JSON, defensive try/catch → `[]` на отсутствие/невалидный JSON. БЕЗ mutex (только read).
    - `clearManualNumbers(week: string): Promise<void>` — удаляет файл (если есть). Под mutex.
    - In-process mutex per-week (тот же паттерн что в storage.ts; реализовать локально, не импортировать withWeekLock — оставляем модули независимыми).
    - НЕ валидируем семантику label/value (D-14). Только sanitize control chars + length.

    Tests:
    1. `addManualNumber` создаёт файл с массивом из 1 элемента; читается обратно с правильным label/value/addedAt (ISO string)
    2. Два последовательных `addManualNumber` — файл содержит 2 элемента в порядке добавления
    3. `addManualNumber("week", "  test  \x00\x01abc", "  value\x7F")` — после sanitize label="test  abc" value="value" (control chars удалены, trim)
    4. `addManualNumber` с label длиной 500 → label обрезан до 200 char
    5. `listManualNumbers` для несуществующей недели → `[]`
    6. `listManualNumbers` для невалидного JSON → `[]` (defensive)
    7. `clearManualNumbers` удаляет файл; повторный `listManualNumbers` → `[]`
    8. Concurrent `addManualNumber` x10 → итоговый файл содержит ровно 10 элементов (mutex предотвращает потерю при read-modify-write)
    9. **XSS mitigation T-04-06**: label/value могут содержать `<script>` / `<` — sanitize их не удаляет (escape будет в reporter), но control chars удалены
  </behavior>
  <action>
    Реализовать по behavior. Sanitize:
    ```
    function sanitize(s: string): string {
      return s.replace(/[\x00-\x1F\x7F]+/g, "").trim().slice(0, 200);
    }
    ```
    Storage path: `path.join(weekDir(week), "manual-numbers.json")`. JSON формат — массив. Atomic: writeFileSync(tmp) + renameSync.

    Mutex per-week через Map (как в storage.ts). Concurrent test ставит 10 параллельных addManualNumber с разными values и ждёт `Promise.all`, потом проверяет `listManualNumbers().length === 10`.
  </action>
  <verify>
    <automated>
      npx vitest run src/__tests__/bitum-manual-numbers.test.ts --reporter=basic
    </automated>
  </verify>
  <acceptance_criteria>
    - exports: `addManualNumber`, `listManualNumbers`, `clearManualNumbers`
    - `grep -c "\\\\x00-\\\\x1F\\\\x7F\\|control" src/bitum/manual-numbers.ts` >= 1 (T-04-06 sanitize present)
    - `grep -c "slice(0, 200)\\|length.*200" src/bitum/manual-numbers.ts` >= 1 (length limit)
    - `grep -c "renameSync" src/bitum/manual-numbers.ts` >= 1 (atomic)
    - test file имеет >= 9 it-блоков
    - `npx vitest run src/__tests__/bitum-manual-numbers.test.ts` exit 0
  </acceptance_criteria>
  <done>manual-numbers.ts реализован с sanitize + mutex + atomic, 9+ тестов зелёные.</done>
</task>

<task type="auto" wave="2" tdd="true">
  <name>Task 5: src/bitum/refineries.ts — словарь НПЗ + покрытие 4 эталонных файлов</name>
  <files>src/bitum/refineries.ts, src/__tests__/bitum-refineries.test.ts, data/refineries.json (extend если нужно)</files>
  <read_first>
    - `data/refineries.json` (текущее содержимое — 29 канонических, проверить покрытие)
    - `src/bitum/types.ts` (ParsedRow* — refineryCanonical/refineryRaw поля)
    - `.planning/phases/04-bitum-weekly-report/04-CONTEXT.md` (REFINERY-01/02 контракт + Claude's Discretion §9 — проверка покрытия)
    - `docs/examples/birzha — суточная по НПЗ_rev.xlsx`, `docs/examples/birzha — цены НПЗ_rev.xlsx`, `docs/examples/BITUM — таблица продавцы_rev.xlsx`, `docs/examples/bitum_price — Сводная таблица_new.xlsx` (нужно вытащить все названия НПЗ через exceljs preview script — см. action)
  </read_first>
  <behavior>
    - `loadRefineriesDict(): RefineriesDict` — читает `paths.dataDir/refineries.json`. Если нет → fallback на `paths.seedDir/refineries.json`. Zod-валидация. Возвращает `{ refineries: Array<{canonical, company, aliases}> }`.
    - `normalizeRefinery(raw: string, dict: RefineriesDict): { canonical: string; matched: boolean }` — pure-функция, dict-аргументом (D-CLAUDE.md conventions).
      - Trim raw, case-insensitive lookup
      - Сначала ищет exact `canonical`, потом `aliases`
      - Если не нашёл — возвращает `{ canonical: raw.trim(), matched: false }` (НЕ throw, parser продолжает работу с raw)
    - Type:
      ```ts
      export interface RefineryEntry { canonical: string; company: string; aliases: string[] }
      export interface RefineriesDict { version: number; refineries: RefineryEntry[] }
      ```
    - Покрытие проверяется отдельным тестом (см. ниже).

    Tests (`bitum-refineries.test.ts`):
    1. `loadRefineriesDict` — возвращает >= 29 entries (текущий dict)
    2. `normalizeRefinery("Омский НПЗ", dict)` → `{ canonical: "Газпромнефть-Омский НПЗ", matched: true }`
    3. `normalizeRefinery("  ОНПЗ  ", dict)` → `{ canonical: "Газпромнефть-Омский НПЗ", matched: true }` (trim + alias)
    4. `normalizeRefinery("Газпромнефть-Омский НПЗ", dict)` → `{ canonical: "Газпромнефть-Омский НПЗ", matched: true }` (exact canonical)
    5. `normalizeRefinery("Какой-то Новый НПЗ", dict)` → `{ canonical: "Какой-то Новый НПЗ", matched: false }`
    6. `normalizeRefinery("", dict)` → `{ canonical: "", matched: false }`
    7. **Coverage test (ОБЯЗАТЕЛЕН)**: загрузить все 4 эталонных xlsx через exceljs, извлечь все названия НПЗ из **точных A1-notation coordinates** (см. ниже), для каждого вызвать `normalizeRefinery`, ассертить matched=true. Если есть unmatched — тест fails со списком отсутствующих, что подсказывает дополнить `data/refineries.json`. Этот тест — quality gate для покрытия. Coverage test reads НПЗ names from:
       - `docs/examples/birzha — суточная по НПЗ_rev.xlsx`: row 3, columns B..T (B3..T3)
       - `docs/examples/birzha — цены НПЗ_rev.xlsx`: row 3, columns B..T (B3..T3)
       - `docs/examples/BITUM — таблица продавцы_rev.xlsx`: column A, rows 4..N (где N = первая пустая ячейка) — `A4..A${lastNonEmpty}`
       - `docs/examples/bitum_price — Сводная таблица_new.xlsx`: column B, rows 5..N — `B5..B${lastNonEmpty}`
       # Default — точные строки/колонки нужно подтвердить чтением xlsx на execute-phase (если файл не соответствует — обновить координаты + закомитить с пометкой)
       
       Извлечение: helper `extractRefineryNamesFromXlsx(filePath, type)` использует ТОЛЬКО эти координаты, НИКАКОЙ heuristic fallback / union-of-cells. Если ячейка пустая — пропускаем; если non-string — приводим к String(value).trim().
    8. Если coverage test находит missing — добавить недостающие entries в `data/refineries.json` в формате `{name, canonical: name, company: "Прочие"}` с предупреждением в `log.warn("[refineries] auto-added: name=X company=Прочие — review needed")` и перезапустить тест. **# Default — нужно подтверждение оператора на execute-phase** (auto-add как «Прочие» vs request operator manual classification).
  </behavior>
  <action>
    Реализовать как описано. Алгоритм lookup:
    ```ts
    export function normalizeRefinery(raw: string, dict: RefineriesDict): {canonical: string; matched: boolean} {
      const trimmed = raw.trim();
      if (!trimmed) return { canonical: "", matched: false };
      const lc = trimmed.toLowerCase();
      for (const e of dict.refineries) {
        if (e.canonical.toLowerCase() === lc) return { canonical: e.canonical, matched: true };
        for (const a of e.aliases) {
          if (a.toLowerCase() === lc) return { canonical: e.canonical, matched: true };
        }
      }
      return { canonical: trimmed, matched: false };
    }
    ```

    Coverage test: написать helper `extractRefineryNamesFromXlsx(filePath: string, type: BitumType): string[]` который для каждого type читает **только указанные A1-notation координаты** (см. behavior §7). НИКАКОГО heuristic fallback (union-of-cells / фильтр по «НПЗ»/«нефть» / dict-membership) — это antipattern WARNING-7. Если координаты по факту не соответствуют — обновить behavior §7 + код helper'а + закомитить с пометкой `# coords confirmed on execute-phase`. Финально: `expect(unmatched).toEqual([])`.

    Если test обнаруживает unmatched НПЗ — расширить `data/refineries.json` (правильный company по списку BITUM-REFINERY-01 в REQUIREMENTS) и закомитить расширение в рамках Task 5.

    Cовместимость: refineries.json уже имеет 29 entries, multi-pass coverage может потребовать добавить пару отсутствующих (например, если в `BITUM — таблица продавцы_rev.xlsx` есть «Орский НПЗ» — он уже в dict как alias «Орский НПЗ» → Орскнефтеоргсинтез).
  </action>
  <verify>
    <automated>
      npx vitest run src/__tests__/bitum-refineries.test.ts --reporter=basic
    </automated>
  </verify>
  <acceptance_criteria>
    - exports: `loadRefineriesDict`, `normalizeRefinery`, `RefineryEntry`, `RefineriesDict`
    - test file имеет >= 7 it-блоков, включая coverage-test
    - Coverage test passes — все НПЗ из 4 эталонных xlsx находятся в dict
    - `! grep -q "module-level" src/bitum/refineries.ts` (нет singleton state — pure-функции)
    - `npx vitest run src/__tests__/bitum-refineries.test.ts` exit 0
  </acceptance_criteria>
  <done>refineries.ts реализован, dict покрывает все НПЗ из 4 эталонных xlsx, 7+ тестов зелёные.</done>
</task>

<!-- ============================================================================ -->
<!-- WAVE 3 — PARSERS (4 параллельных задачи, все depends_on Wave 2; никаких file conflicts) -->
<!-- ============================================================================ -->

<task type="auto" wave="3" tdd="true">
  <name>Task 6a: parseBirzhaVolumes — биржевые объёмы</name>
  <files>src/bitum/parsers/birzha-volumes.ts, src/bitum/parsers/shared.ts (создать; общие helpers), src/__tests__/bitum-parser-birzha-volumes.test.ts</files>
  <read_first>
    - `docs/bitum/algoritm.md` §1 (точная спека формата birzha — суточная по НПЗ.xlsx)
    - `docs/examples/birzha — суточная по НПЗ_rev.xlsx` (ground truth)
    - `src/bitum/types.ts` (ParserResult, ParsedVolumeRow)
    - `src/bitum/refineries.ts` (normalizeRefinery)
    - `.planning/phases/04-bitum-weekly-report/04-CONTEXT.md` (D-CLAUDE.md formulas — defer на plan-phase; ниже фиксируем)
  </read_first>
  <behavior>
    Format (per algoritm.md §1 + rev.xlsx): после нормализации лист имеет:
    - Row 1: header text (игнорируем)
    - Row 2: пустая
    - Row 3: B3..T3 — refinery names (после `Заменить "Объем, тыс. тонн: " → ""`)
    - Row 4..N: A — дата (Excel serial или Date object), B..T — объёмы в тыс.т
    - `parseBirzhaVolumes(buffer: Buffer, dict: RefineriesDict): ParserResult<ParsedVolumeRow>`
    - Каждая non-empty ячейка генерирует long-table row: `{ date, refineryCanonical, refineryRaw, volumeT }` где `volumeT = cellValue * 1000` (тыс.т → т).
    - Пустые / 0 / non-numeric ячейки — НЕ пропускаются (это значимо: 0 != нет торгов? Уточнение: согласно algoritm.md если ячейка пустая/0 — биржа не торговала в этот день; включаем как `volumeT: 0` ТОЛЬКО если оригинальное значение 0; если пусто — пропускаем без error).
    - Дата: Excel serial number → Date. Если cell.value уже Date — используем как есть. ISO YYYY-MM-DD (UTC).
    - Невалидная row (e.g. дата не парсится) → `errors.push({rowNum, reason})`, парсер продолжает.
    - DoS-mitigation (T-04-02): после `workbook.xlsx.load`, проверять `worksheet.rowCount <= BITUM_MAX_ROWS` (env, default 2000). Превышение → `errors: [{rowNum:0, reason:"too many rows (>${MAX})"}]`, `rows: []`, parser returns early.
    - `meta.fileType: "birzha_volumes"`, `meta.sheetName` = первый лист, `meta.cellRange` = "B4:T${lastRow}" (точный диапазон non-empty).
    - Идемпотентность: повторный вызов с тем же buffer → точно тот же result (используем структурное сравнение в тесте).

    Tests:
    1. Парсинг `docs/examples/birzha — суточная по НПЗ_rev.xlsx` → rows.length > 0; errors.length === 0
    2. Каждая row имеет валидный refineryCanonical (matched=true для большинства)
    3. Идемпотентность: `parse(buf, dict)` дважды → deepEqual
    4. Невалидный xlsx (пустой buffer) → errors с rowNum:0
    5. Buffer с rowCount > 2000 (создать в тесте через exceljs) → ParserResult с rows:[] и errors с reason "too many rows"
    6. meta.fileType === "birzha_volumes", meta.sheetName != "", meta.cellRange matches `B\d+:T\d+`
    7. Spot-check: для известной даты+НПЗ значение volumeT соответствует ячейке rev.xlsx × 1000 (взять конкретную ячейку, например Сызранский НПЗ за 8.05.2026)
    8. Заведомо невалидная row (e.g. cell с text "foo" в numeric колонке) — error пушится, парсер продолжает дальше
  </behavior>
  <action>
    Создать `src/bitum/parsers/shared.ts` с общими helpers:
    ```ts
    import ExcelJS from "exceljs";
    export const BITUM_MAX_ROWS = Number(process.env.BITUM_MAX_ROWS ?? 2000);
    // # Default formula — нужно подтверждение оператора на execute-phase
    // тыс.т → т:  volumeT = cellValue * 1000  (birzha_volumes)
    // тыс.руб/т → руб/т:  priceRub = cellValue * 1000  (birzha_prices)
    // БНД-префикс цен: idempotent strip `refineryRaw.replace(/^БНД-/, "")`

    export async function loadFirstSheet(buffer: Buffer): Promise<{sheet: ExcelJS.Worksheet; sheetName: string}> {...}
    export function excelDateToIso(value: unknown): string | null { ... }  // serial → ISO; if Date → ISO; else null
    export function isEmpty(v: unknown): boolean { return v === null || v === undefined || v === ""; }
    ```

    Реализовать `parseBirzhaVolumes(buffer, dict)`:
    1. `loadFirstSheet(buffer)` — если throw → catch → `{rows:[], errors:[{rowNum:0, reason: e.message}], meta:{...}}`
    2. Проверка rowCount > BITUM_MAX_ROWS → early-return с error
    3. Прочитать row 3, столбцы B..T (Excel колонки 2..20) — это refinery names
    4. Для row 4..rowCount: cell A = дата (excelDateToIso), B..T = volumes
    5. На каждую non-empty volume cell — push `{ date, refineryCanonical: normalizeRefinery(headerName).canonical, refineryRaw: headerName, volumeT: cell.value * 1000 }`
    6. meta = `{ fileType: "birzha_volumes", sheetName, cellRange: \`B4:T${rowCount}\`, rowsCount: rows.length }`

    Zod validation на каждую row перед push:
    ```ts
    const RowSchema = z.object({date: z.string(), refineryCanonical: z.string(), refineryRaw: z.string(), volumeT: z.number().nonnegative()});
    ```
    Невалидная → errors.push, не добавляем в rows.

    Тесты используют actual fixture from `docs/examples/birzha — суточная по НПЗ_rev.xlsx`. `import fs from "node:fs"; const buf = fs.readFileSync("docs/examples/birzha — суточная по НПЗ_rev.xlsx");`
  </action>
  <verify>
    <automated>
      npx vitest run src/__tests__/bitum-parser-birzha-volumes.test.ts --reporter=basic
    </automated>
  </verify>
  <acceptance_criteria>
    - exports: `parseBirzhaVolumes`
    - `grep -c "BITUM_MAX_ROWS\|rowCount" src/bitum/parsers/birzha-volumes.ts` >= 1 (T-04-02 mitigation)
    - `grep -c "normalizeRefinery" src/bitum/parsers/birzha-volumes.ts` >= 1 (dict-аргументом)
    - `grep -c "\\* 1000" src/bitum/parsers/birzha-volumes.ts` >= 1 (тыс.т → т conversion)
    - signature `parseBirzhaVolumes(buffer: Buffer, dict: RefineriesDict): Promise<ParserResult<ParsedVolumeRow>>` — async (exceljs load)
    - test file имеет >= 8 it-блоков
    - `npx vitest run src/__tests__/bitum-parser-birzha-volumes.test.ts` exit 0
  </acceptance_criteria>
  <done>parseBirzhaVolumes реализован, идемпотентен, ZOD-валидирован, T-04-02 митигирован, 8+ тестов зелёные.</done>
</task>

<task type="auto" wave="3" tdd="true">
  <name>Task 6b: parseBirzhaPrices — биржевые цены</name>
  <files>src/bitum/parsers/birzha-prices.ts, src/__tests__/bitum-parser-birzha-prices.test.ts</files>
  <read_first>
    - `docs/bitum/algoritm.md` §2
    - `docs/examples/birzha — цены НПЗ_rev.xlsx`
    - `src/bitum/types.ts`, `src/bitum/refineries.ts`, `src/bitum/parsers/shared.ts` (после Task 6a)
  </read_first>
  <behavior>
    Полностью аналогично Task 6a, но:
    - file type: `birzha_prices`
    - row 3 — refinery names (после `Заменить "БНД-" → ""`); сам replace УЖЕ применён в rev.xlsx, парсер просто читает as-is. Дополнительная защита: если в имени есть "БНД-" — strip prefix (idempotent).
    - cell value: `priceRub = cellValue * 1000` (тыс.руб/т → руб/т) — но только если cellValue не 0/пусто
    - Формула в исходном xlsx: `=ЕСЛИ(B4<>0;B4*1000;"")` — если cell.formula present, читаем cell.result (exceljs кэширует вычисленное значение в `cell.value` обычно как `{ formula, result }`); используем `cell.value` если number, иначе `cell.result`. Для cell-trace важно сохранять `cell.address` (e.g. "B4").
    - Output: `ParsedPriceRow[]` с полями {date, refineryCanonical, refineryRaw, priceRub}
    - meta.fileType: "birzha_prices", cellRange "B4:T${rowCount}"

    Tests (≥8):
    1. Парсинг fixture rev.xlsx → rows > 0, errors == 0
    2. priceRub всегда > 0 (после фильтра пустых)
    3. Idempotency
    4. Empty buffer → error
    5. rowCount > BITUM_MAX_ROWS → early-return
    6. meta корректен
    7. Если cell formula present — корректно читается result
    8. Spot-check: знаем конкретную цену (например, Саратовский НПЗ на 8.05.2026 в rev.xlsx) — она присутствует в rows с правильным priceRub
  </behavior>
  <action>
    Реализовать аналогично Task 6a. Различия только в формуле умножения (priceRub × 1000) и обработке formula cells:
    ```ts
    function cellNumber(cell: ExcelJS.Cell): number | null {
      const v = cell.value;
      if (typeof v === "number") return v;
      if (v && typeof v === "object" && "result" in v && typeof v.result === "number") return v.result;
      return null;
    }
    ```
    Если cellNumber === null → cell пуст / formula not evaluated → пропускаем.
    Strip prefix "БНД-" в refineryRaw (`refineryRaw.replace(/^БНД-/, "")`).
  </action>
  <verify>
    <automated>
      npx vitest run src/__tests__/bitum-parser-birzha-prices.test.ts --reporter=basic
    </automated>
  </verify>
  <acceptance_criteria>
    - exports: `parseBirzhaPrices`
    - `grep -c "\\* 1000" src/bitum/parsers/birzha-prices.ts` >= 1
    - `grep -c "БНД-" src/bitum/parsers/birzha-prices.ts` >= 1 (strip prefix idempotently)
    - test file >= 8 it-блоков
    - `npx vitest run src/__tests__/bitum-parser-birzha-prices.test.ts` exit 0
  </acceptance_criteria>
  <done>parseBirzhaPrices реализован, 8+ тестов зелёные.</done>
</task>

<task type="auto" wave="3" tdd="true">
  <name>Task 6c: parseFcaSellers — FCA цены продавцов</name>
  <files>src/bitum/parsers/fca-sellers.ts, src/__tests__/bitum-parser-fca-sellers.test.ts</files>
  <read_first>
    - `docs/bitum/algoritm.md` §3
    - `docs/examples/BITUM — таблица продавцы_rev.xlsx`
    - `src/bitum/types.ts` (ParsedFcaRow), `src/bitum/refineries.ts`, `src/bitum/parsers/shared.ts`
  </read_first>
  <behavior>
    Per algoritm.md §3 rev-структура: после трансформации (3.1-3.12 шаги) лист имеет:
    - Row 1: заголовок ("Битум цены продавцов FCA, руб./тонн") + даты периода в C1/D1
    - Row 2: header text
    - Row 3: column headers (A — НПЗ, B — Регион, C — Пункт отгрузки, D — Цена начала, E — Цена конца, F — Δ формула =D-C нет, в rev-формате E=D-C по 3.11 — точная коллнка с дельтой) + границы
    - Row 4..N: данные

    Точные координаты определить при чтении xlsx. Из спеки:
    - A — Refinery name (alias или canonical)
    - B — Region
    - C — Point of shipment
    - D, E — Price (begin / end period)
    - F или другая — delta (формула =D-C согласно 3.11; нужно проверить точно в rev.xlsx)

    Date: для FCA дата всегда = end of period (D1 cell, single value). Для всех строк ставим эту date.

    `parseFcaSellers(buffer, dict): Promise<ParserResult<ParsedFcaRow>>`. Row: `{date, refineryCanonical, refineryRaw, region, pointOfShipment, priceRub, deltaWeek}`.

    Tests (≥7):
    1. Парсинг fixture rev.xlsx → rows > 0, errors == 0
    2. Каждая row имеет non-empty refineryRaw, region, pointOfShipment, priceRub > 0
    3. Idempotency
    4. Empty buffer → error
    5. rowCount > BITUM_MAX_ROWS → early-return
    6. meta корректен (fileType: "fca_sellers", cellRange ~ "A4:F${rowCount}")
    7. Spot-check: известный seller (например, "Сила Сибири, Якутия, ..." — выбрать конкретную row после чтения rev.xlsx) присутствует с правильным priceRub
  </behavior>
  <action>
    1. Сначала вручную (или через grep на cell-output) прочитать `docs/examples/BITUM — таблица продавцы_rev.xlsx` — определить точные колонки и где лежит date. Зафиксировать координаты в коде через константы.
    2. Реализовать парсер. Date — из D1 cell (если text "DD.MM" — parse год из контекста; если Date — direct). Допустимый fallback: если D1 неконкретно, использовать end-of-current-week через `new Date()`.
    3. Pre-flight rowCount check (T-04-02).
    4. На каждую row: extract A/B/C/D/E (или какие там по факту), normalize refinery, push.
    5. Zod-валидация row.

    Поскольку шага «определить координаты» нет до чтения файла — допустимо в первую очередь написать helper `dumpSheetA1Notation(filePath)` который печатает все ячейки, использовать его в начале implementation чтобы зафиксировать схему. Этот helper можно удалить после.
  </action>
  <verify>
    <automated>
      npx vitest run src/__tests__/bitum-parser-fca-sellers.test.ts --reporter=basic
    </automated>
  </verify>
  <acceptance_criteria>
    - exports: `parseFcaSellers`
    - test file >= 7 it-блоков
    - `grep -c "BITUM_MAX_ROWS\|rowCount" src/bitum/parsers/fca-sellers.ts` >= 1
    - `npx vitest run src/__tests__/bitum-parser-fca-sellers.test.ts` exit 0
  </acceptance_criteria>
  <done>parseFcaSellers реализован, 7+ тестов зелёные.</done>
</task>

<task type="auto" wave="3" tdd="true">
  <name>Task 6d: parseBitumPriceNew — сводная «Битум прайс» (источник дельт + reference)</name>
  <files>src/bitum/parsers/bitum-price-new.ts, src/__tests__/bitum-parser-bitum-price-new.test.ts</files>
  <read_first>
    - `docs/bitum/algoritm.md` §4
    - `docs/examples/bitum_price — Сводная таблица_new.xlsx`
    - `src/bitum/types.ts` (ParsedBitumPriceNewRow), `src/bitum/refineries.ts`, `src/bitum/parsers/shared.ts`
  </read_first>
  <behavior>
    Per algoritm.md §4: «в колонках F указаны цены, G – изменение цен за неделю». В отличие от старого `parseBitumPriceNew` (который был single-snapshot BND/PBV), новый парсер читает таблицу-сводную с компаниями.

    Ожидаемая структура файла (определить через dumpSheet helper на этапе implementation):
    - Какие-то header rows (1-3) с заголовком
    - Колонки: НПЗ, Компания, ... , F (цена недели), G (изменение цены за неделю)

    `parseBitumPriceNew(buffer, dict): Promise<ParserResult<ParsedBitumPriceNewRow>>`. Row: `{date, refineryCanonical, refineryRaw, company, priceRub, deltaWeek}`.

    Date: фиксированная для всего файла, берётся из header или из текущей даты (D-CLAUDE.md — на plan-phase: используем end-of-current-week если нет explicit date cell).

    `company` берётся прямо из колонки xlsx (не через `normalizeRefinery + getCompany`) — потому что «Битум прайс» = источник истины о компании per row.

    Tests (≥7):
    1. Парсинг fixture → rows > 0
    2. Все rows имеют non-empty company
    3. priceRub > 0, deltaWeek может быть отрицательным
    4. Idempotency
    5. Empty buffer → error
    6. meta.fileType === "bitum_price_new"
    7. Spot-check: известный refinery (например, "ООО «РН-Битум»" → Роснефть) присутствует с правильным priceRub
  </behavior>
  <action>
    Аналогично Task 6c — сначала дамп ячеек, потом написать константы координат, потом implementation. Pre-flight rowCount check, Zod-валидация per-row, идемпотентность.
  </action>
  <verify>
    <automated>
      npx vitest run src/__tests__/bitum-parser-bitum-price-new.test.ts --reporter=basic
    </automated>
  </verify>
  <acceptance_criteria>
    - exports: `parseBitumPriceNew`
    - test file >= 7 it-блоков
    - `grep -c "company" src/bitum/parsers/bitum-price-new.ts` >= 1 (company extracted from xlsx)
    - `npx vitest run src/__tests__/bitum-parser-bitum-price-new.test.ts` exit 0
  </acceptance_criteria>
  <done>parseBitumPriceNew реализован, 7+ тестов зелёные.</done>
</task>

<!-- ============================================================================ -->
<!-- WAVE 4 — ANALYZER + REPORTER (sequential — reporter depends on analyzer) -->
<!-- ============================================================================ -->

<task type="auto" wave="4" tdd="true">
  <name>Task 7: src/bitum/analyzer.ts — плоский programmatic анализ + cross-check</name>
  <files>src/bitum/analyzer.ts, src/__tests__/bitum-analyzer.test.ts</files>
  <read_first>
    - `src/bitum/types.ts` (AnalysisResult, PriceMovement, VolumeAggregate, CrossCheckIssue)
    - `src/bitum/parsers/*.ts` (output типы)
    - `.planning/phases/04-bitum-weekly-report/04-CONTEXT.md` (D-11 плоская структура, D-18 cross-check, Claude's Discretion §1, §2, §4)
    - `docs/bitum/algoritm.md` §6 (отменённая структура — взять формулы метрик, но НЕ группировку)
  </read_first>
  <behavior>
    `analyzeBitum(parsed: ParsedByType, dict: RefineriesDict): AnalysisResult`

    Алгоритм:
    1. **Period**:
       - `from`: min date across всех загруженных файлов (volumes/prices/fca/bitum_price_new); если хоть один отсутствует — берём по доступным
       - `to`: max date across всех файлов
       - Формат: ISO YYYY-MM-DD
    2. **Volumes aggregate** (если `birzha_volumes` загружен):
       - Group rows by refineryCanonical, sum volumeT
       - `totalT`: Σ всех volumeT
       - `byRefinery`: sorted desc by sumT, top-7 default (cap; больше не показываем — D-13 partial behavior)
    3. **Movements** — плоский список изменений цен:
       - Источник #1: `birzha_prices` — для каждого refinery первая и последняя дата периода. `priceFrom = priceRub(first date)`, `priceTo = priceRub(last date)`, `deltaAbs = priceTo - priceFrom`, `deltaPct = priceFrom > 0 ? (deltaAbs / priceFrom * 100) : null`. `source: "birzha"`. trace = `[{fileType: "birzha_prices", sheet, cell: "B${firstRow}+B${lastRow}"}]`.
       - Источник #2: `fca_sellers` — если deltaWeek != 0, эта row — movement. priceFrom = priceTo - deltaWeek (если есть); priceTo = priceRub. source: "fca". trace = `[{fileType: "fca_sellers", ...}]`.
       - Источник #3: `bitum_price_new` — для каждой row с deltaWeek != 0 → movement. priceFrom = priceTo - deltaWeek, priceTo = priceRub. source: "bitum_price_new".
       - Movements с deltaAbs === 0 — НЕ включаются (без движения = не интересно для плоского списка).
       - **Сортировка (D-11 default — Claude's Discretion §4)**: по `|deltaAbs|` desc, при равенстве — по `refineryCanonical` ASC. **Default — нужно подтверждение оператора на execute-phase.**
    4. **Cross-check (D-18 + Claude's Discretion §2)**:
       - Threshold: `BITUM_CROSS_CHECK_THRESHOLD` env, default `1.0` (percent). **Default — нужно подтверждение оператора на execute-phase.**
       - Если `bitum_price_new` загружен:
         - Для каждой row из bitum_price_new, ищем same `refineryCanonical` в `birzha_prices` (последний known price) и `fca_sellers` (last priceRub)
         - `deltaPctVsBirzha = abs(bitumPrice - birzhaPrice) / birzhaPrice * 100`
         - `deltaPctVsFca = abs(bitumPrice - fcaPrice) / fcaPrice * 100`
         - Если deltaPct > threshold — push CrossCheckIssue
       - Cross-check НЕ выполняется если `bitum_price_new` отсутствует или другие источники отсутствуют
    5. **available**: `Record<BitumType, boolean>` — какие parsed массивы non-null
    6. **thresholdPct** — текущее значение для отчёта

    Pure-функция, dict-аргументом. No module-level state. No `process.env` read inside (читать в caller).

    Wait — `analyzeBitum` подписан как pure без env. Threshold должен прийти аргументом: `analyzeBitum(parsed, dict, options: {thresholdPct: number, volumesTopN?: number})`. Update interface: добавить параметр options.

    Tests (≥10):
    1. Empty parsed (все null) → AnalysisResult с `available` all false, `movements: []`, `crossCheck: []`, `period: {from: "", to: ""}`
    2. Только volumes → totalT > 0, movements: []
    3. Только prices → movements не пустые, sorted by |deltaAbs| desc
    4. prices + fca: movements включают оба источника
    5. Movement с deltaAbs == 0 не включается
    6. Cross-check: 3 файла + bitum_price совпадает с birzha (< 1%) → crossCheck: []
    7. Cross-check: bitum_price отклоняется от birzha на 5% при threshold 1 → crossCheck.length === 1, issue.otherSource === "birzha"
    8. Threshold из options применяется правильно (5% threshold, расхождение 3% → нет issue)
    9. Sort stability — два movement с равным |deltaAbs| → отсортированы по refinery ASC
    10. Pure: вызов с тем же parsed дважды → deepEqual результаты
    11. Period: from = min, to = max across loaded files
  </behavior>
  <action>
    Реализовать как описано. Сигнатура:
    ```ts
    export function analyzeBitum(
      parsed: ParsedByType,
      dict: RefineriesDict,
      options: { thresholdPct: number; volumesTopN?: number }
    ): AnalysisResult
    ```

    Использовать `Map<string, ...>` для groupBy refineryCanonical. Sort через `Array.prototype.sort` с composite comparator.

    Threshold:
    ```ts
    const DEFAULT_THRESHOLD = Number(process.env.BITUM_CROSS_CHECK_THRESHOLD ?? 1.0);
    ```
    Но `process.env` читать в caller (`bot-bitum.ts:handleBitumReport`), передавать в `analyzeBitum` через `options`. analyzer.ts остаётся pure.

    volumesTopN default 7 — также через options.
  </action>
  <verify>
    <automated>
      npx vitest run src/__tests__/bitum-analyzer.test.ts --reporter=basic
    </automated>
  </verify>
  <acceptance_criteria>
    - exports: `analyzeBitum`
    - `! grep -q "process.env" src/bitum/analyzer.ts` (pure — env читается в caller)
    - `grep -c "Math.abs\|deltaAbs" src/bitum/analyzer.ts` >= 1 (sort by |deltaAbs|)
    - test file >= 10 it-блоков, включая cross-check кейсы
    - `npx vitest run src/__tests__/bitum-analyzer.test.ts` exit 0
  </acceptance_criteria>
  <done>analyzer реализован, pure, threshold/topN через options, 10+ тестов зелёные.</done>
</task>

<task type="auto" wave="4" tdd="true">
  <name>Task 8: src/bitum/reporter.ts — programmatic HTML дайджест (плоский, без LLM)</name>
  <files>src/bitum/reporter.ts, src/__tests__/bitum-reporter.test.ts</files>
  <read_first>
    - `src/bitum/types.ts` (AnalysisResult, ReportResult, ManualNumber, WeekStatus)
    - `src/bitum/analyzer.ts` (структура AnalysisResult)
    - `src/deliver.ts` (chunkHtml для разрезания — НЕ вызывается в reporter, но контракт html-size-safe)
    - `.planning/phases/04-bitum-weekly-report/04-CONTEXT.md` (D-10 нет LLM, D-11 плоский, D-12 структура блоков, D-13 partial render, Claude's Discretion §5 cell-trace format)
    - `docs/bitum/algoritm.md` §6 (старая структура — для образца языка/тона)
  </read_first>
  <behavior>
    `buildReport(analysis: AnalysisResult, manualNumbers: ManualNumber[], weekStatus: WeekStatus): ReportResult`

    Структура HTML (per D-12):
    ```
    <b>Битумный отчёт DD.MM.YY – DD.MM.YY</b>

    <!-- Manual numbers block (если есть) -->
    <i>Контекст оператора:</i>
    <b>{label}:</b> {value}
    <b>{label}:</b> {value}
    ...

    <!-- Partial-render warning (если available !=== all true) -->
    ⚠️ Доступно X/4 типов: {comma-separated}. Отсутствуют: {missing}.

    <!-- Объёмы (если birzha_volumes) -->
    <b>Объёмы биржевых торгов</b>
    Σ за период: {totalT:.2f} тыс.т.
    Топ-{N}:
    • {refineryCanonical}: {sumT:.2f} тыс.т
    ...

    <!-- Движения цен (плоский, sorted by |Δ| desc) -->
    <b>Движения цен</b>
    {если movements.length === 0} Цены остались на уровне начала периода.
    {иначе:}
    • <b>{refineryCanonical}</b> ({source}): {priceFrom} → {priceTo} ₽ (Δ {sign}{deltaAbs} ₽{если deltaPct: , {sign}{deltaPct:.1f}%})
    ...

    <!-- Cross-check warning (если crossCheck.length > 0) -->
    ⚠️ <b>Расхождения цен ≥ {thresholdPct}%</b> (источник: Битум прайс):
    • {refineryCanonical}: Битум прайс {bitumPriceValue} ₽ vs {otherSource} {otherValue} ₽ ({deltaPct:.1f}%)
    ...

    <!-- Cell-trace footer (D-12 §7, Claude's Discretion §5 default — per-file compact) -->
    <code>Источники: {fileType}.xlsx: {numbersCount} чисел из {cellRange}; ...</code>
    ```

    Defaults (помечены `# Default — нужно подтверждение оператора на execute-phase`):
    - **Cell-trace format**: per-file compact, ОДНА строка `{fileType}.xlsx: {numbersCount} чисел из {cellRange}` per file, разделено `; `. Не построчно. **# Default — нужно подтверждение оператора на execute-phase** (D-12 §7 — per-file compact vs построчно).
    - **Числа форматируются**: цены — без десятичных, целые ₽ (round). Объёмы — 2 десятичных тыс.т. Проценты — 1 десятичный.
    - **Sign rendering**: `+` для positive, `−` (U+2212) для negative, `0` без знака.

    Function signature:
    ```ts
    export function buildReport(
      analysis: AnalysisResult,
      manualNumbers: ManualNumber[],
      weekStatus: WeekStatus
    ): ReportResult
    ```

    Возвращает `{html, trace}`. trace — массив ReportTrace (для unit-тестов).

    **HTML escape (T-04-06)**: для всех динамических строк (manual label/value, refineryCanonical, refineryRaw) применять `escapeHtml(s) = s.replace(/&/g,"&amp;").replace(/</g,"&lt;").replace(/>/g,"&gt;")`. Создать helper в reporter.ts (или в shared util).

    Pure-функция, никакого I/O. Не использует chunkHtml (это делает caller через `deliver.ts:chunkHtml(html, 4000)`).

    Tests (≥10):
    1. Full report (все 4 типа available) — html содержит все 5 разделов
    2. Partial render: только prices → есть warning «Доступно 1/4 типов», нет блока объёмов, есть блок движений
    3. Empty analysis (all null) → html содержит только period header + partial warning + "нет данных"
    4. Manual numbers: 2 пары — присутствуют в html после period
    5. Manual numbers: пустой массив — блок отсутствует целиком
    6. HTML escape: manual label `<script>alert(1)</script>` → в html `&lt;script&gt;alert(1)&lt;/script&gt;`, нет реального тега
    7. Cross-check warning присутствует когда crossCheck.length > 0
    8. Cross-check блок отсутствует если crossCheck пустой
    9. trace.length === количество loaded типов
    10. Cell-trace footer присутствует и содержит fileType + numbersCount + cellRange
    11. Movements sorted by |deltaAbs| desc — порядок в html соответствует sorted массиву
    12. Sign rendering: positive → "+", negative → "−" (U+2212), zero — не в movements (отфильтрован analyzer'ом)
  </behavior>
  <action>
    Реализовать как описано. Helper escapeHtml inline (или импорт из shared). Использовать template literals для секций; собирать через `parts: string[] = []; parts.push(...); return parts.join("\n\n");`.

    Числа форматировать через `Number.toFixed(N)` или `Math.round`. Sign:
    ```ts
    function signed(n: number, decimals: number = 0): string {
      if (n > 0) return `+${n.toFixed(decimals)}`;
      if (n < 0) return `−${Math.abs(n).toFixed(decimals)}`;  // U+2212 minus sign
      return `0`;
    }
    ```

    Лимит HTML: не превышать 16000 символов (chunkHtml порежет на 4 части). Если movements слишком много (>50), обрезать с пометкой "... ещё N движений (см. xlsx)". **Default cap 50 movements — нужно подтверждение оператора на execute-phase.**
  </action>
  <verify>
    <automated>
      npx vitest run src/__tests__/bitum-reporter.test.ts --reporter=basic
    </automated>
  </verify>
  <acceptance_criteria>
    - exports: `buildReport`
    - `grep -c "escapeHtml\|replace.*&lt;" src/bitum/reporter.ts` >= 1 (T-04-06)
    - `grep -c "Битумный отчёт" src/bitum/reporter.ts` >= 1 (period header literal)
    - `grep -c "Доступно" src/bitum/reporter.ts` >= 1 (partial-render)
    - `grep -c "Источники:" src/bitum/reporter.ts` >= 1 (cell-trace footer)
    - `! grep -q "DeepSeek\|openai\|llm" src/bitum/reporter.ts` (D-10 — no LLM)
    - test file >= 11 it-блоков
    - `npx vitest run src/__tests__/bitum-reporter.test.ts` exit 0
  </acceptance_criteria>
  <done>reporter реализован, programmatic + escaped + traced, 11+ тестов зелёные.</done>
</task>

<!-- ============================================================================ -->
<!-- WAVE 5a — BOT HANDLERS MODULE (Task 9: src/bot-bitum.ts standalone, ~600 lines) -->
<!-- WAVE 5b — BOT.TS WIRING (Task 10: depends_on Task 9; pause/clear context before continuing) -->
<!-- ============================================================================ -->

<task type="auto" wave="5a" tdd="true">
  <name>Task 9: src/bot-bitum.ts — handler модуль (always-ask UX, 4 команды, callbacks)</name>
  <files>src/bot-bitum.ts, src/__tests__/bitum-bot.test.ts</files>
  <read_first>
    - `src/bot.ts` (после Task 1) — паттерны sendHtml/sendPlain/sendReplyWithKeyboard/tgFetch/downloadTgFile (важно: эти helpers экспортируются, импортировать оттуда)
    - `src/bitum/storage.ts`, `src/bitum/manual-numbers.ts`, `src/bitum/refineries.ts`, парсеры, `src/bitum/analyzer.ts`, `src/bitum/reporter.ts`
    - `src/deliver.ts` (chunkHtml для нарезки отчёта)
    - `.planning/phases/04-bitum-weekly-report/04-CONTEXT.md` (D-05/D-06/D-07/D-08/D-13/D-14/D-15/D-17, Claude's Discretion §3 syntax, §6 pending timeout)
  </read_first>
  <behavior>
    Экспортируемые функции:

    **`handleBitumDocument(token, msg, buffer, fileName): Promise<void>`** — вызывается из `src/bot.ts:handleDocument` после downloadTgFile.
    - T-04-01: проверка `buffer.length <= BITUM_MAX_XLSX_BYTES` (env, default 10*1024*1024). Превышение → `sendPlain(... "⚠️ Файл слишком большой (>10 МБ).")` + `log.warn` + return.
    - Сохраняет buffer в pendingUploads `Map<msgId, {buffer, fileName, uploadedAt}>` (msgId — оригинального user message).
    - Шлёт inline-keyboard сообщение с 5 кнопками: 4 типа + «❌ Не битум/Отмена»:
      ```
      Я получил xlsx `{fileName}` ({size} КБ). Что это за файл?
      [ Биржа суточная по NPZ ]
      [ Биржа цены NPZ ]
      [ Битум таблица продавцы ]
      [ Битум прайс ]
      [ ❌ Не битум / Отмена ]
      ```
      callback_data: `bu:{type}:{originalMsgId}` для type кнопок, `bu:cancel:{originalMsgId}` для отмены.
    - sendReplyWithKeyboard в reply_to=originalMsgId.

    **`handleBitumStatus(token, msg): Promise<void>`** — /bitum_status.
    - Текущая ISO-неделя через `isoWeekFolder(new Date())`.
    - `getWeekStatus(week)` → формат:
      ```
      <b>Битум-неделя {week}</b>
      • Биржа суточная по NPZ: ✅/❌
      • Биржа цены NPZ: ✅/❌
      • Битум таблица продавцы: ✅/❌
      • Битум прайс: ✅/❌
      • Ручные числа: N запис(ь/и/ей)
      Последнее обновление: {lastUpdatedAt или "—"}
      ```
    - sendHtml в DM.

    **`handleBitumReport(token, msg): Promise<void>`** — /bitum_report.
    - Текущая неделя; для каждого present типа загружает xlsx + парсит соответствующим парсером.
    - Threshold из env `BITUM_CROSS_CHECK_THRESHOLD` (default 1.0).
    - `analyzeBitum(parsed, dict, {thresholdPct})` → analysis.
    - `manualNumbers = await listManualNumbers(week)`.
    - `buildReport(analysis, manualNumbers, weekStatus)` → `{html, trace}`.
    - Превью в DM: `sendHtml` + последним сообщением — confirm keyboard:
      ```
      [📤 Опубликовать в канал] [❌ Отмена]
      ```
      callback_data: `br:publish:{previewHashShort}` / `br:cancel:{previewHashShort}`. Где previewHashShort = SHA-256 первых 16 hex символов от html (для аудит-логирования + предотвращения double-publish).
    - pendingReports `Map<previewHashShort, {html, requestedBy, createdAt}>`.
    - **Timeout (Claude's Discretion §6 default)**: 15 минут TTL. setTimeout через `setTimeout(() => pendingReports.delete(hash), 15*60*1000)`. **Default — нужно подтверждение оператора на execute-phase.**

    **`handleBitumReset(token, msg): Promise<void>`** — /bitum_reset.
    - Текущая неделя; проверяет что вообще что-то есть (`getWeekStatus`).
    - Если ничего нет → `sendPlain(... "Неделя {week} пуста, нечего сбрасывать.")`.
    - Иначе confirm keyboard:
      ```
      Сбросить битум-неделю {week}? Будут удалены: ... (list of present files + manual-numbers count).
      [✅ Сбросить] [❌ Отмена]
      ```
      callback_data: `brs:confirm:{week}` / `brs:cancel:{week}`.

    **`handleBitumAdd(token, msg): Promise<void>`** — /bitum_add.
    - Парсит аргумент: текст после "/bitum_add " (split first space). **Syntax (Claude's Discretion §3 default)**: `<label>=<value>` через первый знак `=`. **Default — нужно подтверждение оператора на execute-phase.**
      ```
      /bitum_add Средняя цена БНД=28336 ₽/т
      → label="Средняя цена БНД", value="28336 ₽/т"
      ```
    - Если нет аргумента или нет `=` → reply usage hint.
    - `addManualNumber(week, label, value)` → `sendPlain(... "✅ Добавлено: {label} = {value}. Сейчас записей в неделе: N.")`.

    **`handleBitumCallback(token, cb): Promise<boolean>`** — обработчик callback_query от bitum-кнопок.
    - Распознаёт prefix `bu:` (upload), `br:` (report), `brs:` (reset). Если data prefix не bitum — return false (chain дальше в bot.ts:handleCallbackQuery передаст в /remove_channel handler).
    - **bu:{type}:{originalMsgId}**:
      - `pendingUploads.get(originalMsgId)` — если нет, edit "сессия истекла" и return true.
      - Если type === "cancel" → удалить из pendingUploads + edit "❌ Загрузка отменена".
      - Иначе:
        - `saveXlsx(currentWeek, type, buffer)` (через storage.ts; mutex + atomic + path-traversal-safe T-04-03)
        - Парсинг соответствующим парсером (`parseBirzhaPrices(buffer, dict)` и т.д.); парсер возвращает ParserResult с rows/errors/meta.
        - editMessageText в сообщении-вопросе:
          ```
          ✅ Сохранено как {type}.xlsx.
          Распознано {rows.length} строк, ошибок: {errors.length}.
          Период: {min(dates)}..{max(dates)}.
          Чек-лист недели: {weekStatus.present формат ✅/❌ × 4}
          ```
        - Убрать inline-keyboard (editMessageReplyMarkup with []).
        - Cleanup pendingUploads.
    - **br:publish:{hash}**:
      - `pendingReports.get(hash)` — если нет (timeout или рестарт) → edit "сессия истекла, повторите /bitum_report" + return.
      - Иначе: `sendToChannel(html)` (TG_CHANNEL_ID, использует deliver.ts), edit "📤 Опубликовано в канал.", `log.info("[bitum] publish: userId=%s hash=%s", userId, hash)`.
      - Cleanup pendingReports.
    - **br:cancel:{hash}**: edit "❌ Публикация отменена.", cleanup.
    - **brs:confirm:{week}**: `resetWeek(week)` + `clearManualNumbers(week)`; edit "✅ Неделя {week} сброшена. Удалено файлов: N." (D-17 — обнуляет xlsx + manual-numbers).
    - **brs:cancel:{week}**: edit "❌ Сброс отменён.".

    Tests (≥12):
    1. handleBitumDocument: file > 10MB → sendPlain warn + no save (T-04-01)
    2. handleBitumDocument: ok size → pendingUploads имеет entry, sendReplyWithKeyboard вызван с 5 кнопками
    3. handleBitumCallback `bu:birzha_prices:{msgId}` → saveXlsx вызван с type="birzha_prices" + parsed + edit
    4. handleBitumCallback `bu:cancel:{msgId}` → pendingUploads cleaned, edit "отменено"
    5. handleBitumCallback на expired msgId → "сессия истекла"
    6. handleBitumStatus: пустая неделя → text содержит "❌" × 4
    7. handleBitumStatus: одна загрузка → text содержит "✅" 1 раз
    8. handleBitumReport: пустая неделя → reply "нет файлов в неделе"
    9. handleBitumReport: загружен 1 файл → preview HTML + confirm keyboard
    10. handleBitumCallback `br:publish:{hash}` valid → sendToChannel called
    11. handleBitumCallback `br:cancel:{hash}` → no publish
    12. handleBitumAdd: "/bitum_add foo=bar" → addManualNumber called с (week, "foo", "bar")
    13. handleBitumAdd: "/bitum_add foo" (нет =) → usage hint
    14. handleBitumReset confirm → resetWeek + clearManualNumbers called
    15. T-04-07: ошибка из парсера НЕ содержит token в reply (mock tgFetch, проверить отправленный text)

    Mock через `vi.mock(...)` для tgFetch / sendToChannel / saveXlsx / парсеры. Использовать `vi.spyOn`.
  </behavior>
  <action>
    Реализовать handler-модуль ровно по behavior. Module-level state:
    ```ts
    interface PendingUpload { buffer: Buffer; fileName: string; uploadedAt: number; }
    const pendingUploads = new Map<number, PendingUpload>();  // key: originalMsgId
    interface PendingReport { html: string; requestedBy: number; createdAt: number; }
    const pendingReports = new Map<string, PendingReport>();  // key: previewHashShort
    const REPORT_TTL_MS = 15 * 60 * 1000;
    ```

    Hash helper:
    ```ts
    import { createHash } from "node:crypto";
    function shortHash(s: string): string {
      return createHash("sha256").update(s).digest("hex").slice(0, 16);
    }
    ```

    Парсер dispatch:
    ```ts
    async function parseByType(type: BitumType, buf: Buffer, dict: RefineriesDict): Promise<ParserResult<unknown>> {
      switch (type) {
        case "birzha_volumes": return parseBirzhaVolumes(buf, dict);
        case "birzha_prices":  return parseBirzhaPrices(buf, dict);
        case "fca_sellers":    return parseFcaSellers(buf, dict);
        case "bitum_price_new": return parseBitumPriceNew(buf, dict);
      }
    }
    ```

    Use date from msg.date if available, otherwise `new Date()` to determine current week.

    Все sendHtml/sendPlain/sendReplyWithKeyboard/tgFetch/editMessage* — импортировать из `src/bot.js`.

    `sendToChannel` — из `src/deliver.js` (уже шлёт через chunkHtml).

    Errors в `handleBitumReport` (парсер крашится / read fails) → catch + reply компактным сообщением, БЕЗ token в text (T-04-07).
  </action>
  <verify>
    <automated>
      npx vitest run src/__tests__/bitum-bot.test.ts --reporter=basic
    </automated>
  </verify>
  <acceptance_criteria>
    - exports: `handleBitumDocument`, `handleBitumStatus`, `handleBitumReport`, `handleBitumReset`, `handleBitumAdd`, `handleBitumCallback`
    - `grep -c "BITUM_MAX_XLSX_BYTES\|10 \\* 1024 \\* 1024\|10\\*1024\\*1024" src/bot-bitum.ts` >= 1 (T-04-01)
    - `grep -c "createHash\|sha256" src/bot-bitum.ts` >= 1 (preview hash для аудит-лога)
    - `grep -c "setTimeout.*15\\|REPORT_TTL\\|15 \\* 60" src/bot-bitum.ts` >= 1 (timeout pending preview)
    - `grep -c "sendToChannel" src/bot-bitum.ts` >= 1 (публикация в канал)
    - `grep -c "BITUM_CROSS_CHECK_THRESHOLD" src/bot-bitum.ts` >= 1 (env read в caller)
    - test file >= 14 it-блоков (1-15 кроме 13 объединяется)
    - `npx vitest run src/__tests__/bitum-bot.test.ts` exit 0
  </acceptance_criteria>
  <done>bot-bitum.ts реализован, 14+ тестов зелёные, T-04-01/06/07 митигированы.</done>
  <checkpoint required="true">
    ⏸️ PAUSE BEFORE TASK 10: Task 9 завершено (`src/bot-bitum.ts` ~600 строк + 14+ тестов). Перед стартом Task 10 (extensive rewrite `src/bot.ts`) — executor должен:
    1. Закоммитить результаты Task 9 (`feat(04): src/bot-bitum.ts handlers + tests`).
    2. Выполнить `/clear` для освобождения контекста (Task 10 потребует прочитать current `src/bot.ts` целиком + finalized signatures из `src/bot-bitum.ts`).
    3. Возобновить Wave 5b с чистым контекстом.
    Это explicit checkpoint per checker WARNING-3 — Wave 5 разбит на 5a/5b чтобы не исчерпать context window.
  </checkpoint>
</task>

<task type="auto" wave="5b" depends_on="Task 9">
  <name>Task 10: src/bot.ts — wiring новых битум-handlers (handleDocument, handleCommand, handleCallback, setMyCommands)</name>
  <files>src/bot.ts</files>
  <read_first>
    - `src/bot.ts` (current after Task 1 — base без bitum-веток)
    - `src/bot-bitum.ts` (после Task 9 — finalized signatures)
    - `src/bitum/storage.ts` (isoWeekFolder для /start hint)
    - `.planning/phases/04-bitum-weekly-report/04-CONTEXT.md` (Integration Points)
  </read_first>
  <action>
    1. **Импорты**: добавить
       ```ts
       import {
         handleBitumDocument,
         handleBitumStatus,
         handleBitumReport,
         handleBitumReset,
         handleBitumAdd,
         handleBitumCallback,
       } from "./bot-bitum.js";
       ```
    2. **handleDocument**: переписать (был удалён в Task 1):
       ```ts
       export async function handleDocument(token, msg, allowlist) {
         const userId = msg.from?.id;
         const doc = msg.document;
         if (!userId || !doc) return;
         if (!allowlist.has(userId)) {
           log.info(`[bot] denied: from=${userId} cmd=document`);
           return;
         }
         const chatId = msg.chat.id;
         try {
           // Pre-check size before download (T-04-01 первый уровень)
           const MAX = Number(process.env.BITUM_MAX_XLSX_BYTES ?? 10 * 1024 * 1024);
           if (doc.file_size && doc.file_size > MAX) {
             await sendPlain(token, chatId, `⚠️ Файл слишком большой (>10 МБ). Отклонено.`);
             return;
           }
           await sendPlain(token, chatId, "⏳ Получаю файл…");
           const { buffer, fileName } = await downloadTgFile(token, doc.file_id, doc.file_name ?? "upload.xlsx");
           log.info(`[bot] document received: from=${userId} name=${fileName} size=${buffer.length}`);
           await handleBitumDocument(token, msg, buffer, fileName);
         } catch (err) {
           const msgText = (err as Error).message ?? String(err);
           log.error(`[bot] handleDocument error: ${msgText}`);
           try { await sendPlain(token, chatId, `⚠️ Ошибка: ${msgText.slice(0, 300)}`); } catch {}
         }
       }
       ```
    3. **handleCommand**: добавить routing для 4 битум-команд (после удалённых веток в Task 1):
       ```ts
       if (cmd === "/bitum_status") { await handleBitumStatus(token, msg); return; }
       if (cmd === "/bitum_report") { await handleBitumReport(token, msg); return; }
       if (cmd === "/bitum_reset")  { await handleBitumReset(token, msg);  return; }
       if (cmd === "/bitum_add")    { await handleBitumAdd(token, msg);    return; }
       ```
    4. **EMOJI_BUTTON_MAP**: обновить под битум-команды:
       ```ts
       const EMOJI_BUTTON_MAP: Record<string, string> = {
         "📊 Статус загрузок": "/bitum_status",
         "🧠 Отчёт битум": "/bitum_report",
         "📋 Каналы новостей": "/channels",
         "❓ Помощь": "/help",
       };
       ```
       Также обновить MAIN_KEYBOARD button label `"🧠 Сделать сводку"` → `"🧠 Отчёт битум"`.
    5. **handleCallbackQuery**: восстановить bitum routing:
       ```ts
       if (/^(bu|br|brs):/.test(data)) {
         const handled = await handleBitumCallback(token, cb);
         if (handled) return;
       }
       ```
       Вставить ПОСЛЕ allowlist-проверки (строка ~637) и ДО `parseRemoveCallbackData` parsing.
    6. **registerBotCommands**: финальный набор команд:
       ```ts
       const commands = [
         { command: "start", description: "Запустить бота / показать меню" },
         { command: "help", description: "Инструкция" },
         { command: "bitum_status", description: "Чек-лист битум-недели" },
         { command: "bitum_report", description: "Битум-отчёт (preview → канал)" },
         { command: "bitum_reset", description: "Сбросить текущую битум-неделю" },
         { command: "bitum_add", description: "Добавить ручное число" },
         { command: "channels", description: "Список каналов" },
         { command: "add_channel", description: "Добавить канал" },
         { command: "remove_channel", description: "Удалить канал" },
       ];
       ```
    7. **/help**: финальный helpText:
       ```ts
       const helpText = [
         "Битум-неделя (пакет 4 xlsx):",
         "  /bitum_status — что уже загружено",
         "  /bitum_add label=value — добавить ручное число",
         "  /bitum_report — собрать отчёт и опубликовать в канал",
         "  /bitum_reset — обнулить текущую неделю",
         "",
         "Как пользоваться:",
         "1. Пришлите xlsx — бот спросит «какой это файл?» (4 варианта + Отмена).",
         "2. Когда нужны — /bitum_add для ручных чисел, /bitum_status для проверки.",
         "3. /bitum_report → preview в DM → «📤 Опубликовать» или «❌ Отмена».",
         "",
         "Каналы новостей:",
         "  /channels, /add_channel @name, /remove_channel @name",
       ].join("\n");
       ```
    8. **/start**: обновить welcome text:
       ```ts
       "Привет! Я бот по битуму. Пришлите xlsx — я спрошу что это, накоплю недельный пакет и соберу отчёт по /bitum_report. Список команд — на клавиатуре ниже или /help."
       ```

    После изменений: `npx tsc --noEmit` exit 0, `npx vitest run` exit 0 (все существующие не-битум тесты должны пройти; bot-handlers.test.ts может потребовать минимального обновления если ссылается на старые EMOJI кнопки).
  </action>
  <verify>
    <automated>
      grep -c "handleBitumDocument\|handleBitumStatus\|handleBitumReport\|handleBitumReset\|handleBitumAdd\|handleBitumCallback" src/bot.ts &&
      grep -c "/bitum_status\|/bitum_report\|/bitum_reset\|/bitum_add" src/bot.ts &&
      grep -c "/^(bu|br|brs):/" src/bot.ts &&
      ! grep -q "/bitum_preview\|/summarize\|/upload_status\|handleBitumPreview" src/bot.ts &&
      npx tsc --noEmit &&
      npx vitest run --reporter=basic
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "handleBitumDocument" src/bot.ts` === 2 (импорт + вызов)
    - `grep -c "/bitum_status" src/bot.ts` >= 1
    - `grep -c "/bitum_report" src/bot.ts` >= 1
    - `grep -c "/bitum_reset" src/bot.ts` >= 1
    - `grep -c "/bitum_add" src/bot.ts` >= 1
    - `grep -c "/bitum_preview" src/bot.ts` === 0 (D-09)
    - `grep -c "/summarize\|/upload_status" src/bot.ts` === 0 (full rewrite, deprecation removed)
    - `grep -c "(bu|br|brs):" src/bot.ts` >= 1 (callback routing)
    - `npx tsc --noEmit` exit 0
    - `npx vitest run --reporter=basic` exit 0
  </acceptance_criteria>
  <done>bot.ts wired, все 4 битум-команды доступны, callback routing работает, TS компиляция чистая.</done>
</task>

<!-- ============================================================================ -->
<!-- WAVE 6 — MIGRATION (REQUIREMENTS.md / ROADMAP.md sync; CLAUDE.md deferred) -->
<!-- ============================================================================ -->

<task type="auto" wave="6">
  <name>Task 11: Sync .planning/REQUIREMENTS.md / ROADMAP.md с новым scope (CLAUDE.md deferred — see NOTE)</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <read_first>
    - `.planning/REQUIREMENTS.md` (текущее содержимое — 28 BITUM-* IDs)
    - `.planning/ROADMAP.md` (Phase 4 секция)
    - `.planning/phases/04-bitum-weekly-report/04-CONTEXT.md` (canonical_refs — что отменено)
  </read_first>
  <action>
    **1. REQUIREMENTS.md** — обновить под cancelled scope:

    a) Удалить полностью (header + checkbox строки):
       - BITUM-CLS-01, BITUM-CLS-02, BITUM-CLS-03, BITUM-CLS-04 (классификация — отменена D-02/D-05)
       - BITUM-PARSE-04 (all_prices — отменён D-02)
       - BITUM-REPORT-02, BITUM-REPORT-03, BITUM-REPORT-04, BITUM-REPORT-05 (группировка по холдингам — отменена D-11)
       - BITUM-TG-02 (/bitum_preview — отменён D-09)

    b) Изменить текст оставшихся требований под новый scope:
       - **BITUM-PARSE-05**: переписать. Было «снапшот средняя цена БНД». Стало:
         ```
         **BITUM-PARSE-05**: `parseBitumPriceNew(buffer, dict)` — читает сводную таблицу с колонками НПЗ, Компания, ... Цена недели (F), Изменение за неделю (G); возвращает `ParserResult<ParsedBitumPriceNewRow>` где row = {date, refineryCanonical, refineryRaw, company, priceRub, deltaWeek}. Источник истины для cross-check (REPORT-08) и для дельт цен в отчёте.
         ```
       - **BITUM-REPORT-01**: упростить до period header + manual numbers block + summary line (плоский, без БНД snapshot блока — теперь это часть /bitum_add ручных чисел при необходимости).
       - **BITUM-REPORT-06**: переписать. Было «Прочие и независимые». Стало:
         ```
         **BITUM-REPORT-06**: Блок «Движения цен» — плоский список изменений по всем НПЗ, sorted by |Δ| desc. Каждая строка: `<b>{refinery}</b> ({source}): {priceFrom} → {priceTo} ₽ (Δ {sign}{deltaAbs} ₽, {sign}{deltaPct}%)`. Источник (биржа/FCA/Битум прайс) указывается явно. Без группировки по холдингам.
         ```
       - **BITUM-REPORT-08**: уточнить cross-check rule:
         ```
         **BITUM-REPORT-08**: Cross-check — для каждого refineryCanonical из `bitum_price_new`, найти same refinery в `birzha_prices` (последняя дата периода) и `fca_sellers` (последняя priceRub); рассчитать `|delta| / reference * 100`; если > `BITUM_CROSS_CHECK_THRESHOLD` (env, default 1.0) — push CrossCheckIssue в отчёт. Cross-check direction: `bitum_price_new` (источник истины) vs остальные.
         ```
       - **BITUM-TG-03**: уточнить preview→publish flow с inline-keyboard (без изменений, но переформулировать что preview происходит ВНУТРИ /bitum_report, а не /bitum_preview).
       - **BITUM-TG-05**: переписать. Было «detect type + чек-лист». Стало:
         ```
         **BITUM-TG-05**: На загрузку xlsx бот всегда отвечает inline-keyboard с 4 кнопками типов («Биржа суточная по NPZ» / «Биржа цены NPZ» / «Битум таблица продавцы» / «Битум прайс») + кнопкой «❌ Не битум / Отмена». После тапа: сохраняет файл как `data/uploads/<ISO-week>/<type>.xlsx`, парсит, отвечает чек-листом недели + parsed metadata (период, число строк, errors).
         ```
       - **BITUM-MIGRATE-01**: переписать. Было «refactor src/upload/* → src/bitum/*». Стало:
         ```
         **BITUM-MIGRATE-01**: `src/bitum/*` написан с нуля под упрощённый scope (4 типа, no classifier, no LLM, плоский отчёт). Структура: `types.ts`, `storage.ts`, `manual-numbers.ts`, `refineries.ts`, `parsers/{birzha-volumes, birzha-prices, fca-sellers, bitum-price-new, shared}.ts`, `analyzer.ts`, `reporter.ts`. Старые `src/upload/*` и предыдущая итерация `src/bitum/*` (с classifier/llm) удалены полностью. Никаких legacy-alias'ов команд.
         ```
       - **BITUM-MIGRATE-02**: переписать (deprecation aliases отменены — clean rewrite):
         ```
         **BITUM-MIGRATE-02**: Все алиасы старых команд (`/summarize`, `/upload_status`, `/bitum_preview`) удалены из `src/bot.ts` без deprecation-периода. Заказчик согласился (D-09, full rewrite). В меню Telegram (`setMyCommands`) присутствуют только новые 4 битум-команды + 3 канал-команды + start/help.
         ```
       - **BITUM-MIGRATE-03**: переписать (добавить /bitum_add storage + удалить упоминание all_prices/signatures-learned):
         ```
         **BITUM-MIGRATE-03**: `data/uploads/<week>/` хранит ровно 4 xlsx по фиксированным именам (`birzha_volumes.xlsx`, `birzha_prices.xlsx`, `fca_sellers.xlsx`, `bitum_price_new.xlsx`) + `manual-numbers.json` (массив `{label, value, addedAt}` для команды `/bitum_add`). Каталог `data/bitum/signatures-learned.json` удалён вместе с classifier-фичей.
         ```

    c) **Добавить новое требование `BITUM-TG-08`** (unconditional — фиксированный ID, согласован с ROADMAP.md Phase 4 requirements line):
       ```
       **BITUM-TG-08**: `/bitum_add <label>=<value>` — добавляет одну пару в `manual-numbers.json` текущей ISO-недели; sanitize control chars + length cap 200 char; atomic write + in-process mutex; pure pass-through (без семантической валидации); появляется в отчёте отдельным блоком после period header. (Не путать с BITUM-TG-06 из Future — это «выбор недели» — и BITUM-TG-07 «/bitum_undo». TG-08 — следующий свободный ID.)
       ```
       Это требование уже зафиксировано в frontmatter `requirements:` этого плана (см. line 55) и в ROADMAP Phase 4 Requirements line (line 73).

    d) **Traceability table** — обновить статусы:
       - Удалить строки для удалённых IDs
       - Изменить status оставшихся на `Pending` (была `Complete` от первой итерации)
       - Phase column для каждого: `4`

    e) **Coverage footer** — обновить:
       ```
       - v5.1 requirements: 19 total (16 active BITUM-* + new BITUM-TG-08 + 3 MIGRATE; reconciles to ROADMAP Phase 4 list of 19 IDs)
       - Mapped to phases: 19 (all to Phase 4)
       - Unmapped: 0
       ```

    **2. ROADMAP.md** — обновить Phase 4 секцию:
    - Goal переписать под новый scope:
      ```
      **Goal:** Заказчик присылает 4 xlsx-файла в DM боту в произвольном порядке; на каждый файл бот спрашивает inline-keyboard'ом из 4 кнопок «что это»; накапливает недельный пакет; по /bitum_report бот строит programmatic-дайджест (period header + ручные числа из /bitum_add + плоский список движений с cell-trace footer + cross-check warnings) и публикует в TG_CHANNEL_ID только после подтверждения кнопкой «📤 Опубликовать».
      ```
    - **Requirements:** обновить список (удалить cancelled, добавить новый):
      ```
      BITUM-PARSE-01, BITUM-PARSE-02, BITUM-PARSE-03, BITUM-PARSE-05, BITUM-PARSE-06, BITUM-REFINERY-01, BITUM-REFINERY-02, BITUM-REPORT-01, BITUM-REPORT-06, BITUM-REPORT-07, BITUM-REPORT-08, BITUM-TG-01, BITUM-TG-03, BITUM-TG-04, BITUM-TG-05, BITUM-TG-08, BITUM-MIGRATE-01, BITUM-MIGRATE-02, BITUM-MIGRATE-03
      ```
    - **Depends on:** оставить `Phase 3`
    - **Plans:** `1 plan` + список:
      ```
      Plans:
      - [ ] 04-01-PLAN.md — Битумный недельный отчёт (full rewrite v5.1 — 6 internal waves: cleanup → foundation → parsers → analyzer+reporter → bot → migration)
      ```

    **3. CLAUDE.md — НЕ ТРОГАТЬ В Task 11.**
    
    NOTE: CLAUDE.md project-instructions sync deferred — runs as separate /gsd-quick task after Phase 4 execute-phase verifies green. Причина (WARNING-8 от checker'а): CLAUDE.md содержит project-instructions с пометкой «OVERRIDES default behavior» — модификация должна быть deliberate, отдельным GSD-flow, чтобы не смешивать с migration commit'ом и чтобы оператор мог явно ревьюить diff. После Phase 4 execute-phase оператор запускает:
    ```
    /gsd-quick — sync CLAUDE.md Architecture section to milestone v5.1 (remove classifier/learning/all_prices/llm refs, update src/bitum structure to 8 modules, drop data/bitum/signatures-learned.json)
    ```

    После изменений (REQUIREMENTS.md + ROADMAP.md): документы консистентны, нет упоминания cancelled features (classifier, learning, all_prices, /bitum_preview, group-by-holding) в актуальных секциях. Только в `<!-- GSD: -->` блоках которые регенерируются автоматически — НЕ трогаем (regenerate-via-tools).
  </action>
  <verify>
    <automated>
      ! grep -qE '^\*\*BITUM-(CLS-0[1-4]|PARSE-04|REPORT-0[2-5]|TG-02)\*\*' .planning/REQUIREMENTS.md &&
      grep -q "BITUM-TG-08" .planning/REQUIREMENTS.md &&
      grep -q "milestone v5.1\|simplified bitum\|full rewrite" .planning/ROADMAP.md &&
      grep -q "BITUM-TG-08" .planning/ROADMAP.md
    </automated>
  </verify>
  <acceptance_criteria>
    - `! grep -qE '^\*\*BITUM-(CLS-0[1-4]|PARSE-04|REPORT-0[2-5]|TG-02)\*\*' .planning/REQUIREMENTS.md` (anchored regex — matches только top-level requirement declarations, не задевает historical mentions в Future/Out-of-Scope)
    - `grep -q "BITUM-TG-08" .planning/REQUIREMENTS.md` (новое требование добавлено)
    - В ROADMAP Phase 4 секции `Requirements:` строка содержит ровно 19 IDs (16 active BITUM-* + BITUM-TG-08 + 3 MIGRATE = 19)
    - `grep -q "BITUM-TG-08" .planning/ROADMAP.md` (ID присутствует в ROADMAP)
    - `grep -q "milestone v5.1\|simplified bitum\|full rewrite" .planning/ROADMAP.md` (scope footer)
    - CLAUDE.md НЕ модифицируется в Task 11 (см. NOTE — deferred to separate /gsd-quick after execute-phase verifies green)
  </acceptance_criteria>
  <done>REQUIREMENTS.md / ROADMAP.md синхронизированы со scope v5.1; все cancelled IDs удалены; новый BITUM-TG-08 присутствует и в REQUIREMENTS.md, и в ROADMAP.md Phase 4. CLAUDE.md sync deferred (см. NOTE).</done>
</task>

</tasks>

<verification>
## Phase-level verification

После всех 11 задач:

1. **TypeScript compile**: `npx tsc --noEmit` exit 0
2. **All vitest tests pass**: `npx vitest run --reporter=basic` exit 0 (включая 10+ новых битум-тестов)
3. **Source tree clean**:
   - `ls src/bitum/` — 8 файлов + parsers/ subdir (5 файлов)
   - `ls src/__tests__/bitum-*.test.ts` — ровно 10 файлов (storage, refineries, manual-numbers, 4 parsers, analyzer, reporter, bot)
   - `! ls src/bitum/{classifier,signatures,learned-signatures,llm}.ts` (отменённые модули)
   - `! ls src/bitum/parsers/all-prices.ts` (отменённый парсер)
4. **bot.ts wiring**:
   - `grep -c "/bitum_" src/bot.ts` >= 4 (4 команды)
   - `grep -c "handleBitumCallback" src/bot.ts` >= 1
   - `! grep -q "/bitum_preview\|/summarize\|/upload_status\|deprecationShownByUser" src/bot.ts`
5. **Documentation sync**:
   - `! grep -qE '^\*\*BITUM-(CLS-0[1-4]|PARSE-04|REPORT-0[2-5]|TG-02)\*\*' .planning/REQUIREMENTS.md` (anchored — cancelled IDs not declared as top-level requirements; historical mentions в Future/Out-of-Scope допустимы)
   - ROADMAP Phase 4 Requirements line содержит ровно 19 IDs (16 active BITUM-* + BITUM-TG-08 + 3 MIGRATE)
   - CLAUDE.md sync deferred to separate `/gsd-quick` after Phase 4 execute-phase (NOT verified here)
6. **Manual UAT (separate /gsd-uat-phase step)**:
   - Загрузить 4 xlsx из `docs/examples/*_rev.xlsx` (для bitum_price_new — base file без _rev) в DM боту по очереди, для каждого тапнуть правильную кнопку → каждый раз бот отвечает чек-листом
   - `/bitum_add Средняя цена БНД=28336 ₽/т` → reply «✅ Добавлено»
   - `/bitum_status` → чек-лист 4/4 ✅
   - `/bitum_report` → preview HTML в DM с движениями + cross-check; нажать «📤 Опубликовать» → сообщение появляется в `TG_CHANNEL_ID`
   - `/bitum_reset` → confirm → неделя пуста
</verification>

<success_criteria>

Phase 4 готов к execute-phase когда:

- [ ] Все 11 задач выполнены и automated verify проходит
- [ ] 10 vitest-файлов созданы, все it-блоки зелёные
- [ ] `src/bitum/*` имеет ровно 8 модулей + parsers/{4 + shared}
- [ ] `src/bot-bitum.ts` имеет 6 exported handlers
- [ ] `src/bot.ts` routes 4 битум-команды + callback prefix `(bu|br|brs):` + handleDocument → handleBitumDocument
- [ ] Нет в репо: `src/bitum/{classifier,signatures,learned-signatures,llm}.ts`, `src/bitum/parsers/all-prices.ts`, `src/__tests__/bitum-classifier.test.ts` и т.д. (18 удалённых тестов), `data/bitum/`, `data/uploads/2026-W19`, `data/uploads/2026-W21`, `docs/examples/цены битум все *.xlsx`
- [ ] `data/refineries.json` покрывает все НПЗ из 4 эталонных xlsx (coverage-test зелёный)
- [ ] `.planning/REQUIREMENTS.md` обновлён: 9 cancelled IDs удалены, 8 оставшихся переписаны, 1 новый BITUM-TG-08 добавлен
- [ ] `.planning/ROADMAP.md` Phase 4 секция отражает v5.1 scope (Goal + Requirements line)
- [ ] CLAUDE.md sync deferred to separate `/gsd-quick` task after execute-phase verifies green (см. Task 11 NOTE)
- [ ] STRIDE threat model митигирован: T-04-01 (10MB cap), T-04-02 (rowCount cap), T-04-03 (path traversal), T-04-04/05 (mutexes), T-04-06 (escapeHtml + sanitize), T-04-07 (no token в reply)
- [ ] Все 8 Claude's Discretion defaults зафиксированы в коде с комментарием `# Default — нужно подтверждение оператора на execute-phase`:
   1. Cross-check threshold (1.0%) — `analyzer.ts` options + `bot-bitum.ts` env read
   2. Cross-check formula direction (bitum_price_new vs other) — `analyzer.ts`
   3. /bitum_add syntax (`label=value` split first `=`) — `bot-bitum.ts:handleBitumAdd`
   4. Sort movements by |Δ| desc, secondary refinery ASC — `analyzer.ts`
   5. Cell-trace footer format (per-file compact, one line per file) — `reporter.ts`
   6. Pending preview timeout (15 min) — `bot-bitum.ts:REPORT_TTL_MS`
   7. Overwrite xlsx — silent + log.info — `storage.ts:saveXlsx`
   8. manual-numbers.json — плоский массив — `manual-numbers.ts`
   9. (Бонус) volumesTopN default 7 + movements cap 50 — `analyzer.ts` / `reporter.ts`

</success_criteria>

<output>
After completion, create `.planning/phases/04-bitum-weekly-report/04-01-SUMMARY.md` per `$HOME/.claude/get-shit-done/templates/summary.md`. Include:
- Task-by-task outcome (10 ok / 0 fail)
- File-by-file changes (новые модули + переписанный bot.ts + обновлённая documentation)
- Cells of `docs/examples/*_rev.xlsx` использованные как fixtures
- Точные defaults для 9 Claude's Discretion items
- Список deferred follow-ups (если что-то Phase 4 не закрыл)
</output>
