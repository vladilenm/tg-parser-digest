---
phase: 02
plan: 05
title: "Daemon entrypoint + node-cron + mutex + SIGINT/SIGTERM + smoke-test"
type: execute
wave: 3
depends_on: [1, 2, 4]
files_modified:
  - src/run.ts
  - package.json
  - package-lock.json
autonomous: false
requirements: [DAEMON-01, DAEMON-02, DAEMON-03, DAEMON-04, DEPLOY-02]
requirements_addressed: [DAEMON-01, DAEMON-02, DAEMON-03, DAEMON-04, DEPLOY-02]
must_haves:
  truths:
    - "npm start запускает daemon, процесс не завершается после старта"
    - "В логе при старте: `daemon started, schedule: 0 20 * * * Europe/Moscow`"
    - "Ctrl+C вызывает лог `received SIGINT, stopping cron` → ждёт isRunning → exit 0"
    - "При активном прогоне второй тик cron пишет `prev run still in progress — skipping tick` и не запускает runPipeline"
    - "runOnInit НЕ передаётся в cron.schedule — PM2-рестарт не триггерит дайджест"
    - "node-cron@^3.0.3 и @types/node-cron@^3.0.11 установлены"
    - "Финальный cron-паттерн в src/run.ts — ровно `\"0 20 * * *\"` (smoke-паттерн удалён после Task 3)"
  artifacts:
    - path: "src/run.ts"
      provides: "Daemon-entrypoint с cron + shutdown handler"
      contains: "cron.schedule(\"0 20 * * *\""
    - path: "package.json"
      provides: "Зависимости node-cron + @types/node-cron"
      contains: '"node-cron":'
  key_links:
    - from: "src/run.ts"
      to: "node-cron"
      via: "import cron from 'node-cron'"
      pattern: "import cron from \"node-cron\""
    - from: "src/run.ts"
      to: "src/pipeline.ts"
      via: "runPipeline() in tick()"
      pattern: "await runPipeline\\(\\)"
    - from: "src/run.ts"
      to: "src/logger.ts"
      via: "log.info/warn/error + logRunSummary"
      pattern: "import \\{ log, logRunSummary \\}"
    - from: "src/run.ts"
      to: "src/telegram.ts"
      via: "через runPipeline → fetchLast24h (smoke-тест Task 3 шаг D проверяет reconnect из plan 04)"
      pattern: "await runPipeline\\(\\)"
---

<objective>
Переписать src/run.ts как daemon-entrypoint: node-cron с паттерном `0 20 * * *` и `timezone: "Europe/Moscow"`, mutex `isRunning` для защиты от параллельных тиков, graceful shutdown по SIGINT/SIGTERM с ожиданием активного прогона. Установить runtime-dep `node-cron@^3.0.3` и devDep `@types/node-cron@^3.0.11` в package.json. Убрать старый одноразовый `main().catch(...)` код.

Purpose: DAEMON-01/02/03/04 — ядро milestone v2.0. Без этого плана нет daemon-режима, нет ежедневного прогона в 20:00 MSK. DEPLOY-02 закрывается установкой пакета.

Output:
- src/run.ts — переписан как daemon
- package.json — +node-cron, +@types/node-cron
- package-lock.json — обновлён после npm install

Depends on: plan 01 (runPipeline должен существовать), plan 02 (log, logRunSummary должны существовать), **plan 04 (fetchLast24h с reconnect retry — smoke-тест Task 3 шаг D триггерит reconnect-ветку; без plan 04 SC4 невозможно подтвердить)**.

Blocking task: локальный smoke-тест daemon с временным cron-паттерном `*/2 * * * *` для проверки Success Criterion 2/5 и возврат на `0 20 * * *`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-daemon-50/02-RESEARCH.md
@docs/phase-2.md
@CLAUDE.md
@src/run.ts
@package.json

<interfaces>
From src/pipeline.ts (plan 01):
```typescript
export async function runPipeline(): Promise<RunSummary>;
```

From src/logger.ts (plan 02):
```typescript
export const log: {
  info(msg: string, ...ctx: unknown[]): void;
  warn(msg: string, ...ctx: unknown[]): void;
  error(msg: string, ...ctx: unknown[]): void;
};
export function logRunSummary(s: RunSummary): void;
```

From node-cron v3 (CJS default import, verified in research §1):
```typescript
import cron from "node-cron";
cron.schedule(
  expression: string,
  func: (now: Date | "manual" | "init") => void,
  options?: {
    scheduled?: boolean;
    timezone?: string;
    name?: string;
    runOnInit?: boolean;  // НЕ ИСПОЛЬЗУЕМ (DAEMON-04)
  }
): ScheduledTask;

interface ScheduledTask {
  start(): void;
  stop(): void;
  now(): void;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Установить node-cron и @types/node-cron</name>
  <files>package.json, package-lock.json</files>
  <read_first>
    - package.json (текущий список dependencies/devDependencies)
    - .planning/phases/02-daemon-50/02-RESEARCH.md §1 (версии из npm registry: node-cron@3.0.3, @types/node-cron@3.0.11)
  </read_first>
  <behavior>
    - В package.json dependencies добавлен `"node-cron": "^3.0.3"`
    - В package.json devDependencies добавлен `"@types/node-cron": "^3.0.11"`
    - Существующие deps (openai, telegram, yaml) и devDeps (@types/node, tsx, typescript) не изменены
    - Скрипты `start` и `login` не переименованы
    - package-lock.json обновлён
  </behavior>
  <action>
Выполнить в корне проекта ровно одну команду:

```bash
npm install --save node-cron@^3.0.3 && npm install --save-dev @types/node-cron@^3.0.11
```

Это:
1. Добавит `"node-cron": "^3.0.3"` в dependencies.
2. Добавит `"@types/node-cron": "^3.0.11"` в devDependencies.
3. Обновит package-lock.json.
4. Установит физические файлы в node_modules.

Не редактировать package.json вручную — npm install сам запишет правильный формат.

Проверить:
- `cat package.json | grep node-cron` показывает обе записи
- `ls node_modules/node-cron/package.json` существует
- `ls node_modules/@types/node-cron/index.d.ts` существует
  </action>
  <verify>
    <automated>grep -q '"node-cron"' package.json && grep -q '"@types/node-cron"' package.json && test -f node_modules/node-cron/package.json && test -f node_modules/@types/node-cron/index.d.ts && grep -q '"openai"' package.json && grep -q '"telegram"' package.json && grep -q '"yaml"' package.json && grep -q '"tsx"' package.json</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q '"node-cron"' package.json` exits 0 и значение соответствует `^3.0.*`
    - `grep -q '"@types/node-cron"' package.json` exits 0 и значение соответствует `^3.0.*`
    - `test -f node_modules/node-cron/package.json` exits 0
    - `test -f node_modules/@types/node-cron/index.d.ts` exits 0
    - Существующие deps сохранены: openai, telegram, yaml (все 3 findable через `grep -q`)
    - Существующие devDeps сохранены: @types/node, tsx, typescript
    - Скрипты `npm start` и `npm run login` не переименованы (findable через `grep -q '"start":' package.json`, `grep -q '"login":' package.json`)
    - package-lock.json обновлён (git diff показывает изменения)
  </acceptance_criteria>
  <done>node-cron@^3.0.3 и @types/node-cron@^3.0.11 установлены и зарегистрированы в package.json; существующие зависимости и скрипты не тронуты.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Переписать src/run.ts как daemon-entrypoint</name>
  <files>src/run.ts</files>
  <read_first>
    - src/run.ts (текущий одноразовый main() — полностью заменяется)
    - src/pipeline.ts (подпись runPipeline из plan 01)
    - src/logger.ts (log + logRunSummary из plan 02)
    - docs/phase-2.md §2 (канонический код daemon — копируется почти вербатим)
    - .planning/phases/02-daemon-50/02-RESEARCH.md §2 (Daemon entrypoint) и §3 (Reentrancy guard)
  </read_first>
  <behavior>
    - Импорт `cron` — default import из "node-cron" (CJS interop, research §1 pitfall 1)
    - Модульная переменная `let isRunning = false`
    - `async function tick()` проверяет isRunning → log.warn skipping tick; иначе — set isRunning=true → try/catch → finally isRunning=false
    - `cron.schedule("0 20 * * *", tick, { timezone: "Europe/Moscow" })` — НЕ передавать runOnInit (DAEMON-04)
    - При старте пишет `log.info("daemon started, schedule: 0 20 * * * Europe/Moscow")`
    - Shutdown: `process.on("SIGINT", ...)` + `process.on("SIGTERM", ...)` → task.stop() → while(isRunning) sleep 500ms → process.exit(0)
    - НЕТ `process.exit()` после cron.schedule — event-loop держится от cron handle
    - НЕТ `main().catch(...)` — cron-тик сам ловит ошибки pipeline через log.error
    - Файл заменяется полностью (старый main, ChannelEntry, loadChannelsYaml удаляются — они переехали в pipeline.ts в plan 01)
  </behavior>
  <action>
Полностью заменить содержимое src/run.ts на следующий код ВЕРБАТИМ:

```typescript
// src/run.ts — daemon entrypoint tg-parser-demo (v2.0).
// Запуск: `npm start` — процесс висит, cron триггерит tick() каждые сутки в 20:00 MSK.
// Остановка: Ctrl+C (SIGINT) или SIGTERM от PM2 — graceful shutdown с ожиданием активного прогона.

import cron from "node-cron";
import { runPipeline } from "./pipeline.js";
import { log, logRunSummary } from "./logger.js";

// DAEMON-03: mutex от параллельных тиков. In-memory boolean — достаточно для single-process PM2 fork.
let isRunning = false;

async function tick(): Promise<void> {
  if (isRunning) {
    log.warn("prev run still in progress — skipping tick");
    return;
  }
  isRunning = true;
  try {
    const summary = await runPipeline();
    logRunSummary(summary);
  } catch (err) {
    log.error("pipeline failed", err);
  } finally {
    isRunning = false;
  }
}

// DAEMON-02: ежедневный прогон в 20:00 MSK.
// DAEMON-04: runOnInit НЕ передаём — PM2-рестарт не триггерит дайджест вне расписания.
const task = cron.schedule("0 20 * * *", tick, { timezone: "Europe/Moscow" });
log.info("daemon started, schedule: 0 20 * * * Europe/Moscow");

// DAEMON-01: graceful shutdown — ждём активный прогон, потом exit 0.
const shutdown = async (signal: string): Promise<void> => {
  log.info(`received ${signal}, stopping cron`);
  task.stop();
  while (isRunning) {
    await new Promise((r) => setTimeout(r, 500));
  }
  process.exit(0);
};
process.on("SIGINT", () => void shutdown("SIGINT"));
process.on("SIGTERM", () => void shutdown("SIGTERM"));
```

Важное:
- Удалить всё содержимое старого src/run.ts (main, loadChannelsYaml, ChannelEntry, ChannelsFile — они переехали в pipeline.ts в plan 01).
- НЕ добавлять `process.exit(0)` после `cron.schedule(...)` — daemon должен висеть.
- `import cron from "node-cron"` — ТОЛЬКО default import. Не писать `import { schedule } from "node-cron"` (это CJS пакет без именованных экспортов; runtime сломается).
- `void shutdown(...)` — обязательный префикс для подавления TS-предупреждения о floating promise в event-handler'е.

Запустить `npx tsc --noEmit` — 0 ошибок.
  </action>
  <verify>
    <automated>grep -q 'import cron from "node-cron"' src/run.ts && grep -q "import { runPipeline } from \"./pipeline.js\"" src/run.ts && grep -q "import { log, logRunSummary } from \"./logger.js\"" src/run.ts && grep -q "let isRunning = false" src/run.ts && grep -q 'prev run still in progress — skipping tick' src/run.ts && grep -q 'cron.schedule("0 20 \* \* \*"' src/run.ts && grep -q 'timezone: "Europe/Moscow"' src/run.ts && grep -q 'daemon started, schedule: 0 20 \* \* \* Europe/Moscow' src/run.ts && grep -q 'process.on("SIGINT"' src/run.ts && grep -q 'process.on("SIGTERM"' src/run.ts && grep -q 'received \${signal}, stopping cron' src/run.ts && grep -q "task.stop()" src/run.ts && grep -q "while (isRunning)" src/run.ts && grep -q "process.exit(0)" src/run.ts && ! grep -q "runOnInit" src/run.ts && ! grep -q "function main" src/run.ts && ! grep -q "main().catch" src/run.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'import cron from "node-cron"' src/run.ts` exits 0 (default import, не named)
    - `grep -q 'cron.schedule("0 20 \* \* \*"' src/run.ts` exits 0 (точный cron pattern из DAEMON-02)
    - `grep -q 'timezone: "Europe/Moscow"' src/run.ts` exits 0 (точная IANA timezone)
    - `grep -q 'daemon started, schedule: 0 20 \* \* \* Europe/Moscow' src/run.ts` exits 0 (точная log-строка для SC1)
    - `grep -q 'prev run still in progress — skipping tick' src/run.ts` exits 0 (точная log-строка для SC5)
    - `grep -q 'received \${signal}, stopping cron' src/run.ts` exits 0 (SC1 shutdown log)
    - `grep -q 'task.stop()' src/run.ts` exits 0
    - `grep -q 'while (isRunning)' src/run.ts` exits 0 (graceful wait)
    - `grep -c "process.exit(0)" src/run.ts` = 1 (один exit — только в shutdown-хэндлере)
    - `! grep -q "runOnInit" src/run.ts` exits 0 (DAEMON-04: runOnInit не упомянут вообще)
    - `! grep -q "function main" src/run.ts` exits 0 (старая main удалена)
    - `! grep -q "main().catch" src/run.ts` exits 0 (старый глобальный catch удалён)
    - `! grep -q "loadChannelsYaml" src/run.ts` exits 0 (переехала в pipeline.ts)
    - `process.on("SIGINT"` и `process.on("SIGTERM"` — оба findable
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>src/run.ts полностью переписан как daemon-entrypoint; все grep-ловушки пройдены; tsc проходит.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Smoke-тест daemon (SIGINT + mutex + ручной триггер)</name>
  <what-built>
Claude рефакторнул src/run.ts в node-cron daemon. Установлен node-cron. Pipeline вынесен в pipeline.ts. Нужно подтвердить вручную, что три поведенческих контракта работают:
1. `npm start` не завершается после старта, логирует `daemon started, schedule: 0 20 * * * Europe/Moscow`, Ctrl+C → exit 0.
2. Mutex: при активном прогоне второй тик пишет `prev run still in progress — skipping tick`.
3. Ручной триггер через временный cron-паттерн работает, summary-лог приходит.
  </what-built>
  <how-to-verify>
**Шаг A — проверка startup + SIGINT (SC1):**

```bash
npm start
```

Ожидаемый stdout:
```
[<ISO>] [info] daemon started, schedule: 0 20 * * * Europe/Moscow
```

Процесс ВИСИТ, приглашение терминала не возвращается. Нажми `Ctrl+C`:
```
[<ISO>] [info] received SIGINT, stopping cron
```

Через ≤500мс (если не запущен прогон) процесс завершится. Проверь код выхода:
```bash
echo $?
```
Должен быть `0`.

**Шаг B — проверка mutex + summary (SC2 + SC5):**

Временно поменяй в src/run.ts cron pattern с `"0 20 * * *"` на `"*/2 * * * *"` (каждые 2 минуты) — ТОЛЬКО для smoke-теста. Можно закомментировать строку с `0 20 * * *` и добавить новую со smoke-паттерном.

```bash
npm start
```

Жди до 2 минут → должен запуститься tick → в логе появится `[pipeline] runId=...` → прогон обойдёт все каналы (2-3 минуты на 50 каналов) → выведется логRunSummary:
```
[<ISO>] [summary] runId=<8char>
  duration=<N>s
  channels: total=50 succeeded=<N> skipped=<N>
  posts: collected=<N> deduped=<N>
  delivered=<true|false>
  ...
```

Если `delivered=true` — дайджест ушёл в твой приватный канал, открой его и проверь.

Для проверки mutex (SC5): если первый прогон не успел за 2 минуты (на 50 каналах на задержке 1750мс прогон занимает 2-3 минуты), следующий тик должен написать:
```
[<ISO>] [warn] prev run still in progress — skipping tick
```

Нажми Ctrl+C:
- Если в этот момент prогон активен — лог `received SIGINT, stopping cron`, daemon ДОЛЖЕН дождаться завершения прогона, потом exit 0.
- Если прогон не активен — exit 0 сразу.

**Шаг C — ОБЯЗАТЕЛЬНО вернуть cron-паттерн:**

Верни в src/run.ts `"0 20 * * *"` обратно. Удали временные комментарии. Проверь:

```bash
grep -q 'cron.schedule("0 20 \* \* \*"' src/run.ts && echo "OK: pattern restored"
grep -q '"\*/2 \* \* \* \*"' src/run.ts && echo "FAIL: smoke pattern still present" || echo "OK: smoke pattern removed"
```

Обе команды должны показать `OK: ...`. **Эта проверка обязательна перед тем, как писать `approved` в resume-signal.**

**Шаг D (optional — проверка reconnect SC4, требует реального сбоя сети):**

Запусти daemon со smoke-паттерном `*/2 * * * *`. Во время активного прогона (видно что идут `[pipeline] <username>: N постов` строки) выключи Wi-Fi на 10 секунд, потом включи. В логе должно появиться:
```
[telegram] reconnect attempt 1/3 for <username>, waiting 1000ms
```

Если сбой длится долго — `reconnect attempt 2/3 for ..., waiting 2000ms` и так далее. Прогон продолжается, дайджест уходит.

**Это optional-шаг**, но он НЕТРИВИАЛЬНО зависит от plan 04 (именно plan 04 добавляет reconnect-ветку в fetchLast24h). Depends_on плана 05 = [1, 2, 4] — plan 04 обязательно выполнен до этого чекпоинта, поэтому reconnect-код присутствует в src/telegram.ts даже если smoke не выполнялся.
  </how-to-verify>
  <resume-signal>
**Перед тем как писать `approved`, ОБЯЗАТЕЛЬНО выполни в терминале:**

```bash
grep -q 'cron.schedule("0 20 \* \* \*"' src/run.ts && echo "cron-OK" || echo "cron-FAIL"
```

Если вывод — `cron-OK`, продолжай. Если `cron-FAIL` — верни паттерн `"0 20 * * *"` в src/run.ts и повтори grep.

Пиши один из вариантов:
- `approved` — все 3 обязательных шага прошли (startup, SIGINT/exit 0, summary в логе после ручного триггера), **`grep -q 'cron.schedule("0 20 \* \* \*"' src/run.ts` exits 0 (обязательное условие)**, smoke-паттерн `*/2 * * * *` удалён из файла.
- `fail: <details>` — что-то сломалось (опиши stderr или симптом); Claude фиксит и повторяет тест.
- `skip-d` — шаг D (reconnect smoke с обрывом сети) отложен на реальный деплой.
  </resume-signal>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` — 0 ошибок.
2. **Обязательный post-checkpoint grep (финальная проверка SC возврата cron-паттерна):**
   - `grep -q 'cron.schedule("0 20 \* \* \*", tick, { timezone: "Europe/Moscow" })' src/run.ts` exits 0 — ровно этот канонический вызов присутствует в финальной версии src/run.ts.
   - `! grep -qE '"\*/[0-9]+ \* \* \* \*"' src/run.ts` exits 0 — в файле НЕТ ни одного smoke-паттерна вида `"*/N * * * *"`.
   - `grep -c 'cron.schedule' src/run.ts` = 1 — ровно один вызов cron.schedule, не два (исключает забытую закомментированную/дублированную строку).
3. После Task 3: в summary-логе ручного триггера видны `channels: total=50`, `delivered=true|false`, `durationMs` (через `duration=Xs`).
4. Исходные файлы пайплайна (src/pipeline.ts, src/logger.ts, src/telegram.ts) не затронуты этим планом.
</verification>

<success_criteria>
- npm start запускает daemon, процесс не завершается после старта (DAEMON-01) ✓ Task 3 шаг A
- Ежедневный cron `0 20 * * *` с timezone Europe/Moscow (DAEMON-02) ✓ Task 2 grep + verification §2 финальный grep
- Mutex isRunning отклоняет параллельные тики (DAEMON-03) ✓ Task 2 grep + Task 3 шаг B
- runOnInit отсутствует, рестарт не триггерит дайджест (DAEMON-04) ✓ Task 2 grep (`! grep -q runOnInit`)
- node-cron@^3.0.3 установлен (DEPLOY-02) ✓ Task 1
- Финальный cron-паттерн в src/run.ts ровно `"0 20 * * *"`, smoke-паттерн удалён ✓ verification §2
- npx tsc --noEmit проходит
</success_criteria>

<output>
После выполнения создать `.planning/phases/02-daemon-50/02-05-SUMMARY.md` с:
- Старое/новое состояние src/run.ts (2 блока кода)
- Список зависимостей package.json до/после
- Результаты smoke-теста (startup, SIGINT exit code, наличие `prev run still in progress`, пример summary-лога)
- Подтверждение финального cron-паттерна через `grep -q 'cron.schedule("0 20 \* \* \*"' src/run.ts` exit 0
</output>
