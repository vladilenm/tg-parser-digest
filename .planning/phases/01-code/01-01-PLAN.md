---
phase: 01-code
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - .env.example
  - .gitignore
  - src/types.ts
  - src/schema.ts
  - src/summarize.ts
  - src/dedup.ts
  - src/archive.ts
  - src/alert.ts
  - src/pipeline.ts
  - src/run.ts
  - src/logger.ts
  - docs/RUNBOOK.md
  - docs/CHANNELS.md
autonomous: true
requirements:
  - STRUCT-01
  - STRUCT-02
  - STRUCT-03
  - RENDER-01
  - RENDER-02
  - RENDER-03
  - DEDUP-01
  - DEDUP-02
  - ARCH-01
  - ARCH-02
  - ALERT-01
  - ALERT-02
  - DOC-04
  - DOC-05
user_setup:
  - service: telegram-alert-bot
    why: "ALERT-01 — отдельный alert-bot шлёт в личку владельца на любую необработанную ошибку pipeline. Создание бота через @BotFather и получение chat_id владельца — операторская работа, не выполняется кодом."
    env_vars:
      - name: BOT_TOKEN_ALERTS
        source: "@BotFather → /newbot → Bot Token (отдельный от TG_BOT_TOKEN, не загрязняет канал Заказчика)"
      - name: ALERTS_CHAT_ID
        source: "Личка владельца (numeric chat_id, узнать через @userinfobot или @username_to_id_bot, начав диалог с alert-ботом и переслав сообщение)"
    dashboard_config:
      - task: "Начать диалог с новым alert-ботом (нажать /start), иначе он не сможет писать в личку"
        location: "Telegram client — поиск по username нового бота"

must_haves:
  truths:
    - "Сводка в Telegram-канале Заказчика разбита на 5 именованных секций (🚢 Бункер, 🛢 Масла, ✈️ Керосин, ⚗️ Нефтехимия, 🛣 Битум) + 🏢 Упоминания компаний; пустые секции явно помечены `<i>— нет упоминаний за сутки</i>`"
    - "Каждый item в сводке содержит кликабельный deep-link `https://t.me/<channel>/<msgId>` через `<a href=...>@channel</a>`"
    - "После `pm2 restart` (между cron-тиками) новости из вчерашней сводки не появляются повторно — `data/hash-cache.json` пережил рестарт и фильтрует через 14-дневное окно"
    - "После каждого прогона daemon в директории появляются файлы `data/raw/YYYY-MM-DD.json` (до dedup) и `data/output/YYYY-MM-DD.md` (после доставки); `data/output/*.md` содержит байт-в-байт тот же HTML, что был отправлен в канал"
    - "Любая необработанная ошибка pipeline (DeepSeek timeout, Zod-мисматч после retry, сетевой сбой) порождает алерт в личку владельца за ≤60 секунд через отдельный `BOT_TOKEN_ALERTS`; в канал Заказчика на этой ошибке не уходит ничего"
    - "`npx tsc --noEmit` завершается без ошибок"
    - "Один локальный `npm start` smoke-прогон работает без unhandled rejection и создаёт оба архивных файла"
  artifacts:
    - path: "src/schema.ts"
      provides: "Zod-схема DigestJsonSchema (5 категорий + mentions) + категориальные литералы"
      contains: "export const DigestJsonSchema = z.object"
    - path: "src/dedup.ts"
      provides: "normalize + sha256 + load/save hash-cache + dedupAgainstCache"
      contains: "export function normalize"
    - path: "src/archive.ts"
      provides: "writeRaw + writeOutput + atomic .tmp + rename helper"
      contains: "renameSync"
    - path: "src/alert.ts"
      provides: "sendAlert через BOT_TOKEN_ALERTS Bot API"
      contains: "BOT_TOKEN_ALERTS"
    - path: "src/types.ts"
      provides: "Расширенные типы DigestJson (5 категорий + mentions), DigestItem (mentions[]), RunSummary (postsDropped)"
      contains: "postsDropped"
    - path: "src/summarize.ts"
      provides: "5-категорийный SYSTEM_PROMPT + Zod-валидация + retry x1 + категорийный HTML-рендер с emoji + deep-link + inline маркеры"
      contains: "DigestJsonSchema"
    - path: "src/pipeline.ts"
      provides: "интеграция writeRaw → dedupAgainstCache → summarize → sendToChannel → writeOutput + commitHashCache"
      contains: "writeRaw"
    - path: "src/run.ts"
      provides: "tick() с try/catch и await sendAlert на любую ошибку"
      contains: "sendAlert"
    - path: "docs/RUNBOOK.md"
      provides: "5 сценариев сбоя оператора: DeepSeek 5xx, Telegram FloodWait, парсер не видит канал, диск переполнен, network down"
      min_lines: 80
    - path: "docs/CHANNELS.md"
      provides: "Lifecycle канала: добавление, проверка подписки user-аккаунта, удаление, карантин, замена PLACEHOLDER_NN"
      min_lines: 60
    - path: ".env.example"
      provides: "BOT_TOKEN_ALERTS + ALERTS_CHAT_ID секция с пояснением"
      contains: "BOT_TOKEN_ALERTS"
    - path: "package.json"
      provides: "zod добавлен в dependencies"
      contains: "\"zod\""
    - path: ".gitignore"
      provides: "data/ исключён из git, кроме .gitkeep"
      contains: "data/"
  key_links:
    - from: "src/pipeline.ts"
      to: "src/archive.ts:writeRaw"
      via: "вызов после fetch, до dedup"
      pattern: "await writeRaw\\("
    - from: "src/pipeline.ts"
      to: "src/dedup.ts:dedupAgainstCache"
      via: "вызов перед summarize"
      pattern: "dedupAgainstCache\\("
    - from: "src/pipeline.ts"
      to: "src/dedup.ts:commitHashCache"
      via: "вызов после успешной доставки"
      pattern: "commitHashCache\\("
    - from: "src/pipeline.ts"
      to: "src/archive.ts:writeOutput"
      via: "вызов после успешной доставки"
      pattern: "writeOutput\\("
    - from: "src/run.ts"
      to: "src/alert.ts:sendAlert"
      via: "await в catch блоке tick()"
      pattern: "await sendAlert\\("
    - from: "src/summarize.ts"
      to: "src/schema.ts:DigestJsonSchema"
      via: "import + .safeParse() с retry x1"
      pattern: "DigestJsonSchema\\.(safeParse|parse)"
    - from: "src/summarize.ts:renderHtml"
      to: "DigestJson categories"
      via: "вывод 5 фиксированных секций по эмодзи-заголовкам в порядке D-03"
      pattern: "🚢 Бункер|🛢 Масла|✈️ Керосин|⚗️ Нефтехимия|🛣 Битум|🏢 Упоминания компаний"
---

<objective>
Phase 1 «Code» закрывает 14 REQ из v3.0 milestone в одном mega-plan'е (D-06):
структурированный 5-категорийный дайджест с компаниями-маркерами и deep-link
(STRUCT-01..03, RENDER-01..03), кросс-прогонная SHA-256 дедупа с rolling 14-day
hash-cache (DEDUP-01..02), ФС-архивы raw/output с атомарной записью (ARCH-01..02),
отдельный alert-bot в личку владельца на любую необработанную ошибку (ALERT-01..02),
оперативная документация RUNBOOK + CHANNELS (DOC-04..05).

Purpose: после деплоя этого plan'а на VPS daemon готов к 7-day smoke-acceptance
(Phase 2). Закрывает обязательства Этапа 1 договора №2020 для Заказчика.

Output: ~8 новых/изменённых TS-модулей + 2 markdown-документа + обновлённые
package.json/.env.example/.gitignore. Все типы строгие, `npx tsc --noEmit` чистый.

Wave-порядок task'ов внутри plan'а (D-07, locked):
- Wave 1 (T1-T7): STRUCT-01..03 + RENDER-01..03 — Zod-схема, типы, prompt, рендер
- Wave 2 (T8-T11): DEDUP-01..02 + ARCH-01..02 — dedup-модуль, archive-модуль, интеграция в pipeline
- Wave 3 (T12-T15): ALERT-01..02 + DOC-04..05 — alert-модуль, обёртка run.ts, RUNBOOK, CHANNELS
- Wave 4 (T16): единственный финальный verify (D-08) — tsc + npm start smoke
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-code/01-CONTEXT.md
@docs/intent-v3.0.md
@spec-app.md
@src/types.ts
@src/pipeline.ts
@src/summarize.ts
@src/deliver.ts
@src/run.ts
@src/logger.ts
@src/telegram.ts
@package.json
@.env.example
@.gitignore

<interfaces>
<!-- Контракты, которые executor найдёт в существующем коде. -->
<!-- Эти подписи НЕ менять — только расширять. -->

From src/types.ts (current — будет расширён в T2):
```typescript
export interface Post {
  channelUsername: string;
  messageId: number;
  postedAt: string;  // ISO 8601
  text: string;
  url: string;       // "https://t.me/<username>/<messageId>"
}

export interface DigestItem {
  summary: string;
  keyQuote: string;  // дословная подстрока Post.text
  url: string;
  channel: string;
}

export interface DigestSection {
  title: string;
  items: DigestItem[];
}

export interface DigestJson {
  generatedAt: string;
  sections: DigestSection[];   // СТАРЫЙ формат — будет переписан в T2 на 5 фиксированных категорий
}

export interface RunSummary {
  runId: string;
  startedAt: string;
  finishedAt: string;
  durationMs: number;
  channelsTotal: number;
  channelsSucceeded: number;
  channelsSkipped: number;
  postsCollected: number;
  postsDeduped: number;            // расширим (in-memory + hash-cache hits)
  digestDelivered: boolean;
  errors: string[];
  // NEW в T2: postsDropped: number  (STRUCT-03)
}
```

From src/summarize.ts (текущий, частично переписывается):
```typescript
export function escapeHtml(s: string): string;        // переиспользуем как есть
export async function summarize(posts: Post[]): Promise<string /* html */>;
// internal: SYSTEM_PROMPT, validate(), verifyExtractiveness(), renderHtml()
//           validate() УДАЛЯЕТСЯ (D-16) — заменяется DigestJsonSchema.safeParse
//           verifyExtractiveness() СОХРАНЯЕТСЯ (Core Value: keyQuote.includes())
//           SYSTEM_PROMPT и renderHtml() переписываются под 5-категорийную структуру
```

From src/pipeline.ts (точки интеграции):
```typescript
export async function runPipeline(): Promise<RunSummary>;
// Существующий runId: crypto.randomUUID().slice(0, 8) — line 46
// Insertion points (после T9-T10):
//   line ~83 (после fetch loop, до summarize):       await writeRaw(allPosts, runId)
//   line ~103 (перед summarize):                      const freshPosts = await dedupAgainstCache(allPosts, runId)
//   line ~106 (после sendToChannel):                  await writeOutput(html, runId); await commitHashCache(freshPosts, runId)
```

From src/deliver.ts:
```typescript
export function chunkHtml(html: string, max?: number): string[];   // RENDER-03 — переиспользуем
export async function sendToChannel(html: string): Promise<void>;  // без изменений
```

From src/run.ts:
```typescript
// tick() — оборачивает runPipeline; добавляем await sendAlert(...) в catch
```

From src/telegram.ts (без изменений):
```typescript
export function createClient(): TelegramClient;
export async function fetchLast24h(client, username, opts): Promise<Post[]>;
export function sleep(ms: number): Promise<void>;
export function randomInt(min: number, max: number): number;
```
</interfaces>

</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| process.env → process | Секреты (`BOT_TOKEN_ALERTS`, `ALERTS_CHAT_ID`, `DEEPSEEK_API_KEY`, `TG_BOT_TOKEN`, `TG_SESSION`) читаются из `.env`. `.env` в `.gitignore`. |
| Telegram channel content → DeepSeek API | Контент публичных каналов передаётся как `{posts: [...]}` третьей стороне (DeepSeek). Operator-accepted. |
| DeepSeek JSON response → pipeline | Невалидируемый внешний JSON. Mitigation: Zod-схема `DigestJsonSchema.safeParse` + retry x1, fail → throw → ALERT. |
| Telegram channel content → HTML render → канал Заказчика | Контент попадает в HTML с `parse_mode: HTML`. Mitigation: `escapeHtml()` для `summary`, `keyQuote`, `channel`, маркеров. |
| pipeline error → alert-bot Bot API | Stack/error.message могут содержать пути или секреты. Mitigation: alert-payload не сериализует `process.env`. |
| ФС-state (data/) → ОС | Файлы пишутся в локальную `data/`. Доступ ограничен ОС-правами PM2-юзера. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | `.env` секреты в git | mitigate | `.env` уже в `.gitignore`. Verify: `grep -E '^\.env$' .gitignore` exits 0. Никогда не логировать `process.env`. |
| T-01-02 | Information Disclosure | alert-payload в личку владельца | mitigate | `sendAlert({stage, message, runId, stack})` — НЕ сериализует `process.env`. Acceptance: `grep -E 'process\.env' src/alert.ts` returns 0 lines или только чтение `BOT_TOKEN_ALERTS`/`ALERTS_CHAT_ID`. |
| T-01-03 | Information Disclosure | data/raw/*.json | accept | Архивы локально на VPS, доступ под user'ом PM2; data/ в .gitignore. Acceptable для single-tenant. |
| T-01-04 | Tampering | DeepSeek JSON injection через post.text | mitigate | Zod safeParse строго валидирует enum категорий и структуру. `verifyExtractiveness` отбрасывает item, чьего `keyQuote` нет в `post.text`. |
| T-01-05 | Tampering | HTML injection через summary/keyQuote/channel | mitigate | `escapeHtml()` применяется к каждому user-controlled полю в `renderHtml`. Acceptance: каждое употребление `${field}` в HTML wrapped в `escapeHtml(...)`. Inline-маркеры (`[РОСНЕФТЬ]`) — литералы, не user input. |
| T-01-06 | Tampering | hash-cache poisoning (атакующий с FS-доступом «съедает» будущие посты) | accept | Файл локальный на VPS, ОС-права PM2-user'а. Single-tenant operator scope. Acceptable. |
| T-01-07 | Denial of Service | Alert-loop при petля в pipeline | mitigate | Cron 1 раз в сутки → ≤1 ошибка → ≤1 alert/сутки. Throttle не нужен (D-14). При самом alert-fail — `console.error` без retry (D-15). |
| T-01-08 | Denial of Service | data/raw/*.json безразмерный рост | accept | 7-day smoke оценит размер; ротация в v3.1 backlog. Каждый файл max ~50 каналов × 50 постов = 2500 объектов. |
| T-01-09 | Spoofing | Path traversal в имени data/raw|output | mitigate | Имя = `dayjs(...).format('YYYY-MM-DD')` детерминистично. Не зависит от `username` или `messageId`. Acceptance: regex `\d{4}-\d{2}-\d{2}\.(json|md)$` matches. |
| T-01-10 | Supply Chain | zod compromise | mitigate | Pin `^3.x` в package.json (минорная версия pinned для воспроизводимости). zod — широко аудированный пакет, low risk. |
| T-01-11 | Repudiation | Отсутствие audit-trail отправленных дайджестов | mitigate | `data/output/*.md` сохраняет идентичную копию; `data/raw/*.json` — pre-dedup исходник; `pm2 logs` — runtime trail. ARCH-02 inv: byte-identical to sent. |
</threat_model>

<tasks>

<!-- =================================================================== -->
<!-- WAVE 1 — STRUCT-01..03 + RENDER-01..03 (T1-T7)                      -->
<!-- =================================================================== -->

<task type="auto" tdd="false">
  <name>Task 1: Add zod dependency to package.json</name>
  <files>package.json</files>
  <read_first>
    - package.json (текущий: 4 runtime deps)
    - .planning/phases/01-code/01-CONTEXT.md §«D-16: Zod-схема живёт в src/schema.ts»
    - CLAUDE.md §«Tech stack» (Node 20.6+, ESM, tsx без сборки)
  </read_first>
  <action>
    В `package.json` в секции `"dependencies"` добавить ровно одну строку:
    `"zod": "^3.23.0"` — между существующими `node-cron` и `openai` (alphabetical
    после "openai", т.е. порядок: node-cron, openai, telegram, yaml, zod).
    Не добавлять никаких других пакетов. Не менять `devDependencies`, scripts,
    engines, type. После записи запустить `npm install` для обновления
    `package-lock.json` (если он существует) и установки zod в node_modules.

    Итоговый секция dependencies (5 строк, alphabetical):
    ```json
    "dependencies": {
      "node-cron": "^3.0.3",
      "openai": "^4.0.0",
      "telegram": "^2.22.0",
      "yaml": "^2.5.0",
      "zod": "^3.23.0"
    }
    ```
  </action>
  <acceptance_criteria>
    - `grep -E '"zod":\s*"\^3\.' package.json` → 1 match
    - `node -e "console.log(require('zod').z.string)"` → выводит `[Function: string]` (zod установлен)
    - `node -e "const p=require('./package.json'); console.log(Object.keys(p.dependencies).length)"` → 5
    - dependencies отсортированы alphabetically: `node -e "const d=Object.keys(require('./package.json').dependencies); console.log(JSON.stringify(d)===JSON.stringify([...d].sort()))"` → `true`
  </acceptance_criteria>
  <verify>
    <automated>grep -E '"zod":\s*"\^3\.' package.json && node -e "require('zod')"</automated>
  </verify>
  <done>
    zod ^3.23.0 в package.json, установлен в node_modules, импорт `import { z } from 'zod'` работает в TS-файлах под tsx.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Extend src/types.ts for 5-category digest + mentions</name>
  <files>src/types.ts</files>
  <read_first>
    - src/types.ts (текущая версия — старый плоский DigestJson { sections: DigestSection[] })
    - .planning/phases/01-code/01-CONTEXT.md §«D-04: только орфаны в mentions» + §«code_context: расширения types.ts»
    - .planning/REQUIREMENTS.md §«STRUCT-01, STRUCT-03» (5 категорий + mentions, postsDropped)
  </read_first>
  <action>
    Полностью переписать `src/types.ts`. Сохранить `Post` без изменений.
    Расширить `DigestItem`, переписать `DigestJson`/`DigestSection` под 5 фиксированных
    категорий + блок mentions, расширить `RunSummary` полем `postsDropped`.

    Конкретное содержимое (копировать verbatim):
    ```typescript
    // src/types.ts — общие типы для пайплайна tg-parser-demo (v3.0).

    export interface Post {
      channelUsername: string; // без "@", как в channels.yaml
      messageId: number;
      postedAt: string; // ISO 8601
      text: string;
      url: string; // "https://t.me/<username>/<messageId>"
    }

    /** Литералы 5 категорий v3.0. Порядок секций — D-03. */
    export type Category = "bunker" | "oil" | "kerosene" | "petrochem" | "bitumen";

    /** Литералы 3 целевых компаний-маркеров (D-04). */
    export type Mention = "rosneft" | "lukoil" | "gazprom";

    export interface DigestItem {
      category: Category | null;        // null → пост попал только в mentions (orphan)
      summary: string;                  // 1–2 предложения, до 250 символов
      keyQuote: string;                 // дословная подстрока Post.text (Core Value)
      url: string;                      // должен совпасть с Post.url
      channel: string;                  // Post.channelUsername (без "@")
      mentions: Mention[];              // подмножество ["rosneft","lukoil","gazprom"]; для orphan-mention items длина >=1
    }

    /** Структурированный ответ DeepSeek для v3.0: ровно 5 категорий + блок mentions (orphans only). */
    export interface DigestJson {
      generatedAt: string;              // ISO 8601, заполняется DeepSeek
      bunker: DigestItem[];
      oil: DigestItem[];
      kerosene: DigestItem[];
      petrochem: DigestItem[];
      bitumen: DigestItem[];
      mentions: DigestItem[];           // только orphans (D-04): пост без category, но с mentions[]
    }

    export interface RunSummary {
      runId: string;                    // crypto.randomUUID().slice(0, 8)
      startedAt: string;
      finishedAt: string;
      durationMs: number;
      channelsTotal: number;
      channelsSucceeded: number;
      channelsSkipped: number;
      postsCollected: number;           // уникальных постов после in-memory + hash-cache dedup
      postsDeduped: number;             // отброшено дублей (in-memory + hash-cache hits)
      postsDropped: number;             // STRUCT-03: отброшено LLM (вне 5 категорий и без mentions)
      digestDelivered: boolean;
      errors: string[];
    }
    ```

    Старый `DigestSection` интерфейс УДАЛИТЬ — он больше не используется.
  </action>
  <acceptance_criteria>
    - `grep -E "export type Category" src/types.ts` → 1 match
    - `grep -E "export type Mention" src/types.ts` → 1 match
    - `grep -cE "^\s+(bunker|oil|kerosene|petrochem|bitumen|mentions):\s*DigestItem\[\];" src/types.ts` → 6 (в DigestJson)
    - `grep -E "postsDropped:\s*number" src/types.ts` → 1 match
    - `grep -E "DigestSection" src/types.ts` → 0 matches (старый тип удалён)
    - `grep -E "category:\s*Category\s*\|\s*null" src/types.ts` → 1 match
    - `npx tsc --noEmit` exits 0 (старые потребители временно сломаны — ОЖИДАЕМО, фиксится в T3-T7)
  </acceptance_criteria>
  <verify>
    <automated>grep -E "export type Category" src/types.ts && grep -E "export type Mention" src/types.ts && grep -E "postsDropped:\s*number" src/types.ts && [ "$(grep -cE '^\s+(bunker|oil|kerosene|petrochem|bitumen|mentions):\s*DigestItem\[\];' src/types.ts)" = "6" ]</automated>
  </verify>
  <done>
    src/types.ts содержит расширенные типы. Если `npx tsc --noEmit` падает — это нормально на этом шаге (потребители (`summarize.ts`, `pipeline.ts`) починятся в следующих task'ах). Финальный tsc-чек — в T16.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Create src/schema.ts with Zod DigestJsonSchema</name>
  <files>src/schema.ts</files>
  <read_first>
    - src/types.ts (после T2 — Category, Mention, DigestJson, DigestItem)
    - .planning/REQUIREMENTS.md §«STRUCT-02» (Zod-валидация + retry x1)
    - .planning/phases/01-code/01-CONTEXT.md §«D-16: Zod в schema.ts отдельно от summarize»
  </read_first>
  <action>
    Создать новый файл `src/schema.ts` с Zod-схемой, точно соответствующей типам из `src/types.ts` после T2. Экспортировать также вспомогательные литералы для prompt'а.

    Полное содержимое:
    ```typescript
    // src/schema.ts — Zod-валидация ответа DeepSeek для v3.0 (STRUCT-02).
    // Живёт отдельно от src/summarize.ts (D-16): prompt и schema эволюционируют независимо.

    import { z } from "zod";

    export const CATEGORIES = ["bunker", "oil", "kerosene", "petrochem", "bitumen"] as const;
    export const MENTIONS = ["rosneft", "lukoil", "gazprom"] as const;

    export const CategorySchema = z.enum(CATEGORIES);
    export const MentionSchema = z.enum(MENTIONS);

    export const DigestItemSchema = z.object({
      category: CategorySchema.nullable(),
      summary: z.string().min(1).max(500),
      keyQuote: z.string().min(1),
      url: z.string().url(),
      channel: z.string().min(1),
      mentions: z.array(MentionSchema),
    });

    export const DigestJsonSchema = z.object({
      generatedAt: z.string().min(1),
      bunker: z.array(DigestItemSchema),
      oil: z.array(DigestItemSchema),
      kerosene: z.array(DigestItemSchema),
      petrochem: z.array(DigestItemSchema),
      bitumen: z.array(DigestItemSchema),
      mentions: z.array(DigestItemSchema),
    });

    export type DigestJsonZ = z.infer<typeof DigestJsonSchema>;
    ```
  </action>
  <acceptance_criteria>
    - `test -f src/schema.ts` → exit 0
    - `grep -E "export const DigestJsonSchema = z\.object\(" src/schema.ts` → 1 match
    - `grep -E 'export const CATEGORIES = \["bunker", "oil", "kerosene", "petrochem", "bitumen"\] as const' src/schema.ts` → 1 match
    - `grep -E 'export const MENTIONS = \["rosneft", "lukoil", "gazprom"\] as const' src/schema.ts` → 1 match
    - `grep -cE "^\s+(bunker|oil|kerosene|petrochem|bitumen|mentions):\s*z\.array" src/schema.ts` → 6
    - `node --import tsx -e "import('./src/schema.js').then(m => console.log(typeof m.DigestJsonSchema.safeParse))"` → `function`
  </acceptance_criteria>
  <verify>
    <automated>test -f src/schema.ts && grep -E "export const DigestJsonSchema = z\.object\(" src/schema.ts && grep -E 'CATEGORIES = \["bunker", "oil", "kerosene", "petrochem", "bitumen"\]' src/schema.ts && grep -E 'MENTIONS = \["rosneft", "lukoil", "gazprom"\]' src/schema.ts</automated>
  </verify>
  <done>
    schema.ts экспортирует DigestJsonSchema и литералы CATEGORIES/MENTIONS; safeParse доступен.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Rewrite SYSTEM_PROMPT in src/summarize.ts for 5-category structure</name>
  <files>src/summarize.ts</files>
  <read_first>
    - src/summarize.ts (текущий SYSTEM_PROMPT, validate(), renderHtml() — все будут переписаны)
    - src/schema.ts (после T3)
    - src/types.ts (после T2)
    - .planning/phases/01-code/01-CONTEXT.md §«D-04 (orphans only mentions)» + §«D-16 validate() удалить»
    - .planning/REQUIREMENTS.md §«STRUCT-01, STRUCT-02, STRUCT-03» (drop, retry x1, 5 категорий)
    - spec-app.md §8 (экстрактивный prompt — keyQuote дословно)
  </read_first>
  <action>
    В `src/summarize.ts` переписать `SYSTEM_PROMPT` (lines 10-32 текущего файла) под 5-категорийную структуру v3.0. УДАЛИТЬ полностью функцию `validate()` (lines 60-94 текущего файла) — заменяется Zod-валидацией в T5. Сохранить `escapeHtml()`, `formatDateRu()`, `verifyExtractiveness()` (последний адаптировать в T5 под новый DigestJson).

    Новый SYSTEM_PROMPT (копировать verbatim):
    ```typescript
    const SYSTEM_PROMPT = [
      "Ты — экстрактивный редактор русскоязычной ленты по нефтегазу/нефтехимии РФ.",
      "На вход получаешь JSON { posts: [...] }, где posts[i] = { channelUsername, postedAt, text, url }.",
      "",
      "Твоя задача — вернуть СТРОГИЙ JSON по 5 фиксированным категориям + блок mentions.",
      "",
      "Категории (ровно эти 5 ключей):",
      "  - bunker     — судовое топливо, мазут, бункеровка, морские порты",
      "  - oil        — масла моторные/индустриальные, смазочные материалы",
      "  - kerosene   — авиакеросин, jet fuel, авиатопливо",
      "  - petrochem  — нефтехимия (этилен, пропилен, полимеры, нефтехимические продукты)",
      "  - bitumen    — битум, дорожно-строительные нефтепродукты",
      "",
      "Компании-маркеры (ровно эти 3 значения):",
      "  - rosneft, lukoil, gazprom",
      "",
      "Жёсткие правила:",
      "1) Пиши ТОЛЬКО по фактам из text. Никаких домыслов, чисел или имён, отсутствующих в исходнике.",
      "2) keyQuote каждой записи ДОЛЖЕН быть дословной подстрокой text (для серверной верификации).",
      "3) summary — 1–2 предложения на русском, до 250 символов.",
      "4) Каждый item имеет mentions: [...] — список упомянутых в text компаний из {rosneft,lukoil,gazprom}, может быть пустым.",
      "5) Если пост попадает в одну из 5 категорий — клади его в соответствующий массив (bunker/oil/kerosene/petrochem/bitumen) с category равной этому ключу. Mentions заполняй параллельно.",
      "6) Если пост НЕ попадает ни в одну из 5 категорий, но содержит хотя бы одну из 3 компаний — клади его в массив mentions с category=null и непустым mentions[]. Это «orphan-mention».",
      "7) Если пост НЕ попадает в категорию И не упоминает ни одной из 3 компаний — отбрасывай (не возвращай нигде).",
      "8) Один пост попадает РОВНО в один массив (либо в категорию, либо в orphan-mention), не в оба.",
      "9) Отбирай не более 15 самых содержательных постов в сумме по всем массивам.",
      "10) Возвращай строго JSON без markdown и комментариев следующего вида:",
      "{",
      '  "generatedAt": "ISO8601",',
      '  "bunker":    [ { "category":"bunker",    "summary":"...", "keyQuote":"...", "url":"...", "channel":"...", "mentions":["rosneft"] } ],',
      '  "oil":       [],',
      '  "kerosene":  [],',
      '  "petrochem": [],',
      '  "bitumen":   [],',
      '  "mentions":  [ { "category":null, "summary":"...", "keyQuote":"...", "url":"...", "channel":"...", "mentions":["lukoil"] } ]',
      "}",
    ].join("\n");
    ```

    Также добавить в начало файла import:
    ```typescript
    import { DigestJsonSchema } from "./schema.js";
    import type { Category, Mention } from "./types.js";
    ```

    УДАЛИТЬ функцию `validate(x: unknown): asserts x is DigestJson` целиком (она будет заменена в T5 на DigestJsonSchema.safeParse + retry).

    На этом шаге renderHtml() и summarize() могут временно ломаться по типам (bunker/oil/etc вместо sections) — это ожидаемо, фиксится в T5+T6.
  </action>
  <acceptance_criteria>
    - `grep -cE "BUNKER|OILS|KEROSENE|PETROCHEM|BITUMEN" src/summarize.ts` — игнорировать; вместо этого:
    - `grep -cE "(bunker|oil|kerosene|petrochem|bitumen)" src/summarize.ts` → ≥5 (в SYSTEM_PROMPT)
    - `grep -E "rosneft.*lukoil.*gazprom" src/summarize.ts` → ≥1 match (в prompt)
    - `grep -E "import.*DigestJsonSchema.*from.*schema" src/summarize.ts` → 1 match
    - `grep -cE "^function validate\(x: unknown\)" src/summarize.ts` → 0 (старая функция удалена)
    - `grep -E "keyQuote.*ДОЛЖЕН.*дословной" src/summarize.ts` → 1 match (extractiveness rule сохранён)
    - `grep -E "orphan-mention" src/summarize.ts` → 1 match
  </acceptance_criteria>
  <verify>
    <automated>grep -E "import.*DigestJsonSchema.*from.*schema" src/summarize.ts && grep -E "orphan-mention" src/summarize.ts && [ "$(grep -cE '^function validate\(x: unknown\)' src/summarize.ts)" = "0" ] && grep -E "rosneft.*lukoil.*gazprom" src/summarize.ts</automated>
  </verify>
  <done>
    SYSTEM_PROMPT инструктирует LLM возвращать 5 фиксированных категорий + mentions, старый validate() удалён, импорты для schema подготовлены. Файл может временно не компилиться — починится в T5/T6.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 5: Wire Zod safeParse + retry x1 in src/summarize.ts (STRUCT-02, STRUCT-03)</name>
  <files>src/summarize.ts</files>
  <read_first>
    - src/summarize.ts (после T4)
    - src/schema.ts (T3)
    - src/types.ts (T2 — DigestJson, DigestItem, RunSummary с postsDropped)
    - .planning/REQUIREMENTS.md §«STRUCT-02» (retry x1) + §«STRUCT-03» (drop counted in postsDropped)
    - .planning/phases/01-code/01-CONTEXT.md §«code_context: verifyExtractiveness сохраняется»
  </read_first>
  <action>
    В `src/summarize.ts` (после T4):

    1. Адаптировать `verifyExtractiveness(digest, posts)` под новую структуру `DigestJson` (5 категорий + mentions). Сигнатура остаётся, но внутри теперь итерация не по `digest.sections`, а по 6 массивам (`bunker`, `oil`, `kerosene`, `petrochem`, `bitumen`, `mentions`). Возвращать `{ digest: DigestJson, droppedCount: number }`. Логика keyQuote-substring проверки сохраняется (Core Value). `byUrl: Map<string, Post>` сохраняется. Отброшенные items увеличивают `droppedCount`.

       Точная сигнатура:
       ```typescript
       function verifyExtractiveness(
         digest: DigestJson,
         posts: Post[]
       ): { digest: DigestJson; droppedCount: number } {
         const byUrl = new Map<string, Post>();
         for (const p of posts) byUrl.set(p.url, p);
         let droppedCount = 0;

         const filterArr = (items: DigestItem[]): DigestItem[] => {
           const out: DigestItem[] = [];
           for (const item of items) {
             const post = byUrl.get(item.url);
             if (!post) {
               droppedCount++;
               console.warn(
                 `[summarize] skip (url not in source): channel=${item.channel} url=${item.url}`
               );
               continue;
             }
             const needle = item.keyQuote.trim();
             if (!post.text.includes(needle)) {
               droppedCount++;
               const snippet = post.text.slice(0, 60).replace(/\n/g, " ");
               console.warn(
                 `[summarize] skip (keyQuote not in source): channel=${post.channelUsername} messageId=${post.messageId} keyQuote="${needle}" textSnippet="${snippet}"`
               );
               continue;
             }
             out.push(item);
           }
           return out;
         };

         return {
           digest: {
             generatedAt: digest.generatedAt,
             bunker:    filterArr(digest.bunker),
             oil:       filterArr(digest.oil),
             kerosene:  filterArr(digest.kerosene),
             petrochem: filterArr(digest.petrochem),
             bitumen:   filterArr(digest.bitumen),
             mentions:  filterArr(digest.mentions),
           },
           droppedCount,
         };
       }
       ```

    2. Переписать публичную `summarize(posts)` так:
       - Меняет return type с `Promise<string>` на `Promise<{ html: string; postsDropped: number }>` (нужно, чтобы pipeline.ts мог записать `postsDropped` в RunSummary).
       - Делает первый запрос к DeepSeek с текущим SYSTEM_PROMPT.
       - Парсит JSON; гоняет `DigestJsonSchema.safeParse(parsed)`.
       - Если `success === false` → делает retry x1 с прежним SYSTEM_PROMPT + дополнительным system-сообщением: `"Предыдущий ответ не прошёл схема-валидацию. Верни СТРОГИЙ JSON ровно по структуре, описанной в первой системной инструкции, без markdown."`
       - Если retry также `success === false` → throw `Error("DeepSeek schema mismatch after retry: " + JSON.stringify(result.error.issues).slice(0, 500))` (этот throw поднимется в pipeline → run.ts → ALERT-02).
       - На успехе → вызывает `verifyExtractiveness(parsed, posts)` → получает `{ digest, droppedCount }`.
       - Рендерит HTML через `renderHtml(digest, posts)` (renderHtml переписывается в T6).
       - Возвращает `{ html, postsDropped: droppedCount }`.

    Сохранить existing `console.error` логирование raw JSON при `JSON.parse` fail.

    Не трогать функцию `escapeHtml()` и `formatDateRu()`.
  </action>
  <acceptance_criteria>
    - `grep -E "DigestJsonSchema\.safeParse" src/summarize.ts` → ≥2 matches (initial + retry)
    - `grep -E "retry|повтор" src/summarize.ts` → ≥1 match
    - `grep -E "schema mismatch after retry" src/summarize.ts` → 1 match (throw message)
    - `grep -E "Promise<\{ html: string; postsDropped: number \}>" src/summarize.ts` → 1 match (new return type)
    - `grep -E "verifyExtractiveness\(parsed, posts\)" src/summarize.ts` → 1 match
    - `grep -E "post\.text\.includes\(needle\)" src/summarize.ts` → 1 match (extractiveness preserved)
    - `grep -E "filterArr" src/summarize.ts` → ≥6 matches (для 6 массивов: 1 объявление + 6 вызовов)
  </acceptance_criteria>
  <verify>
    <automated>[ "$(grep -cE 'DigestJsonSchema\.safeParse' src/summarize.ts)" -ge "2" ] && grep -E "schema mismatch after retry" src/summarize.ts && grep -E "Promise<\{ html: string; postsDropped: number \}>" src/summarize.ts && grep -E "post\.text\.includes\(needle\)" src/summarize.ts</automated>
  </verify>
  <done>
    summarize() возвращает `{html, postsDropped}`. Невалидный schema-ответ → 1 retry → throw на повторном fail. verifyExtractiveness работает по 5 категориям + mentions. tsc может временно падать на pipeline.ts (consumer) — починится в T10.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 6: Rewrite renderHtml in src/summarize.ts — 5 fixed sections + emoji + deep-link + inline mention markers</name>
  <files>src/summarize.ts</files>
  <read_first>
    - src/summarize.ts (после T5)
    - src/types.ts (T2)
    - .planning/phases/01-code/01-CONTEXT.md §«D-01 HTML», §«D-02 emoji+`<b>`», §«D-03 фиксированный порядок», §«D-04 orphans only», §«D-05 формат буллета», §«specifics: inline-маркеры [РОСНЕФТЬ]»
    - .planning/REQUIREMENTS.md §«RENDER-01, RENDER-02»
    - src/deliver.ts (chunkHtml — RENDER-03 переиспользуется, не трогаем)
  </read_first>
  <action>
    Полностью переписать функцию `renderHtml(digest: DigestJson, posts: Post[]): string` в `src/summarize.ts`. Заменяет старую реализацию, основанную на `digest.sections`.

    Конкретное содержимое:
    ```typescript
    // RENDER-01..02: 5 фиксированных секций emoji+<b> + блок «Упоминания компаний» (orphans).
    // Порядок секций — D-03 (фиксированный, не сортируем по count).
    // Пустая секция: <i>— нет упоминаний за сутки</i> (D-02).
    // Каждый item содержит deep-link <a href="https://t.me/<channel>/<msgId>">@channel</a> (RENDER-02).
    // Inline-маркер [РОСНЕФТЬ] / [ЛУКОЙЛ] / [ГАЗПРОМ] перед summary, если у item непустой mentions[]
    // (D-04 + specifics). Пробел между маркерами и summary, без точек.

    const SECTION_HEADERS: Array<{ key: keyof Pick<DigestJson, "bunker"|"oil"|"kerosene"|"petrochem"|"bitumen"|"mentions">; header: string }> = [
      { key: "bunker",    header: "🚢 Бункер" },
      { key: "oil",       header: "🛢 Масла" },
      { key: "kerosene",  header: "✈️ Керосин" },
      { key: "petrochem", header: "⚗️ Нефтехимия" },
      { key: "bitumen",   header: "🛣 Битум" },
      { key: "mentions",  header: "🏢 Упоминания компаний" },
    ];

    const MENTION_LABEL: Record<Mention, string> = {
      rosneft: "РОСНЕФТЬ",
      lukoil: "ЛУКОЙЛ",
      gazprom: "ГАЗПРОМ",
    };

    function renderItem(item: DigestItem): string | null {
      // RENDER-02: валидация url через new URL(), невалидный — пропускаем
      let safeUrl: string;
      try {
        safeUrl = new URL(item.url).toString();
      } catch {
        console.warn(`[summarize] skip (bad url): ${item.url}`);
        return null;
      }

      // D-04 + specifics: inline-маркеры для непустого mentions, в фиксированном порядке.
      // Несколько компаний → подряд через пробел: <b>[РОСНЕФТЬ] [ЛУКОЙЛ]</b>.
      let prefix = "";
      if (item.mentions.length > 0) {
        const labels = item.mentions.map((m) => `[${MENTION_LABEL[m]}]`).join(" ");
        prefix = `<b>${labels}</b> `;
      }

      // D-05: формат буллета.
      return `• ${prefix}${escapeHtml(item.summary)} — <i>«${escapeHtml(item.keyQuote)}»</i> — <a href="${safeUrl}">@${escapeHtml(item.channel)}</a>`;
    }

    export function renderHtml(digest: DigestJson, posts: Post[]): string {
      const date = formatDateRu(digest.generatedAt);
      const n = posts.length;
      const k = new Set(posts.map((p) => p.channelUsername)).size;

      const header =
        `<b>Нефтегаз — ${escapeHtml(date)}</b>\n` +
        `<i>${n} постов из ${k} каналов за 24ч</i>\n\n`;

      const sectionsHtml: string[] = [];
      for (const { key, header: sectionHeader } of SECTION_HEADERS) {
        const items = digest[key];
        const lines: string[] = [`<b>${sectionHeader}</b>`];
        if (items.length === 0) {
          // D-02: явная пометка пустой секции.
          lines.push(`<i>— нет упоминаний за сутки</i>`);
        } else {
          for (const item of items) {
            const rendered = renderItem(item);
            if (rendered !== null) lines.push(rendered);
          }
        }
        sectionsHtml.push(lines.join("\n"));
      }

      // Между секциями — \n\n (пустая строка, граница для chunkHtml).
      return header + sectionsHtml.join("\n\n");
    }
    ```

    Также добавить импорт `Mention` (если ещё не) в файл вместе с T4-импортом:
    `import type { Category, Mention } from "./types.js";`

    Старый `renderHtml`, основанный на `digest.sections`, удалить целиком.
  </action>
  <acceptance_criteria>
    - `grep -cE '🚢 Бункер|🛢 Масла|✈️ Керосин|⚗️ Нефтехимия|🛣 Битум|🏢 Упоминания компаний' src/summarize.ts` → ≥6 (все 6 заголовков присутствуют)
    - `grep -E '— нет упоминаний за сутки' src/summarize.ts` → 1 match
    - `grep -E 'https://t\.me/' src/summarize.ts` → 0 matches здесь (URL уже встроен в Post.url; код использует `item.url`, не строит из шаблона)
    - `grep -E 'new URL\(item\.url\)' src/summarize.ts` → 1 match (RENDER-02 deep-link через item.url)
    - `grep -cE 'RU\s*RU\s*RU|РОСНЕФТЬ|ЛУКОЙЛ|ГАЗПРОМ' src/summarize.ts` → ≥3 (literal labels в MENTION_LABEL)
    - `grep -E 'SECTION_HEADERS' src/summarize.ts` → ≥2 matches (declaration + iteration)
    - `grep -E 'export function renderHtml\(' src/summarize.ts` → 1 match
    - `grep -E 'digest\.sections' src/summarize.ts` → 0 matches (старая модель удалена)
    - `grep -E 'escapeHtml\(item\.summary\)' src/summarize.ts` → 1 match (HTML injection mitigation T-01-05)
    - `grep -E 'escapeHtml\(item\.keyQuote\)' src/summarize.ts` → 1 match
    - `grep -E 'escapeHtml\(item\.channel\)' src/summarize.ts` → 1 match
  </acceptance_criteria>
  <verify>
    <automated>[ "$(grep -cE '🚢 Бункер|🛢 Масла|✈️ Керосин|⚗️ Нефтехимия|🛣 Битум|🏢 Упоминания компаний' src/summarize.ts)" -ge "6" ] && grep -E "— нет упоминаний за сутки" src/summarize.ts && grep -E "new URL\(item\.url\)" src/summarize.ts && [ "$(grep -cE 'digest\.sections' src/summarize.ts)" = "0" ] && grep -E "escapeHtml\(item\.summary\)" src/summarize.ts</automated>
  </verify>
  <done>
    renderHtml выдаёт 6 секций в фиксированном порядке D-03 с emoji-заголовками D-02, помечает пустые секции, добавляет deep-link через item.url, префиксует inline-маркеры для items с mentions, всё user-controlled пропущено через escapeHtml. Файл src/summarize.ts должен теперь компилиться — проверка в T16.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 7: Update logger.ts to print postsDropped in RunSummary block</name>
  <files>src/logger.ts</files>
  <read_first>
    - src/logger.ts (текущий logRunSummary)
    - src/types.ts (после T2 — RunSummary с postsDropped)
    - .planning/REQUIREMENTS.md §«STRUCT-03» (postsDropped учитывается в RunSummary)
  </read_first>
  <action>
    В `src/logger.ts` в функции `logRunSummary(s: RunSummary)` отредактировать строку с posts-метриками. Текущая:
    ```
    `  posts: collected=${s.postsCollected} deduped=${s.postsDeduped}`,
    ```
    Заменить на:
    ```
    `  posts: collected=${s.postsCollected} deduped=${s.postsDeduped} dropped=${s.postsDropped}`,
    ```
    Не трогать остальные строки lines[].
  </action>
  <acceptance_criteria>
    - `grep -E 'dropped=\$\{s\.postsDropped\}' src/logger.ts` → 1 match
    - `grep -E 'posts: collected=\$\{s\.postsCollected\} deduped=\$\{s\.postsDeduped\} dropped=' src/logger.ts` → 1 match
  </acceptance_criteria>
  <verify>
    <automated>grep -E 'dropped=\$\{s\.postsDropped\}' src/logger.ts</automated>
  </verify>
  <done>
    logRunSummary печатает postsDropped в той же posts-строке.
  </done>
</task>

<!-- =================================================================== -->
<!-- WAVE 2 — DEDUP-01..02 + ARCH-01..02 (T8-T11)                        -->
<!-- =================================================================== -->

<task type="auto" tdd="false">
  <name>Task 8: Create src/dedup.ts — normalize + sha256 + hash-cache load/save (DEDUP-01, DEDUP-02)</name>
  <files>src/dedup.ts</files>
  <read_first>
    - src/types.ts (после T2 — Post)
    - src/logger.ts (log.info/warn для логирования)
    - .planning/REQUIREMENTS.md §«DEDUP-01, DEDUP-02» (нормализация, SHA-256, rolling 14d, atomic write)
    - .planning/phases/01-code/01-CONTEXT.md §«D-17: dedup.ts инкапсулирует ФС-операции»
  </read_first>
  <action>
    Создать новый файл `src/dedup.ts` с полной реализацией DEDUP-01..02.

    Полное содержимое:
    ```typescript
    // src/dedup.ts — кросс-прогонная дедупа через SHA-256 hash-cache.
    // DEDUP-01: нормализация text → SHA-256 → ключ дедупа.
    // DEDUP-02: hash-cache.json rolling 14 дней, атомарная запись через .tmp + rename.

    import { createHash } from "node:crypto";
    import { readFileSync, writeFileSync, renameSync, existsSync, mkdirSync } from "node:fs";
    import { dirname } from "node:path";
    import type { Post } from "./types.js";
    import { log } from "./logger.js";

    const HASH_CACHE_PATH = "./data/hash-cache.json";
    const TTL_DAYS = Number(process.env.HASH_CACHE_TTL_DAYS ?? 14);
    const TTL_MS = TTL_DAYS * 24 * 60 * 60 * 1000;

    interface HashEntry {
      hash: string;
      ts: string; // ISO 8601 — момент попадания в cache
    }

    interface HashCacheFile {
      entries: HashEntry[];
    }

    /** DEDUP-01: lowercase, удаление эмодзи (Extended_Pictographic), удаление пунктуации, обрезка до 200 chars. */
    export function normalize(text: string): string {
      let n = text.toLowerCase();
      // Убираем эмодзи (Unicode property Extended_Pictographic, требует Node 20+).
      n = n.replace(/\p{Extended_Pictographic}/gu, "");
      // Убираем пунктуацию (Unicode property Punctuation).
      n = n.replace(/\p{P}/gu, "");
      // Сжимаем whitespace.
      n = n.replace(/\s+/g, " ").trim();
      // Первые 200 символов.
      return n.slice(0, 200);
    }

    /** DEDUP-01: SHA-256 hex от нормализованного текста. */
    export function hashText(text: string): string {
      return createHash("sha256").update(normalize(text), "utf8").digest("hex");
    }

    /** DEDUP-02: атомарная запись через .tmp + rename. */
    function atomicWriteJson(path: string, data: unknown): void {
      const dir = dirname(path);
      if (!existsSync(dir)) mkdirSync(dir, { recursive: true });
      const tmp = path + ".tmp";
      writeFileSync(tmp, JSON.stringify(data, null, 2), "utf8");
      renameSync(tmp, path);
    }

    /**
     * DEDUP-02: загружаем hash-cache.json, фильтруем записи старше TTL_DAYS.
     * Если файла нет или повреждён — возвращаем пустой Set + log.warn.
     */
    export function loadHashCache(): Set<string> {
      if (!existsSync(HASH_CACHE_PATH)) {
        return new Set();
      }
      try {
        const raw = readFileSync(HASH_CACHE_PATH, "utf8");
        const parsed = JSON.parse(raw) as HashCacheFile;
        if (!parsed || !Array.isArray(parsed.entries)) {
          log.warn(`[dedup] hash-cache: невалидная структура, начинаем заново`);
          return new Set();
        }
        const cutoff = Date.now() - TTL_MS;
        const fresh = parsed.entries.filter((e) => {
          const t = new Date(e.ts).getTime();
          return Number.isFinite(t) && t >= cutoff;
        });
        return new Set(fresh.map((e) => e.hash));
      } catch (err) {
        log.warn(`[dedup] hash-cache: parse error — ${(err as Error).message}, начинаем заново`);
        return new Set();
      }
    }

    /**
     * DEDUP-02: сохраняем hash-cache на диск; объединяем существующий fresh-set с новыми хешами.
     * Атомарная запись через .tmp + rename.
     */
    export function saveHashCache(existing: Set<string>, newHashes: string[]): void {
      const now = new Date().toISOString();
      // existing — уже отфильтрован на load, в нём только свежие за TTL_DAYS.
      // Им присваиваем "сейчас" не имеет смысла — нужен исходный ts.
      // Поэтому при save мы перезагружаем raw-файл, фильтруем как в load (по ts), и добавляем новые.
      let preserved: HashEntry[] = [];
      if (existsSync(HASH_CACHE_PATH)) {
        try {
          const raw = readFileSync(HASH_CACHE_PATH, "utf8");
          const parsed = JSON.parse(raw) as HashCacheFile;
          if (parsed && Array.isArray(parsed.entries)) {
            const cutoff = Date.now() - TTL_MS;
            preserved = parsed.entries.filter((e) => {
              const t = new Date(e.ts).getTime();
              return Number.isFinite(t) && t >= cutoff;
            });
          }
        } catch {
          // Игнорируем — preserved останется пустым.
        }
      }
      const additions: HashEntry[] = newHashes.map((h) => ({ hash: h, ts: now }));
      const merged: HashCacheFile = { entries: [...preserved, ...additions] };
      atomicWriteJson(HASH_CACHE_PATH, merged);
    }

    /**
     * DEDUP-01: фильтрует posts — оставляет те, чей hash отсутствует в cache.
     * Возвращает { fresh, hits } — fresh идут дальше в LLM, hits — счётчик дедупов.
     */
    export function dedupAgainstCache(
      posts: Post[],
      runId: string
    ): { fresh: Post[]; hits: number; freshHashes: string[] } {
      const cache = loadHashCache();
      const fresh: Post[] = [];
      const freshHashes: string[] = [];
      let hits = 0;
      for (const p of posts) {
        const h = hashText(p.text);
        if (cache.has(h)) {
          hits++;
          continue;
        }
        fresh.push(p);
        freshHashes.push(h);
      }
      log.info(`[dedup] runId=${runId} cache=${cache.size} fresh=${fresh.length} hits=${hits}`);
      return { fresh, hits, freshHashes };
    }

    /**
     * Вызывается ПОСЛЕ успешной доставки (D-09: hash-cache «съедает» только реально доставленные).
     */
    export function commitHashCache(freshHashes: string[], runId: string): void {
      const existing = loadHashCache();
      saveHashCache(existing, freshHashes);
      log.info(`[dedup] runId=${runId} commit hash-cache: +${freshHashes.length} entries`);
    }
    ```
  </action>
  <acceptance_criteria>
    - `test -f src/dedup.ts` exit 0
    - `grep -E "export function normalize\(text: string\): string" src/dedup.ts` → 1 match
    - `grep -E "export function hashText\(text: string\): string" src/dedup.ts` → 1 match
    - `grep -E "export function loadHashCache\(\): Set<string>" src/dedup.ts` → 1 match
    - `grep -E "export function saveHashCache" src/dedup.ts` → 1 match
    - `grep -E "export function dedupAgainstCache" src/dedup.ts` → 1 match
    - `grep -E "export function commitHashCache" src/dedup.ts` → 1 match
    - `grep -E "createHash\(.sha256.\)" src/dedup.ts` → 1 match
    - `grep -E "TTL_DAYS.*14" src/dedup.ts` → 1 match (default 14 days, env override)
    - `grep -E "renameSync\(tmp, path\)" src/dedup.ts` → 1 match (атомарная запись)
    - `grep -E "\\\\p\{Extended_Pictographic\}" src/dedup.ts` → 1 match (эмодзи regex)
    - `grep -E "\\\\p\{P\}" src/dedup.ts` → 1 match (пунктуация regex)
    - `grep -E "\.slice\(0, 200\)" src/dedup.ts` → 1 match (обрезка до 200)
    - Smoke check: `node --import tsx -e "import('./src/dedup.js').then(m => { console.log(m.normalize('Привет, мир! 🚢 OIL@1234')); console.log(m.hashText('test')); })"` → выводит нормализованную строку без эмодзи/пунктуации (например `привет мир oil1234`) + 64-char hex hash
  </acceptance_criteria>
  <verify>
    <automated>test -f src/dedup.ts && grep -E "export function normalize" src/dedup.ts && grep -E "export function hashText" src/dedup.ts && grep -E "export function dedupAgainstCache" src/dedup.ts && grep -E "export function commitHashCache" src/dedup.ts && grep -E "createHash\(.sha256.\)" src/dedup.ts && grep -E "renameSync\(tmp, path\)" src/dedup.ts && grep -E "TTL_DAYS.*14" src/dedup.ts</automated>
  </verify>
  <done>
    src/dedup.ts экспортирует normalize, hashText, loadHashCache, saveHashCache, dedupAgainstCache, commitHashCache. Атомарная запись через .tmp + rename. TTL 14 дней, env override. Эмодзи и пунктуация удаляются Unicode property regex'ами.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 9: Create src/archive.ts — writeRaw + writeOutput (ARCH-01, ARCH-02)</name>
  <files>src/archive.ts</files>
  <read_first>
    - src/types.ts (после T2 — Post)
    - src/dedup.ts (после T8 — паттерн atomicWriteJson)
    - .planning/REQUIREMENTS.md §«ARCH-01, ARCH-02» (data/raw/*.json до dedup, data/output/*.md после доставки, атомарная запись)
    - .planning/phases/01-code/01-CONTEXT.md §«D-09 порядок записи», §«D-10 MSK не UTC», §«D-11 re-run перезаписывает», §«D-17 archive.ts инкапсулирует»
  </read_first>
  <action>
    Создать новый файл `src/archive.ts` с реализацией ARCH-01..02. MSK-дата — без `dayjs` (не зависимость), используем `Intl.DateTimeFormat` с timeZone `Europe/Moscow` для формирования YYYY-MM-DD.

    Полное содержимое:
    ```typescript
    // src/archive.ts — ФС-архивы прогонов: data/raw/*.json + data/output/*.md.
    // ARCH-01: data/raw сохраняется ДО dedup и LLM (D-09 step 2).
    // ARCH-02: data/output сохраняется ПОСЛЕ успешной доставки в Telegram (D-09 step 8),
    //          байт-в-байт идентично отправленному HTML.
    // D-10: дата MSK (Europe/Moscow), не UTC.
    // D-11: re-run за тот же день перезаписывает файл.
    // Атомарная запись через .tmp + rename (D-17).

    import { writeFileSync, renameSync, existsSync, mkdirSync } from "node:fs";
    import { dirname } from "node:path";
    import type { Post } from "./types.js";
    import { log } from "./logger.js";

    const RAW_DIR = "./data/raw";
    const OUTPUT_DIR = "./data/output";

    /** D-10: YYYY-MM-DD в Europe/Moscow. Используем Intl.DateTimeFormat без зависимостей. */
    function todayMsk(): string {
      const fmt = new Intl.DateTimeFormat("en-CA", {
        timeZone: "Europe/Moscow",
        year: "numeric",
        month: "2-digit",
        day: "2-digit",
      });
      // en-CA даёт YYYY-MM-DD напрямую.
      return fmt.format(new Date());
    }

    function ensureDir(dir: string): void {
      if (!existsSync(dir)) mkdirSync(dir, { recursive: true });
    }

    /** Атомарная запись текстового файла через .tmp + rename. */
    function atomicWriteText(path: string, content: string): void {
      ensureDir(dirname(path));
      const tmp = path + ".tmp";
      writeFileSync(tmp, content, "utf8");
      renameSync(tmp, path);
    }

    /**
     * ARCH-01: записать все собранные сообщения за день в data/raw/YYYY-MM-DD.json.
     * Вызывается ПЕРЕД dedup и LLM. D-11: перезаписывает существующий файл за тот же день.
     */
    export function writeRaw(posts: Post[], runId: string): void {
      const path = `${RAW_DIR}/${todayMsk()}.json`;
      // Каждый post сериализуется как { username, messageId, text, date, url } (по REQUIREMENTS).
      const payload = posts.map((p) => ({
        username: p.channelUsername,
        messageId: p.messageId,
        text: p.text,
        date: p.postedAt,
        url: p.url,
      }));
      atomicWriteText(path, JSON.stringify(payload, null, 2));
      log.info(`[archive] runId=${runId} wrote raw: ${path} (${posts.length} posts)`);
    }

    /**
     * ARCH-02: записать финальный HTML-дайджест в data/output/YYYY-MM-DD.md.
     * Вызывается ПОСЛЕ sendToChannel. Содержание байт-в-байт идентично отправленному.
     * D-11: перезаписывает существующий файл за тот же день.
     */
    export function writeOutput(html: string, runId: string): void {
      const path = `${OUTPUT_DIR}/${todayMsk()}.md`;
      atomicWriteText(path, html);
      log.info(`[archive] runId=${runId} wrote output: ${path} (${html.length} chars)`);
    }
    ```
  </action>
  <acceptance_criteria>
    - `test -f src/archive.ts` exit 0
    - `grep -E "export function writeRaw\(posts: Post\[\], runId: string\): void" src/archive.ts` → 1 match
    - `grep -E "export function writeOutput\(html: string, runId: string\): void" src/archive.ts` → 1 match
    - `grep -E "Europe/Moscow" src/archive.ts` → ≥1 match (D-10)
    - `grep -E "renameSync\(tmp, path\)" src/archive.ts` → 1 match (atomic write)
    - `grep -E "data/raw" src/archive.ts` → ≥1 match
    - `grep -E "data/output" src/archive.ts` → ≥1 match
    - `grep -E "\.json\.tmp|\.md\.tmp" src/archive.ts` → 0 matches (литералов .tmp на путях нет — формируется конкатенацией `path + ".tmp"`)
    - `grep -E 'path \+ "\.tmp"' src/archive.ts` → 1 match
    - Smoke check: `node --import tsx -e "import('./src/archive.js').then(m => m.writeRaw([], 'test1234'))"` → создаёт `./data/raw/YYYY-MM-DD.json` (где YYYY-MM-DD — сегодняшняя MSK-дата) с содержимым `[]`
  </acceptance_criteria>
  <verify>
    <automated>test -f src/archive.ts && grep -E "export function writeRaw" src/archive.ts && grep -E "export function writeOutput" src/archive.ts && grep -E "Europe/Moscow" src/archive.ts && grep -E "renameSync\(tmp, path\)" src/archive.ts && grep -E 'path \+ "\.tmp"' src/archive.ts</automated>
  </verify>
  <done>
    src/archive.ts экспортирует writeRaw и writeOutput, оба используют атомарную запись и MSK-дату Europe/Moscow. Директории создаются автоматически.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 10: Wire writeRaw + dedupAgainstCache + writeOutput + commitHashCache into src/pipeline.ts</name>
  <files>src/pipeline.ts</files>
  <read_first>
    - src/pipeline.ts (текущая версия — runPipeline с in-memory dedup)
    - src/dedup.ts (после T8)
    - src/archive.ts (после T9)
    - src/summarize.ts (после T5 — summarize теперь возвращает {html, postsDropped})
    - src/types.ts (после T2 — RunSummary с postsDropped)
    - .planning/phases/01-code/01-CONTEXT.md §«D-09 порядок записи (8 шагов)» + §«code_context: integration points 83/103/106»
  </read_first>
  <action>
    Модифицировать `src/pipeline.ts`. Сохранить весь текущий fetch-loop без изменений (in-memory dedup по `${username}:${messageId}` остаётся как первая ступень). Добавить:

    1. Импорты в начало файла (после существующих `./logger.js` импорта):
       ```typescript
       import { writeRaw, writeOutput } from "./archive.js";
       import { dedupAgainstCache, commitHashCache } from "./dedup.js";
       ```

    2. После закрытия `try/finally` блока fetch-loop (после строки `await client.disconnect();`, после `log.info(`[pipeline] собрано уникальных...`)`), но ПЕРЕД блоком `if (allPosts.length > 0)` — вставить:
       ```typescript
       // ARCH-01 (D-09 step 2): пишем raw СРАЗУ после fetch, до dedup и LLM.
       // Инвариант: «сырые данные за день сохранены, даже если остаток pipeline упал».
       writeRaw(allPosts, runId);
       ```

    3. Внутри блока `if (allPosts.length > 0) { ... }` заменить три строки:
       ```typescript
       const html = await summarize(allPosts);
       await sendToChannel(html);
       digestDelivered = true;
       ```
       на:
       ```typescript
       // DEDUP-01..02 (D-09 step 3-4): фильтруем allPosts через rolling hash-cache.
       const { fresh: freshPosts, hits: hashHits, freshHashes } = dedupAgainstCache(allPosts, runId);
       postsDeduped += hashHits;

       if (freshPosts.length === 0) {
         log.info("[pipeline] all posts dedup'ed by hash-cache — skipping digest");
       } else {
         // STRUCT-01..03 (D-09 step 5): summarize возвращает {html, postsDropped}.
         const { html, postsDropped: dropped } = await summarize(freshPosts);
         postsDropped = dropped;

         // RENDER-01..03 (D-09 step 7): доставка в Telegram.
         await sendToChannel(html);
         digestDelivered = true;
         log.info(`[pipeline] дайджест отправлен.`);

         // ARCH-02 (D-09 step 8): сохраняем output ТОЛЬКО после успешной доставки,
         //   байт-в-байт identical с отправленным.
         writeOutput(html, runId);

         // DEDUP-02 (D-09 step 8): hash-cache «съедает» только реально доставленные.
         commitHashCache(freshHashes, runId);
       }
       ```

    4. Объявить `postsDropped` в локальных переменных рядом с `postsDeduped`:
       ```typescript
       let postsDeduped = 0;
       let postsDropped = 0;
       ```

    5. В возвращаемом объекте `RunSummary` добавить поле `postsDropped`:
       ```typescript
       return {
         runId,
         startedAt,
         finishedAt,
         durationMs: Date.now() - startMs,
         channelsTotal: channels.length,
         channelsSucceeded,
         channelsSkipped,
         postsCollected: allPosts.length,
         postsDeduped,
         postsDropped,
         digestDelivered,
         errors,
       };
       ```

    6. УДАЛИТЬ строку `log.info(`[pipeline] дайджест отправлен.`);` если она дублируется ВНЕ нового блока (она теперь живёт внутри else-ветки нового блока).

    Не трогать функцию `loadChannelsYaml`, не трогать fetch-loop, не трогать try/finally вокруг `client.disconnect()`.
  </action>
  <acceptance_criteria>
    - `grep -E 'import \{ writeRaw, writeOutput \} from "\./archive\.js"' src/pipeline.ts` → 1 match
    - `grep -E 'import \{ dedupAgainstCache, commitHashCache \} from "\./dedup\.js"' src/pipeline.ts` → 1 match
    - `grep -E "writeRaw\(allPosts, runId\)" src/pipeline.ts` → 1 match
    - `grep -E "dedupAgainstCache\(allPosts, runId\)" src/pipeline.ts` → 1 match
    - `grep -E "commitHashCache\(freshHashes, runId\)" src/pipeline.ts` → 1 match
    - `grep -E "writeOutput\(html, runId\)" src/pipeline.ts` → 1 match
    - `grep -E "postsDropped" src/pipeline.ts` → ≥3 matches (declaration + assignment + return field)
    - `grep -E "let postsDropped = 0;" src/pipeline.ts` → 1 match
    - Порядок вызовов в файле: writeRaw < dedupAgainstCache < summarize < sendToChannel < writeOutput < commitHashCache (проверить через `grep -nE "writeRaw|dedupAgainstCache|summarize|sendToChannel|writeOutput|commitHashCache" src/pipeline.ts | sort -t: -k2 -n` — line numbers возрастают именно в этом порядке)
  </acceptance_criteria>
  <verify>
    <automated>grep -E 'import \{ writeRaw, writeOutput \} from "\./archive\.js"' src/pipeline.ts && grep -E 'import \{ dedupAgainstCache, commitHashCache \} from "\./dedup\.js"' src/pipeline.ts && grep -E "writeRaw\(allPosts, runId\)" src/pipeline.ts && grep -E "dedupAgainstCache\(allPosts, runId\)" src/pipeline.ts && grep -E "commitHashCache\(freshHashes, runId\)" src/pipeline.ts && grep -E "writeOutput\(html, runId\)" src/pipeline.ts && grep -E "let postsDropped = 0;" src/pipeline.ts</automated>
  </verify>
  <done>
    pipeline.ts выполняет 8 шагов D-09 в правильном порядке. RunSummary содержит postsDropped. hash-cache коммитится ТОЛЬКО после успешной sendToChannel.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 11: Update .gitignore — exclude data/ except .gitkeep</name>
  <files>.gitignore</files>
  <read_first>
    - .gitignore (текущая версия)
    - .planning/phases/01-code/01-CONTEXT.md §«code_context: .gitignore — добавить data/»
  </read_first>
  <action>
    В `.gitignore` после блока `# Logs\n*.log` добавить новую секцию:
    ```
    # Runtime data archives (raw + output + hash-cache)
    data/*
    !data/.gitkeep
    ```

    Затем создать пустой файл `data/.gitkeep`:
    - Если директории `data/` нет — создать `data/.gitkeep` (touch). mkdir родительской директории — автоматически.

    Это даст: data/ директория сохраняется в git, но всё содержимое (data/raw/*, data/output/*, data/hash-cache.json) игнорируется.
  </action>
  <acceptance_criteria>
    - `grep -E "^data/\*$" .gitignore` → 1 match
    - `grep -E "^!data/\.gitkeep$" .gitignore` → 1 match
    - `test -f data/.gitkeep` → exit 0
    - `git check-ignore data/raw/test.json` → exit 0 (значит игнорируется)
    - `git check-ignore data/.gitkeep` → exit 1 (НЕ игнорируется — exception работает)
  </acceptance_criteria>
  <verify>
    <automated>grep -E "^data/\*$" .gitignore && grep -E "^!data/\.gitkeep$" .gitignore && test -f data/.gitkeep</automated>
  </verify>
  <done>
    data/ исключена из git кроме .gitkeep. Архивы прогонов не попадают в commit.
  </done>
</task>

<!-- =================================================================== -->
<!-- WAVE 3 — ALERT-01..02 + DOC-04..05 (T12-T15)                        -->
<!-- =================================================================== -->

<task type="auto" tdd="false">
  <name>Task 12: Create src/alert.ts — sendAlert via separate BOT_TOKEN_ALERTS (ALERT-01)</name>
  <files>src/alert.ts, .env.example</files>
  <read_first>
    - src/deliver.ts (паттерн fetch к Bot API sendMessage)
    - src/logger.ts (log.error на alert-fail)
    - .env.example (текущий — добавим BOT_TOKEN_ALERTS, ALERTS_CHAT_ID)
    - .planning/REQUIREMENTS.md §«ALERT-01» (отдельный bot, sendMessage в личку)
    - .planning/phases/01-code/01-CONTEXT.md §«D-12..D-15» (await, без throttle, alert-on-alert-fail = console.error)
    - threat_model T-01-02 (alert-payload не сериализует process.env)
  </read_first>
  <action>
    Создать новый файл `src/alert.ts`:
    ```typescript
    // src/alert.ts — alert-bot для технических ошибок pipeline (ALERT-01).
    // Отдельный Bot Token (BOT_TOKEN_ALERTS) — НЕ загрязняем канал Заказчика.
    // Шлёт в личку владельца (ALERTS_CHAT_ID).
    // Threat T-01-02: payload не сериализует process.env, только переданные поля.

    import { log } from "./logger.js";

    export interface AlertPayload {
      stage: string;          // "pipeline" / "tick" / "summarize" — где упало
      message: string;        // err.message
      runId: string;          // crypto.randomUUID().slice(0,8)
      stack?: string;         // err.stack (опционально, обрезается до 1500 chars)
    }

    /**
     * ALERT-01..02: шлёт alert в личку владельца.
     * D-13: await (не fire-and-forget) — чтобы 60-секундное окно гарантировалось.
     * D-15: на alert-fail → console.error без retry; не throw наверх (иначе пропадёт оригинальная ошибка).
     */
    export async function sendAlert(payload: AlertPayload): Promise<void> {
      const token = process.env.BOT_TOKEN_ALERTS;
      const chatId = process.env.ALERTS_CHAT_ID;
      if (!token || !chatId) {
        // Не считаем критичной ошибкой — оператор мог ещё не настроить.
        log.error(
          `[alert] BOT_TOKEN_ALERTS или ALERTS_CHAT_ID не задан — alert не отправлен. payload=${JSON.stringify({
            stage: payload.stage,
            message: payload.message,
            runId: payload.runId,
          })}`
        );
        return;
      }

      // Plain-text формат: безопаснее HTML (никакой инъекции из stack/message).
      const stackTail = payload.stack ? payload.stack.slice(0, 1500) : "(no stack)";
      const text = [
        `🚨 [tg-parser-demo] pipeline failure`,
        `stage: ${payload.stage}`,
        `runId: ${payload.runId}`,
        `error: ${payload.message}`,
        ``,
        `stack:`,
        stackTail,
      ].join("\n");

      // Telegram message limit 4096 chars — режем с запасом.
      const safeText = text.slice(0, 4000);

      try {
        const res = await fetch(`https://api.telegram.org/bot${token}/sendMessage`, {
          method: "POST",
          headers: { "content-type": "application/json" },
          body: JSON.stringify({
            chat_id: chatId,
            text: safeText,
            disable_web_page_preview: true,
          }),
        });
        if (!res.ok) {
          const body = await res.text();
          log.error(`[alert] Telegram sendMessage failed: ${res.status} ${body.slice(0, 300)}`);
          return;
        }
        log.info(`[alert] sent (stage=${payload.stage} runId=${payload.runId})`);
      } catch (err) {
        // D-15: не throw, только console.error — оператор увидит в pm2 logs --err.
        log.error(`[alert] network error: ${(err as Error).message}`);
      }
    }
    ```

    Затем обновить `.env.example`. После последней строки `LOG_LEVEL=info` добавить новую секцию:
    ```
    
    # -----------------------------------------------------------------------------
    # Alert-bot (ALERT-01): отдельный Bot API для технических ошибок pipeline.
    # Шлёт в личку владельца, НЕ загрязняет канал Заказчика.
    # -----------------------------------------------------------------------------
    # @BotFather → /newbot → Bot Token (отдельный от TG_BOT_TOKEN)
    BOT_TOKEN_ALERTS=
    # Numeric chat_id личного чата владельца с alert-ботом.
    # Узнать: начать диалог с alert-ботом (/start), переслать сообщение в @userinfobot.
    ALERTS_CHAT_ID=
    ```
  </action>
  <acceptance_criteria>
    - `test -f src/alert.ts` exit 0
    - `grep -E "export async function sendAlert\(payload: AlertPayload\): Promise<void>" src/alert.ts` → 1 match
    - `grep -E "BOT_TOKEN_ALERTS" src/alert.ts` → ≥1 match
    - `grep -E "ALERTS_CHAT_ID" src/alert.ts` → ≥1 match
    - `grep -cE "process\.env\." src/alert.ts` → 2 (только чтения BOT_TOKEN_ALERTS и ALERTS_CHAT_ID, никакой массовой сериализации) — проверка mitigation T-01-02
    - `grep -E "JSON\.stringify\(process\.env\)" src/alert.ts` → 0 matches (не сериализуем env целиком)
    - `grep -E "BOT_TOKEN_ALERTS=" .env.example` → 1 match
    - `grep -E "ALERTS_CHAT_ID=" .env.example` → 1 match
    - `grep -E "Alert-bot.*ALERT-01" .env.example` → 1 match (поясняющий комментарий)
    - `grep -E "stack: payload\.stack \?" src/alert.ts` → 1 match (есть обрезка stack)
  </acceptance_criteria>
  <verify>
    <automated>test -f src/alert.ts && grep -E "export async function sendAlert" src/alert.ts && grep -E "BOT_TOKEN_ALERTS" src/alert.ts && grep -E "ALERTS_CHAT_ID" src/alert.ts && [ "$(grep -cE 'JSON\.stringify\(process\.env\)' src/alert.ts)" = "0" ] && grep -E "BOT_TOKEN_ALERTS=" .env.example && grep -E "ALERTS_CHAT_ID=" .env.example</automated>
  </verify>
  <done>
    src/alert.ts экспортирует sendAlert; .env.example документирует новые переменные. Payload не сериализует process.env. Alert-on-alert-fail → console.error без throw.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 13: Wire await sendAlert into src/run.ts catch (ALERT-02)</name>
  <files>src/run.ts</files>
  <read_first>
    - src/run.ts (текущий tick() с try/catch)
    - src/alert.ts (после T12)
    - .planning/REQUIREMENTS.md §«ALERT-02» (try/catch на pipeline, alert ≤60s)
    - .planning/phases/01-code/01-CONTEXT.md §«D-12 wrapper в run.ts», §«D-13 await не fire-and-forget»
  </read_first>
  <action>
    В `src/run.ts`:

    1. Добавить импорт после `import { log, logRunSummary } from "./logger.js";`:
       ```typescript
       import { sendAlert } from "./alert.js";
       ```

    2. В функции `tick()` заменить блок:
       ```typescript
       } catch (err) {
         log.error("pipeline failed", err);
       } finally {
       ```
       на:
       ```typescript
       } catch (err) {
         const e = err as Error;
         log.error("pipeline failed", e);
         // ALERT-02 (D-12, D-13): await — не fire-and-forget, чтобы 60s-окно гарантировалось.
         // runId внутри pipeline нам недоступен (упало, не дошло до return);
         // используем отдельный alertId для трассировки.
         const alertId = crypto.randomUUID().slice(0, 8);
         try {
           await sendAlert({
             stage: "tick",
             message: e?.message ?? String(err),
             runId: alertId,
             stack: e?.stack,
           });
         } catch (alertErr) {
           // D-15: alert-on-alert-fail → log.error, не падаем дальше.
           log.error("alert send failed", alertErr);
         }
       } finally {
       ```
  </action>
  <acceptance_criteria>
    - `grep -E 'import \{ sendAlert \} from "\./alert\.js"' src/run.ts` → 1 match
    - `grep -E "await sendAlert\(\{" src/run.ts` → 1 match
    - `grep -E 'stage: "tick"' src/run.ts` → 1 match
    - `grep -E "log\.error..pipeline failed" src/run.ts` → 1 match (старая строка сохранена)
    - `grep -E "alertId" src/run.ts` → ≥2 matches (declaration + use in payload)
  </acceptance_criteria>
  <verify>
    <automated>grep -E 'import \{ sendAlert \} from "\./alert\.js"' src/run.ts && grep -E "await sendAlert\(\{" src/run.ts && grep -E 'stage: "tick"' src/run.ts</automated>
  </verify>
  <done>
    tick() в run.ts на любую ошибку pipeline шлёт await sendAlert; в канал Заказчика на ошибке не уходит ничего (sendToChannel не достигнут).
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 14: Create docs/RUNBOOK.md — 5 failure scenarios (DOC-04)</name>
  <files>docs/RUNBOOK.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md §«DOC-04» (5 сценариев: DeepSeek 5xx, TG FloodWait, parser не видит канал, диск переполнен, network down)
    - .planning/phases/01-code/01-CONTEXT.md §«D-18: структура каждого сценария — Симптом → Диагностика → Действие → Восстановление»
    - docs/intent-v3.0.md §«DOC-04» (формулировка)
    - src/alert.ts (после T12 — alert-payload поля для диагностики)
  </read_first>
  <action>
    Создать новый markdown-файл `docs/RUNBOOK.md` с 5 сценариями сбоя. Структура каждого сценария — D-18: **Симптом → Диагностика → Действие → Восстановление**. Файл — на русском, для оператора (владельца), не для Заказчика.

    Конкретное содержимое (минимум 80 строк):

    ```markdown
    # RUNBOOK — tg-parser-demo

    Оперативная инструкция оператора (владельца) при сбоях daemon на VPS под PM2.
    Все 5 сценариев соответствуют DOC-04 milestone v3.0.

    Документ читается сверху вниз: каждый сценарий — **Симптом → Диагностика → Действие → Восстановление**.

    ---

    ## 1. DeepSeek возвращает 5xx (или timeout)

    **Симптом:**
    - Алерт в личке: `🚨 [tg-parser-demo] pipeline failure ... stage: tick ... error: 500 Internal Server Error` (или `ETIMEDOUT`, `ECONNRESET`).
    - В канале Заказчика тишина (sendToChannel не достигнут).
    - `data/raw/YYYY-MM-DD.json` за сегодня СУЩЕСТВУЕТ (raw-архив пишется до LLM, ARCH-01).
    - `data/output/YYYY-MM-DD.md` за сегодня ОТСУТСТВУЕТ.

    **Диагностика:**
    - `pm2 logs tg-parser --lines 200` — смотрим stack trace целиком.
    - `curl -I https://api.deepseek.com` — доступен ли endpoint вообще?
    - Проверить статус DeepSeek: https://status.deepseek.com (если есть).

    **Действие:**
    - Если DeepSeek упал на их стороне (5xx по всем endpoint'ам) — ждём.
    - Cron сработает следующим тиком в 20:00 MSK завтра — daemon сам прогонит ещё раз.
    - Hash-cache `data/hash-cache.json` НЕ был обновлён (commitHashCache вызывается только после успешной sendToChannel) → завтра те же посты пройдут заново.
    - Ручной retry СЕГОДНЯ: `pm2 restart tg-parser` НЕ запускает прогон (cron 20:00 — единственный триггер). Если очень нужно — временно изменить cron в `src/run.ts` на ближайшую минуту, перезапустить, потом откатить.

    **Восстановление:**
    - Следующая успешная сводка появится в канале на следующем cron-тике.
    - Никаких ручных action'ов на ФС не нужно — invariant ARCH-02 + DEDUP-02 сам восстановит состояние.

    ---

    ## 2. Telegram FloodWait / rate-limit

    **Симптом:**
    - Алерт: `error: A wait of N seconds is required (FloodWaitError)`.
    - Один из каналов помечен skipped в RunSummary; остальные — обработаны.
    - В канале Заказчика — частичная сводка (если хотя бы 1 канал успел). ИЛИ полная тишина (если floodwait во время sendToChannel).

    **Диагностика:**
    - `pm2 logs tg-parser --lines 500 | grep -i flood` — смотрим, какой именно канал флудит.
    - Если флудит fetchLast24h — проблема с user-сессией (TG_SESSION).
    - Если флудит sendToChannel — проблема с Bot API (TG_BOT_TOKEN, лимит ~30 msg/sec).

    **Действие:**
    - Один FloodWait на user-сессии → существующий retry x1 в `src/telegram.ts` уже обработал. Daemon продолжает.
    - Повторный FloodWait подряд → канал помечен skipped, прогон продолжается без него.
    - Если FloodWait на КАЖДЫЙ канал — увеличить `CHANNEL_DELAY_MS` в `.env` (текущий 1750мс, поднять до 3000).
    - `pm2 restart tg-parser` для подхвата нового значения env.

    **Восстановление:**
    - Подождать минимум 1 час, лучше 24, прежде чем поднимать частоту тиков.
    - Telegram банит при повторных нарушениях — лучше пожертвовать 1 каналом, чем сессией.

    ---

    ## 3. Парсер не видит канал (ChannelPrivate / UsernameInvalid / UsernameNotOccupied)

    **Симптом:**
    - В RunSummary `channelsSkipped > 0`.
    - В `errors[]`: `<username>: ChannelPrivateError` / `UsernameNotOccupiedError` / `UsernameInvalidError`.
    - Пакетного алерта НЕ приходит (это per-channel ошибка, локализована).

    **Диагностика:**
    - Какой именно канал? `pm2 logs tg-parser --lines 200 | grep -i "channel skipped"`.
    - Открыть в Telegram-клиенте `https://t.me/<username>` руками. Видим ли мы канал из user-аккаунта (того, чей TG_SESSION в .env)?

    **Действие:**
    - **Канал стал приватным** → удалить из `channels.yaml` ИЛИ перевести в карантин (см. `docs/CHANNELS.md`).
    - **Username не существует** (опечатка/удалили) → удалить запись из `channels.yaml`.
    - **User-аккаунт не подписан** → подписаться руками с того же аккаунта, чей TG_SESSION в .env.
    - `pm2 restart tg-parser` после правки channels.yaml.

    **Восстановление:**
    - Следующий cron-тик прогонит обновлённый список каналов.

    ---

    ## 4. Диск переполнен (data/ или PM2 logs)

    **Симптом:**
    - Алерт: `error: ENOSPC: no space left on device, write` (или `rename` на атомарной записи).
    - VPS-команды зависают, `df -h` показывает `100%`.

    **Диагностика:**
    - `df -h` — какой раздел заполнен.
    - `du -sh ~/.pm2/logs/*` — размер pm2-логов.
    - `du -sh ./data/*` — размер архивов raw/output.

    **Действие:**
    - **PM2 logs раздулись** (>500 МБ): `pm2 flush tg-parser` (очистить).
    - **data/raw/ переполнено** (>1 ГБ): удалить старые `data/raw/*.json` руками (старше 30 дней): `find data/raw -name '*.json' -mtime +30 -delete`.
    - **data/output/ — НЕ удалять**: эти файлы нужны для acceptance-пакета Phase 2.
    - **data/hash-cache.json раздулось** (>100 МБ — нереально, но): rolling 14d сам отфильтрует на следующем load. Ручная очистка: `rm data/hash-cache.json` (но это даст 1 день повторов в канале — нежелательно).

    **Восстановление:**
    - После очистки запустить `pm2 restart tg-parser`.
    - Долгосрочно — настроить logrotate на `~/.pm2/logs/*.log` и автоматическую ротацию `data/raw/`.

    ---

    ## 5. Network down на VPS

    **Симптом:**
    - Никаких алертов не приходит (сам alert-bot не может отправить — D-15 пишет в pm2-err.log).
    - Канал Заказчика молчит несколько суток.

    **Диагностика:**
    - SSH на VPS, проверить `ping 8.8.8.8`, `curl -I https://api.telegram.org`, `curl -I https://api.deepseek.com`.
    - `pm2 list` — daemon вообще запущен?
    - `pm2 logs tg-parser --lines 1000 --err` — что писалось последние сутки.

    **Действие:**
    - **Сеть down** → связаться с провайдером VPS. Daemon продолжит работать на тиках, но они будут падать.
    - **Daemon упал** (`pm2 list` не показывает) → `pm2 resurrect` или `pm2 start ecosystem.config.cjs`.
    - **Token инвалидирован** (`401 Unauthorized` в pm2-err.log) → перевыпустить через @BotFather, обновить `.env`, `pm2 restart tg-parser`.

    **Восстановление:**
    - После восстановления сети — следующий 20:00 MSK тик пройдёт штатно.
    - Если сеть была down >24ч, проверить, что hash-cache не «съел» лишнего (он rolling 14d — не должен; но визуально сравнить data/output/*.md за последние 3 дня).

    ---

    *RUNBOOK v3.0 — обновлено при milestone v3.0 (DOC-04). При появлении новых сценариев добавлять сюда same структуру: Симптом → Диагностика → Действие → Восстановление.*
    ```
  </action>
  <acceptance_criteria>
    - `test -f docs/RUNBOOK.md` exit 0
    - `wc -l docs/RUNBOOK.md` → ≥80
    - `grep -c '^## ' docs/RUNBOOK.md` → ≥5 (5 сценариев — каждый отдельный H2)
    - `grep -cE '\*\*Симптом|\*\*Диагностика|\*\*Действие|\*\*Восстановление' docs/RUNBOOK.md` → ≥20 (5 сценариев × 4 секции = 20)
    - `grep -E 'DeepSeek' docs/RUNBOOK.md` → ≥1 (сценарий 1)
    - `grep -E 'FloodWait' docs/RUNBOOK.md` → ≥1 (сценарий 2)
    - `grep -E 'ChannelPrivate|UsernameInvalid' docs/RUNBOOK.md` → ≥1 (сценарий 3)
    - `grep -E 'ENOSPC|диск' docs/RUNBOOK.md` → ≥1 (сценарий 4)
    - `grep -E 'network down|Network down' docs/RUNBOOK.md` → ≥1 (сценарий 5)
    - `grep -E 'pm2 logs|pm2 restart|pm2 list' docs/RUNBOOK.md` → ≥3 matches (конкретные операторские команды)
  </acceptance_criteria>
  <verify>
    <automated>test -f docs/RUNBOOK.md && [ "$(wc -l < docs/RUNBOOK.md)" -ge "80" ] && [ "$(grep -c '^## ' docs/RUNBOOK.md)" -ge "5" ] && grep -E "DeepSeek" docs/RUNBOOK.md && grep -E "FloodWait" docs/RUNBOOK.md && grep -E "ChannelPrivate|UsernameInvalid" docs/RUNBOOK.md && grep -E "ENOSPC|диск" docs/RUNBOOK.md && grep -iE "network down" docs/RUNBOOK.md</automated>
  </verify>
  <done>
    RUNBOOK.md содержит 5 сценариев в формате Симптом/Диагностика/Действие/Восстановление с конкретными командами оператора.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 15: Create docs/CHANNELS.md — channel lifecycle (DOC-05)</name>
  <files>docs/CHANNELS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md §«DOC-05» (lifecycle: добавление/проверка подписки/удаление/карантин/PLACEHOLDER)
    - .planning/phases/01-code/01-CONTEXT.md §«D-18: CHANNELS.md — checklist с командами»
    - .planning/STATE.md §«Operator Prerequisite» (38 PLACEHOLDER каналов в channels.yaml)
    - docs/intent-v3.0.md §«DOC-05»
  </read_first>
  <action>
    Создать новый файл `docs/CHANNELS.md` с lifecycle канала.

    Конкретное содержимое (минимум 60 строк):

    ```markdown
    # CHANNELS — lifecycle канала в tg-parser-demo

    Оперативная инструкция оператора по управлению `channels.yaml`.
    Соответствует DOC-05 milestone v3.0.

    Каналы — публичные Telegram-каналы по российскому нефтегазу/нефтехимии.
    User-аккаунт (тот, чей `TG_SESSION` в `.env`) обязан быть подписан на каждый канал.

    ---

    ## Структура channels.yaml

    ```yaml
    channels:
      - username: oil_news_ru        # без "@", обязательный
        priority: 1                  # опционально, не используется в v3.0 (зарезервировано)
      - username: bunker_market_ru
      - username: PLACEHOLDER_38     # оператор-задача: заменить на реальный канал
    ```

    Корневой ключ `channels` — массив объектов с полем `username` (обязательный) и опциональным `priority`.

    ---

    ## 1. Добавление нового канала

    1. Найти публичный канал по теме (нефтегаз / нефтехимия / бункер / битум / etc).
    2. **Проверка подписки** (КРИТИЧНО):
       - Открыть Telegram client под user-аккаунтом, чей `TG_SESSION` в `.env`.
       - Найти канал по `@username`.
       - Нажать **Subscribe** / **Подписаться**. Без подписки GramJS не увидит историю.
    3. Открыть `channels.yaml` на VPS:
       ```bash
       ssh user@vps
       cd ~/tg-parser-demo
       vim channels.yaml
       ```
    4. Добавить запись:
       ```yaml
         - username: new_channel_name
       ```
       (без `@`, ровно как в URL `https://t.me/new_channel_name`).
    5. Перезапустить daemon, чтобы подхватил новый channels.yaml на следующем тике:
       ```bash
       pm2 restart tg-parser
       ```
    6. Дождаться следующего cron-тика 20:00 MSK. Проверить `pm2 logs tg-parser --lines 200`:
       - `[pipeline] new_channel_name: N постов` — успех.
       - `[pipeline] channel skipped: new_channel_name — ChannelPrivateError` — забыли подписаться. См. RUNBOOK сценарий 3.

    ---

    ## 2. Проверка подписки user-аккаунта

    Шпаргалка: «у меня есть канал в `channels.yaml`, но парсер выдаёт `channelsSkipped`».

    1. Открыть Telegram client (НЕ приложение бота, а аккаунт владельца с `TG_SESSION`).
    2. Найти канал в списке чатов или через поиск.
    3. Если канала **нет в списке чатов** — подписаться (Subscribe).
    4. Если канал в списке, но в чатах — открыть и убедиться, что не silenced/архивирован (это не блокирует чтение, но удобно для оператора).
    5. После подписки → `pm2 restart tg-parser` → дождаться следующего тика.

    ---

    ## 3. Удаление канала

    Когда:
    - Канал стал приватным (после ребрендинга/закрытия).
    - Username сменился, и старый возвращает `UsernameNotOccupiedError`.
    - Канал перестал давать релевантный контент (всё рекламы, копипаст из других).

    Действия:
    1. `vim channels.yaml`.
    2. Удалить запись (всю строку `- username: ...` + опциональную `priority:`).
    3. `pm2 restart tg-parser`.
    4. Опционально: отписаться в user-аккаунте (не обязательно, но снижает rate-limit риск).

    ---

    ## 4. Карантин канала (временное отключение)

    Когда:
    - Канал ВРЕМЕННО даёт мусор (политика, новогодние поздравления, спам).
    - Канал ВРЕМЕННО недоступен (FloodWait приходит каждый тик).
    - Хочется протестировать, влияет ли канал на качество дайджеста.

    Способ карантина без потери записи:
    1. `vim channels.yaml`.
    2. Закомментировать запись YAML-комментарием:
       ```yaml
       # - username: noisy_channel_ru   # карантин с 2026-04-26 — много рекламы
       ```
    3. `pm2 restart tg-parser`.
    4. Через 7-14 дней раскомментировать обратно (если шум прошёл) либо удалить окончательно.

    ---

    ## 5. Замена PLACEHOLDER_NN из v2.0

    В `channels.yaml` после v2.0 содержится 38 записей `PLACEHOLDER_01..PLACEHOLDER_38` — их нужно заменить на реальные каналы перед 7-day smoke (Phase 2).

    Подход:
    1. Найти подходящий канал по теме (см. п.1 «Добавление»).
    2. Подписаться user-аккаунтом (см. п.2).
    3. `vim channels.yaml`, заменить `PLACEHOLDER_NN` на реальный username:
       ```yaml
       - username: PLACEHOLDER_05         # ДО
       - username: bunker_dispatch_ru     # ПОСЛЕ
       ```
    4. `pm2 restart tg-parser`.
    5. Проверить на следующем тике, что канал не в `channelsSkipped`.

    Заменять можно постепенно — daemon не падает на PLACEHOLDER (`UsernameNotOccupiedError` → channel skipped → прогон продолжается без него).

    ---

    *CHANNELS.md v3.0 — обновлено при milestone v3.0 (DOC-05). Полный жизненный цикл канала в одном месте.*
    ```
  </action>
  <acceptance_criteria>
    - `test -f docs/CHANNELS.md` exit 0
    - `wc -l docs/CHANNELS.md` → ≥60
    - `grep -c '^## ' docs/CHANNELS.md` → ≥5 (5 секций lifecycle: Структура / Добавление / Проверка / Удаление / Карантин / Замена PLACEHOLDER — итого 5-6 H2)
    - `grep -E 'channels\.yaml' docs/CHANNELS.md` → ≥3 matches
    - `grep -E 'PLACEHOLDER' docs/CHANNELS.md` → ≥2 matches (раздел замены v2.0 placeholder'ов)
    - `grep -E 'pm2 restart tg-parser' docs/CHANNELS.md` → ≥3 matches (конкретные команды оператора)
    - `grep -E 'TG_SESSION' docs/CHANNELS.md` → ≥1 match (упоминание подписки user-аккаунта)
    - `grep -E 'карантин|Карантин' docs/CHANNELS.md` → ≥1 match (lifecycle: карантин)
  </acceptance_criteria>
  <verify>
    <automated>test -f docs/CHANNELS.md && [ "$(wc -l < docs/CHANNELS.md)" -ge "60" ] && [ "$(grep -c '^## ' docs/CHANNELS.md)" -ge "5" ] && grep -E "PLACEHOLDER" docs/CHANNELS.md && [ "$(grep -cE 'pm2 restart tg-parser' docs/CHANNELS.md)" -ge "3" ] && grep -E "TG_SESSION" docs/CHANNELS.md && grep -iE "карантин" docs/CHANNELS.md</automated>
  </verify>
  <done>
    CHANNELS.md описывает full lifecycle канала с конкретными vim/pm2-командами; раздел про PLACEHOLDER явно покрыт.
  </done>
</task>

<!-- =================================================================== -->
<!-- WAVE 4 — FINAL VERIFY (T16)                                          -->
<!-- =================================================================== -->

<task type="auto" tdd="false">
  <name>Task 16: Final verify — npx tsc --noEmit + smoke npm start (D-08)</name>
  <files></files>
  <read_first>
    - все 15 предыдущих task'ов (вся работа должна быть закрыта)
    - .planning/phases/01-code/01-CONTEXT.md §«D-08: один финальный verify»
    - .planning/ROADMAP.md §«Phase 1 Success Criteria 1-6»
  </read_first>
  <action>
    Финальная верификация всего plan'а в одном task'е (D-08, locked: промежуточных smoke не делаем).

    Шаг 1 — TypeScript clean compile (Success Criteria #6):
    ```bash
    npx tsc --noEmit
    ```
    Ожидание: exit code 0, no output (или только tsx-shim warnings без errors).
    Если падает — диагностируем по сообщению, чиним конкретный файл, повторяем. НЕ обходим через `// @ts-ignore` или `any` — это запрещено CLAUDE.md (`strict: true`).

    Шаг 2 — Smoke `npm start` (один локальный прогон):
    ```bash
    # Опционально временно поменять cron в src/run.ts на ближайшую минуту,
    # чтобы не ждать 20:00 MSK. После теста — обязательно откатить на '0 20 * * *'.
    # ИЛИ запустить runPipeline() напрямую через временный entrypoint:
    node --env-file=.env --import tsx -e "import('./src/pipeline.js').then(m => m.runPipeline()).then(s => console.log('OK', s)).catch(e => { console.error('FAIL', e); process.exit(1); })"
    ```

    Принимаем второй способ — он чище, не трогает src/run.ts. Требования к smoke:
    - process exits с кодом 0 без unhandled rejection
    - в `data/raw/` создан файл `YYYY-MM-DD.json` (где YYYY-MM-DD — сегодняшняя MSK-дата)
    - В большинстве сценариев `data/output/YYYY-MM-DD.md` тоже создан (если каналы вернули посты И DeepSeek сработал И sendToChannel прошёл). На пустых каналах допустимо отсутствие output — это валидный сценарий.
    - В canale `TG_CHANNEL_ID` пришёл дайджест с 6 секциями (5 категорий + Упоминания), каждая с emoji-заголовком; каждый item содержит deep-link `t.me/<channel>/<msgId>` (Success Criteria #1, #2)
    - data/hash-cache.json появился, содержит `entries: [...]` с непустым массивом (Success Criteria #3)
    - `pm2 logs` или stdout содержат строку с `posts: collected=N deduped=M dropped=K` (Success Criteria #6 косвенно)

    Шаг 3 — финальная проверка ФС-инвариантов (Success Criteria #4):
    ```bash
    ls -la data/raw/ data/output/ 2>/dev/null
    test -f data/hash-cache.json && cat data/hash-cache.json | head -20
    ```

    Если smoke невозможен (нет реальных каналов в channels.yaml, нет TG_SESSION, нет DEEPSEEK_API_KEY на dev-машине) — фиксируем это явно в SUMMARY и отмечаем, что Phase 1 deploy на VPS перед Phase 2 7-day smoke = единственная возможность полного end-to-end smoke. tsc-чек обязателен в любом случае.

    На любых ошибках — фиксим конкретный файл и повторяем шаги 1-3 до зелёного.
  </action>
  <verification_criteria>
    Cumulative — все 6 Success Criteria из ROADMAP.md Phase 1:

    1. **5 секций + блок «Упоминания компаний»** (RENDER-01):
       - В сообщении в канале Заказчика после smoke прогона видны 6 H2-уровневых заголовков: 🚢 Бункер, 🛢 Масла, ✈️ Керосин, ⚗️ Нефтехимия, 🛣 Битум, 🏢 Упоминания компаний.
       - Пустые секции содержат `<i>— нет упоминаний за сутки</i>`.
       - Альтернативная code-проверка: `grep -cE '🚢 Бункер|🛢 Масла|✈️ Керосин|⚗️ Нефтехимия|🛣 Битум|🏢 Упоминания компаний' src/summarize.ts` → ≥6.

    2. **Deep-link на каждом item** (RENDER-02):
       - Каждый буллет содержит `<a href="https://t.me/<channel>/<msgId>">@channel</a>`.
       - Code-проверка: `grep -E 'new URL\(item\.url\)' src/summarize.ts` → 1 match.

    3. **Hash-cache переживает рестарт** (DEDUP-02):
       - `test -f data/hash-cache.json` exit 0 после первого smoke прогона.
       - Файл содержит `"entries":` с массивом записей `{hash: ..., ts: ISO}`.
       - На втором локальном прогоне (если повторим) — тот же набор постов даст `dedup hits` (можно через `grep` в логе).

    4. **data/raw/*.json и data/output/*.md появляются после прогона** (ARCH-01, ARCH-02):
       - `ls data/raw/*.json` → ≥1 файл с YYYY-MM-DD-датой.
       - `ls data/output/*.md` → ≥1 файл с YYYY-MM-DD-датой (если digest был доставлен).
       - Содержимое `data/output/<date>.md` идентично HTML, отправленному в канал (визуальная сверка). Code-проверка: pipeline.ts вызывает writeOutput с тем же `html`, что был передан в sendToChannel — `grep -B2 -A2 'sendToChannel(html)' src/pipeline.ts` показывает `writeOutput(html, runId)` сразу после.

    5. **Алерт в личку владельца ≤60s на ошибку pipeline** (ALERT-01, ALERT-02):
       - При temporary-injection ошибки (например, временно подменить DEEPSEEK_API_KEY на невалидный и запустить smoke) → в личке владельца появляется `🚨 [tg-parser-demo] pipeline failure ... stage: tick ...` через несколько секунд.
       - В канале Заказчика (TG_CHANNEL_ID) на этой же ошибке — тишина (sendToChannel не достигнут).
       - Code-проверка: `grep -E 'await sendAlert' src/run.ts` → 1 match; `grep -E 'BOT_TOKEN_ALERTS' src/alert.ts` → ≥1 match.

    6. **`npx tsc --noEmit` чистый** (Success Criteria #6):
       - Команда `npx tsc --noEmit` exits 0 без ошибок и warnings (типичный output: пустой stdout/stderr).

    Дополнительно — все 14 REQ закрыты:
    - STRUCT-01: `grep -cE 'bunker|oil|kerosene|petrochem|bitumen' src/schema.ts` ≥5; `grep -cE 'rosneft|lukoil|gazprom' src/schema.ts` ≥3.
    - STRUCT-02: `grep -E 'safeParse' src/summarize.ts` ≥2; retry message contains "schema mismatch after retry".
    - STRUCT-03: `grep -E 'postsDropped' src/types.ts src/pipeline.ts src/summarize.ts src/logger.ts | wc -l` ≥4.
    - RENDER-01: проверено в SC#1.
    - RENDER-02: проверено в SC#2.
    - RENDER-03: `grep -E 'chunkHtml' src/deliver.ts` ≥2 (без изменений — переиспользуется).
    - DEDUP-01: `grep -E 'createHash..sha256' src/dedup.ts` 1 match.
    - DEDUP-02: `grep -E 'TTL_DAYS.*14' src/dedup.ts` 1 match; `grep -E 'renameSync' src/dedup.ts` ≥1 match.
    - ARCH-01: `grep -E 'writeRaw' src/archive.ts src/pipeline.ts | wc -l` ≥2.
    - ARCH-02: `grep -E 'writeOutput' src/archive.ts src/pipeline.ts | wc -l` ≥2.
    - ALERT-01: проверено в SC#5.
    - ALERT-02: `grep -E 'await sendAlert' src/run.ts` 1 match.
    - DOC-04: `wc -l docs/RUNBOOK.md` ≥80; `grep -c '^## ' docs/RUNBOOK.md` ≥5.
    - DOC-05: `wc -l docs/CHANNELS.md` ≥60; `grep -c '^## ' docs/CHANNELS.md` ≥5.
  </verification_criteria>
  <verify>
    <automated>npx tsc --noEmit && node --import tsx -e "import('./src/pipeline.js').then(m => m.runPipeline()).then(s => { console.log('OK', JSON.stringify(s)); process.exit(0); }).catch(e => { console.error('FAIL', e); process.exit(1); })"</automated>
  </verify>
  <done>
    `npx tsc --noEmit` зелёный. `npm start`-эквивалентный smoke прогон сработал без unhandled rejection: создал data/raw/YYYY-MM-DD.json, в нормальном случае — data/output/YYYY-MM-DD.md и data/hash-cache.json, отправил структурированный 6-секционный дайджест в канал. Все 14 REQ закрыты по grep'ам выше. Phase 1 готов к деплою на VPS и старту Phase 2 7-day smoke.
  </done>
</task>

</tasks>

<verification>
## Cumulative Phase Verification

См. `<verification_criteria>` в Task 16 — это единственный финальный verify (D-08).
Промежуточных smoke-гейтов нет.

Roadmap Success Criteria 1-6:
1. 5 секций + блок Упоминания, пустые помечены `— нет упоминаний за сутки` → renderHtml в src/summarize.ts (T6) + smoke в T16.
2. Deep-link на каждом item → renderHtml использует item.url через `new URL()` (T6).
3. После pm2 restart нет повторов → data/hash-cache.json rolling 14d (T8) + commit после доставки (T10) + smoke в T16.
4. data/raw + data/output на каждом прогоне, output identical to sent → archive.ts (T9) + integration order в pipeline.ts (T10) + smoke в T16.
5. Alert в личку ≤60s на ошибку → alert.ts (T12) + await sendAlert в run.ts (T13) + smoke в T16.
6. `npx tsc --noEmit` чистый → final verify в T16.
</verification>

<success_criteria>
Phase 1 plan завершён, когда:
- 16 task'ов выполнены в порядке T1→T16 (D-07 wave порядок: 1-7 → 8-11 → 12-15 → 16).
- Все 14 REQ-ID имеют грановое подтверждение (см. T16 verification_criteria).
- `npx tsc --noEmit` exits 0.
- smoke `runPipeline()` на dev-машине либо проходит end-to-end (создаёт оба архива и отправляет дайджест), либо — если креденшилов нет — фиксируется явно в SUMMARY как «требует deploy на VPS».
- threat_model mitigations верифицированы: `.env` в .gitignore, alert.ts не сериализует process.env, escapeHtml применён ко всем user-controlled полям в renderHtml, имена data/raw|output формируются из MSK-даты без user input.
- Plan готов к Phase 2 (7-day smoke + acceptance-пакет).
</success_criteria>

<output>
После завершения создать `.planning/phases/01-code/01-01-SUMMARY.md` со сводкой всех 16 task'ов: что было сделано, сколько LOC изменено по файлам, smoke-результаты, перечень закрытых REQ-ID, любые отклонения от plan'а с обоснованием.
</output>
