---
phase: 02
plan: 07
title: "README: VPS/PM2 + daemon-режим + summary-лог + удалить старую дисциплину"
type: execute
wave: 4
depends_on: [5, 6]
files_modified:
  - README.md
autonomous: true
requirements: [DEPLOY-02, DOC-01, DOC-02, DOC-03]
requirements_addressed: [DEPLOY-02, DOC-01, DOC-02, DOC-03]
must_haves:
  truths:
    - "README содержит секцию `## Запуск на VPS (PM2)` с командами pm2 start/logs/save/startup"
    - "README отражает что `npm start` теперь daemon (Ctrl+C для локальной остановки)"
    - "Старая фраза «не чаще 1 прогона в 10–15 минут» УДАЛЕНА из README (крон контролирует частоту)"
    - "README содержит секцию `## Ежедневный summary-лог` с примером вывода"
    - "README упоминает node-cron в списке runtime-зависимостей (DEPLOY-02 документация)"
  artifacts:
    - path: "README.md"
      provides: "Обновлённая документация v2.0"
      contains: "## Запуск на VPS (PM2)"
---

<objective>
Обновить README.md для v2.0 daemon-режима:
1. Добавить секцию «Запуск на VPS (PM2)» с командами `pm2 start ecosystem.config.js`, `pm2 logs tg-parser`, `pm2 save`, `pm2 startup` (DOC-01).
2. Обновить секцию «Запуск в 3 команды» / «Первый прогон» — `npm start` теперь daemon, Ctrl+C для локальной остановки (DOC-02).
3. УДАЛИТЬ секцию/фразу «Дисциплина запусков» / «Не чаще одного прогона в 10–15 минут» — крон сам контролирует частоту раз в сутки (DOC-02).
4. Добавить секцию «Ежедневный summary-лог» с примером вывода `logRunSummary` (DOC-03).
5. Упомянуть node-cron в списке runtime-зависимостей (4 вместо 3) — закрывает DEPLOY-02 на уровне документации (пакет установлен в plan 05, в README пишем об этом).

Purpose: v2.0 без документации = развёрнутая бомба замедленного действия. Оператор должен иметь пошаговые команды для деплоя на VPS и понимать новый daemon-режим.

Output:
- README.md — обновлённый (новые секции + удаление устаревших)

Depends on: plan 05 (daemon существует, можно ссылаться на `npm start`) и plan 06 (ecosystem.config.js существует, можно ссылаться на PM2-команды).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-daemon-50/02-RESEARCH.md
@docs/phase-2.md
@README.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Обновить раздел «Требования» и «Запуск в 3 команды» — упомянуть daemon</name>
  <files>README.md</files>
  <read_first>
    - README.md (текущий текст)
    - docs/phase-2.md §8 (требования к README)
    - .planning/phases/02-daemon-50/02-RESEARCH.md §10 (DOC-01/02/03 спецификация)
  </read_first>
  <behavior>
    - В самом начале README после headline-строки phrase «Один Node.js-скрипт, запускается руками.» заменяется/дополняется так, что ясно: с v2.0 `npm start` — daemon с node-cron в 20:00 MSK
    - В секции «Запуск в 3 команды» последняя строка меняется со «собрать за 24ч → DeepSeek → отправить в канал» на отражение daemon-режима
    - Секция «Требования» продолжает называть Node.js 20.6+, Telegram-аккаунт, DeepSeek, приватный канал — но отдельно упоминается опциональное требование для VPS деплоя: PM2 (устанавливается глобально).
    - В секции про `npm install` перечисление runtime-зависимостей обновляется: ТЕПЕРЬ ЧЕТЫРЕ (`telegram`, `openai`, `yaml`, `node-cron`).
  </behavior>
  <action>
В README.md внести следующие правки:

**1. Заменить первые 6 строк файла** (от headline до «Без БД, без Docker, без крона.»):

БЫЛО:
```
# tg-parser-demo

За один `npm start` получить в закрытом Telegram-канале HTML-дайджест событий российского нефтегаза/нефтехимии за последние 24 часа, где каждая цитата дословно присутствует в исходном посте — без галлюцинаций LLM.

Один Node.js-скрипт, запускается руками. Без БД, без Docker, без крона.
```

СТАЛО (заменить целиком):
```
# tg-parser-demo

Ежедневный (20:00 MSK) HTML-дайджест событий российского нефтегаза/нефтехимии за последние 24 часа в закрытом Telegram-канале. Каждая цитата дословно присутствует в исходном посте — без галлюцинаций LLM.

v2.0: `npm start` — long-running daemon на Node.js + node-cron; на VPS работает под PM2. Без БД, без Docker, без внешнего крона — всё в одном процессе.
```

**2. В секции «Запуск в 3 команды»** блок кода заменить ТОЧНО на:

БЫЛО:
```
    npm install
    npm run login     # разовая генерация TG_SESSION
    npm start         # собрать за 24ч → DeepSeek → отправить в канал
```

СТАЛО:
```
    npm install
    npm run login     # разовая генерация TG_SESSION
    npm start         # запустить daemon (ежедневно в 20:00 MSK); Ctrl+C — остановить
```

**3. В секции «1. Установка зависимостей»** блок про три зависимости заменить:

БЫЛО:
```
Будет установлено ровно три runtime-зависимости: `telegram` (GramJS), `openai` (DeepSeek через OpenAI-совместимый SDK), `yaml`.
```

СТАЛО:
```
Будет установлено четыре runtime-зависимости: `telegram` (GramJS), `openai` (DeepSeek через OpenAI-совместимый SDK), `yaml`, `node-cron` (добавлен в v2.0 для daemon-режима).
```
  </action>
  <verify>
    <automated>grep -q "long-running daemon" README.md && grep -q "node-cron" README.md && grep -q "Ctrl+C — остановить" README.md && grep -q "четыре runtime-зависимости" README.md && ! grep -q "ровно три runtime-зависимости" README.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "long-running daemon" README.md` exits 0
    - `grep -q "node-cron" README.md` exits 0
    - `grep -q "Ctrl+C — остановить" README.md` exits 0 (обновлённый комментарий в блоке `npm start`)
    - `grep -q "четыре runtime-зависимости" README.md` exits 0
    - `! grep -q "ровно три runtime-зависимости" README.md` exits 0 (старая формулировка удалена)
    - `! grep -q "запускается руками" README.md` exits 0 (старая headline-фраза убрана)
    - `grep -q "20:00 MSK" README.md` exits 0 (упоминание расписания)
  </acceptance_criteria>
  <done>Headline + «Запуск в 3 команды» + «Установка зависимостей» отражают daemon-режим и четыре зависимости.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Удалить секцию «Дисциплина запусков» + обновить «5. Первый прогон»</name>
  <files>README.md</files>
  <read_first>
    - README.md (текущая секция «## Дисциплина запусков» ~строки 68-73 и «### 5. Первый прогон» ~строки 55-65)
    - docs/phase-2.md §8 (DOC-02: удалить фразу «не чаще 1 прогона в 10–15 минут»)
  </read_first>
  <behavior>
    - Секция `## Дисциплина запусков` с абзацами о FloodWait и 10-15 минутах ПОЛНОСТЬЮ УДАЛЕНА
    - В секции `### 5. Первый прогон` текст «Что произойдёт:» переписан под daemon-модель
    - Пункт «Troubleshooting → Второй FloodWaitError подряд → exit 1» обновлён/смягчён (daemon не делает exit 1 на уровне прогона — ошибка пишется в errors[] и summary-лог; следующий тик завтра в 20:00 попробует снова)
  </behavior>
  <action>
**1. Полностью удалить секцию «## Дисциплина запусков»** — весь блок между `## Дисциплина запусков` и следующей секцией `## Как проверить, что всё работает (5 критериев приёмки)`. Удалить заголовок и все абзацы, включая пункт про «Не чаще одного прогона в 10–15 минут.»

**2. Заменить секцию «### 5. Первый прогон»** — весь блок (заголовок + «Что произойдёт:» + 5 нумерованных пунктов) на:

```markdown
### 5. Первый запуск daemon

    npm start

Что произойдёт:

1. Процесс стартует и выводит в stdout: `[<ISO>] [info] daemon started, schedule: 0 20 * * * Europe/Moscow`.
2. Процесс висит (event-loop держится от node-cron handle). Пока не 20:00 MSK — ничего не происходит.
3. В 20:00 MSK запускается `tick()`:
   - Обходит 50 каналов (с задержкой `CHANNEL_DELAY_MS=1750ms + 0–500ms jitter`), собирает посты за последние 24ч (до 50/канал).
   - In-memory дедуплицирует повторы по `${username}:${messageId}`.
   - Если постов 0 — лог `No posts in window — skipping digest`, выхода нет (daemon живёт).
   - Иначе — батч в DeepSeek → верификация дословности `keyQuote` → HTML → Bot API → приватный канал.
4. По итогу tick печатает многострочный summary-лог (см. «Ежедневный summary-лог» ниже).
5. Следующий tick — завтра в 20:00 MSK.

Остановить локально: `Ctrl+C` → graceful shutdown (daemon ждёт активный прогон, потом `exit 0`).

Сетевые сбои во время прогона: GramJS делает до 3 попыток reconnect с exp backoff (1s / 2s / 4s). При исчерпании канал помечается как skipped, попадает в `errors[]` summary-лога, прогон продолжается с остальными каналами.
```

**3. В секции «## Troubleshooting»** переписать пункт про второй FloodWait:

БЫЛО:
```
### Второй `FloodWaitError` подряд → exit 1

Ты запускаешь слишком часто или прогон попал в квоту. Подожди 30–60 минут и попробуй снова. Соблюдай дисциплину «не чаще одного прогона в 10–15 минут».
```

СТАЛО:
```
### Второй `FloodWaitError` на одном канале в прогоне

Канал помечается как skipped и попадает в `errors[]` summary-лога. Прогон продолжается с остальными каналами. Следующий tick завтра в 20:00 MSK попробует канал снова.

Если FloodWait'ы участились на многих каналах — повысь `CHANNEL_DELAY_MS` в `.env` с 1750мс до 2500мс (увеличит общее время прогона ~на 40 секунд).
```
  </action>
  <verify>
    <automated>! grep -q "## Дисциплина запусков" README.md && ! grep -q "не чаще одного прогона в 10" README.md && ! grep -q "не чаще 1 прогона" README.md && grep -q "### 5. Первый запуск daemon" README.md && grep -q "daemon started, schedule: 0 20" README.md && grep -q "graceful shutdown" README.md && grep -q "Второй \`FloodWaitError\` на одном канале в прогоне" README.md && grep -q "CHANNEL_DELAY_MS" README.md</automated>
  </verify>
  <acceptance_criteria>
    - `! grep -q "## Дисциплина запусков" README.md` exits 0 (секция удалена целиком)
    - `! grep -q "не чаще одного прогона" README.md` exits 0 (старая фраза удалена)
    - `! grep -q "не чаще 1 прогона" README.md` exits 0 (варианты написания — все удалены)
    - `grep -q "### 5. Первый запуск daemon" README.md` exits 0 (новый заголовок секции)
    - `grep -q "daemon started, schedule: 0 20" README.md` exits 0 (пример лог-строки в README)
    - `grep -q "graceful shutdown" README.md` exits 0 (упоминание behavior)
    - `grep -q "Второй \`FloodWaitError\` на одном канале в прогоне" README.md` exits 0 (обновлённый troubleshooting)
    - `grep -q "reconnect" README.md` exits 0 (упомянут в объяснении behavior)
  </acceptance_criteria>
  <done>Дисциплина запусков удалена; «5. Первый прогон» переписана как «5. Первый запуск daemon»; troubleshooting FloodWait обновлён.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Добавить секции «## Запуск на VPS (PM2)» и «## Ежедневный summary-лог»</name>
  <files>README.md</files>
  <read_first>
    - README.md (для определения места вставки — ПЕРЕД секцией «## Как проверить, что всё работает» или «## Известные ограничения»)
    - docs/phase-2.md §8 (канонический текст секций)
    - .planning/phases/02-daemon-50/02-RESEARCH.md §10 (шаблон секции VPS/PM2 + пример summary-лога)
  </read_first>
  <behavior>
    - Секция «## Запуск на VPS (PM2)» добавлена ПЕРЕД секцией «## Как проверить, что всё работает (5 критериев приёмки)» (или перед «## Известные ограничения», если удобнее; главное — после «Первый запуск daemon»)
    - Секция содержит 5 пронумерованных шагов с командами pm2
    - Все 4 команды присутствуют: `pm2 start ecosystem.config.js`, `pm2 logs tg-parser`, `pm2 save`, `pm2 startup`
    - Секция «## Ежедневный summary-лог» добавлена ПОСЛЕ секции VPS/PM2 (или перед «## Известные ограничения»)
    - Содержит кодовый блок с примером вывода logRunSummary
  </behavior>
  <action>
**1. Добавить секцию `## Запуск на VPS (PM2)`** после подсекции «### 5. Первый запуск daemon» (из Task 2) и ПЕРЕД заголовком «## Как проверить, что всё работает» (или там, где этот заголовок сейчас находится). Вставить следующий блок целиком:

```markdown
## Запуск на VPS (PM2)

На VPS daemon запускается под PM2: process manager мониторит процесс, рестартует при падении, поднимается автоматически после перезагрузки сервера. Установить PM2 глобально нужно один раз:

    npm install -g pm2

Запуск и базовые операции:

    # 1. Запустить daemon по ecosystem-конфигу
    pm2 start ecosystem.config.js

    # 2. Проверить статус
    pm2 status

    # 3. Посмотреть логи (stdout + stderr, tail -f)
    pm2 logs tg-parser

    # 4. Сохранить список процессов для auto-resurrect после перезагрузки
    pm2 save

    # 5. Настроить автозапуск PM2 при перезагрузке VPS
    pm2 startup
    # → PM2 выведет systemd-команду; выполни её (нужны sudo-права)

Операции после изменения кода:

    pm2 restart tg-parser       # graceful restart (SIGINT → ждёт текущий прогон → exit 0 → respawn)
    pm2 reload tg-parser        # alias restart для fork mode
    pm2 stop tg-parser          # остановить (без удаления из списка)
    pm2 delete tg-parser        # удалить из списка

`pm2 kill && pm2 resurrect` — полный перезапуск PM2-демона с восстановлением сохранённых процессов из `~/.pm2/dump.pm2` (нужно предварительно сделать `pm2 save`).

Логи PM2:

    pm2 logs tg-parser --out          # только stdout (наш logger)
    pm2 logs tg-parser --err          # только stderr (warn/error)
    ~/.pm2/logs/tg-parser-out.log     # файл stdout (для grep/tail)
    ~/.pm2/logs/tg-parser-error.log   # файл stderr

Конфиг daemon'а — в `ecosystem.config.js`. Ключевые параметры:
- `kill_timeout: 180000` — PM2 ждёт до 3 минут после SIGINT перед SIGKILL, чтобы graceful shutdown успел завершить активный прогон.
- `max_restarts: 10` + `min_uptime: "30s"` — защита от флап-рестартов (если daemon падает в первые 30с, PM2 сдаётся после 10 попыток).
- `max_memory_restart: "300M"` — рестарт при утечке памяти.
```

**2. Добавить секцию `## Ежедневный summary-лог`** СРАЗУ ПОСЛЕ предыдущей секции, до «## Известные ограничения»:

```markdown
## Ежедневный summary-лог

По итогу каждого tick (20:00 MSK) daemon печатает многострочный summary-блок в stdout (попадает в `~/.pm2/logs/tg-parser-out.log` на VPS):

    [2026-04-22T17:00:42.123Z] [summary] runId=abc12345
      duration=58.4s
      channels: total=50 succeeded=47 skipped=3
      posts: collected=412 deduped=5
      delivered=true
      errors:
        - neftegazru: FloodWait retry exhausted
        - oil_gas_forum: network disconnect after 3 attempts
        - some_private: ChannelPrivateError

Поля:
- `runId` — короткий UUID прогона (для корреляции с предыдущими логами `[pipeline] runId=...`).
- `duration` — длительность в секундах (на 50 каналах ожидается 90-180 сек).
- `channels: total=N succeeded=M skipped=K` — сколько каналов прошли без ошибок и сколько помечены skipped (попали в errors[]).
- `posts: collected=N deduped=K` — собрано уникальных постов и отброшено дублей по `${username}:${messageId}` в рамках прогона.
- `delivered=true|false` — отправлен ли HTML-дайджест в приватный канал (false если `posts.collected === 0` — пустой день).
- `errors:` — список ошибок в формате `${username}: ${message}`; секция не печатается если ошибок нет.

Посмотреть последний summary-блок быстро:

    pm2 logs tg-parser --out --nostream | grep -A 20 "\[summary\]" | tail -25
```

Проверить что Markdown не сломан — ведущие отступы 4 пробела для кодовых блоков (как в v1.0-README). Порядок секций:
1. Headline
2. Требования
3. Запуск в 3 команды
4. Подготовка перед первым прогоном (1..5)
5. **Запуск на VPS (PM2)** (НОВАЯ)
6. **Ежедневный summary-лог** (НОВАЯ)
7. Как проверить, что всё работает
8. Известные ограничения
9. Troubleshooting
10. Структура проекта
11. Лицензия
  </action>
  <verify>
    <automated>grep -q "^## Запуск на VPS (PM2)$" README.md && grep -q "pm2 start ecosystem.config.js" README.md && grep -q "pm2 logs tg-parser" README.md && grep -q "pm2 save" README.md && grep -q "pm2 startup" README.md && grep -q "kill_timeout: 180000" README.md && grep -q "^## Ежедневный summary-лог$" README.md && grep -q "\[summary\] runId=" README.md && grep -q "channels: total=50 succeeded=" README.md && grep -q "delivered=true" README.md && grep -q "deduped=" README.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "^## Запуск на VPS (PM2)$" README.md` exits 0 (секция существует с точным заголовком)
    - Все 4 команды PM2 присутствуют: `pm2 start ecosystem.config.js`, `pm2 logs tg-parser`, `pm2 save`, `pm2 startup` — каждая findable через `grep -q`
    - `grep -q "kill_timeout: 180000" README.md` exits 0 (упомянуто в контексте конфига)
    - `grep -q "^## Ежедневный summary-лог$" README.md` exits 0 (секция существует с точным заголовком)
    - Пример summary-лога содержит: `[summary] runId=`, `channels: total=50 succeeded=`, `delivered=true`, `posts: collected=`, `deduped=` — все findable через `grep -q`
    - Пример в summary содержит хотя бы одну строку `errors:` секции и пример ошибки вида `- <username>: <message>`
    - Секции находятся в порядке: «5. Первый запуск daemon» → «Запуск на VPS (PM2)» → «Ежедневный summary-лог» → «Как проверить...». Проверка: номера строк от начала файла для этих 4 заголовков должны быть возрастающими:
      `awk '/^### 5\. Первый запуск daemon$/{a=NR} /^## Запуск на VPS \(PM2\)$/{b=NR} /^## Ежедневный summary-лог$/{c=NR} /^## Как проверить/{d=NR} END{exit !(a<b && b<c && c<d)}' README.md` exits 0
  </acceptance_criteria>
  <done>README содержит две новые секции с точными заголовками, все команды PM2 и пример summary-лога на месте; порядок секций логичен.</done>
</task>

</tasks>

<verification>
1. `grep -c "^## " README.md` — общее количество секций (должно увеличиться на 2 по сравнению с v1.0).
2. `grep -q "pm2 startup" README.md && grep -q "pm2 save" README.md && grep -q "pm2 logs tg-parser" README.md && grep -q "pm2 start ecosystem.config.js" README.md` — все 4 команды PM2 присутствуют (DOC-01).
3. `! grep -q "не чаще" README.md` — старая дисциплина удалена (DOC-02).
4. `grep -q "\[summary\] runId=" README.md && grep -q "delivered=true" README.md` — пример summary-лога на месте (DOC-03).
5. Ручная визуальная проверка: открыть README.md в markdown-viewer, убедиться что кодовые блоки отрендерились, не сломались отступы, оглавление логично.
</verification>

<success_criteria>
- Секция «Запуск на VPS (PM2)» с 4 командами pm2 (DOC-01) ✓ Task 3
- README отражает daemon-режим `npm start` (DOC-02) ✓ Task 1 + Task 2
- Старая фраза «не чаще 1 прогона в 10–15 минут» удалена (DOC-02) ✓ Task 2
- Секция «Ежедневный summary-лог» с примером вывода (DOC-03) ✓ Task 3
- node-cron упомянут в списке runtime-зависимостей (DEPLOY-02 doc-side) ✓ Task 1
</success_criteria>

<output>
После выполнения создать `.planning/phases/02-daemon-50/02-07-SUMMARY.md` с:
- Списком добавленных/удалённых секций README
- Diff-summary: что изменилось в «Запуск в 3 команды», «5. Первый прогон», «Troubleshooting»
- Подтверждение что 4 команды pm2 документированы
</output>
