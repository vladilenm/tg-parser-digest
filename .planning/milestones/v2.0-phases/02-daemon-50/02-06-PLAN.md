---
phase: 02
plan: 06
title: "PM2 ecosystem.config.js + kill_timeout"
type: execute
wave: 3
depends_on: [1, 2]
files_modified:
  - ecosystem.config.js
autonomous: true
requirements: [DEPLOY-01]
requirements_addressed: [DEPLOY-01]
must_haves:
  truths:
    - "ecosystem.config.js существует в корне проекта"
    - "script: src/run.ts, interpreter: node, interpreter_args: --env-file=.env --import tsx"
    - "kill_timeout: 180000 (3 минуты) — хватит на graceful shutdown активного прогона"
    - "autorestart: true, max_restarts: 10, min_uptime: 30s (flap protection)"
    - "max_memory_restart: 300M, exec_mode: fork, instances: 1"
  artifacts:
    - path: "ecosystem.config.js"
      provides: "PM2-конфиг для tsx + ESM daemon"
      contains: "tg-parser"
  key_links:
    - from: "ecosystem.config.js"
      to: "src/run.ts"
      via: "script entry"
      pattern: 'script:\s*"src/run\.ts"'
---

<objective>
Создать ecosystem.config.js в корне проекта с конфигом PM2 для запуска daemon (src/run.ts) через node + --env-file + --import tsx. Добавить `kill_timeout: 180000` (3 мин) для гарантии того, что PM2 во время graceful reload/stop не убьёт daemon SIGKILL'ом до завершения активного прогона (PM2 default kill_timeout = 1600мс — недостаточно, прогон суточный длится 2-3 минуты).

Purpose: DEPLOY-01 — без PM2-конфига нельзя запустить daemon на VPS по стандартному пути `pm2 start ecosystem.config.js`. Research §9 pitfall 5 и Assumption A1 явно указывают: нужен `kill_timeout: 180000`, иначе PM2 прерывает graceful shutdown.

Output:
- ecosystem.config.js — новый файл в корне

Depends on: plan 01 (pipeline.ts) и plan 02 (logger.ts) — формально конфиг не зависит от их содержимого, но logically PM2-конфиг запускает daemon, который требует эти файлы. Depends_on = [1, 2] для корректного ordering в исполнении (wave 3 после wave 1).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-daemon-50/02-RESEARCH.md
@docs/phase-2.md
@CLAUDE.md
@package.json
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Создать ecosystem.config.js</name>
  <files>ecosystem.config.js</files>
  <read_first>
    - docs/phase-2.md §6 (ecosystem.config.js канонический шаблон)
    - .planning/phases/02-daemon-50/02-RESEARCH.md §9 (PM2-детали + kill_timeout + pitfall 5)
    - package.json (убедиться что script `"start": "node --env-file=.env --import tsx src/run.ts"` — ecosystem точно повторяет этот вызов)
  </read_first>
  <behavior>
    - Файл является CommonJS (module.exports = ...) — это стандарт для PM2 ecosystem file
    - apps — массив из одного приложения с name "tg-parser"
    - script: "src/run.ts", interpreter: "node", interpreter_args: "--env-file=.env --import tsx"
    - instances: 1, exec_mode: "fork" (DAEMON-03 mutex предполагает один процесс)
    - autorestart: true, max_restarts: 10, min_uptime: "30s" (флап-защита)
    - max_memory_restart: "300M"
    - time: true (timestamp в PM2-логах)
    - kill_timeout: 180000 — 3 минуты, чтобы graceful shutdown успел завершиться (research A1 + pitfall 5)
  </behavior>
  <action>
Создать в корне проекта новый файл `ecosystem.config.js` со следующим содержимым ВЕРБАТИМ:

```javascript
// ecosystem.config.js — PM2-конфиг для tg-parser daemon.
// Запуск: pm2 start ecosystem.config.js (см. README секцию «Запуск на VPS (PM2)»).
// kill_timeout поднят с 1600мс (PM2 default) до 180000мс (3 минуты),
// чтобы graceful shutdown успел дождаться активного прогона перед SIGKILL.

module.exports = {
  apps: [
    {
      name: "tg-parser",
      script: "src/run.ts",
      interpreter: "node",
      interpreter_args: "--env-file=.env --import tsx",
      instances: 1,
      exec_mode: "fork",
      autorestart: true,
      max_restarts: 10,
      min_uptime: "30s",
      max_memory_restart: "300M",
      kill_timeout: 180000,
      time: true,
    },
  ],
};
```

Важно:
- Файл в ФОРМАТЕ CommonJS (`module.exports = { ... }`), несмотря на `"type": "module"` в package.json. PM2 использует Node.js require() для чтения ecosystem-файла, не ESM import. Это штатный паттерн; Node.js распознаёт `.cjs` и `.mjs` по расширению, но для `.js` учитывает `type` в package.json. PM2 принудительно читает `ecosystem.config.js` через require() — CJS синтаксис корректен.
- Если при запуске `pm2 start ecosystem.config.js` будет ошибка `module is not defined in ES module scope`, переименовать файл в `ecosystem.config.cjs` и указать путь `pm2 start ecosystem.config.cjs`. НО этот риск низкий — PM2 документация и research §9 используют `.js` расширение.
- Никаких новых зависимостей (PM2 устанавливается глобально через `npm install -g pm2` на VPS, это НЕ идёт в package.json).
  </action>
  <verify>
    <automated>test -f ecosystem.config.js && grep -q "module.exports" ecosystem.config.js && grep -q 'name: "tg-parser"' ecosystem.config.js && grep -q 'script: "src/run.ts"' ecosystem.config.js && grep -q 'interpreter: "node"' ecosystem.config.js && grep -q 'interpreter_args: "--env-file=.env --import tsx"' ecosystem.config.js && grep -q "instances: 1" ecosystem.config.js && grep -q 'exec_mode: "fork"' ecosystem.config.js && grep -q "autorestart: true" ecosystem.config.js && grep -q "max_restarts: 10" ecosystem.config.js && grep -q 'min_uptime: "30s"' ecosystem.config.js && grep -q 'max_memory_restart: "300M"' ecosystem.config.js && grep -q "kill_timeout: 180000" ecosystem.config.js && grep -q "time: true" ecosystem.config.js && node -e "const c = require('./ecosystem.config.js'); if (!c.apps || c.apps.length !== 1 || c.apps[0].name !== 'tg-parser') process.exit(1)"</automated>
  </verify>
  <acceptance_criteria>
    - Файл ecosystem.config.js создан в корне
    - `grep -q "module.exports" ecosystem.config.js` exits 0 (CJS-формат)
    - `grep -q 'name: "tg-parser"' ecosystem.config.js` exits 0
    - `grep -q 'script: "src/run.ts"' ecosystem.config.js` exits 0
    - `grep -q 'interpreter: "node"' ecosystem.config.js` exits 0
    - `grep -q 'interpreter_args: "--env-file=.env --import tsx"' ecosystem.config.js` exits 0
    - `grep -q "instances: 1" ecosystem.config.js` exits 0
    - `grep -q 'exec_mode: "fork"' ecosystem.config.js` exits 0
    - `grep -q "autorestart: true" ecosystem.config.js` exits 0
    - `grep -q "max_restarts: 10" ecosystem.config.js` exits 0
    - `grep -q 'min_uptime: "30s"' ecosystem.config.js` exits 0
    - `grep -q 'max_memory_restart: "300M"' ecosystem.config.js` exits 0
    - `grep -q "kill_timeout: 180000" ecosystem.config.js` exits 0 — **ключевое требование research A1/pitfall 5: 180000 ≥ 180000мс = 3 минуты**
    - `grep -q "time: true" ecosystem.config.js` exits 0
    - `node -e "const c = require('./ecosystem.config.js'); if (!c.apps || c.apps.length !== 1 || c.apps[0].name !== 'tg-parser') process.exit(1)"` exits 0 (CJS-конфиг валиден как Node module)
  </acceptance_criteria>
  <done>ecosystem.config.js создан в корне; все 11 ключей на месте; kill_timeout = 180000; Node может загрузить файл через require().</done>
</task>

</tasks>

<verification>
1. `test -f ecosystem.config.js` exits 0.
2. `node -e "const c = require('./ecosystem.config.js'); console.log(JSON.stringify(c, null, 2))"` выводит валидный JSON с apps[0] = {name: "tg-parser", script: "src/run.ts", ..., kill_timeout: 180000}.
3. На VPS (если проверка проводится после деплоя): `pm2 start ecosystem.config.js` → `pm2 status` показывает процесс online.
</verification>

<success_criteria>
- ecosystem.config.js создан с правильной структурой PM2 (DEPLOY-01)
- kill_timeout 180000мс закрывает pitfall 5 из research (граница SIGKILL расширена с 1600мс до 3 минут)
- Конфиг валиден как Node module (CJS), `node -e "require(...)"` загружает без ошибок
</success_criteria>

<output>
После выполнения создать `.planning/phases/02-daemon-50/02-06-SUMMARY.md` с описанием ключей ecosystem.config.js и обоснованием kill_timeout=180000.
</output>
