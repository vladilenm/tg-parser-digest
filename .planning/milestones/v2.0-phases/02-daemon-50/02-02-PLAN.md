---
phase: 02
plan: 02
title: "Structured logger + logRunSummary"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/logger.ts
autonomous: true
requirements: [LOG-01, LOG-03]
requirements_addressed: [LOG-01, LOG-03]
must_haves:
  truths:
    - "log.info/warn/error пишут в console с префиксом `[ISO-timestamp] [level]`"
    - "logRunSummary печатает многострочный блок со всеми счётчиками RunSummary"
    - "logger не зависит от сторонних пакетов — только console"
  artifacts:
    - path: "src/logger.ts"
      provides: "log object + logRunSummary(s) function"
      exports: ["log", "logRunSummary"]
  key_links:
    - from: "src/logger.ts"
      to: "src/types.ts"
      via: "import type { RunSummary }"
      pattern: "import type \\{ RunSummary \\} from \"./types.js\""
---

<objective>
Создать src/logger.ts — структурированный логгер с префиксом `[ISO-timestamp] [level]` и функцию `logRunSummary(s: RunSummary)` для многострочного диагностического вывода по итогам прогона. Без сторонних зависимостей — только console.log/warn/error (PM2 перехватит в pm2-out.log / pm2-err.log).

Purpose: Диагностируемые прогоны через один формат логов. Daemon (plan 04) и pipeline (plan 01) будут вызывать log.info/warn/error; summary — основной артефакт для проверки Success Criterion 2 (`channels: total=50`, `delivered=true`, `durationMs`).

Output:
- src/logger.ts — новый файл с `export const log` и `export function logRunSummary`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-daemon-50/02-RESEARCH.md
@docs/phase-2.md
@CLAUDE.md
@src/types.ts

<interfaces>
<!-- RunSummary добавляется в plan 01. Этот plan параллелен: к моменту execution оба файла в одной wave, executor должен ПРОЧИТАТЬ src/types.ts и проверить, что RunSummary уже на месте, либо добавить его сам если исполняется отдельно. -->

Ожидаемая форма RunSummary (из plan 01):
```typescript
export interface RunSummary {
  runId: string;
  startedAt: string;
  finishedAt: string;
  durationMs: number;
  channelsTotal: number;
  channelsSucceeded: number;
  channelsSkipped: number;
  postsCollected: number;
  postsDeduped: number;
  digestDelivered: boolean;
  errors: string[];
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Создать src/logger.ts с log.info/warn/error и logRunSummary</name>
  <files>src/logger.ts</files>
  <read_first>
    - docs/phase-2.md §4 (канонический формат logRunSummary output)
    - .planning/phases/02-daemon-50/02-RESEARCH.md §7 (LOG-01/LOG-03 спецификация + пример шаблона)
    - src/types.ts (проверить что RunSummary уже добавлен из plan 01; если нет — plan 02 выполняется параллельно и плановая wave=1 это допускает, но executor должен убедиться что tsc проходит)
  </read_first>
  <behavior>
    - `log.info(msg, ...ctx)` вызывает `console.log` с префиксом `[ISO-timestamp] [info]` и передаёт `...ctx` как дополнительные аргументы
    - `log.warn(msg, ...ctx)` — `console.warn` + `[warn]`
    - `log.error(msg, ...ctx)` — `console.error` + `[error]`
    - `logRunSummary(s: RunSummary)` печатает многострочный блок ровно в формате из docs/phase-2.md §4
    - Если `s.errors.length === 0` — строка `errors:` и список НЕ печатаются
    - Если `s.errors.length > 0` — строка `  errors:` и список `    - ${err}` печатаются
    - Длительность форматируется как `(durationMs / 1000).toFixed(1) + "s"`
  </behavior>
  <action>
Создать новый файл src/logger.ts со следующим содержимым ВЕРБАТИМ:

```typescript
// src/logger.ts — структурированный логгер для daemon-режима.
// Пишет через console.log/warn/error; PM2 перехватит в pm2-out.log / pm2-err.log.
// Без сторонних зависимостей.

import type { RunSummary } from "./types.js";

function timestamp(): string {
  return new Date().toISOString();
}

export const log = {
  info(msg: string, ...ctx: unknown[]): void {
    console.log(`[${timestamp()}] [info] ${msg}`, ...ctx);
  },
  warn(msg: string, ...ctx: unknown[]): void {
    console.warn(`[${timestamp()}] [warn] ${msg}`, ...ctx);
  },
  error(msg: string, ...ctx: unknown[]): void {
    console.error(`[${timestamp()}] [error] ${msg}`, ...ctx);
  },
};

/**
 * Печатает многострочный summary-блок для RunSummary.
 * Формат зафиксирован в docs/phase-2.md §4.
 */
export function logRunSummary(s: RunSummary): void {
  const dur = (s.durationMs / 1000).toFixed(1);
  const lines = [
    `[${s.finishedAt}] [summary] runId=${s.runId}`,
    `  duration=${dur}s`,
    `  channels: total=${s.channelsTotal} succeeded=${s.channelsSucceeded} skipped=${s.channelsSkipped}`,
    `  posts: collected=${s.postsCollected} deduped=${s.postsDeduped}`,
    `  delivered=${s.digestDelivered}`,
  ];
  if (s.errors.length > 0) {
    lines.push("  errors:");
    for (const e of s.errors) {
      lines.push(`    - ${e}`);
    }
  }
  console.log(lines.join("\n"));
}
```

Важные моменты:
- Импорт RunSummary — type-only (`import type`): logger не используется plan 01 пайплайном в runtime (только типами), type-only импорт не создаёт circular deps.
- Никаких уровней-фильтров (LOG_LEVEL не используется, это out-of-scope по STATE.md).
- `console.log`/`console.warn`/`console.error` — разделение между stdout (info) и stderr (warn/error) нужно PM2 для корректного сплита в `pm2-out.log` / `pm2-err.log`.

Запустить `npx tsc --noEmit` для проверки типов.
  </action>
  <verify>
    <automated>test -f src/logger.ts && grep -q "export const log" src/logger.ts && grep -q "export function logRunSummary" src/logger.ts && grep -q "import type { RunSummary }" src/logger.ts && grep -q '\[\${timestamp()}\] \[info\]' src/logger.ts && grep -q "console.log" src/logger.ts && grep -q "console.warn" src/logger.ts && grep -q "console.error" src/logger.ts && grep -q "runId=\${s.runId}" src/logger.ts && grep -q "channels: total=\${s.channelsTotal}" src/logger.ts && grep -q "delivered=\${s.digestDelivered}" src/logger.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Файл src/logger.ts создан
    - `grep -q "export const log" src/logger.ts` exits 0
    - `grep -q "export function logRunSummary" src/logger.ts` exits 0
    - `grep -q "import type { RunSummary }" src/logger.ts` exits 0 (type-only импорт)
    - Присутствуют три метода: info, warn, error (`grep -c "^  (info\|warn\|error)(" src/logger.ts` >= 3 — соответствуют им)
    - Префикс `[ISO] [level]` реализован через `timestamp()` + template string
    - Формат summary содержит: `runId=`, `duration=`, `channels: total=`, `posts: collected=`, `delivered=`
    - Секция `errors:` печатается только при `s.errors.length > 0` (проверяется чтением кода: `if (s.errors.length > 0)`)
    - Отсутствуют внешние импорты кроме `./types.js` (`grep "^import" src/logger.ts` содержит единственную строку `import type { RunSummary } from "./types.js";`)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>src/logger.ts компилируется; log.info/warn/error используют console.log/warn/error с префиксом `[ISO] [level]`; logRunSummary печатает 5 базовых строк + опциональный блок errors.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` — 0 ошибок.
2. Ручная проверка: `node --env-file=.env --import tsx -e "import('./src/logger.js').then(m => { m.log.info('test', {k: 1}); m.logRunSummary({ runId: 'abc', startedAt: '', finishedAt: '2026-04-22T17:00:00Z', durationMs: 58400, channelsTotal: 50, channelsSucceeded: 47, channelsSkipped: 3, postsCollected: 412, postsDeduped: 5, digestDelivered: true, errors: ['x: y'] }); })"` выводит:
   - `[<ISO>] [info] test { k: 1 }`
   - многострочный блок с `channels: total=50 succeeded=47 skipped=3`, `delivered=true`, `errors:`, `    - x: y`
</verification>

<success_criteria>
- src/logger.ts экспортирует `log` и `logRunSummary` (LOG-01, LOG-03)
- Префикс `[ISO-timestamp] [level]` реализован (LOG-01)
- Формат logRunSummary совпадает с docs/phase-2.md §4 (LOG-03)
- Не добавлено ни одной новой зависимости в package.json (LOG-01: только console)
- npx tsc --noEmit проходит
</success_criteria>

<output>
После выполнения создать `.planning/phases/02-daemon-50/02-02-SUMMARY.md` со списком созданных файлов и примером вывода logRunSummary.
</output>
