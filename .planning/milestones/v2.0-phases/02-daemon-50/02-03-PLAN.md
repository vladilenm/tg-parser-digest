---
phase: 02
plan: 03
title: "channels.yaml → 50 + CHANNEL_DELAY_MS=1750"
type: execute
wave: 1
depends_on: []
files_modified:
  - channels.yaml
  - .env.example
autonomous: false
requirements: [SCALE-01, SCALE-02]
requirements_addressed: [SCALE-01, SCALE-02]
must_haves:
  truths:
    - "channels.yaml содержит ровно 50 записей"
    - "CHANNEL_DELAY_MS=1750 зафиксирован в .env.example"
    - "Структура каналов (username + priority) не сломана"
  artifacts:
    - path: "channels.yaml"
      provides: "Список 50 публичных каналов нефтегаза РФ"
      contains: "- username:"
    - path: ".env.example"
      provides: "Шаблон окружения с CHANNEL_DELAY_MS=1750"
      contains: "CHANNEL_DELAY_MS=1750"
  key_links:
    - from: "channels.yaml"
      to: "src/pipeline.ts loadChannelsYaml()"
      via: "yaml.parse() читает channels[] из этого файла"
      pattern: "channels:"
---

<objective>
Расширить channels.yaml с существующих 12 записей до 50 добавлением 38 публичных каналов российского нефтегаза/нефтехимии (нефтехимия, бункеровка, масла, битум, керосин) и поднять `CHANNEL_DELAY_MS` с `1000` до `1750` в .env.example.

Purpose: SCALE-01 требует 50 каналов в конфиге; SCALE-02 — увеличенная задержка (1750мс + 0-500мс jitter = ~2000мс avg), чтобы 50 каналов прогонялись без FloodWait.

Output:
- channels.yaml — 50 записей со структурой `{ username, priority }`
- .env.example — `CHANNEL_DELAY_MS=1750`

**Подход к 38 новым каналам (HUMAN-ACTION checkpoint):**
Список 38 каналов подбирает ОПЕРАТОР (согласно STATE.md: «Список 38 новых каналов… подбирает оператор, ревью перед мёржем»). Этот план делает две вещи:
1. Task 1 (auto): дополнить channels.yaml плейсхолдерами `- username: "PLACEHOLDER_NN"` — ровно 38 штук, чтобы общая длина списка стала 50. Это гарантирует структурный контракт SCALE-01 и позволяет pipeline pass-through плейсхолдеры (они отвалятся как `UsernameNotOccupiedError` → skipped, ровно как задуман безопасный skip-паттерн).
2. Task 2 (checkpoint:human-action): оператор заменяет каждый PLACEHOLDER_NN на реальный username живого канала перед первым реальным 20:00-прогоном. Без замены прогон всё равно пройдёт, просто 38 каналов пометятся skipped.

Task 3 (auto): обновить CHANNEL_DELAY_MS в .env.example.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-daemon-50/02-RESEARCH.md
@docs/phase-2.md
@channels.yaml
@.env.example
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Дополнить channels.yaml до 50 записей (38 плейсхолдеров)</name>
  <files>channels.yaml</files>
  <read_first>
    - channels.yaml (текущие 12 записей — их НЕ трогаем)
    - docs/phase-2.md §5 (Расширение до 50 каналов — формат и структура)
    - .planning/phases/02-daemon-50/02-RESEARCH.md §8 (SCALE-01 структура {username, priority} без изменений)
  </read_first>
  <behavior>
    - Существующие 12 каналов остаются нетронутыми (в том же порядке и с теми же priority)
    - Добавлено ровно 38 новых записей с username вида "PLACEHOLDER_01" ... "PLACEHOLDER_38" и priority: 5
    - Общая длина channels[] = 50
    - YAML остаётся валидным (yaml.parse() в pipeline не падает)
    - Комментарий в начале файла поясняет, что PLACEHOLDER_NN — стабы, которые оператор заменяет на реальные username
  </behavior>
  <action>
Отредактировать channels.yaml:
1. Сохранить существующие 12 записей БЕЗ изменений (neftegazru, oilfornication, oil_gas_forum, neftianka, energytodaygroup, oilcapital, interfaxonline, tass_agency, rbc_news, kommersant, vedomosti, prime1).
2. ПЕРЕД секцией `channels:` обновить вступительный комментарий, добавив пункт:

```
# PLACEHOLDER_NN-записи (priority: 5) — стабы для SCALE-01. Оператор заменяет
# каждую на реальный username публичного канала нефтегаза РФ перед первым
# 20:00-прогоном. Если не заменить — канал просто пометится skipped
# (UsernameNotOccupiedError/UsernameInvalidError), прогон продолжится.
```

3. После существующих 12 записей дописать ровно 38 новых записей в следующем формате (без дополнительных пустых строк внутри блока):

```yaml
  - username: "PLACEHOLDER_01"
    priority: 5
  - username: "PLACEHOLDER_02"
    priority: 5
  - username: "PLACEHOLDER_03"
    priority: 5
  - username: "PLACEHOLDER_04"
    priority: 5
  - username: "PLACEHOLDER_05"
    priority: 5
  - username: "PLACEHOLDER_06"
    priority: 5
  - username: "PLACEHOLDER_07"
    priority: 5
  - username: "PLACEHOLDER_08"
    priority: 5
  - username: "PLACEHOLDER_09"
    priority: 5
  - username: "PLACEHOLDER_10"
    priority: 5
  - username: "PLACEHOLDER_11"
    priority: 5
  - username: "PLACEHOLDER_12"
    priority: 5
  - username: "PLACEHOLDER_13"
    priority: 5
  - username: "PLACEHOLDER_14"
    priority: 5
  - username: "PLACEHOLDER_15"
    priority: 5
  - username: "PLACEHOLDER_16"
    priority: 5
  - username: "PLACEHOLDER_17"
    priority: 5
  - username: "PLACEHOLDER_18"
    priority: 5
  - username: "PLACEHOLDER_19"
    priority: 5
  - username: "PLACEHOLDER_20"
    priority: 5
  - username: "PLACEHOLDER_21"
    priority: 5
  - username: "PLACEHOLDER_22"
    priority: 5
  - username: "PLACEHOLDER_23"
    priority: 5
  - username: "PLACEHOLDER_24"
    priority: 5
  - username: "PLACEHOLDER_25"
    priority: 5
  - username: "PLACEHOLDER_26"
    priority: 5
  - username: "PLACEHOLDER_27"
    priority: 5
  - username: "PLACEHOLDER_28"
    priority: 5
  - username: "PLACEHOLDER_29"
    priority: 5
  - username: "PLACEHOLDER_30"
    priority: 5
  - username: "PLACEHOLDER_31"
    priority: 5
  - username: "PLACEHOLDER_32"
    priority: 5
  - username: "PLACEHOLDER_33"
    priority: 5
  - username: "PLACEHOLDER_34"
    priority: 5
  - username: "PLACEHOLDER_35"
    priority: 5
  - username: "PLACEHOLDER_36"
    priority: 5
  - username: "PLACEHOLDER_37"
    priority: 5
  - username: "PLACEHOLDER_38"
    priority: 5
```

Проверить результат: `node --input-type=module -e "import('yaml').then(y => { const fs = require('node:fs'); const p = y.default.parse(fs.readFileSync('channels.yaml','utf8')); console.log(p.channels.length); })"` должно напечатать `50`.
  </action>
  <verify>
    <automated>grep -c "^  - username:" channels.yaml | awk '$1 == 50 { exit 0 } { exit 1 }' && grep -q "neftegazru" channels.yaml && grep -q "oilfornication" channels.yaml && grep -q "prime1" channels.yaml && grep -c "PLACEHOLDER_" channels.yaml | awk '$1 == 38 { exit 0 } { exit 1 }' && node --input-type=module -e "import('yaml').then(async y => { const fs = await import('node:fs'); const p = y.default.parse(fs.readFileSync('channels.yaml','utf8')); if (p.channels.length !== 50) { process.exit(1); } })"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^  - username:" channels.yaml` печатает ровно `50`
    - Все 12 существующих username сохранены: neftegazru, oilfornication, oil_gas_forum, neftianka, energytodaygroup, oilcapital, interfaxonline, tass_agency, rbc_news, kommersant, vedomosti, prime1 — каждый findable через `grep -q`
    - `grep -c "PLACEHOLDER_" channels.yaml` печатает `38` (38 плейсхолдер-записей добавлено)
    - `node --input-type=module -e "import('yaml').then(async y => { const fs = await import('node:fs'); const p = y.default.parse(fs.readFileSync('channels.yaml','utf8')); if (p.channels.length !== 50) process.exit(1); })"` exits 0
    - В вступительном комментарии есть упоминание `PLACEHOLDER_NN` — `grep -q "PLACEHOLDER_NN" channels.yaml`
  </acceptance_criteria>
  <done>channels.yaml содержит 50 записей (12 реальных + 38 плейсхолдеров); yaml.parse() успешно возвращает channels.length === 50.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Обновить CHANNEL_DELAY_MS в .env.example</name>
  <files>.env.example</files>
  <read_first>
    - .env.example (текущая строка `CHANNEL_DELAY_MS=1000`)
    - docs/phase-2.md §5 (SCALE-02: CHANNEL_DELAY_MS=1750)
  </read_first>
  <behavior>
    - Строка `CHANNEL_DELAY_MS=1000` заменена на `CHANNEL_DELAY_MS=1750`
    - Комментарий над ней сохранён (`# базовая задержка между каналами; к ней добавляется jitter 0–500мс`) или обновлён с указанием причины (50 каналов + анти-FloodWait)
    - Никакие другие переменные не изменены
  </behavior>
  <action>
В .env.example найти строку:

```
CHANNEL_DELAY_MS=1000
```

Заменить на:

```
CHANNEL_DELAY_MS=1750
```

Остальные переменные (TG_API_ID, TG_API_HASH, TG_SESSION, TG_BOT_TOKEN, TG_CHANNEL_ID, DEEPSEEK_API_KEY, DEEPSEEK_MODEL, DEEPSEEK_BASE_URL, FETCH_WINDOW_HOURS, MAX_MESSAGES_PER_CHANNEL, LOG_LEVEL) НЕ трогать.

Комментарий над CHANNEL_DELAY_MS (`# базовая задержка между каналами; к ней добавляется jitter 0–500мс`) оставить как есть — формула та же, число другое.
  </action>
  <verify>
    <automated>grep -q "^CHANNEL_DELAY_MS=1750$" .env.example && ! grep -q "^CHANNEL_DELAY_MS=1000$" .env.example && grep -q "^TG_API_ID=" .env.example && grep -q "^DEEPSEEK_API_KEY=" .env.example</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "^CHANNEL_DELAY_MS=1750$" .env.example` exits 0
    - `! grep -q "^CHANNEL_DELAY_MS=1000$" .env.example` exits 0 (старое значение удалено)
    - Остальные ключи на месте: TG_API_ID, TG_API_HASH, TG_SESSION, TG_BOT_TOKEN, TG_CHANNEL_ID, DEEPSEEK_API_KEY, DEEPSEEK_MODEL, DEEPSEEK_BASE_URL, FETCH_WINDOW_HOURS, MAX_MESSAGES_PER_CHANNEL, LOG_LEVEL (все findable через `grep -q "^<KEY>="`)
    - Комментарий над строкой (`# базовая задержка...`) сохранён
  </acceptance_criteria>
  <done>.env.example содержит `CHANNEL_DELAY_MS=1750`; остальные переменные нетронуты.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Оператор заменяет PLACEHOLDER_NN на реальные username</name>
  <what-built>channels.yaml теперь содержит 50 записей, но 38 из них — плейсхолдеры. Код работоспособен (daemon запустится, pipeline пройдёт, плейсхолдеры отвалятся как skipped каналы через UsernameNotOccupiedError).</what-built>
  <how-to-verify>
Оператор (ты):
1. Открой channels.yaml.
2. Для каждой записи `- username: "PLACEHOLDER_NN"` найди реальный публичный канал российского нефтегаза/нефтехимии и замени PLACEHOLDER_NN на его username (без `@`).
3. Тематики для подбора: нефтехимия, бункеровка, масла, битум, керосин, смежные отрасли.
4. Критерий: канал публичный (доступен по `t.me/USERNAME`), твой user-аккаунт из TG_SESSION подписан на этот канал.
5. priority можно менять по своему усмотрению (1 = главные, 5 = менее приоритетные). Поле носит справочный характер, pipeline его пока не использует.
6. После замен проверить что PLACEHOLDER_ нигде не остался: `grep -c "PLACEHOLDER_" channels.yaml` должен напечатать `0`.
7. Итоговая длина `channels[]` = 50 (проверить `grep -c "^  - username:" channels.yaml` = 50).

Если не заменить до первого 20:00-прогона — ничего не сломается, просто в summary-логе будет `channels: total=50 succeeded=12 skipped=38` и 38 ошибок `UsernameNotOccupiedError` в `errors[]`. Это допустимо как временное состояние, но смысл расширения до 50 каналов теряется.
  </how-to-verify>
  <resume-signal>Пиши `approved` когда все 38 плейсхолдеров заменены (или `skip` если решил оставить часть плейсхолдеров до следующей итерации — это тоже валидный вариант, phase не блокируется).</resume-signal>
</task>

</tasks>

<verification>
1. `grep -c "^  - username:" channels.yaml` = 50.
2. `grep -q "^CHANNEL_DELAY_MS=1750$" .env.example` exits 0.
3. После Task 3 (optional): `grep -c "PLACEHOLDER_" channels.yaml` = 0 (если оператор заменил все плейсхолдеры).
</verification>

<success_criteria>
- channels.yaml содержит 50 записей (SCALE-01) ✓ Task 1
- .env.example содержит `CHANNEL_DELAY_MS=1750` (SCALE-02) ✓ Task 2
- Оператор проинструктирован и имеет чёткий путь замены плейсхолдеров (не блокер на уровне phase — код рабочий с плейсхолдерами) ✓ Task 3
</success_criteria>

<output>
После выполнения создать `.planning/phases/02-daemon-50/02-03-SUMMARY.md` со списком изменённых файлов и статусом замены плейсхолдеров.
</output>
