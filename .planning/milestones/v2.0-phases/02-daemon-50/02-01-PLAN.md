---
phase: 02
plan: 01
title: "Pipeline extract + RunSummary + in-memory dedupe"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types.ts
  - src/pipeline.ts
autonomous: true
requirements: [PIPE-01, PIPE-02, PIPE-03, LOG-02]
requirements_addressed: [PIPE-01, PIPE-02, PIPE-03, LOG-02]
must_haves:
  truths:
    - "runPipeline() экспортируется из src/pipeline.ts и возвращает Promise<RunSummary>"
    - "process.exit(...) полностью удалён из pipeline (ошибки throw-ятся наверх)"
    - "GramJS-клиент создаётся внутри runPipeline и disconnect'ится в finally"
    - "Повторы по ключу ${username}:${messageId} отбрасываются и учитываются в postsDeduped"
    - "RunSummary содержит 11 полей согласно LOG-02"
    - "pipeline.ts использует log.info/log.warn из logger.ts — НЕ console.log/console.warn напрямую (LOG-01)"
  artifacts:
    - path: "src/types.ts"
      provides: "Интерфейс RunSummary"
      contains: "export interface RunSummary"
    - path: "src/pipeline.ts"
      provides: "runPipeline() — автономная логика одного прогона"
      exports: ["runPipeline"]
  key_links:
    - from: "src/pipeline.ts"
      to: "src/telegram.ts"
      via: "createClient() + fetchLast24h()"
      pattern: "import \\{ createClient, fetchLast24h"
    - from: "src/pipeline.ts"
      to: "src/summarize.ts + src/deliver.ts"
      via: "summarize(posts) → sendToChannel(html)"
      pattern: "await summarize\\(|await sendToChannel\\("
    - from: "src/pipeline.ts"
      to: "src/logger.ts"
      via: "import { log } — единый формат логирования (LOG-01)"
      pattern: "import \\{ log \\} from \"./logger.js\""
---

<objective>
Вынести логику одного прогона из src/run.ts в отдельный модуль src/pipeline.ts с функцией `runPipeline(): Promise<RunSummary>`. Добавить интерфейс `RunSummary` в src/types.ts. Добавить in-memory дедупу постов по ключу `${username}:${messageId}` в рамках одного прогона. Убрать все `process.exit(...)` — ошибки пробрасываются наверх вызывающему (daemon в plan 04).

Purpose: src/run.ts в plan 04 станет daemon-ом поверх node-cron и не должен сам знать, как устроен пайплайн. Pipeline должен быть детерминированной функцией, которую daemon вызывает из tick().

Output:
- src/types.ts — добавлен `RunSummary`
- src/pipeline.ts — новый файл с `runPipeline()`, `loadChannelsYaml()`, типами `ChannelEntry` / `ChannelsFile`
- src/run.ts НЕ трогаем в этом плане (останется старым; daemon-rewrite в plan 04)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-daemon-50/02-RESEARCH.md
@docs/phase-2.md
@CLAUDE.md
@src/run.ts
@src/telegram.ts
@src/types.ts
@src/summarize.ts
@src/deliver.ts

<interfaces>
<!-- Ключевые подписи, которые pipeline использует без изменений. -->

From src/telegram.ts:
```typescript
export function sleep(ms: number): Promise<void>;
export function randomInt(min: number, max: number): number;
export function createClient(): TelegramClient;
export function fetchLast24h(
  client: TelegramClient,
  username: string,
  opts: { limit: number; windowHours: number }
): Promise<Post[]>;
```

From src/summarize.ts:
```typescript
export async function summarize(posts: Post[]): Promise<string>;
```

From src/deliver.ts:
```typescript
export async function sendToChannel(html: string): Promise<void>;
```

From src/logger.ts (создаётся параллельно в plan 02 в той же wave 1):
```typescript
export const log: {
  info(msg: string, ...ctx: unknown[]): void;
  warn(msg: string, ...ctx: unknown[]): void;
  error(msg: string, ...ctx: unknown[]): void;
};
```

**Примечание по wave 1 параллелизму:** pipeline.ts (plan 01) и logger.ts (plan 02) оба — wave 1, то есть исполняются параллельно. На момент старта plan 01 файла `src/logger.ts` может ещё не быть; это нормально — TypeScript ESM-импорт `from "./logger.js"` компилируется позже, чем пишется файл. Обе wave-1 задачи **должны завершиться до wave 2**; финальный `npx tsc --noEmit` после завершения обеих wave 1 плано́в должен пройти. Если executor plan 01 запускается строго изолированно (wave 2 ещё не началась, но plan 02 завершён), импорт `log` уже разрешается.

From src/types.ts (текущее состояние — добавляем RunSummary):
```typescript
export interface Post {
  channelUsername: string;
  messageId: number;
  postedAt: string;
  text: string;
  url: string;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Добавить RunSummary в src/types.ts</name>
  <files>src/types.ts</files>
  <read_first>
    - src/types.ts (текущий файл — добавляем ниже существующих интерфейсов)
    - docs/phase-2.md §4 (канонический шаблон RunSummary)
    - .planning/phases/02-daemon-50/02-RESEARCH.md §7 (LOG-02 спецификация)
  </read_first>
  <behavior>
    - Интерфейс RunSummary экспортируется из src/types.ts
    - Все 11 полей присутствуют с корректными типами
    - `errors: string[]` — массив строк формата `${username}: ${message}`
    - `digestDelivered: boolean` — true только если sendToChannel отработал без throw
  </behavior>
  <action>
В конец src/types.ts (после существующих интерфейсов Post/DigestItem/DigestSection/DigestJson) добавить ВЕРБАТИМ следующий блок:

```typescript

export interface RunSummary {
  runId: string; // crypto.randomUUID().slice(0, 8)
  startedAt: string; // ISO 8601
  finishedAt: string; // ISO 8601
  durationMs: number;
  channelsTotal: number;
  channelsSucceeded: number; // вернули >=0 постов без throw
  channelsSkipped: number; // throw после исчерпания retry
  postsCollected: number; // уникальных постов после дедупа
  postsDeduped: number; // отброшено дублей по ${username}:${messageId}
  digestDelivered: boolean; // true если sendToChannel отработал
  errors: string[]; // `${username}: ${err.message}`
}
```

Больше ничего не менять — существующие Post/DigestItem/DigestSection/DigestJson не трогать.
  </action>
  <verify>
    <automated>grep -q "export interface RunSummary" src/types.ts && grep -qE "runId:\s*string" src/types.ts && grep -qE "channelsTotal:\s*number" src/types.ts && grep -qE "postsDeduped:\s*number" src/types.ts && grep -qE "errors:\s*string\[\]" src/types.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "export interface RunSummary" src/types.ts` exits 0
    - В интерфейсе присутствуют ровно 11 полей: runId, startedAt, finishedAt, durationMs, channelsTotal, channelsSucceeded, channelsSkipped, postsCollected, postsDeduped, digestDelivered, errors
    - `grep -c "^  " src/types.ts | grep -v "^0$"` показывает ≥11 строк с отступом в 2 пробела внутри нового интерфейса (проверка что все поля на месте)
    - Существующие интерфейсы Post, DigestItem, DigestSection, DigestJson не изменены (их сигнатуры остались прежними)
    - `npx tsc --noEmit` exits 0 (нет type errors)
  </acceptance_criteria>
  <done>src/types.ts содержит экспорт RunSummary с 11 полями; tsc проходит без ошибок; старые интерфейсы не изменены.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Создать src/pipeline.ts с runPipeline() + in-memory дедупа</name>
  <files>src/pipeline.ts</files>
  <read_first>
    - src/run.ts (текущий main() — код которого переезжает в runPipeline)
    - src/telegram.ts (подписи createClient/fetchLast24h/sleep/randomInt)
    - src/types.ts (Post + только что добавленный RunSummary)
    - src/summarize.ts (summarize подпись)
    - src/deliver.ts (sendToChannel подпись)
    - docs/phase-2.md §1 (Выделить пайплайн из entrypoint — перечень изменений)
    - .planning/phases/02-daemon-50/02-RESEARCH.md §4 (Примерная сигнатура runPipeline — референс-реализация)
  </read_first>
  <behavior>
    - runPipeline() возвращает Promise<RunSummary> со всеми 11 заполненными полями
    - GramJS-клиент создаётся внутри функции, disconnect в finally перед этапом LLM/delivery (как в v1.0)
    - Каждый канал обёрнут в per-channel try/catch: исключение → channelsSkipped++ и errors.push, прогон продолжается
    - Дедупа: Set<string> с ключом `${post.channelUsername}:${post.messageId}`, повтор → postsDeduped++
    - Если allPosts.length === 0 → digestDelivered = false, summarize/sendToChannel НЕ вызываются
    - runId = crypto.randomUUID().slice(0, 8) (без импорта — crypto глобален в Node 20)
    - Дефолт CHANNEL_DELAY_MS = 1750 (SCALE-02; plan 05 также фиксирует в .env.example)
    - process.exit() НЕ вызывается нигде в pipeline.ts
    - **LOG-01: все информационные и warning-логи в pipeline.ts идут через `log.info()` / `log.warn()` из src/logger.ts, НЕ через `console.log` / `console.warn` напрямую. Это обеспечивает единый префикс `[ISO-timestamp] [level]` по всему проекту.**
  </behavior>
  <action>
Создать новый файл src/pipeline.ts со следующим содержимым ВЕРБАТИМ (без изменений). Импорты используют расширение .js (ESM + TypeScript bundler resolution):

```typescript
// src/pipeline.ts — логика одного прогона tg-parser-demo.
// Вызывается из daemon-tick в src/run.ts; сама не знает про node-cron/PM2.

import { readFileSync } from "node:fs";
import yaml from "yaml";
import type { Post, RunSummary } from "./types.js";
import { createClient, fetchLast24h, sleep, randomInt } from "./telegram.js";
import { summarize } from "./summarize.js";
import { sendToChannel } from "./deliver.js";
import { log } from "./logger.js";

interface ChannelEntry {
  username: string;
  priority?: number;
}

interface ChannelsFile {
  channels: ChannelEntry[];
}

function loadChannelsYaml(path: string): ChannelEntry[] {
  const raw = readFileSync(path, "utf8");
  const parsed = yaml.parse(raw) as ChannelsFile | null;
  if (!parsed || !Array.isArray(parsed.channels)) {
    throw new Error(`${path}: корневой ключ "channels" отсутствует или не массив`);
  }
  const channels: ChannelEntry[] = [];
  for (const c of parsed.channels) {
    if (!c || typeof c.username !== "string" || !c.username) {
      throw new Error(`${path}: запись без строкового поля username: ${JSON.stringify(c)}`);
    }
    channels.push({ username: c.username, priority: c.priority });
  }
  if (channels.length < 1) {
    throw new Error(`${path}: список channels пуст`);
  }
  return channels;
}

/**
 * Один прогон пайплайна: читает channels.yaml, обходит все каналы (per-channel try/catch),
 * дедуплицирует посты в рамках прогона, отдаёт батч DeepSeek'у, шлёт HTML в канал, возвращает RunSummary.
 * Не вызывает process.exit — ошибки пробрасывает вызывающему (daemon в src/run.ts).
 */
export async function runPipeline(): Promise<RunSummary> {
  const runId = crypto.randomUUID().slice(0, 8);
  const startedAt = new Date().toISOString();
  const startMs = Date.now();

  const channels = loadChannelsYaml("./channels.yaml");
  const limit = Number(process.env.MAX_MESSAGES_PER_CHANNEL ?? 50);
  const windowHours = Number(process.env.FETCH_WINDOW_HOURS ?? 24);
  // SCALE-02: дефолт 1750 мс согласован с .env.example.
  const channelDelayMs = Number(process.env.CHANNEL_DELAY_MS ?? 1750);

  log.info(`[pipeline] runId=${runId} channels=${channels.length}`);

  const client = createClient();
  await client.connect();

  const seen = new Set<string>();
  const allPosts: Post[] = [];
  let channelsSucceeded = 0;
  let channelsSkipped = 0;
  let postsDeduped = 0;
  const errors: string[] = [];

  try {
    for (let i = 0; i < channels.length; i++) {
      const { username } = channels[i]!;
      try {
        const fetched = await fetchLast24h(client, username, { limit, windowHours });
        for (const post of fetched) {
          const key = `${post.channelUsername}:${post.messageId}`;
          if (seen.has(key)) {
            postsDeduped++;
            continue;
          }
          seen.add(key);
          allPosts.push(post);
        }
        channelsSucceeded++;
        log.info(`[pipeline] ${username}: ${fetched.length} постов`);
      } catch (err) {
        channelsSkipped++;
        const msg = (err as Error)?.message ?? String(err);
        errors.push(`${username}: ${msg}`);
        log.warn(`[pipeline] channel skipped: ${username} — ${msg}`);
      }
      if (i < channels.length - 1) {
        await sleep(channelDelayMs + randomInt(0, 500));
      }
    }
  } finally {
    await client.disconnect();
  }

  log.info(
    `[pipeline] собрано уникальных: ${allPosts.length}, дублей отброшено: ${postsDeduped}`
  );

  let digestDelivered = false;
  if (allPosts.length > 0) {
    const html = await summarize(allPosts);
    await sendToChannel(html);
    digestDelivered = true;
    log.info(`[pipeline] дайджест отправлен.`);
  } else {
    log.info("[pipeline] No posts in window — skipping digest");
  }

  const finishedAt = new Date().toISOString();
  return {
    runId,
    startedAt,
    finishedAt,
    durationMs: Date.now() - startMs,
    channelsTotal: channels.length,
    channelsSucceeded,
    channelsSkipped,
    postsCollected: allPosts.length,
    postsDeduped,
    digestDelivered,
    errors,
  };
}
```

Важные моменты:
- НЕ использовать `process.exit(...)` нигде в этом файле. Ни одного вызова.
- НЕ использовать `console.log`/`console.warn`/`console.error` в этом файле — только `log.info`/`log.warn`/`log.error` из `./logger.js` (LOG-01: единый формат префикса `[ISO-timestamp] [level]`).
- **Про wave 1 параллелизм**: logger.ts создаётся параллельно в plan 02 (wave 1). TypeScript импорт с `.js` расширением для ESM — файла `src/logger.ts` может ещё не быть на момент старта plan 01, это нормально. Оба плана wave 1 завершаются перед wave 2; tsc-проверка на выходе из plan 01 либо ждёт завершения plan 02, либо (если plan 02 уже завершён) сразу пройдёт. При изолированном исполнении plan 01 без plan 02 `npx tsc --noEmit` упадёт с `Cannot find module './logger.js'` — это ожидаемое поведение, означающее что нужно сначала завершить wave 1 целиком.
- НЕ добавлять глобальный `main().catch(...)` — файл только экспортирует функцию.
- `crypto.randomUUID()` глобален в Node 20 — НЕ импортировать из `node:crypto`.
- Импорт `yaml` как default: `import yaml from "yaml"` (package "yaml" экспортирует default).
- `yaml` пакет поддерживается TS-типами — дополнительный devDep не нужен.

Запустить `npx tsc --noEmit` для проверки (после завершения wave 1 целиком, т.е. когда src/logger.ts уже создан plan 02).
  </action>
  <verify>
    <automated>test -f src/pipeline.ts && grep -q "export async function runPipeline" src/pipeline.ts && grep -q "Promise<RunSummary>" src/pipeline.ts && ! grep -q "process\.exit" src/pipeline.ts && grep -q "new Set<string>" src/pipeline.ts && grep -q '\${post.channelUsername}:\${post.messageId}' src/pipeline.ts && grep -qE "CHANNEL_DELAY_MS \?\? 1750" src/pipeline.ts && grep -q "crypto\.randomUUID" src/pipeline.ts && grep -q 'import { log } from "./logger.js"' src/pipeline.ts && ! grep -qE "console\.(log|warn|error)" src/pipeline.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Файл src/pipeline.ts создан
    - `grep -q "export async function runPipeline" src/pipeline.ts` exits 0
    - `grep -q "Promise<RunSummary>" src/pipeline.ts` exits 0
    - `! grep -q "process\.exit" src/pipeline.ts` exits 0 (ни одного `process.exit()` в файле)
    - `grep -q "new Set<string>" src/pipeline.ts` exits 0 (дедупа реализована через Set)
    - `grep -q '${post.channelUsername}:${post.messageId}' src/pipeline.ts` exits 0 (ключ дедупы — literal template string)
    - `grep -qE "CHANNEL_DELAY_MS \?\? 1750" src/pipeline.ts` exits 0 (SCALE-02 дефолт)
    - `grep -q "crypto\.randomUUID" src/pipeline.ts` exits 0 (runId)
    - `grep -q "await client\.disconnect" src/pipeline.ts` exits 0 (disconnect в finally)
    - `grep -c "try {" src/pipeline.ts | awk '$1 >= 2 { exit 0 } { exit 1 }'` exits 0 (минимум 2 try-блока: outer для disconnect и per-channel)
    - **LOG-01 key_link:** `grep -q 'import { log } from "./logger.js"' src/pipeline.ts` exits 0 (импорт логгера)
    - **LOG-01 negative:** `! grep -qE "console\.(log|warn|error)" src/pipeline.ts` exits 0 — НЕТ ни одного прямого вызова `console.log`/`console.warn`/`console.error`; все логи идут через `log.*`.
    - `grep -c "log\.(info|warn|error)" src/pipeline.ts` >= 5 — минимум 5 вызовов log (runId+channels, per-channel успех, per-channel skip, summary-собрано, digest-отправлен/skipping).
    - `npx tsc --noEmit` exits 0 (нет type errors — подписи RunSummary совпадают; после завершения wave 1 целиком src/logger.ts существует)
    - src/run.ts НЕ изменён в этом плане (diff против HEAD: `git diff --quiet src/run.ts`)
  </acceptance_criteria>
  <done>src/pipeline.ts компилируется; все 11 полей RunSummary заполнены в return; per-channel try/catch реализован; дедупа работает; process.exit отсутствует; все логи идут через log.* (LOG-01); src/run.ts не тронут.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` выводит 0 ошибок (после завершения wave 1 целиком, т.е. plan 02 также завершён и src/logger.ts существует).
2. `git diff --stat src/run.ts` показывает 0 изменений (run.ts трогаем в plan 04, не здесь).
3. `grep "export" src/pipeline.ts` показывает единственный экспорт `runPipeline`.
4. **LOG-01 финальная проверка:** `grep -q 'import { log } from "./logger.js"' src/pipeline.ts` exits 0 AND `! grep -qE "console\.(log|warn|error)" src/pipeline.ts` exits 0.
5. Ручная проверка: открыть src/pipeline.ts, убедиться что вся цепочка (loadChannelsYaml → createClient → loop → summarize → sendToChannel) присутствует.
</verification>

<success_criteria>
- src/types.ts экспортирует RunSummary с 11 полями (LOG-02)
- src/pipeline.ts экспортирует runPipeline(): Promise<RunSummary> (PIPE-01)
- GramJS-клиент создаётся per-run, disconnect в finally (PIPE-02)
- In-memory дедупа по `${username}:${messageId}` (PIPE-03)
- process.exit удалён из кода пайплайна (PIPE-01)
- **pipeline.ts использует log.info/log.warn из logger.ts — единый формат логов (LOG-01)**
- npx tsc --noEmit проходит (после завершения обоих wave-1 планов)
</success_criteria>

<output>
После выполнения создать `.planning/phases/02-daemon-50/02-01-SUMMARY.md` со списком изменённых/созданных файлов и ключевых решений.
</output>
