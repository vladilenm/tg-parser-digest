{
  "name": "tg-parser-demo",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20.6.0"
  },
  "scripts": {
    "login": "node --env-file=.env --import tsx scripts/login.ts",
    "start": "node --env-file=.env --import tsx src/run.ts"
  },
  "dependencies": {
    "openai": "^4.0.0",
    "telegram": "^2.22.0",
    "yaml": "^2.5.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "tsx": "^4.0.0",
    "typescript": "^5.4.0"
  }
}
